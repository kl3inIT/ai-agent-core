<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="1" author="ai-agent">
        <createTable tableName="AI_EXPOSURE_RULE">
            <column name="ID" type="${uuid.type}">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="VERSION" type="int" defaultValueNumeric="1">
                <constraints nullable="false"/>
            </column>
            <column name="ENTITY_NAME" type="varchar(255)">
                <constraints nullable="false"/>
            </column>
            <column name="MODE" type="varchar(16)" defaultValue="EXCLUDE">
                <constraints nullable="false"/>
            </column>
            <column name="ENABLED" type="boolean" defaultValueBoolean="true">
                <constraints nullable="false"/>
            </column>
            <column name="CREATED_BY" type="varchar(255)"/>
            <column name="CREATED_DATE" type="datetime"/>
            <column name="LAST_MODIFIED_BY" type="varchar(255)"/>
            <column name="LAST_MODIFIED_DATE" type="datetime"/>
        </createTable>
    </changeSet>

    <changeSet id="2" author="ai-agent">
        <!--
            BLOCKER-02: index name MUST match the JPA @Index(name="IDX_AI_EXPOSURE_RULE_ENTITY_NAME",
            unique=true) declaration on AiExposureRule. Mismatched names caused JPA schema
            validators (hibernate.hbm2ddl.auto / Jmix validator) to attempt a duplicate index
            creation and broke DDL parity across environments. Liquibase remains the single
            source of truth for the constraint shape; the unique=true index is sufficient —
            no separate addUniqueConstraint is needed.
        -->
        <createIndex indexName="IDX_AI_EXPOSURE_RULE_ENTITY_NAME"
                     tableName="AI_EXPOSURE_RULE"
                     unique="true">
            <column name="ENTITY_NAME"/>
        </createIndex>
        <createIndex indexName="IDX_AI_EXPOSURE_RULE_ENABLED"
                     tableName="AI_EXPOSURE_RULE">
            <column name="ENABLED"/>
        </createIndex>
    </changeSet>

</databaseChangeLog>
