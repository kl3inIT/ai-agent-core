<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd"
        objectQuotingStrategy="QUOTE_ONLY_RESERVED_WORDS">

    <property name="offsetDateTime.type" dbms="postgresql" value="timestamp with time zone"/>
    <property name="uuid.type" value="uuid" dbms="postgresql"/>
    <property name="uuid.type" value="varchar(36)" dbms="hsqldb"/>
    <property name="uuid.type" value="uniqueidentifier" dbms="mssql"/>
    <property name="offsetDateTime.type" value="timestamp" dbms="hsqldb"/>
    <property name="offsetDateTime.type" value="datetimeoffset" dbms="mssql"/>

    <!--
        Plan 17-01 Task 3 — test-only agentstore changelog. Includes the PRODUCTION agentstore
        changelog (so all real ai_* tables still exist) PLUS the agentstore-backed FK fixture
        tables. Selected per-test via @SpringBootTest(properties="agentstore.liquibase.change-log=...")
        so the global test-app.properties agentstore wiring is untouched for every other test.
    -->
    <include file="/com/vn/agent/liquibase/agentstore-changelog.xml"/>
    <include file="/com/vn/agent/test_liquibase/020-mutation-store-fixture.xml"/>

</databaseChangeLog>
