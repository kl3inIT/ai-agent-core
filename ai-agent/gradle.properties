org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8

# Plan 08-07: published artifact version. CI overrides via -Pversion=...
version=1.0.0

# Nexus credentials are no longer committed (Plan 08-07 / R-07a). Set via
# CI secrets (-PnexusUsername / -PnexusPassword) or your user-global
# ~/.gradle/gradle.properties (untracked).
#
# IMPORTANT: the historical credential `admin / admin123` is FOREVER BURNT
# in git history (commit prior to plan 08-07). Rotate it in the Nexus admin
# UI; do NOT just remove these lines and assume safety.