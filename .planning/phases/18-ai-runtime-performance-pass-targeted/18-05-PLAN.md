---
phase: 18-ai-runtime-performance-pass-targeted
plan: 05
type: execute
wave: 3
depends_on: ["18-01", "18-02", "18-03", "18-04"]
files_modified:
  - ai-agent/ai-agent/src/test/java/com/vn/agent/performance/NoNewPerfDependencyInvariantTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/exposure/SettingsEditVisibleNextTurnTest.java
autonomous: true
requirements: [PERF-05]
must_haves:
  truths:
    - "The build adds no JMH, Gatling, or Caffeine dependency (a source/dep-scan invariant fails on any such token)"
    - "An admin edit published via LlmExposureChangedEvent / AiSettingsChangedEvent is reflected on the next turn (cache eviction within one turn)"
    - "Each of PERF-01..04 ships at least one checkable proxy (verified to exist)"
    - "The full existing security/exposure/audit/tool/RAG suite passes with zero test-body edits except ToolQueryCountBaselineTest.java:151"
  artifacts:
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/performance/NoNewPerfDependencyInvariantTest.java"
      provides: "dep-scan invariant: no jmh/gatling/caffeine token in the build scripts; asserts PERF-01..04 proxy test classes exist"
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/exposure/SettingsEditVisibleNextTurnTest.java"
      provides: "eviction-visibility test: an LlmExposureChangedEvent / AiSettingsChangedEvent makes the change visible on the next turn"
  key_links:
    - from: "LlmExposureChangedEvent / AiSettingsChangedEvent published"
      to: "next-turn resolve reflects the change"
      via: "@EventListener .clear() in LlmExposurePolicy (+ AiUiSettingsResolver if Plan 04 added one)"
      pattern: "LlmExposureChangedEvent|AiSettingsChangedEvent"
---

<objective>
Close PERF-05 — the cross-cutting "proxies exist, nothing out-of-scope introduced, suites unchanged,
admin edit visible within one turn" gate:

1. A build-dependency invariant test asserting no `jmh`, `gatling`, or `caffeine` token appears in the
   module/root Gradle build scripts (no new benchmark or cache dependency) AND that each PERF-01..04
   proxy test class exists (the per-optimization-proxy requirement is structurally enforced).
2. An eviction-visibility test proving an admin change published via `LlmExposureChangedEvent` (denylist)
   — and `AiSettingsChangedEvent(UI_SETTINGS)` if Plan 04 added a settings memo — is reflected on the
   NEXT turn (the cache evicts within one turn; the admin-edit-visibility window is closed).
3. The full default suite passes with the ONLY test-body edit being `ToolQueryCountBaselineTest.java:151`.

Purpose: Make the phase's "no out-of-scope perf/cache dependency", "every optimization has a proxy",
and "admin edit visible next turn" guarantees machine-checkable, not argued.
Output: Two new pure-JUnit tests; the full-suite green gate (zero unauthorized test-body edits).

Depends on all of Plans 18-01..04: the dep-scan asserts their proxy classes exist; the eviction-
visibility test exercises the Plan 01 `@EventListener(LlmExposureChangedEvent)` (and any Plan 04
`@EventListener(AiSettingsChangedEvent)`). Runs last (Wave 3), after the implementations land.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/18-ai-runtime-performance-pass-targeted/18-SPEC.md
@.planning/phases/18-ai-runtime-performance-pass-targeted/18-CONTEXT.md
@.planning/phases/18-ai-runtime-performance-pass-targeted/18-RESEARCH.md
@.planning/phases/18-ai-runtime-performance-pass-targeted/18-PATTERNS.md

@ai-agent/ai-agent/src/main/java/com/vn/agent/exposure/LlmExposurePolicy.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/exposure/LlmExposureChangedEvent.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/AiSettingsChangedEvent.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build-dependency invariant — no jmh/gatling/caffeine, and PERF-01..04 proxies exist</name>
  <files>ai-agent/ai-agent/src/test/java/com/vn/agent/performance/NoNewPerfDependencyInvariantTest.java</files>
  <read_first>
    - ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/AiSettingsChangedEventListenerInvariantTest.java (ANALOG — `singlePublishSiteSourceScan` :228-260 file-walk + regex; reuse `findMainJavaRoot()` :322-343, and adapt the walk to scan `.gradle` build scripts)
    - ai-agent/ai-agent/ai-agent.gradle (the module build script — assert no `caffeine`/`jmh`/`gatling` token; `net.ttddyy:datasource-proxy:1.11.0` is the ALLOWED proxy harness at :111)
    - ai-agent/build.gradle (root add-on build script — same dep-scan)
  </read_first>
  <action>
    Create `performance/NoNewPerfDependencyInvariantTest.java` (`@Tag("unit")`, pure JUnit 5). Locate the
    Gradle build scripts (`ai-agent/ai-agent/ai-agent.gradle` and `ai-agent/build.gradle`) relative to a
    resolved module root (reuse a `findMainJavaRoot()`-style walk-up, then navigate to the build scripts;
    or resolve via `System.getProperty("user.dir")` + known relative paths and fall back to a walk-up).
    Read each script's text and assert (case-insensitive) it contains NO `caffeine`, no `jmh`, and no
    `gatling` token. Then assert each PERF-01..04 proxy test class FILE exists on disk by walking the
    test source root: `LlmExposureDenylistMemoTest` + `ExposureCacheEventSubscriptionInvariantTest`
    (PERF-02, Plan 01), `LlmExposurePerTurnMemoTest` + `PerTurnCacheBoundaryInvariantTest` (PERF-01,
    Plan 02), `RetrievalFilterBuilderBuildOncePerRetrievalTest` (PERF-03, Plan 03),
    `TaskFileMediaEncodeOncePerTurnTest` (PERF-04, Plan 04). The presence of these files structurally
    enforces "each optimization ships >=1 checkable proxy". Keep the assertions resilient to path
    separators (Windows + POSIX) via `Path` APIs, not string concatenation.
  </action>
  <verify>
    <automated>cd ai-agent && ./gradlew :ai-agent:test --tests "com.vn.agent.performance.NoNewPerfDependencyInvariantTest" --console=plain</automated>
  </verify>
  <acceptance_criteria>
    - `NoNewPerfDependencyInvariantTest` asserts the build scripts contain no `caffeine`/`jmh`/`gatling` token (case-insensitive)
    - The test asserts all six PERF-01..04 proxy test class files exist on disk
    - The test uses `java.nio.file.Path` APIs (no hardcoded `/` or `\` string concatenation for paths)
    - `cd ai-agent && ./gradlew :ai-agent:test --tests "com.vn.agent.performance.NoNewPerfDependencyInvariantTest"` exits 0
  </acceptance_criteria>
  <done>
    No benchmark/Caffeine dependency exists, and every PERF-01..04 proxy is structurally present.
  </done>
</task>

<task type="auto">
  <name>Task 2: Admin edit visible within one turn (event-eviction visibility) + full-suite gate</name>
  <files>ai-agent/ai-agent/src/test/java/com/vn/agent/exposure/SettingsEditVisibleNextTurnTest.java</files>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/exposure/LlmExposurePolicy.java (post-Plan-01/02 — the denylist memo + `@EventListener(LlmExposureChangedEvent)` eviction; the per-turn cache)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/exposure/LlmExposureChangedEvent.java (the event published on any AiExposureRule change)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/AiSettingsChangedEvent.java (Kind {PARAMETERS, UI_SETTINGS}; the settings-cache evictor if Plan 04 added one)
    - ai-agent/ai-agent/src/test/java/com/vn/agent/exposure/LlmExposureDenylistMemoTest.java (Plan 01 — the call-count eviction pattern this test extends toward an observable "value changed" assertion)
    - .planning/phases/18-ai-runtime-performance-pass-targeted/18-04-SUMMARY.md (read the recorded Plan 04 branch — only assert AiSettingsChangedEvent visibility if Plan 04 took the cache/settings-memo branch)
  </read_first>
  <action>
    Create `exposure/SettingsEditVisibleNextTurnTest.java` (`@Tag("unit")`, pure JUnit 5 + Mockito, NO
    `@SpringBootTest`). Build a real `LlmExposurePolicy` with a Mockito `mock` `LlmExposureRuleRepository`.
    Stub `findEnabledExcludedEntityNames()` to return set A; call `getDenylistedEntityNames()` and assert
    the result CONTAINS A's entities (turn 1). Re-stub the mock to return a DIFFERENT set B (simulating an
    admin denylist edit). Without eviction, assert the policy still returns A (the memo is in effect).
    Then publish/deliver the eviction by calling `policy.onExposureChanged(new LlmExposureChangedEvent(this))`;
    call `getDenylistedEntityNames()` again and assert it now reflects B — the admin edit is visible on the
    next turn (denylist eviction within one turn). If (and ONLY if) the Plan 04 SUMMARY recorded that a
    settings memo with `@EventListener(AiSettingsChangedEvent)` was added, add a parallel sub-test that
    changes the stubbed `AiUiSettings` value, fires `AiSettingsChangedEvent(UI_SETTINGS)`, and asserts the
    next `resolveTaskFile*` read reflects the new value; otherwise skip that leg (the settings path runs
    fresh each turn, so it is visible by construction — note this in the test Javadoc).
    Then run the FULL default suite to confirm zero security/exposure/audit/tool/RAG regressions and that
    the only test-body edit in the phase is `ToolQueryCountBaselineTest.java:151`.
  </action>
  <verify>
    <automated>cd ai-agent && ./gradlew :ai-agent:test --tests "com.vn.agent.exposure.SettingsEditVisibleNextTurnTest" --console=plain</automated>
  </verify>
  <acceptance_criteria>
    - `SettingsEditVisibleNextTurnTest` asserts the denylist returns the OLD value before eviction and the NEW value after `onExposureChanged(...)` (admin edit visible next turn)
    - The `AiSettingsChangedEvent` leg is present only if Plan 04's SUMMARY recorded a settings memo; otherwise its absence is documented in the test Javadoc
    - The full default suite is green: `cd ai-agent && ./gradlew :ai-agent:test` exits 0
    - `git diff` across the phase shows exactly ONE existing-test-body edit: `ToolQueryCountBaselineTest.java:151` (all other changed test files are new files)
    - `cd ai-agent && ./gradlew :ai-agent:test --tests "com.vn.agent.exposure.SettingsEditVisibleNextTurnTest"` exits 0
  </acceptance_criteria>
  <done>
    An admin edit published via the eviction event is reflected on the next turn; the full suite passes
    with only the line-151 ceiling recalibration as an existing-test-body change.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin edit → next-turn LLM surface | An admin's denylist/settings change must close the visibility window within one turn; a non-evicting cache would silently keep a denied entity visible. |
| out-of-scope dependency creep | Adding Caffeine/JMH/Gatling would change the runtime/security posture and violate the locked boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-14 | Tampering | A cache that never evicts after an admin edit (stale-permission reuse across turns) | mitigate | `SettingsEditVisibleNextTurnTest` proves the denylist returns the NEW value after `LlmExposureChangedEvent` (and `AiSettingsChangedEvent` where a settings memo exists) — admin edit visible next turn. |
| T-18-15 | Tampering / supply chain | Introducing a new cache/benchmark dependency (Caffeine/JMH/Gatling) | mitigate | `NoNewPerfDependencyInvariantTest` fails the build on any such token in the Gradle scripts; the only allowed cache primitives are `ConcurrentHashMap`/`ConcurrentMapCacheManager`. |
| T-18-16 | Tampering | Silently editing an existing security/opacity test body to make the suite pass | mitigate | The phase gate asserts the ONLY existing-test-body edit is `ToolQueryCountBaselineTest.java:151`; all other proxy tests are new files; the full suite green confirms no regression. |
| T-18-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero packages installed; this plan's dep-scan is itself the supply-chain guard. |
</threat_model>

<verification>
- `cd ai-agent && ./gradlew :ai-agent:test --tests "com.vn.agent.performance.NoNewPerfDependencyInvariantTest" --tests "com.vn.agent.exposure.SettingsEditVisibleNextTurnTest"` exits 0.
- `cd ai-agent && ./gradlew :ai-agent:test` (full default suite) exits 0 — phase gate.
- `git diff` across the phase confirms `ToolQueryCountBaselineTest.java:151` is the only existing-test-body edit.
</verification>

<success_criteria>
- No JMH/Gatling/Caffeine dependency; each PERF-01..04 proxy class exists (dep-scan invariant green).
- An admin edit via `LlmExposureChangedEvent` (and `AiSettingsChangedEvent` where applicable) is visible on the next turn.
- The full existing security/exposure/audit/tool/RAG suite passes with the only existing-test-body edit being line 151.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- `NoNewPerfDependencyInvariantTest` (new test class, `com.vn.agent.performance`) — dep-scan + proxy-existence invariant.
- `SettingsEditVisibleNextTurnTest` (new test class, `com.vn.agent.exposure`) — event-eviction visibility test.
</artifacts_produced>

<output>
Create `.planning/phases/18-ai-runtime-performance-pass-targeted/18-05-SUMMARY.md` when done.
</output>
