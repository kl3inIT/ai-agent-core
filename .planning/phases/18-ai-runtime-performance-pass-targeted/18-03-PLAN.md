---
phase: 18-ai-runtime-performance-pass-targeted
plan: 03
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - ai-agent/ai-agent/src/main/java/com/vn/agent/rag/RetrievalFilterBuilder.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/rag/RetrievalFilterBuilderBuildOncePerRetrievalTest.java
autonomous: true
requirements: [PERF-03]
must_haves:
  truths:
    - "The RAG Filter.Expression is built once per retrieval (the denylist lookup is invoked exactly once per buildFor call), reusing the PERF-02 cached denylist"
    - "The (source_entity IS NULL) OR (NOT IN <denied>) / role clauses are preserved verbatim — no redundant-clause removal"
    - "RetrievalFilterBuilderDenylistTest passes unchanged (the verbatim-clause guard)"
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/rag/RetrievalFilterBuilder.java"
      provides: "buildFor builds the filter once per retrieval; denylist reused from the PERF-02 cache; NIN clause verbatim"
      contains: "getDenylistedEntityNames"
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/rag/RetrievalFilterBuilderBuildOncePerRetrievalTest.java"
      provides: "call-count times(1) per retrieval on the denylist lookup / build path"
  key_links:
    - from: "rag/RetrievalFilterBuilder.buildFor"
      to: "exposure/LlmExposurePolicy.getDenylistedEntityNames()"
      via: "single call per retrieval, hitting the PERF-02 app-wide denylistCache"
      pattern: "getDenylistedEntityNames"
---

<objective>
Confirm and lock that the RAG `Filter.Expression` is built once per retrieval (PERF-03): the denylist
lookup inside `RetrievalFilterBuilder.buildFor(Authentication)` now hits the Plan 01 app-wide
`denylistCache` (so it is a memoized read, not a per-call agentstore SELECT), and the filter is
constructed exactly once per retrieval call. The `(source_entity IS NULL) OR (NOT IN <denied>)` and
per-role clauses are preserved VERBATIM — no "redundant clause" removal. Role extraction stays
request-fresh.

Ship the PERF-03 proxy: a pure-JUnit call-count test asserting the denylist lookup / build path is
invoked exactly once per `buildFor` retrieval, and confirm the existing
`RetrievalFilterBuilderDenylistTest` (the verbatim-clause guard, 4 tests) passes UNCHANGED.

Purpose: Eliminate the per-call denylist SELECT in the RAG path by reusing the PERF-02 cache, without
touching the byte-for-byte filter-clause shape that the Phase 10 TEST-09 RAG leg depends on.
Output: A once-per-retrieval `RetrievalFilterBuilder` (denylist read memoized via Plan 01) + one new
call-count proxy test; the existing denylist test unchanged.

Depends on Plan 18-01: the denylist read in `buildFor` reuses the app-wide `denylistCache`. Touches a
different file (`RetrievalFilterBuilder.java`) than Plans 02 and 04 → runs parallel with them in Wave 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/18-ai-runtime-performance-pass-targeted/18-SPEC.md
@.planning/phases/18-ai-runtime-performance-pass-targeted/18-CONTEXT.md
@.planning/phases/18-ai-runtime-performance-pass-targeted/18-RESEARCH.md
@.planning/phases/18-ai-runtime-performance-pass-targeted/18-PATTERNS.md

@ai-agent/ai-agent/src/main/java/com/vn/agent/rag/RetrievalFilterBuilder.java
@ai-agent/ai-agent/src/test/java/com/vn/agent/rag/RetrievalFilterBuilderDenylistTest.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the RAG filter once per retrieval, reusing the PERF-02 denylist; preserve clauses verbatim</name>
  <files>ai-agent/ai-agent/src/main/java/com/vn/agent/rag/RetrievalFilterBuilder.java</files>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/rag/RetrievalFilterBuilder.java (the file being modified — `buildFor` :77; role extraction :78-84 stays request-fresh; denylist read `getDenylistedEntityNames()` :99; the verbatim NIN clause :101-103; the WARNING-11 model-pin duplication at :120-131 is INTENTIONAL — do NOT remove)
    - ai-agent/ai-agent/src/test/java/com/vn/agent/rag/RetrievalFilterBuilderDenylistTest.java (the must-pass verbatim-clause guard — 4 tests; assert unchanged)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/exposure/LlmExposurePolicy.java (post-Plan-01 — `getDenylistedEntityNames()` now hits the app-wide `denylistCache`)
  </read_first>
  <action>
    In `rag/RetrievalFilterBuilder.buildFor(Authentication)`, ensure the denylist is read exactly ONCE
    per retrieval via the single existing `llmExposurePolicy.getDenylistedEntityNames()` call (:99) —
    this now hits the Plan 01 app-wide `denylistCache` automatically (no code change to the call is
    required for the cache hit; the optimization is structural via Plan 01). Verify there is exactly ONE
    `getDenylistedEntityNames()` invocation in the method and that the `Filter.Expression` is assembled
    once per `buildFor` call (no loop re-invoking the build). Do NOT remove or merge any clause: the
    `(source_entity IS NULL) OR (NOT IN <denied>)` NIN clause (:101-103), the per-role
    `(model && roleA) || (model && roleB)` composition (:120-131), the admin-bypass branch (:108-112),
    and the fail-closed empty-role branch (:114-118) all stay byte-for-byte. Role extraction (:78-84)
    stays request-fresh (NOT cached — it is per-`Authentication`). If `buildFor` already reads the
    denylist exactly once and builds the filter once (it does, per the source), the production change is
    a no-op beyond a clarifying Javadoc line noting the denylist read now reuses the PERF-02 app-wide
    cache and the filter is built once per retrieval (PERF-03) — record that as a `[NOTE]` in the SUMMARY
    rather than forcing a structural edit. Do NOT introduce any new cache here; the per-retrieval
    once-ness is the contract, and the denylist memo lives in `LlmExposurePolicy` (Plan 01).
  </action>
  <verify>
    <automated>cd ai-agent && ./gradlew :ai-agent:test --tests "com.vn.agent.rag.RetrievalFilterBuilderDenylistTest" --console=plain</automated>
  </verify>
  <acceptance_criteria>
    - `RetrievalFilterBuilder.buildFor` contains exactly ONE `getDenylistedEntityNames()` call
    - The NIN clause `b.nin(ChunkMetadata.SOURCE_ENTITY, ...)` and the per-role OR composition are present and unchanged in shape
    - `RetrievalFilterBuilder.java` adds NO new `ConcurrentHashMap`/`computeIfAbsent`/ThreadLocal cache (the denylist memo lives in `LlmExposurePolicy`); grep for a new cache field == 0
    - `RetrievalFilterBuilderDenylistTest` (existing, 4 tests) passes UNCHANGED — `git diff` on that test file is empty
    - `cd ai-agent && ./gradlew :ai-agent:test --tests "com.vn.agent.rag.RetrievalFilterBuilderDenylistTest"` exits 0
  </acceptance_criteria>
  <done>
    The filter is built once per retrieval, the denylist read reuses the PERF-02 cache, and every clause
    is preserved verbatim; the existing denylist guard test is untouched and green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the PERF-03 once-per-retrieval call-count proxy</name>
  <files>ai-agent/ai-agent/src/test/java/com/vn/agent/rag/RetrievalFilterBuilderBuildOncePerRetrievalTest.java</files>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/rag/RetrievalFilterBuilder.java (unit under test — `buildFor`)
    - ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/RelatedWriteMetadataMemoTest.java (ANALOG — counting-seam / call-count template :56-76)
    - ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/AiUiSettingsResolverReadThroughTest.java (ANALOG — real-unit + Mockito-mocked edges, `@Tag("unit")`, no Spring boot)
    - ai-agent/ai-agent/src/test/java/com/vn/agent/rag/RetrievalFilterBuilderDenylistTest.java (the must-pass guard whose green state Task 2 must NOT disturb)
  </read_first>
  <action>
    Create `rag/RetrievalFilterBuilderBuildOncePerRetrievalTest.java` (`@Tag("unit")`, pure JUnit 5 +
    Mockito, NO `@SpringBootTest`). Construct a real `RetrievalFilterBuilder` with Mockito-mocked
    collaborators (`AiAgentRagProperties`, `AiAgentEmbeddingProperties`, and a Mockito `mock` or `spy`
    `LlmExposurePolicy`). Stub `llmExposurePolicy.getDenylistedEntityNames()` to return a fixed non-empty
    set. Invoke `buildFor(auth)` ONCE for a single retrieval with a role-bearing `Authentication`;
    `verify(llmExposurePolicy, times(1)).getDenylistedEntityNames()` — exactly one denylist lookup per
    retrieval. Assert the returned `Filter.Expression` (via its `toString()`) still contains the NIN
    `source_entity` clause and the role-flag clause (verbatim-clause smoke, complementary to the
    existing denylist test). Optionally drive a SECOND independent `buildFor(auth)` and assert it
    likewise issues exactly one lookup (per-retrieval once-ness, not a cross-retrieval cache claim —
    role extraction stays request-fresh). Do NOT edit `RetrievalFilterBuilderDenylistTest`.
  </action>
  <verify>
    <automated>cd ai-agent && ./gradlew :ai-agent:test --tests "com.vn.agent.rag.RetrievalFilterBuilderBuildOncePerRetrievalTest" --tests "com.vn.agent.rag.RetrievalFilterBuilderDenylistTest" --console=plain</automated>
  </verify>
  <acceptance_criteria>
    - `RetrievalFilterBuilderBuildOncePerRetrievalTest` asserts `verify(llmExposurePolicy, times(1)).getDenylistedEntityNames()` per `buildFor`
    - The test asserts the NIN `source_entity` clause + a role-flag clause are present in the built expression
    - `RetrievalFilterBuilderDenylistTest` `git diff` is empty (unchanged)
    - `cd ai-agent && ./gradlew :ai-agent:test --tests "com.vn.agent.rag.RetrievalFilterBuilderBuildOncePerRetrievalTest" --tests "com.vn.agent.rag.RetrievalFilterBuilderDenylistTest"` exits 0
  </acceptance_criteria>
  <done>
    A call-count proxy proves the denylist lookup / filter build happens once per retrieval; the existing
    denylist guard remains byte-for-byte and green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RAG retrieval filter → vector store query | The `Filter.Expression` is the role/exposure scoping for retrieved chunks; a wrong or stale denylist clause could surface chunks from a denied source entity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-08 | Information Disclosure | A "redundant clause" optimization dropping the `(source_entity IS NULL) OR (NOT IN <denied>)` or role clause | mitigate | Clauses preserved VERBATIM (D — no redundant-clause removal); `RetrievalFilterBuilderDenylistTest` (4 tests) passes unchanged as the byte-for-byte guard; the new proxy asserts the NIN + role clause are still emitted. |
| T-18-09 | Tampering | Stale denylist in the RAG filter after an admin edit | mitigate | The denylist read reuses the Plan 01 app-wide `denylistCache`, evicted on `LlmExposureChangedEvent` — admin edit visible on the next retrieval. |
| T-18-10 | Elevation of Privilege | Caching role extraction (cross-request) so a stale role set scopes a later user's retrieval | accept/mitigate | Role extraction stays request-fresh (NOT cached) — derived per `Authentication` on every `buildFor`; only the denylist portion is memoized. |
| T-18-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero packages installed. |
</threat_model>

<verification>
- `cd ai-agent && ./gradlew :ai-agent:test --tests "com.vn.agent.rag.RetrievalFilterBuilderBuildOncePerRetrievalTest" --tests "com.vn.agent.rag.RetrievalFilterBuilderDenylistTest"` exits 0.
- `git diff` on `RetrievalFilterBuilderDenylistTest.java` is empty.
- The Phase 10 TEST-09 RAG leg passes unchanged at the Wave-2 merge gate (full default suite).
</verification>

<success_criteria>
- The RAG `Filter.Expression` is built once per retrieval; the denylist read reuses the PERF-02 cache.
- All clauses preserved verbatim — no redundant-clause removal.
- `RetrievalFilterBuilderDenylistTest` passes unchanged; the new call-count proxy is green.
- No new cache in `RetrievalFilterBuilder`; role extraction stays request-fresh.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- `RetrievalFilterBuilder.buildFor` — clarifying Javadoc that the denylist read reuses the PERF-02 app-wide cache and the filter is built once per retrieval (no new cache field; structural once-ness preserved).
- `RetrievalFilterBuilderBuildOncePerRetrievalTest` (new test class, `com.vn.agent.rag`).
</artifacts_produced>

<output>
Create `.planning/phases/18-ai-runtime-performance-pass-targeted/18-03-SUMMARY.md` when done.
</output>
