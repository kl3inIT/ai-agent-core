---
phase: 15-right-sidebar-chat-surface-observability-ux
plan: 04
type: execute
wave: 3
depends_on: [15-02-PLAN.md, 15-03-PLAN.md]
files_modified:
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/TurnDetailRenderer.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java
  - ai-agent/ai-agent/src/main/resources/META-INF/resources/frontend/styles/ai-agent-chat.css
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties
  - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/fragment/TurnDetailRendererTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/fragment/ChatPanelFragmentStatusLineTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/fragment/ChatPanelFragmentTurnDetailTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/fragment/ChatPanelFragmentTurnDetailHistoryTest.java
autonomous: true
requirements: [OBS-01, OBS-02, OBS-04]

must_haves:
  truths:
    - "While a turn streams, an ephemeral status line (a <span role=status aria-live=polite> in messageListSlot, a sibling AFTER <vaadin-message-list>, never inside any MessageListItem) shows a KIND-keyed humanized label: CHAT->thinking…, TOOL->searching data…, RETRIEVAL->retrieving documents…, neutral typing indicator before any ActivityKind arrives"
    - "On the terminal Final/Error event the status line is REMOVED entirely (not blanked); the final assistant bubble contains only reply markdown — status text is never concatenated into the bubble"
    - "Each completed turn with >=1 tool call shows a collapsed-by-default Vaadin Details ('what the agent did — N steps · total ms') appended as a sibling after that turn's MessageListItem, expanding to label-only KIND-keyed step rows with per-step ms and an error/rollback indicator; a zero-tool-call turn shows NO Details at all; a step with no known duration renders an em-dash placeholder ('—'), never '0 ms'"
    - "Live turns populate the Details from a bounded per-fragment-instance step list (cleared on Final/Error — nothing accumulates in AiChatSessionState); after a conversation-switch reload (or a fresh navigation), the history-replay path correlates the conversation's AiAuditEvent CHAT roots (ordered by startedAt) against the replayed ASSISTANT turns and appends the same collapsed Details anchored by that runId — expanding lazily re-reads only that runId's AiAuditEvent TOOL/RETRIEVAL children with a narrow fetch plan, respecting row-level access, memoized so re-expand does not re-query"
    - "Both the live and historical paths funnel through one kind -> msg:// mapper (TurnDetailRenderer) that emits only KIND-keyed localized labels — never a tool method name, never a raw entity name, never argsJson/resultSummary/queryText"
    - "All new status/disclosure strings come from msg:// keys present in messages_en.properties AND messages_vi.properties"
    - "No new @Entity / @Table / Liquibase changelog; AiChatSessionState still holds only currentConversationId + listeners; the existing FULL_ROUTE/HEADER_BUTTON flows and the ChatPanelFragment slot ids are unchanged"
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/TurnDetailRenderer.java"
      provides: "Vaadin-free, Spring-free pure-fn mapper: ActivityKind/audit-kind String + timing + outcome -> label-only msg:// rows; statusKeyFor(ActivityKind)"
      contains: "TurnDetailRenderer"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java"
      provides: "Activity arm in doOnNext; status <span>; per-turn Details (live + history-correlated); bounded liveTurnSteps; on-expand AiAuditEvent re-query"
      contains: "ai-agent-status"
    - path: "ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties"
      provides: "status + turn-detail msg:// keys (en)"
      contains: "chatView.status"
    - path: "ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties"
      provides: "status + turn-detail msg:// keys (vi)"
      contains: "chatView.status"
  key_links:
    - from: "ChatPanelFragment.submitChatTurn(...).doOnNext"
      to: "StreamingEvent.Activity"
      via: "else if (evt instanceof StreamingEvent.Activity a) -> accessUi(() -> showStatus(messages.getMessage(TurnDetailRenderer.statusKeyFor(a.kind()))))"
      pattern: "StreamingEvent\\.Activity"
    - from: "ChatPanelFragment history-replay loop (the AiMessage replay region, ~line 563-595)"
      to: "AiAuditEvent (agentstore)"
      via: "after replaying AiMessages, load AiAuditEvent CHAT roots where e.conversation.id=:cid and e.parent is null order by e.startedAt asc; zip the ordered runId list against the replayed ASSISTANT items; appendHistoryTurnDetailsPlaceholder(runId) per turn"
      pattern: "AiAuditEvent"
    - from: "ChatPanelFragment per-turn Details (history turn) on first expand"
      to: "AiAuditEvent (agentstore) TOOL/RETRIEVAL children for one runId"
      via: "load(AiAuditEvent.class).query(... where e.runId=:rid and e.parent is not null ...).fetchPlan(narrow).list() on first expand, memoized; row-level-access-respecting (constrained DataManager if the chat role permits, else UnconstrainedDataManager with a mandatory where e.userUsername=:me and e.conversation.id=:cid)"
      pattern: "AiAuditEvent"
    - from: "status line + Details step rows"
      to: "TurnDetailRenderer"
      via: "label-only kind->msg:// mapping (no toolName/entity name input)"
      pattern: "TurnDetailRenderer"
---

<objective>
Render the two in-fragment observability surfaces inside the shared `ChatPanelFragment` per decisions D-06..D-08: (1) an ephemeral KIND-keyed streaming-status `<span>` in a sibling slot of the message list (the existing NOTICE-row pattern), keyed by the new `StreamingEvent.Activity` event, cleared entirely on terminal `Final`/`Error`; (2) a collapsed-by-default Vaadin `Details` per completed turn with ≥1 tool call ("what the agent did — N steps · total ms" → label-only KIND-keyed step rows with per-step ms + error/rollback indicator), hidden entirely on zero-tool turns, driven by a bounded per-fragment-instance step list for live turns AND — for prior turns after a conversation-switch reload or a fresh navigation — by an additive correlation pass over the conversation's `AiAuditEvent` CHAT roots (ordered by `startedAt`, zipped against the replayed ASSISTANT turns) plus a lazy on-expand `AiAuditEvent`-by-`runId` read (narrow fetch plan, row-level-access-respecting, memoized) for the TOOL/RETRIEVAL children. Add the `Activity` `instanceof` branch to `ChatPanelFragment.doOnNext`. Funnel both paths through one Vaadin-free `TurnDetailRenderer` mapper (`ActivityKind`/`kind` String + timing + outcome → `msg://` label rows; `statusKeyFor(ActivityKind)`) — structural no-leak. Add the `.ai-agent-status` rule + `@keyframes` pulse + `prefers-reduced-motion` guard to the existing `ai-agent-chat.css`. Add all new `msg://` keys to both locale bundles. Resolve RESEARCH Open Q1 (which `DataManager` for the on-expand read) by checking `AiAuditEventListView`'s read path + the `agentstore` roles — `AiAgentUserRole` has NO `AiAuditEvent` policy, so the chat user cannot read audit rows through the constrained `DataManager`; therefore the on-expand (and the correlation) read uses `UnconstrainedDataManager` with a MANDATORY `where e.userUsername = :currentUser and e.conversation.id = :conversationId` clause; never load `AiAuditEvent` by `runId` alone unconstrained (confirm against `AiAuditEventListView` at execute time and record the choice in the SUMMARY).

Purpose: Delivers OBS-01 + OBS-02 + the bounded-state half of OBS-04. The status-row mechanism here is the one Phase 20's STT error/retry row will reuse — keep it generic. TEST-19 (Plan 05) asserts the rendered text never leaks; this plan must make that assertion pass by construction. The post-navigation disclosure (SPEC req 5 / CONTEXT D-07) is a hard acceptance criterion — the `AiAuditEvent`-by-`conversation.id` correlation pass is the in-budget additive way to satisfy it without refactoring history reconstruction.

Output: new `TurnDetailRenderer`; extended `ChatPanelFragment` (status `<span>`, per-turn `Details` for live turns AND a history-correlation pass appending the same `Details` anchored by `runId`, bounded `liveTurnSteps`, on-expand audit re-query, `Activity` branch); new `.ai-agent-status` CSS; new status + turn-detail `msg://` keys (en + vi); `TurnDetailRendererTest` (plain JUnit, Vaadin-free), `ChatPanelFragmentStatusLineTest` (`@UiTest`), `ChatPanelFragmentTurnDetailTest` (`@UiTest`), `ChatPanelFragmentTurnDetailHistoryTest` (`@UiTest` + seeded conversation + matching audit rows).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-SPEC.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-CONTEXT.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-PATTERNS.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-RESEARCH.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-02-SUMMARY.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-03-SUMMARY.md

<interfaces>
<!-- StreamingEvent.Activity(ActivityKind kind) + ActivityKind {CHAT, TOOL, RETRIEVAL} — added in Plan 02. StreamingEvent.Activity(RETRIEVAL) carries NO duration — a live RETRIEVAL step row renders "—", not "0 ms"; the history audit re-read backfills the real latencyMs. -->
<!-- ChatPanelFragment NOTICE-row precedent (appendNoticeRow): com.vaadin.flow.dom.Element notice = new com.vaadin.flow.dom.Element("div"); notice.getClassList().add("ai-agent-attachment-notice"); notice.setText(text); messageListSlot.getElement().appendChild(notice); messageCount++; — appendIntentConfirmRow shows: row.getElement().setAttribute("role","status"); row.getElement().setAttribute("aria-live","polite"); -->
<!-- ChatPanelFragment.submitChatTurn(...) builds: activeStream = source.doOnSubscribe(...).doOnNext(evt -> { if (evt instanceof StreamingEvent.Final f) { if (activeRunId == null) activeRunId = f.runId(); ... } ...existing instanceof branches...; StreamEventRenderer.RenderedStreamEvent rendered = StreamEventRenderer.renderStreamEventDetails(evt, labels, citationState); ...markdown/draft/action-proposal branches... }).doOnError(err -> { ... }).doOnComplete(() -> accessUi(() -> { ... finishStreamInternal(); })).subscribe(); — add an `else if (evt instanceof StreamingEvent.Activity a)` branch. -->
<!-- ChatPanelFragment.accessUi(Runnable) / accessUiAuthenticated(Runnable) — every UI mutation from the reactive callback goes through these. -->
<!-- ChatPanelFragment.clearMessageList(): messageListSlot.removeAll(); messageListSlot.getElement().removeAllChildren(); then re-adds messageList — naturally clears the status row + per-turn Details on conversation switch, BUT the status row must ALSO be removed in finishStreamInternal() / on terminal Final/Error within the same turn (D-06; RESEARCH Pitfall 3), and the turnDetailsByRunId map + liveTurnSteps must be cleared in clearMessageList(). -->
<!-- ChatPanelFragment.finishStreamInternal() — the existing turn-finalization hook (re-enables input, clears the ProgressBar). -->
<!-- ChatPanelFragment HISTORY-REPLAY region (~lines 563-595, inside the conversation-switch path setConversationIdInternal/loadConversation): loads `List<AiMessage> history = dataManager.load(AiMessage.class).query("select m from ai_AiMessage m where m.conversation.id = :cid order by m.seq asc, m.createdDate asc").parameter("cid", cid).list();` then iterates `for (AiMessage m : history) { if (role==USER) items.add(buildItem(m,userName,USER_COLOR)); else if (role==ASSISTANT) items.add(buildItem(m,aiName,AI_COLOR)); else if (role==NOTICE) appendNoticeRow(...); }` then `messageList.setItems(new ArrayList<>(items));`. **AiMessage has NO runId field** — the disclosure runId comes from a separate `AiAuditEvent`-by-`conversation.id` query, NOT from AiMessage. -->
<!-- ChatPanelFragment injects io.jmix.core.Messages (class-less messages.getMessage(key)), DataManager, AND will need UnconstrainedDataManager (io.jmix.core.UnconstrainedDataManager) for the AiAuditEvent reads (AiAgentUserRole has no AiAuditEvent policy — constrained DataManager would throw). activeRunId is a field set from StreamingEvent.Final.runId(). currentAuthentication.getUser().getUsername() gives the current user (the ownership-check at ~line 553 already uses it). citationState / labels are existing locals/fields used by StreamEventRenderer. -->
<!-- AiAuditEvent (@Store("agentstore")): runId (UUID), conversation (AiConversation FK), userUsername (String), kind (String "CHAT"/"TOOL"/"RETRIEVAL"), startedAt (OffsetDateTime), finishedAt (OffsetDateTime), latencyMs (Long, nullable until run finishes), getOutcome()->AiToolCallOutcome (nullable), errorClass (String), parent (AiAuditEvent), children. CHAT root per runId (parent is null) + TOOL/RETRIEVAL children (parent is not null). Typed DataManager.load(AiAuditEvent.class) infers the agentstore store; only raw-JPQL loadValue/loadValues need .store("agentstore") (project memory feedback_jmix_loadvalue_store.md). -->
<!-- AiAuditEventListView — the existing read precedent: `<loader id="auditsDl" readOnly="true"><query>select e from ai_AiAuditEvent e order by e.startedAt desc</query></loader>` behind a genericFilter, view gated by AiAgentAdminRole (AiAgent_AiAuditEvent.list). It is an ADMIN view with full AiAuditEvent EntityPolicy — it does NOT establish that a chat user can read AiAuditEvent. Conclusion for the fragment read: AiAgentUserRole has no AiAuditEvent policy => use UnconstrainedDataManager + a mandatory `where e.userUsername = :me and e.conversation.id = :cid` clause. Confirm at execute time; record in SUMMARY. -->
<!-- AiToolCallOutcome — the outcome enum (includes an ERROR/rollback value); never render its raw name — map to a msg:// indicator key. -->
<!-- Vaadin Details: com.vaadin.flow.component.details.Details; new Details(); d.setOpened(false); d.setSummaryText(MessageFormat.format(messages.getMessage("chatView.turnDetail.summary"), TurnDetailRenderer.summaryArgs(stepCount, totalMs))); d.add(buildStepRows(...)); d.addOpenedChangeListener(e -> { if (e.isOpened() && !alreadyLoaded(d)) lazyLoadHistorySteps(runId, conversationId, d); }); messageListSlot.add(d); — Details is in Jmix's accepted Vaadin set. ComponentUtil.setData(d, LOADED_KEY, Boolean.TRUE) for the memoization flag. -->
<!-- CSS precedent: ai-agent-chat.css .ai-agent-attachment-notice / .ai-agent-intent-confirm / .ai-agent-action-choice use --lumo-* tokens; loaded via @CssImport("./styles/ai-agent-chat.css") on MessageBubbleComponent. Plan 03 added the .ai-agent-sidebar* rules — DO NOT re-add them; only add .ai-agent-status (+ @keyframes + prefers-reduced-motion) and optional .ai-agent-turn-detail tweaks. -->
<!-- Locale precedent: chatView.attachments.notice=... (en) — add chatView.status.chat / .tool / .retrieval / .neutral ; chatView.turnDetail.summary={0} steps · {1} ms (MessageFormat, integer args) ; chatView.turnDetail.step.tool / .step.retrieval ; chatView.turnDetail.errorIndicator ; chatView.turnDetail.unknownDuration (the "—" em-dash placeholder for steps with no known ms — or render the literal "—" inline; either way it must not show "0 ms"). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the Vaadin-free TurnDetailRenderer label-only mapper</name>
  <files>ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/TurnDetailRenderer.java, ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/fragment/TurnDetailRendererTest.java</files>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/StreamEventRenderer.java (the model for a Vaadin-free, Spring-free, pure-static utility with a `labels`-map pull-through — mirror its shape)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/StreamingEvent.java (the `ActivityKind` enum from Plan 02)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallOutcome.java (the outcome enum — the ERROR/rollback value to map to an indicator key; never render its raw name)
    - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/RenderStreamEventTest.java (plain JUnit 5 + AssertJ — the model for `TurnDetailRendererTest`)
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-PATTERNS.md (section "NEW view/chat/fragment/TurnDetailRenderer.java" + "Pattern 4: One kind -> msg:// mapper")
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-RESEARCH.md (Pattern 4, Security Domain "Internal @Tool method name … leaking")
  </read_first>
  <behavior>
    - `TurnDetailRenderer.statusKeyFor(ActivityKind.CHAT)` returns `"chatView.status.chat"`, `TOOL` → `"chatView.status.tool"`, `RETRIEVAL` → `"chatView.status.retrieval"`; a "neutral / no kind yet" helper returns `"chatView.status.neutral"`.
    - A step-row builder takes ONLY: a `kind` (the `ActivityKind` enum OR the audit `kind` String constrained to {"CHAT","TOOL","RETRIEVAL"}), a `latencyMs` (`Long` — NULLABLE; null/unknown means "no known duration"), and an `outcome` (`AiToolCallOutcome` or null) — and returns a structured `StepRow` (or a `List<StepRow>`) carrying only msg:// keys + the `Long latencyMs` (nullable) + a boolean/enum "errored" flag — NEVER a `toolName`, entity name, `argsJson`, `resultSummary`, or `queryText`. There is no method signature that accepts a tool/entity name. A `StepRow` with a null `latencyMs` is the signal for the renderer/UI to show an em-dash placeholder ("—"), never "0 ms".
    - The summary helper produces the `MessageFormat` arg tuple `(stepCount, totalMs)` for `chatView.turnDetail.summary`.
    - The renderer is Vaadin-free and Spring-free (no `@Component`, no Vaadin import) — testable with plain JUnit.
  </behavior>
  <action>
    Create `view/chat/fragment/TurnDetailRenderer.java`: a `final` class with private constructor and static methods, modeled on `StreamEventRenderer` (Vaadin-free, Spring-free). Provide: `static String statusKeyFor(StreamingEvent.ActivityKind kind)` returning `chatView.status.chat` / `.tool` / `.retrieval`; `static String neutralStatusKey()` returning `chatView.status.neutral`; a nested `record StepRow(String labelKey, Long latencyMs, boolean errored)` (latencyMs NULLABLE — keep it data-only); `static StepRow stepRow(StreamingEvent.ActivityKind kind, Long latencyMs, AiToolCallOutcome outcome)` and an overload `static StepRow stepRow(String auditKind, Long latencyMs, AiToolCallOutcome outcome)` that validates `auditKind ∈ {"CHAT","TOOL","RETRIEVAL"}` (throw `IllegalArgumentException` otherwise — defensive; the audit query already filters) and maps it to the same `labelKey` (`chatView.turnDetail.step.tool` for TOOL, `chatView.turnDetail.step.retrieval` for RETRIEVAL, and a sensible key for CHAT if it ever appears as a child — typically TOOL/RETRIEVAL are the only children) and `errored = outcome != null && outcome` is the ERROR/rollback value; `static String errorIndicatorKey()` returning `chatView.turnDetail.errorIndicator`; `static String unknownDurationKey()` returning `chatView.turnDetail.unknownDuration` (or expose the literal `"—"` — the contract is: a null-latency step renders the em-dash, never `0 ms` / `null ms`); `static Object[] summaryArgs(int stepCount, long totalMs)`. Crucially: NO public method takes a `String toolName` / entity name / free text — the input alphabet is the closed `ActivityKind` enum, the validated audit-`kind` String, the nullable `Long` ms, and the `AiToolCallOutcome` enum. Create `TurnDetailRendererTest.java` (plain JUnit 5 + AssertJ, mirror `RenderStreamEventTest`): assert `statusKeyFor` returns the right keys for every `ActivityKind`; assert `stepRow(...)` for TOOL/RETRIEVAL produces the right `labelKey` and `errored` flag for an ERROR outcome vs a success outcome; assert `stepRow("BOGUS", ...)` throws; assert a `stepRow(...)` with a `null` latencyMs is recognized as "unknown duration" (the row exposes null latency, and whatever ms-rendering helper exists returns the em-dash / `unknownDurationKey()`, NOT "0 ms"); assert no overload accepts a tool/entity-name argument (a compile-time fact — note it in a test comment / a reflective check that the public method param types are only `ActivityKind` / `String` (the validated audit-kind) / `Long` / `AiToolCallOutcome` / primitives).
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.fragment.TurnDetailRendererTest"</automated>
  </verify>
  <acceptance_criteria>
    - `TurnDetailRenderer.java` exists, is Vaadin-free + Spring-free, and exposes `statusKeyFor(ActivityKind)`, the step-row builder(s) (with a NULLABLE `Long latencyMs`), `errorIndicatorKey()`, an unknown-duration ("—") helper, and a summary-args helper — and no public method accepts a tool/entity name or free text.
    - `TurnDetailRendererTest` asserts the key mappings, the ERROR-outcome flag, the bad-audit-kind rejection, the null-latency ⇒ em-dash (not "0 ms") contract, and the label-only input-alphabet contract; `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.fragment.TurnDetailRendererTest"` exits 0.
  </acceptance_criteria>
  <done>TurnDetailRenderer is a pure-fn label-only mapper with a closed input alphabet and a null-latency ⇒ "—" contract; TurnDetailRendererTest green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the ephemeral KIND-keyed streaming-status line in ChatPanelFragment</name>
  <files>ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java, ai-agent/ai-agent/src/main/resources/META-INF/resources/frontend/styles/ai-agent-chat.css, ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties, ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties, ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/fragment/ChatPanelFragmentStatusLineTest.java</files>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java (the file being modified — read: `appendNoticeRow`, `appendIntentConfirmRow` (the `role`/`aria-live` attribute pattern), `submitChatTurn(...).doOnNext/.doOnError/.doOnComplete`, `clearMessageList`, `finishStreamInternal`, `accessUi`/`accessUiAuthenticated`, `activeRunId`, how `messageListSlot` is composed, the Messages injection)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/TurnDetailRenderer.java (Task 1 — `statusKeyFor` / `neutralStatusKey`)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/StreamingEvent.java (the `Activity` variant + `ActivityKind`)
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties + messages_vi.properties (the `chatView.attachments.notice` precedent — add the status keys near it)
    - ai-agent/ai-agent/src/main/resources/META-INF/resources/frontend/styles/ai-agent-chat.css (the `.ai-agent-attachment-notice` style precedent — add `.ai-agent-status` + the `@keyframes` + `prefers-reduced-motion`; do NOT touch the `.ai-agent-sidebar*` rules Plan 03 added)
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-PATTERNS.md (sections "view/chat/fragment/ChatPanelFragment.java" (NOTICE-row sibling pattern), "META-INF/resources/frontend/styles/ai-agent-chat.css", "com/vn/agent/messages_*.properties")
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-RESEARCH.md (Pattern 2, Pitfall 3, Code Examples "status-line + per-turn <details> in ChatPanelFragment", Anti-Patterns "Concatenating status text into the assistant MessageListItem", Security Domain "HTML injection via a rendered label")
    - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/fragment/ChatPanelFragmentLoadingIndicatorTest.java (the model for `ChatPanelFragmentStatusLineTest`)
  </read_first>
  <behavior>
    - When the stream emits `StreamingEvent.Activity(TOOL)`, a `<span class="ai-agent-status" role="status" aria-live="polite">` with the text from `messages.getMessage("chatView.status.tool")` appears appended to `messageListSlot.getElement()` AFTER the `<vaadin-message-list>` block (a sibling, NOT inside any `MessageListItem`); a subsequent `Activity(RETRIEVAL)` updates the same `<span>`'s text to `chatView.status.retrieval`; before any `Activity` arrives during a streaming turn, the status `<span>` (if shown) carries the neutral text (`chatView.status.neutral`).
    - On the terminal `StreamingEvent.Final` (or `StreamingEvent.Error`), and in `finishStreamInternal()`, the status `<span>` is removed from its parent and the field nulled — it is GONE, not blanked.
    - The assistant `MessageListItem` for the turn contains only the reply markdown — no status text ("thinking…" / "searching data…" / "retrieving documents…") is ever appended into it.
    - Switching conversations (`clearMessageList()`) also removes the status `<span>` (it lives in `messageListSlot.getElement()` which is cleared) — but the explicit terminal-event removal is what handles same-turn cleanup.
    - The status text is HTML-escaped (`Element.setText(...)` / `Span(text)`) — no `innerHTML`, no markdown rendering on this element.
  </behavior>
  <action>
    In `ChatPanelFragment`: add fields `private com.vaadin.flow.dom.Element statusRow;` (null when no turn streaming). Add `private void showStatus(String key)`: if `statusRow == null`, create `new com.vaadin.flow.dom.Element("span")`, `getClassList().add("ai-agent-status")`, `setAttribute("role", "status")`, `setAttribute("aria-live", "polite")`, `messageListSlot.getElement().appendChild(statusRow)`; then `statusRow.setText(messages.getMessage(key))`. Add `private void removeStatusRow()`: `if (statusRow != null) { statusRow.removeFromParent(); statusRow = null; }`. In `submitChatTurn(...).doOnNext(...)`: add an `else if (evt instanceof StreamingEvent.Activity a) { accessUi(() -> showStatus(TurnDetailRenderer.statusKeyFor(a.kind()))); ... (Task 3 also records a RETRIEVAL step here) ... return; }` branch (place it before the `StreamEventRenderer.renderStreamEventDetails` call; `return` so the markdown path isn't entered for an `Activity` event); optionally, when the turn starts (in `.doOnSubscribe` or on the first `Content` if no `Activity` has arrived), `accessUi(() -> showStatus(TurnDetailRenderer.neutralStatusKey()))` — Claude's Discretion per D-05, but a neutral typing indicator before the first KIND is the D-06 behavior. In `.doOnError(...)` and `.doOnComplete(...)` and `finishStreamInternal()`: call `removeStatusRow()` (within the existing `accessUi(...)` wrapper for the reactive callbacks). Do NOT change the existing `botMsg.appendText(md)` prose-in-bubble behavior — the status line is a SEPARATE sibling element (RESEARCH Anti-Pattern). Append to `ai-agent-chat.css`: `.ai-agent-status { display: block; text-align: center; font-size: var(--lumo-font-size-s); color: var(--lumo-secondary-text-color); padding: var(--lumo-space-xs) 0; }` plus a subtle three-dot/pulse `@keyframes ai-agent-status-pulse { ... }` applied to it (or a `::after` content of dots) and `@media (prefers-reduced-motion: reduce) { .ai-agent-status { animation: none; } }`. Add to `messages_en.properties`: `chatView.status.chat=thinking…`, `chatView.status.tool=searching data…`, `chatView.status.retrieval=retrieving documents…`, `chatView.status.neutral=…` (a neutral typing indicator string, e.g. `working…` or three dots) — and the Vietnamese equivalents to `messages_vi.properties`. Create `ChatPanelFragmentStatusLineTest.java` (`@UiTest`, model on `ChatPanelFragmentLoadingIndicatorTest` + the chat-flow stub harness): drive a stub `Flux<StreamingEvent>` that emits `Activity(TOOL)`, then `Activity(RETRIEVAL)`, then a `Content`, then `Final`; assert after `Activity(TOOL)` a `<span.ai-agent-status>` exists in `messageListSlot` (sibling of `<vaadin-message-list>`, not inside any `MessageListItem`) with the `chatView.status.tool` text and `role="status"`/`aria-live="polite"`; assert after `Activity(RETRIEVAL)` its text is `chatView.status.retrieval`; assert after `Final` the `<span.ai-agent-status>` is gone; assert the assistant `MessageListItem` text equals the reply markdown only (no "searching data…" substring); assert switching conversations also removes it.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.fragment.ChatPanelFragmentStatusLineTest"</automated>
  </verify>
  <acceptance_criteria>
    - `ChatPanelFragment.java` has a `statusRow` field, `showStatus(...)` / `removeStatusRow()` helpers, an `else if (evt instanceof StreamingEvent.Activity a)` branch in `doOnNext` calling `showStatus(TurnDetailRenderer.statusKeyFor(...))`, and `removeStatusRow()` called on terminal `Final`/`Error` and in `finishStreamInternal()`.
    - `ai-agent-chat.css` contains a `.ai-agent-status` rule + a `@keyframes` for its pulse + a `@media (prefers-reduced-motion: reduce)` guard; the `.ai-agent-sidebar*` rules from Plan 03 are untouched.
    - `messages_en.properties` + `messages_vi.properties` each contain `chatView.status.chat`, `chatView.status.tool`, `chatView.status.retrieval`, `chatView.status.neutral`.
    - `ChatPanelFragmentStatusLineTest` asserts: status `<span>` appears keyed by `ActivityKind`, in `messageListSlot` as a sibling (never inside a `MessageListItem`), with `role="status"`/`aria-live="polite"`; updates on a later `Activity`; is REMOVED on `Final`; the assistant bubble has only reply markdown; cleared on conversation switch.
    - `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.fragment.ChatPanelFragmentStatusLineTest"` exits 0.
    - `git diff` shows no change to `chat-panel-fragment.xml` (no slot id touched) and no change to the existing `botMsg.appendText(md)` behavior.
  </acceptance_criteria>
  <done>The ephemeral KIND-keyed status line renders as a sibling in messageListSlot, updates on Activity events, is removed on terminal events, never leaks into the bubble; CSS + locale + test in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render the collapsed-by-default per-turn tool-detail Details — live turns + a post-navigation AiAuditEvent-by-conversation correlation pass, with bounded live state and a lazy memoized on-expand AiAuditEvent re-query</name>
  <files>ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java, ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/fragment/ChatPanelFragmentTurnDetailTest.java, ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/fragment/ChatPanelFragmentTurnDetailHistoryTest.java</files>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java (the file being modified — read: `appendIntentConfirmRow` / `appendActionChoiceRow` (the `Div`+`Span`+`Button` sibling-row pattern, the `Map<UUID, ...>` anchoring like `actionChoiceRowsByProposalId`), `submitChatTurn(...).doOnNext/.doOnComplete`, `activeRunId`, `clearMessageList`, the `DataManager` injection, `accessUi`, AND — load-bearing for this revision — the HISTORY-REPLAY region ~lines 563-595: the `dataManager.load(AiMessage.class).query("...where m.conversation.id = :cid order by m.seq asc...")` load, the `for (AiMessage m : history)` loop, the `buildItem(m, name, color)` calls, the `items` list, and the final `messageList.setItems(new ArrayList<>(items))`; also the ownership-check at ~line 553 that already uses `currentAuthentication.getUser().getUsername()`)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/TurnDetailRenderer.java (Task 1 — the step-row builder + `summaryArgs` + `errorIndicatorKey` + the null-latency ⇒ "—" contract)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/StreamingEvent.java (`ToolCall`, `ToolResult` (carries `toolCallId`, `outcome`), `Activity(RETRIEVAL)` — the live-turn step sources; `Activity(RETRIEVAL)` has NO duration ⇒ the live RETRIEVAL row gets a null latencyMs ⇒ renders "—", not "0 ms")
    - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiAuditEvent.java (the @Store("agentstore") entity — `runId`, `conversation` (AiConversation FK), `userUsername`, `kind`, `startedAt`, `finishedAt`, `latencyMs` (nullable), `getOutcome()`, `errorClass`, `parent`, `children`; CHAT root = `parent is null`, TOOL/RETRIEVAL children = `parent is not null`)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/audit/AiAuditEventListView.java + the matching `ai-audit-event-list-view.xml` + ai-agent/ai-agent/src/main/java/com/vn/agent/security/AiAgentUserRole.java + AiAgentAdminRole.java (the read precedent + the roles — confirm: the ListView is an ADMIN view with full AiAuditEvent EntityPolicy; AiAgentUserRole has NO AiAuditEvent policy ⇒ the fragment read MUST use UnconstrainedDataManager with a mandatory `where e.userUsername = :me and e.conversation.id = :cid` clause, never `runId`-only unconstrained; RESEARCH Open Q1)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/AiChatSessionState.java (confirm it stays at `currentConversationId` + listeners — the bounded step list does NOT go here)
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-PATTERNS.md (sections "view/chat/fragment/ChatPanelFragment.java" (Per-turn Details sibling, On-expand history-turn read), "Agentstore typed reads", "No Analog Found" → the on-expand read guidance; the history-replay loop region)
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-RESEARCH.md (Pitfall 4, Pitfall 8, Open Question 1, Validation Architecture `ChatPanelFragmentTurnDetailHistoryTest` row, Code Examples "<details> in XML", Anti-Patterns "Re-querying AiAuditEvent … on every render" / "Putting the per-turn step accumulator in AiChatSessionState", Security Domain "Cross-user audit-row exposure via the on-expand query")
    - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/fragment/ (existing fragment @UiTest harness — model `ChatPanelFragmentTurnDetailTest` / `...HistoryTest` on it)
  </read_first>
  <behavior>
    - During a streaming turn, `ToolCall` / `ToolResult` / `Activity(RETRIEVAL)` events accumulate into a bounded per-fragment-instance step list (current/last turn only or a small capped list — dedupe `ToolCall`/`ToolResult` by `toolCallId`); a TOOL step records its real ms from the `ToolCall`→`ToolResult` timing; a live RETRIEVAL step records a NULL latencyMs (the `Activity` event has no duration) ⇒ its row renders the em-dash "—", never "0 ms"; on terminal `Final`/`Error` the list is consumed to build the `Details` and then CLEARED (nothing accumulates in `AiChatSessionState`).
    - On `.doOnComplete`, if the consumed step count > 0 and `activeRunId != null`, a `com.vaadin.flow.component.details.Details` with `setOpened(false)` is appended to `messageListSlot` as a sibling AFTER that turn's assistant `MessageListItem` (anchored by `runId` in a `Map<UUID, Details>` like `actionChoiceRowsByProposalId`), with `setSummaryText(MessageFormat.format(messages.getMessage("chatView.turnDetail.summary"), TurnDetailRenderer.summaryArgs(stepCount, totalMs)))` and content = a `VerticalLayout` of label-only step rows (each: `messages.getMessage(step.labelKey())` + the per-step ms text (the em-dash "—" if `step.latencyMs()` is null) + the `messages.getMessage(TurnDetailRenderer.errorIndicatorKey())` indicator iff `step.errored()`).
    - A completed turn with zero tool calls produces NO `Details` at all (not a "0 steps" row).
    - **Post-navigation / conversation-switch reload (SPEC req 5 / CONTEXT D-07):** after the history-replay loop replays the conversation's `AiMessage`s into `MessageListItem`s, the fragment runs ONE additive correlation query — `unconstrainedDataManager.load(AiAuditEvent.class).query("select e from ai_AiAuditEvent e where e.userUsername = :me and e.conversation.id = :cid and e.parent is null order by e.startedAt asc").parameter("me", currentUser).parameter("cid", cid).fetchPlan(fp -> fp.add("runId").add("startedAt")).list()` (typed load infers the agentstore store; narrow fetch plan — `runId`, `startedAt` only; the mandatory `userUsername` + `conversation.id` filter is the row-level-access guard since `AiAgentUserRole` has no `AiAuditEvent` policy) — this yields the conversation's CHAT roots in turn order. Zip that ordered `runId` list against the replayed ASSISTANT `MessageListItem`s in the same turn order (the Nth ASSISTANT turn ↔ the Nth CHAT root); for each pair, append a collapsed `Details` (`appendHistoryTurnDetailsPlaceholder(runId, cid)`) as a sibling after that ASSISTANT item. (Defensive: if the counts mismatch — e.g. a turn errored before producing an audit root — zip up to the shorter length and skip the rest; do NOT throw.) This is an ADDITIVE correlation pass over an existing query path — NOT a refactor of history reconstruction; `AiMessage` still has no `runId` and is not modified.
    - On FIRST expand of a history `Details` (`addOpenedChangeListener`, `e.isOpened()` && not yet loaded), it runs ONE `unconstrainedDataManager.load(AiAuditEvent.class).query("select e from ai_AiAuditEvent e where e.userUsername = :me and e.conversation.id = :cid and e.runId = :rid and e.parent is not null order by e.startedAt asc").parameter("me", currentUser).parameter("cid", cid).parameter("rid", runId).fetchPlan(fp -> fp.add("kind").add("startedAt").add("finishedAt").add("latencyMs").add("outcome").add("errorClass")).list()` (narrow fetch plan; the `userUsername` + `conversation.id` filter is mandatory — NEVER load by `runId` alone unconstrained), maps the rows through `TurnDetailRenderer.stepRow(auditKind, latencyMs, outcome)`, populates the content `VerticalLayout` with label-only rows (per-step ms — real values from `latencyMs`, em-dash if a row's `latencyMs` is null), sets a "loaded" flag on the `Details` (`ComponentUtil.setData(details, LOADED_KEY, Boolean.TRUE)`) so a second expand/collapse does NOT re-query; if the query returns zero TOOL/RETRIEVAL children, REMOVE the `Details` (a zero-tool prior turn shows no disclosure).
    - The `Details` summary uses `MessageFormat` with integer args only (step count, ms) — no tool/entity name is interpolated; the step-row labels come exclusively from `TurnDetailRenderer` keys.
    - Confirm at execute time against `AiAuditEventListView` + the `agentstore` roles whether a constrained `DataManager` read is actually permitted for the chat user; the analysis says NO (no `AiAuditEvent` policy on `AiAgentUserRole`), so `UnconstrainedDataManager` + the mandatory `userUsername`+`conversation.id` filter is the path — record the confirmed choice + rationale in the SUMMARY.
  </behavior>
  <action>
    In `ChatPanelFragment`: inject `io.jmix.core.UnconstrainedDataManager unconstrainedDataManager` (alongside the existing `DataManager`). Add fields `private final java.util.List<TurnStep> liveTurnSteps = new java.util.ArrayList<>();` (a small local `record TurnStep(String labelKey, Long latencyMs, boolean errored, String toolCallId)` or reuse/wrap `TurnDetailRenderer.StepRow` — keep it bounded; if you want a cap, cap at e.g. 50 and note it) and `private final java.util.Map<UUID, com.vaadin.flow.component.details.Details> turnDetailsByRunId = new java.util.HashMap<>();` and a `private static final String LOADED_KEY = "...";` marker. In `submitChatTurn(...).doOnNext(...)`: on `StreamingEvent.ToolCall tc` add a "started" step keyed by `tc.toolCallId()` (start time noted); on `StreamingEvent.ToolResult tr` mark/finish that step with `tr.outcome()` and the elapsed ms (dedupe by id); on `StreamingEvent.Activity a` where `a.kind() == RETRIEVAL` add a RETRIEVAL step with a NULL latencyMs (the live event has no duration — the row will render "—"; the on-expand history re-read has the real `latencyMs`). (These are added alongside the status-line `Activity` branch from Task 2 — that branch already `return`s, so for `Activity(RETRIEVAL)` record the step before the `return`, or in the same branch.) In `.doOnComplete(() -> accessUi(() -> { ... }))`: compute `stepCount = liveTurnSteps.size()`; if `stepCount > 0 && activeRunId != null`, call `appendTurnDetails(activeRunId, copyOf(liveTurnSteps))`; then `liveTurnSteps.clear()`; then the existing `finishStreamInternal()`. In `.doOnError(...)`: also `liveTurnSteps.clear()` (no `Details` on an errored turn unless steps ran — Claude's Discretion; if steps ran, you may still render the `Details` — but at minimum clear the list). Add `private void appendTurnDetails(UUID runId, List<TurnStep> steps)`: build a `Details` (`setOpened(false)`, summary via `MessageFormat`+`summaryArgs`, content = `VerticalLayout` of label-only rows via `TurnDetailRenderer` — render the per-step ms as the em-dash when null, never "0 ms"), `messageListSlot.add(d)`, `turnDetailsByRunId.put(runId, d)`. Add `private void appendHistoryTurnDetailsPlaceholder(UUID runId, UUID conversationId)`: build a collapsed `Details` whose `addOpenedChangeListener` — guarded by `!Boolean.TRUE.equals(ComponentUtil.getData(d, LOADED_KEY))` — lazily runs the `UnconstrainedDataManager` `AiAuditEvent`-by-`runId` query (with the mandatory `userUsername` + `conversation.id` clause + the narrow fetch plan), maps rows through `TurnDetailRenderer.stepRow(...)`, populates the content (ms rendered, em-dash for null), sets `ComponentUtil.setData(d, LOADED_KEY, Boolean.TRUE)`, and if the query returns zero children REMOVES the `Details`; `messageListSlot.add(d)` and `turnDetailsByRunId.put(runId, d)`. **In the HISTORY-REPLAY region (~lines 563-595):** after the `for (AiMessage m : history)` loop builds `items` and AFTER `messageList.setItems(...)`, do the additive correlation: `List<AiAuditEvent> chatRoots = unconstrainedDataManager.load(AiAuditEvent.class).query("select e from ai_AiAuditEvent e where e.userUsername = :me and e.conversation.id = :cid and e.parent is null order by e.startedAt asc").parameter("me", currentAuthentication.getUser().getUsername()).parameter("cid", cid).fetchPlan(fp -> fp.add("runId").add("startedAt")).list();` then walk the replayed `history` (or `items`) in order, and for the k-th ASSISTANT turn pair it with `chatRoots.get(k).getRunId()` (zip up to `min(assistantCount, chatRoots.size())`; skip extras silently), calling `appendHistoryTurnDetailsPlaceholder(runId, cid)` so the collapsed `Details` is appended after that turn. (The `appendNoticeRow`/sibling-append pattern already used in this loop is the model; appending after `setItems` is fine since `Details` lives in `messageListSlot.getElement()` as a sibling of `<vaadin-message-list>`, not in the message list itself — match the existing post-`setItems` sibling-append ordering.) Make sure `clearMessageList()` (conversation switch) also clears `turnDetailsByRunId` and `liveTurnSteps`. Create `ChatPanelFragmentTurnDetailTest.java` (`@UiTest`): drive a stub stream emitting `ToolCall`/`ToolResult` (one OK, one ERROR) + an `Activity(RETRIEVAL)` + `Final` for a known `runId`; assert a `<details opened="false">` appears after the assistant `MessageListItem` with summary text matching `chatView.turnDetail.summary` formatted with the 3-step count + total ms, expanding to 3 label-only rows (the two TOOL rows show their resolved `chatView.turnDetail.step.tool` label + a real ms value; the ERROR row also shows the `chatView.turnDetail.errorIndicator` text; the RETRIEVAL row shows the `chatView.turnDetail.step.retrieval` label and the em-dash "—" (NOT "0 ms" / "null ms"); no row contains a `toolName` or entity name); drive a second stub stream with NO `ToolCall`/`ToolResult` + `Final` and assert NO `<details>` is added; assert after a turn the next turn's `liveTurnSteps` starts empty (bounded/cleared). Create `ChatPanelFragmentTurnDetailHistoryTest.java` (`@UiTest` + seeded conversation + matching audit rows in agentstore): seed an `AiConversation` (owned by the test user) with N ASSISTANT `AiMessage`s (plus their USER turns) AND N matching `AiAuditEvent` CHAT roots (`parent is null`, `conversation` = the seeded conversation, `userUsername` = the test user, `startedAt` in turn order) — make some CHAT roots have ≥1 TOOL/RETRIEVAL child (`parent` = the root, `latencyMs` set on TOOL, null on at least one RETRIEVAL child to exercise the em-dash) and at least one CHAT root with zero children; open the fragment for that conversation (a fresh instance / a reload triggering the history-replay path); assert each prior ASSISTANT turn that has ≥1 child shows a collapsed `<details opened="false">` anchored by the right `runId` (sibling after that turn's `MessageListItem`), and the zero-child prior turn shows NO `<details>`; expand one `Details` → assert exactly ONE `AiAuditEvent` re-query happened for that `runId` (a query/call-count proxy — e.g. a Mockito spy on `UnconstrainedDataManager`, or assert the content `VerticalLayout` got the seeded children's count of label-only rows whose label/ms match the seeded `kind`/`latencyMs`, and the null-`latencyMs` child renders "—"); collapse + re-expand → assert NO second re-query (the `LOADED_KEY` flag short-circuits). (If a query-count spy is awkward in the `@UiTest` harness, a call-count `AtomicInteger` wired into a test-double `UnconstrainedDataManager` or asserting the row content stays stable across re-expands is acceptable — but the memoization MUST be asserted somehow.)
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.fragment.ChatPanelFragmentTurnDetailTest" --tests "com.vn.agent.view.chat.fragment.ChatPanelFragmentTurnDetailHistoryTest"</automated>
  </verify>
  <acceptance_criteria>
    - `ChatPanelFragment.java` injects `UnconstrainedDataManager`, has a bounded `liveTurnSteps` list (NOT in `AiChatSessionState`), a `turnDetailsByRunId` map, an `appendTurnDetails(...)` that creates a collapsed `Details` with a `MessageFormat`-summary + label-only step rows (em-dash for null ms, never "0 ms"), an `appendHistoryTurnDetailsPlaceholder(UUID runId, UUID conversationId)` whose `addOpenedChangeListener` does a MEMOIZED `AiAuditEvent`-by-`runId` `UnconstrainedDataManager` query carrying a mandatory `where e.userUsername = :me and e.conversation.id = :cid` clause + a narrow fetch plan, and `liveTurnSteps.clear()` on `.doOnComplete` / `.doOnError`; the HISTORY-REPLAY region (~line 563-595) gains an ADDITIVE post-`setItems` correlation pass that loads the conversation's `AiAuditEvent` CHAT roots (`parent is null`, ordered by `startedAt`, same `userUsername`+`conversation.id` filter, fetch plan = `runId`/`startedAt`), zips them against the replayed ASSISTANT turns, and calls `appendHistoryTurnDetailsPlaceholder(runId, cid)` per turn (zipping up to the shorter length, no throw on mismatch); `clearMessageList()` clears the map + list; `AiMessage.java` is NOT modified.
    - `ChatPanelFragmentTurnDetailTest` asserts: ≥1-step turn → collapsed `<details>` after the assistant `MessageListItem` with the N-step·ms summary, expanding to N label-only rows with per-step ms + an ERROR indicator on the errored row + the em-dash "—" (not "0 ms") on a no-known-duration (live RETRIEVAL) row, no tool/entity name anywhere; 0-step turn → no `<details>`; the step list is cleared after the turn.
    - `ChatPanelFragmentTurnDetailHistoryTest` asserts the end-to-end post-navigation behavior: open the fragment for a seeded conversation (fresh instance / reload) ⇒ each prior ASSISTANT turn with ≥1 tool/retrieval step shows a collapsed `Details` anchored by `runId`; a zero-tool prior turn shows no `Details`; expanding one triggers exactly one `AiAuditEvent` re-query for that `runId` (via the `UnconstrainedDataManager`, `userUsername`+`conversation.id`+`runId` filtered — never `runId`-only unconstrained) and renders label-only rows (real ms on TOOL, em-dash on the null-`latencyMs` child); re-expanding does NOT re-query (memoized).
    - `AiChatSessionState.java` is unchanged (still `currentConversationId` + listeners) — verified by `git diff` and (in Plan 05) `AiChatSessionStateTest`.
    - `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.fragment.ChatPanelFragmentTurnDetailTest" --tests "com.vn.agent.view.chat.fragment.ChatPanelFragmentTurnDetailHistoryTest"` exits 0.
    - No new `@Entity`/`@Table`/Liquibase changelog; `git diff` shows no change to `AiAuditEvent.java`, `AiMessage.java`, `chat-panel-fragment.xml`, or `AiAuditEventListView`.
    - The SUMMARY records: the confirmed constrained-vs-unconstrained decision for the `AiAuditEvent` reads + rationale (the `AiAgentUserRole`-has-no-`AiAuditEvent`-policy fact and how `AiAuditEventListView` was checked), the exact correlation query + zip semantics used in the history-replay region (and what happens on a count mismatch), the bounded-step-list shape/cap, the em-dash placeholder mechanism for unknown-duration steps, and all new `msg://` key names.
  </acceptance_criteria>
  <done>Per-turn collapsed Details renders for ≥1-tool turns with label-only steps + per-step ms (em-dash for unknown) + error indicator; hidden on zero-tool turns; live state bounded + cleared; after a conversation-switch reload an additive AiAuditEvent-by-conversation correlation pass appends the same Details anchored by runId on every prior turn, and expanding lazily + memoized-ly re-reads that runId's AiAuditEvent children respecting row-level access (UnconstrainedDataManager + mandatory userUsername+conversation filter); no new persisted state; AiMessage untouched; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| streaming flux (server) → rendered status line / Details (browser) | Only the closed `ActivityKind` enum + server-side timing/outcome cross; no user free-text, no `toolName`/`argsJson`/`resultSummary` |
| `AiAuditEvent` (agentstore, per-user rows) → on-expand + history-correlation fragment reads | A user must only read their own conversation's audit rows (the reads carry a mandatory `userUsername` + `conversation.id` filter; `AiAgentUserRole` has no `AiAuditEvent` policy so the constrained `DataManager` is not an option) |
| user input ↔ the new rendering | The only "input" is the closed `ActivityKind` enum from the flux and the server-originated `runId` UUID — no user text reaches the new elements |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-D1 | Information disclosure | internal `@Tool` method name / raw host-entity name leaking into the streaming-status line or the per-turn `Details` (the headline threat for this phase) | mitigate | Closed `ActivityKind` enum + `kind`-keyed `msg://` mapper (`TurnDetailRenderer`) — the rendering path's input alphabet structurally cannot contain a tool/entity name; never pass `StreamingEvent.ToolResult.toolName()` / `AiAuditEvent.eventName`/`argumentsJson`/`resultSummary`/`queryText` into a rendered label; the audit queries select only `runId`/`startedAt` (correlation pass) and `kind`/`startedAt`/`finishedAt`/`latencyMs`/`outcome`/`errorClass` (on-expand) — no name columns, no LOBs. TEST-19 (Plan 05) enforces with the Phase 9 leak regexes. |
| T-15-D2 | Tampering / XSS | HTML injection via a rendered status/disclosure label | mitigate | Use Vaadin `Element.setText(...)` / `Span(text)` (HTML-escaped) — never `setProperty("innerHTML", ...)` or `MarkdownRenderer` on the `<span>`/`Details` rows; the `Details` summary uses `MessageFormat` with integer args only; the NOTICE-row precedent (`appendNoticeRow` uses `notice.setText(text)`) is the model. |
| T-15-D3 | Elevation of privilege / Information disclosure | cross-user `AiAuditEvent` exposure via the on-expand query or the history-correlation pass | mitigate | `AiAgentUserRole` has NO `AiAuditEvent` policy, so the constrained `DataManager` cannot read it — the fragment uses `UnconstrainedDataManager` with a MANDATORY `where e.userUsername = :currentUser and e.conversation.id = :conversationId` clause on BOTH the history-correlation CHAT-roots query and the on-expand TOOL/RETRIEVAL-children query; NEVER load `AiAuditEvent` by `runId` alone unconstrained; the conversation itself was already ownership-checked (`conversationGateway.loadOrCreate(currentUsername, cid, ...)` at ~line 553). Decision confirmed against `AiAuditEventListView`'s read path (an admin-only view with full `AiAuditEvent` `EntityPolicy`). |
| T-15-D4 | Denial of service / resource exhaustion | unbounded per-turn step accumulation in session state, or N DB round-trips for N expand clicks, or N round-trips for N history turns | mitigate | The live-turn step list is per-`ChatPanelFragment`-instance (NOT `AiChatSessionState`), bounded (current/last turn or a small cap), cleared on `Final`/`Error` (OBS-04); the history-correlation pass is ONE query per conversation switch (fetches only `runId`/`startedAt` for the CHAT roots); the on-expand audit read runs ONCE per `Details` (memoized via a `LOADED_KEY` flag) — re-expand/collapse is a no-op. Tested in `ChatPanelFragmentTurnDetailTest` / `...HistoryTest`. |
| T-15-D5 | Tampering | new persisted "turn" state / parallel store | mitigate (avoid) | No new `@Entity`/`@Table`/Liquibase changelog; the disclosure is driven by the existing `StreamingEvent` flux (live) and `AiAuditEvent` tree (history); `AiMessage` is read-only here (not modified — the `runId` comes from `AiAuditEvent`, not a new `AiMessage` column); the `Details` open state is component-local (session-only by construction). Verified by `git diff` (no new entity, no `AiMessage` change) + Plan 05's `NoNewPersistedStateTest`. |
</threat_model>

<verification>
- `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.fragment.*"` green (the fragment package — renderer, status line, turn detail, history).
- `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.*"` green (no chat-flow regression).
- `./gradlew :ai-agent:ai-agent:test` (full add-on module) green.
- `git diff --stat` shows no change to `chat-panel-fragment.xml`, `AiAuditEvent.java`, `AiMessage.java`, `AiAuditEventListView.java`, `AiChatSessionState.java`; no new file under `**/liquibase/**`.
- Manual UI verification (app on http://localhost:8088 — do NOT auto-start): during a turn with a tool call, a centered ephemeral status line shows "searching data…" / "retrieving documents…" and the final answer bubble has no leftover status prose; after the turn a collapsed "what the agent did — N steps · total ms" disclosure appears, expanding to label-only rows with per-step ms (a live RETRIEVAL row shows "—" until you reload); a chat-only turn (no tool call) shows no disclosure; navigating away and back (or switching conversation and back), each prior tool-using turn shows a collapsed disclosure, expanding it re-reads from `AiAuditEvent` and shows the rows (with real per-step ms now), and re-collapsing/re-expanding does not re-query; a chat-only prior turn shows no disclosure.
</verification>

<success_criteria>
- OBS-01 met: ephemeral KIND-keyed status line in a sibling slot, cleared on finalize, never in the bubble, never internal names, `msg://` in en + vi.
- OBS-02 met (deep-link descoped): collapsed-by-default per-turn label-only disclosure with per-step ms (em-dash for unknown-duration steps, never "0 ms") + error/rollback indicator, hidden on zero-tool turns, session-only expand state, driven by the flux (live) + an `AiAuditEvent`-by-`conversation.id` correlation pass + lazy memoized `AiAuditEvent`-by-`runId` re-read (history) — disclosure works after a fresh navigation / conversation-switch reload; no `?runId=` link.
- OBS-04 (bounded-state half) met: no new persisted state; `AiMessage` not modified; per-turn detail bounded per-fragment, cleared on terminal event; `AiChatSessionState` unchanged.
- One `kind → msg://` mapper (`TurnDetailRenderer`) with a closed input alphabet — TEST-19 passes by construction.
- `AiAuditEvent` reads respect row-level access (`UnconstrainedDataManager` + mandatory `userUsername` + `conversation.id` filter; never `runId`-only).
- Existing surfaces + slot ids unchanged; all tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-04-SUMMARY.md` recording: the confirmed constrained-vs-unconstrained `AiAuditEvent`-read decision + rationale (the `AiAgentUserRole`/`AiAuditEventListView` check), the exact history-replay correlation query + zip semantics (and count-mismatch handling), the bounded-step-list shape/cap, the em-dash unknown-duration mechanism, the neutral-status-key behavior, and all new `msg://` key names.
</output>
</output>
