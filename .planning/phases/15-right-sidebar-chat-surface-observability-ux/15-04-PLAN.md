---
phase: 15-right-sidebar-chat-surface-observability-ux
plan: 04
type: execute
wave: 3
depends_on: [15-02-PLAN.md, 15-03-PLAN.md]
files_modified:
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/TurnDetailRenderer.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java
  - ai-agent/ai-agent/src/main/resources/META-INF/resources/frontend/styles/ai-agent-chat.css
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties
  - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/fragment/TurnDetailRendererTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/fragment/ChatPanelFragmentStatusLineTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/fragment/ChatPanelFragmentTurnDetailTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/fragment/ChatPanelFragmentTurnDetailHistoryTest.java
autonomous: true
requirements: [OBS-01, OBS-02, OBS-04]

# REVIEW RESOLUTION (15-REVIEWS.md):
# - Point #2 (Codex HIGH) — VERIFIED: the transcript substrate is a single Vaadin `MessageList` (`messageList = new
#   MessageList()` inside the `messageListSlot` VerticalLayout). MessageListItems are DATA items inside one
#   `<vaadin-message-list>`; you CANNOT insert a sibling between two MessageListItems. NOTICE / intent-confirm /
#   action-choice rows are appended as SIBLINGS of `<vaadin-message-list>` inside `messageListSlot` (via
#   `messageListSlot.add(row)` / `messageListSlot.getElement().appendChild(notice)`) — they land AFTER the whole list.
#   FIX (refinement of D-08): the per-turn disclosures render as ONE GROUPED ACTIVITY AREA — an ordered block of
#   collapsed per-turn `Details`, newest last, each anchored by its `runId`, appended after `<vaadin-message-list>`
#   inside `messageListSlot` (a single container Div `.ai-agent-turn-activity` holding the per-turn `Details` in
#   turn order). Still per-turn (one `Details` per turn with ≥1 tool call), still collapsed-by-default, still hidden
#   entirely for zero-tool turns, still label-only. This is within the component-mechanics discretion the SPEC/CONTEXT
#   leave open ("appended as a sibling after that turn's MessageListItem" → "appended in a per-turn-ordered block
#   after the message list" — same information, the only achievable rendering on the MessageList substrate).
# - Point #3 (Codex HIGH / OpenCode MEDIUM) — `AiMessage` has no `runId` (verified). The history correlation must
#   know each CHAT root's tool/retrieval CHILD COUNT up front so zero-child turns get NO `Details` at all (no
#   placeholder). FIX: the correlation query fetches, per CHAT root, a child count via a `count` subquery over the
#   children with `parent is not null` (a single `loadValues` grouped query over the conversation's audit rows, or
#   a `count` per root — see <action>). On any mismatch between ASSISTANT-turn count and CHAT-root count, prefer
#   SHOWING NO DETAILS for the ambiguous turns over a wrong association — zip up to `min(...)` only when the counts
#   are equal; if they differ, render NO history disclosures for that conversation (log debug, never throw). Exact
#   query + zip/skip/abort semantics stated below.
# - Point #6 (Codex MEDIUM) — status-sequencing: the LATEST `Activity` wins, BUT the first `Content` event of a
#   turn implies CHAT — so on the first `Content` (regardless of any prior `Activity(RETRIEVAL)`) flip the status
#   back to CHAT/"thinking…". (And before any `Activity`/`Content`, show the neutral typing indicator.)
# - Point #7 (Codex MEDIUM) — mid-stream cleanup must clear `statusRow` AND `liveTurnSteps` in EVERY teardown site:
#   `.doOnComplete`, `.doOnError`, `finishStreamInternal()`, `clearMessageList()` (conversation switch), the user
#   STOP path (the existing `CancellationRegistry.cancel`/stop-button handler), and fragment detach (the existing
#   dispose-on-detach hook, Pitfall #8). Enumerated below.
# - Point #8 (Codex MEDIUM) — live per-step timing is approximate (`ToolResult` has no latency field on the live
#   path beyond the ToolCall→ToolResult arrival delta; `Activity(RETRIEVAL)` has no duration). DECISION (option a):
#   on the terminal `Final` of the JUST-COMPLETED turn, the live disclosure ALSO lazily reads that runId's
#   `AiAuditEvent` TOOL/RETRIEVAL children (same narrow fetch plan, same Unconstrained + `userUsername`+`conversation.id`-
#   filtered path as the history re-read) so the disclosure shows REAL `latencyMs` for every step — unifying the
#   live and history code paths into one `loadTurnSteps(runId, conversationId)` helper. The transient ToolCall→ToolResult
#   arrival delta is used only as a fallback if the audit read returns nothing (best-effort); a step with no known ms
#   still renders the em-dash "—", never "0 ms".
# - Point #12 (OpenCode LOW) — `liveTurnSteps` gets an explicit cap of 50 (clearing on terminal events already
#   prevents cross-turn accumulation; the cap protects against a single pathological turn).
# - Point #13 (OpenCode LOW) — the memoization test wires a call-count `AtomicInteger` into a test-double
#   `UnconstrainedDataManager` (a delegating wrapper around the real bean that increments a counter on the relevant
#   `load(AiAuditEvent.class)` call); the test asserts the counter is 1 after first expand and still 1 after
#   re-expand. (Fallback if a test-double can't be wired into the `@UiTest` harness: assert the rendered row
#   content is byte-stable across re-expands.)
# - RESEARCH Open Q1 (verified): AiAgentUserRole has NO AiAuditEvent EntityPolicy (its Javadoc says so explicitly:
#   "No policies on AiAuditEvent, AiParameters, or AiKnowledgeDocument"). AiAuditEventListView is an ADMIN view
#   (AiAgentAdminRole has AiAuditEvent ALL). So the chat user CANNOT read AiAuditEvent via the constrained DataManager;
#   the fragment uses `UnconstrainedDataManager` with a MANDATORY `where e.userUsername = :me and e.conversation.id
#   = :cid` clause on every audit read (correlation + on-expand + the live-turn post-Final read). Never load by `runId`
#   alone unconstrained. Typed `load(AiAuditEvent.class)` infers the agentstore store; raw-JPQL `loadValues` needs
#   `.store("agentstore")` (project memory feedback_jmix_loadvalue_store).

must_haves:
  truths:
    - "While a turn streams, an ephemeral status line (a <span role=status aria-live=polite> in messageListSlot, a sibling AFTER <vaadin-message-list>, never inside any MessageListItem) shows a KIND-keyed humanized label: neutral typing indicator before any Activity/Content arrives, CHAT->thinking… (also on the FIRST Content event — point #6), TOOL->searching data… on Activity(TOOL), RETRIEVAL->retrieving documents… on Activity(RETRIEVAL); the latest signal wins but Content arrival always implies CHAT"
    - "On the terminal Final/Error event the status line is REMOVED entirely (not blanked); the final assistant bubble contains only reply markdown — status text is never concatenated into the bubble"
    - "Each completed turn with >=1 tool call shows a collapsed-by-default Vaadin Details ('what the agent did — N steps · total ms') inside a single per-turn-ordered activity block (.ai-agent-turn-activity, newest last, each Details anchored by runId) appended after <vaadin-message-list> in messageListSlot — NOT a sibling between MessageListItems (impossible on the MessageList substrate); expanding lists label-only KIND-keyed step rows with per-step ms (real latencyMs from AiAuditEvent; em-dash '—' for any step with no known duration, never '0 ms') and an error/rollback indicator; a zero-tool-call turn shows NO Details at all (no placeholder)"
    - "For the just-completed turn: on Final the disclosure is built and its step timings come from a lazy read of that runId's AiAuditEvent TOOL/RETRIEVAL children (unified loadTurnSteps path) so they are real, not arrival-time approximations; the live ToolCall→ToolResult arrival delta is only a fallback if the audit read is empty. The transient per-turn step list is bounded (capped at 50, cleared on every terminal/teardown site) — nothing accumulates in AiChatSessionState"
    - "After a conversation-switch reload / fresh navigation, the history-replay path: (1) loads the conversation's AiAuditEvent CHAT roots (parent is null, ordered by startedAt) WITH each root's tool/retrieval child COUNT up front; (2) zips the ordered runId list against the replayed ASSISTANT turns ONLY when the counts are equal — if they differ it renders NO history disclosures for that conversation (debug-log, never throw, never guess); (3) for each pair with child-count > 0 appends a collapsed Details anchored by that runId into the activity block; child-count == 0 turns get NO Details; (4) expanding lazily re-reads only that runId's AiAuditEvent TOOL/RETRIEVAL children (narrow fetch plan, UnconstrainedDataManager + mandatory userUsername+conversation.id filter), memoized so re-expand does not re-query"
    - "Both the live and historical paths funnel through one kind -> msg:// mapper (TurnDetailRenderer) that emits only KIND-keyed localized labels — never a tool method name, never a raw entity name, never argsJson/resultSummary/queryText"
    - "All new status/disclosure strings come from msg:// keys present in messages_en.properties AND messages_vi.properties"
    - "No new @Entity / @Table / Liquibase changelog; AiChatSessionState still holds only currentConversationId + listeners; AiMessage is not modified; the existing FULL_ROUTE/HEADER_BUTTON flows and the ChatPanelFragment slot ids are unchanged; chat-panel-fragment.xml is unchanged"
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/TurnDetailRenderer.java"
      provides: "Vaadin-free, Spring-free pure-fn mapper: ActivityKind/audit-kind String + nullable Long ms + AiToolCallOutcome -> label-only msg:// rows; statusKeyFor(ActivityKind); neutralStatusKey(); errorIndicatorKey(); unknownDurationKey(); summaryArgs(int,long)"
      contains: "TurnDetailRenderer"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java"
      provides: "Activity arm + first-Content-implies-CHAT in doOnNext; status <span>; the .ai-agent-turn-activity block with per-turn Details (live + history-correlated); capped liveTurnSteps; unified loadTurnSteps(runId,cid); on-expand AiAuditEvent re-query (memoized); cleanup in all teardown sites"
      contains: "ai-agent-turn-activity"
    - path: "ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties"
      provides: "status + turn-detail msg:// keys (en) incl. chatView.status.neutral"
      contains: "chatView.status"
    - path: "ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties"
      provides: "status + turn-detail msg:// keys (vi) incl. chatView.status.neutral"
      contains: "chatView.status"
  key_links:
    - from: "ChatPanelFragment.submitChatTurn(...).doOnNext"
      to: "StreamingEvent.Activity / StreamingEvent.Content"
      via: "else if (evt instanceof StreamingEvent.Activity a) -> showStatus(statusKeyFor(a.kind())) [+ record a RETRIEVAL step]; on the FIRST Content of the turn -> showStatus(statusKeyFor(CHAT))"
      pattern: "StreamingEvent\\.Activity"
    - from: "ChatPanelFragment history-replay region (the AiMessage replay loop, ~line 563-595)"
      to: "AiAuditEvent (agentstore)"
      via: "after messageList.setItems(...), loadValues a grouped query: per CHAT root (parent is null, e.userUsername=:me, e.conversation.id=:cid, order by startedAt asc) its runId + a count of its children with parent is not null; if root-count == assistant-turn-count, zip in order and appendHistoryTurnDetails(runId, cid) for each root whose child-count>0; else render none"
      pattern: "AiAuditEvent"
    - from: "ChatPanelFragment per-turn Details (live OR history) on Final / on first expand"
      to: "AiAuditEvent (agentstore) TOOL/RETRIEVAL children for one runId"
      via: "loadTurnSteps(runId, cid) = unconstrainedDataManager.load(AiAuditEvent.class).query(... where e.userUsername=:me and e.conversation.id=:cid and e.runId=:rid and e.parent is not null order by e.startedAt asc).fetchPlan(narrow: kind,startedAt,finishedAt,latencyMs,outcome,errorClass).list(); memoized for history via a LOADED_KEY flag on the Details"
      pattern: "AiAuditEvent"
    - from: "status line + Details step rows"
      to: "TurnDetailRenderer"
      via: "label-only kind->msg:// mapping (no toolName/entity name input)"
      pattern: "TurnDetailRenderer"
---

<objective>
Render the two in-fragment observability surfaces inside the shared `ChatPanelFragment` per decisions D-06..D-08 (refined by review points #2/#3/#6/#7/#8/#12/#13): (1) an ephemeral KIND-keyed streaming-status `<span>` in a sibling slot of the message list (the existing NOTICE-row pattern), keyed by the new `StreamingEvent.Activity` event with first-`Content`-implies-CHAT sequencing, cleared entirely on every terminal/teardown site; (2) a collapsed-by-default Vaadin `Details` per completed turn with ≥1 tool call ("what the agent did — N steps · total ms" → label-only KIND-keyed step rows with per-step ms + error/rollback indicator), hidden entirely on zero-tool turns, rendered inside ONE per-turn-ordered activity block (`.ai-agent-turn-activity`) appended after `<vaadin-message-list>` — the only achievable rendering on the current `MessageList` substrate; driven for the just-completed turn by a lazy `AiAuditEvent`-by-`runId` read on `Final` (unified `loadTurnSteps` path — real `latencyMs`), and — for prior turns after a conversation-switch reload — by an additive correlation pass over the conversation's `AiAuditEvent` CHAT roots (ordered by `startedAt`, with each root's child count fetched up front, zipped against the replayed ASSISTANT turns only when the counts match) plus a lazy on-expand `loadTurnSteps`-by-`runId` read (memoized). Add the `Activity` `instanceof` branch + the first-`Content` status flip to `ChatPanelFragment.doOnNext`. Funnel both paths through one Vaadin-free `TurnDetailRenderer` mapper (`ActivityKind`/`kind` String + nullable `Long` ms + `AiToolCallOutcome` → `msg://` label rows; `statusKeyFor(ActivityKind)`; `neutralStatusKey()`) — structural no-leak. Add the `.ai-agent-status` rule + `@keyframes` pulse + `prefers-reduced-motion` guard + a minimal `.ai-agent-turn-activity` rule to the existing `ai-agent-chat.css` (which now loads via the `@CssImport` Plan 03 added to `ChatPanelFragment`). Add all new `msg://` keys (including `chatView.status.neutral`) to both locale bundles. The audit reads use `UnconstrainedDataManager` with a MANDATORY `where e.userUsername = :me and e.conversation.id = :cid` clause (RESEARCH Open Q1 — `AiAgentUserRole` has no `AiAuditEvent` policy); never load `AiAuditEvent` by `runId` alone unconstrained; confirm against `AiAuditEventListView` at execute time and record the choice in the SUMMARY.

Purpose: Delivers OBS-01 + OBS-02 + the bounded-state half of OBS-04. The status-row mechanism here is the one Phase 20's STT error/retry row will reuse — keep it generic. TEST-19 (Plan 05) asserts the rendered text never leaks; this plan must make that assertion pass by construction. The post-navigation disclosure (SPEC req 5 / CONTEXT D-07) is a hard acceptance criterion — the `AiAuditEvent`-by-`conversation.id` correlation pass is the in-budget additive way to satisfy it without refactoring history reconstruction.

Output: new `TurnDetailRenderer`; extended `ChatPanelFragment` (status `<span>`, the `.ai-agent-turn-activity` block with per-turn `Details` for live turns AND a history-correlation pass appending the same `Details` anchored by `runId`, capped `liveTurnSteps`, unified `loadTurnSteps` audit read, on-expand re-query memoization, `Activity` branch + first-`Content` flip, cleanup in all teardown sites); new `.ai-agent-status` + `.ai-agent-turn-activity` CSS; new status + turn-detail `msg://` keys (en + vi, incl. `chatView.status.neutral`); `TurnDetailRendererTest` (plain JUnit, Vaadin-free), `ChatPanelFragmentStatusLineTest` (`@UiTest`), `ChatPanelFragmentTurnDetailTest` (`@UiTest`), `ChatPanelFragmentTurnDetailHistoryTest` (`@UiTest` + seeded conversation + matching audit rows).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-SPEC.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-CONTEXT.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-PATTERNS.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-RESEARCH.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-REVIEWS.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-02-SUMMARY.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-03-SUMMARY.md

<interfaces>
<!-- VERIFIED CODE FACTS:
  - ChatPanelFragment substrate: `messageListSlot` is a `@ViewComponent private VerticalLayout messageListSlot;` from chat-panel-fragment.xml.
    Inside it the fragment does `messageList = new MessageList(); messageList.setMarkdown(true); ...; messageListSlot.add(messageList); messageList.setItems(items);`.
    Turns are MessageListItems in `private final List<MessageListItem> items`. NOTICE rows: `Element notice = new Element("div"); notice.getClassList().add("ai-agent-attachment-notice"); notice.setText(text); messageListSlot.getElement().appendChild(notice); messageCount++;`.
    appendIntentConfirmRow / appendActionChoiceRow build a Div/Span/Button row and do `messageListSlot.add(row)` (and one uses `actionChoiceRowsByProposalId` Map<UUID,Div> anchoring) — i.e. SIBLINGS of `<vaadin-message-list>`, appended AFTER it. There is NO way to put a sibling between two MessageListItems. ⇒ the per-turn Details go in ONE `.ai-agent-turn-activity` Div appended after `<vaadin-message-list>` (after `messageList.setItems(...)`), holding the per-turn `Details` in turn order.
  - appendIntentConfirmRow sets `row.getElement().setAttribute("role","status"); row.getElement().setAttribute("aria-live","polite");` — copy that for the status <span>.
  - submitChatTurn(...) builds `activeStream = source.doOnSubscribe(...).doOnNext(evt -> { if (evt instanceof StreamingEvent.Final f) { if (activeRunId == null) activeRunId = f.runId(); ... } ... (intent-confirm / action-choice / Content->botMsg.appendText(md) branches) ... StreamEventRenderer.RenderedStreamEvent rendered = StreamEventRenderer.renderStreamEventDetails(evt, labels, citationState); ... }).doOnError(err -> { accessUi(() -> { showGenericErrorNotification(); finishStreamInternal(); }); }).doOnComplete(() -> accessUi(this::finishStreamInternal)).subscribe();` — add an `else if (evt instanceof StreamingEvent.Activity a)` branch BEFORE the renderStreamEventDetails call (and `return`/skip the markdown path for Activity); add the first-Content flip in the existing Content branch.
  - finishStreamInternal(): re-enables input, clears the ProgressBar, `botMsg = null;` — add `removeStatusRow()` here.
  - clearMessageList(): `messageListSlot.removeAll(); messageListSlot.getElement().removeAllChildren(); messageList = new MessageList(); ...; messageListSlot.add(messageList); messageList.setItems(items); botMsg = null;` — add: clear `liveTurnSteps`, clear `turnDetailsByRunId`, null out the `.ai-agent-turn-activity` Div + `statusRow` (they were removed by removeAllChildren but the fields must be nulled so the next turn re-creates them).
  - The STOP path: the stop button (`@ViewComponent private JmixButton stopButton;`) handler calls `cancellationRegistry.cancel(...)` and then `finishStreamInternal()` (verify the exact wiring). The dispose-on-detach hook (Pitfall #8) disposes `activeStream` on fragment detach. Both must also clear `liveTurnSteps` + `statusRow` (finishStreamInternal already covers it once it gets the removeStatusRow/clear calls; confirm the stop and detach paths route through finishStreamInternal or add the cleanup there too).
  - hostView() = getParentController() cast to View<?> — fine, the fragment is hosted by ChatView / ChatDialogView / AiAgentSidebarView (all Jmix views).
  - ChatPanelFragment injects io.jmix.core.Messages (class-less messages.getMessage(key)), DataManager, CurrentAuthentication (currentAuthentication.getUser().getUsername() — used at the ownership check ~line 553), AiChatSessionState. ADD: io.jmix.core.UnconstrainedDataManager unconstrainedDataManager. activeRunId is a field set from StreamingEvent.Final.runId(). labels / citationState are existing locals/fields used by StreamEventRenderer.
  - HISTORY-REPLAY region ~lines 563-595 (inside setConversationIdInternal/loadConversation): `List<AiMessage> history = dataManager.load(AiMessage.class).query("select m from ai_AiMessage m where m.conversation.id = :cid order by m.seq asc, m.createdDate asc").parameter("cid", cid).list();` then `for (AiMessage m : history) { if role==USER -> items.add(buildItem(m,userName,USER_COLOR)); else if role==ASSISTANT -> items.add(buildItem(m,aiName,AI_COLOR)); else if role==NOTICE -> appendNoticeRow(m.getContent()); }` then `if (messageList != null) messageList.setItems(new ArrayList<>(items));`. AiMessage has NO runId — the disclosure runId comes from the AiAuditEvent CHAT-roots query, NOT from AiMessage.
  - AiAuditEvent (@Store("agentstore") @Entity(name="ai_AiAuditEvent")): id, version, parent (AiAuditEvent FK, parent is null = CHAT root), children (@Composition OneToMany mappedBy=parent), conversation (AiConversation FK), userUsername (String len 255), runId (UUID), kind (String len 16, "CHAT"/"TOOL"/"RETRIEVAL"), eventName/argumentsJson/resultSummary/queryText (do NOT read these — leak surface), outcome (String len 32, getOutcome()->AiToolCallOutcome via fromId, nullable), denialReason, errorClass (String len 255), promptHash, latencyMs (Long, nullable until run finishes), startedAt (OffsetDateTime, @NotNull), finishedAt (OffsetDateTime, nullable). Indexes exist on PARENT_ID, CONVERSATION_ID, STARTED_AT, RUN_ID.
  - AiAgentUserRole: @ResourceRole; NO @EntityPolicy on AiAuditEvent (Javadoc: "No policies on AiAuditEvent, AiParameters, or AiKnowledgeDocument — users have zero access"). @ViewPolicy lists AiAgent_Chat / AiAgent_ChatDialog / AiAgent_Conversation.* (and AiAgent_Sidebar after Plan 03). AiAgentAdminRole: @EntityPolicy(AiAuditEvent.class, ALL); the AiAuditEventListView is admin-only. ⇒ chat user reads AiAuditEvent only via UnconstrainedDataManager + mandatory userUsername+conversation.id filter.
  - Typed `unconstrainedDataManager.load(AiAuditEvent.class)...` infers the agentstore store. Raw-JPQL `loadValues(...)` on agentstore entities needs `.store("agentstore")` (project memory feedback_jmix_loadvalue_store) — so the grouped child-count query, if done via loadValues, must add `.store("agentstore")`.
  - AiToolCallOutcome — the outcome enum; it has an ERROR/rollback-flavoured value. Never render its raw name — map to a msg:// indicator key.
  - Vaadin Details: com.vaadin.flow.component.details.Details; `Details d = new Details(); d.setOpened(false); d.setSummaryText(MessageFormat.format(messages.getMessage("chatView.turnDetail.summary"), TurnDetailRenderer.summaryArgs(stepCount, totalMs))); d.add(buildStepRows(...)); d.addOpenedChangeListener(e -> { if (e.isOpened() && !Boolean.TRUE.equals(ComponentUtil.getData(d, LOADED_KEY))) lazyLoadHistorySteps(runId, cid, d); }); turnActivityBlock.add(d);` — Details is in Jmix's accepted Vaadin set. ComponentUtil.setData(d, LOADED_KEY, Boolean.TRUE) for the memoization flag.
  - CSS: ai-agent-chat.css now loads via @CssImport on ChatPanelFragment (Plan 03). Plan 03 added the .ai-agent-sidebar* rules — DO NOT re-add. Only add .ai-agent-status (+ @keyframes + prefers-reduced-motion) and a minimal .ai-agent-turn-activity rule.
  - Locale precedent: chatView.attachments.notice=... (en). Add chatView.status.chat / .tool / .retrieval / .neutral ; chatView.turnDetail.summary={0} steps · {1} ms (MessageFormat, integer args) ; chatView.turnDetail.step.tool / .step.retrieval ; chatView.turnDetail.errorIndicator ; chatView.turnDetail.unknownDuration (the "—" placeholder — or render the literal "—" inline; either way never "0 ms"). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the Vaadin-free TurnDetailRenderer label-only mapper</name>
  <files>ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/TurnDetailRenderer.java, ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/fragment/TurnDetailRendererTest.java</files>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/StreamEventRenderer.java (the model for a Vaadin-free, Spring-free, pure-static utility with a `labels`-map pull-through — mirror its shape)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/StreamingEvent.java (the `ActivityKind` enum from Plan 02)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallOutcome.java (the outcome enum — the ERROR/rollback value to map to an indicator key; never render its raw name)
    - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/RenderStreamEventTest.java (plain JUnit 5 + AssertJ — the model for `TurnDetailRendererTest`)
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-PATTERNS.md (section "NEW view/chat/fragment/TurnDetailRenderer.java" + "Pattern 4: One kind -> msg:// mapper")
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-RESEARCH.md (Pattern 4, Security Domain "Internal @Tool method name … leaking")
  </read_first>
  <behavior>
    - `TurnDetailRenderer.statusKeyFor(ActivityKind.CHAT)` returns `"chatView.status.chat"`, `TOOL` → `"chatView.status.tool"`, `RETRIEVAL` → `"chatView.status.retrieval"`; `neutralStatusKey()` returns `"chatView.status.neutral"`.
    - A step-row builder takes ONLY: a `kind` (the `ActivityKind` enum OR the audit `kind` String constrained to {"CHAT","TOOL","RETRIEVAL"}), a `latencyMs` (`Long` — NULLABLE; null/unknown means "no known duration"), and an `outcome` (`AiToolCallOutcome` or null) — and returns a structured `StepRow` carrying only msg:// keys + the `Long latencyMs` (nullable) + a boolean "errored" flag — NEVER a `toolName`, entity name, `argsJson`, `resultSummary`, or `queryText`. There is no method signature that accepts a tool/entity name. A `StepRow` with a null `latencyMs` is the signal to show an em-dash placeholder ("—"), never "0 ms".
    - The summary helper produces the `MessageFormat` arg tuple `(stepCount, totalMs)` for `chatView.turnDetail.summary`.
    - The renderer is Vaadin-free and Spring-free (no `@Component`, no Vaadin import) — testable with plain JUnit.
  </behavior>
  <action>
    Create `view/chat/fragment/TurnDetailRenderer.java`: a `final` class with private constructor and static methods, modeled on `StreamEventRenderer` (Vaadin-free, Spring-free). Provide: `static String statusKeyFor(StreamingEvent.ActivityKind kind)` returning `chatView.status.chat` / `.tool` / `.retrieval`; `static String neutralStatusKey()` returning `chatView.status.neutral`; a nested `record StepRow(String labelKey, Long latencyMs, boolean errored)` (latencyMs NULLABLE — keep it data-only); `static StepRow stepRow(StreamingEvent.ActivityKind kind, Long latencyMs, AiToolCallOutcome outcome)` and an overload `static StepRow stepRow(String auditKind, Long latencyMs, AiToolCallOutcome outcome)` that validates `auditKind ∈ {"CHAT","TOOL","RETRIEVAL"}` (throw `IllegalArgumentException` otherwise — defensive; the audit query already filters) and maps it to the same `labelKey` (`chatView.turnDetail.step.tool` for TOOL, `chatView.turnDetail.step.retrieval` for RETRIEVAL, and a sensible key for CHAT if it ever appears as a child — typically TOOL/RETRIEVAL are the only children) and `errored = outcome != null && outcome` is the ERROR/rollback value; `static String errorIndicatorKey()` returning `chatView.turnDetail.errorIndicator`; `static String unknownDurationKey()` returning `chatView.turnDetail.unknownDuration` (or expose the literal `"—"` — the contract is: a null-latency step renders the em-dash, never `0 ms` / `null ms`); `static Object[] summaryArgs(int stepCount, long totalMs)`. Crucially: NO public method takes a `String toolName` / entity name / free text — the input alphabet is the closed `ActivityKind` enum, the validated audit-`kind` String, the nullable `Long` ms, and the `AiToolCallOutcome` enum. Create `TurnDetailRendererTest.java` (plain JUnit 5 + AssertJ, mirror `RenderStreamEventTest`): assert `statusKeyFor` returns the right keys for every `ActivityKind`; assert `neutralStatusKey()` returns `chatView.status.neutral`; assert `stepRow(...)` for TOOL/RETRIEVAL produces the right `labelKey` and `errored` flag for an ERROR outcome vs a success outcome; assert `stepRow("BOGUS", ...)` throws; assert a `stepRow(...)` with a `null` latencyMs is recognized as "unknown duration" (the row exposes null latency; the ms-rendering helper returns the em-dash / `unknownDurationKey()`, NOT "0 ms"); assert no overload accepts a tool/entity-name argument (a reflective check that the public method param types are only `ActivityKind` / `String` (the validated audit-kind) / `Long` / `AiToolCallOutcome` / primitives).
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.fragment.TurnDetailRendererTest"</automated>
  </verify>
  <acceptance_criteria>
    - `TurnDetailRenderer.java` exists, is Vaadin-free + Spring-free, and exposes `statusKeyFor(ActivityKind)`, `neutralStatusKey()`, the step-row builder(s) (with a NULLABLE `Long latencyMs`), `errorIndicatorKey()`, an unknown-duration ("—") helper, and a summary-args helper — and no public method accepts a tool/entity name or free text.
    - `TurnDetailRendererTest` asserts the key mappings (incl. neutral), the ERROR-outcome flag, the bad-audit-kind rejection, the null-latency ⇒ em-dash (not "0 ms") contract, and the label-only input-alphabet contract; `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.fragment.TurnDetailRendererTest"` exits 0.
  </acceptance_criteria>
  <done>TurnDetailRenderer is a pure-fn label-only mapper with a closed input alphabet, a statusKeyFor + neutralStatusKey, and a null-latency ⇒ "—" contract; TurnDetailRendererTest green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the ephemeral KIND-keyed streaming-status line in ChatPanelFragment, with first-Content-implies-CHAT and full teardown cleanup</name>
  <files>ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java, ai-agent/ai-agent/src/main/resources/META-INF/resources/frontend/styles/ai-agent-chat.css, ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties, ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties, ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/fragment/ChatPanelFragmentStatusLineTest.java</files>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java (the file being modified — read: `appendNoticeRow`, `appendIntentConfirmRow` (the `role`/`aria-live` attribute pattern), `submitChatTurn(...).doOnNext/.doOnError/.doOnComplete`, the Content branch (`botMsg.appendText(md)`), `clearMessageList`, `finishStreamInternal`, the stop-button handler + `cancellationRegistry.cancel`, the dispose-on-detach hook, `accessUi`/`accessUiAuthenticated`, `activeRunId`, how `messageList`/`messageListSlot` are composed, the Messages injection)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/TurnDetailRenderer.java (Task 1 — `statusKeyFor` / `neutralStatusKey`)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/StreamingEvent.java (the `Activity` variant + `ActivityKind`, `Content`)
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties + messages_vi.properties (the `chatView.attachments.notice` precedent — add the status keys near it)
    - ai-agent/ai-agent/src/main/resources/META-INF/resources/frontend/styles/ai-agent-chat.css (the `.ai-agent-attachment-notice` style precedent — add `.ai-agent-status` + the `@keyframes` + `prefers-reduced-motion`; do NOT touch the `.ai-agent-sidebar*` rules Plan 03 added)
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-PATTERNS.md (sections "view/chat/fragment/ChatPanelFragment.java" (NOTICE-row sibling pattern), "META-INF/resources/frontend/styles/ai-agent-chat.css", "com/vn/agent/messages_*.properties")
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-RESEARCH.md (Pattern 2, Pitfall 3, Code Examples "status-line in ChatPanelFragment", Anti-Patterns "Concatenating status text into the assistant MessageListItem", Security Domain "HTML injection via a rendered label")
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-REVIEWS.md (points #6 — first-Content flip; #7 — teardown-site enumeration; #11 — neutral-status decision)
    - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/fragment/ChatPanelFragmentLoadingIndicatorTest.java (the model for `ChatPanelFragmentStatusLineTest`)
  </read_first>
  <behavior>
    - At turn start (in `.doOnSubscribe` or the first event), before any `Activity`/`Content`, the status `<span class="ai-agent-status" role="status" aria-live="polite">` shows the neutral text (`messages.getMessage(TurnDetailRenderer.neutralStatusKey())`), appended to `messageListSlot.getElement()` AFTER the `<vaadin-message-list>` block (a sibling, NOT inside any `MessageListItem`).
    - On `StreamingEvent.Activity(TOOL)` the same `<span>`'s text becomes `chatView.status.tool`; on `Activity(RETRIEVAL)` it becomes `chatView.status.retrieval`. On the FIRST `StreamingEvent.Content` event of the turn (regardless of any prior `Activity(RETRIEVAL)`), the text flips to `chatView.status.chat` ("thinking…") — Content arrival implies CHAT (review point #6). A LATER `Activity(...)` after content still wins (latest signal), but Content always implies at least CHAT.
    - On the terminal `StreamingEvent.Final` (or `StreamingEvent.Error`), and in `finishStreamInternal()`, and in `clearMessageList()` (conversation switch), and in the stop-button handler (after `cancellationRegistry.cancel`), and in the dispose-on-detach hook — the status `<span>` is removed from its parent and the field nulled. It is GONE in every teardown path, not blanked.
    - The assistant `MessageListItem` for the turn contains only the reply markdown — no status text ("thinking…" / "searching data…" / "retrieving documents…" / the neutral string) is ever appended into it.
    - The status text is HTML-escaped (`Element.setText(...)`) — no `innerHTML`, no markdown rendering on this element.
  </behavior>
  <action>
    In `ChatPanelFragment`: add fields `private com.vaadin.flow.dom.Element statusRow;` (null when no turn streaming) and `private boolean turnContentSeen;` (reset to false at turn start). Add `private void showStatus(String key)`: if `statusRow == null`, create `new com.vaadin.flow.dom.Element("span")`, `getClassList().add("ai-agent-status")`, `setAttribute("role", "status")`, `setAttribute("aria-live", "polite")`, `messageListSlot.getElement().appendChild(statusRow)`; then `statusRow.setText(messages.getMessage(key))`. Add `private void removeStatusRow()`: `if (statusRow != null) { statusRow.removeFromParent(); statusRow = null; }`. In `submitChatTurn(...)` at turn start (in `.doOnSubscribe` or before the flux runs): set `turnContentSeen = false;` and `accessUi(() -> showStatus(TurnDetailRenderer.neutralStatusKey()))`. In `.doOnNext(...)`: add `else if (evt instanceof StreamingEvent.Activity a) { accessUi(() -> showStatus(TurnDetailRenderer.statusKeyFor(a.kind()))); ... (Task 3 also records a RETRIEVAL step here) ... return; }` (place it BEFORE the `StreamEventRenderer.renderStreamEventDetails` call; `return`/skip the markdown path for an `Activity` event). In the existing `Content` branch (the one that does `botMsg.appendText(md)`): at the top, add `if (!turnContentSeen) { turnContentSeen = true; accessUi(() -> showStatus(TurnDetailRenderer.statusKeyFor(StreamingEvent.ActivityKind.CHAT))); }` — then the existing `botMsg.appendText(md)` runs unchanged (the status line is a SEPARATE sibling element — RESEARCH Anti-Pattern: never concatenate status into the bubble). In `.doOnError(...)` and `.doOnComplete(...)`: call `removeStatusRow()` (within the existing `accessUi(...)` wrapper). In `finishStreamInternal()`: call `removeStatusRow()`. In `clearMessageList()`: after `messageListSlot.getElement().removeAllChildren()`, also `statusRow = null;` (and `turnContentSeen = false;`) so the next turn re-creates the span. In the stop-button handler (after `cancellationRegistry.cancel(...)`) and the dispose-on-detach hook: ensure `removeStatusRow()` runs (if those paths already route through `finishStreamInternal()`, that suffices once `finishStreamInternal` calls `removeStatusRow()` — verify; if not, add the call). Append to `ai-agent-chat.css`: `.ai-agent-status { display: block; text-align: center; font-size: var(--lumo-font-size-s); color: var(--lumo-secondary-text-color); padding: var(--lumo-space-xs) 0; }` plus a subtle three-dot/pulse `@keyframes ai-agent-status-pulse { ... }` applied to it (or a `::after` content of animated dots) and `@media (prefers-reduced-motion: reduce) { .ai-agent-status { animation: none; } }`. Add to `messages_en.properties`: `chatView.status.chat=thinking…`, `chatView.status.tool=searching data…`, `chatView.status.retrieval=retrieving documents…`, `chatView.status.neutral=…` (a neutral typing-indicator string, e.g. `working…` or three dots) — and the Vietnamese equivalents to `messages_vi.properties` (e.g. `chatView.status.chat=đang suy nghĩ…`, `chatView.status.tool=đang tìm dữ liệu…`, `chatView.status.retrieval=đang truy xuất tài liệu…`, `chatView.status.neutral=đang xử lý…` — use natural vi). Create `ChatPanelFragmentStatusLineTest.java` (`@UiTest`, model on `ChatPanelFragmentLoadingIndicatorTest` + the chat-flow stub harness): drive a stub `Flux<StreamingEvent>` that emits, in order, an `Activity(RETRIEVAL)`, then a `Content`, then `Final`; assert: before the first event the `<span.ai-agent-status>` text is the `chatView.status.neutral` text (sibling of `<vaadin-message-list>` in `messageListSlot`, role/aria-live set); after `Activity(RETRIEVAL)` its text is `chatView.status.retrieval`; after the first `Content` its text flips to `chatView.status.chat` (the first-Content-implies-CHAT rule, point #6); after `Final` the `<span.ai-agent-status>` is gone; the assistant `MessageListItem` text equals the reply markdown only (no "retrieving documents…" / "thinking…" / neutral substring); also drive a second stub with `Activity(TOOL)` then `Final` and assert the span shows `chatView.status.tool` then is removed; also assert switching conversations (`clearMessageList()`) removes the span; also assert a turn that errors (`Error` event) removes the span.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.fragment.ChatPanelFragmentStatusLineTest"</automated>
  </verify>
  <acceptance_criteria>
    - `ChatPanelFragment.java` has a `statusRow` field, a `turnContentSeen` flag, `showStatus(...)` / `removeStatusRow()` helpers, an `else if (evt instanceof StreamingEvent.Activity a)` branch in `doOnNext` calling `showStatus(TurnDetailRenderer.statusKeyFor(...))`, a first-`Content` flip to `statusKeyFor(CHAT)` in the Content branch, a neutral-status call at turn start, and `removeStatusRow()` called in `.doOnComplete` / `.doOnError` / `finishStreamInternal()` / `clearMessageList()` (via field-null) / the stop path / the detach path.
    - `ai-agent-chat.css` contains a `.ai-agent-status` rule + a `@keyframes` for its pulse + a `@media (prefers-reduced-motion: reduce)` guard; the `.ai-agent-sidebar*` rules from Plan 03 are untouched.
    - `messages_en.properties` + `messages_vi.properties` each contain `chatView.status.chat`, `chatView.status.tool`, `chatView.status.retrieval`, `chatView.status.neutral`.
    - `ChatPanelFragmentStatusLineTest` asserts: neutral text before any event; KIND-keyed text on `Activity`; the first-`Content`-implies-CHAT flip; removal on `Final`, on `Error`, and on conversation switch; the assistant bubble has only reply markdown.
    - `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.fragment.ChatPanelFragmentStatusLineTest"` exits 0.
    - `git diff` shows no change to `chat-panel-fragment.xml` (no slot id touched) and no change to the existing `botMsg.appendText(md)` behavior.
  </acceptance_criteria>
  <done>The ephemeral KIND-keyed status line renders as a sibling in messageListSlot, shows neutral at start, flips to CHAT on the first Content (point #6), updates on Activity events, is removed in every teardown site (point #7), never leaks into the bubble; CSS + locale + test in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render the per-turn tool-detail Details as a grouped activity block — live turns + a tightened post-navigation AiAuditEvent correlation pass, with capped live state, a unified loadTurnSteps audit read, and a memoized on-expand re-query</name>
  <files>ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java, ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/fragment/ChatPanelFragmentTurnDetailTest.java, ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/fragment/ChatPanelFragmentTurnDetailHistoryTest.java</files>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java (the file being modified — read: `appendIntentConfirmRow` / `appendActionChoiceRow` (the `Div`+`Span`+`Button` sibling-row pattern, the `Map<UUID, ...>` anchoring like `actionChoiceRowsByProposalId`), `submitChatTurn(...).doOnNext/.doOnComplete/.doOnError`, the `ToolCall`/`ToolResult` handling (if any) on the live path, `activeRunId` (set from `StreamingEvent.Final.runId()`), `clearMessageList`, the `DataManager` injection + the new `UnconstrainedDataManager` from Task 2's sibling task, `accessUi`, the ownership check ~line 553 (`currentAuthentication.getUser().getUsername()`), AND — load-bearing — the HISTORY-REPLAY region ~lines 563-595: the `dataManager.load(AiMessage.class).query("...where m.conversation.id = :cid order by m.seq asc...")` load, the `for (AiMessage m : history)` loop, the `buildItem(m, name, color)` calls, the `items` list, and `messageList.setItems(new ArrayList<>(items))`)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/TurnDetailRenderer.java (Task 1 — the step-row builder + `summaryArgs` + `errorIndicatorKey` + the null-latency ⇒ "—" contract)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/StreamingEvent.java (`ToolCall` (toolCallId, toolName, argsJson), `ToolResult` (toolCallId, toolName, summary, outcome, payloadJson), `Activity(RETRIEVAL)` — the live-turn step signals; do NOT read `toolName`/`argsJson`/`summary`/`payloadJson` for the disclosure — leak surface)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiAuditEvent.java (the @Store("agentstore") entity — `runId`, `conversation`, `userUsername`, `kind`, `startedAt`, `finishedAt`, `latencyMs` (nullable), `getOutcome()`, `errorClass`, `parent`, `children`; CHAT root = `parent is null`, TOOL/RETRIEVAL children = `parent is not null`)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/audit/AiAuditEventListView.java + the matching `ai-audit-event-list-view.xml` + ai-agent/ai-agent/src/main/java/com/vn/agent/security/AiAgentUserRole.java + AiAgentAdminRole.java (the read precedent + the roles — confirm: the ListView is an ADMIN view with full AiAuditEvent EntityPolicy; AiAgentUserRole has NO AiAuditEvent policy ⇒ the fragment read MUST use UnconstrainedDataManager with a mandatory `where e.userUsername = :me and e.conversation.id = :cid` clause, never `runId`-only unconstrained; RESEARCH Open Q1)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/AiChatSessionState.java (confirm it stays at `currentConversationId` + listeners — the bounded step list does NOT go here)
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-PATTERNS.md (sections "view/chat/fragment/ChatPanelFragment.java" (Per-turn Details sibling, On-expand history-turn read), "Agentstore typed reads", "No Analog Found" → the on-expand read guidance; the history-replay loop region)
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-RESEARCH.md (Pitfall 4, Pitfall 8, Open Question 1, Validation Architecture `ChatPanelFragmentTurnDetailHistoryTest` row, Code Examples, Anti-Patterns "Re-querying AiAuditEvent … on every render" / "Putting the per-turn step accumulator in AiChatSessionState", Security Domain "Cross-user audit-row exposure via the on-expand query")
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-REVIEWS.md (points #2 — grouped activity block instead of per-MessageListItem sibling; #3 — child-counts-up-front + no-guess-on-mismatch; #8 — lazy-load on Final for real timings; #12 — liveTurnSteps cap; #13 — query-count-spy mechanism)
    - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/fragment/ (existing fragment @UiTest harness — model `ChatPanelFragmentTurnDetailTest` / `...HistoryTest` on it)
  </read_first>
  <behavior>
    - **The activity block.** A single container `Div` with class `ai-agent-turn-activity` is appended to `messageListSlot` AFTER `<vaadin-message-list>` (created lazily on first use; held in a field). Per-turn `Details` are added INTO that block in turn order (newest last). This is the only achievable rendering on the `MessageList` substrate (review point #2) — a per-turn `Details` cannot be inserted between two `MessageListItem`s.
    - **Live turn.** During a streaming turn, `ToolCall` / `ToolResult` / `Activity(RETRIEVAL)` events accumulate into a per-fragment-instance step list capped at 50 entries (dedupe `ToolCall`/`ToolResult` by `toolCallId`); a step records its transient ToolCall→ToolResult arrival-delta ms as a *fallback only*; a live RETRIEVAL step records a NULL latencyMs. On the terminal `Final` of the turn, IF `activeRunId != null`: call `loadTurnSteps(activeRunId, conversationId)` (the unified `UnconstrainedDataManager` `AiAuditEvent`-by-`runId` read — see below) to get the REAL per-step `latencyMs`/`outcome`; if that read returns ≥1 child, build the `Details` from it (real timings); if it returns nothing, fall back to the transient `liveTurnSteps` (arrival-delta ms / em-dash). If the resulting step count > 0, append a collapsed `Details` (`setOpened(false)`, `summary` = `MessageFormat.format(messages.getMessage("chatView.turnDetail.summary"), TurnDetailRenderer.summaryArgs(stepCount, totalMs))`, content = a `VerticalLayout` of label-only step rows via `TurnDetailRenderer` — each: `messages.getMessage(step.labelKey())` + the per-step ms text (the em-dash "—" if `step.latencyMs()` is null) + `messages.getMessage(TurnDetailRenderer.errorIndicatorKey())` iff `step.errored()`) into the `.ai-agent-turn-activity` block, anchored by `activeRunId` in `turnDetailsByRunId` (a `Map<UUID, Details>`). Then `liveTurnSteps.clear()`. A completed turn whose `loadTurnSteps` (and fallback) yield zero steps produces NO `Details` (no "0 steps" row, no placeholder).
    - **On `.doOnError`**: `liveTurnSteps.clear()` (no `Details` on an errored turn unless steps actually ran and the audit read confirms them — if `loadTurnSteps` returns ≥1 child you MAY still render the `Details`, but at minimum clear the list and never throw).
    - **Post-navigation / conversation-switch reload (SPEC req 5 / CONTEXT D-07).** After the history-replay loop replays the conversation's `AiMessage`s and calls `messageList.setItems(...)`, the fragment runs ONE additive correlation step:
      (a) load, per CHAT root of the conversation, its `runId` AND its tool/retrieval child count — via `loadValues` of a grouped query against the agentstore: `select e.runId, count(c) from ai_AiAuditEvent e left join ai_AiAuditEvent c on c.parent = e where e.userUsername = :me and e.conversation.id = :cid and e.parent is null group by e.runId, e.startedAt order by e.startedAt asc` with `.store("agentstore")` (raw-JPQL loadValues does NOT infer the agentstore store — project memory), parameters `me` = `currentAuthentication.getUser().getUsername()`, `cid` = the conversation id. (If a `left join` on a `@Composition` self-relation is awkward in JPQL, an equivalent two-query approach is acceptable: one `loadValues` of `select e.runId from ... where ... and e.parent is null order by e.startedAt asc` and one `loadValues` of `select e.runId, count(e) from ... where ... and e.parent is not null group by e.runId`, then merge — same net result. State which form you used in the SUMMARY.) This yields, in turn order: the conversation's CHAT-root `runId`s and, for each, how many TOOL/RETRIEVAL children it has.
      (b) Count the replayed ASSISTANT turns in `history`/`items`. If `assistantTurnCount == chatRootCount`: zip the ordered `runId` list against the ASSISTANT turns 1:1; for each pair whose child-count > 0, append a collapsed `Details` anchored by that `runId` into the `.ai-agent-turn-activity` block (via `appendHistoryTurnDetails(runId, cid)` — see below); for each pair whose child-count == 0, append NOTHING (no placeholder). If `assistantTurnCount != chatRootCount`: render NO history disclosures for this conversation (`log.debug("turn-detail correlation skipped: {} assistant turns vs {} audit roots", ...)`); never throw, never guess. This is an ADDITIVE correlation pass over existing query paths — NOT a refactor of history reconstruction; `AiMessage` still has no `runId` and is not modified.
    - **`appendHistoryTurnDetails(runId, cid)`**: build a collapsed `Details` (`setOpened(false)`, a placeholder summary text — but ONLY because we already know child-count > 0, so this is a real disclosure, never shown for a zero-child turn) whose `addOpenedChangeListener` — guarded by `!Boolean.TRUE.equals(ComponentUtil.getData(d, LOADED_KEY))` — calls `loadTurnSteps(runId, cid)`, sets the summary to `MessageFormat.format(messages.getMessage("chatView.turnDetail.summary"), TurnDetailRenderer.summaryArgs(stepCount, totalMs))`, populates the content `VerticalLayout` with label-only rows (ms rendered from the real `latencyMs`, em-dash for any null), sets `ComponentUtil.setData(d, LOADED_KEY, Boolean.TRUE)` so a second expand/collapse does NOT re-query; if (defensively) `loadTurnSteps` returns zero children (shouldn't, since we filtered on child-count > 0 — but a row could have been deleted between the two reads), REMOVE the `Details` from the block. Add it to `turnActivityBlock`, put it in `turnDetailsByRunId`.
    - **`loadTurnSteps(UUID runId, UUID cid)`** (the unified live + history audit read): `return unconstrainedDataManager.load(AiAuditEvent.class).query("select e from ai_AiAuditEvent e where e.userUsername = :me and e.conversation.id = :cid and e.runId = :rid and e.parent is not null order by e.startedAt asc").parameter("me", currentAuthentication.getUser().getUsername()).parameter("cid", cid).parameter("rid", runId).fetchPlan(fp -> { fp.add("kind"); fp.add("startedAt"); fp.add("finishedAt"); fp.add("latencyMs"); fp.add("outcome"); fp.add("errorClass"); }).list();` then `.stream().map(e -> TurnDetailRenderer.stepRow(e.getKind(), e.getLatencyMs(), e.getOutcome())).toList()` — the narrow fetch plan deliberately omits `eventName`/`argumentsJson`/`resultSummary`/`queryText` (no name columns, no LOBs); the mandatory `userUsername`+`conversation.id` filter is the row-level-access guard (never `runId`-only unconstrained). The conversation itself was already ownership-checked at ~line 553.
    - **`clearMessageList()`**: also clears `turnDetailsByRunId`, clears `liveTurnSteps`, nulls the `turnActivityBlock` Div field (the `removeAllChildren()` already detaches it).
    - **No leak.** The `Details` summary uses `MessageFormat` with integer args only — no tool/entity name is interpolated; the step-row labels come exclusively from `TurnDetailRenderer` keys.
    - Confirm at execute time against `AiAuditEventListView` + the `agentstore` roles whether a constrained `DataManager` read is actually permitted for the chat user; the analysis says NO (no `AiAuditEvent` policy on `AiAgentUserRole`), so `UnconstrainedDataManager` + the mandatory filter is the path — record the confirmed choice + rationale in the SUMMARY.
  </behavior>
  <action>
    In `ChatPanelFragment`: inject `io.jmix.core.UnconstrainedDataManager unconstrainedDataManager` (alongside the existing `DataManager`). Add fields: `private static final int LIVE_TURN_STEP_CAP = 50;` ; `private final java.util.List<TurnStep> liveTurnSteps = new java.util.ArrayList<>();` where `TurnStep` is a small nested `record TurnStep(String labelKey, Long latencyMs, boolean errored, java.util.UUID toolCallId)` (or wrap `TurnDetailRenderer.StepRow` + a transient toolCallId — keep it bounded; add a step only if `liveTurnSteps.size() < LIVE_TURN_STEP_CAP`) ; `private final java.util.Map<java.util.UUID, com.vaadin.flow.component.details.Details> turnDetailsByRunId = new java.util.HashMap<>();` ; `private com.vaadin.flow.component.html.Div turnActivityBlock;` ; `private static final String LOADED_KEY = "...";`. Add `private com.vaadin.flow.component.html.Div turnActivityBlock()`: if null, `turnActivityBlock = new Div(); turnActivityBlock.addClassName("ai-agent-turn-activity"); messageListSlot.add(turnActivityBlock);` then return it. In `submitChatTurn(...).doOnNext(...)`: on `StreamingEvent.ToolCall tc` add a "started" step keyed by `tc.toolCallId()` (note the start time; do NOT store `tc.toolName()`/`tc.argsJson()`) — only if under the cap; on `StreamingEvent.ToolResult tr` finish that step (dedupe by `tr.toolCallId()`) recording `tr.outcome()` and the arrival-delta ms as a fallback (do NOT store `tr.toolName()`/`tr.summary()`/`tr.payloadJson()`); on `StreamingEvent.Activity a` where `a.kind() == RETRIEVAL` add a RETRIEVAL step with a NULL latencyMs (under the cap). (These are added alongside the status-line `Activity` branch from Task 2 — that branch already `return`s; for `Activity(RETRIEVAL)` record the step before/within the `return`.) In `.doOnComplete(() -> accessUi(() -> { ... }))`: if `activeRunId != null`, `List<TurnDetailRenderer.StepRow> steps = loadTurnSteps(activeRunId, conversationId);` ; if `steps.isEmpty()` build `steps` from `liveTurnSteps` (the arrival-delta / em-dash fallback) ; if `!steps.isEmpty()` call `appendTurnDetails(activeRunId, steps)` ; then `liveTurnSteps.clear()` ; then the existing `finishStreamInternal()`. In `.doOnError(...)`: `liveTurnSteps.clear()` (and optionally still `loadTurnSteps`+`appendTurnDetails` if it returns ≥1 child — Claude's Discretion; never throw). Add `private void appendTurnDetails(UUID runId, List<TurnDetailRenderer.StepRow> steps)`: compute `stepCount`/`totalMs` (sum of non-null `latencyMs`), build a `Details` (`setOpened(false)`, summary via `MessageFormat`+`summaryArgs`, content = `VerticalLayout` of label-only rows via `TurnDetailRenderer` — render per-step ms as the em-dash when null, never "0 ms"), `turnActivityBlock().add(d)`, `turnDetailsByRunId.put(runId, d)`. Add `private void appendHistoryTurnDetails(UUID runId, UUID conversationId)`: build a collapsed `Details` (we already know child-count > 0) whose `addOpenedChangeListener` — guarded by `!Boolean.TRUE.equals(ComponentUtil.getData(d, LOADED_KEY))` — calls `loadTurnSteps(runId, conversationId)`, sets the summary + content as in `appendTurnDetails`, sets the `LOADED_KEY` flag, and removes the `Details` if (defensively) zero children come back; `turnActivityBlock().add(d)`, `turnDetailsByRunId.put(runId, d)`. Add `private List<TurnDetailRenderer.StepRow> loadTurnSteps(UUID runId, UUID conversationId)` exactly as in `<behavior>` (UnconstrainedDataManager, narrow fetch plan, mandatory `userUsername`+`conversation.id`+`runId` filter, map rows through `TurnDetailRenderer.stepRow(kind, latencyMs, outcome)`). **In the HISTORY-REPLAY region (~lines 563-595):** after `messageList.setItems(...)`, do the additive correlation: load the CHAT roots' `(runId, childCount)` pairs in `startedAt` order via the grouped `loadValues` query in `<behavior>` (with `.store("agentstore")`); count the ASSISTANT turns in `history`; if equal, zip 1:1 and for each pair with `childCount > 0` call `appendHistoryTurnDetails(runId, cid)` (skip `childCount == 0` pairs entirely — no placeholder); if not equal, render none and `log.debug(...)`; never throw. Make `clearMessageList()` also clear `turnDetailsByRunId`, clear `liveTurnSteps`, and null `turnActivityBlock`. Do NOT modify `AiMessage.java`, `AiAuditEvent.java`, `chat-panel-fragment.xml`, or `AiAuditEventListView`. Create `ChatPanelFragmentTurnDetailTest.java` (`@UiTest`): drive a stub stream emitting two `ToolCall`/`ToolResult` pairs (one OK outcome, one ERROR outcome) + an `Activity(RETRIEVAL)` + a `Final` for a known `runId`; the test must also stand up matching `AiAuditEvent` rows for that `runId` (CHAT root + 3 children: TOOL with `latencyMs` set, TOOL ERROR with `latencyMs` set, RETRIEVAL with `latencyMs` = null) so the `loadTurnSteps`-on-`Final` path returns real timings — assert a `<details opened="false">` appears inside the `.ai-agent-turn-activity` block (a sibling Div after `<vaadin-message-list>`, NOT between MessageListItems) with summary text matching `chatView.turnDetail.summary` formatted with 3 steps + the summed ms, expanding to 3 label-only rows (the two TOOL rows show their resolved `chatView.turnDetail.step.tool` label + a real ms value; the ERROR row also shows `chatView.turnDetail.errorIndicator` text; the RETRIEVAL row shows `chatView.turnDetail.step.retrieval` + the em-dash "—" (NOT "0 ms"/"null ms"); no row contains a `toolName`/entity name); drive a second stub with NO `ToolCall`/`ToolResult` + `Final` and NO audit children and assert NO `<details>` is added; assert after a turn the next turn's `liveTurnSteps` starts empty (bounded/cleared). Create `ChatPanelFragmentTurnDetailHistoryTest.java` (`@UiTest` + seeded conversation + matching audit rows in agentstore): seed an `AiConversation` (owned by the test user) with N ASSISTANT `AiMessage`s (plus their USER turns) AND N matching `AiAuditEvent` CHAT roots (`parent is null`, `conversation` = the seeded conversation, `userUsername` = the test user, `startedAt` in turn order); make some CHAT roots have ≥1 TOOL/RETRIEVAL child (`parent` = the root, `latencyMs` set on TOOL, null on at least one RETRIEVAL child) and at least one CHAT root with zero children; open the fragment for that conversation (a fresh instance / a reload triggering the history-replay path); assert: each prior ASSISTANT turn whose CHAT root has ≥1 child shows a collapsed `<details opened="false">` anchored by the right `runId` inside the `.ai-agent-turn-activity` block; the zero-child prior turn shows NO `<details>` (no placeholder); expand one `Details` → assert the content `VerticalLayout` got the seeded children's count of label-only rows whose label/ms match the seeded `kind`/`latencyMs`, the null-`latencyMs` child renders "—", and exactly ONE `AiAuditEvent` `loadTurnSteps` query happened for that `runId` (wire a delegating test-double `UnconstrainedDataManager` around the real bean that increments an `AtomicInteger` on `load(AiAuditEvent.class)` calls — point #13; if a test-double can't be wired into the `@UiTest` harness, assert the rendered row content is byte-stable across re-expands as a fallback); collapse + re-expand → assert the counter is still 1 (the `LOADED_KEY` flag short-circuits). Also add a count-mismatch case: seed N ASSISTANT turns but only N-1 CHAT roots → assert NO `<details>` rendered anywhere (the no-guess rule, point #3) and no exception.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.fragment.ChatPanelFragmentTurnDetailTest" --tests "com.vn.agent.view.chat.fragment.ChatPanelFragmentTurnDetailHistoryTest"</automated>
  </verify>
  <acceptance_criteria>
    - `ChatPanelFragment.java` injects `UnconstrainedDataManager`, has a `LIVE_TURN_STEP_CAP = 50` capped `liveTurnSteps` list (NOT in `AiChatSessionState`), a `turnDetailsByRunId` map, a `turnActivityBlock()` lazy-creating a `Div.ai-agent-turn-activity` appended after `<vaadin-message-list>`, an `appendTurnDetails(...)` that creates a collapsed `Details` (`MessageFormat` summary + label-only step rows; em-dash for null ms, never "0 ms"), an `appendHistoryTurnDetails(UUID runId, UUID cid)` whose `addOpenedChangeListener` does a MEMOIZED `loadTurnSteps` call (LOADED_KEY flag), a `loadTurnSteps(UUID, UUID)` doing the `UnconstrainedDataManager` `AiAuditEvent`-by-`runId` read with a mandatory `where e.userUsername = :me and e.conversation.id = :cid` clause + a narrow fetch plan (no name columns/LOBs), and `liveTurnSteps.clear()` on `.doOnComplete`/`.doOnError`; `.doOnComplete` calls `loadTurnSteps(activeRunId, ...)` to get real timings (falling back to `liveTurnSteps` only if empty) before `appendTurnDetails`; the HISTORY-REPLAY region gains an ADDITIVE post-`setItems` correlation pass that loads the CHAT roots' `(runId, childCount)` pairs in `startedAt` order (raw-JPQL `loadValues` with `.store("agentstore")`), zips 1:1 against the ASSISTANT turns ONLY when the counts match (else renders none + `log.debug`, never throws), and calls `appendHistoryTurnDetails(runId, cid)` only for pairs with `childCount > 0`; `clearMessageList()` clears the map + list + nulls `turnActivityBlock`; `AiMessage.java` is NOT modified.
    - `ChatPanelFragmentTurnDetailTest` asserts: a ≥1-step turn → a collapsed `<details>` inside `.ai-agent-turn-activity` (a sibling Div after `<vaadin-message-list>`, never between MessageListItems) with the N-step·ms summary, expanding to N label-only rows with per-step ms (real values from the audit children) + an ERROR indicator on the errored row + the em-dash "—" (not "0 ms") on the null-latency RETRIEVAL row, no tool/entity name anywhere; a 0-step turn → no `<details>`; the live step list is cleared after the turn.
    - `ChatPanelFragmentTurnDetailHistoryTest` asserts: open the fragment for a seeded conversation (fresh instance / reload) ⇒ each prior ASSISTANT turn whose CHAT root has ≥1 child shows a collapsed `Details` anchored by `runId` in the `.ai-agent-turn-activity` block; a zero-child prior turn shows no `Details` (no placeholder); expanding one triggers exactly one `loadTurnSteps` query for that `runId` (via the `UnconstrainedDataManager`, `userUsername`+`conversation.id`+`runId` filtered — never `runId`-only unconstrained) and renders label-only rows (real ms on TOOL, em-dash on the null-`latencyMs` child); re-expanding does NOT re-query (counter stays 1 / content byte-stable); a count-mismatch conversation (N assistant turns vs N-1 audit roots) renders NO `Details` anywhere and throws nothing.
    - `AiChatSessionState.java` is unchanged (still `currentConversationId` + listeners) — verified by `git diff` and (in Plan 05) `AiChatSessionStateTest`.
    - `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.fragment.ChatPanelFragmentTurnDetailTest" --tests "com.vn.agent.view.chat.fragment.ChatPanelFragmentTurnDetailHistoryTest"` exits 0.
    - No new `@Entity`/`@Table`/Liquibase changelog; `git diff` shows no change to `AiAuditEvent.java`, `AiMessage.java`, `chat-panel-fragment.xml`, or `AiAuditEventListView`.
    - The SUMMARY records: the confirmed constrained-vs-unconstrained decision for the `AiAuditEvent` reads + rationale (the `AiAgentUserRole`-has-no-`AiAuditEvent`-policy fact + how `AiAuditEventListView` was checked), the exact correlation query form used (single grouped `left join` vs two-query merge) + the zip/no-guess semantics, the `liveTurnSteps` cap, the unified `loadTurnSteps`-on-`Final` decision for real live timings (review point #8), the em-dash unknown-duration mechanism, the grouped activity-block rendering (review point #2 — why per-`MessageListItem` siblings are impossible), the query-count-spy mechanism used in the history test (review point #13), and all new `msg://` key names.
  </acceptance_criteria>
  <done>Per-turn collapsed Details renders inside one ordered .ai-agent-turn-activity block for ≥1-tool turns with label-only steps + per-step ms (real from AiAuditEvent, em-dash for unknown) + error indicator; hidden on zero-tool turns (no placeholder); live state capped+cleared; on Final the live disclosure reads the runId's audit children for real timings (unified loadTurnSteps); after a conversation-switch reload an additive correlation pass with child-counts-up-front appends the same Details anchored by runId only when the assistant/root counts match (else none, no guess), and expanding lazily + memoized-ly re-reads that runId's children respecting row-level access; no new persisted state; AiMessage untouched; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| streaming flux (server) → rendered status line / Details (browser) | Only the closed `ActivityKind` enum + server-side timing/outcome cross; no user free-text, no `toolName`/`argsJson`/`resultSummary`/`payloadJson`/`queryText` |
| `AiAuditEvent` (agentstore, per-user rows) → on-expand + history-correlation + live-Final fragment reads | A user must only read their own conversation's audit rows (the reads carry a mandatory `userUsername` + `conversation.id` filter; `AiAgentUserRole` has no `AiAuditEvent` policy so the constrained `DataManager` is not an option) |
| user input ↔ the new rendering | The only "input" is the closed `ActivityKind` enum from the flux and the server-originated `runId` UUID — no user text reaches the new elements |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-D1 | Information disclosure | internal `@Tool` method name / raw host-entity name leaking into the streaming-status line or the per-turn `Details` (the headline threat for this phase) | mitigate | Closed `ActivityKind` enum + `kind`-keyed `msg://` mapper (`TurnDetailRenderer`) — the rendering path's input alphabet structurally cannot contain a tool/entity name; never pass `StreamingEvent.ToolCall.toolName()`/`argsJson()` / `StreamingEvent.ToolResult.toolName()`/`summary()`/`payloadJson()` / `AiAuditEvent.eventName`/`argumentsJson`/`resultSummary`/`queryText` into a rendered label; the audit reads' fetch plan selects only `kind`/`startedAt`/`finishedAt`/`latencyMs`/`outcome`/`errorClass` (and the correlation `loadValues` selects only `runId` + a count) — no name columns, no LOBs. TEST-19 (Plan 05) enforces with the Phase 9 leak regexes against the rendered text. |
| T-15-D2 | Tampering / XSS | HTML injection via a rendered status/disclosure label | mitigate | Use Vaadin `Element.setText(...)` / `Span(text)` (HTML-escaped) — never `setProperty("innerHTML", ...)` or `MarkdownRenderer` on the `<span>`/`Details` rows; the `Details` summary uses `MessageFormat` with integer args only; the NOTICE-row precedent (`appendNoticeRow` uses `notice.setText(text)`) is the model. |
| T-15-D3 | Elevation of privilege / Information disclosure | cross-user `AiAuditEvent` exposure via the on-expand query, the live-Final read, or the history-correlation pass | mitigate | `AiAgentUserRole` has NO `AiAuditEvent` policy (its Javadoc says so), so the constrained `DataManager` cannot read it — the fragment uses `UnconstrainedDataManager` with a MANDATORY `where e.userUsername = :currentUser and e.conversation.id = :conversationId` clause on ALL three reads (the live-Final `loadTurnSteps`, the on-expand `loadTurnSteps`, and the grouped CHAT-roots/child-count correlation `loadValues`); NEVER load `AiAuditEvent` by `runId` alone unconstrained; the conversation itself was already ownership-checked (`currentAuthentication.getUser().getUsername()` at ~line 553). Decision confirmed against `AiAuditEventListView`'s read path (an admin-only view with full `AiAuditEvent` `EntityPolicy`). |
| T-15-D4 | Denial of service / resource exhaustion | unbounded per-turn step accumulation in session state, or N DB round-trips for N expand clicks, or many round-trips on a fresh navigation | mitigate | The live-turn step list is per-`ChatPanelFragment`-instance (NOT `AiChatSessionState`), capped at 50 entries, cleared on `Final`/`Error` and every teardown site (OBS-04); the history-correlation pass is ONE grouped `loadValues` query per conversation switch (fetches only `runId` + a count); each disclosure reads its `runId`'s children at most ONCE — the live disclosure on `Final` (and that result is reused, not re-fetched on expand of the same live `Details` — set `LOADED_KEY` there too), the history disclosure on first expand (memoized via `LOADED_KEY`) — re-expand/collapse is a no-op. Tested in `ChatPanelFragmentTurnDetailTest` / `...HistoryTest`. |
| T-15-D5 | Tampering | new persisted "turn" state / parallel store | mitigate (avoid) | No new `@Entity`/`@Table`/Liquibase changelog; the disclosure is driven by the existing `StreamingEvent` flux (live signals) and the existing `AiAuditEvent` tree (real timings, history); `AiMessage` is read-only here (not modified — the `runId` comes from `AiAuditEvent`, not a new `AiMessage` column); the `Details` open state is component-local (session-only by construction). Verified by `git diff` (no new entity, no `AiMessage` change) + Plan 05's `NoNewPersistedStateTest`. |
</threat_model>

<verification>
- `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.fragment.*"` green (the fragment package — renderer, status line, turn detail, history).
- `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.*"` green (no chat-flow regression).
- `./gradlew :ai-agent:ai-agent:test` (full add-on module) green.
- `git diff --stat` shows no change to `chat-panel-fragment.xml`, `AiAuditEvent.java`, `AiMessage.java`, `AiAuditEventListView.java`, `AiChatSessionState.java`; no new file under `**/liquibase/**`.
- Manual UI verification (app on http://localhost:8088 — do NOT auto-start): during a turn with a tool call, a centered ephemeral status line shows neutral → "searching data…" / "retrieving documents…" → "thinking…" (on first content) and the final answer bubble has no leftover status prose; after the turn a collapsed "what the agent did — N steps · total ms" disclosure appears in the activity block below the message list, expanding to label-only rows with per-step ms (real values, em-dash for a RETRIEVAL step with no recorded latency); a chat-only turn (no tool call) shows no disclosure; navigating away and back (or switching conversation and back), each prior tool-using turn shows a collapsed disclosure, expanding it re-reads from `AiAuditEvent` and shows the rows, re-collapsing/re-expanding does not re-query; a chat-only prior turn shows no disclosure; a conversation whose assistant-turn count doesn't match its audit-root count shows no disclosures (no crash).
</verification>

<success_criteria>
- OBS-01 met: ephemeral KIND-keyed status line in a sibling slot (neutral at start, CHAT on first content, TOOL/RETRIEVAL on the matching Activity), cleared on every terminal/teardown site, never in the bubble, never internal names, `msg://` in en + vi (incl. `chatView.status.neutral`).
- OBS-02 met (deep-link descoped): collapsed-by-default per-turn label-only disclosure (rendered in one ordered `.ai-agent-turn-activity` block — the achievable rendering on the `MessageList` substrate) with per-step ms (real from `AiAuditEvent`; em-dash for unknown-duration, never "0 ms") + error/rollback indicator, hidden on zero-tool turns (no placeholder), session-only expand state, driven by the flux (live signals) + a child-count-aware `AiAuditEvent`-by-`conversation.id` correlation pass + lazy memoized `AiAuditEvent`-by-`runId` re-read (history) — disclosure works after a fresh navigation / conversation-switch reload; no `?runId=` link.
- OBS-04 (bounded-state half) met: no new persisted state; `AiMessage` not modified; per-turn detail capped (50) per-fragment, cleared on every terminal/teardown site; `AiChatSessionState` unchanged.
- One `kind → msg://` mapper (`TurnDetailRenderer`) with a closed input alphabet — TEST-19 passes by construction.
- `AiAuditEvent` reads respect row-level access (`UnconstrainedDataManager` + mandatory `userUsername` + `conversation.id` filter; never `runId`-only).
- Existing surfaces + slot ids unchanged; all tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-04-SUMMARY.md` recording: the confirmed constrained-vs-unconstrained `AiAuditEvent`-read decision + rationale, the exact correlation query form (single grouped `left join` vs two-query merge) + zip/no-guess semantics, the `liveTurnSteps` cap, the unified `loadTurnSteps`-on-`Final` decision (real live timings — review point #8), the em-dash unknown-duration mechanism, the grouped activity-block rendering (review point #2), the neutral-status behavior + the first-`Content`-implies-CHAT sequencing (points #6/#11), the teardown-site cleanup list (point #7), the query-count-spy mechanism (point #13), and all new `msg://` key names.
</output>
