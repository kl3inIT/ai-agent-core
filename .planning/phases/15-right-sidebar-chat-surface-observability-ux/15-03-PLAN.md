---
phase: 15-right-sidebar-chat-surface-observability-ux
plan: 03
type: execute
wave: 2
depends_on: [15-01-PLAN.md]
files_modified:
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/ChatSurfaceMounter.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/AiAgentSidebarView.java
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/ai-agent-sidebar-view.xml
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/AiChatUIState.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java
  - ai-agent/ai-agent/src/main/resources/META-INF/resources/frontend/styles/ai-agent-chat.css
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/security/AiAgentUserRole.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/ChatSurfaceMounterTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/ChatDialogViewTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/ChatPanelFragmentSurfaceSwitchTest.java
autonomous: true
requirements: [SURF-11]

# REVIEW RESOLUTION (15-REVIEWS.md):
# - Point #1 (Codex HIGH) — VERIFIED against the code: there is NO single physical shared ChatPanelFragment.
#   ChatView (FULL_ROUTE) has its own `@ViewComponent ChatPanelFragment` from chat-view.xml; ChatDialogView
#   (HEADER_BUTTON) has its own, created fresh via `views.create(ChatDialogView.class)` on each open. What is
#   actually shared: the same ChatPanelFragment IMPLEMENTATION (class), the same ChatService Spring bean (and
#   therefore the same chat memory), and cross-surface conversation continuity via AiChatSessionState.currentConversationId.
#   ChatPanelFragment.hostView() does `getParentController() instanceof View<?>` and THROWS otherwise — so the
#   fragment MUST be hosted by a Jmix view (FragmentOwner); attaching it directly under a raw UI Div is illegal.
#   FIX: the sidebar hosts the ChatPanelFragment THROUGH a lean Jmix-owned host view (new AiAgentSidebarView,
#   a StandardView whose XML contains only `<fragment id="chatPanelFragment" class="...ChatPanelFragment"/>` —
#   mirroring chat-dialog-view.xml), created via `views.create(AiAgentSidebarView.class)` exactly the way
#   `openDialog()` creates `ChatDialogView`; then ChatSurfaceMounter attaches THAT VIEW's element into the
#   fixed-position panel Div. SURF-11's "no second ChatService / no second chat memory / no duplicate fragment
#   implementation" guarantee holds because: ChatService is a singleton bean injected into the fragment; chat
#   memory is owned by ChatService; AiAgentSidebarView reuses the existing ChatPanelFragment class via XML
#   (no new fragment class); cross-surface continuity is via AiChatSessionState. The must-haves below say "same
#   implementation + same ChatService/chat memory + AiChatSessionState continuity", NOT "same physical instance".
# - Point #4 (Codex MEDIUM / OpenCode LOW) — `clamp(420px, 32vw, 32vw)` is malformed (preferred == max) and 420px
#   is too narrow: the fragment's internal 68/32 split needs ~200px for the attachments pane, and the project's
#   own note (configureDialogWindow comment in ChatSurfaceMounter) records that 32% of a 35%-of-1366px dialog
#   (~150px) was too narrow, prompting the bump to 50%. FIX: sidebar width `clamp(640px, 32vw, 760px)` (640px min
#   ⇒ 32% ≈ 205px for the attachments pane; 32vw preferred; 760px cap on wide screens). Below the small-device
#   breakpoint (~768px) the panel is a 100vw overlay and the push is dropped (the attachments pane stacking note
#   stays in the CSS as a comment).
# - Point #5 (Codex MEDIUM) — `@CssImport("./styles/ai-agent-chat.css")` currently sits on MessageBubbleComponent,
#   which the current MessageList-based ChatPanelFragment NEVER instantiates (it is only used by ConversationDetailView).
#   So `.ai-agent-sidebar*` / `.ai-agent-status` would not load when the sidebar/chat fragment renders. FIX: add
#   `@CssImport("./styles/ai-agent-chat.css")` to ChatPanelFragment itself (always present in every chat/sidebar
#   render path). Keep the existing import on MessageBubbleComponent too (harmless duplicate; ConversationDetailView
#   still relies on it). This plan only ADDS the annotation to ChatPanelFragment — no other change to that file here.
# - RESEARCH Open Q1 (touched here only for the sidebar's view-permission gate): AiAgentUserRole has NO AiAuditEvent
#   policy (confirmed) but it DOES list "AiAgent_ChatDialog" under @ViewPolicy — the new AiAgentSidebarView needs its
#   own view-id added to both AiAgentUserRole.userViews() and AiAgentAdminRole.adminViews() so chat users can open it.

must_haves:
  truths:
    - "With SIDEBAR enabled, ChatSurfaceMounter mounts a non-modal right side panel (a position:fixed Div on the UI element) hosting the ChatPanelFragment THROUGH a lean Jmix-owned host view (AiAgentSidebarView), plus a distinct far-right navbar toggle button"
    - "The sidebar hosts the same ChatPanelFragment IMPLEMENTATION the existing surfaces use, backed by the same singleton ChatService and the same chat memory — no second ChatService, no second chat-memory store, no duplicate fragment-implementation class; cross-surface continuity is preserved via AiChatSessionState.currentConversationId (a conversation started in any enabled surface is visible after switching to another)"
    - "The navbar toggle is visually distinct from aiAgentHeaderChatButton (different id aiAgentSidebarToggleButton, different icon VaadinIcon.PANEL, same LUMO_TERTIARY+LUMO_ICON theming) and reflects open/closed state via aria-pressed + an active CSS class"
    - "Both the navbar toggle and an in-panel close button route through one toggleSidebar() method so state never drifts"
    - "The panel survives route navigation between two non-chat routes (still attached to the UI element, push class still on the AppLayout) and the main view stays interactive (no modality curtain)"
    - "With SIDEBAR disabled (or the chat view not permitted) no panel and no toggle are in the rendered DOM (absent/setVisible(false), not greyed)"
    - "All new user-visible text (the toggle + in-panel-closer aria-labels) comes from msg:// keys in messages_en.properties AND messages_vi.properties"
    - "The existing FULL_ROUTE / HEADER_BUTTON flows and the ChatPanelFragment slot ids (messageListSlot, messageInputSlot, attachmentsPanel) are unchanged; chat-panel-fragment.xml is unchanged"
    - "ai-agent-chat.css is @CssImport-ed from ChatPanelFragment so the .ai-agent-sidebar* rules load on every chat/sidebar render"
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/ChatSurfaceMounter.java"
      provides: "createSidebarToggleButton / mountSidebarToggle / mountSidebarPanel / toggleSidebar / shouldShowSidebar / SIDEBAR refresh on AfterNavigationEvent; the host view created via views.create(AiAgentSidebarView.class)"
      contains: "AiChatSurface.SIDEBAR"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/AiAgentSidebarView.java"
      provides: "lean StandardView hosting ChatPanelFragment via XML — the FragmentOwner for the sidebar's fragment"
      contains: "AiAgent_Sidebar"
    - path: "ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/ai-agent-sidebar-view.xml"
      provides: "<fragment id=chatPanelFragment class=...ChatPanelFragment/> — no second fragment class"
      contains: "ChatPanelFragment"
    - path: "ai-agent/ai-agent/src/main/resources/META-INF/resources/frontend/styles/ai-agent-chat.css"
      provides: ".ai-agent-sidebar / .ai-agent-sidebar--open / .ai-agent-sidebar__header / .ai-agent-content--pushed / .ai-agent-sidebar-toggle--active rules + small-device overlay media query"
      contains: ".ai-agent-sidebar"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java"
      provides: "@CssImport(./styles/ai-agent-chat.css) added so the stylesheet loads on every chat/sidebar render"
      contains: "CssImport"
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/ChatSurfaceMounterTest.java"
      provides: "SIDEBAR mount / toggle / persistence-across-navigation / disabled-absence / same-implementation+continuity cases"
  key_links:
    - from: "ChatSurfaceMounter sidebar panel Div"
      to: "UI.getCurrent().getElement()"
      via: "ui.getElement().appendChild(panelDiv.getElement()) — NOT the AppLayout content slot"
      pattern: "getElement\\(\\)\\.appendChild"
    - from: "ChatSurfaceMounter sidebar host"
      to: "AiAgentSidebarView (which owns the ChatPanelFragment)"
      via: "views.create(AiAgentSidebarView.class) — mirrors openDialog()'s views.create(ChatDialogView.class); panelDiv.getElement().appendChild(sidebarView.getElement())"
      pattern: "AiAgentSidebarView"
    - from: "ChatSurfaceMounter sidebar mount/show"
      to: "AiUiSettings.getEnabledSurfaceSet().contains(AiChatSurface.SIDEBAR) AND a UiShowViewContext permission check"
      via: "shouldShowSidebar(...) — enabled-surface set + accessManager.applyRegisteredConstraints(new UiShowViewContext(SIDEBAR_VIEW_ID)).isPermitted()"
      pattern: "getEnabledSurfaceSet"
    - from: "navbar toggle button + in-panel closer"
      to: "one toggleSidebar() method"
      via: "click listeners both call toggleSidebar(); it flips the push class + aria-pressed + the active class together"
      pattern: "toggleSidebar"
---

<objective>
Extend `ChatSurfaceMounter` to mount the `SIDEBAR` chat surface per decisions D-01..D-04 (refined by review point #1): a `position:fixed` docked `Div` on the UI element that hosts the `ChatPanelFragment` THROUGH a lean Jmix-owned host view (`AiAgentSidebarView`) — created via `views.create(AiAgentSidebarView.class)` exactly the way `openDialog()` creates `ChatDialogView` — so the fragment has a valid `FragmentOwner` and `hostView()` works (non-modal, push-mode on desktop via a CSS class on the AppLayout content area, full-screen overlay below ~768px), a distinct far-right navbar toggle button (`VaadinIcon.PANEL`, `aiAgentSidebarToggleButton`, `LUMO_TERTIARY`+`LUMO_ICON`, `aria-pressed` + an active CSS class for state), and an in-panel close button — both routing through one `toggleSidebar()` method. The panel starts CLOSED (`defaultSurface=SIDEBAR` means only "the toggle is mounted"). It survives route navigation by living on the UI element (not the AppLayout content slot, which `setContent()`-replaces on every navigation); on `AfterNavigationEvent` re-assert the Div attachment, the push class, and the toggle. Gate the mount/show on `getEnabledSurfaceSet().contains(AiChatSurface.SIDEBAR)` AND a `UiShowViewContext`-based permission check for the new `AiAgentSidebarView` (and add that view id to `AiAgentUserRole`/`AiAgentAdminRole` so chat users can open it). Add `.ai-agent-sidebar*` CSS to `ai-agent-chat.css` (justified raw-CSS exception per D-01) with a real `clamp(640px, 32vw, 760px)` width (per review point #4 — 640px min ⇒ ~205px for the fragment's 32%-width attachments pane). Add `@CssImport("./styles/ai-agent-chat.css")` to `ChatPanelFragment` so the stylesheet loads on every chat/sidebar render (per review point #5 — it currently sits only on the no-longer-instantiated `MessageBubbleComponent`). Resolve RESEARCH Open Q3 (mount-then-`setVisible(false)` vs mount-only-when-enabled) consistently with the existing magic-button pattern (mount-always-then-`setVisible(false)`) and document it. Zero behavior change to the existing two surfaces, `chat-panel-fragment.xml`, and the `ChatPanelFragment` slot ids.

Purpose: Delivers SURF-11 — the third chat surface and its launch affordance, with the fragment-hosting reframed to be Vaadin/Jmix-legal. Plan 04 (observability inside the fragment) is independent of this and lands after (it shares `ai-agent-chat.css` + the locale bundles + the `@CssImport` this plan adds).

Output: new `AiAgentSidebarView` + `ai-agent-sidebar-view.xml`; extended `ChatSurfaceMounter`; new fields on `MountedChatSurfaceState` (or `AiChatUIState`) for the sidebar host view / panel Div / toggle / open-flag; new CSS rules; `@CssImport` on `ChatPanelFragment`; the new view id added to `AiAgentUserRole`/`AiAgentAdminRole`; new toggle + in-panel-closer aria-label `msg://` keys (en + vi); extended `ChatSurfaceMounterTest` / `ChatDialogViewTest` / `ChatPanelFragmentSurfaceSwitchTest`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-SPEC.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-CONTEXT.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-PATTERNS.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-RESEARCH.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-REVIEWS.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-01-SUMMARY.md

<interfaces>
<!-- VERIFIED CODE FACTS (read these — do not re-derive):
  - ChatSurfaceMounter is a @Component VaadinServiceInitListener. It does NOT host ChatPanelFragment directly.
    FULL_ROUTE = ChatView (route /ai-agent/chat) whose chat-view.xml has `<fragment id="chatPanelFragment" class="...ChatPanelFragment"/>`.
    HEADER_BUTTON = ChatDialogView, created fresh by openDialog(): `ChatDialogView dialogView = views.create(ChatDialogView.class);
    DialogWindow<ChatDialogView> dw = new DialogWindow<>(dialogView); dw.setApplicationContext(applicationContext); dw.afterPropertiesSet();
    configureDialogWindow(dw); ... chatUIState.setDialogInstance(dw); dw.open(); attachDialogWindowToUi(dw);` where
    attachDialogWindowToUi(dw) does `UI ui = UI.getCurrent(); if (ui != null) ui.getElement().appendChild(dw.getElement());` — the survives-navigation trick.
  - chat-dialog-view.xml is JUST: `<view><layout width="100%" height="100%" padding="false" spacing="false"><fragment id="chatPanelFragment" class="com.vn.agent.view.chat.fragment.ChatPanelFragment"/></layout></view>` — copy this shape for ai-agent-sidebar-view.xml (no @ViewDescriptor surprises).
  - ChatDialogView.java is a `StandardView` with `@ViewController("AiAgent_ChatDialog") @ViewDescriptor("chat-dialog-view.xml")`, a `@ViewComponent private ChatPanelFragment chatPanelFragment;` and `@Subscribe onBeforeShow/onReady -> chatPanelFragment.setConversationId(aiChatSessionState.getCurrentConversationId());` — copy this shape for AiAgentSidebarView (view id e.g. "AiAgent_Sidebar", descriptor "ai-agent-sidebar-view.xml"). It needs `@Autowired AiChatSessionState` and the same sync-on-show so the sidebar opens onto the current conversation (continuity).
  - ChatPanelFragment extends Fragment<VerticalLayout> @FragmentDescriptor("chat-panel-fragment.xml"); hostView() = `FragmentOwner po = getParentController(); if (po instanceof View<?> v) return v; throw new IllegalStateException("ChatPanelFragment must be attached to a Jmix view");` — hence the host MUST be a Jmix view. ChatService is `@Autowired` (singleton bean); chat memory is owned by ChatService. AiChatSessionState (@VaadinSessionScope) holds `currentConversationId` + listeners.
  - @CssImport("./styles/ai-agent-chat.css") currently sits ONLY on MessageBubbleComponent, which the MessageList-based ChatPanelFragment never instantiates (MessageBubbleComponent is only used by ConversationDetailView). Add `@CssImport("./styles/ai-agent-chat.css")` to ChatPanelFragment (class-level annotation; that is the ONLY edit to ChatPanelFragment.java in this plan).
  - ChatSurfaceMounter machinery to mirror: createChatButton() (uiComponents.create(JmixButton.class) -> setId -> addClassName -> setIcon(VaadinIcon.MAGIC.create()) -> addThemeVariants(LUMO_TERTIARY, LUMO_ICON) -> getElement().setAttribute("aria-label", messages.getMessage("chatSurfaceMounter.headerButton.ariaLabel")) -> addClickListener(e -> toggleDialog())); mountHeaderButton() self-healing (re-find by id if parent gone, else appLayout.get().addToNavbar(...)); afterNavigation()/refreshMountedSurfaces() re-sync on every AfterNavigationEvent; shouldShowHeaderButton() = enabled-surface set contains HEADER_BUTTON && isDialogViewPermitted() && !fullChatRoute; isDialogViewPermitted() = `UiShowViewContext c = new UiShowViewContext(CHAT_DIALOG_VIEW_ID); accessManager.applyRegisteredConstraints(c); return c.isPermitted();`; toggleDialog()/closeExistingDialog one-toggle precedent; updateFullRouteMenuVisibility uses ui.getElement().executeJs(...) for a DOM toggle but a CSS push class can be added/removed server-side via appLayout.get().addClassName/removeClassName; MountedChatSurfaceState is a private static final class stored via ComponentUtil.getData(ui, ...) with fields {chatButton, missingAppLayoutWarned} — add {sidebarToggleButton, sidebarPanelDiv, sidebarHostView, sidebarOpen}; warnMissingAppLayoutOnce(...) — reuse for the sidebar.
  - AiChatUIState (@UIScope) currently holds only the modeless DialogWindow (dialogInstance) — an alternative home for the sidebar host-view handle; planner picks (record which in the SUMMARY).
  - The existing magic button is mounted-always-then-setVisible(false) when disabled; Vaadin setVisible(false) removes the element from the rendered DOM, satisfying "absent, not greyed". Be consistent and document.
  - Locale precedent: chatSurfaceMounter.headerButton.ariaLabel=Open AI chat (en) / "Mở trò chuyện AI" (vi) at messages_vi.properties:229. Add chatSurfaceMounter.sidebarToggle.ariaLabel.open / .closed (distinct labels per D-02) + chatSurfaceMounter.sidebarCloser.ariaLabel.
  - CSS precedent in ai-agent-chat.css: .ai-agent-attachment-notice / .ai-agent-intent-confirm / .ai-agent-action-choice use --lumo-* tokens, flex centering, border-radius, box-shadow. The fragment's chat-panel-fragment.xml has a `<split orientation="HORIZONTAL" splitterPosition="68">` — 68% chat / 32% attachments — so the panel must be wide enough that 32% of it ≥ ~200px ⇒ min-width ≥ ~640px. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a lean Jmix host view for the sidebar and wire ChatSurfaceMounter to mount the SIDEBAR panel + navbar toggle + one toggle method</name>
  <files>ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/AiAgentSidebarView.java, ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/ai-agent-sidebar-view.xml, ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/ChatSurfaceMounter.java, ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/AiChatUIState.java, ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java, ai-agent/ai-agent/src/main/java/com/vn/agent/security/AiAgentUserRole.java, ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties, ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties, ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/ChatSurfaceMounterTest.java, ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/ChatDialogViewTest.java, ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/ChatPanelFragmentSurfaceSwitchTest.java</files>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/ChatSurfaceMounter.java (the file being modified — read ALL of it: createChatButton, mountHeaderButton, attachDialogWindowToUi, openDialog (the views.create(...) pattern), afterNavigation, refreshMountedSurfaces, shouldShowHeaderButton, toggleDialog/closeExistingDialog, updateFullRouteMenuVisibility, isDialogViewPermitted, MountedChatSurfaceState, warnMissingAppLayoutOnce)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/ChatDialogView.java + ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/chat-dialog-view.xml (the model for AiAgentSidebarView — copy the shape: a StandardView with a single `<fragment id="chatPanelFragment" class="...ChatPanelFragment"/>` and a sync-on-show that calls `chatPanelFragment.setConversationId(aiChatSessionState.getCurrentConversationId())`)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/ChatView.java + ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/chat-view.xml (the FULL_ROUTE surface — confirm it also hosts ChatPanelFragment via XML; do NOT modify)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/AiChatUIState.java (the @UIScope bean holding the modeless DialogWindow — candidate home for the sidebar host-view handle)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/AiChatSessionState.java (the @VaadinSessionScope bean — `currentConversationId` + listeners; the continuity mechanism)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiChatSurface.java (confirm SIDEBAR is present from Plan 01)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiUiSettings.java (getEnabledSurfaceSet)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java (DO NOT modify it here EXCEPT to add the class-level `@CssImport("./styles/ai-agent-chat.css")` annotation — see review point #5; read enough to see it extends Fragment<VerticalLayout>, has hostView()=getParentController()-cast-to-View, and currently has no @CssImport)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/MessageBubbleComponent.java (read just the `@CssImport("./styles/ai-agent-chat.css")` line — confirm it sits there and that ChatPanelFragment does NOT instantiate MessageBubbleComponent; this is why the import must also go on ChatPanelFragment)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/security/AiAgentUserRole.java + AiAgentAdminRole.java (the @ViewPolicy(viewIds=...) lists — confirm "AiAgent_ChatDialog" is listed; the new sidebar view id must be added to both userViews() and adminViews())
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-PATTERNS.md (section "view/chat/ChatSurfaceMounter.java")
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-RESEARCH.md (Pattern 1, Pitfalls 1/2/7, Open Question 3, Code Examples "ChatSurfaceMounter SIDEBAR mount sketch", Security Domain "Side-panel Div mounted for a user without chat permission")
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-REVIEWS.md (point #1 — the fragment-hosting reframe; point #5 — the @CssImport move)
    - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/ChatSurfaceMounterTest.java (the test being extended)
    - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/ChatPanelFragmentSurfaceSwitchTest.java (the cross-surface continuity test being extended)
  </read_first>
  <behavior>
    - `AiAgentSidebarView` exists: a `StandardView` (`@ViewController("AiAgent_Sidebar") @ViewDescriptor("ai-agent-sidebar-view.xml")`) whose XML contains ONLY `<fragment id="chatPanelFragment" class="com.vn.agent.view.chat.fragment.ChatPanelFragment"/>` (no new fragment class — it reuses the existing `ChatPanelFragment`), with `@ViewComponent private ChatPanelFragment chatPanelFragment;` and `@Autowired private AiChatSessionState aiChatSessionState;` and a sync-on-show (`@Subscribe onBeforeShow`/`onReady`) that calls `chatPanelFragment.setConversationId(aiChatSessionState.getCurrentConversationId())` — mirroring `ChatDialogView`.
    - With a default `AiUiSettings` (SIDEBAR enabled), after UI init the rendered tree contains: a navbar button with id `aiAgentSidebarToggleButton`, icon `VaadinIcon.PANEL`, theme variants `LUMO_TERTIARY`+`LUMO_ICON`, an `aria-label` resolved from `chatSurfaceMounter.sidebarToggle.ariaLabel.closed`, `aria-pressed="false"`; AND a `Div` with class `ai-agent-sidebar` (not `ai-agent-sidebar--open`) attached to the UI element, whose children are a header `Div.ai-agent-sidebar__header` with an in-panel close `JmixButton` (icon `VaadinIcon.ANGLE_DOUBLE_RIGHT` or `VaadinIcon.CLOSE`, `aria-label` from `chatSurfaceMounter.sidebarCloser.ariaLabel`, click → `toggleSidebar(ui)`) and below it the `AiAgentSidebarView`'s root element (created via `views.create(AiAgentSidebarView.class)` — which owns the `ChatPanelFragment`).
    - Clicking the navbar toggle: the panel Div gains `ai-agent-sidebar--open`, the AppLayout gains `ai-agent-content--pushed`, the toggle gains `ai-agent-sidebar-toggle--active` and `aria-pressed="true"` and its `aria-label` switches to `chatSurfaceMounter.sidebarToggle.ariaLabel.open`. Clicking again (or the in-panel closer) reverses all of it.
    - With both SIDEBAR and HEADER_BUTTON enabled, two distinct navbar buttons exist (`aiAgentHeaderChatButton` with `VaadinIcon.MAGIC` and `aiAgentSidebarToggleButton` with `VaadinIcon.PANEL`); toggling the sidebar does not open/close the HEADER_BUTTON dialog and vice versa.
    - With SIDEBAR disabled in `AiUiSettings`, neither the toggle button nor the panel Div is in the rendered DOM (assert DOM-absence / `setVisible(false)`); FULL_ROUTE menu + HEADER_BUTTON button behave exactly as before.
    - With the sidebar view not permitted (the new `UiShowViewContext(SIDEBAR_VIEW_ID)` check returns false), the sidebar toggle + panel are not mounted even if SIDEBAR is in the enabled set.
    - After `AfterNavigationEvent` (navigate to another route, then another): the panel Div is still attached to the (same) UI element, the push class is still on the (possibly new) AppLayout, the toggle is still mounted, the open/closed state is unchanged, and the main view content is interactive (no modal-overlay element added by the panel).
    - Cross-surface continuity: a conversation started while the sidebar panel is open is visible after switching to FULL_ROUTE / HEADER_BUTTON and back — because all surfaces sync from the same `AiChatSessionState.currentConversationId` and the chat memory is owned by the single `ChatService` bean. (Note: it does NOT require the same physical `ChatPanelFragment` object — the sidebar's fragment is a distinct instance hosted by `AiAgentSidebarView`, just like `ChatDialogView` already owns a distinct instance.)
    - `ChatPanelFragment.java` has gained a class-level `@CssImport("./styles/ai-agent-chat.css")` (and nothing else changed in that file in this plan).
  </behavior>
  <action>
    Create `view/chat/AiAgentSidebarView.java` + `view/chat/ai-agent-sidebar-view.xml` mirroring `ChatDialogView` + `chat-dialog-view.xml`: `@ViewController("AiAgent_Sidebar") @ViewDescriptor("ai-agent-sidebar-view.xml")` `public class AiAgentSidebarView extends StandardView`; XML = `<view><layout width="100%" height="100%" padding="false" spacing="false"><fragment id="chatPanelFragment" class="com.vn.agent.view.chat.fragment.ChatPanelFragment"/></layout></view>`; `@ViewComponent private ChatPanelFragment chatPanelFragment; @Autowired private AiChatSessionState aiChatSessionState;` + `@Subscribe onBeforeShow(BeforeShowEvent)` and `@Subscribe onReady(ReadyEvent)` each calling `chatPanelFragment.setConversationId(aiChatSessionState.getCurrentConversationId())`. Add the new view id `"AiAgent_Sidebar"` to `AiAgentUserRole.userViews()` `@ViewPolicy(viewIds=...)` and `AiAgentAdminRole.adminViews()` `@ViewPolicy(viewIds=...)` (next to `"AiAgent_ChatDialog"`). Add the class-level annotation `@CssImport("./styles/ai-agent-chat.css")` (`com.vaadin.flow.component.dependency.CssImport`) to `ChatPanelFragment` — the ONLY change to that file here. In `ChatSurfaceMounter`: add constants `SIDEBAR_VIEW_ID = "AiAgent_Sidebar"`, `SIDEBAR_TOGGLE_BUTTON_ID = "aiAgentSidebarToggleButton"`, `SIDEBAR_TOGGLE_BUTTON_CLASS = "ai-agent-sidebar-toggle-button"`, `SIDEBAR_PANEL_CLASS = "ai-agent-sidebar"`, `SIDEBAR_PANEL_HEADER_CLASS = "ai-agent-sidebar__header"`, `SIDEBAR_PANEL_OPEN_CLASS = "ai-agent-sidebar--open"`, `SIDEBAR_TOGGLE_ACTIVE_CLASS = "ai-agent-sidebar-toggle--active"`, `CONTENT_PUSHED_CLASS = "ai-agent-content--pushed"`. `createSidebarToggleButton()`: mirror `createChatButton()` exactly but with the new id/class, `VaadinIcon.PANEL.create()`, same `addThemeVariants(LUMO_TERTIARY, LUMO_ICON)`, `aria-label` from `messages.getMessage("chatSurfaceMounter.sidebarToggle.ariaLabel.closed")`, `getElement().setAttribute("aria-pressed", "false")`, click listener → `toggleSidebar(ui)`. `createSidebarPanel(ui)`: a `com.vaadin.flow.component.html.Div` with `addClassName(SIDEBAR_PANEL_CLASS)`, containing a header `Div` with `addClassName(SIDEBAR_PANEL_HEADER_CLASS)` holding the close `JmixButton` (icon `VaadinIcon.ANGLE_DOUBLE_RIGHT.create()`, `aria-label` from `messages.getMessage("chatSurfaceMounter.sidebarCloser.ariaLabel")`, click → `toggleSidebar(ui)`), and below it the sidebar host view: `AiAgentSidebarView sidebarView = views.create(AiAgentSidebarView.class);` then attach it the way `openDialog()` prepares `ChatDialogView` — `sidebarView` needs to be a live, shown view so the fragment's `onReady`/`onBeforeShow` fire and `setConversationId` runs; the cleanest is to wrap it in a `DialogWindow<AiAgentSidebarView>` (mirroring `openDialog()`'s `dw.setApplicationContext(applicationContext); dw.afterPropertiesSet(); dw.open();`) configured headless/borderless (no chrome) and then re-parent the dialog's element into the panel Div, OR (preferred if the Jmix fragments API allows showing a non-routed view without a DialogWindow) use `views.create(...)` + the standard show machinery and `panelDiv.getElement().appendChild(sidebarView.getElement())`. Pick whichever the Jmix 2.8 fragments/Views API supports cleanly for a non-routed, non-dialog view — verify via the jmix-fragments / jmix-views skills + Context7 jmix-framework/jmix-context7 at execute time; record the chosen mechanism in the SUMMARY. Whatever the mechanism, the panel Div ends up containing the header + the `AiAgentSidebarView`-hosted `ChatPanelFragment`, and the host-view handle is stored (on `MountedChatSurfaceState` or `AiChatUIState` — pick one, record which) so navigation re-attachment is idempotent. `mountSidebarToggle(ui)` / `mountSidebarPanel(ui)`: mirror `mountHeaderButton()`'s self-healing (re-find the toggle by id if the parent is gone, else `appLayout.get().addToNavbar(...)`; for the panel: `ui.getElement().appendChild(panelDiv.getElement())` — the panel attaches to the UI element like `attachDialogWindowToUi`, NEVER the AppLayout content slot); `warnMissingAppLayoutOnce(...)` when no `AppLayout` and skip (no throw). `toggleSidebar(ui)`: flip a `sidebarOpen` boolean (stored on `MountedChatSurfaceState` via `ComponentUtil` OR on `AiChatUIState` — pick one, record which; if the chosen home is `MountedChatSurfaceState`, leave `AiChatUIState.java` untouched — do NOT add an empty diff (it stays in `files_modified` so the executor checks it, but it only changes if the sidebar state actually lands there)); when opening: `panelDiv.addClassName(SIDEBAR_PANEL_OPEN_CLASS)`, `appLayout.get().addClassName(CONTENT_PUSHED_CLASS)`, `toggleButton.addClassName(SIDEBAR_TOGGLE_ACTIVE_CLASS)`, `toggleButton.getElement().setAttribute("aria-pressed", "true")`, `toggleButton.getElement().setAttribute("aria-label", messages.getMessage("chatSurfaceMounter.sidebarToggle.ariaLabel.open"))`; when closing: the inverse. Wire `afterNavigation()` / `refreshMountedSurfaces()` to also `mountSidebarToggle` + re-assert the panel Div is still attached (re-append if not) + re-assert the push class is on the (possibly new) AppLayout if `sidebarOpen` + set toggle/panel visibility = `shouldShowSidebar(settings, sidebarViewPermitted)`. Add `shouldShowSidebar(AiUiSettings settings, boolean sidebarViewPermitted)` = `settings.getEnabledSurfaceSet().contains(AiChatSurface.SIDEBAR) && sidebarViewPermitted` (mirror `shouldShowHeaderButton(...)`), and a `isSidebarViewPermitted()` helper = `UiShowViewContext c = new UiShowViewContext(SIDEBAR_VIEW_ID); accessManager.applyRegisteredConstraints(c); return c.isPermitted();` (mirror `isDialogViewPermitted()`). For the disabled case: be consistent with the existing magic-button pattern (mount-always-then-`setVisible(false)`) — RESEARCH Open Q3 recommends mirroring it; document the choice + that DOM-absence holds via `setVisible(false)`. Extend `MountedChatSurfaceState` (or `AiChatUIState`) with the `sidebarToggleButton` / `sidebarPanelDiv` / `sidebarHostView` (or the DialogWindow wrapping it) / `sidebarOpen` fields. Do NOT touch `chat-panel-fragment.xml`, the FULL_ROUTE menu logic, the HEADER_BUTTON dialog logic, or any slot id. Extend `ChatSurfaceMounterTest` with the cases in `<behavior>` (mount the host view + the panel + the toggle; toggle flips the classes/aria; in-panel closer also closes; both-enabled ⇒ two distinct independent buttons; SIDEBAR disabled ⇒ neither in DOM; not-permitted ⇒ neither mounted; navigation between two routes keeps panel+push+state; the panel hosts a `ChatPanelFragment` whose `ChatService` is the same singleton bean as the other surfaces' — assert by reflecting/looking up the `chatService` bean, or by a structural check that no second `ChatService` `@Bean`/`@Component` and no second `ChatPanelFragment`-subclass and no second fragment-class XML reference exists). Extend `ChatDialogViewTest` only if a regression assertion is warranted (likely no behavior change for it). Extend `ChatPanelFragmentSurfaceSwitchTest` to cover continuity across the SIDEBAR surface (a conversation set on the session is reflected after opening the sidebar view, and vice versa).
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.ChatSurfaceMounterTest" --tests "com.vn.agent.view.chat.ChatDialogViewTest" --tests "com.vn.agent.view.chat.ChatPanelFragmentSurfaceSwitchTest"</automated>
  </verify>
  <acceptance_criteria>
    - `AiAgentSidebarView.java` + `ai-agent-sidebar-view.xml` exist; the XML references `com.vn.agent.view.chat.fragment.ChatPanelFragment` (the EXISTING class — no new fragment subclass); the view syncs `setConversationId` from `AiChatSessionState` on show.
    - `AiAgentUserRole.userViews()` and `AiAgentAdminRole.adminViews()` `@ViewPolicy(viewIds=...)` lists both include `"AiAgent_Sidebar"`.
    - `ChatPanelFragment.java` has a class-level `@CssImport("./styles/ai-agent-chat.css")`; `git diff` shows no other change to that file in this plan.
    - `ChatSurfaceMounter.java` references `AiChatSurface.SIDEBAR`, has `createSidebarToggleButton()` / `mountSidebarToggle(...)` / `mountSidebarPanel(...)` / `createSidebarPanel(...)` / `toggleSidebar(...)` / `shouldShowSidebar(...)` / `isSidebarViewPermitted()` (or equivalently named), creates the host view via `views.create(AiAgentSidebarView.class)`, and appends the panel Div to `UI.getCurrent().getElement()` (not the AppLayout content slot).
    - `ChatSurfaceMounterTest` asserts: SIDEBAR enabled ⇒ a navbar button `aiAgentSidebarToggleButton` (icon PANEL, theme LUMO_TERTIARY+LUMO_ICON) AND a `Div.ai-agent-sidebar` containing a header with an in-panel closer + the `AiAgentSidebarView`-hosted `ChatPanelFragment`; toggling flips `ai-agent-sidebar--open` + `ai-agent-content--pushed` + `ai-agent-sidebar-toggle--active` + `aria-pressed`; the in-panel closer also closes; SIDEBAR + HEADER_BUTTON both enabled ⇒ two distinct buttons operating independently; SIDEBAR disabled ⇒ neither the toggle nor the panel is in the rendered DOM; not-permitted (`isSidebarViewPermitted()` false) ⇒ neither mounted; navigation between two routes keeps the panel attached + push class + state; the sidebar's fragment uses the SAME singleton `ChatService` (no second `ChatService` bean, no second `ChatPanelFragment` subclass, no second fragment-class XML reference) — a structural/source assertion.
    - `ChatPanelFragmentSurfaceSwitchTest` asserts cross-surface `AiChatSessionState.currentConversationId` continuity including the SIDEBAR surface.
    - `messages_en.properties` + `messages_vi.properties` each contain `chatSurfaceMounter.sidebarToggle.ariaLabel.open`, `chatSurfaceMounter.sidebarToggle.ariaLabel.closed`, `chatSurfaceMounter.sidebarCloser.ariaLabel`.
    - `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.ChatSurfaceMounterTest" --tests "com.vn.agent.view.chat.ChatDialogViewTest" --tests "com.vn.agent.view.chat.ChatPanelFragmentSurfaceSwitchTest"` exits 0.
    - `git diff` shows no change to `chat-panel-fragment.xml`, the FULL_ROUTE menu logic, or the HEADER_BUTTON dialog logic; if the sidebar state lives on `MountedChatSurfaceState`, `git diff` also shows no change to `AiChatUIState.java` (no empty diff).
    - The SUMMARY records: the mechanism used to show/host the `AiAgentSidebarView` (DialogWindow-wrapper-no-chrome vs Views.create + standard show) and why; where the sidebar state lives (`MountedChatSurfaceState` vs `AiChatUIState`); the disabled-case approach (mount-always-then-hide); the chosen panel-width / `clamp` / breakpoint / `top` values used in the CSS task; the SURF-11 "same implementation + same ChatService/chat memory + AiChatSessionState continuity" framing and which test proves "no second ChatService / no second chat memory / no duplicate fragment implementation"; and the new `msg://` key names.
  </acceptance_criteria>
  <done>A lean Jmix host view (AiAgentSidebarView) owns the sidebar's ChatPanelFragment; ChatSurfaceMounter mounts the SIDEBAR panel (the host view inside a fixed-position Div on the UI element) + the toggle, both routing through one toggle method; survives navigation; absent when disabled/not-permitted; same singleton ChatService + AiChatSessionState continuity; ChatPanelFragment carries the @CssImport; existing surfaces + slot ids untouched; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Add the side-panel + push-class + toggle-active CSS rules and the small-device overlay media query (with a real clamp width)</name>
  <files>ai-agent/ai-agent/src/main/resources/META-INF/resources/frontend/styles/ai-agent-chat.css</files>
  <read_first>
    - ai-agent/ai-agent/src/main/resources/META-INF/resources/frontend/styles/ai-agent-chat.css (the file being modified — read the existing `.ai-agent-attachment-notice` / `.ai-agent-intent-confirm` / `.ai-agent-action-choice` / `.ai-agent-bubble*` rules for the Lumo-token style to mirror; note any header comment about editing THIS file for visuals)
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/fragment/chat-panel-fragment.xml (confirm the `<split orientation="HORIZONTAL" splitterPosition="68">` — the 68/32 chat/attachments split that drives the min-width sizing)
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-PATTERNS.md (section "META-INF/resources/frontend/styles/ai-agent-chat.css")
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-RESEARCH.md (Pitfall 1/2, Assumptions A1/A2, Code Examples — the navbar-height variable / fixed-top fallback, the `padding-right` push approach)
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-CONTEXT.md (D-01 "Planner detail" — ≈32% width starting point)
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-REVIEWS.md (point #4 — the malformed clamp + the too-narrow-for-the-fragment finding)
  </read_first>
  <action>
    Append to `ai-agent-chat.css` (following the existing `--lumo-*`-token style; use the EXACT class names from Task 1): `.ai-agent-sidebar { position: fixed; right: 0; top: var(--lumo-size-xl, 3.5rem); bottom: 0; width: clamp(640px, 32vw, 760px); display: none; z-index: 5; background: var(--lumo-base-color); box-shadow: var(--lumo-box-shadow-l); }` — and a CSS comment explaining the clamp: "640px min ⇒ the fragment's 32%-width attachments pane (chat-panel-fragment.xml `<split splitterPosition=68>`) gets ≈205px, matching the project's ~200px attachment-card note (cf. ChatSurfaceMounter.configureDialogWindow); 32vw preferred; 760px cap on wide screens. Replaces the malformed `clamp(420px, 32vw, 32vw)` flagged in 15-REVIEWS point #4." Use a Lumo navbar-height variable for `top` if one is reliably present on this app's shell; else the `3.5rem` fallback shown — note the choice in a CSS comment. `.ai-agent-sidebar--open { display: flex; flex-direction: column; }`. `.ai-agent-sidebar__header { display: flex; justify-content: flex-end; padding: var(--lumo-space-xs); flex: 0 0 auto; }` (the close-button row). The host view's element below the header should flex-grow: `.ai-agent-sidebar > :not(.ai-agent-sidebar__header) { flex: 1 1 auto; min-height: 0; overflow: hidden; }`. `.ai-agent-content--pushed { padding-right: clamp(640px, 32vw, 760px); }` (applied to the AppLayout content area — `padding-right` suffices since the navbar is `position: relative`; only add `padding-top` if a host uses a fixed navbar — note this in a comment). `@media (max-width: 768px) { .ai-agent-sidebar { width: 100vw; left: 0; right: 0; } .ai-agent-content--pushed { padding-right: 0; } }` (full-screen overlay on small devices, drop the push; the fragment's internal 68/32 split stacks per its own responsive rules — note this in a comment). `.ai-agent-sidebar-toggle--active { background: var(--lumo-primary-color-10pct); color: var(--lumo-primary-text-color); }` (the "pressed" toggle state — Lumo has no built-in pressed variant). Keep the rules minimal; do NOT add a new Vaadin theme / `theme.json` / `frontend/themes/` directory (the `@CssImport("./styles/ai-agent-chat.css")` added to `ChatPanelFragment` in Task 1 loads this file on every chat/sidebar render). (The `.ai-agent-status` rule + its `@keyframes` pulse + `prefers-reduced-motion` guard belong to Plan 04 — do NOT add them here.)
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.ChatSurfaceMounterTest"</automated>
  </verify>
  <acceptance_criteria>
    - `ai-agent-chat.css` contains `.ai-agent-sidebar`, `.ai-agent-sidebar--open`, `.ai-agent-sidebar__header`, `.ai-agent-content--pushed`, `.ai-agent-sidebar-toggle--active` rules and a `@media (max-width: 768px)` block collapsing the panel to a full-screen overlay.
    - The `.ai-agent-sidebar` / `.ai-agent-content--pushed` widths use a well-formed `clamp(min, preferred, max)` with `min < preferred-bound < max` (no `clamp(x, y, y)`); the `min` is ≥ ~640px so the fragment's 32%-width attachments pane is ≥ ~200px; a CSS comment references 15-REVIEWS point #4.
    - The new rules use `--lumo-*` tokens consistent with the existing `.ai-agent-attachment-notice` style; no `theme.json` / new `frontend/themes/` directory added.
    - The class names match those used in `ChatSurfaceMounter.java` (Task 1).
    - `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.ChatSurfaceMounterTest"` exits 0 (Vaadin frontend build / `@CssImport` scan still succeeds).
    - No `.ai-agent-status` rule added in this plan (deferred to Plan 04).
  </acceptance_criteria>
  <done>The side-panel shell (with a real clamp width sized for the fragment's 68/32 split), push class, toggle-active state, and small-device overlay are styled in the existing CSS file; class names align with ChatSurfaceMounter; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host shell (AppLayout / StandardMainView) ↔ ChatSurfaceMounter | The mounter walks the UI tree to find the AppLayout; degrades gracefully (warn-once, skip) if absent — no throw |
| browser DOM (the fixed-position panel Div, the push class) ↔ server-side Flow component tree | The panel Div + push class are server-managed Flow elements; the only "client input" is the toggle click event |
| user ↔ SIDEBAR surface visibility | A user without permission to the new `AiAgentSidebarView` must not get the panel or the toggle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-C1 | Broken access control | the side-panel Div + navbar toggle mounted for a user without chat permission | mitigate | Gate the SIDEBAR mount/show on `isSidebarViewPermitted()` (`UiShowViewContext(SIDEBAR_VIEW_ID)` + `accessManager.applyRegisteredConstraints`) AND on `AiUiSettings.getEnabledSurfaceSet().contains(AiChatSurface.SIDEBAR)`. The new `AiAgentSidebarView` view id is added to `AiAgentUserRole`/`AiAgentAdminRole` `@ViewPolicy` (parity with `AiAgent_ChatDialog`); a user without the chat role has neither role's policy and the check returns false. Tested: not-permitted ⇒ neither mounted. The `ChatPanelFragment` hosted inside it still applies its own existing data-access security (constrained `DataManager`, conversation ownership) — the host view adds no privileged data path. |
| T-15-C2 | Information disclosure | (none new) — the panel hosts the existing `ChatPanelFragment` implementation backed by the existing singleton `ChatService`; no new data path | accept | No new data crosses to the UI here; the fragment's existing security posture is unchanged. The observability text (status line / disclosure) is added in Plan 04 with its own threat model + TEST-19. |
| T-15-C3 | Tampering / XSS | the in-panel close button / panel header text | mitigate | All button labels via `msg://` keys + `Element.setAttribute("aria-label", ...)` / `Element.setText(...)` (HTML-escaped) — no `innerHTML`, no user free-text. The panel header carries only the close button (icon + aria-label from `msg://`). |
| T-15-C4 | Denial of service | the panel Div / host view torn down or re-created racing the framework on every navigation | mitigate | Attach the panel Div to `UI.getCurrent().getElement()` (not the AppLayout content slot, which `setContent()`-replaces on navigation); the host view is created ONCE per UI (handle stored on `MountedChatSurfaceState`/`AiChatUIState`) and re-attached idempotently on `AfterNavigationEvent` (re-append only if detached) — mirrors the existing `attachDialogWindowToUi`/`reattachOpenDialogToUi` pattern for the modeless dialog. Tested: panel persists across two route changes; the host view is not re-created. |
| T-15-C5 | Session-scope leak / state drift | sidebar open-state / panel handle stored at too-broad a scope, or a second `ChatService`/chat memory created | mitigate | Store the sidebar `panelDiv` / `sidebarHostView` / `toggleButton` / `sidebarOpen` on the per-UI `MountedChatSurfaceState` (`ComponentUtil.getData(ui, ...)`) or the `@UIScope` `AiChatUIState` — both correctly per-UI, never `AiChatSessionState` (which stays at `currentConversationId` + listeners). The sidebar reuses the singleton `ChatService` bean and the existing `ChatPanelFragment` class (no new fragment subclass, no second fragment-class XML reference, no new chat-memory store) — verified by a structural/source assertion in `ChatSurfaceMounterTest` and (in Plan 05) `NoNewPersistedStateTest`. Cross-surface continuity is via `AiChatSessionState.currentConversationId` only. |
</threat_model>

<verification>
- `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.*" --tests "com.vn.agent.view.uisettings.*"` green (the chat-surface + ui-settings packages).
- `./gradlew :ai-agent:ai-agent:test` (full add-on module) green — catches any regression in the existing surface flows and any view-policy/role test.
- `git diff --stat` shows no change to `chat-panel-fragment.xml`; no new file under `**/liquibase/**`; no new `frontend/themes/` directory or `theme.json`; the only edit to `ChatPanelFragment.java` is the added `@CssImport` line.
- Manual UI verification (app already running on http://localhost:8088 — do NOT auto-start `bootRun`): with SIDEBAR enabled, the far-right navbar shows a second tertiary icon button (PANEL) distinct from the MAGIC button; clicking opens a non-modal right panel hosting the shared chat fragment; the main view stays clickable; navigating to another route leaves the panel open and the main view interactive; the in-panel closer collapses it; below ~768px the panel is a full-screen overlay; with SIDEBAR disabled in the admin UI-settings view, no panel and no toggle appear; the attachments pane inside the panel is usable at desktop width (~205px+); a conversation continued from the sidebar appears in the FULL_ROUTE/HEADER_BUTTON surface and back (continuity).
</verification>

<success_criteria>
- SURF-11 met: a non-modal push-mode right `SIDEBAR` panel hosting the `ChatPanelFragment` implementation (through a lean Jmix-owned `AiAgentSidebarView`), mounted by `ChatSurfaceMounter`, gated by `AiUiSettings` + a `UiShowViewContext` permission check, with a distinct navbar toggle reflecting open/closed state, an in-panel closer, both via one toggle method, surviving route navigation with the main view interactive, absent (not greyed) when disabled/not-permitted.
- No second `ChatService`, no second chat memory, no duplicate `ChatPanelFragment` implementation; cross-surface continuity preserved via `AiChatSessionState.currentConversationId` — provable by `ChatSurfaceMounterTest` / `ChatPanelFragmentSurfaceSwitchTest`.
- `ai-agent-chat.css` is `@CssImport`-ed from `ChatPanelFragment` so the `.ai-agent-sidebar*` rules load on every chat/sidebar render; the panel width is a well-formed `clamp` sized for the fragment's 68/32 split.
- New aria-label text via `msg://` in en + vi; CSS in the existing stylesheet (justified per D-01); no new theme machinery.
- Existing FULL_ROUTE / HEADER_BUTTON flows + slot ids unchanged; all tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-03-SUMMARY.md` recording: the `AiAgentSidebarView` host mechanism + how it is shown/attached, the SURF-11 "same implementation + same ChatService/chat memory + AiChatSessionState continuity" framing and the test that proves it, where the sidebar state lives, the disabled-case approach, the chosen panel-width/`clamp`/breakpoint/`top` values, the `@CssImport` placement, and the new `msg://` key names.
</output>
