---
phase: 15-right-sidebar-chat-surface-observability-ux
plan: 06
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [SURF-11, OBS-01, OBS-02]
files_modified:
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java
  - ai-agent/ai-agent/src/main/resources/META-INF/resources/frontend/styles/ai-agent-chat.css
  - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/fragment/ChatPanelFragmentConversationIdTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/fragment/ChatPanelFragmentTurnDetailTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/fragment/ChatPanelFragmentTurnDetailHistoryTest.java

must_haves:
  truths:
    - "Clicking 'new conversation' (newChatButton → confirm Yes → startNewChat) clears all on-screen messages even when the prior turn errored and conversationId is still null"
    - "After an errored turn (no StreamingEvent.Final), if the orchestration created the conversation server-side, conversationId is kept in sync from .doOnError when the runId is known"
    - "The per-turn 'what the agent did' <vaadin-details>, the phase-14 action-choice Div, and NOTICE Divs render in the DOM immediately AFTER the <vaadin-message> element of the turn they belong to, inside <vaadin-message-list>, not at the tail of messageListSlot"
    - "Those anchored elements are re-anchored after every MessageList.setItems(...) call and after history replay, so they survive re-renders"
    - "The turn-detail disclosure is visually styled per 15-option-A-mockup.html (border + light tint + ⚙ in summary + rotating caret + per-step icons + right-aligned tabular ms + red error-rollback row); the action-choice block has a primary left border + primary-10 tint; uses --lumo-* tokens only, no new theme.json / frontend/themes, no edits in frontend/generated"
    - "Step rows remain label-only (no tool/entity names) — ObservabilityLeakTest still passes; history step rows still lazy-load once; the transient KIND-keyed status line behavior, messageCount bookkeeping, clearMessageList()/onDetach teardown, and 3-surface sharing are unchanged"
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java"
      provides: "startNewChat force-clear; .doOnError conversationId sync; per-turn anchoring of extra elements (turn-detail / action-choice / NOTICE) with re-anchor after setItems + history replay"
    - path: "ai-agent/ai-agent/src/main/resources/META-INF/resources/frontend/styles/ai-agent-chat.css"
      provides: "Restyled .ai-agent-turn-activity disclosure block + .ai-agent-action-choice per the mockup; .ai-agent-turn-extra indent wrapper"
  key_links:
    - from: "ChatPanelFragment.startNewChat / setConversationIdInternal"
      to: "clearMessageList"
      via: "force-clear path bypassing the null==null early-return when messageCount>0"
      pattern: "startNewChat|setConversationIdInternal"
    - from: "ChatPanelFragment.appendTurnDetails / appendHistoryTurnDetails / appendActionChoiceRow / appendNoticeRow"
      to: "messageList <vaadin-message> sibling DOM position"
      via: "anchorExtraAfterTurn(turnIndex, element) + reanchorAllExtras() after setItems"
      pattern: "anchorExtra|reanchor"
---

<objective>
Close the two open Phase 15 UAT gaps in `ChatPanelFragment` (and its CSS):

1. **Gap 1 (major) — "new conversation" no-ops after an errored turn.** `conversationId` is only set on `StreamingEvent.Final`; an errored turn leaves it `null` while the user message is still rendered (`messageCount > 0`). `startNewChat()` → `setConversationIdInternal(null)` hits the `if (Objects.equals(this.conversationId, cid)) return;` guard (`null == null`) and returns without clearing. Fix: force a clear when resetting and messages are still on screen; orthogonally, `.doOnError` should keep `conversationId` in sync if the orchestration already created the conversation server-side.

2. **Gap 2 (minor, user-prioritized) — turn-detail disclosure + phase-14 action-choice prompt + NOTICE rows must render INLINE directly below the assistant `<vaadin-message>` they belong to (inside `<vaadin-message-list>`), not stacked at the bottom of `messageListSlot`; and the disclosure + action-choice need real styling per the user-approved mockup `15-option-A-mockup.html` ("Option A").** Re-anchor after every `MessageList.setItems(...)` and after history replay; indent the extra block under the assistant bubble; restyle.

Purpose: ship the polished, correct chat-observability UX the user signed off on; unblock `/gsd-verify-work` for Phase 15.
Output: one `ChatPanelFragment.java` edit, one `ai-agent-chat.css` edit, regression tests extended in the established plain-JUnit-5 + Mockito fragment-test style.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-UAT.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-option-A-mockup.html
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-04-SUMMARY.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-PATTERNS.md
@ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/TurnDetailRenderer.java
@ai-agent/ai-agent/src/main/resources/META-INF/resources/frontend/styles/ai-agent-chat.css
@ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/fragment/ChatPanelFragmentTurnDetailTest.java
@ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/fragment/ChatPanelFragmentTurnDetailHistoryTest.java
@ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/fragment/ChatPanelFragmentConversationIdTest.java

<interfaces>
Key call sites in ChatPanelFragment.java (line numbers approximate — verify before editing):
- `messageListSlot` (VerticalLayout, @ViewComponent, ~line 144) — the slot; currently holds `<vaadin-message-list>` then (as tail siblings) `turnActivityBlock` Div, NOTICE Divs, action-choice Divs, the status `<span>`.
- `messageList` (MessageList, ~line 184) — `messageList.setItems(...)` re-renders all `<vaadin-message>` light-DOM children, wiping any injected siblings inside it.
- `items` (`List<MessageListItem>`, ~line 187) — the ordered transcript items; index N in `items` corresponds to the N-th `<vaadin-message>` child of `messageList.getElement()` after `setItems`.
- `botMsg` (MessageListItem, ~line 242) — the current streaming assistant item; it is `items.get(items.size()-1)` during a turn.
- `messageCount` (int, ~line 248) — O(1) hasMessages() counter.
- `setConversationIdInternal(UUID cid)` (~line 593) — early-returns on `Objects.equals(this.conversationId, cid)`.
- `startNewChat()` (~line 678) — `setConversationIdInternal(null); updateSessionConversationId(null);`.
- `onNewChatButtonClick` (~line 557) — confirm-dialog → `startNewChat()`.
- `submitChatTurn(...)` (~line 768) — adds userItem + botMsg to `items`, `messageList.setItems(...)` (~788), `.doOnNext` (~811), `.doOnError` (~882: `liveTurnSteps.clear()` + `showGenericErrorNotification()` + `finishStreamInternal()`), `.doOnComplete` (~887: loadTurnSteps → appendTurnDetails → liveTurnSteps.clear() → finishStreamInternal()).
- `initializeConversationFromFinalEvent(UUID)` (~line 947) — sets `conversationId` from a Final event; guarded by `conversationId != null`.
- `activeRunId` (UUID) — set on the first Final event in `.doOnNext` (~line 813-815) BUT also potentially never set if the stream errors before any event. NOTE: `cancellationRegistry.register(activeRunId, ...)` in `.doOnSubscribe` (~807) implies `activeRunId` may already be assigned earlier — verify; the `.doOnError` sync below must null-check it.
- `appendTurnDetails(UUID runId, List<StepRow>)` (~1083) → `replaceTurnDetails(runId, details)` → `turnActivityBlock().add(details)`.
- `appendHistoryTurnDetails(UUID runId, UUID conversationId)` (~1094) → builds collapsed Details w/ lazy openedChangeListener → `replaceTurnDetails`.
- `turnActivityBlock()` (~1069) — lazily `new Div().addClassName("ai-agent-turn-activity"); messageListSlot.add(it)`.
- `appendNoticeRow(String text)` (~1385) — `messageListSlot.getElement().appendChild(notice <div ai-agent-attachment-notice>); messageCount++`.
- `appendActionChoiceRow(StreamEventRenderer.ActionProposalPayload)` (~1425) — builds `<div ai-agent-action-choice>`; `messageListSlot.add(row); actionChoiceRowsByProposalId.put(...); messageCount++`.
- `appendIntentConfirmRow(...)` (~1396) — `<div ai-agent-intent-confirm>`; `messageListSlot.add(row); messageCount++`. (Out of scope per the gap — leave as-is unless trivial, but if you anchor action-choice you may as well anchor this the same way; treat as optional.)
- `clearMessageList()` (~1623) — `items.clear(); turnDetailsByRunId.clear(); turnActivityBlock = null; messageListSlot.removeAll(); messageListSlot.getElement().removeAllChildren(); new MessageList(); messageListSlot.add(messageList); messageList.setItems(items); messageCount = 0; botMsg = null;`. ALSO nulls `liveTurnSteps`/`statusRow` (via removeAllChildren) — verify and preserve.
- `onDetach` (~539) — explicit `liveTurnSteps.clear()` + `removeStatusRow()`.
- TurnDetailRenderer constants: `STEP_TOOL_KEY = "chatView.turnDetail.step.tool"`, `STEP_RETRIEVAL_KEY = "...step.retrieval"`, `STEP_CHAT_KEY = "...step.chat"`, `UNKNOWN_DURATION_TEXT = "—"`, `summaryKey()`, `summaryArgs(int,long)`, `errorIndicatorKey()`.

Vaadin MessageList DOM contract (verify via Context7 `vaadin/flow-components` or `vaadin/web-components` "message-list message"): a `<vaadin-message-list>` renders one `<vaadin-message>` light-DOM child per `MessageListItem`, in order. After `setItems(list)` those children are replaced. You can `messageList.getElement().insertChild(domIndex, extraElement)` to splice a sibling between two `<vaadin-message>` elements — it stays until the next `setItems`. To anchor "after the N-th transcript item" insert at DOM index `N+1` IF only `<vaadin-message>` children exist; since you also splice your own extras you must recompute positions from a model (see Task 2).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Force a clear on "new conversation" after an errored turn + keep conversationId in sync on stream error</name>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java
    ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/fragment/ChatPanelFragmentConversationIdTest.java
  </files>
  <behavior>
    - Test (in ChatPanelFragmentConversationIdTest, plain JUnit5 + Mockito + reflective field injection — mirror the existing cases in that class): construct a ChatPanelFragment with `messageListSlot`, `messageList`, `messageInput`, `streamProgressBar`, `messages`, `currentAuthentication` injected; set `conversationId = null` (reflective), set `messageCount = 1` (reflective, simulating an errored turn that rendered a user message); call `startNewChat()`; assert `messageCount == 0` afterwards (the message list was cleared) AND `messageListSlot` no longer contains the stale rows. Without the fix this fails (early-return). Also assert the happy-path no-op is preserved: `conversationId = null, messageCount = 0` → `startNewChat()` does not throw and `messageCount` stays 0.
    - Test: `.doOnError` keeps `conversationId` in sync — assert via a source scan that `.doOnError` calls `initializeConversationFromFinalEvent(...)` (or an equivalent `conversationId`-sync helper) when `activeRunId != null` and `conversationId == null` (the orchestration created it server-side). A behavioral test of the reactive lambda needs a live UI, so a source-scan assertion (the established precedent in 15-04 tests) is acceptable here.
  </behavior>
  <action>
    GAP 1 FIX — two changes in `ChatPanelFragment.java`:

    (a) Force-clear on reset. In `setConversationIdInternal(UUID cid)` (~line 593), change the early-return guard so it does NOT short-circuit when there are stranded on-screen messages and we are resetting to no-conversation. Replace `if (Objects.equals(this.conversationId, cid)) { return; }` with: `if (Objects.equals(this.conversationId, cid) && !(cid == null && messageCount > 0)) { return; }`. Rationale: an errored turn leaves `conversationId == null` with `messageCount > 0`; `startNewChat()` must still clear. The rest of the method already handles `cid == null` (resets title/loader, calls `clearMessageList()` which resets `messageCount`/`items`/`turnDetailsByRunId`/`turnActivityBlock`). Add a one-line comment referencing "15-UAT Gap 1: new-conversation after an errored turn". Keep it minimal — do NOT touch the `isStreaming()`/`stopActiveStream()` block or the `cid != null` ownership-check branch.

    Optional belt-and-suspenders (only if (a) does not fully satisfy the test): make `startNewChat()` itself idempotently call `clearMessageList()` before/after `setConversationIdInternal(null)` — but prefer the single-guard fix; do NOT double-clear if (a) already works.

    (b) Sync conversationId on stream error. In `submitChatTurn(...)`'s `.doOnError` handler (~line 882), BEFORE `liveTurnSteps.clear()`, add: if `activeRunId != null && conversationId == null` then resolve the server-side conversation for this run and set `conversationId` via `accessUiAuthenticated(submitAuthentication, () -> initializeConversationFromFinalEvent(serverConversationId))`. SOURCING THE CONVERSATION ID: prefer the cheapest path that does not introduce a new dependency — read it from the `AiAuditEvent` CHAT root for `activeRunId` via the already-injected `unconstrainedDataManager` (`select e.conversation.id from ai_AiAuditEvent e where e.userUsername = :me and e.runId = :rid and e.parent is null` — mirror the `loadTurnSteps` mandatory-filter pattern; `.store("agentstore")` if raw `loadValues`; wrap in `catch (RuntimeException)` → `log.debug` + skip). If that read returns nothing (the run never reached the orchestration / never created a conversation), do nothing — the (a) fix already handles the clear. Do NOT block the error path on this read failing. Add a comment "15-UAT Gap 1 (orthogonal): keep conversationId in sync when the run errored after the server created the conversation".

    TEST: extend `ChatPanelFragmentConversationIdTest` with the two `@Test`s described in <behavior> above (force-clear-after-errored-turn + happy-path-no-op preserved; source-scan that `.doOnError` syncs conversationId). Follow the file's existing `inject(...)` / `setField(...)` helpers and construction pattern exactly.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.fragment.ChatPanelFragmentConversationIdTest"</automated>
  </verify>
  <done>`startNewChat()` clears stranded messages when `conversationId == null && messageCount > 0`; the happy-path no-op is preserved; `.doOnError` resolves+sets `conversationId` from the run's audit CHAT root when known (best-effort, never throws); the new tests are green.</done>
</task>

<task type="auto">
  <name>Task 2: Anchor turn-detail / action-choice / NOTICE elements inline under their turn's <vaadin-message>, re-anchored after every setItems and after history replay</name>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java
    ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/fragment/ChatPanelFragmentTurnDetailTest.java
    ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/fragment/ChatPanelFragmentTurnDetailHistoryTest.java
  </files>
  <behavior>
    - Test (ChatPanelFragmentTurnDetailTest): after `appendTurnDetails(runId, steps)` for a turn whose assistant `<vaadin-message>` is at transcript index N, the disclosure element (a `Div.ai-agent-turn-extra` wrapping the `<vaadin-details>`) is a child of `messageList.getElement()` (NOT a tail sibling of `<vaadin-message-list>` in `messageListSlot`) and sits at DOM index N+1 (immediately after the N-th `<vaadin-message>`), and `turnActivityBlock` is no longer used / no longer added to `messageListSlot`.
    - Test: after a subsequent `messageList.setItems(...)` (simulating the next turn's flush), calling the re-anchor pass (or `submitChatTurn`'s post-`setItems` hook) re-splices the extra back to DOM index N+1 — it survives the re-render.
    - Test: `appendActionChoiceRow(payload)` for the current `botMsg` turn splices the `Div.ai-agent-action-choice` immediately after that turn's `<vaadin-message>` (not at the `messageListSlot` tail); `messageCount` still increments by 1; the same-`proposalId` replace still works (no duplicate).
    - Test: `appendNoticeRow(text)` for the current turn splices the `Div.ai-agent-attachment-notice` (or keep it an `Element` `<div>`) after the current turn's `<vaadin-message>`; `messageCount` still increments.
    - Test (ChatPanelFragmentTurnDetailHistoryTest): after a history replay (`setConversationIdInternal(cid)` path is heavy to unit-test — instead drive `correlateHistoryTurnDetails` then the re-anchor pass directly, or assert via source scan that `correlateHistoryTurnDetails`/the replay loop calls the re-anchor pass after `messageList.setItems(...)`), each history `<vaadin-details>` is anchored after its ASSISTANT turn's `<vaadin-message>`, in order, and lazy-load-on-first-expand + memoization still hold (the existing query-count-spy assertions must still pass).
  </behavior>
  <action>
    GAP 2 — PART A: PER-TURN DOM ANCHORING. The constraint that motivated the old "tail of messageListSlot" shortcut (15-04 SUMMARY: "you cannot insert a sibling between two MessageListItems") is FALSE for the light DOM — `<vaadin-message>` elements are real light-DOM children of `<vaadin-message-list>` and `Element.insertChild(index, el)` splices between them; the splice survives until the next `setItems`. We exploit that, and re-anchor after each `setItems`.

    1. **Model the extras.** Add a small ordered registry keyed by transcript index (the index into `items`). Suggested shape: `private final List<TurnExtra> turnExtras = new ArrayList<>();` where `record TurnExtra(int turnIndex, Element element)` — `turnIndex` is the index in `items` of the assistant (or relevant) `<vaadin-message>` the extra hangs under; `element` is the wrapper Div's `.getElement()` (or the raw `<div>` Element for NOTICE). Keep `turnDetailsByRunId` as-is for replace-by-runId (its value is the `Details`; you also need the wrapper — store `Map<UUID, Element>` of wrapper elements, or wrap the Details lookup so `replaceTurnDetails` can find+remove the right `TurnExtra`). Choose the cleanest of: (i) `turnExtras` holds all extras + a `Map<UUID, TurnExtra>` side-index for runId; (ii) keep `turnDetailsByRunId` mapping runId→wrapper Div and reconstruct `turnExtras` order on demand. Either is fine — keep it simple, document the choice.

    2. **Resolve "turnIndex" for live appends.** When `appendTurnDetails(runId, steps)` / `appendActionChoiceRow(payload)` / `appendNoticeRow(text)` run during a live turn, the relevant `<vaadin-message>` is the current assistant turn = `items.size() - 1` IF `botMsg` is the last item (true during streaming and in `.doOnComplete`, which still runs before `finishStreamInternal`). For NOTICE rows replayed from history they get their `turnIndex` from the replay loop (see point 5). Capture the index at append time and store it in the `TurnExtra`.

    3. **`anchorExtra(int turnIndex, Element element)` helper** — replaces direct `messageListSlot.add(...)` / `messageListSlot.getElement().appendChild(...)` for the three extra kinds:
       - Wrap turn-detail `Details` and action-choice `Div` in a `Div` with class `ai-agent-turn-extra` (the indent wrapper from the mockup — `.ai-agent-turn-extra { margin-left: ...; }`). NOTICE rows: keep their `ai-agent-attachment-notice` `<div>` Element; either wrap them too in `ai-agent-turn-extra` or give the notice div the same `margin-left` directly — pick one and be consistent (the mockup only styles disclosure + action-choice; NOTICE indent is at your discretion but should not regress).
       - Compute the DOM insert index inside `messageList.getElement()`: walk the message-list's children, counting only `<vaadin-message>` children until you have passed `turnIndex` of them, plus any already-spliced `ai-agent-turn-extra` children that belong to turns ≤ turnIndex (so multiple extras on the same/earlier turns stay ordered). The robust way: rebuild from the model — `domChildIndex = 0; messagesSeen = 0;` iterate `messageList.getElement().getChildren()`; for each child, if it's a `<vaadin-message>` increment `messagesSeen`; insert position is "right after the (turnIndex+1)-th `<vaadin-message>`, after any extras already attached to turns ≤ turnIndex". Simplest correct implementation: do a full `reanchorAllExtras()` every time (point 4) rather than a surgical single-insert — call `reanchorAllExtras()` from `anchorExtra` after adding the new `TurnExtra` to `turnExtras` (kept sorted by `turnIndex`, stable for equal indices in append order).
       - `messageCount` bookkeeping: action-choice and NOTICE still `messageCount++` exactly as before; turn-detail does NOT count (it never did). Do not change the counter semantics.

    4. **`reanchorAllExtras()`** — the re-render-survival pass. (a) detach every `TurnExtra.element` from its current parent (`element.removeFromParent()`); (b) for each `TurnExtra` in `turnExtras` sorted by `(turnIndex, insertion-order)`, compute the DOM index in `messageList.getElement()` = position immediately after the `(turnIndex+1)`-th `<vaadin-message>` child, shifted past any extras for the same/earlier turns already re-inserted in this pass; `messageList.getElement().insertChild(domIndex, element)`. Guard: if `turnIndex >= number of <vaadin-message> children` (e.g. an extra whose turn was cleared), drop that `TurnExtra`. Call `reanchorAllExtras()` AFTER every `messageList.setItems(...)` in: `submitChatTurn` (~line 788, after the userItem+botMsg flush — extras for prior turns must re-anchor before the new turn streams), the streaming `.doOnComplete`/final-flush `setItems` if any (`finishStreamInternal` region — check ~line 786-788 and ~1019), `setConversationIdInternal`'s history replay (`messageList.setItems(new ArrayList<>(items))` ~line 660 — but the history extras are created AFTER that by `correlateHistoryTurnDetails`/`appendNoticeRow`, so call `reanchorAllExtras()` at the END of `setConversationIdInternal` once all history extras exist), and `clearMessageList`'s `messageList.setItems(items)` (~1645 — but `clearMessageList` clears `turnExtras` too, so nothing to re-anchor; just make sure `turnExtras.clear()` is added there alongside `turnDetailsByRunId.clear()`).

    5. **History replay.** In `setConversationIdInternal(cid)` (~line 628-666): when iterating `history`, for a NOTICE `AiMessage` you currently call `appendNoticeRow(m.getContent())` — that fires DURING the loop, before the final `messageList.setItems(...)`, so the notice would have no `<vaadin-message>` to anchor to yet. Restructure: while iterating, record the NOTICE's intended `turnIndex` = the index in `items` of the message it should follow (the last USER/ASSISTANT item appended so far, i.e. `items.size() - 1` at the moment the NOTICE is seen — or `items.size()` if you want it after; match the current "end-of-list approximation" but per-turn). Build the NOTICE extras after the `messageList.setItems(...)` flush, then `correlateHistoryTurnDetails(cid, assistantTurnCount)` runs (it already anchors history `<vaadin-details>` via `appendHistoryTurnDetails` → which must now also take a `turnIndex` — the index in `items` of the ASSISTANT `<vaadin-message>` for that root; the existing zip is positional over ASSISTANT turns, so track `assistantTurnIndex` = the `items` index of each ASSISTANT item in replay order and pass it). Finally call `reanchorAllExtras()`. Keep the no-guess correlation semantics (mismatch → no disclosures) exactly as-is.

    6. **Remove the dead `turnActivityBlock`.** `turnActivityBlock()` and the `turnActivityBlock` field become unused once `replaceTurnDetails` goes through `anchorExtra`. Delete them (and the field-null lines in `clearMessageList`/`onDetach`). NOTE: 15-04 left a literal-source-scan test (`NoticeRenderTest` scans `ChatPanelFragment.java` for banned tokens) — make sure removing `turnActivityBlock` does not leave a dangling reference, and that no new comment introduces a banned token (no `MessageBubbleComponent`, no raw tool/entity names).

    7. **Streaming wiring touch points to re-check:** `appendActionChoiceRow` is called from `.doOnNext` via `accessUi(...)` while `botMsg` is the last item → `turnIndex = items.size()-1` is correct there. `removeActionChoiceRow` / `removeAllActionChoiceRows` must also drop the matching `TurnExtra` from `turnExtras` (and decrement `messageCount` exactly as today — check `removeActionChoiceRow` ~line 1564 already does `messageCount--`). `appendIntentConfirmRow` (~1396): OPTIONAL — anchor it the same way for visual consistency, but if it complicates the diff, leave it as a `messageListSlot` tail sibling (it is out of the gap's explicit scope); document the choice either way.

    TESTS: extend `ChatPanelFragmentTurnDetailTest` and `ChatPanelFragmentTurnDetailHistoryTest` per <behavior>. The DOM-position assertions can run against a real detached `MessageList` (`new MessageList()`, `messageList.setItems(List.of(itemA, itemB, ...))`, then call `appendTurnDetails`/`anchorExtra`/`reanchorAllExtras` and assert `messageList.getElement().getChild(i)` identity / `.getTag()` / class list). The reactive-lambda wiring (re-anchor after `setItems` inside `submitChatTurn`/`.doOnComplete`) is asserted via a source scan, per the established precedent. Keep all existing assertions in those two files green (lazy-load query-count spy, no-leak input alphabet, em-dash, error indicator, no duplicates, count-mismatch → none).
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.fragment.ChatPanelFragmentTurnDetailTest" --tests "com.vn.agent.view.chat.fragment.ChatPanelFragmentTurnDetailHistoryTest" --tests "com.vn.agent.view.chat.fragment.ChatPanelFragmentStatusLineTest"</automated>
  </verify>
  <done>Turn-detail `<vaadin-details>` (wrapped in `.ai-agent-turn-extra`), action-choice `Div`, and NOTICE `<div>` are spliced inside `<vaadin-message-list>` immediately after their turn's `<vaadin-message>`; `reanchorAllExtras()` runs after every `messageList.setItems(...)` and at the end of history replay; `turnActivityBlock` is removed; `messageCount` bookkeeping, lazy-load + memoization, no-guess history correlation, status-line behavior, and `clearMessageList`/`onDetach` teardown are unchanged; the no-leak source-scan tests still pass; new + existing tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Restyle the turn-detail disclosure + action-choice block per the approved Option-A mockup</name>
  <files>ai-agent/ai-agent/src/main/resources/META-INF/resources/frontend/styles/ai-agent-chat.css</files>
  <action>
    GAP 2 — PART B: CSS RESTYLE. Edit ONLY `ai-agent-chat.css` (no `theme.json`, no `frontend/themes/`, no `frontend/generated/` edits — `@CssImport("./styles/ai-agent-chat.css")` on `ChatPanelFragment` already loads it on all 3 surfaces). Translate the mockup's `.turn-extra` / `details.turn-detail` / `.step-row` / `.action-choice` rules into the existing class names, swapping the mockup's `:root` custom props for `--lumo-*` tokens. Concretely:

    - `.ai-agent-turn-extra` — the indent wrapper from Task 2: `margin: var(--lumo-space-xs) 0 0 calc(var(--lumo-size-m) + var(--lumo-space-s));` (≈ avatar + gap so it aligns under the assistant bubble; the mockup uses 40px = avatar 30 + gap 10 — use the Lumo-token equivalent; if Vaadin's `<vaadin-message>` avatar size differs, eyeball it to ~40px via `var(--lumo-size-m)`). It wraps both the `<vaadin-details>` and the action-choice `Div`.
    - `.ai-agent-turn-activity` is now the class on the `<vaadin-details>` element itself (it was the old grouping Div — repurpose the selector, or rename to `.ai-agent-turn-detail` and update Task 2's `addClassName`; pick one, keep them consistent). Style: `border: 1px solid var(--lumo-contrast-10pct); border-radius: var(--lumo-border-radius-m); background: var(--lumo-contrast-5pct); overflow: hidden;`. Target the Vaadin Details shadow parts where needed: `vaadin-details::part(summary)` for padding/cursor/font-size/color (`var(--lumo-secondary-text-color)`, `var(--lumo-font-size-s)`), `vaadin-details::part(content)` for the steps padding. Add a ⚙ gear before the summary text — either a `::before` on `::part(summary)` with `content: "⚙ "` (simplest, no Java change) or, if shadow-part `::before` is unreliable in this Vaadin version, have Task 2 prepend a `Span` "⚙" with class `ai-agent-turn-activity__gear` into the Details summary (Details API: `setSummary(Component)` — coordinate with Task 2). Prefer the CSS `::before` if it renders; verify with Context7 `vaadin/web-components` "vaadin-details parts summary toggle". The rotating caret: Vaadin Details already shows a toggle in `::part(toggle)` — leave the default toggle (it already rotates) and just ensure it's visible; only restyle if it's hidden. (The mockup's hand-rolled `▸`/rotate is the native-`<details>` analog; `<vaadin-details>` has its own toggle button — do not double up.)
    - `.ai-agent-turn-activity__steps` (the content `VerticalLayout`) — `padding: var(--lumo-space-xs) var(--lumo-space-s) var(--lumo-space-s) var(--lumo-space-xl);` (left-indented under the summary, per mockup `.steps{padding:... 28px}`).
    - `.ai-agent-turn-activity__step` (each row `Div`) — `display: flex; align-items: center; gap: var(--lumo-space-s); font-size: var(--lumo-font-size-s); padding: 2px 0; color: var(--lumo-body-text-color);`.
    - Per-step icon: add a small leading icon chip. Cheapest path with NO Java change beyond a modifier class: in Task 2's `buildStepRow`, add a modifier class to the row based on `step.labelKey()` — `ai-agent-turn-activity__step--tool` / `--retrieval` / `--chat` (compare against `TurnDetailRenderer.STEP_TOOL_KEY` / `STEP_RETRIEVAL_KEY` / `STEP_CHAT_KEY`). Then in CSS, `.ai-agent-turn-activity__step::before { content: ""; width: 18px; height: 18px; border-radius: var(--lumo-border-radius-s); background: var(--lumo-primary-color-10pct); color: var(--lumo-primary-text-color); display: grid; place-items: center; font-size: 11px; flex-shrink: 0; }` and `.ai-agent-turn-activity__step--tool::before { content: "🔍"; }` / `--retrieval::before { content: "📄"; }` / `--chat::before { content: "💬"; }`. (Coordinate the modifier-class addition with Task 2 — note it in that task too.) The errored row: `.ai-agent-turn-activity__step--errored::before { content: "✕"; background: var(--lumo-error-color-10pct); color: var(--lumo-error-text-color); }` and `.ai-agent-turn-activity__step-error { color: var(--lumo-error-text-color); font-size: var(--lumo-font-size-xs); }` — Task 2's `buildStepRow` already adds `__step-error` Span when `step.errored()`; also add a `--errored` modifier class to the row in that case.
    - `.ai-agent-turn-activity__step-label` — `flex: 1;`.
    - `.ai-agent-turn-activity__step-duration` — `color: var(--lumo-secondary-text-color); font-variant-numeric: tabular-nums; font-size: var(--lumo-font-size-xs);` and ensure it's pushed to the right (`margin-left: auto;` if the label's `flex:1` doesn't already do it given the error span ordering — put the error span before the duration in DOM so duration stays rightmost, matching the mockup; coordinate with Task 2's `buildStepRow` child order: label, [error], duration).
    - `.ai-agent-action-choice` (already exists ~line 233) — restyle per mockup: `border: 1px solid var(--lumo-contrast-10pct); border-left: 3px solid var(--lumo-primary-color); border-radius: var(--lumo-border-radius-m); background: var(--lumo-primary-color-10pct); padding: var(--lumo-space-s) var(--lumo-space-m);`. (It now lives inside `.ai-agent-turn-extra`, so drop any old `margin` that assumed it was a `messageListSlot` tail sibling.) `.ai-agent-action-choice__summary` — `font-size: var(--lumo-font-size-s); margin-bottom: var(--lumo-space-s);`. The buttons inside already get Lumo `primary`/`small`/`tertiary` theme variants from Java — no extra button CSS needed beyond a `display:flex; gap:var(--lumo-space-s); flex-wrap:wrap;` on a button container if there is one (there isn't currently — buttons are direct children of the row; that's fine, just let them flow, or add `.ai-agent-action-choice { display: flex; flex-direction: column; }` and wrap buttons — keep minimal, don't over-engineer).
    - Keep the existing `.ai-agent-status` / `@keyframes ai-agent-status-pulse` / `prefers-reduced-motion` rules and the `.ai-agent-sidebar*` rules untouched. Keep `.ai-agent-attachment-notice` mostly as-is; if Task 2 wraps NOTICE in `.ai-agent-turn-extra`, the indent comes from the wrapper — otherwise add the same `margin-left` to `.ai-agent-attachment-notice` so it aligns too (your call, but don't regress its current look badly).
    - Do not introduce a `<style>` tag, do not touch `frontend/generated/`, do not add a new CSS file.

    If any Vaadin Details shadow-part selector doesn't take effect, fall back to a Java-side `Span`/`Component` summary (coordinate with Task 2) rather than `!important` hacks. Verify Details part names via Context7 (`vaadin/web-components`, query "vaadin-details summary content toggle parts").
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.*"</automated>
  </verify>
  <done>`.ai-agent-turn-activity` (the disclosure) has border + light Lumo-contrast tint + ⚙ in the summary + visible rotating toggle + per-step icon chips (🔍/📄/💬, ✕ red for errored) + right-aligned tabular-nums ms + red `(lỗi — đã hoàn tác)` row; `.ai-agent-action-choice` has a primary left border + primary-10pct tint; both indented via `.ai-agent-turn-extra`; only `--lumo-*` tokens; only `ai-agent-chat.css` changed; the `com.vn.agent.view.chat.*` suite (incl. ObservabilityLeakTest if it lives there — else run it explicitly per <verification>) is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chat user → AiAuditEvent (agentstore) | the new `.doOnError` conversation-id read crosses into audit data the chat user has no `@EntityPolicy` for |
| streamed assistant content → rendered DOM | turn-detail step rows / action-choice / NOTICE text spliced into the live message list |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06-01 | Information disclosure | `.doOnError` audit read for the run's conversation id | mitigate | Reuse the `loadTurnSteps` pattern exactly: `UnconstrainedDataManager` + MANDATORY `where e.userUsername = :me and e.runId = :rid and e.parent is null` filter; never `runId`-only unconstrained; narrow projection (only `conversation.id`); `catch (RuntimeException)` → `log.debug` + skip. No new dependency, no new query shape beyond the established one. |
| T-15-06-02 | Information disclosure | turn-detail step rows after the DOM re-anchor | mitigate | Anchoring changes only the DOM POSITION of the existing wrapper elements — the content path is unchanged: still funnels through `TurnDetailRenderer` (label-only `msg://` keys), still `Element.setText` / `Span(messages.getMessage(...))` (HTML-escaped), still no `toolName()`/`argsJson()`/`summary()`/`payloadJson()`/`eventName`/`queryText`. `ObservabilityLeakTest` re-run as a gate. |
| T-15-06-03 | Tampering / XSS | NOTICE row text spliced into `<vaadin-message-list>` light DOM | mitigate | NOTICE text already set via `Element.setText` (HTML-escaped by Vaadin); splicing via `insertChild` does not change escaping. No `innerHTML`, no markdown on these elements. |
| T-15-06-04 | Denial of service | `reanchorAllExtras()` O(extras × messages) per `setItems` | accept | Bounded: `turnExtras` ≤ (turns with tool activity) + (action-choice rows, deduped by proposalId) + (NOTICE rows); a single conversation's transcript. Same order of magnitude as the `items` list itself. No new persisted state. |
</threat_model>

<verification>
- `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.*"` — green (fragment + surface + stream + stop + intent + notice + turn-detail + crm-layout; no chat-flow regression).
- `./gradlew :ai-agent:ai-agent:test --tests "*ObservabilityLeakTest"` (or the exact class — the 15-05 leak gate; confirm the FQCN, it may be `com.vn.agent.view.chat.ObservabilityLeakTest` or under `...fragment`) — green: no `find_records`, no `jmixapp_*`, no args JSON in the rendered status text / turn-detail rows.
- `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.fragment.ChatPanelFragment*Test"` — green: the new Gap-1 (`...ConversationIdTest`) and Gap-2 (`...TurnDetailTest`, `...TurnDetailHistoryTest`) cases plus all pre-existing fragment tests.
- `./gradlew :ai-agent:ai-agent:test` — green (full add-on module; HSQLDB, no DB needed). NOTE: `:jmix-app:test` requires a live PostgreSQL `agentstore` and is OUT OF SCOPE (see `.planning/phases/15-.../deferred-items.md`); do not run it.
- `git diff --stat` sanity: only `ChatPanelFragment.java`, `ai-agent-chat.css`, and the three test files changed; NO change to `chat-panel-fragment.xml`, `AiAuditEvent.java`, `AiMessage.java`, `AiChatSessionState.java`, `TurnDetailRenderer.java` (Task 3's modifier-class CSS hooks are added in `buildStepRow` which is in `ChatPanelFragment.java`, not the renderer); NO new file under `**/liquibase/**`, NO `theme.json` / `frontend/themes/` / `frontend/generated/` edits; NO new `msg://` key needed (the ⚙ / step icons are CSS `::before` content or a hardcoded `Span("⚙")` — neither is localizable text; if Task 3 ends up needing the gear as a Java `Span`, "⚙" is a glyph, not a translatable string, so still no new key — but if you DO add any user-facing string, add it to BOTH `messages_en.properties` AND `messages_vi.properties`).
- Optional manual UI (app at http://localhost:8088 — do NOT auto-start `bootRun`): send a message that triggers a tool turn → the disclosure appears INDENTED under that assistant bubble inside the scroll area, styled (border/tint/⚙/icons/tabular ms); reload a history conversation → prior disclosures anchored under their turns, expand lazily; force an errored turn (e.g. break the OpenRouter key) → "new conversation" → confirm Yes → the screen clears.
</verification>

<success_criteria>
- `startNewChat()` clears stranded on-screen messages when the prior turn errored (`conversationId == null && messageCount > 0`); the happy-path no-op is preserved.
- `.doOnError` best-effort syncs `conversationId` from the run's `AiAuditEvent` CHAT root when `activeRunId` is known (never throws on failure).
- Turn-detail `<vaadin-details>` (in `.ai-agent-turn-extra`), action-choice `Div`, and NOTICE `<div>` render inside `<vaadin-message-list>` immediately after their turn's `<vaadin-message>`, re-anchored after every `messageList.setItems(...)` and at the end of history replay; `turnActivityBlock` is gone.
- The disclosure + action-choice match `15-option-A-mockup.html` (border + tint + ⚙ + caret + per-step icons + right-aligned tabular ms + red error-rollback row; action-choice = primary left border + primary-10pct tint), using only `--lumo-*` tokens, only `ai-agent-chat.css` touched.
- Label-only content preserved (`ObservabilityLeakTest` green); lazy-load + memoization of history step rows preserved; transient KIND-keyed status line, `messageCount` bookkeeping, `clearMessageList()`/`onDetach` teardown, and 3-surface sharing all unchanged.
- All `com.vn.agent.view.chat.*` tests + the new Gap-1/Gap-2 fragment tests + the full `:ai-agent:ai-agent` module suite are green.
</success_criteria>

<output>
After completion, create `.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-06-SUMMARY.md`
</output>
</content>
</invoke>
