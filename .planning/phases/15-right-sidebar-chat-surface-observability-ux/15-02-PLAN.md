---
phase: 15-right-sidebar-chat-surface-observability-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/StreamingEvent.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/StreamEventRenderer.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/rag/advisor/AuditingDocumentRetriever.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/rag/advisor/RetrievalAugmentationAdvisorFactory.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/audit/ToolCallbackAuditDecorator.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/RenderStreamEventTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/orchestration/StreamingActivityEventTest.java
autonomous: true
requirements: [OBS-01, OBS-04]

# REVIEW RESOLUTION (15-REVIEWS.md, points #6 + #11): The "emit Activity(CHAT) explicitly vs derive CHAT in the
# UI" discretion left open by D-05 is DECIDED HERE — do NOT emit Activity(CHAT) from the orchestration edge.
# Rationale: (a) avoids emit-ordering races with the toolSink registration in DefaultChatServiceImpl.stream(...)
# (the sink is registered before subscription, but a CHAT marker emitted there is fragile relative to when the
# model stream actually starts); (b) D-05 only *requires* TOOL and RETRIEVAL be explicitly signalled. Plan 04's
# UI shows the neutral typing indicator at turn start and flips to CHAT/"thinking…" on the first `Content` event
# (latest Activity wins, but Content arrival always implies CHAT — point #6). Consequence: `DefaultChatServiceImpl.java`
# is NOT modified by this plan (it was dropped from files_modified). `chatView.status.neutral` ships in Plan 04.
# `ActivityKind` still includes CHAT (it is a legal audit `kind` value and the UI uses statusKeyFor(CHAT)); we
# simply never *emit* Activity(CHAT) from the edge.

must_haves:
  truths:
    - "A new additive StreamingEvent.Activity(ActivityKind) variant exists; ActivityKind is a closed enum {CHAT, TOOL, RETRIEVAL}"
    - "AuditingDocumentRetriever emits Activity(RETRIEVAL) at retrieval start via the existing streaming sink holder, best-effort (never breaks retrieval)"
    - "ToolCallbackAuditDecorator emits Activity(TOOL) alongside the existing ToolCall emit, best-effort"
    - "Activity(CHAT) is NOT emitted from the orchestration edge (decided per review point #11) — the UI derives CHAT from the first Content event of the turn; DefaultChatServiceImpl is not modified"
    - "Adding the variant breaks no existing code: every exhaustive switch over StreamingEvent gains a no-op Activity arm; ChatPanelFragment's instanceof chain is updated in Plan 04 (not here)"
    - "The emit sites are OUTSIDE ChatService-proper, the advisor chain, and the persisted AiAuditEvent shape — no change to persisted audit rows"
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/StreamingEvent.java"
      provides: "StreamingEvent.Activity record + ActivityKind enum + permits-clause entry"
      contains: "record Activity"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/StreamEventRenderer.java"
      provides: "no-op `case StreamingEvent.Activity` arm in renderStreamEventDetails"
      contains: "Activity"
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/orchestration/StreamingActivityEventTest.java"
      provides: "tests proving the RETRIEVAL/TOOL emit sites push Activity(...) onto the sink, best-effort"
  key_links:
    - from: "AuditingDocumentRetriever.retrieve(query)"
      to: "StreamingSinkHolder.currentOrForRun(runId)"
      via: "tryEmitNext(new StreamingEvent.Activity(ActivityKind.RETRIEVAL)) wrapped in try/catch(RuntimeException)"
      pattern: "Activity\\(.*RETRIEVAL"
    - from: "ToolCallbackAuditDecorator.callInternal(...)"
      to: "StreamingSinkHolder sink (via emitToolEvent)"
      via: "emitToolEvent(runId, sink -> sink.tryEmitNext(new StreamingEvent.Activity(ActivityKind.TOOL)))"
      pattern: "Activity\\(.*TOOL"
    - from: "RetrievalAugmentationAdvisorFactory.retrievalAugmentationAdvisor(...)"
      to: "new AuditingDocumentRetriever(..., streamingSinkHolder)"
      via: "added StreamingSinkHolder constructor arg"
      pattern: "StreamingSinkHolder"
---

<objective>
Add the additive `StreamingEvent.Activity(ActivityKind kind)` variant (closed enum `{CHAT, TOOL, RETRIEVAL}`) per decision D-05, and emit it from the orchestration edge for the two kinds D-05 requires — `AuditingDocumentRetriever.retrieve(...)` emits `Activity(RETRIEVAL)` at retrieval start; `ToolCallbackAuditDecorator` emits `Activity(TOOL)` alongside its existing `ToolCall` emit. Per review point #11, `Activity(CHAT)` is NOT emitted from the edge — `DefaultChatServiceImpl` is unchanged, and Plan 04's UI derives CHAT from the first `Content` event of the turn. Add the compiler-forced no-op `Activity` arm to every exhaustive `switch` over `StreamingEvent` (currently only `StreamEventRenderer.renderStreamEventDetails`). All emit sites are best-effort (swallow `RuntimeException` — observability must never break a turn).

Purpose: This variant is the KIND source for Plan 04's ephemeral streaming-status line. The no-leak guarantee is structural — only a closed enum constant ever crosses to the UI; the fragment maps it to a `msg://` string and never touches `toolName`/`argsJson`. Plan 04 (the fragment consumer) and Plan 03 (the surface) both depend on or sit alongside this; landing it in Wave 1 unblocks them.

Output: `StreamingEvent.Activity` + `ActivityKind`; `Activity(RETRIEVAL)`/`Activity(TOOL)` emit calls in the retriever and tool decorator; the `RetrievalAugmentationAdvisorFactory` constructor-arg wiring; updated `StreamEventRenderer`; extended `RenderStreamEventTest`; new `StreamingActivityEventTest`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-SPEC.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-CONTEXT.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-PATTERNS.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-RESEARCH.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-REVIEWS.md

<interfaces>
<!-- StreamingEvent is a sealed interface; existing permits: Content, ToolCall, ToolResult, Citation, Final, Error. Add Activity. -->
<!-- New nested enum: enum ActivityKind { CHAT, TOOL, RETRIEVAL } ; new record: record Activity(ActivityKind kind) implements StreamingEvent {} -->
<!-- StreamingSinkHolder exposes currentOrForRun(UUID runId): Optional<Sinks.Many<StreamingEvent>> (prefers forRun(runId), falls back to current()); current() reads RunContext.get(). This is the existing emit hook used by ToolCallbackAuditDecorator.emitToolEvent. register(runId, sink)/unregister(runId) are called by DefaultChatServiceImpl.stream(...). -->
<!-- ToolCallbackAuditDecorator.emitToolEvent(UUID runId, Consumer<Sinks.Many<StreamingEvent>> emitter): { if (streamingSinkHolder == null) return; try { streamingSinkHolder.currentOrForRun(runId).ifPresent(emitter); } catch (RuntimeException ex) { log.debug(...); } } — the best-effort pattern to mirror everywhere. -->
<!-- AuditingDocumentRetriever resolves runId in retrieve(Query): UUID runId = RunContext.get(); if (runId == null && query.context().get("audit.runId") instanceof UUID u) runId = u; — emit Activity(RETRIEVAL) at that point. It is instantiated INLINE by RetrievalAugmentationAdvisorFactory (not a @Component), so the factory must pass it the StreamingSinkHolder bean (constructor change). -->
<!-- DefaultChatServiceImpl: NOT modified by this plan (per review point #11 — Activity(CHAT) is not emitted from the edge; the UI derives CHAT). -->
<!-- StreamEventRenderer.renderStreamEventDetails(StreamingEvent event, ...) is an exhaustive `switch (event)` over the sealed type (arms for Content/ToolCall/ToolResult/Citation/Error/Final) — the compiler forces a `case StreamingEvent.Activity ... -> RenderedStreamEvent.markdown("")` (no markdown contribution; status is handled by the fragment). It is the ONLY exhaustive switch over StreamingEvent in main (DefaultChatServiceImpl and ChatPanelFragment use instanceof chains, not switch). -->
<!-- AiToolCallOutcome — the existing outcome enum carried by StreamingEvent.ToolResult. ActivityKind is independent of it. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the StreamingEvent.Activity variant and the compiler-forced renderer arm</name>
  <files>ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/StreamingEvent.java, ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/StreamEventRenderer.java, ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/RenderStreamEventTest.java</files>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/StreamingEvent.java (the sealed interface — add to `permits`, add the `ActivityKind` enum + the `Activity` record)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/StreamEventRenderer.java (the `renderStreamEventDetails(...)` exhaustive `switch` at ~line 120 — arms `case StreamingEvent.Content`/`ToolCall`/`ToolResult`/`Citation`/`Error`/`Final`; add the no-op `Activity` arm)
    - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/RenderStreamEventTest.java (the test being extended)
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-PATTERNS.md (sections "orchestration/StreamingEvent.java" + "view/chat/fragment/StreamEventRenderer.java")
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-RESEARCH.md (Pattern 3 + Pitfall 5 — the sealed-switch exhaustiveness hazard)
  </read_first>
  <behavior>
    - `StreamingEvent.ActivityKind.values()` == {CHAT, TOOL, RETRIEVAL}.
    - `new StreamingEvent.Activity(StreamingEvent.ActivityKind.TOOL) instanceof StreamingEvent` is true; `.kind()` returns TOOL.
    - `StreamEventRenderer.renderStreamEventDetails(new StreamingEvent.Activity(...), labels, citationState)` returns a `RenderedStreamEvent` whose markdown is empty (no contribution).
    - Existing renderer behavior for Content/ToolCall/ToolResult/Citation/Final/Error is unchanged.
  </behavior>
  <action>
    In `StreamingEvent.java`: add `StreamingEvent.Activity,` to the `permits` clause (between `Citation` and `Final`, mirroring the existing layout); add a nested closed enum `enum ActivityKind { CHAT, TOOL, RETRIEVAL }` with a Javadoc noting it never carries a `@Tool` method name or an entity name (Phase 15 D-05) and noting that CHAT is a legal value but is never *emitted* from the orchestration edge (the UI derives it from the first Content event — review point #11); add `record Activity(ActivityKind kind) implements StreamingEvent {}` alongside the other records. In `StreamEventRenderer.renderStreamEventDetails(...)`: add `case StreamingEvent.Activity ignoredActivity -> RenderedStreamEvent.markdown("");` (no markdown contribution — the status line is rendered by the fragment from the raw event, not via the markdown path), mirroring the existing `case StreamingEvent.Final ignoredFinal -> RenderedStreamEvent.markdown("");` arm. Extend `RenderStreamEventTest` with a case asserting `renderStreamEventDetails(new StreamingEvent.Activity(...))` produces empty markdown for each `ActivityKind`. Run a grep proxy after the change: `grep -rn "switch.*event\|StreamingEvent\\.Activity" ai-agent/ai-agent/src/main/java` and confirm `StreamEventRenderer.renderStreamEventDetails` is the only exhaustive `switch` over `StreamingEvent` (the fragment + `DefaultChatServiceImpl` use `instanceof` chains — verified at execute time); record the grep result in the SUMMARY.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.RenderStreamEventTest" --tests "com.vn.agent.view.chat.RenderStreamEventIntentPayloadTest"</automated>
  </verify>
  <acceptance_criteria>
    - `StreamingEvent.java` contains `record Activity(ActivityKind kind)` and `enum ActivityKind { CHAT, TOOL, RETRIEVAL }` and `StreamingEvent.Activity,` in the `permits` clause.
    - `StreamEventRenderer.java` contains a `case StreamingEvent.Activity` arm returning empty markdown.
    - `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.RenderStreamEventTest" --tests "com.vn.agent.view.chat.RenderStreamEventIntentPayloadTest"` exits 0.
    - The whole add-on test module still compiles (`./gradlew :ai-agent:ai-agent:compileTestJava` exits 0) — no other exhaustive `switch` over `StreamingEvent` was missed.
    - The SUMMARY records the grep proxy result confirming the renderer is the only exhaustive switch.
  </acceptance_criteria>
  <done>StreamingEvent.Activity + ActivityKind exist; the renderer has its no-op arm; RenderStreamEventTest green; add-on test module compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit Activity(RETRIEVAL) / Activity(TOOL) from the orchestration edge, best-effort (NOT Activity(CHAT))</name>
  <files>ai-agent/ai-agent/src/main/java/com/vn/agent/rag/advisor/AuditingDocumentRetriever.java, ai-agent/ai-agent/src/main/java/com/vn/agent/rag/advisor/RetrievalAugmentationAdvisorFactory.java, ai-agent/ai-agent/src/main/java/com/vn/agent/audit/ToolCallbackAuditDecorator.java, ai-agent/ai-agent/src/test/java/com/vn/agent/orchestration/StreamingActivityEventTest.java</files>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/audit/ToolCallbackAuditDecorator.java (the `callInternal(...)` body + the `emitToolEvent(runId, ...)` helper — the best-effort emit pattern; add the Activity(TOOL) emit alongside the existing ToolCall emit)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/rag/advisor/AuditingDocumentRetriever.java (the `retrieve(Query)` method — where `runId` is already resolved; add the Activity(RETRIEVAL) emit there; add a `private final StreamingSinkHolder streamingSinkHolder;` constructor field)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/rag/advisor/RetrievalAugmentationAdvisorFactory.java (the `retrievalAugmentationAdvisor(...)` @Bean — add a `StreamingSinkHolder` constructor param and pass it to the inline `new AuditingDocumentRetriever(...)`)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/StreamingSinkHolder.java (the `currentOrForRun(runId)` hook)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java (READ ONLY — confirm the `toolSink` register/unregister machinery; do NOT modify it: Activity(CHAT) is not emitted per review point #11)
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-PATTERNS.md (sections "audit/ToolCallbackAuditDecorator.java", "rag/advisor/AuditingDocumentRetriever.java", "rag/advisor/RetrievalAugmentationAdvisorFactory.java")
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-RESEARCH.md (Pitfall 6 — the wrong-runId hazard; the "Best-effort streaming emit" shared pattern; Code Examples for the emit sites)
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-REVIEWS.md (points #6 + #11 — the CHAT-emit decision and the first-Content-implies-CHAT sequencing rule that Plan 04 consumes)
    - ai-agent/ai-agent/src/test/java/com/vn/agent/audit/ (any existing ToolCallbackAuditDecorator test to model the new test on)
  </read_first>
  <behavior>
    - When a tool callback runs with a registered streaming sink for the run, the sink receives a `StreamingEvent.Activity(ActivityKind.TOOL)` (in addition to the existing `ToolCall`/`ToolResult` events).
    - When RAG retrieval runs with a registered streaming sink for the run, the sink receives a `StreamingEvent.Activity(ActivityKind.RETRIEVAL)` at the start of `retrieve(...)`.
    - `Activity(CHAT)` is never emitted from the orchestration edge — `DefaultChatServiceImpl` is unchanged.
    - If `streamingSinkHolder` is null or `tryEmitNext` throws, the tool call / retrieval still completes normally (no exception propagates) — same best-effort semantics as the existing `emitToolEvent`.
    - The persisted `AiAuditEvent` rows for a turn are byte-identical to before — no new audit row, no changed shape.
  </behavior>
  <action>
    In `ToolCallbackAuditDecorator.callInternal(...)`: before/with the existing `emitToolEvent(runId, sink -> sink.tryEmitNext(new StreamingEvent.ToolCall(...)))` add `emitToolEvent(runId, sink -> sink.tryEmitNext(new StreamingEvent.Activity(StreamingEvent.ActivityKind.TOOL)));` — reuse the existing `emitToolEvent` helper (it already null-guards `streamingSinkHolder` and swallows `RuntimeException`). In `AuditingDocumentRetriever`: add `private final StreamingSinkHolder streamingSinkHolder;` as a constructor field (last param); at the START of `retrieve(Query query)`, after `runId` is resolved exactly as the existing code does (`RunContext.get()` then `query.context().get("audit.runId")`), if `streamingSinkHolder` is non-null call `streamingSinkHolder.currentOrForRun(runId).ifPresent(sink -> sink.tryEmitNext(new StreamingEvent.Activity(StreamingEvent.ActivityKind.RETRIEVAL)))` wrapped in `try { ... } catch (RuntimeException ignored) { /* observability emit must never break retrieval */ }` — mirror the existing audit-write-failure-never-rethrows convention in this class. In `RetrievalAugmentationAdvisorFactory.retrievalAugmentationAdvisor(...)`: add a `StreamingSinkHolder streamingSinkHolder` constructor/method param (it's a `@Component`, injectable) and pass it as the new last arg to the inline `new AuditingDocumentRetriever(...)`. Do NOT touch `DefaultChatServiceImpl` (Activity(CHAT) is not emitted — review point #11). Add a new `StreamingActivityEventTest` (under `com.vn.agent.orchestration`) that: drives a `ToolCallbackAuditDecorator` with a stub delegate + a `StreamingSinkHolder` carrying a real `Sinks.Many<StreamingEvent>` for a known `runId`, runs a tool call, and asserts the sink emitted an `Activity(TOOL)`; drives an `AuditingDocumentRetriever` (with a stub `VectorStore` delegate or the existing stub config) with a sink registered for the run and asserts an `Activity(RETRIEVAL)` was emitted; asserts that with a `null` `streamingSinkHolder` (or a sink whose `tryEmitNext` throws) the tool call / retrieval still completes. Keep the test minimal and mirror whatever stub harness existing audit-decorator tests use.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.orchestration.StreamingActivityEventTest" --tests "com.vn.agent.audit.*" --tests "com.vn.agent.rag.advisor.*"</automated>
  </verify>
  <acceptance_criteria>
    - `ToolCallbackAuditDecorator.java` contains a `new StreamingEvent.Activity(StreamingEvent.ActivityKind.TOOL)` emit via `emitToolEvent`.
    - `AuditingDocumentRetriever.java` has a `StreamingSinkHolder` constructor field and a `new StreamingEvent.Activity(StreamingEvent.ActivityKind.RETRIEVAL)` emit at retrieval start wrapped in a `catch (RuntimeException ...)`.
    - `RetrievalAugmentationAdvisorFactory.java` passes a `StreamingSinkHolder` into `new AuditingDocumentRetriever(...)`.
    - `DefaultChatServiceImpl.java` is unchanged (`git diff` shows no edit) — Activity(CHAT) is not emitted.
    - `StreamingActivityEventTest` exists and asserts: a tool call emits `Activity(TOOL)`; a retrieval emits `Activity(RETRIEVAL)`; a throwing/absent sink does not break the call.
    - `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.orchestration.StreamingActivityEventTest" --tests "com.vn.agent.audit.*" --tests "com.vn.agent.rag.advisor.*"` exits 0.
    - `git diff` shows no change to `AiAuditEvent.java`, no new audit-row write, no change to the advisor-chain assembly beyond passing the sink-holder bean.
  </acceptance_criteria>
  <done>The RETRIEVAL/TOOL emit sites push Activity(...) onto the sink best-effort; the advisor factory wires the sink-holder; DefaultChatServiceImpl untouched; StreamingActivityEventTest green; persisted audit shape unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestration edge (retriever / tool decorator) → streaming sink → UI | The new `Activity(ActivityKind)` event crosses to the browser via the existing `StreamingEvent` flux; only a closed enum constant ever crosses — no `toolName`, no `argsJson`, no entity name |
| RAG retrieval / tool callback → observability emit | A best-effort side-effect; a failure here must not affect the security-critical retrieval/tool path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-B1 | Information disclosure | `StreamingEvent.Activity` payload reaching the UI | mitigate | `ActivityKind` is a closed enum `{CHAT, TOOL, RETRIEVAL}` — structurally cannot carry a `@Tool` method name or entity name. The emit sites pass only the enum constant, never `toolName`/`argsJson`/`resultSummary`. TEST-19 (Plan 05) enforces the rendered text never matches the Phase 9 leak regexes. |
| T-15-B2 | Denial of service | observability emit failing inside `retrieve(...)` or a tool callback | mitigate | Every `tryEmitNext(new StreamingEvent.Activity(...))` is wrapped in `try { } catch (RuntimeException ignored) { }` (mirrors the existing `ToolCallbackAuditDecorator.emitToolEvent` and the retriever's audit-write-never-rethrows convention); `streamingSinkHolder` is null-guarded. Tested in `StreamingActivityEventTest`. |
| T-15-B3 | Tampering | persisted `AiAuditEvent` shape / the advisor chain / `DefaultChatServiceImpl` | mitigate (avoid) | The emit sites are deliberately OUTSIDE `ChatService`-proper, the advisor chain, and the audit-write path — no new audit row, no changed row shape, no advisor-chain behavior change beyond the factory injecting one extra `@Component`; `DefaultChatServiceImpl` is not modified at all (review point #11). Verified by `git diff` (no `AiAuditEvent.java` / `DefaultChatServiceImpl.java` change) and the unchanged audit-row tests. |
| T-15-B4 | Spoofing | `Activity(RETRIEVAL)` emitted with the wrong `runId` so it lands on another run's sink | mitigate | `runId` resolved exactly as the existing retriever code does (`RunContext.get()` then `query.context().get("audit.runId")`); `streamingSinkHolder.currentOrForRun(runId)` (prefers `forRun(runId)`, falls back to `current()`) — same lookup `ToolCallbackAuditDecorator` already trusts. Worst case on mismatch: the status line silently doesn't show — no cross-user leak (the sink is per-run, registered by `DefaultChatServiceImpl.stream`). |
</threat_model>

<verification>
- `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.RenderStreamEventTest" --tests "com.vn.agent.orchestration.StreamingActivityEventTest" --tests "com.vn.agent.audit.*" --tests "com.vn.agent.rag.advisor.*"` green.
- `./gradlew :ai-agent:ai-agent:test` (full add-on module) green — catches any sealed-`switch` exhaustiveness fallout and chat-flow regression (`ChatViewStreamTest` etc.).
- `git diff` shows no change to `AiAuditEvent.java` or `DefaultChatServiceImpl.java`; no new file under `**/liquibase/**`; the advisor-chain `@Bean` only gained a `StreamingSinkHolder` arg.
- Grep proxy: `StreamEventRenderer.renderStreamEventDetails` is the only exhaustive `switch` over `StreamingEvent`; recorded in the SUMMARY.
</verification>

<success_criteria>
- `StreamingEvent.Activity(ActivityKind)` exists; `ActivityKind` is `{CHAT, TOOL, RETRIEVAL}`.
- `Activity(RETRIEVAL)` / `Activity(TOOL)` emitted at the right edge, best-effort; `Activity(CHAT)` is NOT emitted (decided per review point #11); `DefaultChatServiceImpl` unchanged.
- No exhaustive `switch` over `StreamingEvent` left without an `Activity` arm; add-on test module compiles.
- No change to the persisted `AiAuditEvent` shape or the advisor-chain behavior beyond the sink-holder injection.
- All touched + full add-on tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-02-SUMMARY.md` recording: the confirmed no-Activity(CHAT) decision and that `DefaultChatServiceImpl` was not modified, the grep proxy result (the only exhaustive switch), and the exact emit-site lines added.
</output>
