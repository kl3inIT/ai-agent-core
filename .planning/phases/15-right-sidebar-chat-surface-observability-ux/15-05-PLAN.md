---
phase: 15-right-sidebar-chat-surface-observability-ux
plan: 05
type: execute
wave: 4
depends_on: [15-04-PLAN.md]
files_modified:
  - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/ObservabilityLeakTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/ObservabilityMessagesCompletenessTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/NoNewPersistedStateTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/AiChatSessionStateTest.java
  - .planning/todos/pending/2026-04-26-add-collapsible-tool-detail-and-ephemeral-status-to-chat-ui.md
  - .planning/todos/done/2026-04-26-add-collapsible-tool-detail-and-ephemeral-status-to-chat-ui.md
  - .planning/todos/done/_INDEX.md
  - .planning/todos/pending/_INDEX.md
autonomous: true
requirements: [TEST-19, OBS-01, OBS-02, OBS-04]

must_haves:
  truths:
    - "D-09: An automated UI-layer leak test reuses the Phase 9 HOST_PREFIX_LEAK / TOOL_NAME_LEAK pattern packs (NOT forked) against the rendered text of the streaming-status line and the per-turn tool-detail disclosure, across all ActivityKind values and an errored/rolled-back step, and asserts no @Tool method name and no raw entity name appears"
    - "The leak test FAILS when a tool/entity name is deliberately routed through the status/disclosure rendering path (negative control)"
    - "A test asserts every new Phase-15 msg:// key resolves in messages_en.properties AND messages_vi.properties"
    - "A structural test asserts no new @Entity / @Table / Liquibase changelog was added for the observability features (no Phase-15 include in agentstore-changelog.xml / changelog.xml; the @Entity-class count is unchanged)"
    - "AiChatSessionStateTest asserts AiChatSessionState still holds only currentConversationId + listeners — no new field, no growing per-turn collection"
    - "The 2026-04-26-add-collapsible-tool-detail-and-ephemeral-status-to-chat-ui todo is moved from .planning/todos/pending/ to .planning/todos/done/ and the todo INDEX files are updated"
  artifacts:
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/ObservabilityLeakTest.java"
      provides: "TEST-19 — UI-layer leak test reusing Phase 9 pattern packs"
      contains: "ToolNamePatternProvider"
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/NoNewPersistedStateTest.java"
      provides: "structural assertion: no new @Entity/@Table/changelog for Phase 15"
      contains: "NoNewPersistedStateTest"
    - path: ".planning/todos/done/2026-04-26-add-collapsible-tool-detail-and-ephemeral-status-to-chat-ui.md"
      provides: "the folded todo, moved to done/"
  key_links:
    - from: "ObservabilityLeakTest"
      to: "ToolNamePatternProvider / HostPrefixPatternProvider (Phase 9)"
      via: "new ToolNamePatternProvider(List.of(), new AiAgentGuardProperties(...)).buildPattern() -> Pattern.compile(provider.asPattern().orElseThrow().regex()); autowired HostPrefixPatternProvider in @SpringBootTest"
      pattern: "PatternProvider"
    - from: "ObservabilityLeakTest"
      to: "TurnDetailRenderer + the ChatPanelFragment status line / Details rendering"
      via: "render the actual <span> text and the Details step-row texts for every ActivityKind + an ERROR outcome and assert the regexes find nothing; negative control routes a tool name and asserts a hit"
      pattern: "TurnDetailRenderer"
---

<objective>
Close out Phase 15 with the cross-cutting safety + structural tests and the folded-todo move: (1) TEST-19 — `ObservabilityLeakTest`, a UI-layer leak test reusing the Phase 9 `HOST_PREFIX_LEAK` / `TOOL_NAME_LEAK` pattern-pack regexes VERBATIM (not forked) against the rendered text of the streaming-status line and the per-turn tool-detail disclosure, across every `ActivityKind` value and an errored/rolled-back step, with a negative control that deliberately routes a tool/entity name through the rendering path and asserts the matcher trips (D-09); (2) `ObservabilityMessagesCompletenessTest` — every new Phase-15 `msg://` key resolves in `messages_en.properties` AND `messages_vi.properties`; (3) `NoNewPersistedStateTest` — no new `@Entity`/`@Table`/Liquibase changelog was added for the observability features (OBS-04); (4) extend `AiChatSessionStateTest` to assert `AiChatSessionState` is unchanged (still `currentConversationId` + listeners, no growing per-turn collection); (5) move `.planning/todos/pending/2026-04-26-add-collapsible-tool-detail-and-ephemeral-status-to-chat-ui.md` to `.planning/todos/done/` and update both todo `_INDEX.md` files (CONTEXT.md "Folded Todos" + SPEC.md "In scope").

Purpose: TEST-19 is the explicit Phase 15 leak gate; the structural tests lock OBS-04; the todo move is the documented phase-completion bookkeeping. This plan depends on Plan 04 (the fragment rendering + `TurnDetailRenderer` must exist to render the text the leak test scans).

Output: `ObservabilityLeakTest`, `ObservabilityMessagesCompletenessTest`, `NoNewPersistedStateTest`; extended `AiChatSessionStateTest`; the moved todo file + updated INDEX files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-SPEC.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-CONTEXT.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-PATTERNS.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-RESEARCH.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-04-SUMMARY.md
@.planning/todos/pending/2026-04-26-add-collapsible-tool-detail-and-ephemeral-status-to-chat-ui.md

<interfaces>
<!-- Phase 9 leak-guard providers (do NOT fork — reuse verbatim):
  - com.vn.agent.guard.ToolNamePatternProvider — PATTERN_KEY = "TOOL_NAME_LEAK"; construct: new ToolNamePatternProvider(java.util.List.of(), new com.vn.agent.guard.AiAgentGuardProperties(null, null, null, null)) -> .buildPattern() -> .asPattern().orElseThrow().regex() -> Pattern.compile(...). (Modeled in guard/ToolNameLeakScannerTest.java.)
  - com.vn.agent.guard.HostPrefixPatternProvider — PATTERN_KEY = "HOST_PREFIX_LEAK"; needs a Metadata with the host metaclasses. In a @SpringBootTest the real Metadata bean is available — autowire HostPrefixPatternProvider and call .asPattern(). Outside one: mirror ToolNameLeakScannerTest.noOpHostProvider() (mocked Metadata) or HostPrefixLeakScannerTest.providerFor(Set<String> metaclassNames, boolean enabled).
  - com.vn.agent.guard.AiAgentGuardProperties — the 4-arg constructor (nulls => enabled defaults).
  - asPattern() returns Optional<AiAgentGuardProperties.OutputScanner.Pattern(key, regex)>. -->
<!-- TEST harness: @UiTest + @SpringBootTest(classes = {AITestConfiguration.class, FlowuiTestAssistConfiguration.class}) + @ImportAutoConfiguration({AIAutoConfiguration.class, SpiDefaultsAutoConfiguration.class}) — used by ChatSurfaceMounterTest; reuse for ObservabilityLeakTest (needs Messages + the fragment + the real HostPrefixPatternProvider). TurnDetailRenderer is Vaadin-free so its label text can be resolved with Messages and scanned without a UI. -->
<!-- TurnDetailRenderer (Plan 04): statusKeyFor(ActivityKind) / neutralStatusKey() / stepRow(ActivityKind|String, long, AiToolCallOutcome) / errorIndicatorKey() / summaryArgs(int,long) — label-only msg:// keys. The rendered status <span> text == messages.getMessage(TurnDetailRenderer.statusKeyFor(kind)); a Details step-row text == messages.getMessage(step.labelKey()) [+ ms] [+ messages.getMessage(errorIndicatorKey())]. -->
<!-- AiToolCallOutcome — the outcome enum with an ERROR/rollback value (for the errored-step case in the leak test). -->
<!-- AiChatSessionState — @VaadinSessionScope (or similar); fields: currentConversationId + a listeners collection. AiChatSessionStateTest is the existing test. -->
<!-- New Phase-15 msg:// keys to assert present in both bundles (final list from the 15-01 / 15-03 / 15-04 SUMMARYs): com.vn.agent.entity/AiChatSurface.SIDEBAR ; chatSurfaceMounter.sidebarToggle.ariaLabel.open ; chatSurfaceMounter.sidebarToggle.ariaLabel.closed ; the in-panel-closer aria-label key ; chatView.status.chat ; chatView.status.tool ; chatView.status.retrieval ; chatView.status.neutral ; chatView.turnDetail.summary ; chatView.turnDetail.step.tool ; chatView.turnDetail.step.retrieval ; chatView.turnDetail.errorIndicator . (Confirm the exact names against the SUMMARYs at execute time.) -->
<!-- @Entity classes: enumerate them via classpath scan or a hardcoded known set; the count must be unchanged vs. before Phase 15 (Phase 15 adds NONE). Liquibase: ai-agent/ai-agent/src/main/resources/**/liquibase/changelog/** + agentstore-changelog.xml + changelog.xml — no Phase-15 include. -->
<!-- Todo move: .planning/todos/pending/2026-04-26-add-collapsible-tool-detail-and-ephemeral-status-to-chat-ui.md -> .planning/todos/done/ ; update .planning/todos/pending/_INDEX.md (remove the entry) and .planning/todos/done/_INDEX.md (add the entry). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TEST-19 — ObservabilityLeakTest reusing the Phase 9 pattern packs against the rendered status line + per-turn disclosure</name>
  <files>ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/ObservabilityLeakTest.java</files>
  <read_first>
    - ai-agent/ai-agent/src/test/java/com/vn/agent/guard/ToolNameLeakScannerTest.java (the construction pattern for `ToolNamePatternProvider` + `Pattern.compile(provider.asPattern().orElseThrow().regex())` + `noOpHostProvider()`)
    - ai-agent/ai-agent/src/test/java/com/vn/agent/guard/HostPrefixLeakScannerTest.java (the construction pattern for `HostPrefixPatternProvider` — `providerFor(Set<String> metaclassNames, boolean enabled)` / autowired in `@SpringBootTest`)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/guard/ToolNamePatternProvider.java + HostPrefixPatternProvider.java + AiAgentGuardProperties.java (the providers being reused — do NOT modify them)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/TurnDetailRenderer.java (Plan 04 — the label-only mapper whose output is scanned)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java (Plan 04 — `showStatus` / `appendTurnDetails` produce the rendered text; the test renders that text)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/StreamingEvent.java (`ActivityKind` values)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallOutcome.java (the ERROR/rollback outcome value)
    - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/ChatSurfaceMounterTest.java (the `@UiTest` + `@SpringBootTest` + `@ImportAutoConfiguration` harness to mirror)
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-PATTERNS.md (section "NEW view/chat/ObservabilityLeakTest.java" — exact construction + assertion shape)
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-RESEARCH.md (Validation Architecture → TEST-19 row; Code Examples "TEST-19 leak assertion"; Security Domain "Internal @Tool method name … leaking")
  </read_first>
  <behavior>
    - For every `StreamingEvent.ActivityKind k`, the rendered streaming-status `<span>` text (i.e. `messages.getMessage(TurnDetailRenderer.statusKeyFor(k))`, plus the neutral text) matches NEITHER the compiled `TOOL_NAME_LEAK` regex NOR the compiled `HOST_PREFIX_LEAK` regex.
    - For a per-turn `Details` containing steps for every renderable `kind` ({"TOOL","RETRIEVAL"}) including one with an `AiToolCallOutcome` ERROR/rollback value, every rendered step-row text (the resolved `step.labelKey()` label, the per-step ms text, and the error-indicator text) matches NEITHER regex; the `Details` summary text (`MessageFormat`-formatted with the step count + total ms) matches NEITHER regex.
    - Negative control: deliberately constructing a step-row label / a status text from a raw tool name (`find_records`) or a host-prefixed entity name (a metaclass name from the host metamodel) and scanning it → the `TOOL_NAME_LEAK` (or `HOST_PREFIX_LEAK`) regex DOES match (`.find()` true) — proving the test would catch a regression.
    - The test reuses `ToolNamePatternProvider` / `HostPrefixPatternProvider` exactly as the Phase 9 scanner tests do — no new pattern definitions.
  </behavior>
  <action>
    Create `view/chat/ObservabilityLeakTest.java` using the `@UiTest` + `@SpringBootTest(classes = {AITestConfiguration.class, FlowuiTestAssistConfiguration.class})` + `@ImportAutoConfiguration({AIAutoConfiguration.class, SpiDefaultsAutoConfiguration.class})` harness from `ChatSurfaceMounterTest` (so `Messages` and the real `HostPrefixPatternProvider` are available). Build the regexes: `Pattern toolNameRegex()` via `new com.vn.agent.guard.ToolNamePatternProvider(java.util.List.of(), new com.vn.agent.guard.AiAgentGuardProperties(null, null, null, null))` → `.buildPattern()` → `Pattern.compile(provider.asPattern().orElseThrow().regex())` (mirror `ToolNameLeakScannerTest`); `Pattern hostPrefixRegex()` from the autowired `HostPrefixPatternProvider.asPattern()` (or `HostPrefixLeakScannerTest.providerFor(...)` if no host metaclasses are registered in the test context — pick whichever the existing scanner tests do). Test `statusLineNeverEmitsInternalNames`: for each `StreamingEvent.ActivityKind k` and the neutral key, resolve `messages.getMessage(TurnDetailRenderer.statusKeyFor(k))` (and `neutralStatusKey()`), assert `tool.matcher(text).find()` is false and `host.matcher(text).find()` is false. Test `turnDetailDisclosureNeverEmitsInternalNames`: build `TurnDetailRenderer.StepRow`s for {`TOOL`, `RETRIEVAL`} including one with the ERROR/rollback `AiToolCallOutcome`; for each, resolve `messages.getMessage(step.labelKey())`, the per-step-ms text (`String.valueOf(step.latencyMs())` or however the row renders ms), and `messages.getMessage(TurnDetailRenderer.errorIndicatorKey())` for the errored row; also resolve the summary text via `MessageFormat.format(messages.getMessage("chatView.turnDetail.summary"), TurnDetailRenderer.summaryArgs(2, 1234L))`; assert neither regex matches any of those strings. (If feasible without excessive ceremony, also drive a stub stream through a real `ChatPanelFragment` instance and scan the actual `<span.ai-agent-status>` text and the actual `Details` row texts from the rendered DOM — but the `TurnDetailRenderer`-output scan is the load-bearing assertion since the renderer is the only thing that feeds those elements.) Test `negativeControl_routingAToolNameTrips`: build a string from `"find_records"` (and one from a host metaclass name obtained via the autowired `Metadata`) and assert `toolNameRegex().matcher("...find_records...").find()` is true (and `hostPrefixRegex().matcher("...<metaclass>...").find()` is true) — proving the regexes are live and the test is meaningful. Add a comment in the test referencing Phase 9 (`HOST_PREFIX_LEAK` / `TOOL_NAME_LEAK` pattern packs, reused verbatim per D-09).
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.ObservabilityLeakTest"</automated>
  </verify>
  <acceptance_criteria>
    - `ObservabilityLeakTest.java` exists, references `com.vn.agent.guard.ToolNamePatternProvider` and `com.vn.agent.guard.HostPrefixPatternProvider` (reused, not forked), and contains: a status-line scan over all `ActivityKind` values + the neutral key; a disclosure scan over {TOOL, RETRIEVAL} step rows incl. an ERROR/rollback outcome + the summary text; and a negative-control case asserting the regexes DO match a deliberately-routed `find_records` / host-metaclass name.
    - No new file or change under `ai-agent/ai-agent/src/main/java/com/vn/agent/guard/` (the pattern packs are reused as-is).
    - `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.ObservabilityLeakTest"` exits 0.
  </acceptance_criteria>
  <done>TEST-19 is implemented: the UI-layer leak test reuses the Phase 9 pattern packs against the rendered status line + disclosure across all ActivityKind values + an errored step, with a passing negative control; green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Locale-completeness + no-new-persisted-state + AiChatSessionState-unchanged tests</name>
  <files>ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/ObservabilityMessagesCompletenessTest.java, ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/NoNewPersistedStateTest.java, ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/AiChatSessionStateTest.java</files>
  <read_first>
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties + messages_vi.properties (the bundles — confirm the exact new Phase-15 key names against the 15-01 / 15-03 / 15-04 SUMMARYs)
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-01-SUMMARY.md, 15-03-SUMMARY.md, 15-04-SUMMARY.md (the authoritative list of new msg:// keys + the no-DDL confirmations)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/AiChatSessionState.java (confirm it's still `currentConversationId` + listeners)
    - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/AiChatSessionStateTest.java (the test being extended)
    - ai-agent/ai-agent/src/main/resources/ — locate `agentstore-changelog.xml` / `changelog.xml` and the `liquibase/changelog/` directory (to assert no Phase-15 include)
    - any existing locale-completeness test in the codebase (e.g. a `MessagesCompletenessTest` / `I18n*Test`) to model `ObservabilityMessagesCompletenessTest` on; if none exists, write a minimal one that loads both `.properties` files via `java.util.Properties` and asserts the key set
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-RESEARCH.md (Validation Architecture → the OBS-04 / OBS-01-locale rows; Wave 0 Gaps for `NoNewPersistedStateTest` / `ObservabilityMessagesCompletenessTest`)
  </read_first>
  <behavior>
    - `ObservabilityMessagesCompletenessTest`: for the explicit list of new Phase-15 `msg://` keys (from the SUMMARYs), every key is present and non-empty in `messages_en.properties` AND in `messages_vi.properties` (load both with `java.util.Properties`); a key present in one bundle but missing in the other fails the test.
    - `NoNewPersistedStateTest`: the count of `@Entity`-annotated classes under `com.vn.agent.entity` (classpath scan or a snapshot list) equals the pre-Phase-15 count (Phase 15 adds NONE); `agentstore-changelog.xml` and the root `changelog.xml` contain no `<include>` referencing a Phase-15 changelog; no file under `**/liquibase/changelog/**` has a Phase-15-related name. (If a classpath scan is awkward, a hardcoded expected entity-name set with a comment "Phase 15 adds no entity — OBS-04" is acceptable.)
    - `AiChatSessionStateTest` (extended): `AiChatSessionState` declares only `currentConversationId` (+ its accessor) and the listeners collection (+ its add/remove/notify methods) — no new field, and in particular no collection that grows per chat turn; a reflective field-set assertion or an explicit "expected fields" set enforces this.
  </behavior>
  <action>
    Create `view/chat/ObservabilityMessagesCompletenessTest.java`: a plain JUnit test that loads `messages_en.properties` and `messages_vi.properties` via `java.util.Properties` (from the classpath, e.g. `getClass().getResourceAsStream("/com/vn/agent/messages_en.properties")`), defines the list of new Phase-15 keys (copy the final names from the 15-01 / 15-03 / 15-04 SUMMARYs — at minimum: `com.vn.agent.entity/AiChatSurface.SIDEBAR`, `chatSurfaceMounter.sidebarToggle.ariaLabel.open`, `chatSurfaceMounter.sidebarToggle.ariaLabel.closed`, the in-panel-closer aria-label key, `chatView.status.chat`, `chatView.status.tool`, `chatView.status.retrieval`, `chatView.status.neutral`, `chatView.turnDetail.summary`, `chatView.turnDetail.step.tool`, `chatView.turnDetail.step.retrieval`, `chatView.turnDetail.errorIndicator`), and asserts each is present + non-blank in both. Create `view/chat/NoNewPersistedStateTest.java`: assert no Phase-15 `@Entity` was added (classpath scan of `com.vn.agent.entity` for `@JmixEntity`/`@Entity` and compare to an expected set/count with the OBS-04 rationale comment) and that `agentstore-changelog.xml` / `changelog.xml` (read as resources) contain no Phase-15 `<include file=...>` and no `**/liquibase/changelog/**` file has a Phase-15 name. Extend `AiChatSessionStateTest` with a `sessionStateHasNoPerTurnAccumulation` test asserting `AiChatSessionState`'s declared fields are exactly {`currentConversationId`, the listeners collection} (reflective `getDeclaredFields()` filtered to non-static, compared to an expected name set) — i.e. no new field was added by Phase 15. Keep all three tests minimal and dependency-light.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.ObservabilityMessagesCompletenessTest" --tests "com.vn.agent.view.chat.NoNewPersistedStateTest" --tests "com.vn.agent.view.chat.AiChatSessionStateTest"</automated>
  </verify>
  <acceptance_criteria>
    - `ObservabilityMessagesCompletenessTest.java` asserts every new Phase-15 `msg://` key resolves non-blank in `messages_en.properties` AND `messages_vi.properties`.
    - `NoNewPersistedStateTest.java` asserts no new `@Entity` and no Phase-15 Liquibase changelog/include (OBS-04).
    - `AiChatSessionStateTest` has a test asserting `AiChatSessionState`'s fields are unchanged (no per-turn accumulation).
    - `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.ObservabilityMessagesCompletenessTest" --tests "com.vn.agent.view.chat.NoNewPersistedStateTest" --tests "com.vn.agent.view.chat.AiChatSessionStateTest"` exits 0.
  </acceptance_criteria>
  <done>Locale completeness, no-new-persisted-state, and bounded-session-state are all asserted by automated tests; green.</done>
</task>

<task type="auto">
  <name>Task 3: Move the folded todo to done/ and update the todo INDEX files</name>
  <files>.planning/todos/pending/2026-04-26-add-collapsible-tool-detail-and-ephemeral-status-to-chat-ui.md, .planning/todos/done/2026-04-26-add-collapsible-tool-detail-and-ephemeral-status-to-chat-ui.md, .planning/todos/pending/_INDEX.md, .planning/todos/done/_INDEX.md</files>
  <read_first>
    - .planning/todos/pending/2026-04-26-add-collapsible-tool-detail-and-ephemeral-status-to-chat-ui.md (the todo being moved — read it to write the done/ entry summary)
    - .planning/todos/pending/_INDEX.md (the pending index — remove the entry)
    - .planning/todos/done/_INDEX.md (the done index — add the entry, mirroring the format of the existing done entries like `2026-04-26-hide-internal-tool-and-entity-names-from-user-facing-chat.md`)
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-CONTEXT.md ("Folded Todos" section — the directive)
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-SPEC.md ("In scope" — "Moving the … todo to done/ when the phase ships")
  </read_first>
  <action>
    Move `.planning/todos/pending/2026-04-26-add-collapsible-tool-detail-and-ephemeral-status-to-chat-ui.md` to `.planning/todos/done/2026-04-26-add-collapsible-tool-detail-and-ephemeral-status-to-chat-ui.md` (use the Write tool to create the file in `done/` with the original content plus a short trailing note: "Resolved by Phase 15 — Right-Sidebar Chat Surface & Observability UX (OBS-01 + OBS-02, decisions D-05..D-08); shipped <date>." — then delete the `pending/` copy via `rm`). In `.planning/todos/pending/_INDEX.md`: remove the line referencing this todo. In `.planning/todos/done/_INDEX.md`: add a line for this todo mirroring the format of the existing done entries (date-prefixed filename + a one-line description noting it was resolved by Phase 15). Do not touch any other todo file.
  </action>
  <verify>
    <automated>test -f .planning/todos/done/2026-04-26-add-collapsible-tool-detail-and-ephemeral-status-to-chat-ui.md && ! test -f .planning/todos/pending/2026-04-26-add-collapsible-tool-detail-and-ephemeral-status-to-chat-ui.md && echo MOVED</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/todos/done/2026-04-26-add-collapsible-tool-detail-and-ephemeral-status-to-chat-ui.md` exists and carries the resolution note; `.planning/todos/pending/2026-04-26-add-collapsible-tool-detail-and-ephemeral-status-to-chat-ui.md` no longer exists.
    - `.planning/todos/pending/_INDEX.md` no longer references the todo; `.planning/todos/done/_INDEX.md` references it (with a Phase-15 resolution note), formatted like the other done entries.
    - The verify command prints `MOVED`.
  </acceptance_criteria>
  <done>The 2026-04-26 collapsible-tool-detail / ephemeral-status todo is in done/ with a Phase-15 resolution note; both INDEX files updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (test-only) the rendered status line / disclosure text ↔ the Phase 9 leak regexes | The test asserts the rendering path's output never matches the internal-name patterns — this IS the threat mitigation, exercised |
| planning bookkeeping (todo files) | No runtime trust boundary — file moves under `.planning/` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-E1 | Information disclosure | internal `@Tool` method name / raw host-entity name leaking into the streaming-status line or per-turn disclosure | mitigate | `ObservabilityLeakTest` (TEST-19) reuses the Phase 9 `TOOL_NAME_LEAK` / `HOST_PREFIX_LEAK` regexes against the rendered text across all `ActivityKind` values + an errored step, with a passing negative control proving the test catches a regression. This is the explicit Phase 15 leak gate; combined with the structural no-leak design from Plan 02 (closed `ActivityKind`) + Plan 04 (`TurnDetailRenderer` closed input alphabet), the leak surface is closed and verified. |
| T-15-E2 | Tampering | new persisted state / parallel store sneaking in under the observability features | mitigate | `NoNewPersistedStateTest` asserts no new `@Entity`/`@Table`/Liquibase changelog; `AiChatSessionStateTest` asserts `AiChatSessionState` is unchanged (no per-turn accumulation). Locks OBS-04. |
| T-15-E3 | Information disclosure | a new user-visible label shipping in only one locale (untranslated string leaking through) | mitigate | `ObservabilityMessagesCompletenessTest` asserts every new Phase-15 `msg://` key resolves non-blank in `messages_en.properties` AND `messages_vi.properties`. |
| T-15-E4 | (none) | the todo-file move | accept | Pure `.planning/` bookkeeping; no runtime impact, no threat. |
</threat_model>

<verification>
- `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.ObservabilityLeakTest" --tests "com.vn.agent.view.chat.ObservabilityMessagesCompletenessTest" --tests "com.vn.agent.view.chat.NoNewPersistedStateTest" --tests "com.vn.agent.view.chat.AiChatSessionStateTest"` green.
- `./gradlew test` (whole composite) green — the phase gate before `/gsd-verify-work`.
- No change under `ai-agent/ai-agent/src/main/java/com/vn/agent/guard/`; no new file under `**/liquibase/**`.
- `.planning/todos/done/2026-04-26-add-collapsible-tool-detail-and-ephemeral-status-to-chat-ui.md` exists; the `pending/` copy is gone; both INDEX files updated.
</verification>

<success_criteria>
- TEST-19 met: a UI-layer leak test reusing the Phase 9 pattern packs proves the status line + disclosure never emit internal `@Tool`/entity names (across all `ActivityKind` values + an errored step), with a passing negative control.
- OBS-04 locked by `NoNewPersistedStateTest` + the extended `AiChatSessionStateTest`; OBS-01/OBS-02 locale coverage locked by `ObservabilityMessagesCompletenessTest`.
- The 2026-04-26 todo is moved to `done/` with a Phase-15 resolution note; INDEX files updated.
- Whole composite test suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-05-SUMMARY.md` recording: the final list of Phase-15 `msg://` keys asserted by `ObservabilityMessagesCompletenessTest`, how `NoNewPersistedStateTest` enumerates entities, and confirmation the todo move + INDEX updates are done.
</output>
