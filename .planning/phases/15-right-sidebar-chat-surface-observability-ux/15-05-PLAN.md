---
phase: 15-right-sidebar-chat-surface-observability-ux
plan: 05
type: execute
wave: 4
depends_on: [15-04-PLAN.md]
files_modified:
  - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/ObservabilityLeakTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/ObservabilityMessagesCompletenessTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/NoNewPersistedStateTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/AiChatSessionStateTest.java
  - .planning/todos/pending/2026-04-26-add-collapsible-tool-detail-and-ephemeral-status-to-chat-ui.md
  - .planning/todos/done/2026-04-26-add-collapsible-tool-detail-and-ephemeral-status-to-chat-ui.md
  - .planning/todos/done/_INDEX.md
  - .planning/todos/pending/_INDEX.md
autonomous: true
requirements: [TEST-19, OBS-01, OBS-02, OBS-04]

# REVIEW RESOLUTION (15-REVIEWS.md):
# - Point #9 (Codex LOW) — TEST-19 must scan a REAL rendered component, not just the TurnDetailRenderer mapper
#   output. FIX: ObservabilityLeakTest keeps the TurnDetailRenderer-output scans (cheap, exhaustive over ActivityKind
#   + the error outcome) AND adds at least one assertion that drives a stub stream through a real ChatPanelFragment
#   (`@UiTest`) and scans the ACTUAL `<span.ai-agent-status>` text and an ACTUAL rendered `Details` step-row text
#   (inside the `.ai-agent-turn-activity` block) against the Phase 9 regexes. Keeps the negative control.
# - Point #10 (Codex LOW) — NoNewPersistedStateTest must NOT pin a global @Entity count (an adjacent phase could
#   add an entity concurrently). FIX: it asserts (i) no Phase-15-named Liquibase changelog file exists and no
#   Phase-15 `<include>` was added to `agentstore-changelog.xml` / `changelog.xml`, AND (ii) the specific state
#   holders this phase touches did not gain a per-turn-growing collection — `AiChatSessionState` keeps exactly
#   {currentConversationId, the listeners collection}, and the sidebar open-state holder (`MountedChatSurfaceState`
#   inside `ChatSurfaceMounter`, or `AiChatUIState` — whichever Plan 03 chose) gained only the fixed sidebar
#   fields {sidebarToggleButton, sidebarPanelDiv/host, sidebarOpen} and no per-turn collection — rather than a
#   brittle hardcoded entity count. (Optional belt-and-braces: a classpath scan asserting no class under
#   `com.vn.agent.entity` references "Turn"/"Activity"/"Disclosure" in its `@Table` name — but the changelog +
#   state-holder assertions are the load-bearing ones.)

must_haves:
  truths:
    - "D-09: An automated UI-layer leak test reuses the Phase 9 HOST_PREFIX_LEAK / TOOL_NAME_LEAK pattern packs (NOT forked) against (a) the TurnDetailRenderer-mapped status/disclosure text across all ActivityKind values + an errored/rolled-back step AND (b) the ACTUAL rendered <span.ai-agent-status> text + an ACTUAL rendered Details step-row text from a real ChatPanelFragment driven by a stub stream — asserting no @Tool method name and no raw entity name appears"
    - "The leak test FAILS when a tool/entity name is deliberately routed through the status/disclosure rendering path (negative control)"
    - "A test asserts every new Phase-15 msg:// key resolves non-blank in messages_en.properties AND messages_vi.properties"
    - "A structural test asserts no Phase-15 Liquibase changelog/include was added (no Phase-15-named changelog file; no Phase-15 <include> in agentstore-changelog.xml / changelog.xml) — NOT a hardcoded global @Entity count"
    - "AiChatSessionStateTest asserts AiChatSessionState still holds exactly {currentConversationId, listeners} — no new field, no per-turn-growing collection — and the sidebar open-state holder gained only fixed sidebar fields, not a per-turn collection"
    - "The 2026-04-26-add-collapsible-tool-detail-and-ephemeral-status-to-chat-ui todo is moved from .planning/todos/pending/ to .planning/todos/done/ and the todo INDEX files are updated"
  artifacts:
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/ObservabilityLeakTest.java"
      provides: "TEST-19 — UI-layer leak test reusing Phase 9 pattern packs, scanning both the mapper output and a real rendered ChatPanelFragment's status span + Details row"
      contains: "ToolNamePatternProvider"
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/NoNewPersistedStateTest.java"
      provides: "structural assertion: no Phase-15 Liquibase changelog/include + touched state holders gained no per-turn collection"
      contains: "NoNewPersistedStateTest"
    - path: ".planning/todos/done/2026-04-26-add-collapsible-tool-detail-and-ephemeral-status-to-chat-ui.md"
      provides: "the folded todo, moved to done/"
  key_links:
    - from: "ObservabilityLeakTest"
      to: "ToolNamePatternProvider / HostPrefixPatternProvider (Phase 9)"
      via: "new ToolNamePatternProvider(List.of(), new AiAgentGuardProperties(...)).buildPattern() -> Pattern.compile(provider.asPattern().orElseThrow().regex()); autowired HostPrefixPatternProvider in @SpringBootTest"
      pattern: "PatternProvider"
    - from: "ObservabilityLeakTest"
      to: "TurnDetailRenderer + a real ChatPanelFragment (status span + Details row)"
      via: "scan messages.getMessage(TurnDetailRenderer.statusKeyFor(k)) etc. AND scan the rendered <span.ai-agent-status> text + a rendered Details step-row text from a stub-stream-driven ChatPanelFragment; negative control routes a tool name and asserts a hit"
      pattern: "TurnDetailRenderer"
---

<objective>
Close out Phase 15 with the cross-cutting safety + structural tests and the folded-todo move: (1) TEST-19 — `ObservabilityLeakTest`, a UI-layer leak test reusing the Phase 9 `HOST_PREFIX_LEAK` / `TOOL_NAME_LEAK` pattern-pack regexes VERBATIM (not forked) against BOTH the `TurnDetailRenderer`-mapped status/disclosure text (across every `ActivityKind` value + an errored/rolled-back step + the summary) AND the ACTUAL rendered text of a real `ChatPanelFragment` (the `<span.ai-agent-status>` and one `Details` step-row inside the `.ai-agent-turn-activity` block, driven by a stub stream — review point #9), with a negative control that deliberately routes a tool/entity name through the rendering path and asserts the matcher trips (D-09); (2) `ObservabilityMessagesCompletenessTest` — every new Phase-15 `msg://` key resolves non-blank in `messages_en.properties` AND `messages_vi.properties`; (3) `NoNewPersistedStateTest` — no Phase-15 Liquibase changelog/include was added AND the state holders this phase touched gained no per-turn-growing collection (OBS-04 — NOT a hardcoded global `@Entity` count, review point #10); (4) extend `AiChatSessionStateTest` to assert `AiChatSessionState` is unchanged (exactly `{currentConversationId, listeners}`, no per-turn collection) and the sidebar open-state holder gained only fixed sidebar fields; (5) move `.planning/todos/pending/2026-04-26-add-collapsible-tool-detail-and-ephemeral-status-to-chat-ui.md` to `.planning/todos/done/` and update both todo `_INDEX.md` files (CONTEXT.md "Folded Todos" + SPEC.md "In scope").

Purpose: TEST-19 is the explicit Phase 15 leak gate (now a genuine UI-layer test, not just a mapper unit test); the structural tests lock OBS-04 without brittleness; the todo move is the documented phase-completion bookkeeping. This plan depends on Plan 04 (the fragment rendering + `TurnDetailRenderer` must exist to render the text the leak test scans) and on Plan 03 (the sidebar open-state holder it asserts).

Output: `ObservabilityLeakTest`, `ObservabilityMessagesCompletenessTest`, `NoNewPersistedStateTest`; extended `AiChatSessionStateTest`; the moved todo file + updated INDEX files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-SPEC.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-CONTEXT.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-PATTERNS.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-RESEARCH.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-REVIEWS.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-01-SUMMARY.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-03-SUMMARY.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-04-SUMMARY.md
@.planning/todos/pending/2026-04-26-add-collapsible-tool-detail-and-ephemeral-status-to-chat-ui.md

<interfaces>
<!-- Phase 9 leak-guard providers (do NOT fork — reuse verbatim):
  - com.vn.agent.guard.ToolNamePatternProvider — PATTERN_KEY = "TOOL_NAME_LEAK"; construct: new ToolNamePatternProvider(java.util.List.of(), new com.vn.agent.guard.AiAgentGuardProperties(null, null, null, null)) -> .buildPattern() -> .asPattern().orElseThrow().regex() -> Pattern.compile(...). (Modeled in guard/ToolNameLeakScannerTest.java.)
  - com.vn.agent.guard.HostPrefixPatternProvider — PATTERN_KEY = "HOST_PREFIX_LEAK"; needs a Metadata with the host metaclasses. In a @SpringBootTest the real Metadata bean is available — autowire HostPrefixPatternProvider and call .asPattern(). Outside one: mirror ToolNameLeakScannerTest.noOpHostProvider() (mocked Metadata) or HostPrefixLeakScannerTest.providerFor(Set<String> metaclassNames, boolean enabled).
  - com.vn.agent.guard.AiAgentGuardProperties — the 4-arg constructor (nulls => enabled defaults).
  - asPattern() returns Optional<AiAgentGuardProperties.OutputScanner.Pattern(key, regex)>. -->
<!-- TEST harness: @UiTest + @SpringBootTest(classes = {AITestConfiguration.class, FlowuiTestAssistConfiguration.class}) + @ImportAutoConfiguration({AIAutoConfiguration.class, SpiDefaultsAutoConfiguration.class}) — used by ChatSurfaceMounterTest / the fragment @UiTests; reuse for ObservabilityLeakTest (needs Messages + the fragment + the real HostPrefixPatternProvider + the chat-flow stub harness). TurnDetailRenderer is Vaadin-free so its label text can be resolved with Messages and scanned without a UI; the rendered-component scan needs the @UiTest fragment harness. -->
<!-- TurnDetailRenderer (Plan 04): statusKeyFor(ActivityKind) / neutralStatusKey() / stepRow(ActivityKind|String, Long, AiToolCallOutcome) (NULLABLE latencyMs) / errorIndicatorKey() / unknownDurationKey() / summaryArgs(int,long) — label-only msg:// keys. The rendered status <span.ai-agent-status> text == messages.getMessage(TurnDetailRenderer.statusKeyFor(kind)) (or neutralStatusKey()); a rendered Details step-row text == messages.getMessage(step.labelKey()) [+ ms or em-dash] [+ messages.getMessage(errorIndicatorKey())]; the Details summary == MessageFormat.format(messages.getMessage("chatView.turnDetail.summary"), TurnDetailRenderer.summaryArgs(count, ms)). The per-turn Details live inside a `.ai-agent-turn-activity` Div appended after `<vaadin-message-list>` in `messageListSlot` (Plan 04 review-point #2 refinement). -->
<!-- AiToolCallOutcome — the outcome enum with an ERROR/rollback value (for the errored-step case in the leak test). -->
<!-- AiChatSessionState — @VaadinSessionScope; fields: `currentConversationId` (volatile UUID) + `listenerRegistrations` (CopyOnWriteArrayList). AiChatSessionStateTest is the existing test. The sidebar open-state holder is MountedChatSurfaceState (private static final class in ChatSurfaceMounter, stored via ComponentUtil.getData(ui, ...)) OR AiChatUIState (@UIScope) — Plan 03's SUMMARY records which; whichever it is, it gained only fixed sidebar fields (sidebarToggleButton / sidebarPanelDiv-or-host / sidebarOpen), no per-turn collection. The per-turn step list (`liveTurnSteps`, capped 50) lives on the ChatPanelFragment INSTANCE, not in any session/UI-scoped holder. -->
<!-- New Phase-15 msg:// keys to assert present in both bundles (confirm the final names against the 15-01 / 15-03 / 15-04 SUMMARYs): com.vn.agent.entity/AiChatSurface.SIDEBAR ; chatSurfaceMounter.sidebarToggle.ariaLabel.open ; chatSurfaceMounter.sidebarToggle.ariaLabel.closed ; chatSurfaceMounter.sidebarCloser.ariaLabel ; chatView.status.chat ; chatView.status.tool ; chatView.status.retrieval ; chatView.status.neutral ; chatView.turnDetail.summary ; chatView.turnDetail.step.tool ; chatView.turnDetail.step.retrieval ; chatView.turnDetail.errorIndicator ; chatView.turnDetail.unknownDuration (if a key was used rather than a literal "—"). -->
<!-- Liquibase: ai-agent/ai-agent/src/main/resources/**/liquibase/changelog/** + agentstore-changelog.xml + changelog.xml — assert no Phase-15-named changelog file (no "15-"/"phase-15"/"sidebar"/"observability" changelog file) and no Phase-15 `<include file=...>` was added. -->
<!-- Todo move: .planning/todos/pending/2026-04-26-add-collapsible-tool-detail-and-ephemeral-status-to-chat-ui.md -> .planning/todos/done/ ; update .planning/todos/pending/_INDEX.md (remove the entry) and .planning/todos/done/_INDEX.md (add the entry). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TEST-19 — ObservabilityLeakTest reusing the Phase 9 pattern packs against the mapper output AND a real rendered ChatPanelFragment</name>
  <files>ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/ObservabilityLeakTest.java</files>
  <read_first>
    - ai-agent/ai-agent/src/test/java/com/vn/agent/guard/ToolNameLeakScannerTest.java (the construction pattern for `ToolNamePatternProvider` + `Pattern.compile(provider.asPattern().orElseThrow().regex())` + `noOpHostProvider()`)
    - ai-agent/ai-agent/src/test/java/com/vn/agent/guard/HostPrefixLeakScannerTest.java (the construction pattern for `HostPrefixPatternProvider` — `providerFor(Set<String> metaclassNames, boolean enabled)` / autowired in `@SpringBootTest`)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/guard/ToolNamePatternProvider.java + HostPrefixPatternProvider.java + AiAgentGuardProperties.java (the providers being reused — do NOT modify them)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/TurnDetailRenderer.java (Plan 04 — the label-only mapper whose output is scanned)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java (Plan 04 — `showStatus` produces the `<span.ai-agent-status>` text; `appendTurnDetails` / `appendHistoryTurnDetails` produce the `Details` rows inside the `.ai-agent-turn-activity` block; the test renders that actual DOM via a stub stream)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/StreamingEvent.java (`ActivityKind` values + the stub-stream event types)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallOutcome.java (the ERROR/rollback outcome value)
    - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/fragment/ChatPanelFragmentTurnDetailTest.java (Plan 04 — the stub-stream + audit-row harness for driving a real ChatPanelFragment; reuse its setup)
    - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/ChatSurfaceMounterTest.java (the `@UiTest` + `@SpringBootTest` + `@ImportAutoConfiguration` harness to mirror)
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-PATTERNS.md (section "NEW view/chat/ObservabilityLeakTest.java")
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-RESEARCH.md (Validation Architecture → TEST-19 row; Code Examples "TEST-19 leak assertion"; Security Domain "Internal @Tool method name … leaking")
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-REVIEWS.md (point #9 — scan a real rendered component)
  </read_first>
  <behavior>
    - For every `StreamingEvent.ActivityKind k`, the resolved status text (`messages.getMessage(TurnDetailRenderer.statusKeyFor(k))`, plus the neutral text) matches NEITHER the compiled `TOOL_NAME_LEAK` regex NOR the compiled `HOST_PREFIX_LEAK` regex.
    - For per-turn `Details` step rows for {"TOOL","RETRIEVAL"} including one with an `AiToolCallOutcome` ERROR/rollback value: every resolved step-row text (the `step.labelKey()` label, the per-step ms text, and the error-indicator text) matches NEITHER regex; the `Details` summary text (`MessageFormat`-formatted with the step count + total ms) matches NEITHER regex.
    - A real `ChatPanelFragment` driven by a stub stream (an `Activity(TOOL)` then a `ToolCall`/`ToolResult` then `Final`, with a matching `AiAuditEvent` CHAT root + TOOL child seeded so the `Details` populates): the actual `<span.ai-agent-status>` text and at least one actual rendered `Details` step-row text (read from the rendered DOM, inside the `.ai-agent-turn-activity` block) match NEITHER regex.
    - Negative control: a step-row label / status text deliberately built from a raw tool name (`find_records`) or a host-prefixed entity name (a metaclass name from the host metamodel via the autowired `Metadata`) → the `TOOL_NAME_LEAK` (resp. `HOST_PREFIX_LEAK`) regex DOES match (`.find()` true) — proving the test would catch a regression.
    - The test reuses `ToolNamePatternProvider` / `HostPrefixPatternProvider` exactly as the Phase 9 scanner tests do — no new pattern definitions.
  </behavior>
  <action>
    Create `view/chat/ObservabilityLeakTest.java` using the `@UiTest` + `@SpringBootTest(classes = {AITestConfiguration.class, FlowuiTestAssistConfiguration.class})` + `@ImportAutoConfiguration({AIAutoConfiguration.class, SpiDefaultsAutoConfiguration.class})` harness from `ChatSurfaceMounterTest` / the fragment `@UiTest`s (so `Messages`, the real `HostPrefixPatternProvider`, the fragment, and the chat-flow stub harness are all available). Build the regexes: `Pattern toolNameRegex()` via `new com.vn.agent.guard.ToolNamePatternProvider(java.util.List.of(), new com.vn.agent.guard.AiAgentGuardProperties(null, null, null, null))` → `.buildPattern()` → `Pattern.compile(provider.asPattern().orElseThrow().regex())` (mirror `ToolNameLeakScannerTest`); `Pattern hostPrefixRegex()` from the autowired `HostPrefixPatternProvider.asPattern()` (or `HostPrefixLeakScannerTest.providerFor(...)` if no host metaclasses are registered in the test context — pick whichever the existing scanner tests do). Test `statusLineMapperNeverEmitsInternalNames`: for each `StreamingEvent.ActivityKind k` and the neutral key, resolve `messages.getMessage(TurnDetailRenderer.statusKeyFor(k))` (and `neutralStatusKey()`), assert `tool.matcher(text).find()` is false and `host.matcher(text).find()` is false. Test `turnDetailMapperNeverEmitsInternalNames`: build `TurnDetailRenderer.StepRow`s for {`TOOL`, `RETRIEVAL`} including one with the ERROR/rollback `AiToolCallOutcome` and one with a `null` latencyMs; for each, resolve `messages.getMessage(step.labelKey())`, the per-step-ms text (a real value or the em-dash / `unknownDurationKey()`), and `messages.getMessage(TurnDetailRenderer.errorIndicatorKey())` for the errored row; also resolve the summary text via `MessageFormat.format(messages.getMessage("chatView.turnDetail.summary"), TurnDetailRenderer.summaryArgs(2, 1234L))`; assert neither regex matches any of those strings. Test `renderedFragmentNeverEmitsInternalNames` (the genuine UI-layer assertion — review point #9): using the Plan-04 fragment stub harness, seed an `AiConversation`+CHAT root+one TOOL child (with `latencyMs` set, a benign `outcome`) for a known `runId`, open the fragment for that conversation, drive a stub stream emitting `Activity(TOOL)` then a matching `ToolCall`/`ToolResult` then `Final(runId, ...)`; from the rendered DOM read the actual `<span.ai-agent-status>` text (after `Activity(TOOL)`, before `Final` removes it — capture it mid-stream) and, after `Final`, read at least one actual rendered `Details` step-row text from the `.ai-agent-turn-activity` block; assert neither regex matches either string. Test `negativeControl_routingAToolNameTrips`: build a string from `"find_records"` and one from a host metaclass name obtained via the autowired `Metadata` and assert `toolNameRegex().matcher("...find_records...").find()` is true (and `hostPrefixRegex().matcher("...<metaclass>...").find()` is true) — proving the regexes are live. Add a comment in the test referencing Phase 9 (`HOST_PREFIX_LEAK` / `TOOL_NAME_LEAK` pattern packs, reused verbatim per D-09) and 15-REVIEWS point #9 (why a rendered-component assertion was added on top of the mapper scan).
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.ObservabilityLeakTest"</automated>
  </verify>
  <acceptance_criteria>
    - `ObservabilityLeakTest.java` exists, references `com.vn.agent.guard.ToolNamePatternProvider` and `com.vn.agent.guard.HostPrefixPatternProvider` (reused, not forked), and contains: a mapper-output status scan over all `ActivityKind` values + the neutral key; a mapper-output disclosure scan over {TOOL, RETRIEVAL} step rows incl. an ERROR/rollback outcome + a null-latency row + the summary text; a RENDERED-COMPONENT scan that drives a stub stream through a real `ChatPanelFragment` and scans the actual `<span.ai-agent-status>` text and an actual `Details` step-row text from the `.ai-agent-turn-activity` block; and a negative-control case asserting the regexes DO match a deliberately-routed `find_records` / host-metaclass name.
    - No new file or change under `ai-agent/ai-agent/src/main/java/com/vn/agent/guard/` (the pattern packs are reused as-is).
    - `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.ObservabilityLeakTest"` exits 0.
  </acceptance_criteria>
  <done>TEST-19 is implemented as a genuine UI-layer leak test: the Phase 9 pattern packs are run against the TurnDetailRenderer mapper output (all ActivityKind values + an errored step + a null-latency row + the summary) AND against the actual rendered status span + Details step-row of a stub-driven ChatPanelFragment, with a passing negative control; green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Locale-completeness + no-Phase-15-changelog + state-holders-unchanged tests</name>
  <files>ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/ObservabilityMessagesCompletenessTest.java, ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/NoNewPersistedStateTest.java, ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/AiChatSessionStateTest.java</files>
  <read_first>
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties + messages_vi.properties (the bundles — confirm the exact new Phase-15 key names against the 15-01 / 15-03 / 15-04 SUMMARYs)
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-01-SUMMARY.md, 15-03-SUMMARY.md, 15-04-SUMMARY.md (the authoritative list of new msg:// keys + the no-DDL confirmations + where the sidebar open-state lives)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/AiChatSessionState.java (confirm it's still `currentConversationId` (volatile UUID) + `listenerRegistrations` (CopyOnWriteArrayList))
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/ChatSurfaceMounter.java (the `MountedChatSurfaceState` private static final class — confirm it gained only fixed sidebar fields) AND ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/AiChatUIState.java (if Plan 03 put the sidebar state there instead)
    - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/AiChatSessionStateTest.java (the test being extended)
    - ai-agent/ai-agent/src/main/resources/ — locate `agentstore-changelog.xml` / `changelog.xml` and the `liquibase/changelog/` directory (to assert no Phase-15 changelog file / include)
    - any existing locale-completeness test in the codebase (e.g. a `MessagesCompletenessTest` / `I18n*Test`) to model `ObservabilityMessagesCompletenessTest` on; if none exists, write a minimal one that loads both `.properties` files via `java.util.Properties` and asserts the key set
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-RESEARCH.md (Validation Architecture → the OBS-04 / OBS-01-locale rows; Wave 0 Gaps)
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-REVIEWS.md (point #10 — no hardcoded global entity count)
  </read_first>
  <behavior>
    - `ObservabilityMessagesCompletenessTest`: for the explicit list of new Phase-15 `msg://` keys (from the SUMMARYs), every key is present and non-blank in `messages_en.properties` AND in `messages_vi.properties` (load both with `java.util.Properties`); a key present in one bundle but missing in the other fails the test.
    - `NoNewPersistedStateTest`: (i) no file under `**/liquibase/changelog/**` has a Phase-15-related name (no `15-…` / `phase-15` / `sidebar` / `observability` changelog file added by this phase); (ii) `agentstore-changelog.xml` and the root `changelog.xml` contain no `<include file=...>` referencing a Phase-15 changelog (read both as resources and scan their `<include>` entries). Optional belt-and-braces: a classpath scan asserting no class under `com.vn.agent.entity` has a `@Table(name=...)` containing "TURN"/"ACTIVITY"/"DISCLOSURE" — but (i)+(ii) are the load-bearing assertions. NO hardcoded global `@Entity` count.
    - `AiChatSessionStateTest` (extended): `AiChatSessionState`'s declared non-static fields are exactly {`currentConversationId`, the listeners collection} (reflective `getDeclaredFields()` filtered to non-static, compared to an expected name set) — no new field, no per-turn-growing collection; AND the sidebar open-state holder (`MountedChatSurfaceState` inside `ChatSurfaceMounter`, or `AiChatUIState` — whichever the Plan 03 SUMMARY records) declares only fixed sidebar fields and no `List`/`Collection`/`Map`-typed field that could grow per chat turn (reflective field-type check).
  </behavior>
  <action>
    Create `view/chat/ObservabilityMessagesCompletenessTest.java`: a plain JUnit test that loads `messages_en.properties` and `messages_vi.properties` via `java.util.Properties` (from the classpath, e.g. `getClass().getResourceAsStream("/com/vn/agent/messages_en.properties")`), defines the list of new Phase-15 keys (copy the final names from the 15-01 / 15-03 / 15-04 SUMMARYs — at minimum: `com.vn.agent.entity/AiChatSurface.SIDEBAR`, `chatSurfaceMounter.sidebarToggle.ariaLabel.open`, `chatSurfaceMounter.sidebarToggle.ariaLabel.closed`, `chatSurfaceMounter.sidebarCloser.ariaLabel`, `chatView.status.chat`, `chatView.status.tool`, `chatView.status.retrieval`, `chatView.status.neutral`, `chatView.turnDetail.summary`, `chatView.turnDetail.step.tool`, `chatView.turnDetail.step.retrieval`, `chatView.turnDetail.errorIndicator`, plus `chatView.turnDetail.unknownDuration` IF Plan 04 used a key rather than a literal "—"), and asserts each is present + non-blank in both. Create `view/chat/NoNewPersistedStateTest.java`: assert no Phase-15-named file under `**/liquibase/changelog/**` (resolve the changelog dir as a resource / classpath path and check filenames) and that `agentstore-changelog.xml` / `changelog.xml` (read as resources) contain no `<include file=...>` referencing a Phase-15 changelog (regex/parse their `<include>` entries); optionally a classpath scan of `com.vn.agent.entity` asserting no `@Table` name contains "TURN"/"ACTIVITY"/"DISCLOSURE"; add a comment "OBS-04: Phase 15 adds no entity/table/changelog — this asserts the negative without pinning a global entity count (15-REVIEWS point #10)". Extend `AiChatSessionStateTest` with a `sessionStateHasNoPerTurnAccumulation` test asserting `AiChatSessionState`'s declared non-static fields are exactly the expected name set ({`currentConversationId`, `listenerRegistrations`} — confirm the exact field names) and that none is a growing per-turn collection; add a second assertion (or a second test) that the sidebar open-state holder (look up `ChatSurfaceMounter.MountedChatSurfaceState` reflectively, or `AiChatUIState` — whichever the Plan 03 SUMMARY says) declares only the fixed sidebar fields and no `List`/`Collection`/`Map` field that grows per chat turn. Keep all three tests minimal and dependency-light.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.ObservabilityMessagesCompletenessTest" --tests "com.vn.agent.view.chat.NoNewPersistedStateTest" --tests "com.vn.agent.view.chat.AiChatSessionStateTest"</automated>
  </verify>
  <acceptance_criteria>
    - `ObservabilityMessagesCompletenessTest.java` asserts every new Phase-15 `msg://` key resolves non-blank in `messages_en.properties` AND `messages_vi.properties`.
    - `NoNewPersistedStateTest.java` asserts no Phase-15-named Liquibase changelog file and no Phase-15 `<include>` in `agentstore-changelog.xml` / `changelog.xml` (OBS-04) — NOT a hardcoded global `@Entity` count.
    - `AiChatSessionStateTest` has a test asserting `AiChatSessionState`'s fields are exactly `{currentConversationId, listenerRegistrations}` (no new field, no per-turn collection) AND that the sidebar open-state holder gained only fixed sidebar fields (no per-turn-growing collection).
    - `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.ObservabilityMessagesCompletenessTest" --tests "com.vn.agent.view.chat.NoNewPersistedStateTest" --tests "com.vn.agent.view.chat.AiChatSessionStateTest"` exits 0.
  </acceptance_criteria>
  <done>Locale completeness, no-Phase-15-changelog, and bounded-session/sidebar-state are all asserted by automated tests (without a brittle global entity count); green.</done>
</task>

<task type="auto">
  <name>Task 3: Move the folded todo to done/ and update the todo INDEX files</name>
  <files>.planning/todos/pending/2026-04-26-add-collapsible-tool-detail-and-ephemeral-status-to-chat-ui.md, .planning/todos/done/2026-04-26-add-collapsible-tool-detail-and-ephemeral-status-to-chat-ui.md, .planning/todos/pending/_INDEX.md, .planning/todos/done/_INDEX.md</files>
  <read_first>
    - .planning/todos/pending/2026-04-26-add-collapsible-tool-detail-and-ephemeral-status-to-chat-ui.md (the todo being moved — read it to write the done/ entry summary)
    - .planning/todos/pending/_INDEX.md (the pending index — remove the entry)
    - .planning/todos/done/_INDEX.md (the done index — add the entry, mirroring the format of the existing done entries like `2026-04-26-hide-internal-tool-and-entity-names-from-user-facing-chat.md`)
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-CONTEXT.md ("Folded Todos" section — the directive)
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-SPEC.md ("In scope" — "Moving the … todo to done/ when the phase ships")
  </read_first>
  <action>
    Move `.planning/todos/pending/2026-04-26-add-collapsible-tool-detail-and-ephemeral-status-to-chat-ui.md` to `.planning/todos/done/2026-04-26-add-collapsible-tool-detail-and-ephemeral-status-to-chat-ui.md` (use the Write tool to create the file in `done/` with the original content plus a short trailing note: "Resolved by Phase 15 — Right-Sidebar Chat Surface & Observability UX (OBS-01 + OBS-02, decisions D-05..D-08, refined per 15-REVIEWS); shipped <date>." — then delete the `pending/` copy via `rm`). In `.planning/todos/pending/_INDEX.md`: remove the line referencing this todo. In `.planning/todos/done/_INDEX.md`: add a line for this todo mirroring the format of the existing done entries (date-prefixed filename + a one-line description noting it was resolved by Phase 15). Do not touch any other todo file.
  </action>
  <verify>
    <automated>test -f .planning/todos/done/2026-04-26-add-collapsible-tool-detail-and-ephemeral-status-to-chat-ui.md && ! test -f .planning/todos/pending/2026-04-26-add-collapsible-tool-detail-and-ephemeral-status-to-chat-ui.md && echo MOVED</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/todos/done/2026-04-26-add-collapsible-tool-detail-and-ephemeral-status-to-chat-ui.md` exists and carries the resolution note; `.planning/todos/pending/2026-04-26-add-collapsible-tool-detail-and-ephemeral-status-to-chat-ui.md` no longer exists.
    - `.planning/todos/pending/_INDEX.md` no longer references the todo; `.planning/todos/done/_INDEX.md` references it (with a Phase-15 resolution note), formatted like the other done entries.
    - The verify command prints `MOVED`.
  </acceptance_criteria>
  <done>The 2026-04-26 collapsible-tool-detail / ephemeral-status todo is in done/ with a Phase-15 resolution note; both INDEX files updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (test-only) the rendered status line / disclosure text ↔ the Phase 9 leak regexes | The test asserts the rendering path's output never matches the internal-name patterns — this IS the threat mitigation, exercised against both the mapper output AND a real rendered component |
| planning bookkeeping (todo files) | No runtime trust boundary — file moves under `.planning/` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-E1 | Information disclosure | internal `@Tool` method name / raw host-entity name leaking into the streaming-status line or per-turn disclosure | mitigate | `ObservabilityLeakTest` (TEST-19) reuses the Phase 9 `TOOL_NAME_LEAK` / `HOST_PREFIX_LEAK` regexes against (a) the `TurnDetailRenderer` mapper output across all `ActivityKind` values + an errored step + a null-latency row + the summary AND (b) the ACTUAL rendered `<span.ai-agent-status>` text + a rendered `Details` step-row text of a stub-driven `ChatPanelFragment`, with a passing negative control proving the test catches a regression. Combined with the structural no-leak design from Plan 02 (closed `ActivityKind`) + Plan 04 (`TurnDetailRenderer` closed input alphabet + the audit reads' name-column-free fetch plan), the leak surface is closed and verified at the UI layer. |
| T-15-E2 | Tampering | new persisted state / parallel store sneaking in under the observability features | mitigate | `NoNewPersistedStateTest` asserts no Phase-15 Liquibase changelog/include; `AiChatSessionStateTest` asserts `AiChatSessionState` is unchanged and the sidebar open-state holder gained no per-turn collection. Locks OBS-04 without pinning a brittle global entity count. |
| T-15-E3 | Information disclosure | a new user-visible label shipping in only one locale (untranslated string leaking through) | mitigate | `ObservabilityMessagesCompletenessTest` asserts every new Phase-15 `msg://` key resolves non-blank in `messages_en.properties` AND `messages_vi.properties`. |
| T-15-E4 | (none) | the todo-file move | accept | Pure `.planning/` bookkeeping; no runtime impact, no threat. |
</threat_model>

<verification>
- `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.ObservabilityLeakTest" --tests "com.vn.agent.view.chat.ObservabilityMessagesCompletenessTest" --tests "com.vn.agent.view.chat.NoNewPersistedStateTest" --tests "com.vn.agent.view.chat.AiChatSessionStateTest"` green.
- `./gradlew test` (whole composite) green — the phase gate before `/gsd-verify-work`.
- No change under `ai-agent/ai-agent/src/main/java/com/vn/agent/guard/`; no new file under `**/liquibase/**`.
- `.planning/todos/done/2026-04-26-add-collapsible-tool-detail-and-ephemeral-status-to-chat-ui.md` exists; the `pending/` copy is gone; both INDEX files updated.
</verification>

<success_criteria>
- TEST-19 met: a genuine UI-layer leak test reusing the Phase 9 pattern packs proves the status line + disclosure never emit internal `@Tool`/entity names — scanning both the `TurnDetailRenderer` mapper output (all `ActivityKind` values + an errored step + a null-latency row + the summary) AND a real rendered `ChatPanelFragment`'s status span + `Details` step-row — with a passing negative control.
- OBS-04 locked by `NoNewPersistedStateTest` (no Phase-15 changelog/include) + the extended `AiChatSessionStateTest` (state holders unchanged, no per-turn collection); OBS-01/OBS-02 locale coverage locked by `ObservabilityMessagesCompletenessTest`.
- The 2026-04-26 todo is moved to `done/` with a Phase-15 resolution note; INDEX files updated.
- Whole composite test suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-05-SUMMARY.md` recording: the final list of Phase-15 `msg://` keys asserted by `ObservabilityMessagesCompletenessTest`, how `NoNewPersistedStateTest` asserts the negative (changelog/include scan + state-holder reflection, no global entity count — review point #10), how the rendered-component leak assertion was wired (review point #9), and confirmation the todo move + INDEX updates are done.
</output>
