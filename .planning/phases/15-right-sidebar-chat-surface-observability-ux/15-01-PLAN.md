---
phase: 15-right-sidebar-chat-surface-observability-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiChatSurface.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiUiSettings.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/AiUiSettingsService.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/uisettings/AiUiSettingsDetailView.java
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties
  - ai-agent/ai-agent/src/test/java/com/vn/agent/entity/AiUiSettingsModelTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/view/uisettings/AiUiSettingsDetailViewTest.java
autonomous: true
requirements: [SURF-11, OBS-04]

must_haves:
  truths:
    - "AiChatSurface has a SIDEBAR value usable wherever FULL_ROUTE/HEADER_BUTTON are used"
    - "SIDEBAR is selectable in the admin UI-settings enabled-surface control and the defaultSurface selector with a localized label"
    - "A fresh-install default AiUiSettings still resolves a valid enabled-surface set including SIDEBAR (or an intentionally chosen subset)"
    - "No new @Entity / @Table / Liquibase changelog is added — enabledSurfaceIds/defaultSurface are existing varchar columns"
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiChatSurface.java"
      provides: "SIDEBAR enum constant on AiChatSurface (EnumClass<String>)"
      contains: "SIDEBAR"
    - path: "ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties"
      provides: "AiChatSurface.SIDEBAR label key (en)"
      contains: "AiChatSurface.SIDEBAR"
    - path: "ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties"
      provides: "AiChatSurface.SIDEBAR label key (vi)"
      contains: "AiChatSurface.SIDEBAR"
  key_links:
    - from: "AiUiSettingsDetailView enabled-surface control"
      to: "AiChatSurface.SIDEBAR"
      via: "setItems(AiChatSurface.class) + setItemLabelGenerator(messages::getMessage)"
      pattern: "AiChatSurface"
    - from: "AiUiSettingsService.createDefaultSettings()"
      to: "AiChatSurface.SIDEBAR"
      via: "EnumSet.allOf(AiChatSurface.class) or an intentional subset"
      pattern: "AiChatSurface"
---

<objective>
Add the `SIDEBAR` value to `AiChatSurface` and make it a first-class surface in the persistence + admin layer: selectable in the admin UI-settings enabled-surface control and the `defaultSurface` selector, with a localized label in both locale bundles. Decide and document whether `SIDEBAR` ships enabled-by-default in the seed (RESEARCH Open Question 2). No schema change — `AI_UI_SETTINGS.ENABLED_SURFACE_IDS` / `DEFAULT_SURFACE` are varchar columns holding comma-joined ids (OBS-04 satisfied by construction).

Purpose: This is the foundation other Phase 15 plans build on — `ChatSurfaceMounter` (Plan 03) gates the side-panel mount on `getEnabledSurfaceSet().contains(AiChatSurface.SIDEBAR)`, so the enum value must exist first.

Output: `AiChatSurface.SIDEBAR` constant; new `msg://` label key in `messages_en.properties` + `messages_vi.properties`; whatever (likely-minimal) changes `AiUiSettings`/`AiUiSettingsService`/`AiUiSettingsDetailView` need so the admin control auto-lists the new value and the seed default is correct; extended `AiUiSettingsModelTest` + `AiUiSettingsDetailViewTest`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-SPEC.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-CONTEXT.md
@.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-PATTERNS.md

<interfaces>
<!-- AiChatSurface is an EnumClass<String>. Pattern to mirror (whole file is the template — add ONE constant): -->
<!-- public enum AiChatSurface implements EnumClass<String> { FULL_ROUTE("FULL_ROUTE"), HEADER_BUTTON("HEADER_BUTTON"), SIDEBAR("SIDEBAR"); ... getId() / fromId() unchanged } -->
<!-- AiUiSettings: @Column(name="ENABLED_SURFACE_IDS") String + @Column(name="DEFAULT_SURFACE", length=64) String + getEnabledSurfaceSet() (parses comma-joined ids back to Set<AiChatSurface>). No DDL needed. -->
<!-- AiUiSettingsService.createDefaultSettings() currently uses EnumSet.allOf(AiChatSurface.class) for enabledSurfaceIds and FULL_ROUTE as defaultSurface — adding the enum value auto-includes SIDEBAR. -->
<!-- AiUiSettingsDetailView: enabledSurfacesField.setItems(AiChatSurface.class) + setItemLabelGenerator(messages::getMessage); defaultSurfaceField similar — auto-lists a new enum value, only the label msg:// key is new. Inject io.jmix.core.Messages, class-less messages.getMessage(key) form. -->
<!-- Existing locale precedent (messages_en.properties): com.vn.agent.entity/AiChatSurface.FULL_ROUTE=Full route ; com.vn.agent.entity/AiChatSurface.HEADER_BUTTON=Header button -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add AiChatSurface.SIDEBAR and its locale labels</name>
  <files>ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiChatSurface.java, ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties, ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties, ai-agent/ai-agent/src/test/java/com/vn/agent/entity/AiUiSettingsModelTest.java</files>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiChatSurface.java (the file being modified — mirror the FULL_ROUTE/HEADER_BUTTON constant exactly)
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties (find the `com.vn.agent.entity/AiChatSurface.FULL_ROUTE` line — add the SIDEBAR sibling near it)
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties (same — add the SIDEBAR sibling)
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-PATTERNS.md (section "entity/AiChatSurface.java" — the in-file analog)
    - ai-agent/ai-agent/src/test/java/com/vn/agent/entity/AiUiSettingsModelTest.java (the test being extended)
  </read_first>
  <behavior>
    - AiChatSurface.SIDEBAR exists, getId() returns "SIDEBAR", AiChatSurface.fromId("SIDEBAR") (or the existing reverse-lookup mechanism) returns AiChatSurface.SIDEBAR.
    - AiChatSurface.values() has length 3 and contains SIDEBAR.
    - A round-trip of an AiUiSettings with enabledSurfaceIds containing "SIDEBAR" parses back to a Set including AiChatSurface.SIDEBAR via getEnabledSurfaceSet().
  </behavior>
  <action>
    Add `SIDEBAR("SIDEBAR")` as the third constant in `AiChatSurface` (after `HEADER_BUTTON`), mirroring the existing constant declaration form exactly — keep `getId()` / the reverse-lookup (`fromId`) and any `@SuppressWarnings`/serialization bits unchanged. Add the label key `com.vn.agent.entity/AiChatSurface.SIDEBAR=Right sidebar` to `messages_en.properties` next to `com.vn.agent.entity/AiChatSurface.FULL_ROUTE` / `.HEADER_BUTTON`, and `com.vn.agent.entity/AiChatSurface.SIDEBAR=Thanh bên phải` to `messages_vi.properties` next to its siblings (use a sensible Vietnamese label consistent with the existing vi entries). Extend `AiUiSettingsModelTest` with assertions: `AiChatSurface.values()` has length 3 and contains `SIDEBAR`; `getId()` round-trips for `SIDEBAR`; an `AiUiSettings` whose `enabledSurfaceIds` includes `"SIDEBAR"` exposes `SIDEBAR` via `getEnabledSurfaceSet()`.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.entity.AiUiSettingsModelTest"</automated>
  </verify>
  <acceptance_criteria>
    - AiChatSurface.java contains `SIDEBAR("SIDEBAR")`.
    - messages_en.properties contains a `com.vn.agent.entity/AiChatSurface.SIDEBAR=` line; messages_vi.properties contains a `com.vn.agent.entity/AiChatSurface.SIDEBAR=` line.
    - `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.entity.AiUiSettingsModelTest"` exits 0.
    - No new file under any `liquibase/changelog/` directory and no edit to `agentstore-changelog.xml`/`changelog.xml`.
  </acceptance_criteria>
  <done>AiChatSurface has SIDEBAR; both locale bundles carry its label; AiUiSettingsModelTest green; zero DDL.</done>
</task>

<task type="auto">
  <name>Task 2: Make SIDEBAR selectable in the admin UI-settings view and confirm the seed default</name>
  <files>ai-agent/ai-agent/src/main/java/com/vn/agent/view/uisettings/AiUiSettingsDetailView.java, ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/AiUiSettingsService.java, ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiUiSettings.java, ai-agent/ai-agent/src/test/java/com/vn/agent/view/uisettings/AiUiSettingsDetailViewTest.java</files>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/uisettings/AiUiSettingsDetailView.java (the enabled-surface control + defaultSurface selector — confirm they use `setItems(AiChatSurface.class)` so a new enum is auto-listed; if they enumerate explicitly, add SIDEBAR)
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/view/uisettings/ai-ui-settings-detail-view.xml (if it exists — the XML descriptor for the detail view; check whether surface fields are declared declaratively)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/AiUiSettingsService.java (the `createDefaultSettings()` seed — confirm `EnumSet.allOf(AiChatSurface.class)` so SIDEBAR is auto-enabled on fresh installs; RESEARCH Open Q2)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiUiSettings.java (the persisted varchar columns + getEnabledSurfaceSet)
    - ai-agent/ai-agent/src/test/java/com/vn/agent/view/uisettings/AiUiSettingsDetailViewTest.java (the test being extended)
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-PATTERNS.md (sections "view/uisettings/AiUiSettingsDetailView.java" + "view/chat/AiUiSettingsService.java")
    - .planning/phases/15-right-sidebar-chat-surface-observability-ux/15-RESEARCH.md (Open Question 2 + Assumption A8 — the seed decision)
  </read_first>
  <action>
    Verify the enabled-surface control and the `defaultSurface` selector in `AiUiSettingsDetailView` (Java or XML) auto-include a new `AiChatSurface` value. If they use `setItems(AiChatSurface.class)` + `setItemLabelGenerator(messages::getMessage)` (or the XML `<radioButtonGroup>`/`<checkboxGroup>` with `itemsEnum="com.vn.agent.entity.AiChatSurface"`), NO Java/XML change is needed beyond the Task-1 label key — confirm and note that in the SUMMARY. If anything enumerates `FULL_ROUTE`/`HEADER_BUTTON` explicitly, add `SIDEBAR`. Then resolve RESEARCH Open Q2: keep `AiUiSettingsService.createDefaultSettings()` as `EnumSet.allOf(AiChatSurface.class)` so a fresh install ships all three surfaces enabled with `FULL_ROUTE` as default — record the rationale in the SUMMARY ("the SIDEBAR panel starts closed per D-04, so enabled-by-default only means the navbar toggle is present — parity with HEADER_BUTTON shipping a navbar button by default"). Do NOT change `defaultSurface` away from `FULL_ROUTE`. Extend `AiUiSettingsDetailViewTest` with: opening the view shows `SIDEBAR` among the enabled-surface options and among the `defaultSurface` options (assert the control's items contain `AiChatSurface.SIDEBAR`); a freshly created default `AiUiSettings` (via `AiUiSettingsService.createDefaultSettings()` or the view's default-load path) has `getEnabledSurfaceSet()` containing `SIDEBAR`. If `AiUiSettings.java` / `AiUiSettingsService.java` genuinely need no change, leave them untouched (do not add empty diffs) — but they remain in `files_modified` so the executor knows to check them.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.uisettings.AiUiSettingsDetailViewTest"</automated>
  </verify>
  <acceptance_criteria>
    - The admin enabled-surface control's item set includes `AiChatSurface.SIDEBAR` (asserted in `AiUiSettingsDetailViewTest`).
    - The `defaultSurface` selector's item set includes `AiChatSurface.SIDEBAR`.
    - A default `AiUiSettings` exposes `SIDEBAR` via `getEnabledSurfaceSet()` (or the SUMMARY documents an intentional decision to exclude it from the seed — but the recommended choice is to keep it).
    - `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.uisettings.AiUiSettingsDetailViewTest"` exits 0.
    - The SUMMARY states which of AiUiSettings.java / AiUiSettingsService.java / AiUiSettingsDetailView.java actually changed (possibly none beyond the Task-1 label) and the seed-default rationale.
  </acceptance_criteria>
  <done>SIDEBAR is selectable in both admin controls; the seed-default decision is made and recorded; AiUiSettingsDetailViewTest green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin user → AiUiSettings persistence | The admin picks enabled surfaces / default surface; values are constrained to the `AiChatSurface` enum set — no free text reaches a sensitive path |
| persisted enabledSurfaceIds (varchar) → AiChatSurface set | `getEnabledSurfaceSet()` parses comma-joined ids back to the closed enum; an unknown id is dropped/ignored (existing behavior) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-A1 | Information disclosure | `AiChatSurface.SIDEBAR` enum constant rendered in the admin control | accept | The constant id "SIDEBAR" is not sensitive; its rendered label comes from a `msg://` key, not a tool/entity name. No leak surface here. |
| T-15-A2 | Tampering | persisted `enabledSurfaceIds`/`defaultSurface` varchar columns | accept | Already constrained to the enum set by `getEnabledSurfaceSet()`; admin-only write path via the existing `AccessManager`-gated detail view; no new column, no new write path. |
| T-15-A3 | Elevation of privilege | who can edit `AiUiSettings` | mitigate | Unchanged — the existing admin role / `AiUiSettingsDetailView` security policy still gates edits; this plan adds no new editable field and no new view. Verified by the unchanged view-permission test path. |
</threat_model>

<verification>
- `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.entity.AiUiSettingsModelTest" --tests "com.vn.agent.view.uisettings.AiUiSettingsDetailViewTest"` green.
- `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.*"` green (no regression in surface-related tests from the new enum value).
- No new file under `**/liquibase/changelog/**`; `git diff --stat` shows no change to `agentstore-changelog.xml` / `changelog.xml`.
- Both `messages_en.properties` and `messages_vi.properties` contain `com.vn.agent.entity/AiChatSurface.SIDEBAR=`.
</verification>

<success_criteria>
- `AiChatSurface` has a working `SIDEBAR` value (`getId()`/reverse-lookup round-trip).
- `SIDEBAR` is selectable in the admin enabled-surface control and the `defaultSurface` selector with a localized label in en + vi.
- The seed-default behavior for `SIDEBAR` is decided and recorded; `defaultSurface` stays `FULL_ROUTE`.
- No new `@Entity`/`@Table`/Liquibase changelog (OBS-04).
- All touched tests green; no regression in `com.vn.agent.view.chat.*`.
</success_criteria>

<output>
After completion, create `.planning/phases/15-right-sidebar-chat-surface-observability-ux/15-01-SUMMARY.md` recording: which of the AiUiSettings* files actually changed (vs. auto-listed by the enum), the seed-default decision + rationale, and the new `msg://` key name.
</output>
