---
phase: 02-foundations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-agent/ai-agent/src/main/java/com/vn/agent/spi/ToolContributor.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/spi/ContextContributor.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/spi/PromptContextContributor.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/spi/ToolGuard.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/spi/ToolVetoedException.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/spi/AuditListener.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/spi/CustomIngester.java
autonomous: true
requirements: [SPI-01, SPI-02, SPI-03, SPI-05, SPI-06, SPI-07]
must_haves:
  truths:
    - "Six SPI interfaces exist in `com.vn.agent.spi` package with verbatim RESEARCH.md signatures"
    - "Each interface compiles independently (no impl, no cross-SPI deps)"
    - "ToolVetoedException is a RuntimeException subclass"
    - "Every interface carries Javadoc including at least one integration example or contract clause"
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/spi/ToolContributor.java"
      contains: "List<Object> contribute"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/spi/ContextContributor.java"
      contains: "void contribute(java.util.Map<String, Object> bag)"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/spi/PromptContextContributor.java"
      contains: "String fragment()"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/spi/ToolGuard.java"
      contains: "throws ToolVetoedException"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/spi/ToolVetoedException.java"
      contains: "extends RuntimeException"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/spi/AuditListener.java"
      contains: "void onToolCallAudited(java.util.UUID auditId)"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/spi/CustomIngester.java"
      contains: "List<org.springframework.ai.document.Document> read"
  key_links:
    - from: "ToolGuard.check(...)"
      to: "ToolVetoedException"
      via: "throws clause"
      pattern: "throws ToolVetoedException"
---

<objective>
Create the 6 SPI interfaces + `ToolVetoedException` in `com.vn.agent.spi`. Phase 2 ships signatures + Javadoc only; no-op defaults come in plan 07; implementations come in Phase 3+.

Purpose: Downstream phases (3-6) extend the add-on via these interfaces. Locking the surface now gives host developers a stable contract and prevents API churn during impl waves.
Output: 7 Java files in `ai-agent/ai-agent/src/main/java/com/vn/agent/spi/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-foundations/02-CONTEXT.md
@.planning/phases/02-foundations/02-RESEARCH.md
@CLAUDE.md

<interfaces>
Authoritative signatures — copy verbatim from RESEARCH.md §"SPI Signatures" (lines 653-742):

```java
package com.vn.agent.spi;

// ToolContributor — SPI-01
public interface ToolContributor {
    java.util.List<Object> contribute();
}

// ContextContributor — SPI-02
public interface ContextContributor {
    void contribute(java.util.Map<String, Object> bag);
}

// PromptContextContributor — SPI-03
public interface PromptContextContributor {
    String fragment();
    default int getOrder() { return 0; }
}

// ToolGuard — SPI-05
public interface ToolGuard {
    void check(String toolName, java.util.Map<String, Object> arguments) throws ToolVetoedException;
}

// ToolVetoedException — supporting class
public class ToolVetoedException extends RuntimeException {
    public ToolVetoedException(String message) { super(message); }
    public ToolVetoedException(String message, Throwable cause) { super(message, cause); }
}

// AuditListener — SPI-06
public interface AuditListener {
    void onToolCallAudited(java.util.UUID auditId);
}

// CustomIngester — SPI-07
public interface CustomIngester {
    String getId();
    String getDisplayName();
    java.util.List<org.springframework.ai.document.Document> read();
}
```

Note: `org.springframework.ai.document.Document` comes from Spring AI 1.1.4 (already on classpath via BOM from Phase 1).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 4 simple SPI interfaces + ToolVetoedException</name>
  <read_first>
    - .planning/phases/02-foundations/02-RESEARCH.md §"SPI Signatures" (lines 653-742)
    - .planning/phases/02-foundations/02-PATTERNS.md §"SPIs" (lines 427-434) and §"No Analog Found" (lines 617-627)
    - .planning/phases/02-foundations/02-CONTEXT.md D-04 (flat package, interfaces + Javadoc only)
    - ai-agent/ai-agent/build.gradle (confirm Spring AI 1.1.4 Document type is on compileClasspath via BOM)
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/spi/ToolContributor.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/spi/ContextContributor.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/spi/PromptContextContributor.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/spi/AuditListener.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/spi/ToolVetoedException.java
  </files>
  <action>
Create 5 files in `ai-agent/ai-agent/src/main/java/com/vn/agent/spi/`. All in `package com.vn.agent.spi;`.

**ToolContributor.java** (SPI-01):
```java
package com.vn.agent.spi;

/**
 * Host extension point for contributing additional {@code @Tool}-annotated beans to the agent.
 * <p>Spring AI 2.x tool-callback resolution consumes every ToolContributor bean in the
 * application context; returned beans' {@code @Tool} methods are exposed via
 * {@code ToolCallbacks.from(bean)}.</p>
 *
 * <p><b>Example:</b>
 * <pre>{@code
 * @Component
 * class CrmTools implements ToolContributor {
 *     @Override public List<Object> contribute() { return List.of(this); }
 *
 *     @Tool(description = "Look up a CRM contact by email")
 *     public Contact lookup(@ToolParam String email) { ... }
 * }
 * }</pre>
 */
public interface ToolContributor {
    /** @return beans whose {@code @Tool} methods should be exposed; empty list = no contribution. */
    java.util.List<Object> contribute();
}
```

**ContextContributor.java** (SPI-02):
```java
package com.vn.agent.spi;

/**
 * Host extension point for injecting per-request context (user, tenant, env, correlation id)
 * into the tool-execution ToolContext. Fires once per {@code ChatService.ask}/{@code stream} call.
 */
public interface ContextContributor {
    /**
     * @param bag mutable key-to-value map attached to ToolContext; keys MUST be namespaced,
     *            e.g. {@code "host.tenantId"}, never a bare key like {@code "tenant"}.
     */
    void contribute(java.util.Map<String, Object> bag);
}
```

**PromptContextContributor.java** (SPI-03):
```java
package com.vn.agent.spi;

/**
 * Host extension point for appending host-specific text into the system prompt at request time.
 * Fragments are concatenated in ascending {@link #getOrder()} value after the profile's base
 * system prompt.
 */
public interface PromptContextContributor {
    /** @return text to append; empty string = no contribution. Avoid trailing newlines. */
    String fragment();

    /** Lower values placed earlier in the concatenated prompt. Default 0. */
    default int getOrder() { return 0; }
}
```

**AuditListener.java** (SPI-06):
```java
package com.vn.agent.spi;

/**
 * Host extension point fired after every {@code AiToolCallAudit} write for side-channel
 * observability (Slack, SIEM, metrics). Listeners run fire-and-forget in Phase 4 wiring;
 * exceptions thrown here MUST NOT fail the primary request.
 */
public interface AuditListener {
    /** @param auditId the just-persisted {@code AiToolCallAudit.id} */
    void onToolCallAudited(java.util.UUID auditId);
}
```

**ToolVetoedException.java** (supporting class — unchecked per RESEARCH recommendation):
```java
package com.vn.agent.spi;

/**
 * Thrown by {@link ToolGuard#check(String, java.util.Map)} to veto a tool invocation.
 * Unchecked so tool bodies are not forced to declare {@code throws}.
 */
public class ToolVetoedException extends RuntimeException {
    public ToolVetoedException(String message) { super(message); }
    public ToolVetoedException(String message, Throwable cause) { super(message, cause); }
}
```

After writing, if JetBrains MCP is available run `get_file_problems(path, onlyErrors=false)` on each file.
  </action>
  <verify>
    <automated>
grep -q "package com.vn.agent.spi;" ai-agent/ai-agent/src/main/java/com/vn/agent/spi/ToolContributor.java && \
grep -q "java.util.List<Object> contribute" ai-agent/ai-agent/src/main/java/com/vn/agent/spi/ToolContributor.java && \
grep -q "void contribute(java.util.Map<String, Object> bag)" ai-agent/ai-agent/src/main/java/com/vn/agent/spi/ContextContributor.java && \
grep -q "String fragment()" ai-agent/ai-agent/src/main/java/com/vn/agent/spi/PromptContextContributor.java && \
grep -q "default int getOrder()" ai-agent/ai-agent/src/main/java/com/vn/agent/spi/PromptContextContributor.java && \
grep -q "void onToolCallAudited(java.util.UUID auditId)" ai-agent/ai-agent/src/main/java/com/vn/agent/spi/AuditListener.java && \
grep -q "class ToolVetoedException extends RuntimeException" ai-agent/ai-agent/src/main/java/com/vn/agent/spi/ToolVetoedException.java
    </automated>
  </verify>
  <acceptance_criteria>
    - All 5 files in `com.vn.agent.spi` package
    - Each interface has Javadoc with at least one sentence describing its role
    - `ToolVetoedException extends RuntimeException` with two constructors
    - No additional methods beyond those specified
    - Files compile (verified by next task's `./gradlew compileJava`)
  </acceptance_criteria>
  <done>Five files exist with exact signatures and Javadoc.</done>
</task>

<task type="auto">
  <name>Task 2: Create ToolGuard + CustomIngester and compile-verify the spi package</name>
  <read_first>
    - .planning/phases/02-foundations/02-RESEARCH.md §"SPI Signatures" (lines 702-739)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/spi/ToolVetoedException.java (just created)
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/spi/ToolGuard.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/spi/CustomIngester.java
  </files>
  <action>
**ToolGuard.java** (SPI-05):
```java
package com.vn.agent.spi;

/**
 * Host extension point that can veto a tool invocation before it runs.
 * <p>Multiple guards compose by short-circuit AND: any guard throwing {@link ToolVetoedException}
 * blocks the call and produces an {@code AiToolCallAudit} row with
 * {@code outcome = BLOCKED} and the thrown message captured as {@code denialReason}.</p>
 */
public interface ToolGuard {
    /**
     * @param toolName  the {@code @Tool} name being invoked
     * @param arguments the resolved tool arguments as a map
     * @throws ToolVetoedException when the invocation must be blocked
     */
    void check(String toolName, java.util.Map<String, Object> arguments) throws ToolVetoedException;
}
```

**CustomIngester.java** (SPI-07):
```java
package com.vn.agent.spi;

/**
 * Host extension point to plug in custom knowledge-base ingestion sources
 * (S3, Confluence, SharePoint, classpath markdown, etc.).
 * <p>Phase 5 wires {@code IngesterManager} which invokes every bean of this type;
 * Phase 2 ships the interface only.</p>
 */
public interface CustomIngester {
    /** @return stable identifier, surfaced in admin UI. */
    String getId();

    /** @return human-readable label for admin UI. */
    String getDisplayName();

    /** Pull documents from the source; Phase 5 splits + embeds + writes to the vector store. */
    java.util.List<org.springframework.ai.document.Document> read();
}
```

Then run `./gradlew :ai-agent:ai-agent:compileJava` and confirm exit 0 (proves the Spring AI `Document` type resolves).
  </action>
  <verify>
    <automated>
grep -q "throws ToolVetoedException" ai-agent/ai-agent/src/main/java/com/vn/agent/spi/ToolGuard.java && \
grep -q "void check(String toolName" ai-agent/ai-agent/src/main/java/com/vn/agent/spi/ToolGuard.java && \
grep -q "String getId()" ai-agent/ai-agent/src/main/java/com/vn/agent/spi/CustomIngester.java && \
grep -q "String getDisplayName()" ai-agent/ai-agent/src/main/java/com/vn/agent/spi/CustomIngester.java && \
grep -q "org.springframework.ai.document.Document" ai-agent/ai-agent/src/main/java/com/vn/agent/spi/CustomIngester.java && \
./gradlew :ai-agent:ai-agent:compileJava --quiet
    </automated>
  </verify>
  <acceptance_criteria>
    - ToolGuard declares `throws ToolVetoedException` on `check`
    - CustomIngester has three methods: `getId()`, `getDisplayName()`, `read()`
    - `read()` return type references `org.springframework.ai.document.Document`
    - `./gradlew :ai-agent:ai-agent:compileJava` exits 0 — full package compiles including cross-file references
  </acceptance_criteria>
  <done>All 7 files under `com.vn.agent.spi` compile together; ToolGuard references ToolVetoedException correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host code → add-on | Host implementations of these SPIs will execute in-process with full JVM authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SPI-01 | Elevation of Privilege | Host `ToolContributor` registers a tool that writes data, bypassing read-only posture | mitigate (downstream) | Phase 3 TOOL-04 enforces DataManager-only; Javadoc here is informational. Phase 2 cannot block this — SPI by design delegates trust |
| T-02-SPI-02 | Denial of Service | Host `ToolGuard` throws non-vetoed RuntimeException, crashes request | mitigate | Phase 4 wiring will catch+log; Javadoc clarifies only `ToolVetoedException` is expected |
| T-02-SPI-03 | Information Disclosure | `AuditListener` exception propagates and surfaces audit details to caller | mitigate | Javadoc states "exceptions MUST NOT fail primary request"; Phase 4 wraps listener calls in try/catch |
| T-02-SPI-04 | Tampering | `ContextContributor` overwrites core tool-context keys | mitigate | Javadoc mandates namespaced keys (e.g., `host.tenantId`); Phase 4 wiring can reject un-namespaced keys if needed |

No Phase 2 executable code is shipped for these interfaces; threats surface only when Phase 3+ implementations exist. Accepted risk.
</threat_model>

<verification>
- `./gradlew :ai-agent:ai-agent:compileJava` exits 0
- No file imports anything from `com.vn.agent.entity` (SPIs must not depend on entities)
</verification>

<success_criteria>
All 6 SPI interfaces + `ToolVetoedException` compile. Signatures match RESEARCH.md verbatim. Each has Javadoc.
</success_criteria>

<output>
Create `.planning/phases/02-foundations/02-02-SUMMARY.md` after completion.
</output>
