---
phase: 02-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiMessageRole.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiKnowledgeDocumentStatus.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallOutcome.java
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties
autonomous: true
requirements: [ENT-01]
must_haves:
  truths:
    - "Three `EnumClass<String>` enums exist under `com.vn.agent.entity` with stable string IDs"
    - "`AiMessageRole` values exactly match the Spring AI 1.1.4 chat-memory CHECK constraint USER/ASSISTANT/SYSTEM/TOOL"
    - "Every enum value has an EN label AND a VI label in the messages bundles"
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiMessageRole.java"
      contains: "implements EnumClass<String>"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiKnowledgeDocumentStatus.java"
      contains: "implements EnumClass<String>"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallOutcome.java"
      contains: "implements EnumClass<String>"
    - path: "ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties"
      provides: "Vietnamese translations for all enum values"
  key_links:
    - from: "AiMessageRole.USER/ASSISTANT/SYSTEM/TOOL"
      to: "SPRING_AI_CHAT_MEMORY.type CHECK constraint"
      via: "identical string id"
      pattern: "USER|ASSISTANT|SYSTEM|TOOL"
---

<objective>
Create the three enum classes used by Phase 2 entities (AiMessage, AiKnowledgeDocument, AiToolCallAudit) and add i18n keys for every enum value in both EN and VI locales.

Purpose: Without the enums, entity classes in plan 03 cannot compile. i18n must land in lockstep with enums per CLAUDE.md "single-locale messages forbidden" rule.
Output: 3 Java enums + extended messages.properties + new messages_vi.properties.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-foundations/02-CONTEXT.md
@.planning/phases/02-foundations/02-RESEARCH.md
@.planning/phases/02-foundations/02-PATTERNS.md
@CLAUDE.md

<interfaces>
Analog (copy verbatim, substitute values):
File: jmix-app/src/main/java/com/vn/jmixapp/entity/OrderStatus.java
```java
package com.vn.jmixapp.entity;
import io.jmix.core.metamodel.datatype.EnumClass;
import org.springframework.lang.Nullable;

public enum OrderStatus implements EnumClass<String> {
    NEW("NEW"), CONFIRMED("CONFIRMED"), SHIPPED("SHIPPED"), CANCELLED("CANCELLED");
    private final String id;
    OrderStatus(String id) { this.id = id; }
    @Override public String getId() { return id; }
    @Nullable public static OrderStatus fromId(String id) {
        for (OrderStatus at : values()) if (at.getId().equals(id)) return at;
        return null;
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create three enum classes under com.vn.agent.entity</name>
  <read_first>
    - jmix-app/src/main/java/com/vn/jmixapp/entity/OrderStatus.java (exact analog to copy)
    - .planning/phases/02-foundations/02-RESEARCH.md §"Enum Definitions" (lines 611-651)
    - .planning/phases/02-foundations/02-PATTERNS.md lines 197-232 (enum pattern assignment)
    - CLAUDE.md (no Lombok, project naming)
    - .claude/skills/jmix-enums/SKILL.md (invoke via Skill tool if available)
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiMessageRole.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiKnowledgeDocumentStatus.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallOutcome.java
  </files>
  <action>
Create three Java enums, each implementing `io.jmix.core.metamodel.datatype.EnumClass<String>`, mirroring OrderStatus.java exactly (package, imports, fromId signature, @Nullable import, no Lombok).

Package: `com.vn.agent.entity`

**AiMessageRole** — values (id strings MUST match Spring AI 1.1.4 chat-memory type CHECK constraint per D-RESEARCH):
- USER("USER")
- ASSISTANT("ASSISTANT")
- SYSTEM("SYSTEM")
- TOOL("TOOL")

**AiKnowledgeDocumentStatus** — values per RAG-03 lifecycle:
- PENDING("PENDING")
- PROCESSING("PROCESSING")
- READY("READY")
- FAILED("FAILED")

**AiToolCallOutcome** — values per AUD-03:
- SUCCESS("SUCCESS")
- BLOCKED("BLOCKED")
- ERROR("ERROR")

Each file verbatim structure from OrderStatus.java: `private final String id;` + single-arg constructor + `@Override getId()` + `@Nullable static fromId(String id)` using `for (var v : values())` loop. No Lombok annotations. No additional methods.

After writing, if JetBrains MCP is available, run `get_file_problems` on each new file with `onlyErrors=false`.
  </action>
  <verify>
    <automated>
grep -q "public enum AiMessageRole implements EnumClass<String>" ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiMessageRole.java && \
grep -q 'USER("USER")' ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiMessageRole.java && \
grep -q 'ASSISTANT("ASSISTANT")' ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiMessageRole.java && \
grep -q 'SYSTEM("SYSTEM")' ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiMessageRole.java && \
grep -q 'TOOL("TOOL")' ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiMessageRole.java && \
grep -q "public enum AiKnowledgeDocumentStatus implements EnumClass<String>" ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiKnowledgeDocumentStatus.java && \
grep -q 'PENDING("PENDING")' ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiKnowledgeDocumentStatus.java && \
grep -q 'READY("READY")' ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiKnowledgeDocumentStatus.java && \
grep -q "public enum AiToolCallOutcome implements EnumClass<String>" ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallOutcome.java && \
grep -q 'SUCCESS("SUCCESS")' ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallOutcome.java && \
grep -q 'BLOCKED("BLOCKED")' ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallOutcome.java && \
grep -q 'ERROR("ERROR")' ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallOutcome.java && \
! grep -r "lombok" ai-agent/ai-agent/src/main/java/com/vn/agent/entity/
    </automated>
  </verify>
  <acceptance_criteria>
    - All three enum files exist with `package com.vn.agent.entity;`
    - Each `implements EnumClass<String>`
    - Each has the exact id strings listed in action
    - Each defines `@Nullable static fromId(String id)`
    - Zero Lombok imports across the three files
    - `./gradlew :ai-agent:ai-agent:compileJava` exits 0
  </acceptance_criteria>
  <done>Three enum classes compile; IDs align with Spring AI chat-memory CHECK constraint.</done>
</task>

<task type="auto">
  <name>Task 2: Append enum i18n keys to messages.properties and create messages_vi.properties</name>
  <read_first>
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties (current 3 lines — preserve)
    - jmix-app/src/main/resources/com/vn/jmixapp/messages_en.properties (key pattern analog)
    - jmix-app/src/main/resources/com/vn/jmixapp/messages_vi.properties (VI analog)
    - .planning/phases/02-foundations/02-RESEARCH.md §"i18n Keys" (lines 828-928) — authoritative EN + VI enum values
    - .claude/skills/jmix-i18n/SKILL.md (invoke via Skill tool if available)
    - CLAUDE.md (messages in ALL locale files rule)
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties,
    ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties
  </files>
  <action>
**Append** to `messages.properties` (keep existing 3 lines at top — `localeDisplayName.en=English`, blank, `com.vn.agent/menu.addon=Ai-agent`). Add a blank line then these enum keys exactly:

```
# ===== Enums =====
com.vn.agent.entity/AiMessageRole.USER=User
com.vn.agent.entity/AiMessageRole.ASSISTANT=Assistant
com.vn.agent.entity/AiMessageRole.SYSTEM=System
com.vn.agent.entity/AiMessageRole.TOOL=Tool

com.vn.agent.entity/AiKnowledgeDocumentStatus.PENDING=Pending
com.vn.agent.entity/AiKnowledgeDocumentStatus.PROCESSING=Processing
com.vn.agent.entity/AiKnowledgeDocumentStatus.READY=Ready
com.vn.agent.entity/AiKnowledgeDocumentStatus.FAILED=Failed

com.vn.agent.entity/AiToolCallOutcome.SUCCESS=Success
com.vn.agent.entity/AiToolCallOutcome.BLOCKED=Blocked
com.vn.agent.entity/AiToolCallOutcome.ERROR=Error
```

**Create** `messages_vi.properties` with header + same key set in Vietnamese (per RESEARCH.md §i18n Keys VI block, lines 905-928):

```
localeDisplayName.vi=Tiếng Việt

com.vn.agent/menu.addon=Ai-agent

# ===== Enums =====
com.vn.agent.entity/AiMessageRole.USER=Người dùng
com.vn.agent.entity/AiMessageRole.ASSISTANT=Trợ lý
com.vn.agent.entity/AiMessageRole.SYSTEM=Hệ thống
com.vn.agent.entity/AiMessageRole.TOOL=Công cụ

com.vn.agent.entity/AiKnowledgeDocumentStatus.PENDING=Chờ xử lý
com.vn.agent.entity/AiKnowledgeDocumentStatus.PROCESSING=Đang xử lý
com.vn.agent.entity/AiKnowledgeDocumentStatus.READY=Sẵn sàng
com.vn.agent.entity/AiKnowledgeDocumentStatus.FAILED=Thất bại

com.vn.agent.entity/AiToolCallOutcome.SUCCESS=Thành công
com.vn.agent.entity/AiToolCallOutcome.BLOCKED=Bị chặn
com.vn.agent.entity/AiToolCallOutcome.ERROR=Lỗi
```

Do NOT remove any existing keys. Entity/role labels come in plans 03 + 08 (those plans will append to the same files).

File encoding MUST be UTF-8 (VI characters present).
  </action>
  <verify>
    <automated>
grep -q "com.vn.agent.entity/AiMessageRole.USER=User" ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties && \
grep -q "com.vn.agent.entity/AiMessageRole.TOOL=Tool" ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties && \
grep -q "com.vn.agent.entity/AiKnowledgeDocumentStatus.PENDING=Pending" ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties && \
grep -q "com.vn.agent.entity/AiToolCallOutcome.SUCCESS=Success" ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties && \
grep -q "com.vn.agent/menu.addon=Ai-agent" ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties && \
test -f ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties && \
grep -q "localeDisplayName.vi" ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties && \
grep -q "com.vn.agent.entity/AiMessageRole.USER=" ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties && \
grep -q "com.vn.agent.entity/AiToolCallOutcome.ERROR=" ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties
    </automated>
  </verify>
  <acceptance_criteria>
    - Existing 3 lines in messages.properties still present (not overwritten)
    - All 11 enum keys (4 role + 4 status + 3 outcome) appear in both files
    - messages_vi.properties exists with `localeDisplayName.vi=Tiếng Việt` at top
    - File is UTF-8 (VI chars render correctly when cat'd)
  </acceptance_criteria>
  <done>Both locale files contain identical key sets for all three enums.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Plan ships data-only enum classes + resource bundles; no authentication, no trust crossing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | AiMessageRole enum IDs drift from Spring AI CHECK constraint | mitigate | Hard-code id strings USER/ASSISTANT/SYSTEM/TOOL; grep-verified in acceptance criteria; cross-reference enforced by chat-memory changelog (plan 05) using identical literals |
| T-02-02 | Information Disclosure | Untranslated VI keys leak EN labels to Vietnamese users | accept | Identical key set asserted; no sensitive data — labels are UI strings |
</threat_model>

<verification>
- `./gradlew :ai-agent:ai-agent:compileJava` exits 0
- Both locale files parse as valid Java `Properties` (no malformed lines)
</verification>

<success_criteria>
Three enums compile; both locale files contain matching key sets for every enum value; no Lombok anywhere.
</success_criteria>

<output>
Create `.planning/phases/02-foundations/02-01-SUMMARY.md` after completion.
</output>
