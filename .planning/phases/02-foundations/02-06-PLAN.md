---
phase: 02-foundations
plan: 06
type: execute
wave: 4
depends_on: [04]
files_modified:
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/changelog/070-ai-kb-vector-store.xml
autonomous: true
requirements: [ENT-04]
must_haves:
  truths:
    - "pgvector extension + AI_AGENT_KB_VECTOR_STORE table DDL exists, gated to postgresql only"
    - "HSQLDB boot skips the changeset cleanly via dbms='postgresql' on changeSet AND preConditions onFail='MARK_RAN'"
    - "Vector dimension is 1536 (OpenAI default; hosts override both dimensions and DDL for other models)"
    - "HNSW index on embedding column uses vector_cosine_ops"
  artifacts:
    - path: "ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/changelog/070-ai-kb-vector-store.xml"
      contains: "CREATE EXTENSION IF NOT EXISTS vector"
    - path: "ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/changelog/070-ai-kb-vector-store.xml"
      contains: "AI_AGENT_KB_VECTOR_STORE"
    - path: "ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/changelog/070-ai-kb-vector-store.xml"
      contains: "vector(1536)"
    - path: "ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/changelog/070-ai-kb-vector-store.xml"
      contains: "USING HNSW"
  key_links:
    - from: "AI_AGENT_KB_VECTOR_STORE columns (id/content/metadata/embedding)"
      to: "Spring AI PgVectorStore.builder() (Phase 5)"
      via: "reference schema from pgvector.adoc"
      pattern: "id uuid.*content text.*metadata json.*embedding vector"
---

<objective>
Create 070-ai-kb-vector-store.xml with pgvector extension + AI_AGENT_KB_VECTOR_STORE table + HNSW index. All gated to postgresql. HSQLDB boot must MARK_RAN the whole changeset.

Purpose: ENT-04 mandates the pgvector schema ships in Phase 2. Phase 5 `PgVectorStore` reads this exact shape at runtime. Gating prevents HSQLDB test boots from failing on unknown `CREATE EXTENSION` syntax.
Output: 1 new changelog file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-foundations/02-CONTEXT.md
@.planning/phases/02-foundations/02-RESEARCH.md
@.planning/research/PITFALLS.md

<interfaces>
Reference schema (per Spring AI pgvector.adoc, verified in RESEARCH lines 402-418):
- `id uuid DEFAULT uuid_generate_v4() PRIMARY KEY`
- `content text`
- `metadata json`
- `embedding vector(1536)` — OpenAI default dim

Phase 5 will wire `PgVectorStore.builder().schemaName("public").vectorTableName("ai_agent_kb_vector_store").initializeSchema(false)`.

Gating: belt-and-suspenders — `dbms="postgresql"` on `<changeSet>` AND `<preConditions onFail="MARK_RAN"><dbms type="postgresql"/></preConditions>`. Per Pitfall #3, the preCondition MUST wrap the entire changeSet body, not just CREATE TABLE — otherwise raw `<sql>CREATE EXTENSION...</sql>` runs on HSQLDB and throws.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 070-ai-kb-vector-store.xml with gated pgvector DDL</name>
  <read_first>
    - .planning/phases/02-foundations/02-RESEARCH.md §"070-ai-kb-vector-store.xml" (lines 379-416)
    - .planning/phases/02-foundations/02-RESEARCH.md §Pitfall 3 (lines 233-236) — preConditions MUST wrap entire changeSet
    - .planning/phases/02-foundations/02-CONTEXT.md D-03 (pgvector ships in Phase 2 via preCondition)
    - .planning/research/PITFALLS.md (pgvector init notes)
  </read_first>
  <files>ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/changelog/070-ai-kb-vector-store.xml</files>
  <action>
Create `ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/changelog/070-ai-kb-vector-store.xml`:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
        pgvector extension + AI_AGENT_KB_VECTOR_STORE table.
        Gated to PostgreSQL via dbms attribute AND preConditions onFail="MARK_RAN"
        (belt-and-suspenders per Pitfall #3). HSQLDB boot skips the entire changeset.

        Table shape matches the Spring AI PgVectorStore reference schema exactly
        (id uuid DEFAULT uuid_generate_v4, content text, metadata json, embedding vector(1536)).
        Phase 5 wires PgVectorStore.builder().vectorTableName("ai_agent_kb_vector_store")
        .initializeSchema(false); Liquibase owns the DDL.
    -->

    <changeSet id="1" author="ai-agent" dbms="postgresql">
        <preConditions onFail="MARK_RAN">
            <dbms type="postgresql"/>
        </preConditions>
        <sql>CREATE EXTENSION IF NOT EXISTS vector;</sql>
        <sql>CREATE EXTENSION IF NOT EXISTS hstore;</sql>
        <sql>CREATE EXTENSION IF NOT EXISTS "uuid-ossp";</sql>
    </changeSet>

    <changeSet id="2" author="ai-agent" dbms="postgresql">
        <preConditions onFail="MARK_RAN">
            <dbms type="postgresql"/>
        </preConditions>
        <sql>
            CREATE TABLE IF NOT EXISTS AI_AGENT_KB_VECTOR_STORE (
                id uuid DEFAULT uuid_generate_v4() PRIMARY KEY,
                content text,
                metadata json,
                embedding vector(1536)
            );
        </sql>
        <sql>
            CREATE INDEX IF NOT EXISTS IDX_AI_AGENT_KB_VECTOR_STORE__EMBEDDING
            ON AI_AGENT_KB_VECTOR_STORE USING HNSW (embedding vector_cosine_ops);
        </sql>
    </changeSet>
</databaseChangeLog>
```

Do not replace `uuid_generate_v4()` with `gen_random_uuid()` — Spring AI's reference schema uses `uuid-ossp` and the changeset `CREATE EXTENSION IF NOT EXISTS "uuid-ossp"` installs it.

Do not change vector dimension from 1536 — this is the OpenAI default; a mismatch breaks Phase 5 PgVectorStore.

Do not drop the belt-and-suspenders preCondition. `dbms` on `<changeSet>` handles most cases; preCondition handles hosts with a Postgres-compat proxy where `dbms` may resolve late.
  </action>
  <verify>
    <automated>
test -f ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/changelog/070-ai-kb-vector-store.xml && \
grep -q 'changeSet id="1" author="ai-agent" dbms="postgresql"' ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/changelog/070-ai-kb-vector-store.xml && \
grep -q 'changeSet id="2" author="ai-agent" dbms="postgresql"' ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/changelog/070-ai-kb-vector-store.xml && \
grep -c '<preConditions onFail="MARK_RAN">' ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/changelog/070-ai-kb-vector-store.xml | awk '{exit !($1 == 2)}' && \
grep -q "CREATE EXTENSION IF NOT EXISTS vector" ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/changelog/070-ai-kb-vector-store.xml && \
grep -q 'CREATE EXTENSION IF NOT EXISTS "uuid-ossp"' ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/changelog/070-ai-kb-vector-store.xml && \
grep -q "AI_AGENT_KB_VECTOR_STORE" ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/changelog/070-ai-kb-vector-store.xml && \
grep -q "embedding vector(1536)" ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/changelog/070-ai-kb-vector-store.xml && \
grep -q "USING HNSW (embedding vector_cosine_ops)" ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/changelog/070-ai-kb-vector-store.xml && \
grep -q "IDX_AI_AGENT_KB_VECTOR_STORE__EMBEDDING" ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/changelog/070-ai-kb-vector-store.xml
    </automated>
  </verify>
  <acceptance_criteria>
    - Exactly 2 changesets, both with dbms="postgresql" AND preConditions onFail="MARK_RAN" with <dbms type="postgresql"/>
    - All three extensions present: vector, hstore, uuid-ossp
    - Table columns exactly: id uuid, content text, metadata json, embedding vector(1536)
    - HNSW index with vector_cosine_ops on embedding
    - No CREATE EXTENSION statements outside a gated changeset
  </acceptance_criteria>
  <done>pgvector DDL shipped; HSQLDB boots cleanly skipping this file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Liquibase DDL → Postgres server | `CREATE EXTENSION` requires superuser or pg-specific privileges |
| AI_AGENT_KB_VECTOR_STORE ← Phase 5 retrieval advisor | Row security not at DB; filtered by Spring AI `FILTER_EXPRESSION` at query time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-VEC-01 | Elevation of Privilege | `CREATE EXTENSION vector` requires superuser on stock Postgres | mitigate (docs) | Plan 11 doc updates include an operator note: "the DB user running Liquibase must have CREATE EXTENSION privilege". Falls under Phase 8 operator docs finalization |
| T-02-VEC-02 | Denial of Service | HSQLDB boot fails because pgvector DDL leaks through gating | mitigate | Belt-and-suspenders: `dbms="postgresql"` on `<changeSet>` AND `<preConditions onFail="MARK_RAN">` wrapping BOTH changesets. Plan 10 smoke test on HSQLDB verifies changeset is MARK_RAN and table is absent |
| T-02-VEC-03 | Information Disclosure | Vector chunks with no row-level policy → any DB read exposes all tenants' docs | mitigate (downstream) | Phase 5 wires `FILTER_EXPRESSION` per request from `CurrentAuthentication.getAuthorities()` (RAG-05, RAG-06). Phase 2 scope is DDL only; risk accepted pending Phase 5 enforcement |
| T-02-VEC-04 | Tampering | Vector dimension mismatch (host uses 768-dim model) silently corrupts retrieval | mitigate (docs) | Plan 11 docs note: hosts using non-1536-dim embeddings must override both `.dimensions()` config and the changelog. Phase 5 adds bean-collision test (RAG-02) |
</threat_model>

<verification>
- 070-ai-kb-vector-store.xml parses as valid Liquibase XML
- Plan 10 smoke test asserts AI_AGENT_KB_VECTOR_STORE is absent on HSQLDB (changeset was MARK_RAN)
</verification>

<success_criteria>
pgvector extension + AI_AGENT_KB_VECTOR_STORE table DDL shipped and properly gated; HNSW index in place; HSQLDB boots cleanly.
</success_criteria>

<output>
Create `.planning/phases/02-foundations/02-06-SUMMARY.md` after completion.
</output>
