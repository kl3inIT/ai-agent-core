---
phase: 08
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-agent/README.md
autonomous: true
requirements:
  - TEST-07
must_haves:
  truths:
    - "ai-agent/README.md exists with all seven mandated sections from CONTEXT D-14: Quick Start, Required Environment Variables, Configuration Matrix, Entity/Table Ownership, SPI Cookbook, Upgrade Checklist, Air-Gap Notes"
    - "Configuration Matrix lists every jmix.ai-agent.* property currently exposed by the add-on with default value and 1-line description"
    - "Entity/Table Ownership section tabulates every AI_AGENT_* table + the Liquibase changeset that creates it"
    - "SPI Cookbook contains a runnable code snippet for each of the six SPIs: ToolContributor, ContextContributor, PromptContextContributor, ToolGuard, AuditListener, CustomIngester"
    - "Quick Start describes a 3-command path to a running chat (clone, set OPENROUTER_API_KEY, gradlew bootRun) leading to a working chat UI in <10 minutes (ROADMAP success criterion #3)"
  artifacts:
    - path: "ai-agent/README.md"
      provides: "Operator README per CONTEXT D-14 + RESEARCH Example 5"
      contains: "Quick Start"
  key_links:
    - from: "ai-agent/README.md"
      to: "docs/consumer-smoke.md (existing companion)"
      via: "Markdown link"
      pattern: "consumer-smoke"
    - from: "ai-agent/README.md"
      to: "ai-agent/ai-agent/src/main/resources/com/vn/agent/agent.properties (or wherever @ConfigurationProperties live)"
      via: "Configuration Matrix table cross-references property names"
      pattern: "jmix.ai-agent"
---

<objective>
Land the operator README per CONTEXT D-14: a single Markdown file at `ai-agent/README.md` containing the seven mandated sections, plus an SPI cookbook with one runnable code snippet per SPI.

**Placement decision (CONTEXT § Claude's Discretion):** `ai-agent/README.md` (not `ai-agent/ai-agent/README.md`). Rationale: a host integrator's first-look path is the published artifact's group/repository, which surfaces the `ai-agent/` directory README on GitHub/Nexus listings. The deeper `ai-agent/ai-agent/` README would be inside the functional subproject and less discoverable.

Purpose: Satisfy ROADMAP Phase 8 success criterion #3 ("Operator README walkthrough produces a working demo in under 10 minutes").
Output: One Markdown file. English-only per CONTEXT § code_context (i18n parity NOT required for SPI cookbook code).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-integration-hardening-release-readiness/08-CONTEXT.md
@.planning/phases/08-integration-hardening-release-readiness/08-RESEARCH.md
@.planning/phases/08-integration-hardening-release-readiness/08-PATTERNS.md
@docs/consumer-smoke.md
@docs/versions.md
@ai-agent/ai-agent/src/main/java/com/vn/agent/spi/ToolContributor.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/spi/ContextContributor.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/spi/PromptContextContributor.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/spi/ToolGuard.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/spi/AuditListener.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/spi/CustomIngester.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ai-agent/README.md operator docs</name>
  <files>ai-agent/README.md</files>
  <read_first>
    - docs/consumer-smoke.md (existing operator-doc tone)
    - docs/versions.md (version-matrix doc; cross-reference)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/spi/*.java (READ ALL six SPI interfaces — capture exact method signatures for SPI cookbook snippets; do not invent signatures)
    - All `@ConfigurationProperties` classes under `ai-agent/ai-agent/src/main/java/com/vn/agent/` (grep for `@ConfigurationProperties(prefix = "jmix.ai-agent.` to enumerate all live config keys + their default values)
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/*.xml (enumerate AI_AGENT_* table changesets — D-17/Phase 7.2 may have renamed some)
  </read_first>
  <action>
    Create new file at `ai-agent/README.md` (NOT `ai-agent/ai-agent/README.md`).

    Use RESEARCH Example 5 (lines 805-953) as the skeleton. Adapt the Configuration Matrix table by enumerating the ACTUAL `@ConfigurationProperties` classes in the codebase — do NOT use the RESEARCH example values blindly; the RESEARCH example was illustrative and may drift from the current codebase.

    **Required structure (in order):**

    ```markdown
    # Jmix AI Copilot

    Drop-in Jmix add-on (2.8+) that lets end-users safely converse with their data and documents. Built on Spring AI 1.1.4. No agent framework code required from the host team.

    ## Quick Start (3 commands)

    **Prerequisites:** JDK 21 (per the Gradle toolchain declared in `ai-agent/build.gradle` and `consumer-smoke/consumer-smoke.gradle`). Java 17 is NOT supported.

    ```bash
    git clone https://github.com/<org>/ai-agent-core.git
    export OPENROUTER_API_KEY=sk-or-v1-...
    ./gradlew :jmix-app:bootRun
    ```

    Open http://localhost:8080 — log in as `admin` / `admin` — click "AI Agent → Chat".

    ## Required Environment Variables

    | Variable | Purpose | Default |
    |---|---|---|
    | `OPENROUTER_API_KEY` | LLM provider key | (none — required) |
    | `OPENROUTER_BASE_URL` | API endpoint | `https://openrouter.ai/api/v1` |
    | ... | ... | ... |

    **Runtime:** Java 21 (JDK 21). The Gradle toolchain enforces this; running on JDK 17 will fail to compile. Note: the repo-root `CLAUDE.md` mentions Java 17 — that text is stale and contradicts the actual `JavaLanguageVersion.of(21)` toolchain in `ai-agent/build.gradle`. Treat this README as the authoritative runtime statement.

    ## Configuration Matrix

    [Concrete table — read every @ConfigurationProperties class in the codebase and list each property + default + 1-line description.]

    | Property | Default | Description |
    |---|---|---|
    | `jmix.ai-agent.tools.max-filter-depth` | `3` | Max attribute-path depth for find_records filters |
    | ... | ... | ... |

    ## Entity / Table Ownership

    [Concrete table — read every Liquibase changelog under ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/ and tabulate.]

    | Entity | Table | Liquibase Changeset | Created In |
    |---|---|---|---|
    | `AiConversation` | `AI_AGENT_CONVERSATION` | `010-...xml` | Phase 2 |
    | `AiAuditEvent` | `AI_AGENT_AUDIT_EVENT` | `09X-...xml` (Phase 7.2 redesign) | Phase 7.2 |
    | ... | ... | ... | ... |

    ## SPI Cookbook

    Six SPIs ship with the add-on. Each example is a host-side `@Component` implementing the interface.

    ### `ToolContributor` — register additional `@Tool` methods

    [code snippet — read ToolContributor.java to confirm method signature]

    ### `ContextContributor` — inject per-request context
    ### `PromptContextContributor` — augment system prompt
    ### `ToolGuard` — veto tool calls
    ### `AuditListener` — observe audit writes  *(post-Phase-7.2 SPI: `onEventAudited(UUID auditId, String kind)`)*
    ### `CustomIngester` — plug in additional KB sources

    ## Upgrade Checklist

    - **From 0.x to 1.0.0:** ...

    ## Air-Gap Notes

    - No telemetry. No phone-home.
    - All LLM/embedding traffic goes to whatever `spring.ai.openai.base-url` resolves to. Air-gapped deployments point this at an internal model gateway (e.g. Ollama, vLLM with OpenAI compatibility shim).
    - No bundled API keys. All credentials are host-supplied.

    ## See Also

    - [`docs/consumer-smoke.md`](../docs/consumer-smoke.md) — manual clean-consumer smoke walkthrough
    - [`docs/versions.md`](../docs/versions.md) — Jmix / Spring Boot / Spring AI version matrix
    - [`CHANGELOG.md`](../CHANGELOG.md) — release history (created in Plan 08-07)
    ```

    **Critical executor instructions:**

    1. **Configuration Matrix MUST be derived from actual code.** Run `grep -rn "@ConfigurationProperties" ai-agent/ai-agent/src/main/java/com/vn/agent/` and follow each result. For each `@ConfigurationProperties` class, list every field with its default value (read the field initializer or the `@DefaultValue` annotation). Do NOT carry over RESEARCH Example 5 values verbatim — they may not match.

    2. **Entity/Table Ownership MUST be derived from Liquibase changelogs.** Run `ls ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/changelog/` (or wherever the changelogs live) and read each XML to capture the table name + changeset id + which phase introduced it (use git blame or commit history).

    3. **SPI Cookbook code snippets MUST match real method signatures.** Read each SPI interface in `ai-agent/ai-agent/src/main/java/com/vn/agent/spi/`. For example, `AuditListener` has been REDESIGNED in Phase 7.2 to `onEventAudited(UUID auditId, String kind)` per CONTEXT canonical_refs — DO NOT use the old `dispatchToolCallAudited` shape from older docs.

    4. **NO i18n** — the README is operator-facing (English only) per CONTEXT § code_context. Per memory `feedback_jmix_first_ui.md`: SPI cookbook examples can show raw Vaadin OR Jmix XML — but if any UI snippet appears in the cookbook, default to Jmix views over raw Vaadin.

    5. Per memory `feedback_no_abbreviations.md`: spell out names in code snippets (`OrderSummaryToolContributor`, not `OSTC`).

    6. Per memory `feedback_no_archunit.md`: do NOT mention ArchUnit in the README. The forbidden-imports convention in CLAUDE.md is the documented enforcement mechanism.

    7. Cross-link to `CHANGELOG.md` (Plan 08-07 creates it) — the link target may not exist when this plan executes; that is acceptable. The link will resolve once Plan 08-07 lands.

    8. Length target: 200-400 lines. Below 150 = too thin; above 500 = too verbose for an operator first-look doc.
  </action>
  <verify>
    <automated>test -f ai-agent/README.md && wc -l ai-agent/README.md && grep -c "## " ai-agent/README.md</automated>
  </verify>
  <acceptance_criteria>
    - File `ai-agent/README.md` exists
    - `grep -c "^## Quick Start" ai-agent/README.md` returns 1
    - `grep -c "^## Required Environment Variables" ai-agent/README.md` returns 1
    - `grep -c "^## Configuration Matrix" ai-agent/README.md` returns 1
    - `grep -c "^## Entity / Table Ownership" ai-agent/README.md` returns 1
    - `grep -c "^## SPI Cookbook" ai-agent/README.md` returns 1
    - `grep -c "^## Upgrade Checklist" ai-agent/README.md` returns 1
    - `grep -c "^## Air-Gap Notes" ai-agent/README.md` returns 1
    - `grep -c "ToolContributor" ai-agent/README.md` returns at least 2 (heading + code snippet)
    - `grep -c "ContextContributor" ai-agent/README.md` returns at least 2
    - `grep -c "PromptContextContributor" ai-agent/README.md` returns at least 2
    - `grep -c "ToolGuard" ai-agent/README.md` returns at least 2
    - `grep -c "AuditListener" ai-agent/README.md` returns at least 2
    - `grep -c "CustomIngester" ai-agent/README.md` returns at least 2
    - `grep -c "onEventAudited" ai-agent/README.md` returns at least 1 (Phase 7.2 SPI signature, NOT the old `dispatchToolCallAudited`)
    - `grep -c "dispatchToolCallAudited" ai-agent/README.md` returns 0 (deprecated SPI shape must NOT appear)
    - `grep -c "ArchUnit" ai-agent/README.md` returns 0 (deferred per D-10)
    - `grep -c "AiExposureRule\|EntityExposurePolicy\|ExposureRuleListView" ai-agent/README.md` returns 0 (dropped per D-10)
    - `grep -c "jmix.ai-agent\." ai-agent/README.md` returns at least 5 (Configuration Matrix has multiple entries)
    - `grep -c "AI_AGENT_" ai-agent/README.md` returns at least 5 (Entity/Table Ownership has multiple entries)
    - `grep -i "JDK 21\|Java 21" ai-agent/README.md` returns at least 1 match (W-01: explicit runtime statement)
    - File length between 150 and 500 lines
  </acceptance_criteria>
  <done>README exists with all seven mandated sections; Configuration Matrix and Entity/Table Ownership reflect real codebase state (not RESEARCH placeholder values); SPI cookbook uses post-Phase-7.2 signatures.</done>
</task>

</tasks>

<verification>
- All seven mandated sections present (Quick Start, Required Env Vars, Configuration Matrix, Entity/Table Ownership, SPI Cookbook, Upgrade Checklist, Air-Gap Notes).
- Configuration Matrix references real `@ConfigurationProperties` keys (verify by spot-checking 2-3 of the listed properties against grep output).
- SPI Cookbook references the post-Phase-7.2 `onEventAudited` signature, NOT the old `dispatchToolCallAudited`.
- Deprecated/dropped concepts (ArchUnit, AiExposureRule, EntityExposurePolicy, ExposureRuleListView) absent.
- Cross-links to `docs/consumer-smoke.md`, `docs/versions.md`, and `CHANGELOG.md`.
</verification>

<success_criteria>
- ROADMAP Phase 8 success criterion #3 ("operator README walkthrough produces a working demo in under 10 minutes for someone new to the repo") is backstopped by a concrete README.
- D-14 mandated sections all present.
- D-15 satisfied: README walkthrough uses the existing `jmix-app` harness; no separate demo-seed script.
</success_criteria>

<output>
After completion, create `.planning/phases/08-integration-hardening-release-readiness/08-06-SUMMARY.md`
</output>
