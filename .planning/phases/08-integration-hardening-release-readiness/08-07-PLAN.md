---
phase: 08
plan: 07
type: execute
wave: 2
depends_on: [08-01, 08-02, 08-03, 08-04, 08-05, 08-06]
files_modified:
  - ai-agent/build.gradle
  - ai-agent/gradle.properties
  - .gitignore
  - CHANGELOG.md
  - .github/workflows/ai-agent-ci.yml
  - .github/workflows/ai-agent-live.yml
  - .github/workflows/ai-agent-publish.yml
autonomous: false
requirements:
  - TEST-07
must_haves:
  truths:
    - "ai-agent/build.gradle line 12 reads `version = project.findProperty('version') ?: '1.0.0'` (or equivalent expression that resolves to 1.0.0 by default with -Pversion override capability)"
    - "ai-agent/gradle.properties contains `version=1.0.0` AND does NOT contain the lines `nexusUsername=admin` / `nexusPassword=admin123` (committed-secret hazard remediated)"
    - "Repo-root .gitignore contains a pattern excluding any local nexus-credentials file"
    - "CHANGELOG.md exists at repo root following Keep-a-Changelog v1.1.0 with a [1.0.0] section backfilled from ROADMAP phase summaries"
    - "Three GitHub Actions workflows exist under .github/workflows/: ai-agent-ci.yml (PR-blocking — test + integrationTest + publishToMavenLocal + bootRunSmoke), ai-agent-live.yml (workflow_dispatch — liveTest gated on OPENROUTER_API_KEY secret), ai-agent-publish.yml (workflow_dispatch + tag — publish to Nexus using -PnexusUsername/-PnexusPassword from secrets)"
    - "The publishing block at ai-agent/build.gradle:63-81 is NOT redeclared anywhere — it remains the single source of publishing config"
    - "Snapshot-vs-release URL conditional is wired in ai-agent/build.gradle: snapshot URL when version ends with -SNAPSHOT, release URL otherwise (per RESEARCH Pitfall 6)"
  artifacts:
    - path: "ai-agent/build.gradle"
      provides: "version source moved to gradle.properties; snapshot-vs-release URL conditional added to repositories.maven.url"
      contains: "1.0.0"
    - path: "ai-agent/gradle.properties"
      provides: "version=1.0.0; nexus credentials lines REMOVED"
      contains: "version=1.0.0"
    - path: ".gitignore"
      provides: "Pattern excluding local nexus-credentials files"
      contains: "nexus"
    - path: "CHANGELOG.md"
      provides: "Keep-a-Changelog v1.1.0 with backfilled [1.0.0] section"
      contains: "[1.0.0]"
    - path: ".github/workflows/ai-agent-ci.yml"
      provides: "PR-blocking workflow: test + integrationTest + publishToMavenLocal + bootRunSmoke"
      contains: "ai-agent CI"
    - path: ".github/workflows/ai-agent-live.yml"
      provides: "workflow_dispatch live-tier workflow gated on OPENROUTER_API_KEY"
      contains: "workflow_dispatch"
    - path: ".github/workflows/ai-agent-publish.yml"
      provides: "workflow_dispatch + tag-triggered publish to Nexus"
      contains: "publish"
  key_links:
    - from: "ai-agent/gradle.properties"
      to: "ai-agent/build.gradle line 12"
      via: "project.findProperty('version')"
      pattern: "findProperty.*version"
    - from: ".github/workflows/ai-agent-publish.yml"
      to: "ai-agent/build.gradle publishing block"
      via: "-PnexusUsername=${{ secrets.NEXUS_USERNAME }} -PnexusPassword=${{ secrets.NEXUS_PASSWORD }}"
      pattern: "nexusUsername"
    - from: ".github/workflows/ai-agent-ci.yml"
      to: "consumer-smoke project"
      via: "Gradle invocation `:consumer-smoke:bootRunSmoke`"
      pattern: "bootRunSmoke"
---

<objective>
Land release polish + CI per CONTEXT D-16/D-17/D-18/D-19: bump version to 1.0.0, remove committed Nexus credentials, write CHANGELOG.md, wire three GitHub Actions workflows.

**Critical adaptations encoded here (per planning_context):**

1. **`maven-publish` already wired at `ai-agent/build.gradle:63-81`** (RESEARCH Pitfall 5). Plan does NOT re-declare publishing — verifies + edits version source + adds snapshot/release URL conditional.

2. **Version is in `ai-agent/build.gradle` line 12, NOT `gradle.properties`** (CONTEXT D-16 implied wrong location). This plan moves the source to `gradle.properties` (idiomatic Gradle) AND adds `findProperty` indirection so CI can pass `-Pversion=1.0.0` on tag pushes.

3. **Committed Nexus credentials at `ai-agent/gradle.properties:3-4`** (CONTEXT canonical_refs + RESEARCH Pitfall 3). MUST remove + add `.gitignore` pattern + flag credential rotation as autonomous: false (rotation requires Nexus admin UI access which Claude cannot perform).

4. **Snapshot URL `…/jmix-internal-snapshots/` rejects `1.0.0` release uploads** (RESEARCH Pitfall 6). Resolution: conditional URL based on `version.endsWith('SNAPSHOT')`. Release URL is `…/jmix-internal-releases/` (assumed; flag for user confirmation).

5. **Tests-first audit framing** (CONTEXT D-01/D-02). This plan does NOT pre-bake fixes for any deferred Phase 7.1 issue. CHANGELOG entries reflect real merged work only.

This plan is `autonomous: false` because: (a) credential rotation requires Nexus admin UI; (b) the release URL needs user confirmation per RESEARCH Pitfall 6; (c) `nexusUsername` / `nexusPassword` migration to a CI-secrets path requires user to add secrets in GitHub repo settings.

Output: 7 files modified/created.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-integration-hardening-release-readiness/08-CONTEXT.md
@.planning/phases/08-integration-hardening-release-readiness/08-RESEARCH.md
@.planning/phases/08-integration-hardening-release-readiness/08-PATTERNS.md
@ai-agent/build.gradle
@ai-agent/gradle.properties
@ai-agent/ai-agent/ai-agent.gradle
@ai-agent/ai-agent-starter/ai-agent-starter.gradle
@settings.gradle
@docs/consumer-smoke.md
</context>

<tasks>

<task type="auto" autonomous="false">
  <name>Task 1: Version bump + version-source extraction in ai-agent/build.gradle + ai-agent/gradle.properties</name>
  <files>ai-agent/build.gradle, ai-agent/gradle.properties, .gitignore</files>
  <read_first>
    - ai-agent/build.gradle (READ ENTIRE FILE — confirm line 12 is `version = '0.0.1-SNAPSHOT'`; confirm publishing block at lines 63-81 with snapshot URL at line 67)
    - ai-agent/gradle.properties (READ — confirm lines 3-4 are `nexusUsername=admin` / `nexusPassword=admin123`)
    - .gitignore (READ existing patterns; we add additively, not replace)
  </read_first>
  <action>
    **File A — `ai-agent/gradle.properties`:**

    Read current content (4 lines: org.gradle.jvmargs, blank, nexusUsername, nexusPassword).

    Replace ENTIRE FILE with:
    ```properties
    org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8

    # Phase 8 plan 08-07 (CONTEXT D-16) — version source.
    # CI overrides via -Pversion=1.0.0 on tag push.
    version=1.0.0

    # Phase 8 plan 08-07 (RESEARCH Pitfall 3 + CONTEXT canonical_refs):
    # nexusUsername / nexusPassword previously committed here have been REMOVED.
    # Local development: create ai-agent/gradle-local.properties (gitignored) with:
    #   nexusUsername=<your-nexus-user>
    #   nexusPassword=<your-nexus-pass>
    # CI: passes -PnexusUsername / -PnexusPassword from GitHub Actions secrets
    # (see .github/workflows/ai-agent-publish.yml).
    ```

    **File B — `ai-agent/build.gradle`:**

    Locate line 12: `version = '0.0.1-SNAPSHOT'`.

    Replace with:
    ```groovy
    version = project.findProperty('version') ?: '1.0.0'
    ```

    (`findProperty` returns the value from gradle.properties or `-Pversion=…` override; defaults to '1.0.0' if neither set.)

    Locate the publishing block at lines 63-81. Find line 67: `url = 'https://nexus.x2h.com.vn/repository/jmix-internal-snapshots/'`.

    Replace with the snapshot-vs-release conditional (RESEARCH Pitfall 6 option C):
    ```groovy
    url = version.toString().endsWith('SNAPSHOT')
            ? 'https://nexus.x2h.com.vn/repository/jmix-internal-snapshots/'
            : 'https://nexus.x2h.com.vn/repository/jmix-internal-releases/'
    ```

    **OPEN QUESTION (autonomous: false trigger):** the release URL `…/jmix-internal-releases/` is the Nexus standard naming convention but NOT yet confirmed for x2h.com.vn. Before this task is declared `done`, the user must confirm the actual release-repository URL with the Nexus admin. If the URL differs, update the literal in the conditional. Acceptance is gated on user confirmation.

    **W-03 GATING — manual confirmation required.** This task is `autonomous: false`. The release URL `https://nexus.x2h.com.vn/repository/jmix-internal-releases/` is ASSUMED (RESEARCH Pitfall 6). It MUST NOT be merged until Task 4 (human-action checkpoint) has been signed off. An executor running Task 1 in isolation MUST stop after writing the literal and wait for the Task 4 sign-off, then either keep the literal as-written OR replace it with the user-confirmed URL before declaring `done`.

    Add a comment above the publishing block:
    ```groovy
    // Phase 8 plan 08-07 (CONTEXT D-18 + RESEARCH Pitfall 6):
    // Snapshot-vs-release URL conditional. Snapshot URL accepts -SNAPSHOT versions;
    // release URL accepts non-SNAPSHOT versions (e.g. 1.0.0). Confirm the release
    // URL with Nexus admin before first release publish.
    ```

    DO NOT touch any other line in `ai-agent/build.gradle`.

    **File C — `.gitignore`** (repo root):

    Read current content. Append:
    ```
    # Phase 8 plan 08-07 — local Nexus credentials.
    # Production credentials must NEVER be committed. CI uses GitHub Actions secrets
    # (-PnexusUsername / -PnexusPassword from secrets.NEXUS_USERNAME / NEXUS_PASSWORD).
    # Local development: place credentials in ai-agent/gradle-local.properties.
    ai-agent/gradle-local.properties
    *.gradle.local.properties
    ```

    Verify Gradle picks up `gradle-local.properties` automatically — the convention is to use `~/.gradle/gradle.properties` for cross-project secrets, OR Gradle's init script. If `ai-agent/gradle-local.properties` is NOT auto-loaded by default, add an explicit load at the top of `ai-agent/build.gradle`:
    ```groovy
    // Phase 8 plan 08-07 — load local non-committed credentials if present.
    def localProps = file('gradle-local.properties')
    if (localProps.exists()) {
        Properties p = new Properties()
        localProps.withInputStream { p.load(it) }
        p.each { k, v -> ext."$k" = v }
    }
    ```
    Place this after the `buildscript` block but before `subprojects`. The credentials end up on the `ext.` namespace, accessible via `project.findProperty('nexusUsername')` in the publishing block (already that shape — no additional edit needed there).
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:tasks --quiet --no-daemon | head -1 && grep -c "nexusUsername=admin" ai-agent/gradle.properties && grep -c "nexusPassword=admin123" ai-agent/gradle.properties</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "version=1.0.0" ai-agent/gradle.properties` returns 1
    - `grep -c "nexusUsername=admin" ai-agent/gradle.properties` returns 0 (line removed)
    - `grep -c "nexusPassword=admin123" ai-agent/gradle.properties` returns 0 (line removed)
    - `grep -c "findProperty('version')" ai-agent/build.gradle` returns 1
    - `grep -c "endsWith('SNAPSHOT')" ai-agent/build.gradle` returns 1
    - `grep -c "jmix-internal-releases" ai-agent/build.gradle` returns 1
    - `grep -c "gradle-local.properties" .gitignore` returns at least 1
    - `./gradlew :ai-agent:ai-agent:properties --quiet | grep "^version:"` returns `version: 1.0.0` (proves version source resolved correctly)
    - `./gradlew :ai-agent:ai-agent:tasks` exits 0 (build script still parses)
    - **W-03 gate:** Task 4 (human-action checkpoint) has been signed off by the user (release URL confirmed; credentials rotated; GitHub secrets created). This task MUST NOT be marked `done` until Task 4 is `approved`.
  </acceptance_criteria>
  <done>Version is 1.0.0; credentials removed; snapshot/release conditional in place. **Done is blocked until Task 4 is `approved`** — the assumed release URL must be human-confirmed before this task can be merged (W-03).</done>
</task>

<task type="auto">
  <name>Task 2: Create CHANGELOG.md with backfilled [1.0.0] section</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - .planning/ROADMAP.md (READ — phase summaries are the source for backfill)
    - .planning/phases/01-walking-skeleton/*-SUMMARY.md (skim — what landed)
    - .planning/phases/02-foundations/*-SUMMARY.md (skim)
    - .planning/phases/03-metadata-runtime-six-tools/*-SUMMARY.md (skim)
    - .planning/phases/04-orchestration-core/*-SUMMARY.md (skim)
    - .planning/phases/05-rag-layer/*-SUMMARY.md (skim)
    - .planning/phases/06-parameters-guardrails/*-SUMMARY.md (skim)
    - .planning/phases/07-flow-ui/*-SUMMARY.md (skim)
    - .planning/phases/07.1-vaadin-message-list/*-SUMMARY.md (skim)
    - .planning/phases/07.2-redesign-audit-schema-tree-lite/*-SUMMARY.md (skim)
  </read_first>
  <action>
    Create new file at repo root: `CHANGELOG.md`.

    Per CONTEXT D-17 + Keep-a-Changelog v1.1.0 spec.

    Skeleton (per RESEARCH Code Examples §CHANGELOG.md):
    ```markdown
    # Changelog

    All notable changes to this project will be documented in this file.

    The format is based on [Keep a Changelog v1.1.0](https://keepachangelog.com/en/1.1.0/),
    and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

    ## [Unreleased]

    ## [1.0.0] - 2026-04-26

    ### Added

    - **Walking skeleton + packaging (Phase 1)**: 2-module shape (`ai-agent` + `ai-agent-starter`); Spring AI 1.1.4 BOM; OpenRouter ChatModel + JDBC chat memory; @Tag("live") test tier; clean-consumer smoke documented.
    - **Foundations (Phase 2)**: Five JPA entities (`AiConversation`, `AiMessage`, `AiAuditEvent` (originally `AiToolCallAudit`, renamed in 7.2), `AiParameters`, `AiKnowledgeDocument`); Liquibase changelogs with `AI_AGENT_*` prefix; pgvector extension table; Spring AI JDBC chat-memory schema; `AiAgentUserRole` / `AiAgentAdminRole` / `AiAgentUserRowLevelRole`; six SPI interfaces.
    - **Metadata runtime + six tools (Phase 3)**: `MetamodelScanner`; per-user effective schema via `CurrentUserSchemaAccess`; six `@Tool` methods (`list_entities`, `describe_entity`, `find_records`, `get_record`, `count_records`, `get_related_records`); structured filter DSL; `<data>`-delimited safe formatter; hard row-count cap (`ToolLimits.MAX_LIMIT = 100`).
    - **Orchestration core (Phase 4)**: `ChatClientFactory` advisor chain; `JdbcChatMemoryRepository` + `ConversationProjector` dual-layer; `AuditAdvisor` with REQUIRES_NEW; `AuditListener` SPI fan-out; `ContextContributor` / `PromptContextContributor` defaults.
    - **RAG layer (Phase 5)**: File-upload ingestion via Tika + TokenTextSplitter; pgvector storage with `allowedRoles` metadata; `QuestionAnswerAdvisor` with per-request `FILTER_EXPRESSION` from caller's roles; fail-closed default for untagged chunks; `CustomIngester` SPI + classpath-markdown sample.
    - **Parameters + guardrails (Phase 6)**: `AiParameters` profiles with YAML editor; `default-params.yaml` bootstrap; per-conversation overrides; `ToolGuard` SPI; iteration cap (default 6); per-session token circuit breaker; per-user rate limiter; output-side injection scanner; structured output via `BeanOutputConverter` + `.entity(Class)` + bounded retry.
    - **Flow UI (Phase 7)**: `ChatView` (streaming + tool-call cards + citations); `ConversationListView` / `ConversationDetailView` (ownership filter); `ParametersListView` / `ParametersDetailView` (YAML editor); `KnowledgeBaseView`; `ToolCallAuditListView` with Excel + JSON export; bilingual EN+VI menu and labels; role-gated visibility.
    - **Operator README** (Phase 8): Quick start, env vars, configuration matrix, entity/table ownership, SPI cookbook, upgrade checklist, air-gap notes.
    - **CI workflows** (Phase 8): `.github/workflows/ai-agent-ci.yml` (PR-blocking test + smoke), `ai-agent-live.yml` (workflow_dispatch live tier), `ai-agent-publish.yml` (workflow_dispatch + tag publish to Nexus).
    - **Clean-consumer smoke** (Phase 8): `consumer-smoke/` Gradle subproject + `bootRunSmoke` task; assert ChatService bean + view registration via mavenLocal-resolved artifact.
    - **TEST-04 negative-case suite** (Phase 8): `FilteredSchemaAndExecutionDenialTest`, `RagRoleFilterNegativeTest`, `CrossUserConversationAccessTest`.
    - **TEST-05 live-tier golden suite** (Phase 8): six capability-coverage questions in `golden-questions.yaml`; `containsAnyOf` semantic anchors.
    - **TEST-02/03 extensions** (Phase 8): poisoned tool-result fixture in `PromptInjectionHarnessTest`; tool-throw + outer-rollback case in `AuditDurabilityTest`.
    - **Performance smoke** (Phase 8): `datasource-proxy`-based per-tool query-count baseline; `find_records` limit-cap test.

    ### Changed

    - **Audit schema redesigned to tree-lite (Phase 7.2)**: flat `AI_AGENT_TOOL_CALL_AUDIT` + `KIND` discriminator replaced by self-FK `PARENT_ID` tree. One root row per chat turn (`PARENT_ID = null`, `KIND = CHAT`); tool / retrieval / future guardrail events as children. PRE/POST dual rows collapsed into single mutable row with `startedAt` / `finishedAt`. Entity renamed `AiToolCallAudit` → `AiAuditEvent`.
    - **AuditListener SPI breaking change (Phase 7.2)**: `dispatchToolCallAudited(UUID)` → `onEventAudited(UUID auditId, String kind)`. Host listeners must update.
    - **ChatView refactored to Vaadin MessageList/MessageInput (Phase 7.1)**: custom component tree (`ChatPanelFragment` + `MessageBubbleComponent` + `ToolCallCardComponent` + `MarkdownRenderer`) replaced with stock Vaadin primitives.
    - **Spring AI BOM upgraded 1.0.2 → 1.1.4** (Phase 1 → Phase 2 transition).
    - **JDK 21 toolchain enforced** (Phase 8 plan 08-07): `ai-agent/build.gradle` and `consumer-smoke/consumer-smoke.gradle` both pin `JavaLanguageVersion.of(21)`. Java 17 is NOT supported (the historical `CLAUDE.md` reference to Java 17 is stale; this CHANGELOG and `ai-agent/README.md` are the authoritative runtime contract).

    ### Removed

    - `AiExposureRule` entity, `EntityExposurePolicy` SPI, `ExposureRuleListView` (D-10 — Jmix native security is authoritative).
    - ArchUnit rules (TEST-06, D-10) — replaced by code-review + targeted unit tests.

    ### Security

    - Removed dev Nexus credentials previously committed at `ai-agent/gradle.properties:3-4`. Credentials must now be provided via `-PnexusUsername` / `-PnexusPassword` (CI: GitHub Actions secrets; local: `gradle-local.properties` gitignored).
    - Negative-case integration suite (TEST-04) backstops cross-user data isolation, RAG role-filter fail-closed, and per-user schema filtering.

    [Unreleased]: https://github.com/<org>/ai-agent-core/compare/v1.0.0...HEAD
    [1.0.0]: https://github.com/<org>/ai-agent-core/releases/tag/v1.0.0
    ```

    **Backfill granularity (CONTEXT § Claude's Discretion):** per-phase entries (one bullet per phase under each Section). Per-plan would be too noisy for a v1 release.

    Replace `<org>` placeholder with the actual GitHub organization. If unknown at execution time, leave `<org>` as-is and document in SUMMARY for user fix.

    **Date:** Use today's date `2026-04-26` in the `[1.0.0]` line. The actual release date may be later — this is the v1.0.0 cut date.
  </action>
  <verify>
    <automated>test -f CHANGELOG.md && grep -c "^## \[1.0.0\]" CHANGELOG.md && grep -c "Keep a Changelog" CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - File `CHANGELOG.md` exists at repo root
    - `grep -c "^# Changelog" CHANGELOG.md` returns 1
    - `grep -c "Keep a Changelog" CHANGELOG.md` returns at least 1
    - `grep -c "^## \[Unreleased\]" CHANGELOG.md` returns 1
    - `grep -c "^## \[1.0.0\]" CHANGELOG.md` returns 1
    - `grep -c "^### Added" CHANGELOG.md` returns at least 1
    - `grep -c "^### Changed" CHANGELOG.md` returns at least 1
    - `grep -c "^### Removed" CHANGELOG.md` returns at least 1
    - `grep -c "^### Security" CHANGELOG.md` returns at least 1
    - `grep -c "Phase 1\|Phase 2\|Phase 3\|Phase 4\|Phase 5\|Phase 6\|Phase 7" CHANGELOG.md` returns at least 7 (one mention per phase)
    - `grep -c "Phase 7.2" CHANGELOG.md` returns at least 1 (audit redesign mention)
    - `grep -c "AiExposureRule\|EntityExposurePolicy" CHANGELOG.md` returns at least 1 (Removed section documents the drop)
    - `grep -i "JDK 21\|Java 21" CHANGELOG.md` returns at least 1 (W-01: JDK toolchain change documented)
  </acceptance_criteria>
  <done>CHANGELOG.md backfilled with all phase deliverables under appropriate Keep-a-Changelog sections.</done>
</task>

<task type="auto">
  <name>Task 3: Create three GitHub Actions workflow files</name>
  <files>.github/workflows/ai-agent-ci.yml, .github/workflows/ai-agent-live.yml, .github/workflows/ai-agent-publish.yml</files>
  <read_first>
    - ai-agent/ai-agent/ai-agent.gradle (confirm `liveTest` and `integrationTest` task names; lines 124-155)
    - .planning/phases/08-integration-hardening-release-readiness/08-RESEARCH.md (Example 6 — full YAML drafts)
    - settings.gradle (confirm Gradle path syntax `:ai-agent:ai-agent:test`)
  </read_first>
  <action>
    Create directory `.github/workflows/` if not present, then write all three files.

    **File A — `.github/workflows/ai-agent-ci.yml`** (verbatim from RESEARCH Example 6 with minor adjustments for the Phase 8 deliverables):

    ```yaml
    name: ai-agent CI

    on:
      pull_request:
        branches: [main]
      push:
        branches: [main]

    jobs:
      test:
        name: Unit + integration tests
        runs-on: ubuntu-latest
        services:
          postgres:
            image: pgvector/pgvector:pg16
            env:
              POSTGRES_USER: postgres
              POSTGRES_PASSWORD: postgres
              POSTGRES_DB: ai_agent
            ports: ['5432:5432']
            options: >-
              --health-cmd pg_isready
              --health-interval 10s
              --health-timeout 5s
              --health-retries 5

        steps:
          - uses: actions/checkout@v4
          - uses: actions/setup-java@v4
            with:
              java-version: '21'
              distribution: 'temurin'
          - uses: gradle/actions/setup-gradle@v4
          - name: Run unit + integration tests
            run: ./gradlew :ai-agent:ai-agent:test :ai-agent:ai-agent:integrationTest --no-daemon
            env:
              POSTGRES_URL: jdbc:postgresql://localhost:5432/ai_agent
              POSTGRES_USER: postgres
              POSTGRES_PASSWORD: postgres

      consumer-smoke:
        name: Clean-consumer smoke (TEST-07)
        runs-on: ubuntu-latest
        needs: test
        steps:
          - uses: actions/checkout@v4
          - uses: actions/setup-java@v4
            with:
              java-version: '21'
              distribution: 'temurin'
          - uses: gradle/actions/setup-gradle@v4
          - name: publishToMavenLocal + bootRunSmoke
            run: |
              ./gradlew :ai-agent:ai-agent:publishToMavenLocal \
                        :ai-agent:ai-agent-starter:publishToMavenLocal \
                        --no-daemon
              ./gradlew :consumer-smoke:bootRunSmoke -PaiAgentVersion=1.0.0 --no-daemon
    ```

    **File B — `.github/workflows/ai-agent-live.yml`**:

    ```yaml
    name: ai-agent Live Tier

    on:
      workflow_dispatch:

    jobs:
      liveTest:
        name: Live-tier semantic suite (golden questions)
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - uses: actions/setup-java@v4
            with:
              java-version: '21'
              distribution: 'temurin'
          - uses: gradle/actions/setup-gradle@v4
          - name: Run @Tag('live') tests
            run: ./gradlew :ai-agent:ai-agent:liveTest --no-daemon
            env:
              OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
    ```

    Note: the `if: ${{ secrets.OPENROUTER_API_KEY != '' }}` guard from RESEARCH Example 6 was removed because GitHub Actions does not allow direct secret references in `if` conditions; the @EnabledIfEnvironmentVariable in the test class is the gate. If the secret is unset, the env var is empty, and the test class skips — correct behavior.

    **File C — `.github/workflows/ai-agent-publish.yml`**:

    ```yaml
    name: ai-agent Publish to Nexus

    on:
      workflow_dispatch:
      push:
        tags: ['v*']

    jobs:
      publish:
        name: Publish ai-agent + ai-agent-starter to private Nexus
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - uses: actions/setup-java@v4
            with:
              java-version: '21'
              distribution: 'temurin'
          - uses: gradle/actions/setup-gradle@v4
          - name: Derive version from tag (if tag-triggered)
            id: derive_version
            run: |
              if [ "${{ github.ref_type }}" = "tag" ]; then
                # strip leading 'v' from tag (v1.0.0 -> 1.0.0)
                echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"
              else
                echo "version=1.0.0" >> "$GITHUB_OUTPUT"
              fi
          - name: Publish
            run: |
              ./gradlew publish --no-daemon \
                -Pversion=${{ steps.derive_version.outputs.version }} \
                -PnexusUsername=${{ secrets.NEXUS_USERNAME }} \
                -PnexusPassword=${{ secrets.NEXUS_PASSWORD }}
    ```

    **Note:** the `publish` task fans out to all subprojects' `javaMaven` publication via the `subprojects { ... }` block already in `ai-agent/build.gradle`. Both `ai-agent:ai-agent` and `ai-agent:ai-agent-starter` are published in one invocation.

    **CRITICAL:** the user MUST add three repository secrets in GitHub Actions BEFORE this workflow can succeed:
    - `OPENROUTER_API_KEY` — for ai-agent-live.yml
    - `NEXUS_USERNAME` — for ai-agent-publish.yml
    - `NEXUS_PASSWORD` — for ai-agent-publish.yml

    This is part of the Task 4 user-action checkpoint.
  </action>
  <verify>
    <automated>test -f .github/workflows/ai-agent-ci.yml && test -f .github/workflows/ai-agent-live.yml && test -f .github/workflows/ai-agent-publish.yml</automated>
  </verify>
  <acceptance_criteria>
    - All three workflow files exist
    - `grep -c "pull_request" .github/workflows/ai-agent-ci.yml` returns at least 1
    - `grep -c "workflow_dispatch" .github/workflows/ai-agent-live.yml` returns 1
    - `grep -c "workflow_dispatch" .github/workflows/ai-agent-publish.yml` returns 1
    - `grep -c "tags:" .github/workflows/ai-agent-publish.yml` returns 1 (tag-triggered path)
    - `grep -c "consumer-smoke:bootRunSmoke" .github/workflows/ai-agent-ci.yml` returns 1
    - `grep -c "OPENROUTER_API_KEY" .github/workflows/ai-agent-live.yml` returns 1
    - `grep -c "NEXUS_USERNAME" .github/workflows/ai-agent-publish.yml` returns at least 1
    - `grep -c "java-version: '21'" .github/workflows/ai-agent-*.yml` returns 3 (one per workflow)
    - `grep -c "actions/setup-gradle@v4" .github/workflows/ai-agent-*.yml` returns 3 (current action per RESEARCH State of the Art)
    - YAML files parse: `python -c "import yaml; yaml.safe_load(open('.github/workflows/ai-agent-ci.yml'))"` exits 0 (or use `yq` if available)
  </acceptance_criteria>
  <done>Three workflows committed; YAML parses; references match Phase 8 Gradle task names (test, integrationTest, liveTest, bootRunSmoke, publish).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: User-action — credential rotation, Nexus release-URL confirmation, GitHub secrets</name>
  <what-built>
    Tasks 1-3 wired the version bump, removed committed credentials, added .gitignore patterns, wrote CHANGELOG, and created three CI workflows.

    What CANNOT be automated by Claude (per CONTEXT canonical_refs + RESEARCH Pitfalls 3, 6):
  </what-built>
  <how-to-verify>
    Three concrete user actions are required BEFORE this plan can be marked done:

    **Action 1 — Rotate the previously-committed Nexus dev credentials.**
       - Old credentials at `ai-agent/gradle.properties:3-4` were `nexusUsername=admin / nexusPassword=admin123`. They are now removed from the repo, but they may already be in git history. Rotate the password at `https://nexus.x2h.com.vn/` admin UI (or whatever your Nexus instance's UI is) so the previously-committed value cannot be used by anyone reading the git history.
       - Acceptance: log in to Nexus admin UI, change the `admin` user's password to a fresh value. Confirm by attempting to log in with the old password — must fail.

    **Action 2 — Confirm the Nexus release-repository URL.**
       - The publishing block now uses a snapshot/release URL conditional. Snapshot URL `https://nexus.x2h.com.vn/repository/jmix-internal-snapshots/` is verified. Release URL is currently set to `https://nexus.x2h.com.vn/repository/jmix-internal-releases/` (Nexus naming convention; assumption per RESEARCH Pitfall 6).
       - Verify the actual release-repository URL exists at your Nexus instance. If it is named differently (e.g. `jmix-internal-release` singular, or `releases`), update `ai-agent/build.gradle`'s URL conditional.
       - Acceptance: confirm release-repo exists and is writable; URL literal in `ai-agent/build.gradle` matches the actual URL.

    **Action 3 — Add three GitHub Actions secrets.**
       - In the GitHub repository settings → Secrets and variables → Actions → New repository secret:
         - `OPENROUTER_API_KEY` — value: a valid OpenRouter API key with funded balance (used by `ai-agent-live.yml`).
         - `NEXUS_USERNAME` — value: rotated Nexus user from Action 1 (used by `ai-agent-publish.yml`).
         - `NEXUS_PASSWORD` — value: rotated Nexus password from Action 1.
       - Acceptance: all three secrets exist in repo settings (visible as `***` masked entries).

    **After all three actions complete:**
       - Reply with `approved` to mark this plan done.
       - If you also want to rotate at-rest history (BFG / `git filter-repo`), document that as a follow-up — the plan does not block on it because the credentials were dev-only and Action 1 invalidates them.
  </how-to-verify>
  <resume-signal>
    Reply `approved` when:
    1. Nexus admin password rotated
    2. Release URL confirmed (and updated in build.gradle if different)
    3. Three GitHub Actions secrets created (OPENROUTER_API_KEY, NEXUS_USERNAME, NEXUS_PASSWORD)

    OR reply `skip` to defer Action 3 (CI secrets) but acknowledge that `ai-agent-publish.yml` and `ai-agent-live.yml` will fail until secrets exist. Action 1 (credential rotation) and Action 2 (URL confirmation) are NOT skippable — the plan stays open until those complete.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo (public/internal git history) → Nexus | Committed credentials at `ai-agent/gradle.properties:3-4` exposed via git history |
| GitHub Actions runner → Nexus | Workflow injects credentials via `-P` properties from secrets |
| GitHub Actions runner → OpenRouter | Live test uses API key from secrets; not exposed in logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Information Disclosure | Committed Nexus credentials in git history | mitigate | Task 1 removes lines from `gradle.properties` + `.gitignore` blocks re-commit; Task 4 user-action rotates the password at Nexus admin UI (HIGH severity — committed plaintext secret is industry-flagged risk) |
| T-08-07 | Tampering | Nexus credential rotation | mitigate | Task 4 user-action: rotate at Nexus admin UI; old credential MUST fail authentication (HIGH severity) |
| T-08-08 | Information Disclosure | GitHub Actions workflow_dispatch secret handling | mitigate | Workflows use `${{ secrets.NAME }}` (masked in logs); never echo to stdout; CI runner is GitHub-hosted ephemeral VM (MEDIUM severity) |
| T-08-09 | Repudiation | Tag-triggered publish without manual approval gate | accept | `workflow_dispatch + push tag v*` requires either explicit dispatch or a tag push (which is a write-protected operation requiring `contents: write` permission). Acceptable for v1 (LOW severity); v2 may add manual approval gate via environments. |
</threat_model>

<verification>
- Version source is in `ai-agent/gradle.properties` (`version=1.0.0`); `ai-agent/build.gradle` line 12 reads `version = project.findProperty('version') ?: '1.0.0'`.
- `nexusUsername=admin` and `nexusPassword=admin123` no longer appear in `ai-agent/gradle.properties`.
- Snapshot/release URL conditional present in `ai-agent/build.gradle`.
- CHANGELOG.md exists at repo root with [1.0.0] backfilled section.
- `.github/workflows/ai-agent-ci.yml`, `ai-agent-live.yml`, `ai-agent-publish.yml` all parse as valid YAML.
- All workflow Gradle invocations match the actual task names verified against `ai-agent/ai-agent/ai-agent.gradle`.
- Task 4 checkpoint completed by user: credentials rotated, release URL confirmed, three GitHub secrets created.
</verification>

<success_criteria>
- D-16: version 1.0.0 (achieved via gradle.properties; build.gradle line 12 indirection).
- D-17: CHANGELOG.md at repo root, Keep-a-Changelog v1.1.0, with backfilled 1.0.0 section.
- D-18: maven-publish wired (existing block at lines 63-81 verified, NOT redeclared); URL is now version-conditional.
- D-19: three workflows present; `ai-agent-ci.yml` is PR-blocking; live + publish are workflow_dispatch.
- ROADMAP Phase 8 success criterion #1 (all integration tests green on CI non-live tier) is now CI-enforced via `ai-agent-ci.yml`.
- Committed-secret hazard remediated.
</success_criteria>

<output>
After completion, create `.planning/phases/08-integration-hardening-release-readiness/08-07-SUMMARY.md`
</output>
