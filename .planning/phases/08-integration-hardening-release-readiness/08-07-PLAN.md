---
phase: 08
plan: 07
type: execute
wave: 2
depends_on: [08-01, 08-02, 08-03, 08-04, 08-05, 08-06]
files_modified:
  - ai-agent/build.gradle
  - ai-agent/gradle.properties
  - .gitignore
  - CHANGELOG.md
  - .github/workflows/ai-agent-ci.yml
  - .github/workflows/ai-agent-live.yml
  - .github/workflows/ai-agent-publish.yml
autonomous: false
requirements:
  - TEST-07
must_haves:
  truths:
    - "ai-agent/build.gradle line 12 reads `version = project.findProperty('version') ?: '1.0.0'` (or equivalent expression)"
    - "ai-agent/gradle.properties contains `version=1.0.0` AND does NOT contain the lines `nexusUsername=admin` / `nexusPassword=admin123`. Edits to gradle.properties are SURGICAL (R-07d) — other project-specific settings preserved."
    - "Repo-root .gitignore contains a pattern excluding any local nexus-credentials file"
    - "CHANGELOG.md exists at repo root following Keep-a-Changelog v1.1.0 with a [Unreleased] discipline note (R-XP-1) AND a [1.0.0] section backfilled per phase, with explicit Phase 7 → 7.1 → 7.2 chronology (R-07g)"
    - "Three GitHub Actions workflows exist with explicit `permissions:` blocks and `concurrency:` (R-07f) and a preflight secrets-check on publish.yml (R-07f)"
    - "ai-agent-ci.yml uses dynamic `aiAgentVersion` (R-07b — read from gradle.properties or env, NOT hardcoded 1.0.0)"
    - "Snapshot-vs-release URL conditional is wired in ai-agent/build.gradle, with the [W-03 GATING] comment block intact for orchestrator visibility (R-07c)"
    - "Threat model + Task 4 explicitly call out that the historical Nexus credential is FOREVER BURNT (R-07a) — rotation invalidates future use but git history retains the plaintext"
  artifacts:
    - path: "ai-agent/build.gradle"
      provides: "version source moved to gradle.properties; snapshot-vs-release URL conditional added; [W-03 GATING] comment preserved (R-07c)"
      contains: "1.0.0"
    - path: "ai-agent/gradle.properties"
      provides: "version=1.0.0; nexus credentials lines REMOVED via SURGICAL edit (R-07d)"
      contains: "version=1.0.0"
    - path: ".gitignore"
      provides: "Pattern excluding local nexus-credentials files"
      contains: "nexus"
    - path: "CHANGELOG.md"
      provides: "Keep-a-Changelog v1.1.0 with [Unreleased] discipline note (R-XP-1) + backfilled [1.0.0] section ordered by phase (R-07g)"
      contains: "[1.0.0]"
    - path: ".github/workflows/ai-agent-ci.yml"
      provides: "PR-blocking workflow: test + integrationTest + publishToMavenLocal + bootRunSmoke; explicit permissions + concurrency (R-07f); dynamic aiAgentVersion (R-07b)"
      contains: "permissions:"
    - path: ".github/workflows/ai-agent-live.yml"
      provides: "workflow_dispatch live-tier workflow gated on OPENROUTER_API_KEY; explicit permissions (R-07f)"
      contains: "permissions:"
    - path: ".github/workflows/ai-agent-publish.yml"
      provides: "workflow_dispatch + tag-triggered publish to Nexus; preflight secrets check + permissions (R-07f)"
      contains: "permissions:"
  key_links:
    - from: "ai-agent/gradle.properties"
      to: "ai-agent/build.gradle line 12"
      via: "project.findProperty('version')"
      pattern: "findProperty.*version"
    - from: ".github/workflows/ai-agent-publish.yml"
      to: "ai-agent/build.gradle publishing block"
      via: "-PnexusUsername=${{ secrets.NEXUS_USERNAME }} -PnexusPassword=${{ secrets.NEXUS_PASSWORD }}"
      pattern: "nexusUsername"
    - from: ".github/workflows/ai-agent-ci.yml"
      to: "consumer-smoke project"
      via: "Gradle invocation `:consumer-smoke:bootRunSmoke -PaiAgentVersion=${{ env.VERSION }}` (R-07b dynamic)"
      pattern: "bootRunSmoke"
---

<objective>
Land release polish + CI per CONTEXT D-16/D-17/D-18/D-19: bump version to 1.0.0, remove committed Nexus credentials, write CHANGELOG.md, wire three GitHub Actions workflows.

**Critical adaptations encoded here (per planning_context):**

1. **`maven-publish` already wired at `ai-agent/build.gradle:63-81`** (RESEARCH Pitfall 5).
2. **Version is in `ai-agent/build.gradle` line 12, NOT `gradle.properties`** — this plan moves it.
3. **Committed Nexus credentials at `ai-agent/gradle.properties:3-4`** — MUST remove + SURGICAL gitignore + flag rotation.
4. **Snapshot URL `…/jmix-internal-snapshots/` rejects `1.0.0` release uploads** — conditional URL.
5. **Tests-first audit framing** — CHANGELOG entries reflect real merged work only.

**Review-incorporation markers (R-NN) applied per 08-REVIEWS.md:**
- R-07a (Claude HIGH): historical credential is FOREVER BURNT. Add explicit threat-model bullet AND Task 4 paragraph documenting permanence.
- R-07b (Claude MEDIUM): `aiAgentVersion` in ai-agent-ci.yml MUST be dynamic (read from gradle.properties or env), not hardcoded `1.0.0`.
- R-07c (OpenCode HIGH): preserve the `[W-03 GATING]` comment block when refactoring URL — orchestrator must see it.
- R-07d (OpenCode MEDIUM): SURGICAL `gradle.properties` edits — use Edit/grep-and-replace on the credential lines + version line; do NOT `Write` the entire file.
- R-07e (OpenCode MEDIUM): document the local-properties loader convention choice. Recommend user-global `~/.gradle/gradle.properties`; if `gradle-local.properties` is chosen, document the loader AND add to `.gitignore`.
- R-07f (OpenCode LOW): add explicit `permissions:` blocks to all three workflows; `concurrency:` on PR-blocking ci.yml; preflight secrets-check on publish.yml.
- R-07g (Claude MEDIUM): CHANGELOG ordering — Phase 7 → 7.1 → 7.2 chronological note or sub-grouping.
- R-XP-1 (Claude SUGGESTION): add `[Unreleased]` discipline note (~3-5 lines) explaining how future PRs add entries without bumping version.

This plan is `autonomous: false` because: (a) credential rotation requires Nexus admin UI; (b) release URL needs user confirmation; (c) GitHub Actions secrets need user setup.

Output: 7 files modified/created.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-integration-hardening-release-readiness/08-CONTEXT.md
@.planning/phases/08-integration-hardening-release-readiness/08-RESEARCH.md
@.planning/phases/08-integration-hardening-release-readiness/08-PATTERNS.md
@.planning/phases/08-integration-hardening-release-readiness/08-REVIEWS.md
@ai-agent/build.gradle
@ai-agent/gradle.properties
@ai-agent/ai-agent/ai-agent.gradle
@ai-agent/ai-agent-starter/ai-agent-starter.gradle
@settings.gradle
@docs/consumer-smoke.md
</context>

<tasks>

<task type="auto" autonomous="false">
  <name>Task 1: Version bump + version-source extraction (R-07c/d/e — surgical edits, W-03 gate preserved)</name>
  <files>ai-agent/build.gradle, ai-agent/gradle.properties, .gitignore</files>
  <read_first>
    - ai-agent/build.gradle (READ ENTIRE FILE — confirm line 12 is `version = '0.0.1-SNAPSHOT'`; confirm publishing block at lines 63-81 with snapshot URL at line 67)
    - ai-agent/gradle.properties (READ — confirm lines 3-4 are `nexusUsername=admin` / `nexusPassword=admin123` AND identify any OTHER project-specific settings that MUST be preserved per R-07d)
    - .gitignore (READ existing patterns; we add additively, not replace)
  </read_first>
  <action>
    **File A — `ai-agent/gradle.properties` (R-07d SURGICAL EDITS only):**

    R-07d CRITICAL: do NOT use `Write` to rewrite the entire file. Use line-precise Edit operations:

    1. Find the line `nexusUsername=admin` → DELETE the line.
    2. Find the line `nexusPassword=admin123` → DELETE the line.
    3. Insert (immediately after the existing `org.gradle.jvmargs=...` line) the new version block:
       ```properties

       # Phase 8 plan 08-07 (CONTEXT D-16) — version source.
       # CI overrides via -Pversion=1.0.0 on tag push.
       version=1.0.0

       # Phase 8 plan 08-07 (RESEARCH Pitfall 3 + R-07a):
       # nexusUsername / nexusPassword previously committed here have been REMOVED.
       # NOTE (R-07a): the committed plaintext is FOREVER BURNT in git history. Rotation at
       # the Nexus admin UI invalidates future use but `git log -p` still recovers the old
       # value. The credential MUST never be reused — not on this Nexus instance, not on
       # any other system.
       # R-07e: local development conventions:
       #   PREFERRED: user-global ~/.gradle/gradle.properties for cross-project secrets.
       #   ALTERNATIVE: ai-agent/gradle-local.properties (gitignored; loaded by the
       #     loader block in ai-agent/build.gradle).
       # CI: passes -PnexusUsername / -PnexusPassword from GitHub Actions secrets
       # (see .github/workflows/ai-agent-publish.yml).
       ```
    4. Verify all OTHER lines in gradle.properties are byte-identical to before. Do NOT modify any other setting.

    **File B — `ai-agent/build.gradle` (R-07c — preserve W-03 gate; surgical edits only):**

    1. Locate line 12: `version = '0.0.1-SNAPSHOT'`. Replace with:
       ```groovy
       version = project.findProperty('version') ?: '1.0.0'
       ```

    2. Locate the publishing block at lines 63-81. Find line 67: `url = 'https://nexus.x2h.com.vn/repository/jmix-internal-snapshots/'`.

    3. Replace with the snapshot-vs-release conditional + the [W-03 GATING] block (R-07c — orchestrator must read this before merging Task 1):

       ```groovy
       // [W-03 GATING] — DO NOT REMOVE THIS COMMENT BLOCK.
       // Phase 8 plan 08-07 (CONTEXT D-18 + RESEARCH Pitfall 6 + R-07c):
       // Snapshot-vs-release URL conditional. Snapshot URL accepts -SNAPSHOT versions;
       // release URL accepts non-SNAPSHOT versions (e.g. 1.0.0). The release URL literal
       // is ASSUMED based on Nexus naming convention; it MUST be human-confirmed via the
       // Task 4 (W-03) checkpoint before any 1.0.0 release publish. The orchestrator
       // gates Task 1 done-marking on Task 4 sign-off.
       url = version.toString().endsWith('SNAPSHOT')
               ? 'https://nexus.x2h.com.vn/repository/jmix-internal-snapshots/'
               : 'https://nexus.x2h.com.vn/repository/jmix-internal-releases/'
       ```

    4. Add the local-properties loader (R-07e — document the convention; recommend user-global but support per-project file). Place this block AFTER the `buildscript` block but BEFORE `subprojects` (or wherever existing top-level Groovy code lives):

       ```groovy
       // Phase 8 plan 08-07 (R-07e) — load local non-committed credentials if present.
       // Convention preference: user-global ~/.gradle/gradle.properties (auto-loaded by
       // Gradle for cross-project secrets). This loader is a fallback for developers who
       // prefer a per-project file `gradle-local.properties` (gitignored — see .gitignore).
       def localProps = file('gradle-local.properties')
       if (localProps.exists()) {
           Properties p = new Properties()
           localProps.withInputStream { p.load(it) }
           p.each { k, v -> ext."$k" = v }
       }
       ```

    DO NOT touch any other line in `ai-agent/build.gradle`.

    **File C — `.gitignore` (repo root) — additive:**

    Append:
    ```
    # Phase 8 plan 08-07 (R-07e) — local Nexus credentials.
    # Production credentials must NEVER be committed. CI uses GitHub Actions secrets
    # (-PnexusUsername / -PnexusPassword from secrets.NEXUS_USERNAME / NEXUS_PASSWORD).
    # Local development: place credentials in ai-agent/gradle-local.properties (auto-loaded
    # by the loader block in ai-agent/build.gradle), OR in user-global ~/.gradle/gradle.properties
    # (Gradle's idiomatic cross-project secrets path).
    ai-agent/gradle-local.properties
    *.gradle.local.properties
    ```
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:tasks --quiet --no-daemon | head -1 && grep -c "nexusUsername=admin" ai-agent/gradle.properties && grep -c "nexusPassword=admin123" ai-agent/gradle.properties</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "version=1.0.0" ai-agent/gradle.properties` returns 1
    - `grep -c "nexusUsername=admin" ai-agent/gradle.properties` returns 0 (line removed)
    - `grep -c "nexusPassword=admin123" ai-agent/gradle.properties` returns 0 (line removed)
    - **R-07d:** `git diff ai-agent/gradle.properties` shows ONLY the credential lines deleted + the new version block inserted. NO other setting modified.
    - `grep -c "findProperty('version')" ai-agent/build.gradle` returns 1
    - `grep -c "endsWith('SNAPSHOT')" ai-agent/build.gradle` returns 1
    - `grep -c "jmix-internal-releases" ai-agent/build.gradle` returns 1
    - **R-07c:** `grep -c "\\[W-03 GATING\\]" ai-agent/build.gradle` returns at least 1 (gate comment preserved for orchestrator)
    - **R-07e:** `grep -c "gradle-local.properties" .gitignore` returns at least 1
    - **R-07e:** `grep -c "gradle-local.properties" ai-agent/build.gradle` returns at least 1 (loader block present)
    - **R-07a:** `grep -c "FOREVER BURNT\\|never be reused\\|forever burnt" ai-agent/gradle.properties` returns at least 1 (warning comment)
    - `./gradlew :ai-agent:ai-agent:properties --quiet | grep "^version:"` returns `version: 1.0.0`
    - `./gradlew :ai-agent:ai-agent:tasks` exits 0
    - **W-03 gate:** Task 4 (human-action checkpoint) has been signed off by the user.
  </acceptance_criteria>
  <done>Version is 1.0.0; credentials removed via surgical edit (R-07d); snapshot/release conditional in place with W-03 gate comment intact (R-07c); local-properties loader documented (R-07e). **Done is blocked until Task 4 is `approved`.**</done>
</task>

<task type="auto">
  <name>Task 2: Create CHANGELOG.md with [Unreleased] discipline + Phase 7→7.1→7.2 ordering (R-07g + R-XP-1)</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - .planning/ROADMAP.md (READ — phase summaries are the source for backfill)
    - .planning/phases/01-walking-skeleton/*-SUMMARY.md (skim)
    - .planning/phases/02-foundations/*-SUMMARY.md (skim)
    - .planning/phases/03-metadata-runtime-six-tools/*-SUMMARY.md (skim)
    - .planning/phases/04-orchestration-core/*-SUMMARY.md (skim)
    - .planning/phases/05-rag-layer/*-SUMMARY.md (skim)
    - .planning/phases/06-parameters-guardrails/*-SUMMARY.md (skim)
    - .planning/phases/07-flow-ui/*-SUMMARY.md (skim)
    - .planning/phases/07.1-vaadin-message-list/*-SUMMARY.md (skim)
    - .planning/phases/07.2-redesign-audit-schema-tree-lite/*-SUMMARY.md (skim)
  </read_first>
  <action>
    Create new file at repo root: `CHANGELOG.md`.

    Per CONTEXT D-17 + Keep-a-Changelog v1.1.0 + R-07g chronological note + R-XP-1 [Unreleased] discipline.

    ```markdown
    # Changelog

    All notable changes to this project will be documented in this file.

    The format is based on [Keep a Changelog v1.1.0](https://keepachangelog.com/en/1.1.0/),
    and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

    ## [Unreleased]

    > **R-XP-1 — [Unreleased] discipline note.** Future PRs land changes here under
    > Added/Changed/Deprecated/Removed/Fixed/Security WITHOUT bumping the version. Version
    > bumps + cutting a new dated section happen ONLY at release time (manual step in
    > `ai-agent/gradle.properties` + a follow-up commit that moves `[Unreleased]` content
    > under the new dated heading and inserts a fresh empty `[Unreleased]` block).

    ## [1.0.0] - 2026-04-26

    > **R-07g — Phase chronology note.** Phase 7 shipped the Flow UI baseline; Phase 7.1
    > re-platformed ChatView onto Vaadin MessageList/MessageInput primitives; Phase 7.2
    > redesigned the audit schema (flat → tree-lite). The Changed section below is
    > sub-grouped by phase to make the upgrade trail readable: Phase 7 first, then 7.1
    > UI re-platform, then 7.2 audit redesign.

    ### Added

    - **Walking skeleton + packaging (Phase 1)**: 2-module shape (`ai-agent` + `ai-agent-starter`); Spring AI 1.1.4 BOM; OpenRouter ChatModel + JDBC chat memory; @Tag("live") test tier; clean-consumer smoke documented.
    - **Foundations (Phase 2)**: Five JPA entities (`AiConversation`, `AiMessage`, `AiAuditEvent` (originally `AiToolCallAudit`, renamed in 7.2), `AiParameters`, `AiKnowledgeDocument`); Liquibase changelogs with `AI_AGENT_*` prefix; pgvector extension table; Spring AI JDBC chat-memory schema; `AiAgentUserRole` / `AiAgentAdminRole` / `AiAgentUserRowLevelRole`; six SPI interfaces.
    - **Metadata runtime + six tools (Phase 3)**: `MetamodelScanner`; per-user effective schema via `CurrentUserSchemaAccess`; six `@Tool` methods; structured filter DSL; `<data>`-delimited safe formatter; hard row-count cap.
    - **Orchestration core (Phase 4)**: `ChatClientFactory` advisor chain; `JdbcChatMemoryRepository` + `ConversationProjector`; `AuditAdvisor` REQUIRES_NEW; `AuditListener` SPI fan-out.
    - **RAG layer (Phase 5)**: File-upload ingestion; pgvector storage with `allowedRoles` metadata; `QuestionAnswerAdvisor` per-request `FILTER_EXPRESSION`; fail-closed default; `CustomIngester` SPI.
    - **Parameters + guardrails (Phase 6)**: `AiParameters` profiles; `default-params.yaml`; per-conversation overrides; `ToolGuard` SPI; iteration cap; per-session token circuit breaker; per-user rate limiter; output-side injection scanner; structured output via `BeanOutputConverter`.
    - **Flow UI (Phase 7)**: `ChatView`, `ConversationListView` / `ConversationDetailView`, `ParametersListView` / `ParametersDetailView`, `KnowledgeBaseView`, `ToolCallAuditListView` (Excel + JSON export); bilingual EN+VI menu and labels; role-gated visibility.
    - **Operator README** (Phase 8): Quick start, env vars, configuration matrix, entity/table ownership, SPI cookbook, upgrade checklist, air-gap notes, troubleshooting (R-06b), verification footer (R-06a).
    - **CI workflows** (Phase 8): `.github/workflows/ai-agent-ci.yml` (PR-blocking), `ai-agent-live.yml` (workflow_dispatch), `ai-agent-publish.yml` (workflow_dispatch + tag publish to Nexus). All three carry explicit `permissions:` blocks (R-07f).
    - **Clean-consumer smoke** (Phase 8): `consumer-smoke/` Gradle subproject + `bootRunSmoke` task.
    - **TEST-04 negative-case suite** (Phase 8): `FilteredSchemaAndExecutionDenialTest` (R-01a/f), `RagRoleFilterNegativeTest` (R-01b), `CrossUserConversationAccessTest` (R-01c/e).
    - **TEST-05 live-tier golden suite** (Phase 8): seven capability-coverage questions in `golden-questions.yaml` (R-04b RAG positive + empty-KB split); `containsAnyOf` semantic anchors.
    - **TEST-02/03 extensions** (Phase 8): two poisoned tool-result fixtures in `PromptInjectionHarnessTest` (R-02a SYSTEM-prefix + delimiter tokens); decorator-routed tool-throw + outer-rollback case in `AuditDurabilityTest` (R-02b).
    - **Performance smoke** (Phase 8): `datasource-proxy`-based per-tool query-count baseline; N-scaling N+1 detector (R-03h); `find_records` limit-cap test (R-03b/f).

    ### Changed

    **Phase 7 — Flow UI baseline:**
    - ChatView, ConversationListView, ParametersListView, KnowledgeBaseView, ToolCallAuditListView landed on Jmix FlowUI.

    **Phase 7.1 — Vaadin MessageList re-platform (after Phase 7):**
    - **ChatView refactored to Vaadin MessageList/MessageInput**: custom component tree (`ChatPanelFragment` + `MessageBubbleComponent` + `ToolCallCardComponent` + `MarkdownRenderer`) replaced with stock Vaadin primitives.

    **Phase 7.2 — Audit schema redesign (after Phase 7.1):**
    - **Audit schema redesigned to tree-lite**: flat `AI_AGENT_TOOL_CALL_AUDIT` + `KIND` discriminator replaced by self-FK `PARENT_ID` tree. One root row per chat turn (`PARENT_ID = null`, `KIND = CHAT`); tool / retrieval events as children. PRE/POST dual rows collapsed into single mutable row with `startedAt` / `finishedAt`. Entity renamed `AiToolCallAudit` → `AiAuditEvent`.
    - **AuditListener SPI breaking change**: `dispatchToolCallAudited(UUID)` → `onEventAudited(UUID auditId, String kind)`. Host listeners must update.

    **Phase 1 → 2 — BOM upgrade:**
    - Spring AI BOM upgraded 1.0.2 → 1.1.4.

    **Phase 8 — JDK toolchain enforcement:**
    - **JDK 21 toolchain enforced**: `ai-agent/build.gradle` and `consumer-smoke/consumer-smoke.gradle` both pin `JavaLanguageVersion.of(21)`. Java 17 is NOT supported. The historical `CLAUDE.md` Java-17 reference was corrected to Java 21 in Plan 08-06 (R-06c).

    ### Removed

    - `AiExposureRule` entity, `EntityExposurePolicy` SPI, `ExposureRuleListView` (D-10 — Jmix native security is authoritative).
    - ArchUnit rules (TEST-06, D-10) — replaced by code-review + targeted unit tests.

    ### Security

    - **R-07a — historical credential considered FOREVER BURNT.** Removed dev Nexus credentials previously committed at `ai-agent/gradle.properties:3-4`. The credential lines have been deleted in 1.0.0, and the password rotated at the Nexus admin UI per Plan 08-07 Task 4. However, the historical plaintext remains recoverable from git history via `git log -p ai-agent/gradle.properties`. The credential MUST never be reused — not on this Nexus instance, not on any other system. (Optional follow-up: scrub git history with BFG / `git filter-repo` — destructive; requires force-push and team coordination.)
    - Credentials must now be provided via `-PnexusUsername` / `-PnexusPassword` (CI: GitHub Actions secrets; local: `~/.gradle/gradle.properties` user-global OR `ai-agent/gradle-local.properties` gitignored).
    - Negative-case integration suite (TEST-04) backstops cross-user data isolation, RAG role-filter fail-closed, and per-user schema filtering.

    [Unreleased]: https://github.com/<org>/ai-agent-core/compare/v1.0.0...HEAD
    [1.0.0]: https://github.com/<org>/ai-agent-core/releases/tag/v1.0.0
    ```

    Replace `<org>` with the actual GitHub org if known; otherwise document for user fix.
  </action>
  <verify>
    <automated>test -f CHANGELOG.md && grep -c "^## \[1.0.0\]" CHANGELOG.md && grep -c "Keep a Changelog" CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - File `CHANGELOG.md` exists at repo root
    - `grep -c "^# Changelog" CHANGELOG.md` returns 1
    - `grep -c "Keep a Changelog" CHANGELOG.md` returns at least 1
    - `grep -c "^## \[Unreleased\]" CHANGELOG.md` returns 1
    - `grep -c "^## \[1.0.0\]" CHANGELOG.md` returns 1
    - **R-XP-1:** `grep -c "discipline note\\|Future PRs land changes here\\|WITHOUT bumping the version" CHANGELOG.md` returns at least 1
    - **R-07g:** `grep -c "Phase chronology\\|Phase 7 first, then 7.1, then 7.2\\|Phase 7 first, then 7.1\\|after Phase 7" CHANGELOG.md` returns at least 1 (chronological note OR sub-grouping headings present)
    - **R-07g:** `grep -c "^\\*\\*Phase 7" CHANGELOG.md` returns at least 3 (sub-grouped by phase: 7, 7.1, 7.2)
    - `grep -c "^### Added" CHANGELOG.md` returns at least 1
    - `grep -c "^### Changed" CHANGELOG.md` returns at least 1
    - `grep -c "^### Removed" CHANGELOG.md` returns at least 1
    - `grep -c "^### Security" CHANGELOG.md` returns at least 1
    - `grep -c "Phase 1\\|Phase 2\\|Phase 3\\|Phase 4\\|Phase 5\\|Phase 6\\|Phase 7" CHANGELOG.md` returns at least 7
    - `grep -c "Phase 7.2" CHANGELOG.md` returns at least 1
    - `grep -c "AiExposureRule\\|EntityExposurePolicy" CHANGELOG.md` returns at least 1
    - `grep -i "JDK 21\\|Java 21" CHANGELOG.md` returns at least 1
    - **R-07a:** `grep -c "FOREVER BURNT\\|forever burnt\\|never be reused" CHANGELOG.md` returns at least 1 (Security section calls out permanence)
  </acceptance_criteria>
  <done>CHANGELOG.md backfilled per phase; [Unreleased] discipline (R-XP-1); chronological Phase 7→7.1→7.2 ordering (R-07g); historical credential burnt-warning (R-07a).</done>
</task>

<task type="auto">
  <name>Task 3: Create three GitHub Actions workflow files (R-07b dynamic version + R-07f permissions/concurrency/preflight)</name>
  <files>.github/workflows/ai-agent-ci.yml, .github/workflows/ai-agent-live.yml, .github/workflows/ai-agent-publish.yml</files>
  <read_first>
    - ai-agent/ai-agent/ai-agent.gradle (confirm `liveTest` and `integrationTest` task names)
    - .planning/phases/08-integration-hardening-release-readiness/08-RESEARCH.md (Example 6 — full YAML drafts)
    - settings.gradle (confirm Gradle path syntax `:ai-agent:ai-agent:test`)
    - ai-agent/gradle.properties (confirm `version=1.0.0` is in place after Task 1 — needed for R-07b dynamic read)
  </read_first>
  <action>
    Create directory `.github/workflows/` if not present, then write all three files.

    **File A — `.github/workflows/ai-agent-ci.yml` (R-07b dynamic + R-07f permissions/concurrency):**

    ```yaml
    name: ai-agent CI

    on:
      pull_request:
        branches: [main]
      push:
        branches: [main]

    # R-07f: explicit least-privilege permissions for PR-blocking workflow.
    permissions:
      contents: read

    # R-07f: cancel superseded PR runs to save CI time.
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
      cancel-in-progress: true

    jobs:
      test:
        name: Unit + integration tests
        runs-on: ubuntu-latest
        services:
          postgres:
            image: pgvector/pgvector:pg16
            env:
              POSTGRES_USER: postgres
              POSTGRES_PASSWORD: postgres
              POSTGRES_DB: ai_agent
            ports: ['5432:5432']
            options: >-
              --health-cmd pg_isready
              --health-interval 10s
              --health-timeout 5s
              --health-retries 5

        steps:
          - uses: actions/checkout@v4
          - uses: actions/setup-java@v4
            with:
              java-version: '21'
              distribution: 'temurin'
          - uses: gradle/actions/setup-gradle@v4
          - name: Run unit + integration tests
            run: ./gradlew :ai-agent:ai-agent:test :ai-agent:ai-agent:integrationTest --no-daemon
            env:
              POSTGRES_URL: jdbc:postgresql://localhost:5432/ai_agent
              POSTGRES_USER: postgres
              POSTGRES_PASSWORD: postgres

      consumer-smoke:
        name: Clean-consumer smoke (TEST-07)
        runs-on: ubuntu-latest
        needs: test
        steps:
          - uses: actions/checkout@v4
          - uses: actions/setup-java@v4
            with:
              java-version: '21'
              distribution: 'temurin'
          - uses: gradle/actions/setup-gradle@v4
          # R-07b: derive aiAgentVersion dynamically from gradle.properties — do NOT
          # hardcode 1.0.0. PRs against a 1.1.0-SNAPSHOT branch must work too.
          - name: Read aiAgentVersion from gradle.properties
            id: read_version
            run: echo "version=$(grep '^version=' ai-agent/gradle.properties | cut -d'=' -f2)" >> "$GITHUB_OUTPUT"
          - name: publishToMavenLocal + bootRunSmoke
            run: |
              ./gradlew :ai-agent:ai-agent:publishToMavenLocal \
                        :ai-agent:ai-agent-starter:publishToMavenLocal \
                        --no-daemon
              ./gradlew :consumer-smoke:bootRunSmoke -PaiAgentVersion=${{ steps.read_version.outputs.version }} --no-daemon
    ```

    **File B — `.github/workflows/ai-agent-live.yml` (R-07f permissions):**

    ```yaml
    name: ai-agent Live Tier

    on:
      workflow_dispatch:

    # R-07f: explicit least-privilege permissions.
    permissions:
      contents: read

    jobs:
      liveTest:
        name: Live-tier semantic suite (golden questions)
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - uses: actions/setup-java@v4
            with:
              java-version: '21'
              distribution: 'temurin'
          - uses: gradle/actions/setup-gradle@v4
          - name: Run @Tag('live') tests
            run: ./gradlew :ai-agent:ai-agent:liveTest --no-daemon
            env:
              OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
    ```

    **File C — `.github/workflows/ai-agent-publish.yml` (R-07f preflight + permissions):**

    ```yaml
    name: ai-agent Publish to Nexus

    on:
      workflow_dispatch:
      push:
        tags: ['v*']

    # R-07f: publish needs read for checkout and (potentially) write for packages metadata.
    permissions:
      contents: read
      packages: write

    jobs:
      publish:
        name: Publish ai-agent + ai-agent-starter to private Nexus
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          # R-07f: preflight — fail-fast with a clear message if required secrets are absent.
          - name: Preflight — verify required Nexus secrets
            run: |
              if [ -z "${{ secrets.NEXUS_USERNAME }}" ]; then
                echo "::error::NEXUS_USERNAME secret is missing. Add it under repo settings → Secrets and variables → Actions."
                exit 1
              fi
              if [ -z "${{ secrets.NEXUS_PASSWORD }}" ]; then
                echo "::error::NEXUS_PASSWORD secret is missing. Add it under repo settings → Secrets and variables → Actions."
                exit 1
              fi
          - uses: actions/setup-java@v4
            with:
              java-version: '21'
              distribution: 'temurin'
          - uses: gradle/actions/setup-gradle@v4
          - name: Derive version from tag (if tag-triggered)
            id: derive_version
            run: |
              if [ "${{ github.ref_type }}" = "tag" ]; then
                echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"
              else
                # R-07b: fall back to gradle.properties when not tag-triggered.
                echo "version=$(grep '^version=' ai-agent/gradle.properties | cut -d'=' -f2)" >> "$GITHUB_OUTPUT"
              fi
          - name: Publish
            run: |
              ./gradlew publish --no-daemon \
                -Pversion=${{ steps.derive_version.outputs.version }} \
                -PnexusUsername=${{ secrets.NEXUS_USERNAME }} \
                -PnexusPassword=${{ secrets.NEXUS_PASSWORD }}
    ```

    **CRITICAL:** the user MUST add three repository secrets in GitHub Actions BEFORE these workflows can succeed:
    - `OPENROUTER_API_KEY` — for ai-agent-live.yml
    - `NEXUS_USERNAME` — for ai-agent-publish.yml
    - `NEXUS_PASSWORD` — for ai-agent-publish.yml

    This is part of the Task 4 user-action checkpoint.
  </action>
  <verify>
    <automated>test -f .github/workflows/ai-agent-ci.yml && test -f .github/workflows/ai-agent-live.yml && test -f .github/workflows/ai-agent-publish.yml</automated>
  </verify>
  <acceptance_criteria>
    - All three workflow files exist
    - `grep -c "pull_request" .github/workflows/ai-agent-ci.yml` returns at least 1
    - `grep -c "workflow_dispatch" .github/workflows/ai-agent-live.yml` returns 1
    - `grep -c "workflow_dispatch" .github/workflows/ai-agent-publish.yml` returns 1
    - `grep -c "tags:" .github/workflows/ai-agent-publish.yml` returns 1
    - `grep -c "consumer-smoke:bootRunSmoke" .github/workflows/ai-agent-ci.yml` returns 1
    - `grep -c "OPENROUTER_API_KEY" .github/workflows/ai-agent-live.yml` returns 1
    - `grep -c "NEXUS_USERNAME" .github/workflows/ai-agent-publish.yml` returns at least 2 (preflight + publish step)
    - `grep -c "java-version: '21'" .github/workflows/ai-agent-*.yml` returns 3
    - `grep -c "actions/setup-gradle@v4" .github/workflows/ai-agent-*.yml` returns 3
    - **R-07b:** `grep -c "PaiAgentVersion=1.0.0" .github/workflows/ai-agent-ci.yml` returns 0 (NOT hardcoded)
    - **R-07b:** `grep -c "read_version.outputs.version\\|steps.read_version" .github/workflows/ai-agent-ci.yml` returns at least 1
    - **R-07f:** `grep -c "^permissions:" .github/workflows/ai-agent-ci.yml .github/workflows/ai-agent-live.yml .github/workflows/ai-agent-publish.yml` returns 3 (one per workflow)
    - **R-07f:** `grep -c "^concurrency:" .github/workflows/ai-agent-ci.yml` returns 1
    - **R-07f:** `grep -c "Preflight" .github/workflows/ai-agent-publish.yml` returns at least 1
    - YAML files parse: `python -c "import yaml; yaml.safe_load(open('.github/workflows/ai-agent-ci.yml'))"` exits 0
  </acceptance_criteria>
  <done>Three workflows committed; YAML parses; explicit permissions + concurrency + preflight (R-07f); dynamic aiAgentVersion (R-07b).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: User-action — credential rotation, Nexus release-URL confirmation, GitHub secrets, and acknowledgment of historical-credential burn (R-07a)</name>
  <what-built>
    Tasks 1-3 wired the version bump (surgical edits per R-07d), removed committed credentials, added .gitignore patterns + local-properties loader (R-07e), wrote CHANGELOG with Phase 7→7.1→7.2 ordering and [Unreleased] discipline (R-07g + R-XP-1), and created three CI workflows with explicit permissions / concurrency / preflight (R-07f) and dynamic aiAgentVersion (R-07b).

    What CANNOT be automated by Claude (per CONTEXT canonical_refs + RESEARCH Pitfalls 3, 6, R-07a):
  </what-built>
  <how-to-verify>
    Four concrete user actions are required BEFORE this plan can be marked done:

    **Action 1 — Rotate the previously-committed Nexus dev credentials.**
       - Old credentials at `ai-agent/gradle.properties:3-4` were `nexusUsername=admin / nexusPassword=admin123`. Removed in this plan, but:

       **R-07a — THE HISTORICAL CREDENTIAL IS FOREVER BURNT.**
       Anyone with read access to this repo can `git log -p ai-agent/gradle.properties` and recover the old plaintext password. Rotation invalidates future use, but the historical leak is PERMANENT. The credential MUST never be reused — not on this Nexus instance after rotation, not on any other system, not as a base for derived passwords. Treat the literal `admin123` as compromised forever.

       Acceptance: log in to Nexus admin UI at `https://nexus.x2h.com.vn/`, change the `admin` user's password to a fresh value (use a password manager; do NOT reuse a personal password). Confirm by attempting to log in with `admin123` — must fail.

    **Action 2 — Confirm the Nexus release-repository URL.**
       - The publishing block now uses a snapshot/release URL conditional. Snapshot URL `https://nexus.x2h.com.vn/repository/jmix-internal-snapshots/` is verified. Release URL is currently set to `https://nexus.x2h.com.vn/repository/jmix-internal-releases/` (Nexus naming convention; assumption per RESEARCH Pitfall 6).
       - Verify the actual release-repository URL exists at your Nexus instance. If named differently, update `ai-agent/build.gradle`'s URL conditional.
       - Acceptance: confirm release-repo exists and is writable; URL literal in `ai-agent/build.gradle` matches the actual URL.

    **Action 3 — Add three GitHub Actions secrets.**
       - In the GitHub repository settings → Secrets and variables → Actions → New repository secret:
         - `OPENROUTER_API_KEY` — value: a valid OpenRouter API key with funded balance.
         - `NEXUS_USERNAME` — value: rotated Nexus user from Action 1.
         - `NEXUS_PASSWORD` — value: rotated Nexus password from Action 1.
       - Acceptance: all three secrets exist in repo settings (visible as `***` masked entries). The publish workflow's preflight step (R-07f) will fail-fast on missing secrets — this provides early signal.

    **Action 4 (R-07a OPTIONAL but RECOMMENDED) — Decide on git-history scrubbing.**
       - The historical credential remains in git log forever unless you rewrite history with BFG / `git filter-repo`. This is destructive (requires force-push, breaks any forks/clones, requires team coordination).
       - Decision options:
         - (a) Accept the risk: rotation invalidates the credential's use on the Nexus instance, but document "credential considered burnt; never reuse" — already captured in CHANGELOG Security section per R-07a.
         - (b) Scrub history: run `bfg --replace-text passwords.txt` or `git filter-repo --replace-text passwords.txt`, force-push, and notify everyone with clones to re-clone.
       - This action is not strictly required to ship 1.0.0, but the decision MUST be made and documented in the SUMMARY.

    **After Actions 1, 2, 3 complete (Action 4 documented either way):**
       - Reply with `approved` to mark this plan done.
  </how-to-verify>
  <resume-signal>
    Reply `approved` when:
    1. Nexus admin password rotated (Action 1)
    2. Release URL confirmed and updated in build.gradle if different (Action 2)
    3. Three GitHub Actions secrets created (Action 3)
    4. Action 4 decision documented in SUMMARY (R-07a — accept burn vs scrub history)

    OR reply `skip` to defer Action 3 (CI secrets) but acknowledge that `ai-agent-publish.yml` and `ai-agent-live.yml` will fail until secrets exist. Actions 1, 2, and the Action 4 decision are NOT skippable.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo (public/internal git history) → Nexus | **R-07a — historical credentials at `ai-agent/gradle.properties:3-4` are FOREVER recoverable from git log; rotation invalidates use but does not erase the leak** |
| GitHub Actions runner → Nexus | Workflow injects credentials via `-P` properties from secrets |
| GitHub Actions runner → OpenRouter | Live test uses API key from secrets; not exposed in logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Information Disclosure | Committed Nexus credentials in git history | mitigate (with permanent residual per R-07a) | Task 1 removes lines from `gradle.properties` + `.gitignore` blocks re-commit; Task 4 user-action rotates the password at Nexus admin UI. **R-07a residual: historical plaintext is recoverable forever via `git log -p`. Optional Action 4 (BFG / git filter-repo) addresses this destructively. Without history scrub: credential MUST never be reused on any system.** (HIGH severity — committed plaintext secret is industry-flagged risk) |
| T-08-07 | Tampering | Nexus credential rotation | mitigate | Task 4 user-action: rotate at Nexus admin UI; old credential MUST fail authentication |
| T-08-08 | Information Disclosure | GitHub Actions workflow_dispatch secret handling | mitigate | Workflows use `${{ secrets.NAME }}` (masked in logs); never echo to stdout; CI runner is GitHub-hosted ephemeral VM. R-07f preflight catches missing secrets early (no half-published artifacts). |
| T-08-09 | Repudiation | Tag-triggered publish without manual approval gate | accept | `workflow_dispatch + push tag v*` requires either explicit dispatch or a tag push (which is a write-protected operation). Acceptable for v1; v2 may add manual approval gate via environments. |
| T-08-10 | Spoofing | Workflow runs without explicit permissions | mitigate | R-07f: all three workflows declare explicit `permissions:` blocks (least-privilege: read for ci/live; read+packages:write for publish). Reduces blast radius of a compromised action. |
</threat_model>

<verification>
- Version source is in `ai-agent/gradle.properties` (`version=1.0.0`); `ai-agent/build.gradle` line 12 reads `version = project.findProperty('version') ?: '1.0.0'`.
- `nexusUsername=admin` and `nexusPassword=admin123` no longer appear in `ai-agent/gradle.properties`.
- Snapshot/release URL conditional present in `ai-agent/build.gradle` with `[W-03 GATING]` comment intact (R-07c).
- `ai-agent/build.gradle` contains the local-properties loader block (R-07e).
- CHANGELOG.md exists at repo root with [Unreleased] discipline note (R-XP-1) + chronological Phase 7→7.1→7.2 ordering (R-07g) + R-07a burnt-warning under Security.
- All three workflows have explicit `permissions:` blocks (R-07f); ci.yml has `concurrency:`; publish.yml has preflight.
- ai-agent-ci.yml uses dynamic `aiAgentVersion` from gradle.properties (R-07b).
- All workflow Gradle invocations match the actual task names verified against `ai-agent/ai-agent/ai-agent.gradle`.
- Task 4 checkpoint completed by user: credentials rotated, release URL confirmed, three GitHub secrets created, Action 4 decision documented.
</verification>

<success_criteria>
- D-16: version 1.0.0 (achieved via gradle.properties; build.gradle line 12 indirection).
- D-17: CHANGELOG.md at repo root, Keep-a-Changelog v1.1.0, with backfilled 1.0.0 section + [Unreleased] discipline (R-XP-1) + Phase ordering (R-07g).
- D-18: maven-publish wired (existing block at lines 63-81 verified, NOT redeclared); URL is now version-conditional with W-03 gate comment preserved (R-07c).
- D-19: three workflows present with R-07f permissions + R-07b dynamic version + R-07f preflight.
- ROADMAP Phase 8 success criterion #1 is now CI-enforced.
- Committed-secret hazard remediated; historical-leak permanence acknowledged (R-07a).
</success_criteria>

<output>
After completion, create `.planning/phases/08-integration-hardening-release-readiness/08-07-SUMMARY.md`
</output>
