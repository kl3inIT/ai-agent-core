---
phase: 08
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-agent/ai-agent/ai-agent.gradle
  - ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditWriter.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/FoundationsBootSmokeTest.java
autonomous: true
gap_closure: true
gap_closure_target: "VERIFICATION.md Gap 1 — 3 RED tests in com.vn.agent.security; broadened scope per 08-08-INVESTIGATION.md"
supersedes: 08-08-PLAN.md@4955924
requirements:
  - TEST-04
  - R-XP-2-FIX-1
  - R-XP-2-FIX-2
  - R-XP-2-FIX-3
tags: [security, gap-closure, jmix-security-data, jmix-unconstrained-datamanager, dependency-fix, audit-writer-fix, TEST-04, replan]

must_haves:
  truths:
    - "carol's CurrentUserSchemaAccess.getReadableSchema() does NOT include ai_AiAuditEvent (RED-1 flips GREEN — R-XP-2-FIX-1)"
    - "carol's BuiltInDataTools.findRecords(\"ai_AiAuditEvent\", null, 10) returns the access_denied JSON envelope (RED-2 flips GREEN — R-XP-2-FIX-2)"
    - "bob's dataManager.load(AiConversation.class).all().list() does NOT include alice's row (RED-3 flips GREEN — R-XP-2-FIX-3)"
    - "All 12 tests in com.vn.agent.security.* are GREEN after fix (closure target)"
    - "AuditDurabilityTest.toolAuditRowSurvivesOuterRollback remains GREEN under jmix-security-data enforcement (regression bar)"
    - "AuditDurabilityTest.writeChatFinish_doesNotOrphanChildren_writtenInSeparateRequiresNew remains GREEN (regression bar)"
    - "AuditDurabilityTest.errorPathToolAuditAlsoSurvivesOuterRollback_viaDecorator remains GREEN (regression bar)"
    - "CrossUserConversationAccessTest.userB_replayingUserAConversation_throwsSameTypeAndCodeAsRandomUuid remains GREEN (regression bar)"
    - "FoundationsBootSmokeTest.all_five_entities_round_trip remains GREEN (regression bar — write path under runWithSystem)"
    - "PromptInjectionHarnessTest 5/5, ToolQueryCountBaselineTest 7/7, FindRecordsLimitCapTest 2/2 baselines preserved (no regression in 08-02 + 08-03 cross-cutting)"
  artifacts:
    - path: "ai-agent/ai-agent/ai-agent.gradle"
      provides: "io.jmix.security:jmix-security-data-starter as implementation dependency"
      contains: "io.jmix.security:jmix-security-data-starter"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditWriter.java"
      provides: "AuditWriter persists ai_AiAuditEvent rows via UnconstrainedDataManager so end-user roles do not need audit-entity CRUD grants"
      contains: "private final UnconstrainedDataManager dataManager"
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/FoundationsBootSmokeTest.java"
      provides: "all_five_entities_round_trip line 165 uses dataManager.unconstrained().save(audit) so the persistence sanity check passes under jmix-security-data enforcement"
      contains: "dataManager.unconstrained().save(audit)"
  key_links:
    - from: "ai-agent.gradle implementation 'io.jmix.security:jmix-security-data-starter'"
      to: "io.jmix.securitydata.impl.constraint.SecurityDataConstraintsRegistration"
      via: "Spring autoconfig (META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports → SecurityDataAutoConfiguration)"
      pattern: "registers CrudEntityConstraint + ReadEntityQueryConstraint into AccessConstraintsRegistry at @PostConstruct"
    - from: "AccessManager.applyRegisteredConstraints(CrudEntityContext)"
      to: "CrudEntityConstraint.applyTo(context)"
      via: "AccessConstraintsRegistry.getConstraints() now contains CrudEntityConstraint"
      pattern: "secureOperations.isEntityReadPermitted(metaClass, policyStore) → context.setReadDenied() if no ALLOW policy"
    - from: "AuditWriter.dataManager.save(AiAuditEvent)"
      to: "EclipseLink INSERT into ai_AiAuditEvent (regardless of caller role grants)"
      via: "UnconstrainedDataManager bypasses CrudEntityConstraint chain"
      pattern: "io.jmix.core.UnconstrainedDataManager — canonical Jmix pattern for system-internal persistence (per /jmix-framework/jmix-context7 data-manager.html)"
---

<objective>
Close VERIFICATION.md Gap 1 (3 RED tests in `com.vn.agent.security`) by landing three coordinated changes:

1. Add `io.jmix.security:jmix-security-data-starter` as `implementation` in `ai-agent/ai-agent/ai-agent.gradle` — registers `CrudEntityConstraint` + `ReadEntityQueryConstraint` into `AccessConstraintsRegistry` at `@PostConstruct`, flipping the 3 RED tests at the AccessManager + DataManager layers.
2. Switch `AuditWriter` from `DataManager` to `UnconstrainedDataManager` — system-internal audit persistence must succeed regardless of caller authentication. This is Jmix's canonical pattern for infrastructure writes (per `/jmix-framework/jmix-context7` `data-manager.html`); `runWithSystem` is NOT a substitute under jmix-security-data because the system user is still policy-gated.
3. Edit `FoundationsBootSmokeTest.java` line 165 to use `dataManager.unconstrained().save(audit)` for the AiAuditEvent persistence sanity check — the test fixture writes `AiAuditEvent` directly (not via AuditWriter) under `runWithSystem`, and the same architectural rule applies.

Purpose: VERIFICATION.md Gap 1 closure (R-XP-2 trigger payload). The 3 RED tests in `com.vn.agent.security.*` (FilteredSchemaAndExecutionDenialTest x2, CrossUserConversationAccessTest x1) are not test bugs — they expose a real production-runtime gap: the ai-agent module ships its own `AccessManager` calls and `DataManager.load(...).all().list()` paths but does NOT depend on the Jmix module that registers the corresponding `AccessConstraint` implementations. Without the dep, end-user CRUD policies + row-level policies are silently bypassed in any host that does not also pull `jmix-security-data-starter`. Adding the dep alone, however, breaks `AuditWriter` because end-user roles intentionally do not grant CREATE on `ai_AiAuditEvent` (audit log is tamper-evident system infrastructure). The architectural fix routes audit writes through `UnconstrainedDataManager`.

Output: 3 source-file edits; verification harness output proving 12/12 GREEN in `com.vn.agent.security.*`, AuditDurabilityTest 3/3 GREEN, FoundationsBootSmokeTest 5/5 GREEN, and zero regression in 08-02/08-03 baselines (29/29 total across the named regression classes).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-integration-hardening-release-readiness/08-CONTEXT.md
@.planning/phases/08-integration-hardening-release-readiness/08-VERIFICATION.md
@.planning/phases/08-integration-hardening-release-readiness/08-08-INVESTIGATION.md
@.planning/phases/08-integration-hardening-release-readiness/08-01-SUMMARY.md
@.planning/phases/08-integration-hardening-release-readiness/08-02-SUMMARY.md
@.planning/phases/08-integration-hardening-release-readiness/08-03-SUMMARY.md
@CLAUDE.md
@ai-agent/ai-agent/ai-agent.gradle
@ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditWriter.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/metadata/CurrentUserSchemaAccess.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/security/AiAgentUserRowLevelRole.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/tools/BuiltInDataTools.java
@ai-agent/ai-agent/src/test/java/com/vn/agent/FoundationsBootSmokeTest.java
@ai-agent/ai-agent/src/test/java/com/vn/agent/security/FilteredSchemaAndExecutionDenialTest.java
@ai-agent/ai-agent/src/test/java/com/vn/agent/security/CrossUserConversationAccessTest.java
@ai-agent/ai-agent/src/test/java/com/vn/agent/security/RagRoleFilterNegativeTest.java
@ai-agent/ai-agent/src/test/java/com/vn/agent/security/AdminViewAccessTest.java
@ai-agent/ai-agent/src/test/java/com/vn/agent/test_support/NoCustomerReadRoleConfiguration.java
@ai-agent/ai-agent/src/test/java/com/vn/agent/test_support/TestUsersConfiguration.java
@jmix-app/build.gradle

<root_cause_analysis>

## Why all three RED tests share one classpath root cause

### The Jmix authorization contract

Jmix 2.8 separates authorization into two layers:

1. **Decision layer** — `AccessManager.applyRegisteredConstraints(AccessContext)` walks `AccessConstraintsRegistry.getConstraints()` and invokes `constraint.applyTo(context)` for each registered constraint matching the context type. The decision is then read off the populated context (`crudEntityContext.isReadPermitted()`, etc.). If NO matching constraint is registered, the context's default state is preserved — and `CrudEntityContext` defaults to PERMIT-ALL.

2. **Registration layer** — Spring beans annotated with `@PostConstruct registerConstraints()` push individual constraints into the registry at startup. `jmix-security-2.8.0` ships `SecurityConstraintsRegistration` (registers `ExportImportEntityConstraint`, `SpecificConstraintImpl`, `EntityAttributeConstraint`). `jmix-security-data-2.8.1` ships `SecurityDataConstraintsRegistration` (registers `CrudEntityConstraint`, `ReadEntityQueryConstraint`, `InMemoryCrudEntityConstraint`, `LoadValuesConstraint`).

### Current ai-agent.gradle state (verified 2026-04-26)

```gradle
implementation 'io.jmix.core:jmix-core-starter'
implementation 'io.jmix.data:jmix-eclipselink-starter'
implementation 'io.jmix.security:jmix-security-starter'        // EXPORT/IMPORT, SPECIFIC, ATTRIBUTE only
implementation 'io.jmix.security:jmix-security-flowui-starter' // UiShowViewContext only
// MISSING: implementation 'io.jmix.security:jmix-security-data-starter'
```

Compare to `jmix-app/build.gradle:52`:

```gradle
implementation 'io.jmix.security:jmix-security-data-starter'  // jmix-app already ships this
```

### Why each RED test is RED

- **RED-1** `carol_filteredSchema_excludesDenied_andDeniedAttributes`: `CurrentUserSchemaAccess.canReadEntity` calls `accessManager.applyRegisteredConstraints(crudEntityContext)` then `crudEntityContext.isReadPermitted()`. Without `jmix-security-data-starter`, `CrudEntityConstraint` is NOT in the registry; the call is a no-op; `isReadPermitted()` returns `true` by default. Carol therefore sees ALL entities, including `ai_AiAuditEvent`.
- **RED-2** `carol_findRecordsDeniedEntity_returnsAccessDeniedJson`: `BuiltInDataTools.resolveReadableEntityOrThrow` calls `currentUserSchemaAccess.canReadEntity(metaClass)`. Same root cause as RED-1 — returns `true` for carol; the `access_denied` envelope is never thrown.
- **RED-3** `userB_listingConversations_doesNotIncludeUserA_agentStoreScoped`: `dataManager.load(AiConversation.class).all().list()` does NOT route through `CurrentUserSchemaAccess`. Row-level enforcement at the JPA layer is performed by `ReadEntityQueryConstraint` (also registered by `SecurityDataConstraintsRegistration`), which intercepts query transformation and appends the `@JpqlRowLevelPolicy where {E}.createdBy = :current_user_username` clause. Without the dep, the query runs unfiltered and bob sees alice's row.

### Why the bare dep change is insufficient — AuditWriter's constrained DataManager regresses audit-write paths

Per `08-08-INVESTIGATION.md`, the gsd-executor attempted the bare one-line gradle dep change in commit-tree state and observed:

| Test | Pre-08-08 | Post-bare-dep |
|---|---|---|
| RED-1 (carol filtered schema) | RED | **GREEN** ✓ |
| RED-2 (carol findRecords access_denied) | RED | **GREEN** ✓ |
| RED-3 (bob listing) | RED | **NEW FAIL** ✗ — alice setup `chatService.ask` throws AccessDenied at `AuditWriter.writeChatStart:95` before assertion runs |
| `CrossUserConversationAccessTest.userB_replayingUserAConversation_throwsSameTypeAndCodeAsRandomUuid` | GREEN | **REGRESSION** ✗ |
| `AuditDurabilityTest.toolAuditRowSurvivesOuterRollback` | GREEN | **REGRESSION** ✗ |
| `AuditDurabilityTest.writeChatFinish_doesNotOrphanChildren_writtenInSeparateRequiresNew` | GREEN | **REGRESSION** ✗ |
| `AuditDurabilityTest.errorPathToolAuditAlsoSurvivesOuterRollback_viaDecorator` | GREEN | **REGRESSION** ✗ |
| `FoundationsBootSmokeTest.all_five_entities_round_trip` | GREEN | **REGRESSION** ✗ — `AccessDenied: ai_AiConversation, action: create` under `runWithSystem` |

**Net of bare dep change: 2 RED→GREEN, 1 RED stays RED (cause shifted), 5 previously-GREEN tests regress.**

Root cause: `AuditWriter` writes `ai_AiAuditEvent` rows via the **constrained** `DataManager` under the calling user's authentication. Pre-fix, no `CrudEntityConstraint` was registered, so the entity-CRUD policy check was a silent no-op — any authenticated user could write audit rows. Post-fix, `CrudEntityConstraint` enforces `secureOperations.isEntityReadPermitted(metaClass, policyStore)` and end-user roles (`AiAgentUserRole`) intentionally do not grant CREATE on `ai_AiAuditEvent` (audit log is tamper-evident system infrastructure).

This is the exact concern documented at `FoundationsBootSmokeTest.java:198-209` (Phase 2 forensics) and listed in the original Plan 08-08 threat model entry T-08-08-03. `runWithSystem` is **not** a substitute fix — under jmix-security-data, the system user is still policy-gated.

### Architectural fix (validated against Jmix 2.8 docs)

Per Jmix Context7 docs (`/jmix-framework/jmix-context7` → `data-access/data-manager.html`):

> The `UnconstrainedDataManager` interface provides the same methods as `DataManager` but bypasses all security policy checks. It can be used to override security constraints when necessary in your application code.

Use case match: audit log writes are system-internal persistence that must succeed regardless of caller authentication. `UnconstrainedDataManager` declares this intent at the type level, is the canonical Jmix pattern (also recommended for Quartz jobs and unauthenticated contexts per the same doc), and avoids the role-grant brittleness of `runWithSystem`. Project memory `feedback_jmix_unconstrained_for_system_writes` confirms this is the project standard going forward.

### Why this is one cohesive plan

- **Single classpath fix** (gradle dep) registers BOTH `CrudEntityConstraint` (RED-1, RED-2) AND `ReadEntityQueryConstraint` (RED-3) via the same `SecurityDataConstraintsRegistration` bean.
- **Single architectural fix** (`AuditWriter` → `UnconstrainedDataManager`) prevents the regression cascade introduced by the dep change.
- **Single test-fixture parity edit** (`FoundationsBootSmokeTest:165` → `.unconstrained().save(audit)`) keeps the persistence sanity check honest under the new enforcement regime.

All three changes are coordinated; landing any subset would either leave gaps RED or break previously-green tests.

</root_cause_analysis>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Add jmix-security-data-starter to ai-agent.gradle (do NOT run tests yet)</name>
  <files>ai-agent/ai-agent/ai-agent.gradle</files>

  <read_first>
    - ai-agent/ai-agent/ai-agent.gradle (current dep block — locate the existing `implementation 'io.jmix.security:jmix-security-flowui-starter'` line at line 9)
    - jmix-app/build.gradle line 52 (canonical reference: `implementation 'io.jmix.security:jmix-security-data-starter'`)
    - .planning/phases/08-integration-hardening-release-readiness/08-08-INVESTIGATION.md (confirm the dep alone breaks AuditWriter — Tasks 2 + 3 are mandatory before running any tests)
  </read_first>

  <action>
    Add EXACTLY ONE LINE plus a comment block to `ai-agent/ai-agent/ai-agent.gradle`. Insert AFTER the existing `implementation 'io.jmix.security:jmix-security-flowui-starter'` line (currently line 9) and BEFORE the `implementation 'io.jmix.flowui:jmix-flowui-starter'` line. The block to insert is:

    ```
        // Plan 08-08 (gap-closure for VERIFICATION.md Gap 1 / R-XP-2-FIX-1/2/3):
        // SecurityDataConstraintsRegistration (in jmix-security-data) registers
        // CrudEntityConstraint + ReadEntityQueryConstraint into AccessConstraintsRegistry at
        // @PostConstruct. Without this starter, AccessManager.applyRegisteredConstraints(CrudEntityContext)
        // is a no-op (defaults to PERMIT-ALL) AND DataManager.load(...).all().list() does not apply
        // @JpqlRowLevelPolicy predicates. jmix-app already ships this dep at build.gradle:52 — this
        // dependency makes the ai-agent module's security posture self-contained for any consumer.
        // Co-fix: AuditWriter switches to UnconstrainedDataManager (Task 2) so end-user roles do NOT
        // need audit-entity CRUD grants. See 08-08-INVESTIGATION.md and project memory
        // feedback_jmix_unconstrained_for_system_writes for the architectural rationale.
        implementation 'io.jmix.security:jmix-security-data-starter'
    ```

    Match existing 4-space indentation. DO NOT add a version (Jmix BOM resolves it). DO NOT add to ai-agent-starter.gradle. DO NOT add as `testImplementation`.

    **DO NOT run tests in this task.** Tests will fail without Task 2's AuditWriter fix and Task 3's FoundationsBootSmokeTest edit. Acceptance is grep-based + compileJava only.
  </action>

  <verify>
    <automated>./gradlew :ai-agent:ai-agent:compileJava</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "io.jmix.security:jmix-security-data-starter" ai-agent/ai-agent/ai-agent.gradle` returns `1` (exactly one occurrence)
    - `grep -c "testImplementation.*jmix-security-data" ai-agent/ai-agent/ai-agent.gradle` returns `0` (production scope only)
    - The line `implementation 'io.jmix.security:jmix-security-data-starter'` appears between `jmix-security-flowui-starter` and `jmix-flowui-starter` in the dependencies block
    - `./gradlew :ai-agent:ai-agent:compileJava` exits 0
    - `git diff --name-only HEAD` shows ONLY `ai-agent/ai-agent/ai-agent.gradle` modified at end of Task 1
    - JetBrains MCP `get_file_problems("ai-agent/ai-agent/ai-agent.gradle", onlyErrors=false)` returns zero errors (or fallback `compileJava` PASS)
    - **No test execution attempted in Task 1** (test execution is gated to Task 4 after all source edits are complete)
  </acceptance_criteria>

  <done>
    Single-line dependency + comment block added to ai-agent.gradle; `compileJava` passes; no other files modified; no tests executed yet.
  </done>
</task>

<task type="auto">
  <name>Task 2: Switch AuditWriter from DataManager to UnconstrainedDataManager</name>
  <files>ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditWriter.java</files>

  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditWriter.java (the SUT — note line 8 `import io.jmix.core.DataManager;`, line 9 `import io.jmix.core.Metadata;`, line 60 field declaration `private final DataManager dataManager;`, line 64 constructor parameter `DataManager dataManager`)
    - ai-agent/ai-agent/src/test/java/com/vn/agent/audit/AuditDurabilityTest.java (the regression bar — 3 tests must remain GREEN after the switch)
    - .planning/phases/08-integration-hardening-release-readiness/08-08-INVESTIGATION.md (confirm UnconstrainedDataManager has the same fluent API surface — `save`, `load`, `loadValue` all work unchanged)
  </read_first>

  <action>
    Edit `ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditWriter.java` with these EXACT edits, in order:

    **Edit 1 — Imports (lines 8-9):**

    Replace:
    ```
    import io.jmix.core.DataManager;
    import io.jmix.core.Metadata;
    ```

    With:
    ```
    import io.jmix.core.Metadata;
    import io.jmix.core.UnconstrainedDataManager;
    ```

    (`UnconstrainedDataManager` lives in the same `io.jmix.core` package, alphabetically AFTER `Metadata`. Remove the now-unused `DataManager` import — all usages in this file are via the field and the field's declared type changes below. Keep all other imports unchanged.)

    **Edit 2 — Field Javadoc + declaration (line 60):**

    Replace:
    ```
        private final DataManager dataManager;
    ```

    With:
    ```
        /**
         * Unconstrained: audit-event writes are system infrastructure that must persist regardless
         * of caller authentication. End-user roles (e.g. AiAgentUserRole) intentionally do NOT grant
         * CREATE on ai_AiAuditEvent — the audit log is tamper-evident. Switching from DataManager
         * to UnconstrainedDataManager is the canonical Jmix pattern for system-internal persistence
         * (see /jmix-framework/jmix-context7 data-manager.html) and avoids the role-grant brittleness
         * of runWithSystem (which is itself policy-gated under jmix-security-data). See project memory
         * feedback_jmix_unconstrained_for_system_writes and 08-08-INVESTIGATION.md.
         */
        private final UnconstrainedDataManager dataManager;
    ```

    **Edit 3 — Constructor parameter type (line 64):**

    Replace:
    ```
        public AuditWriter(DataManager dataManager, Metadata metadata, AuditListenerDispatcher dispatcher) {
    ```

    With:
    ```
        public AuditWriter(UnconstrainedDataManager dataManager, Metadata metadata, AuditListenerDispatcher dispatcher) {
    ```

    Variable name stays `dataManager`. All call sites inside the class (`dataManager.save(...)`, `dataManager.load(...)`, `dataManager.loadValue(...)`) work unchanged because `UnconstrainedDataManager` exposes the same fluent API surface.

    **DO NOT** modify any other line in `AuditWriter.java`. DO NOT touch other files in this task.

    After the edits:

    1. Run `./gradlew :ai-agent:ai-agent:compileJava` to confirm zero compile errors.
    2. Run AuditDurabilityTest in isolation to confirm the audit pipeline still works (the audit-tx propagation contract is independent of constraint registration; this is the canary):
       ```bash
       ./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.audit.AuditDurabilityTest"
       ```
       Expected: 3 tests run, 3 pass, 0 fail.
    3. If JetBrains MCP available: `get_file_problems("ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditWriter.java", onlyErrors=false)` → zero errors.
  </action>

  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.audit.AuditDurabilityTest"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "private final UnconstrainedDataManager dataManager" ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditWriter.java` returns `1`
    - `grep -c "private final DataManager dataManager" ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditWriter.java` returns `0`
    - `grep -c "import io.jmix.core.UnconstrainedDataManager;" ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditWriter.java` returns `1`
    - `grep -c "import io.jmix.core.DataManager;" ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditWriter.java` returns `0`
    - `grep -c "public AuditWriter(UnconstrainedDataManager dataManager" ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditWriter.java` returns `1`
    - The Javadoc note is present on the field (grep `feedback_jmix_unconstrained_for_system_writes` returns `1` match in this file)
    - `./gradlew :ai-agent:ai-agent:compileJava` exits 0 (zero compile errors)
    - `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.audit.AuditDurabilityTest"` reports 3 tests run, 3 pass, 0 fail (audit pipeline regression canary)
    - `git diff --name-only HEAD` shows exactly `ai-agent/ai-agent/ai-agent.gradle` (Task 1) + `ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditWriter.java` (Task 2)
    - JetBrains MCP `get_file_problems("ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditWriter.java", onlyErrors=false)` reports zero errors (or fallback `compileJava` PASS)
  </acceptance_criteria>

  <done>
    AuditWriter field + constructor + import switched to UnconstrainedDataManager; field has the rationale Javadoc; compile passes; AuditDurabilityTest 3/3 GREEN under jmix-security-data enforcement; only the gradle file (Task 1) and AuditWriter.java are modified.
  </done>
</task>

<task type="auto">
  <name>Task 3: Edit FoundationsBootSmokeTest line 165 to use unconstrained save for AiAuditEvent</name>
  <files>ai-agent/ai-agent/src/test/java/com/vn/agent/FoundationsBootSmokeTest.java</files>

  <read_first>
    - ai-agent/ai-agent/src/test/java/com/vn/agent/FoundationsBootSmokeTest.java lines 128-195 (the `all_five_entities_round_trip` method — note line 165 `AiAuditEvent savedAudit = dataManager.save(audit);`)
    - ai-agent/ai-agent/src/test/java/com/vn/agent/FoundationsBootSmokeTest.java lines 198-209 (the Phase 2 forensics comment about AccessDenied under runWithSystem — context for the edit)
    - .planning/phases/08-integration-hardening-release-readiness/08-08-INVESTIGATION.md "Test code" section (confirm: only line 165 is edited; the other 4 entities round-trip correctly under runWithSystem because their roles grant CRUD)
  </read_first>

  <action>
    Edit `ai-agent/ai-agent/src/test/java/com/vn/agent/FoundationsBootSmokeTest.java` with this EXACT single-line edit:

    **Edit — Line 165:**

    Replace:
    ```
                AiAuditEvent savedAudit = dataManager.save(audit);
    ```

    With:
    ```
                // jmix-security-data: system user is still policy-gated; use unconstrained DataManager
                // for the audit-row sanity check (see feedback_jmix_unconstrained_for_system_writes).
                AiAuditEvent savedAudit = dataManager.unconstrained().save(audit);
    ```

    DO NOT modify anything else in this test file. The other 4 entities (AiConversation+AiMessage at line 146, AiParameters at line 176, AiKnowledgeDocument at line 188) round-trip correctly under `runWithSystem` because their role configuration grants the system user CREATE/READ on those entities; only `AiAuditEvent` specifically lacks a system grant by intent.

    DO NOT change the surrounding `runWithSystem` block. DO NOT add new `@Disabled`. DO NOT modify `Stores` partitioning or persistence-unit configuration.

    After the edit:

    1. `./gradlew :ai-agent:ai-agent:compileTestJava` to confirm zero test-compile errors.
    2. Run FoundationsBootSmokeTest in isolation:
       ```bash
       ./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.FoundationsBootSmokeTest"
       ```
       Expected: 5 tests run, 5 pass, 0 fail. **Particular attention** to `all_five_entities_round_trip` (the line-165 path) and `row_level_policy_restricts_conversation_visibility` (proves the row-level role registration is intact under the new constraint regime).
    3. If JetBrains MCP available: `get_file_problems("ai-agent/ai-agent/src/test/java/com/vn/agent/FoundationsBootSmokeTest.java", onlyErrors=false)` → zero errors.

    If `all_five_entities_round_trip` still fails after this edit (e.g. EclipseLink "not a known Entity type" — the Phase 2 forensics concern), DO NOT mask with `@Disabled`. Capture the stack and surface as a deviation in Task 4 SUMMARY; do NOT proceed to Task 4 commit until the test is green.
  </action>

  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.FoundationsBootSmokeTest"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "dataManager.unconstrained().save(audit)" ai-agent/ai-agent/src/test/java/com/vn/agent/FoundationsBootSmokeTest.java` returns `1`
    - `grep -c "AiAuditEvent savedAudit = dataManager.save(audit);" ai-agent/ai-agent/src/test/java/com/vn/agent/FoundationsBootSmokeTest.java` returns `0`
    - `grep -c "feedback_jmix_unconstrained_for_system_writes" ai-agent/ai-agent/src/test/java/com/vn/agent/FoundationsBootSmokeTest.java` returns `1` (the rationale comment is present immediately above line 165)
    - `grep -c "@Disabled" ai-agent/ai-agent/src/test/java/com/vn/agent/FoundationsBootSmokeTest.java` returns its prior value (must NOT introduce any new `@Disabled` to mask test failures)
    - `./gradlew :ai-agent:ai-agent:compileTestJava` exits 0
    - `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.FoundationsBootSmokeTest"` reports 5 tests run, 5 pass, 0 fail
    - `git diff --name-only HEAD` shows exactly the 3 files in `files_modified`: `ai-agent/ai-agent/ai-agent.gradle` (Task 1), `ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditWriter.java` (Task 2), `ai-agent/ai-agent/src/test/java/com/vn/agent/FoundationsBootSmokeTest.java` (Task 3)
    - The 3 RED test files (`FilteredSchemaAndExecutionDenialTest.java`, `CrossUserConversationAccessTest.java`, `RagRoleFilterNegativeTest.java`) MUST NOT appear in the git diff — they are pinned contracts
    - `NoCustomerReadRoleConfiguration.java`, `TestUsersConfiguration.java`, `AiAgentUserRowLevelRole.java`, `CurrentUserSchemaAccess.java`, `BuiltInDataTools.java` MUST NOT appear in the git diff — pinned per user constraint
    - JetBrains MCP `get_file_problems` reports zero errors (or fallback `compileTestJava` PASS)
  </acceptance_criteria>

  <done>
    Line 165 of FoundationsBootSmokeTest uses `dataManager.unconstrained().save(audit)` with rationale comment; 5/5 GREEN; no other test files modified; pinned files untouched.
  </done>
</task>

<task type="auto">
  <name>Task 4: Full regression broom + 2 atomic commits + 08-08-SUMMARY.md</name>
  <files>
    .planning/phases/08-integration-hardening-release-readiness/08-08-SUMMARY.md
  </files>

  <read_first>
    - .planning/phases/08-integration-hardening-release-readiness/08-01-SUMMARY.md (frontmatter shape — phase, plan, subsystem, tags, requires, provides, affects, tech-stack, patterns, key-files, decisions, metrics)
    - .planning/phases/08-integration-hardening-release-readiness/08-02-SUMMARY.md (more SUMMARY structural reference — Outcome / Tasks Executed / Verification Results sections)
    - $HOME/.claude/get-shit-done/templates/summary.md (canonical SUMMARY template)
    - .planning/phases/08-integration-hardening-release-readiness/08-VERIFICATION.md "Gaps / Findings" → Gap 1 (the closure target)
    - .planning/phases/08-integration-hardening-release-readiness/08-08-INVESTIGATION.md (the architectural rationale this SUMMARY references)
    - ai-agent/ai-agent/ai-agent.gradle (final state — to record the exact added line)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditWriter.java (final state — to record the field type change)
  </read_first>

  <action>
    Three sub-actions, in order:

    **(a) Final broad regression run** — execute the full ai-agent functional-module test suite plus the named regression-bar classes:

    ```bash
    ./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.security.*" --tests "com.vn.agent.tools.PromptInjectionHarnessTest" --tests "com.vn.agent.audit.AuditDurabilityTest" --tests "com.vn.agent.performance.ToolQueryCountBaselineTest" --tests "com.vn.agent.performance.FindRecordsLimitCapTest" --tests "com.vn.agent.FoundationsBootSmokeTest"
    ```

    Expected per class:
    - `com.vn.agent.security.AdminViewAccessTest`: 5/5
    - `com.vn.agent.security.RagRoleFilterNegativeTest`: 2/2
    - `com.vn.agent.security.CrossUserConversationAccessTest`: 2/2 (incl. RED-3 GREEN + the previously-GREEN replay test still GREEN)
    - `com.vn.agent.security.FilteredSchemaAndExecutionDenialTest`: 3/3 (incl. RED-1, RED-2 now GREEN + dave's attribute-denial still GREEN)
    - **Total `com.vn.agent.security.*` ≡ 12/12** (closure-target gate)
    - `com.vn.agent.tools.PromptInjectionHarnessTest`: 5/5
    - `com.vn.agent.audit.AuditDurabilityTest`: 3/3
    - `com.vn.agent.performance.ToolQueryCountBaselineTest`: 7/7
    - `com.vn.agent.performance.FindRecordsLimitCapTest`: 2/2
    - `com.vn.agent.FoundationsBootSmokeTest`: 5/5
    - **Total ≡ 29/29**

    Then run the broad broom:
    ```bash
    ./gradlew :ai-agent:ai-agent:test
    ```
    Expected: BUILD SUCCESSFUL with all non-tagged tests green.

    **Stale-artifact gotcha (per 08-VERIFICATION.md):** if multi-class run fails on entity-enhancer mismatch, run `./gradlew :ai-agent:ai-agent:clean` then re-run; document the clean step in SUMMARY.

    If ANY test fails: capture stack, surface in SUMMARY "Deviations from Plan", and HALT — do not proceed to commit until green.

    **(b) Commit (a) — source changes** — single atomic commit staging ONLY the 3 files in `files_modified` (no STATE.md, no ROADMAP.md, no SUMMARY.md, no INVESTIGATION.md):

    ```bash
    git add ai-agent/ai-agent/ai-agent.gradle ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditWriter.java ai-agent/ai-agent/src/test/java/com/vn/agent/FoundationsBootSmokeTest.java
    ```

    Commit message via HEREDOC (per CLAUDE.md commit conventions):

    ```
    fix(security): wire jmix-security-data-starter; switch AuditWriter to UnconstrainedDataManager; fix FoundationsBootSmokeTest persistence assertion

    Closes the 3 RED tests from 08-01-SUMMARY.md "Out-of-Scope Issues
    Discovered" by adding the missing classpath dep that registers
    CrudEntityConstraint + ReadEntityQueryConstraint into
    AccessConstraintsRegistry, and routes system-internal audit
    writes through UnconstrainedDataManager so end-user roles do
    not need audit-entity CRUD grants.

    - ai-agent.gradle: + io.jmix.security:jmix-security-data-starter
    - AuditWriter.java: DataManager -> UnconstrainedDataManager
    - FoundationsBootSmokeTest.java:165: dataManager.save(audit)
      -> dataManager.unconstrained().save(audit)

    Closes Phase 8 VERIFICATION.md Gap 1 (TEST-04 negative-case suite
    SUT fix). Architectural rationale: see 08-08-INVESTIGATION.md and
    project memory feedback_jmix_unconstrained_for_system_writes.
    ```

    **(c) Write 08-08-SUMMARY.md, then commit (b) — docs only**

    Stage ONLY the SUMMARY:
    ```bash
    git add .planning/phases/08-integration-hardening-release-readiness/08-08-SUMMARY.md
    git commit -m "docs(08-08): SUMMARY for jmix-security-data-starter + AuditWriter unconstrained gap closure"
    ```

    SUMMARY.md required content (mirror 08-01-SUMMARY.md / 08-02-SUMMARY.md shape):

    Frontmatter:
    - `phase: 08`
    - `plan: 08`
    - `subsystem: security`
    - `tags: [security, gap-closure, jmix-security-data, jmix-unconstrained-datamanager, dependency-fix, audit-writer-fix, TEST-04, R-XP-2, replan]`
    - `gap_closure: true`
    - `gap_closure_target: VERIFICATION.md Gap 1`
    - `supersedes: 08-08-PLAN.md@4955924`
    - `requires:` `[com.vn.agent.metadata.CurrentUserSchemaAccess#canReadEntity, com.vn.agent.tools.BuiltInDataTools#findRecords, com.vn.agent.security.AiAgentUserRowLevelRole, com.vn.agent.audit.AuditWriter, io.jmix.securitydata.impl.constraint.SecurityDataConstraintsRegistration, io.jmix.core.UnconstrainedDataManager]`
    - `provides:` `[12/12 GREEN in com.vn.agent.security.*, AuditWriter on UnconstrainedDataManager, io.jmix.security:jmix-security-data-starter as production dep]`
    - `affects:` `[ai-agent/ai-agent/ai-agent.gradle, ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditWriter.java, ai-agent/ai-agent/src/test/java/com/vn/agent/FoundationsBootSmokeTest.java]`
    - `key-files: modified: [<3 paths>]` (created: [])
    - `metrics: tasks_completed: 4, tasks_total: 4, files_modified: 3, tests_red_to_green: 3, tests_green_baseline_preserved: 26, regression_count: 0, completed: <date>`

    Body sections (mirror 08-02-SUMMARY.md shape):

    - **Outcome** — table: Before / After (RED→GREEN counts; 12/12 security suite total; AuditDurabilityTest 3/3 preserved; FoundationsBootSmokeTest 5/5 preserved)
    - **Tasks Executed** — 4-row table with Task / Name / Commit hash / Files
    - **Verification Results** — paste the actual gradle output line (`tests=N skipped=0 failures=0 errors=0`) for each command run in sub-action (a)
    - **Root Cause Analysis** — distillation: which class registered (`SecurityDataConstraintsRegistration`), why bare dep regresses AuditWriter (constrained DataManager + end-user role lacks audit CRUD), why `UnconstrainedDataManager` is the canonical fix (Jmix Context7 docs `/jmix-framework/jmix-context7` `data-manager.html`)
    - **R-XP-2 Coverage Map** — table mapping R-XP-2-FIX-1/2/3 to specific assertion + how satisfied
    - **Deviations from Plan** — record any actual surprises encountered (clean step needed? unexpected failure?). If none: "None — three coordinated edits matched the architectural plan exactly."
    - **Authentication Gates** — N/A (no external auth)
    - **Threat Flags** — T-08-08-03 transitions from `mitigate (planned)` → `mitigated and tested under realistic user authentication`. T-08-08-06 (new): Tampering — audit log write path bypassed by adversarial code path. Disposition: accept. Mitigation: UnconstrainedDataManager is intentionally scoped to AuditWriter only; all other write paths in ai-agent module remain on constrained DataManager and continue to enforce CrudEntityConstraint.
    - **Self-Check** — checklist with all artifact paths + commit hashes (use the actual hashes from sub-actions b and c)
    - **TDD Gate Compliance** — N/A (gap-closure on existing pinned tests; plan type is `execute`)
    - **Supersession traceability** — explicit pointer: "Supersedes original 08-08-PLAN.md@4955924 (one-line gradle dep change). Original plan's `<notes>` section 'Out-of-band escalation paths' correctly anticipated this scenario; the architectural escalation became the primary scope after gsd-executor surfaced the AuditWriter regression. See 08-08-INVESTIGATION.md."
  </action>

  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.security.*" --tests "com.vn.agent.tools.PromptInjectionHarnessTest" --tests "com.vn.agent.audit.AuditDurabilityTest" --tests "com.vn.agent.performance.ToolQueryCountBaselineTest" --tests "com.vn.agent.performance.FindRecordsLimitCapTest" --tests "com.vn.agent.FoundationsBootSmokeTest"</automated>
  </verify>

  <acceptance_criteria>
    - Targeted regression run: 12/12 in `com.vn.agent.security.*` GREEN; PromptInjectionHarnessTest 5/5 GREEN; AuditDurabilityTest 3/3 GREEN; ToolQueryCountBaselineTest 7/7 GREEN; FindRecordsLimitCapTest 2/2 GREEN; FoundationsBootSmokeTest 5/5 GREEN — total 29/29
    - Broad broom `./gradlew :ai-agent:ai-agent:test` reports BUILD SUCCESSFUL with no failures (all non-tagged tests green)
    - `git log --oneline -2` shows TWO new commits: (a) `fix(security): wire jmix-security-data-starter; switch AuditWriter to UnconstrainedDataManager; fix FoundationsBootSmokeTest persistence assertion` (b) `docs(08-08): SUMMARY for jmix-security-data-starter + AuditWriter unconstrained gap closure`
    - `git show --stat HEAD~1 --name-only` lists exactly 3 files: `ai-agent/ai-agent/ai-agent.gradle`, `ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditWriter.java`, `ai-agent/ai-agent/src/test/java/com/vn/agent/FoundationsBootSmokeTest.java`
    - `git show --stat HEAD --name-only` lists exactly 1 file: `.planning/phases/08-integration-hardening-release-readiness/08-08-SUMMARY.md`
    - `.planning/phases/08-integration-hardening-release-readiness/08-08-SUMMARY.md` exists with frontmatter keys `gap_closure: true`, `gap_closure_target: VERIFICATION.md Gap 1`, `supersedes: 08-08-PLAN.md@4955924`, `tests_red_to_green: 3`, `files_modified: 3`
    - SUMMARY body contains: (a) the exact added gradle dep line; (b) the AuditWriter field type change; (c) the FoundationsBootSmokeTest line-165 edit; (d) gradle test-run output snippet for each regression class; (e) reference to `SecurityDataConstraintsRegistration` and `UnconstrainedDataManager` as the architectural anchors; (f) explicit confirmation that the 3 RED test files + `NoCustomerReadRoleConfiguration.java` + `TestUsersConfiguration.java` + `AiAgentUserRowLevelRole.java` + `CurrentUserSchemaAccess.java` + `BuiltInDataTools.java` were NOT modified; (g) supersession pointer to 08-08-PLAN.md@4955924 and 08-08-INVESTIGATION.md
    - SUMMARY ≥ 80 lines (substantive, not stub)
    - JetBrains MCP `get_file_problems` reports zero errors on all 3 modified source files (or fallback `compileJava`+`compileTestJava` PASS)
  </acceptance_criteria>

  <done>
    Targeted + broad regression runs both BUILD SUCCESSFUL with 29/29 in named classes and zero failures elsewhere; two clean commits in git log (3 source files in commit (a), SUMMARY in commit (b)); 08-08-SUMMARY.md exists with all required sections + frontmatter + supersession traceability + 29/29 evidence; VERIFICATION.md Gap 1 closure traceability established.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dependency-classpath → AccessConstraintsRegistry | Adding a new starter changes the runtime constraint set; Spring autoconfig discovery is the trust boundary |
| AuthenticationPolicyStore → CurrentAuthentication | Roles read from the current authentication context; SystemAuthenticator.withUser provides isolated test contexts |
| AuditWriter (UnconstrainedDataManager) → ai_AiAuditEvent table | UnconstrainedDataManager bypasses CrudEntityConstraint; trust boundary is the AuditWriter class itself (system-internal scope) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08-01 | Elevation of Privilege | AccessManager.applyRegisteredConstraints(CrudEntityContext) | mitigate | Adding jmix-security-data-starter registers CrudEntityConstraint, which translates @EntityPolicy annotations into setReadDenied(); without this, ai-agent ships a deny-by-default contract that silently permits — Task 1 IS the mitigation |
| T-08-08-02 | Information Disclosure | DataManager.load(AiConversation.class).all().list() | mitigate | ReadEntityQueryConstraint (registered by the new dep) appends the @JpqlRowLevelPolicy where clause `{E}.createdBy = :current_user_username` so cross-user reads are filtered at the SQL layer |
| T-08-08-03 | Tampering (regression) | AuditWriter constrained DataManager under user auth + Phase 2 EclipseLink HSQLDB metadata wiring | mitigated and tested under realistic user authentication | Task 2 switches AuditWriter to UnconstrainedDataManager (canonical Jmix pattern per /jmix-framework/jmix-context7 data-manager.html); Task 3 propagates the same idiom to the FoundationsBootSmokeTest persistence assertion. Task 4 regression run validates AuditDurabilityTest 3/3 + FoundationsBootSmokeTest 5/5 GREEN under jmix-security-data enforcement — strictly stronger than the original "regression-gate detect" disposition |
| T-08-08-04 | Denial of Service (perf regression) | ToolQueryCountBaselineTest R-03h slope detector | accept | The R-03e calibration ceilings already accommodate ~600 SELECTs from Jmix permission iteration; CrudEntityConstraint adds in-memory checks (no extra DB round-trips); slope-based assertion remains valid. Surface in Task 4 deviation if absolute counts shift but slope stays intact |
| T-08-08-05 | Repudiation | Audit pipeline (Phase 4 AuditWriter REQUIRES_NEW) | accept | REQUIRES_NEW propagation contract is independent of constraint registration; AuditDurabilityTest 3/3 regression gate confirms the decorator-path REQUIRES_NEW contract holds under the new UnconstrainedDataManager wiring |
| T-08-08-06 | Tampering | Audit log write path bypassed by adversarial code path | accept | UnconstrainedDataManager is intentionally scoped to AuditWriter only; all other write paths in ai-agent module remain on constrained DataManager and continue to enforce CrudEntityConstraint. The unconstrained scope is narrow (one class, three write methods: writeChatStart, writeChatFinish, writeToolCall, writeRetrieval), all are @Transactional REQUIRES_NEW, and the audit pipeline is the only legitimate consumer |
</threat_model>

<verification>
**Phase-level closure check (Gap 1 from VERIFICATION.md):**

After all 4 tasks complete, manually verify by re-reading 08-VERIFICATION.md "Gaps / Findings" → Gap 1: the carry-over note "3 RED tests in the negative-case suite surface real security gaps in the SUTs (per-user schema filter on `ai_AiAuditEvent` for `carol`; cross-user conversation listing for `bob`)" must now be addressable as CLOSED.

REQUIREMENTS.md TEST-04 line 114: `[ ] **TEST-04**` was previously unchecked because of the 3 REDs. After this plan, all 6 of the @Test methods originally landed in 08-01 are GREEN — TEST-04 acceptance bullets all satisfied. (REQUIREMENTS.md line update is OUT OF SCOPE for this plan; the orchestrator may flip the checkbox during a future verification pass.)

**Three closure checks** (one per RED that flips, plus the audit-pipeline regression check):

1. RED-1 + RED-2 closure: `FilteredSchemaAndExecutionDenialTest` 3/3 GREEN — `CurrentUserSchemaAccess.canReadEntity` now denies carol on `ai_AiAuditEvent` because `CrudEntityConstraint` is registered.
2. RED-3 closure: `CrossUserConversationAccessTest` 2/2 GREEN — `dataManager.load(AiConversation.class).all().list()` now applies the row-level predicate via `ReadEntityQueryConstraint`.
3. **AuditWriter regression check** (NEW vs original plan): `AuditDurabilityTest` 3/3 GREEN — audit-write pipeline persists `ai_AiAuditEvent` rows under user authentication via `UnconstrainedDataManager`, regardless of whether the user has CRUD grants on the audit entity.

The plan does NOT update VERIFICATION.md, REQUIREMENTS.md, or ROADMAP.md — this is gap-closure execution only.
</verification>

<success_criteria>
- 3 RED tests from 08-01-SUMMARY.md "Out-of-Scope Issues Discovered" flip to GREEN (R-XP-2-FIX-1/2/3)
- 12/12 PASS in `com.vn.agent.security.*` (closure target)
- 5 previously-GREEN tests preserved under the broader fix (the regression bar that the original plan's bare dep would have broken):
  - `AuditDurabilityTest.toolAuditRowSurvivesOuterRollback`
  - `AuditDurabilityTest.writeChatFinish_doesNotOrphanChildren_writtenInSeparateRequiresNew`
  - `AuditDurabilityTest.errorPathToolAuditAlsoSurvivesOuterRollback_viaDecorator`
  - `CrossUserConversationAccessTest.userB_replayingUserAConversation_throwsSameTypeAndCodeAsRandomUuid`
  - `FoundationsBootSmokeTest.all_five_entities_round_trip`
- 17/17 in 08-02 + 08-03 cross-cutting (PromptInjectionHarnessTest 5/5, AuditDurabilityTest 3/3, ToolQueryCountBaselineTest 7/7, FindRecordsLimitCapTest 2/2)
- **Total 29/29** across the named regression classes
- THREE files modified: gradle dep + AuditWriter + FoundationsBootSmokeTest line 165 (was 1 file in original plan)
- ZERO new test files created
- ZERO test deletions
- ZERO changes to ai-agent-starter, jmix-app
- ZERO changes to the 3 RED test files (`FilteredSchemaAndExecutionDenialTest.java`, `CrossUserConversationAccessTest.java`, `RagRoleFilterNegativeTest.java`)
- ZERO changes to pinned files: `NoCustomerReadRoleConfiguration.java`, `TestUsersConfiguration.java`, `AiAgentUserRowLevelRole.java`, `CurrentUserSchemaAccess.java`, `BuiltInDataTools.java`
- TWO atomic commits land: (a) the 3 source-file fix; (b) the 08-08-SUMMARY.md
- 08-08-SUMMARY.md exists with required frontmatter (incl. `supersedes: 08-08-PLAN.md@4955924`) + body sections + supersession traceability
</success_criteria>

<output>
After completion, create `.planning/phases/08-integration-hardening-release-readiness/08-08-SUMMARY.md` per Task 4 sub-action (c). The SUMMARY consolidates Tasks 1-4 evidence, records supersession traceability (original 08-08-PLAN.md@4955924 + 08-08-INVESTIGATION.md), and serves as the closure-traceability artifact for VERIFICATION.md Gap 1.
</output>

<notes>
## Why this plan replaces the original 08-08 (commit 4955924)

The original 08-08-PLAN.md attempted a **single-line gradle dep change** as the entire fix. Its `<notes>` section "Out-of-band escalation paths" correctly anticipated the failure mode:

> If after adding the dep, RED-3 (bob's `.all().list()`) STILL returns alice's row... DO NOT modify `AiAgentUserRowLevelRole`... Surface deviation; orchestrator handles via further `--gaps` round.

The gsd-executor ran the original plan's Task 1, observed exactly that escalation pattern (plus 4 additional GREEN-tests-now-RED regressions in AuditDurabilityTest + FoundationsBootSmokeTest + the previously-passing replay test in CrossUserConversationAccessTest), reverted the change cleanly, and surfaced the architectural escalation. See `08-08-INVESTIGATION.md` for the full executor evidence.

This revision converts the escalation path into the primary scope. Three coordinated edits land together because:

1. **The classpath fix alone is necessary but insufficient** — it flips RED-1/RED-2 GREEN at the AccessManager layer but breaks AuditWriter at the constrained-DataManager layer.
2. **The AuditWriter fix is the architectural anchor** — `UnconstrainedDataManager` is Jmix's canonical pattern for system-internal persistence (per `/jmix-framework/jmix-context7` `data-manager.html`); it declares system-scope intent at the type level and avoids the role-grant brittleness of `runWithSystem`.
3. **The FoundationsBootSmokeTest fix propagates the same idiom** — the test fixture writes `AiAuditEvent` directly (not via AuditWriter), so it needs the same `.unconstrained()` call to survive jmix-security-data enforcement.

## What is FALSIFIED from the original plan

The original plan's `<notes>` subsection "Why no SUT code changes are needed" claimed:

> The fix is purely classpath: the registration of constraints into the registry is what's missing, not the user-authentication wiring or the role definitions.

That claim is FALSIFIED for `AuditWriter`. We now do change one production source file. The architectural rationale is grounded in:
- Jmix Context7 docs (`/jmix-framework/jmix-context7` `data-manager.html`) — UnconstrainedDataManager is the canonical pattern for system-internal persistence
- Project memory `feedback_jmix_unconstrained_for_system_writes` — confirms this is the project standard going forward
- 08-08-INVESTIGATION.md — gsd-executor evidence that constrained DataManager + end-user roles is the regression source

## Why one plan, not two or three

- **Single classpath dep** registers BOTH `CrudEntityConstraint` (RED-1, RED-2) AND `ReadEntityQueryConstraint` (RED-3) from the same Spring autoconfig.
- **Single architectural pattern** (UnconstrainedDataManager) covers both the AuditWriter production path AND the FoundationsBootSmokeTest fixture path.
- **Single regression surface** — the 5 previously-GREEN tests that would regress under bare dep all flow through the same `AuditWriter.dataManager.save(...)` chain (or its test-fixture equivalent).
- **Single wave / depends_on: []** — no prior gap-closure plan needed; this lands atomically.

Splitting would force redundant regression runs and add coordination overhead with zero parallel-execution benefit (all three edits would conflict at the file/file-pair level).

## Pinned files (must NOT be modified)

Per user constraint and INVESTIGATION:
- `FilteredSchemaAndExecutionDenialTest.java`, `CrossUserConversationAccessTest.java`, `RagRoleFilterNegativeTest.java` — the 3 RED test files verify the fix; modifying them would defeat the purpose
- `NoCustomerReadRoleConfiguration.java` — carol's role is pinned
- `TestUsersConfiguration.java` — alice/bob/admin baseline is pinned
- `AiAgentUserRowLevelRole.java` — `@JpqlRowLevelPolicy` declaration is correct; ReadEntityQueryConstraint registration was the missing piece
- `CurrentUserSchemaAccess.java`, `BuiltInDataTools.java` — SUTs pinned; `applyRegisteredConstraints` + `canReadEntity` patterns are correct Jmix idioms; CrudEntityConstraint registration was the missing piece
- `AdminViewAccessTest.java` — already 5/5 GREEN; uses UiShowViewContext (registered by jmix-security-flowui-starter, already on classpath)
- `ai-agent-starter.gradle`, `jmix-app/build.gradle` — out of scope

The ONLY test-file unlock is `FoundationsBootSmokeTest.java` line 165 (one-line `.unconstrained()` insertion + 2-line rationale comment).

## Out-of-band escalation paths (if Task 2 or Task 3 fails)

If after Task 2 (`AuditWriter` switch), `AuditDurabilityTest` still has failures:
- The audit-tx propagation contract is independent of the DataManager subtype; UnconstrainedDataManager has the same `@Transactional` semantics. Any failure here is a deeper Jmix bean-wiring concern.
- DO NOT revert to constrained DataManager — that re-introduces the regression cascade.
- Surface deviation in Task 4 SUMMARY; orchestrator decides next steps.

If after Task 3 (`FoundationsBootSmokeTest` line 165), `all_five_entities_round_trip` still fails:
- Likely cause: EclipseLink "not a known Entity type" persistence-unit reshape (the Phase 2 forensics concern).
- Mitigation: confirm `AIConfiguration.java:35` has `EclipselinkConfiguration.class` in `dependsOn` (it does as of pre-Plan-08-08 state); confirm `RowLevelPolicyEntity` / `ResourcePolicyEntity` are scoped to the `main` store (different from `agentstore`).
- DO NOT mask with `@Disabled`. Surface deviation in Task 4 SUMMARY; orchestrator decides whether a follow-up Phase 8.1 is needed.

If after Task 4 broad broom, an unrelated test fails (something not in the named regression-bar classes):
- Capture the test name + stack; document in SUMMARY "Deviations from Plan".
- The targeted regression run (12/12 + 17/17 = 29/29 in the named classes) is the contractual gate; the broad broom is a defense-in-depth check.
</notes>
</content>
</invoke>