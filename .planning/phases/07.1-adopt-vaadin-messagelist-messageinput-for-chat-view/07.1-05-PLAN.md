---
phase: 7.1-adopt-vaadin-messagelist-messageinput-for-chat-view
plan: 05
type: execute
wave: 3
depends_on: ["7.1-03", "7.1-04"]
files_modified:
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ToolCallCardComponent.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/CitationDialog.java
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/fragment/citation-dialog.xml
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties
  - ai-agent/ai-agent/src/main/resources/META-INF/resources/frontend/styles/ai-agent-chat.css
autonomous: true
requirements: [UI-01, UI-02]

must_haves:
  truths:
    - "ToolCallCardComponent.java, CitationDialog.java, and citation-dialog.xml are deleted"
    - "MarkdownRenderer.java, MessageBubbleComponent.java, MarkdownRendererXssTest.java are KEPT (still consumed by ConversationDetailView per A-01)"
    - "flexmark + owasp-html-sanitizer dependencies in ai-agent.gradle are KEPT (D-12 deferred)"
    - "CitationDialog-scoped i18n keys are removed; MessageBubble/ToolCard-scoped keys are KEPT (bubble still used)"
    - "CSS: ai-agent-tool-card__* selectors removed; ai-agent-bubble--* selectors KEPT"
    - "ConversationDetailView still compiles (imports of MarkdownRenderer + MessageBubbleComponent resolve)"
    - "Full :ai-agent:ai-agent:compileJava and :ai-agent:ai-agent:compileTestJava still pass"
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ToolCallCardComponent.java"
      provides: "DELETED"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/CitationDialog.java"
      provides: "DELETED"
    - path: "ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/fragment/citation-dialog.xml"
      provides: "DELETED"
  key_links:
    - from: "ConversationDetailView.java"
      to: "MarkdownRenderer + MessageBubbleComponent"
      via: "imports at lines 12-13, used at lines 61 + 100-110"
      pattern: "import com\\.vn\\.agent\\.view\\.chat\\.(MarkdownRenderer|fragment\\.MessageBubbleComponent)"
---

<objective>
Narrowed deletion set per amendment A-01. Delete only the three chat-view-scoped files + their CSS/i18n that have no cross-consumer. Keep `MarkdownRenderer`, `MessageBubbleComponent`, `MarkdownRendererXssTest`, and the flexmark/owasp-sanitizer gradle dependencies — these remain live via `ConversationDetailView` replay and drop only when that view migrates to MessageList (D-12 follow-up phase).

Purpose: Close the scope-reduction loop from Phase 7 without regressing UI-03 conversation replay. Depends on Plans 03 and 04 because those are the last callers of the soon-deleted components.
Output: 3 files deleted; partial CSS purge; i18n keys scoped to `CitationDialog` removed from both bundles; everything else compiles and `LocaleParityTest` stays green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/7.1-adopt-vaadin-messagelist-messageinput-for-chat-view/7.1-CONTEXT.md
@.planning/phases/7.1-adopt-vaadin-messagelist-messageinput-for-chat-view/7.1-RESEARCH.md
@ai-agent/ai-agent/src/main/java/com/vn/agent/view/conversation/ConversationDetailView.java
@ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties
@ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight audit — confirm no in-tree callers remain after Plans 03+04</name>
  <files>(no writes; audit-only task)</files>
  <read_first>
    - .planning/phases/7.1-adopt-vaadin-messagelist-messageinput-for-chat-view/7.1-CONTEXT.md §A-01 (narrowed deletion set) + §A-02 (ChatProgressView does not exist)
    - .planning/phases/7.1-adopt-vaadin-messagelist-messageinput-for-chat-view/7.1-RESEARCH.md §Files-to-Delete Confirmation (the authoritative grep matrix)
    - .planning/phases/7.1-adopt-vaadin-messagelist-messageinput-for-chat-view/7.1-03-SUMMARY.md (verifies Plan 03 removed ToolCallCard/CitationDialog refs from Fragment)
    - .planning/phases/7.1-adopt-vaadin-messagelist-messageinput-for-chat-view/7.1-04-SUMMARY.md (verifies Plan 04 removed refs from ChatView)
  </read_first>
  <action>
    Run these greps. ALL must pass before proceeding to Task 2. If any fail, STOP and file a blocker note (callers survive in Plan 03/04 output — not ready to delete):

    1. `grep -rn "ToolCallCardComponent" ai-agent/ai-agent/src/main/java/` — must return 0 hits (file itself doesn't count for this purpose; grep should not find any import/use).
       Acceptable: the file's own `class ToolCallCardComponent` declaration line only.
       If any production .java file outside `view/chat/fragment/ToolCallCardComponent.java` references it → STOP.

    2. `grep -rn "CitationDialog" ai-agent/ai-agent/src/main/java/` — same rule; only the file's own class declaration may remain.

    3. `grep -rn "import com.vn.agent.view.chat.fragment.ToolCallCardComponent\|import com.vn.agent.view.chat.fragment.CitationDialog" ai-agent/ai-agent/src/` — must return 0 hits.

    4. `grep -rn "MarkdownRenderer\|MessageBubbleComponent" ai-agent/ai-agent/src/main/java/com/vn/agent/view/conversation/` — MUST return ≥ 1 hit each (A-01 keeps them alive FOR ConversationDetailView; no hit means ConversationDetailView was changed unexpectedly and flexmark can now drop — different decision path, outside Phase 7.1 scope, also STOP).

    5. `grep -rn "ChatProgressView" ai-agent/ai-agent/src/main/java/` — must return 0 hits (A-02: file never existed in our codebase).

    Record results in the task completion note for the SUMMARY. If all checks green → proceed to Task 2.
  </action>
  <verify>
    <automated>test 0 -eq $(grep -rn "import com.vn.agent.view.chat.fragment.ToolCallCardComponent\|import com.vn.agent.view.chat.fragment.CitationDialog" ai-agent/ai-agent/src/ | wc -l) &amp;&amp; test 0 -lt $(grep -rn "MarkdownRenderer" ai-agent/ai-agent/src/main/java/com/vn/agent/view/conversation/ | wc -l)</automated>
  </verify>
  <acceptance_criteria>
    - No imports of `ToolCallCardComponent` or `CitationDialog` anywhere in `ai-agent/ai-agent/src/`.
    - `ConversationDetailView.java` still imports `MarkdownRenderer` AND `MessageBubbleComponent` (A-01 keeps them).
    - No references to a `ChatProgressView` symbol in java source (A-02 confirmation).
  </acceptance_criteria>
  <done>Audit greps all satisfy their acceptance predicates; safe to delete.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the 3 narrowed files + purge CitationDialog-scoped i18n keys + partial CSS cleanup</name>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ToolCallCardComponent.java (DELETE),
    ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/CitationDialog.java (DELETE),
    ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/fragment/citation-dialog.xml (DELETE),
    ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties,
    ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties,
    ai-agent/ai-agent/src/main/resources/META-INF/resources/frontend/styles/ai-agent-chat.css
  </files>
  <read_first>
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties (locate CitationDialog-scoped keys: `chatView.citation.dialogTitle`, `chatView.citation.openInKb`, `chatView.citation.close`)
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties (parity targets)
    - ai-agent/ai-agent/src/main/resources/META-INF/resources/frontend/styles/ai-agent-chat.css (identify `.ai-agent-tool-card__*` selectors — those are dead after ToolCallCardComponent deletion; `.ai-agent-bubble--*` selectors are KEPT for MessageBubbleComponent)
  </read_first>
  <action>
    Execute in this order:

    1. **Delete files:**
       ```bash
       rm ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ToolCallCardComponent.java
       rm ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/CitationDialog.java
       rm ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/fragment/citation-dialog.xml
       ```

    2. **Remove CitationDialog-scoped i18n keys from `messages.properties`:**
       Delete these exact lines (preserve surrounding keys — `chatView.citation.*` keys scoped only to the dialog; the trailing `**Sources:**` list built by StreamEventRenderer uses `chatView.stream.sources` from Plan 01, not these):
       - `chatView.citation.dialogTitle=Source: {0}`
       - `chatView.citation.openInKb=Open in Knowledge base`
       - `chatView.citation.close=Close`

    3. **Remove matching keys from `messages_vi.properties`:**
       - `chatView.citation.dialogTitle=Nguồn: {0}`
       - `chatView.citation.openInKb=Mở trong Cơ sở tri thức`
       - `chatView.citation.close=Đóng`

    4. **Partial CSS cleanup** (`ai-agent-chat.css`):
       - DELETE all `.ai-agent-tool-card__*` rule blocks (dead after Task 2 file-delete).
       - KEEP all `.ai-agent-bubble--*` rule blocks (MessageBubbleComponent still uses them — A-01).
       - KEEP `.ai-agent-chat-panel*` rule blocks if present (used by Plan 03's new XML shell).
       - If unsure whether a selector is still referenced, `grep -rn "<className>" ai-agent/ai-agent/src/main/` — if zero hits, delete; otherwise keep.

    5. **DO NOT touch `ai-agent/ai-agent/ai-agent.gradle`:**
       Flexmark + owasp-html-sanitizer MUST remain per amendment A-01. If you see flexmark removal listed anywhere in CONTEXT's original D-10, that is overridden by A-01. Hands off the gradle file.

    6. **DO NOT delete**: `MarkdownRenderer.java`, `MessageBubbleComponent.java`, `MarkdownRendererXssTest.java` — all kept for ConversationDetailView (A-01).

    After deletions, run `./gradlew :ai-agent:ai-agent:compileJava :ai-agent:ai-agent:compileTestJava` to confirm compilation of the full module (including ConversationDetailView which still uses the kept components) survives.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:compileJava :ai-agent:ai-agent:compileTestJava</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ToolCallCardComponent.java` (file absent).
    - `test ! -f ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/CitationDialog.java` (file absent).
    - `test ! -f ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/fragment/citation-dialog.xml` (file absent).
    - `test -f ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/MarkdownRenderer.java` (file PRESENT — A-01).
    - `test -f ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/MessageBubbleComponent.java` (file PRESENT — A-01).
    - `test -f ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/MarkdownRendererXssTest.java` (file PRESENT — A-01).
    - `grep -c "^chatView\.citation\." ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties` returns `0`.
    - `grep -c "^chatView\.citation\." ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties` returns `0`.
    - `grep -c "com\.vladsch\.flexmark" ai-agent/ai-agent/ai-agent.gradle` returns ≥ `3` (unchanged — A-01).
    - `grep -c "owasp-java-html-sanitizer" ai-agent/ai-agent/ai-agent.gradle` returns ≥ `1` (unchanged — A-01).
    - `grep -c "ai-agent-tool-card" ai-agent/ai-agent/src/main/resources/META-INF/resources/frontend/styles/ai-agent-chat.css` returns `0` (dead selectors purged).
    - `./gradlew :ai-agent:ai-agent:compileJava` exits 0.
    - `./gradlew :ai-agent:ai-agent:compileTestJava` exits 0 (MarkdownRendererXssTest still compiles against kept MarkdownRenderer).
    - `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.i18n.LocaleParityTest"` exits 0 (EN/VI key sets still match after matching deletions).
  </acceptance_criteria>
  <done>3 files deleted; 6 CitationDialog-scoped i18n keys (3 EN + 3 VI) removed with parity; tool-card CSS purged; MarkdownRenderer + MessageBubbleComponent + XssTest + flexmark/sanitizer deps all PRESENT; full compileJava + compileTestJava + LocaleParityTest green.</done>
</task>

</tasks>

<verification>
- Compile: `./gradlew :ai-agent:ai-agent:compileJava :ai-agent:ai-agent:compileTestJava`.
- Locale parity: `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.i18n.LocaleParityTest"`.
- ConversationDetailView should be unaffected — its imports still resolve, and its `MessageBubbleComponent` replay path is unchanged.
</verification>

<success_criteria>
- 3 files deleted (`ToolCallCardComponent.java`, `CitationDialog.java`, `citation-dialog.xml`).
- 6 CitationDialog-scoped i18n keys removed (3 EN + 3 VI) — parity preserved.
- Dead `.ai-agent-tool-card__*` CSS rules removed.
- `MarkdownRenderer` / `MessageBubbleComponent` / `MarkdownRendererXssTest` / flexmark / owasp-sanitizer all KEPT (A-01).
- Module compiles; ConversationDetailView unaffected.
</success_criteria>

<output>
Create `.planning/phases/7.1-adopt-vaadin-messagelist-messageinput-for-chat-view/7.1-05-SUMMARY.md` listing: deleted files, deleted i18n keys (EN and VI side-by-side), deleted CSS rule names, and an explicit "KEPT" list re-affirming A-01 (flexmark + sanitizer + MarkdownRenderer + MessageBubbleComponent + XssTest). Flag the D-12 follow-up phase that must migrate `ConversationDetailView` before full cleanup.
</output>
