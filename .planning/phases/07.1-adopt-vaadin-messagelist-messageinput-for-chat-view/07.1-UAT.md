---
status: accepted
phase: 07.1-adopt-vaadin-messagelist-messageinput-for-chat-view
source:
  - 07.1-01-SUMMARY.md
  - 07.1-02-SUMMARY.md
  - 07.1-03-SUMMARY.md
  - 07.1-04-SUMMARY.md
  - 07.1-05-SUMMARY.md
  - 07.1-06-SUMMARY.md
  - 07.1-07-SUMMARY.md
started: 2026-04-21T17:22:58.1953053Z
updated: 2026-04-25T00:00:00+07:00
accepted: 2026-04-25
acceptance_note: "Accept-as-is close. 5 issues (1 blocker streaming error/i18n keys, majors on tool-call markdown and knowledge upload) deferred to Phase 08 hardening. Streaming blocker NOT fixed — must be addressed before merge to master."
---

## Current Test

[testing complete]

## Tests

### 1. Streaming renders into MessageList
expected: Open `/ai-agent/chat`, submit a prompt, and verify the chat uses the Vaadin MessageList/MessageInput flow: your message appears immediately, an assistant message appears beneath it, and the assistant bubble accumulates markdown as the response streams in.
result: issue
reported: "Playwright login plus chat smoke reached the MessageList UI and added two messages, but after clicking Send the assistant rendered `error: chatView.error.generic` instead of a streamed answer. The chat heading and New chat button also displayed raw i18n keys (`chatView.title`, `chatView.action.newChat`)."
severity: blocker

### 2. Tool calls render as inline markdown
expected: During or after a tool-using response, the assistant message includes inline markdown for tool activity: a bold tool name, an italic outcome line such as `_done - <summary>_`, and a separator. There should be no custom expand/collapse tool card UI.
result: issue
reported: "@C:\\Users\\admin\\Pictures\\Screenshots\\Screenshot\\ 2026-04-23\\ 094540.png chưa đúng đúng kh"
severity: major

### 3. Citations render as clickable Sources list
expected: When a response includes citations, the assistant message ends with a `Sources` list (or localized equivalent). Each source is clickable and navigates to `/ai-agent/knowledge?documentId=<uuid>`.
result: issue
reported: "cant upload an source"
severity: major

### 4. Stop cancels stream and writes CANCELLED audit
expected: While a response is streaming, clicking `Stop` halts the stream, re-enables input, hides the Stop button, and leaves an audit row with `outcome = CANCELLED` for that run in `/ai-agent/audit`.
result: issue
reported: "it dont audit if i cancelled"
severity: major

### 5. New chat confirm gate works
expected: Clicking `New chat` prompts for confirmation only when the current chat has messages or is still streaming. Confirming clears the chat and removes any `conversationId` query param; a fresh empty chat does not prompt.
result: pass

### 6. conversationId query param replays transcript
expected: Opening `/ai-agent/chat?conversationId=<uuid>` for an existing chat loads the prior user and assistant messages in chronological order into the MessageList.
result: issue
reported: "After sending a message, the browser URL remained `/ai-agent/chat` with no `conversationId` query parameter, so replay-by-URL could not be exercised."
severity: major

### 7. Bilingual locale smoke
expected: In Vietnamese locale, chat labels are localized: user label `Ban`, assistant label `Tro ly AI`, localized `Stop`, and localized `Sources` header with no stray hardcoded English in the chat surface.
result: pass

## Summary

total: 7
passed: 2
issues: 5
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "Chat uses the MessageList/MessageInput flow and the assistant bubble accumulates a real streamed markdown response"
  status: failed
  reason: "Playwright observed the assistant render `error: chatView.error.generic` instead of a streamed answer, and the chat title / New chat button showed raw i18n keys."
  severity: blocker
  test: 1
  root_cause: "SPRING_AI_CHAT_MEMORY is missing in the agentstore path while spring.ai chat-memory schema auto-init is disabled; stream failures are mapped to chatView.error.generic."
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java"
      issue: "Unclassified stream exceptions are translated to chatView.error.generic."
    - path: "jmix-app/src/main/resources/application.properties"
      issue: "spring.ai.chat.memory.repository.jdbc.initialize-schema=never."
    - path: "ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/060-ai-chat-memory.xml"
      issue: "No recovery changeSet for drift case where table is dropped but Liquibase history remains applied."
  missing:
    - "Add a new idempotent Liquibase recovery changeSet to ensure SPRING_AI_CHAT_MEMORY and index exist."
    - "Verify deployed runtime matches current i18n keys after migration succeeds."
  debug_session: ".planning/debug/issue1-chat-error-generic.md"
- truth: "After sending a message, the chat URL exposes a `conversationId` query parameter so the transcript can be replayed by URL"
  status: failed
  reason: "Playwright observed the URL remain `/ai-agent/chat` with no `conversationId` query parameter after send."
  severity: major
  test: 6
  root_cause: "Conversation state is synchronized from URL to fragment only; there is no reverse sync from fragment state to URL after send."
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/ChatView.java"
      issue: "Reads conversationId from query params but never writes it back."
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java"
      issue: "Internal conversationId is updated but no query-param navigation is triggered."
  missing:
    - "Add post-send URL query-param synchronization when conversationId becomes available."
  debug_session: ".planning/debug/uat6-chat-url-missing-convid.md"
- truth: "Tool-using responses render inline tool-call markdown (bold tool name, italic outcome line, and separator) without custom tool card UI"
  status: failed
  reason: "User reported: @C:\\Users\\admin\\Pictures\\Screenshots\\Screenshot\\ 2026-04-23\\ 094540.png chưa đúng đúng kh"
  severity: major
  test: 2
  root_cause: "Tool event emission depends on RunContext ThreadLocal, but streaming threads propagate SecurityContext only; ToolCall/ToolResult events are dropped."
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/audit/ToolCallbackAuditDecorator.java"
      issue: "Publishes tool events via StreamingSinkHolder.current()."
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/StreamingSinkHolder.java"
      issue: "Sink lookup is RunContext ThreadLocal-based."
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/ChatStreamingSchedulerConfig.java"
      issue: "Context propagation covers SecurityContext but not RunContext."
  missing:
    - "Propagate RunContext (or pass runId/sink explicitly) across streaming execution."
    - "Add regression test ensuring tool-using stream emits inline markdown."
  debug_session: ".planning/debug/phase-07-1-uat2-tool-markdown.md"
- truth: "When a response includes citations, the assistant message ends with a `Sources` list where each source link opens `/ai-agent/knowledge?documentId=<uuid>`"
  status: failed
  reason: "User reported: cant upload an source"
  severity: major
  test: 3
  root_cause: "UI uploads always submit staged file: URIs, but file: sources are rejected when jmix.ai-agent.rag.upload.file-staging-root is unset."
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/view/knowledge/KnowledgeBaseView.java"
      issue: "Upload flow forwards staged file URI to service."
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/rag/KnowledgeDocumentUploadService.java"
      issue: "validateSourceUri rejects file: URIs when staging root property is blank."
    - path: "jmix-app/src/main/resources/application.properties"
      issue: "Missing jmix.ai-agent.rag.upload.file-staging-root host setting."
  missing:
    - "Set jmix.ai-agent.rag.upload.file-staging-root to the upload staging directory."
    - "Surface configuration-specific upload error to users."
  debug_session: ".planning/debug/07-1-uat3-cant-upload-source.md"
- truth: "Cancelling an in-progress stream creates an audit entry with `outcome = CANCELLED`"
  status: failed
  reason: "User reported: it dont audit if i cancelled"
  severity: major
  test: 4
  root_cause: "No implemented cancellation audit path: stop often occurs before final event/runId assignment, and outcome enum lacks CANCELLED."
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java"
      issue: "Stop-before-final disposes stream without writing audit."
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/rag/CancellationRegistry.java"
      issue: "Cancellation path does not persist audit records."
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallOutcome.java"
      issue: "Outcome model does not include CANCELLED."
  missing:
    - "Add explicit cancellation audit write path when Stop is clicked."
    - "Support CANCELLED outcome in model and audit rendering."
  debug_session: ".planning/debug/cancel-stream-no-audit.md"
