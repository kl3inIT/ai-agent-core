---
phase: 7.1-adopt-vaadin-messagelist-messageinput-for-chat-view
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties
autonomous: true
requirements: [UI-01, UI-02]

must_haves:
  truths:
    - "Every new chatView.stream.* key exists in BOTH locale bundles"
    - "renderStreamEvent implementation (Plan 02) can resolve all labels it needs from MessageSource"
  artifacts:
    - path: "ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties"
      provides: "English chatView.stream.* keys (D-09 namespace)"
    - path: "ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties"
      provides: "Vietnamese chatView.stream.* keys (parity)"
  key_links:
    - from: "renderStreamEvent (Plan 02)"
      to: "chatView.stream.* keys"
      via: "MessageSource resolution from Map<String,String> labels"
      pattern: "chatView\\.stream\\."
---

<objective>
Add the 11 new `chatView.stream.*` i18n keys per RESEARCH §i18n Key Set (D-09). Two-file, text-only change. Prepares the label set that Plan 02's pure `renderStreamEvent(StreamingEvent, Map<String,String>)` consumes and that Plan 03's Fragment passes in via `MessageSource`. Runs parallel to Plan 02 (disjoint files).

Purpose: Keys must land first so downstream plans have a single source of truth for English/Vietnamese strings (D-30 parity carries forward from Phase 7).
Output: 11 new key pairs, alphabetically grouped, added to the existing `chatView.stream.*` / `chatView.message.*` blocks without touching pre-existing `chatView.toolCard.*` / `chatView.error.*` / `chatView.citation.*` lines (those remain for `ConversationDetailView` + retained `MessageBubbleComponent` per A-01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/7.1-adopt-vaadin-messagelist-messageinput-for-chat-view/7.1-CONTEXT.md
@.planning/phases/7.1-adopt-vaadin-messagelist-messageinput-for-chat-view/7.1-RESEARCH.md
@ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties
@ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add chatView.stream.* keys to messages.properties (English)</name>
  <files>ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties</files>
  <read_first>
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties (inspect existing chatView.* block, lines ~120-141)
    - .planning/phases/7.1-adopt-vaadin-messagelist-messageinput-for-chat-view/7.1-RESEARCH.md §i18n Key Set (table of 11 EN/VI pairs)
  </read_first>
  <action>
    Skill: jmix-i18n. Locate the `chatView.error.flagged=...` line in messages.properties (currently line 141). Immediately BELOW it, append a new block labeled `# Phase 7.1 — MessageList/MessageInput stream rendering (D-09)` with exactly these 11 key=value pairs in this order, preserving existing indent/style (no trailing spaces):

    chatView.stream.sources=Sources
    chatView.stream.outcome.SUCCESS=done
    chatView.stream.outcome.BLOCKED=blocked
    chatView.stream.outcome.ERROR=error
    chatView.stream.outcome.FLAGGED=flagged
    chatView.stream.receivedIn=Received response in
    chatView.stream.promptTokens=prompt tokens
    chatView.stream.completionTokens=completion tokens
    chatView.stream.error=error
    chatView.message.userName=You
    chatView.message.assistantName=AI Assistant

    Do NOT edit or delete any pre-existing `chatView.toolCard.*`, `chatView.citation.*`, `chatView.error.*`, `chatView.streaming.indicator`, or `chatView.message.stopped` keys — those remain live because `MessageBubbleComponent` + `MarkdownRenderer` are KEPT per amendment A-01 (still consumed by `ConversationDetailView`).
  </action>
  <verify>
    <automated>grep -c "^chatView\.stream\." ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^chatView\.stream\." ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties` returns `9`
    - `grep "^chatView\.message\.userName=" ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties` returns one hit with value `You`
    - `grep "^chatView\.message\.assistantName=" ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties` returns one hit with value `AI Assistant`
    - `grep -c "^chatView\.toolCard\." ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties` still returns `6` (no accidental deletion)
    - `grep -c "^chatView\.citation\." ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties` still returns `3`
    - `grep "^chatView\.stream\.outcome\.SUCCESS=" ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties` returns `chatView.stream.outcome.SUCCESS=done`
  </acceptance_criteria>
  <done>English bundle contains exactly the 11 new keys in the `chatView.stream.*` / `chatView.message.*` namespaces with the values from RESEARCH §i18n Key Set; no pre-existing keys removed.</done>
</task>

<task type="auto">
  <name>Task 2: Add matching Vietnamese keys to messages_vi.properties (parity)</name>
  <files>ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties</files>
  <read_first>
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties (inspect existing chatView.* block, lines ~120-141)
    - .planning/phases/7.1-adopt-vaadin-messagelist-messageinput-for-chat-view/7.1-RESEARCH.md §i18n Key Set (VI column)
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties (the sibling file just edited by Task 1 — key list MUST match 1:1)
  </read_first>
  <action>
    Skill: jmix-i18n. Locate `chatView.error.flagged=...` in messages_vi.properties (currently line 141). Immediately BELOW it, append the SAME labeled comment block (`# Phase 7.1 — MessageList/MessageInput stream rendering (D-09)`) and the same 11 keys in the SAME order (parity with Task 1 is load-bearing — `I18nParityTest` / `LocaleParityTest` from Phase 7-07b WILL fail if the key set differs):

    chatView.stream.sources=Nguồn tham khảo
    chatView.stream.outcome.SUCCESS=hoàn tất
    chatView.stream.outcome.BLOCKED=đã chặn
    chatView.stream.outcome.ERROR=lỗi
    chatView.stream.outcome.FLAGGED=đã gắn cờ
    chatView.stream.receivedIn=Phản hồi nhận trong
    chatView.stream.promptTokens=token đầu vào
    chatView.stream.completionTokens=token đầu ra
    chatView.stream.error=lỗi
    chatView.message.userName=Bạn
    chatView.message.assistantName=Trợ lý AI

    Same no-deletion rule as Task 1.
  </action>
  <verify>
    <automated>diff <(grep -oE "^chatView\.[a-zA-Z]+\.[a-zA-Z.]+" ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties | sort -u) <(grep -oE "^chatView\.[a-zA-Z]+\.[a-zA-Z.]+" ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties | sort -u)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^chatView\.stream\." ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties` returns `9`
    - `grep "^chatView\.message\.userName=" ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties` returns a line ending in `Bạn`
    - `grep "^chatView\.message\.assistantName=" ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties` returns a line ending in `Trợ lý AI`
    - Diff command above exits 0 (empty — both bundles have IDENTICAL chatView.* key sets)
    - `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.i18n.LocaleParityTest"` exits 0
  </acceptance_criteria>
  <done>Vietnamese bundle contains exactly the 11 parity keys; EN/VI chatView.* key-set diff is empty; `LocaleParityTest` green.</done>
</task>

</tasks>

<verification>
- Both property files hold the new `chatView.stream.*` / `chatView.message.*` keys.
- Phase 7-07b's `LocaleParityTest` passes: `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.i18n.LocaleParityTest"`.
- No pre-existing `chatView.toolCard.*`, `chatView.citation.*`, `chatView.error.*`, or `chatView.streaming.indicator` keys deleted (those are consumed by the KEPT `MessageBubbleComponent`/`CitationDialog`-alternate callers and by Phase 7 error i18n that `StreamingEvent.Error.messageKey` already resolves against).
</verification>

<success_criteria>
- 11 new EN key=value pairs; 11 new VI key=value pairs; 1:1 key-name parity.
- Pre-existing chat i18n untouched.
- LocaleParityTest stays green.
</success_criteria>

<output>
After completion, create `.planning/phases/7.1-adopt-vaadin-messagelist-messageinput-for-chat-view/7.1-01-SUMMARY.md` summarizing: the 11 keys added (copied verbatim), the line numbers they landed at in each bundle, and a note that Plan 02's `renderStreamEvent` will consume them via a `Map<String,String>` resolved through `MessageSource` (so Plan 02 can now start).
</output>
