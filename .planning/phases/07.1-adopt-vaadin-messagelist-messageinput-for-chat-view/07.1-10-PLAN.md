---
phase: 7.1-adopt-vaadin-messagelist-messageinput-for-chat-view
plan: 10
type: execute
wave: 2
depends_on: ["7.1-08", "7.1-09"]
files_modified:
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/ChatView.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/chat-view.xml
  - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/ChatViewStopTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/ChatViewQueryParamSyncTest.java
autonomous: true
requirements: [UI-01, UI-02]
gap_closure: true

must_haves:
  truths:
    - "After first send, chat URL includes `?conversationId=<uuid>` and can be replayed by URL (UAT gap #6 / #2 list item in UAT file)."
    - "Stop path uses pre-allocated runId from submit time, so cancel is available before Final event (UAT gap #5 UI side, per D-04 intent)."
    - "Chat title and New chat labels resolve normally from message bundles (no raw key text in header)."
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/ChatView.java"
      provides: "Reverse sync from fragment conversation state into route query params."
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java"
      provides: "Pre-allocated runId usage and conversationId change notifications."
    - path: "ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/chat-view.xml"
      provides: "Canonical msg key wiring for title/new-chat label."
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/ChatViewQueryParamSyncTest.java"
      provides: "Regression test guarding URL conversationId sync behavior."
  key_links:
    - from: "ChatPanelFragment.ensureConversationIdForSubmit"
      to: "ChatView query-parameter updater"
      via: "conversationId change callback"
      pattern: "conversationId"
    - from: "ChatPanelFragment onSubmit"
      to: "ChatService.stream(..., runId)"
      via: "pre-allocated runId path"
      pattern: "stream\\(.*runId\\)"
---

<objective>
Close the remaining UI-layer UAT gaps: query-parameter reverse sync, stop-before-final run lifecycle continuity, and chat header i18n key resolution.

Purpose: make replay-by-URL and Stop behavior observable and reliable from the actual chat surface.
Output: ChatView/ChatPanelFragment wiring updates plus focused chat-view regression tests.
</objective>

<execution_context>
@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-adopt-vaadin-messagelist-messageinput-for-chat-view/07.1-UAT.md
@.planning/phases/07.1-adopt-vaadin-messagelist-messageinput-for-chat-view/07.1-CONTEXT.md
@ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/ChatView.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java
@ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/chat-view.xml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement conversationId reverse-sync from fragment state to route URL</name>
  <files>ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/ChatView.java, ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java</files>
  <action>
    Close UAT replay gap (conversationId missing in URL) while preserving D-06/D-07 responsibilities:
    - Keep `ChatView` as route owner and query-param writer.
    - Add a minimal fragment-to-view callback (`conversationId changed`) so the view can write/remove `conversationId` query param after send/new-chat transitions.
    - Update URL only on actual value change to avoid navigation loops.
    - On `startNewChat`, clear query param from the URL.

    Do not move stream orchestration out of `ChatPanelFragment` (D-05 remains intact).
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.ChatViewQueryParamSyncTest"</automated>
  </verify>
  <done>Sending the first message writes `conversationId` to URL and clearing chat removes it, enabling replay-by-URL behavior expected in UAT.</done>
</task>

<task type="auto">
  <name>Task 2: Pre-allocate runId on submit and route Stop exclusively through cancellation registry</name>
  <files>ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java, ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/ChatViewStopTest.java</files>
  <action>
    Close UAT gap #5 UI-side cause:
    - Generate runId at submit start, assign to `activeRunId` before stream subscription.
    - Call runId-aware stream overload from Plan 09 so run lifecycle is known before any events.
    - Ensure Stop click uses `cancellationRegistry.cancel(activeRunId)` path (per D-04 intent) rather than direct `Disposable.dispose()` as operational path.

    Keep detach-time defensive fallback only for truly missing runId edge cases.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.ChatViewStopTest"</automated>
  </verify>
  <done>Stop can cancel auditable runs even before Final event arrives; regression test protects against reintroducing direct-dispose bypass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add query-param sync regression test and normalize chat-view XML message keys</name>
  <files>ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/ChatViewQueryParamSyncTest.java, ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/chat-view.xml</files>
  <behavior>
    - Test 1: Chat view source/wiring contains a concrete query-param write path for `conversationId`.
    - Test 2: Chat view source/wiring contains a clear-path for conversation reset.
    - Test 3: XML message key expressions use canonical `msg://` form for title/new-chat label.
  </behavior>
  <action>
    Add `ChatViewQueryParamSyncTest` (lightweight regression guard suitable for addon module) and normalize `chat-view.xml` message-key expressions to canonical Jmix `msg://...` syntax so header labels resolve reliably.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.ChatViewQueryParamSyncTest" --tests "com.vn.agent.view.chat.ChatViewStreamTest"</automated>
  </verify>
  <done>URL sync and header i18n key wiring are locked by tests and no longer regress silently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser URL query param -> ChatView binder | Untrusted `conversationId` input must not bypass ownership checks. |
| Stop button -> cancellation backend | User action must target only the active run and not stale/null run ids. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7.1-10-01 | Information Disclosure | `conversationId` URL replay | mitigate | Keep existing ConversationGateway ownership enforcement and generic error path; add reverse sync only, not bypass logic. |
| T-7.1-10-02 | Denial of Service | Stop action race before Final | mitigate | Pre-allocate runId before stream starts; Stop always routes via cancellation registry for deterministic lifecycle handling. |
| T-7.1-10-03 | Tampering | XML message key resolution | accept | Canonicalize `msg://` usage to avoid accidental raw-key rendering; no security-sensitive data exposed. |
</threat_model>

<verification>
- `ChatViewQueryParamSyncTest` passes.
- `ChatViewStopTest` passes with pre-allocated runId expectations.
- `ChatViewStreamTest` remains green after URL/key wiring changes.
</verification>

<success_criteria>
- UAT conversationId replay gap is technically closed through URL reverse sync.
- Stop-before-final cancellation now follows auditable run lifecycle.
- Raw header i18n key rendering no longer appears from malformed message-key syntax.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-adopt-vaadin-messagelist-messageinput-for-chat-view/07.1-10-SUMMARY.md`.
</output>

