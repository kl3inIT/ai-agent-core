---
phase: 7.1-adopt-vaadin-messagelist-messageinput-for-chat-view
plan: 08
type: execute
wave: 1
depends_on: ["7.1-07"]
files_modified:
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/081-ai-chat-memory-recovery.xml
  - jmix-app/src/main/resources/application.properties
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/knowledge/KnowledgeBaseView.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/rag/KnowledgeDocumentUploadServiceTest.java
autonomous: true
requirements: [UI-01, UI-02]
gap_closure: true

must_haves:
  truths:
    - "Chat sends no longer fail with `chatView.error.generic` due to missing `SPRING_AI_CHAT_MEMORY` table drift (UAT gap #1)."
    - "Knowledge upload accepts `file:` URIs from the configured staging root and shows a configuration-specific error when staging root is missing (UAT gap #4)."
    - "Host runtime explicitly configures `jmix.ai-agent.rag.upload.file-staging-root` for the Flow UI upload path."
  artifacts:
    - path: "ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/081-ai-chat-memory-recovery.xml"
      provides: "Idempotent recovery DDL for `SPRING_AI_CHAT_MEMORY` + composite index."
    - path: "jmix-app/src/main/resources/application.properties"
      provides: "Host-side upload staging-root configuration."
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/view/knowledge/KnowledgeBaseView.java"
      provides: "Upload error handling that distinguishes staging-root misconfiguration from generic file rejection."
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/rag/KnowledgeDocumentUploadServiceTest.java"
      provides: "Regression tests for `file:` URI acceptance/rejection under staging-root policy."
  key_links:
    - from: "KnowledgeBaseView upload handler"
      to: "KnowledgeDocumentUploadService.validateSourceUri"
      via: "IllegalArgumentException mapping to user-facing notification text"
      pattern: "uploadService.upload\\(stagedFile.toURI\\(\\)\\.toString\\(\\)"
    - from: "agentstore Liquibase includeAll"
      to: "081-ai-chat-memory-recovery.xml"
      via: "automatic include via add-on `agentstore-changelog.xml`"
      pattern: "includeAll path=\"/com/vn/agent/liquibase/agentstore-changelog\""
---

<objective>
Close UAT blocker #1 and major gap #4 with runtime-hardening fixes: recover chat-memory schema drift safely and make upload staging-root configuration explicit and diagnosable.

Purpose: restore stable chat streaming and unblock source uploads without manual DB surgery or opaque UI errors.
Output: recovery Liquibase changeset, host property wiring, upload error-path refinement, and regression tests.
</objective>

<execution_context>
@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-adopt-vaadin-messagelist-messageinput-for-chat-view/07.1-UAT.md
@.planning/phases/07.1-adopt-vaadin-messagelist-messageinput-for-chat-view/07.1-RESEARCH.md
@ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/060-ai-chat-memory.xml
@ai-agent/ai-agent/src/main/java/com/vn/agent/view/knowledge/KnowledgeBaseView.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/rag/KnowledgeDocumentUploadService.java
@jmix-app/src/main/resources/application.properties
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add idempotent chat-memory recovery changelog for drifted environments</name>
  <files>ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/081-ai-chat-memory-recovery.xml</files>
  <action>
    Create `081-ai-chat-memory-recovery.xml` to close UAT gap #1 root cause (table dropped out-of-band while `060` is already marked applied).

    Implement DB-specific idempotent recovery changesets (PostgreSQL required; include HSQLDB parity if feasible) that:
    - Create `SPRING_AI_CHAT_MEMORY` when missing with the same schema contract used by `JdbcChatMemoryRepository`.
    - Create index `SPRING_AI_CHAT_MEMORY_CONVERSATION_ID_TIMESTAMP_IDX` when missing.
    - Use preconditions so the recovery only executes when drift exists.

    Keep `060-ai-chat-memory.xml` untouched (no id/author rewrites, no historical mutation).
  </action>
  <verify>
    <automated>rg -n "SPRING_AI_CHAT_MEMORY|SPRING_AI_CHAT_MEMORY_CONVERSATION_ID_TIMESTAMP_IDX" ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/081-ai-chat-memory-recovery.xml</automated>
  </verify>
  <done>Recovery changeset exists and is safely re-runnable; it restores missing chat-memory table/index without replaying historical `060` changesets.</done>
</task>

<task type="auto">
  <name>Task 2: Wire staging-root property and surface configuration-aware upload errors</name>
  <files>jmix-app/src/main/resources/application.properties, ai-agent/ai-agent/src/main/java/com/vn/agent/view/knowledge/KnowledgeBaseView.java</files>
  <action>
    Close UAT gap #4 missing items:
    - Add `jmix.ai-agent.rag.upload.file-staging-root=${jmix.core.work-dir}/temp` in host `application.properties`.
    - Refine `KnowledgeBaseView` upload exception handling so `IllegalArgumentException` from URI validation is surfaced with config-specific detail instead of only generic file-type rejection.

    Preserve fail-closed behavior: invalid/unallowed URIs must still reject upload and must not be persisted.
  </action>
  <verify>
    <automated>rg -n "jmix.ai-agent.rag.upload.file-staging-root|notifyDetailedError|Upload rejected by staging-root allowlist" jmix-app/src/main/resources/application.properties ai-agent/ai-agent/src/main/java/com/vn/agent/view/knowledge/KnowledgeBaseView.java</automated>
  </verify>
  <done>Host config explicitly sets staging root and users receive actionable error text when upload fails due to missing/mis-scoped staging-root configuration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add/extend upload policy regression tests for file-staging-root scenarios</name>
  <files>ai-agent/ai-agent/src/test/java/com/vn/agent/rag/KnowledgeDocumentUploadServiceTest.java</files>
  <behavior>
    - Test 1: file URI under configured staging root is accepted and persisted.
    - Test 2: file URI outside configured staging root is rejected pre-persistence.
    - Test 3: missing staging root keeps rejecting file URIs (existing fail-closed behavior retained).
  </behavior>
  <action>
    Extend `KnowledgeDocumentUploadServiceTest` to cover positive/negative `file:` URI behavior with explicit `AiAgentRagProperties.Upload(fileStagingRoot=...)`.
    Keep existing classpath/scheme rejection assertions intact.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.rag.KnowledgeDocumentUploadServiceTest"</automated>
  </verify>
  <done>Upload validation behavior is locked by automated tests for all staging-root paths relevant to UAT gap #4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser upload request -> KnowledgeDocumentUploadService | Untrusted file metadata and URI enters server-side validation. |
| Liquibase history table -> runtime schema | Drift between recorded changesets and physical table state can break chat memory reads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7.1-08-01 | Tampering | `sourceUri` handling in upload flow | mitigate | Keep fail-closed URI allowlist enforcement; test both in-root and out-of-root `file:` URIs. |
| T-7.1-08-02 | Denial of Service | Missing `SPRING_AI_CHAT_MEMORY` table | mitigate | Add idempotent recovery changeset guarded by table/index existence preconditions. |
| T-7.1-08-03 | Information Disclosure | Error text shown to user on upload failure | accept | Surface config cause without exposing secrets (path/config class only, no credentials). |
</threat_model>

<verification>
- `081-ai-chat-memory-recovery.xml` exists and contains table+index recovery DDL.
- Host `application.properties` contains `jmix.ai-agent.rag.upload.file-staging-root`.
- `KnowledgeDocumentUploadServiceTest` passes with new staging-root cases.
</verification>

<success_criteria>
- UAT gap #1 root cause is technically resolved by automated recovery DDL.
- UAT gap #4 root cause is technically resolved by explicit host config + upload error-path clarity.
- No regression in existing upload validation tests.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-adopt-vaadin-messagelist-messageinput-for-chat-view/07.1-08-SUMMARY.md`.
</output>

