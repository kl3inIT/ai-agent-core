---
phase: 7.1-adopt-vaadin-messagelist-messageinput-for-chat-view
plan: 09
type: execute
wave: 1
depends_on: ["7.1-07"]
files_modified:
  - ai-agent/ai-agent/src/main/java/com/vn/agent/ChatService.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/tools/AgentToolCallbacks.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/audit/ToolCallbackAuditDecorator.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallOutcome.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/audit/ToolCallAuditDetailDialog.java
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties
  - ai-agent/ai-agent/src/test/java/com/vn/agent/rag/ChatServiceFilterParamContractTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/i18n/I18nParityTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/audit/ToolCallbackAuditDecoratorStreamingTest.java
autonomous: true
requirements: [UI-01, UI-02]
gap_closure: true

must_haves:
  truths:
    - "Tool-using streams emit `StreamingEvent.ToolCall` and `StreamingEvent.ToolResult` reliably under scheduler hops (UAT gap #2)."
    - "Stream cancellation writes an audit row with `outcome = CANCELLED` for the same run id (UAT gap #5)."
    - "Audit outcome model and badges support `CANCELLED` without fallback/raw enum artifacts."
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/ChatService.java"
      provides: "RunId-aware streaming contract used by UI and backend."
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/audit/ToolCallbackAuditDecorator.java"
      provides: "Explicit runId-based tool-event emission and audit correlation."
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java"
      provides: "Cancellation audit write on `SignalType.CANCEL` and runId propagation to callbacks."
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallOutcome.java"
      provides: "`CANCELLED` enum value."
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/audit/ToolCallbackAuditDecoratorStreamingTest.java"
      provides: "Regression test that prevents dropped tool events when `RunContext` is absent."
  key_links:
    - from: "DefaultChatServiceImpl.stream(runId)"
      to: "AgentToolCallbacks.callbacksFor(..., runId)"
      via: "per-request callback construction"
      pattern: "callbacksFor\\(userId, convId, runId\\)"
    - from: "ToolCallbackAuditDecorator"
      to: "StreamingSinkHolder.forRun(runId)"
      via: "explicit runId sink lookup"
      pattern: "forRun\\(.*runId"
    - from: "doFinally(CANCEL)"
      to: "AuditWriter.writeToolCall(..., AiToolCallOutcome.CANCELLED, ...)"
      via: "cancellation path"
      pattern: "AiToolCallOutcome\\.CANCELLED"
---

<objective>
Close UAT gaps #2 and #5 at the backend contract layer by removing run-context ambiguity during streaming and adding explicit cancellation auditing semantics.

Purpose: ensure inline tool markdown has source events to render, and Stop behavior is auditable as `CANCELLED`.
Output: runId-aware stream/callback contract, deterministic tool-event emission, cancellation audit write path, and regression tests.
</objective>

<execution_context>
@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-adopt-vaadin-messagelist-messageinput-for-chat-view/07.1-UAT.md
@ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/tools/AgentToolCallbacks.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/audit/ToolCallbackAuditDecorator.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/StreamingSinkHolder.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/guard/GuardedToolCallingManager.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce runId-aware stream + callback contracts</name>
  <files>ai-agent/ai-agent/src/main/java/com/vn/agent/ChatService.java, ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java, ai-agent/ai-agent/src/main/java/com/vn/agent/tools/AgentToolCallbacks.java</files>
  <action>
    Implement explicit runId plumbing for streaming calls to remove reliance on implicit ThreadLocal continuity (UAT gap #2 root cause):

    - Add a runId-aware stream signature in `ChatService`:
      `Flux<StreamingEvent> stream(String userId, UUID conversationId, String message, Overrides overrides, UUID runId)`.
    - Keep the existing 4-arg signature and delegate it to the new signature with internally generated runId for backward compatibility.
    - In `DefaultChatServiceImpl.stream`, stop regenerating runId when one is provided; use that runId consistently for sink registration, Final event, and cancellation registration.
    - Extend `AgentToolCallbacks.callbacksFor(...)` to accept runId and pass it into decorated callbacks.
  </action>
  <verify>
    <automated>rg -n "stream\\(String userId, UUID conversationId, String message, Overrides overrides, UUID runId\\)|callbacksFor\\(String userId, java.util.UUID conversationId, UUID runId\\)" ai-agent/ai-agent/src/main/java/com/vn/agent/ChatService.java ai-agent/ai-agent/src/main/java/com/vn/agent/tools/AgentToolCallbacks.java ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java</automated>
  </verify>
  <done>Streaming API and callback assembly can carry a deterministic runId from caller to tool-event/audit paths.</done>
</task>

<task type="auto">
  <name>Task 2: Emit tool events by explicit runId and persist CANCELLED on stream cancellation</name>
  <files>ai-agent/ai-agent/src/main/java/com/vn/agent/audit/ToolCallbackAuditDecorator.java, ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java, ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallOutcome.java, ai-agent/ai-agent/src/main/java/com/vn/agent/view/audit/ToolCallAuditDetailDialog.java, ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties, ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties</files>
  <action>
    Implement two concrete fixes:

    1) Tool-event propagation (UAT gap #2):
    - In `ToolCallbackAuditDecorator`, use an explicit runId field (from callbacksFor) as primary correlation id.
    - Lookup sink via `StreamingSinkHolder.forRun(explicitRunId)`; keep `RunContext` fallback only for non-upgraded callers.

    2) Cancellation audit path (UAT gap #5, per D-04 behavior intent):
    - In `DefaultChatServiceImpl.stream`, detect cancellation in `doFinally` and write one request-level audit row with `AiToolCallOutcome.CANCELLED`.
    - Add `CANCELLED` to `AiToolCallOutcome`.
    - Extend audit badge/theme handling and locale bundles for `CANCELLED` (entity enum key + audit list/detail text consistency).

    Keep cancellation path idempotent (no duplicate CANCELLED rows for a single run).
  </action>
  <verify>
    <automated>rg -n "AiToolCallOutcome\\.CANCELLED|case CANCELLED|com.vn.agent.entity/AiToolCallOutcome.CANCELLED" ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallOutcome.java ai-agent/ai-agent/src/main/java/com/vn/agent/view/audit/ToolCallAuditDetailDialog.java ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties</automated>
  </verify>
  <done>Tool events are correlated by explicit runId and cancellation is represented by first-class `CANCELLED` outcome in both data model and UI rendering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add regression tests for tool-event emission and cancellation auditing</name>
  <files>ai-agent/ai-agent/src/test/java/com/vn/agent/audit/ToolCallbackAuditDecoratorStreamingTest.java, ai-agent/ai-agent/src/test/java/com/vn/agent/rag/ChatServiceFilterParamContractTest.java, ai-agent/ai-agent/src/test/java/com/vn/agent/i18n/I18nParityTest.java</files>
  <behavior>
    - Test 1: explicit runId decorator emits ToolCall/ToolResult even when `RunContext` is empty.
    - Test 2: cancelling stream subscription triggers `auditWriter.writeToolCall(..., CANCELLED, ...)`.
    - Test 3: locale parity test includes `AiToolCallOutcome.CANCELLED` key in both EN and VI bundles.
  </behavior>
  <action>
    Add `ToolCallbackAuditDecoratorStreamingTest` and update existing contract tests for new method signatures and cancellation assertions.
    Update `I18nParityTest` enum-key check to include `CANCELLED`.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.audit.ToolCallbackAuditDecoratorStreamingTest" --tests "com.vn.agent.rag.ChatServiceFilterParamContractTest" --tests "com.vn.agent.i18n.I18nParityTest"</automated>
  </verify>
  <done>Automated regression suite fails if tool events drop under thread hops or if cancellation stops producing `CANCELLED` audit rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM tool-callback execution -> streaming UI sink | Cross-thread event correlation must preserve run ownership and order. |
| Stop action -> cancellation/audit persistence | User-triggered cancellation must be durable and not forge another run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7.1-09-01 | Repudiation | Stream cancellation audits | mitigate | Persist explicit `CANCELLED` row in stream cancellation finalizer with correlated runId. |
| T-7.1-09-02 | Tampering | Tool-event correlation | mitigate | Use explicit runId from request contract, not only ambient thread-local state. |
| T-7.1-09-03 | Denial of Service | Duplicate cancellation writes | mitigate | Add idempotent cancel-audit guard per run in stream finalizer. |
</threat_model>

<verification>
- RunId-aware stream API and callback signatures compile and are exercised by tests.
- Tool-event streaming test proves no dropped ToolCall/ToolResult events when `RunContext` is absent.
- Cancellation audit test proves `AiToolCallOutcome.CANCELLED` write path.
- Locale parity includes new enum key in EN+VI.
</verification>

<success_criteria>
- UAT gap #2 backend cause is removed (tool events no longer dependent on fragile ThreadLocal continuity).
- UAT gap #5 backend cause is removed (`CANCELLED` is persisted and renderable).
- Regression tests lock both behaviors.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-adopt-vaadin-messagelist-messageinput-for-chat-view/07.1-09-SUMMARY.md`.
</output>

