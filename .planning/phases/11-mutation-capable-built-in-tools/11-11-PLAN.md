---
phase: 11-mutation-capable-built-in-tools
plan: 11
type: execute
wave: 11
depends_on:
  - 11-10-PLAN.md
files_modified:
  - ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/BuiltInMutationToolsCommitFailedAuditTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/BuiltInMutationToolsRelatedWriteSecurityTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/tools/link/BuiltInLinkToolsOpacityTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/MutationErrorTranslatorTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/guard/AgentSystemPromptRulesComposerTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/guard/ToolNamePatternProviderMutationToolsTest.java
autonomous: true
requirements:
  - TEST-12
  - MUT-07
  - MUT-10
  - MUT-12
must_haves:
  truths:
    - "TEST-12 asserts post-host-save idempotency finalization failure writes exactly one durable audit row with outcome=AiToolCallOutcome.COMMIT_FAILED and leaves the intent non-reclaimable (COMMIT_UNKNOWN or PENDING), never FAILED; separate known-rollback save failures map to validation_failed/ERROR and may mark FAILED"
    - "TEST-12 also covers the post-COMMITTED catch scope: if audit writing fails after markCommitted succeeds, safeWriteAudit logs and the tool still returns success; the AiMutationIntent row remains COMMITTED and an exact retry replays instead of duplicating the host write"
    - "Related-write success tests use non-composition MutationLinkedParentFixture/MutationLinkedChildFixture metadata and prove add_related_record/remove_related_record can succeed, not only fail closed"
    - "Related-write security tests use concrete MutationParentFixture/MutationChildFixture metadata and cover parent update, relationship attribute modify, child LLM exposure, child read/update, inverse attribute modify, unsupported relationship shape, and composition/orphanRemoval removal rejection before any host save/remove"
    - "BuiltInLinkToolsOpacityTest asserts hidden entities return unknown_entity, never access_denied"
    - "MutationErrorTranslatorTest covers all 6 stable codes and remaps invalid_literal/unsupported_type/invalid_id to parameter_conversion_error"
    - "AgentSystemPromptRulesComposerTest asserts mutation prompt rules are conditional and never mention prepare_form_draft"
    - "ToolNamePatternProviderMutationToolsTest asserts the output scanner covers 6 read + 2 link + 4 mutation built-in names and excludes delete_record"
    - "All tests use ToolUserError.toDto().error(); no test calls ToolUserError.getCode()"
    - "Mockito test doubles use Spring's @MockitoBean when needed, not deprecated @MockBean / @SpyBean; TEST-12 does not throw inside MutationSaveExecutor for the commit-unknown case"
  artifacts:
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/BuiltInMutationToolsCommitFailedAuditTest.java"
      provides: "TEST-12 commit-failed audit durability proof"
      contains: "AiToolCallOutcome.COMMIT_FAILED"
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/BuiltInMutationToolsRelatedWriteSecurityTest.java"
      provides: "Related-write authorization matrix"
      contains: "MutationSaveExecutor"
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/guard/ToolNamePatternProviderMutationToolsTest.java"
      provides: "Tool-name leakage scanner coverage"
      contains: "create_record"
  key_links:
    - from: "BuiltInMutationToolsCommitFailedAuditTest"
      to: "AuditWriter REQUIRES_NEW durability"
      via: "MutationSaveExecutor save failure and AiAuditEvent assertion"
      pattern: "COMMIT_FAILED"
    - from: "ToolNamePatternProviderMutationToolsTest"
      to: "ToolNamePatternProvider.BUILT_IN_TOOL_NAMES"
      via: "assert all built-in names are scanned"
      pattern: "remove_related_record"
---

<objective>
Land the remaining Phase 11 supporting regression tests after the core fixture and callback/idempotency tests from Plan 11-10. This plan covers TEST-12 plus the review-driven support gates for related-write security, link opacity, error-code translation, conditional prompt rules, and output-scanner tool-name coverage.

Purpose: Plan 11-10 keeps the core regression set within GSD scope budget. This plan finishes the late test surface with a smaller file set and focused verification commands.

Output: six focused tests under `src/test/java`, all compiling against current APIs and passing with Gradle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-mutation-capable-built-in-tools/11-CONTEXT.md
@.planning/phases/11-mutation-capable-built-in-tools/11-REVIEWS.md
@.planning/phases/11-mutation-capable-built-in-tools/11-07-PLAN.md
@.planning/phases/11-mutation-capable-built-in-tools/11-09-PLAN.md
@.planning/phases/11-mutation-capable-built-in-tools/11-10-PLAN.md

@ai-agent/ai-agent/src/test/java/com/vn/agent/audit/AuditDurabilityTest.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/tools/mutation/BuiltInMutationTools.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/tools/mutation/MutationSaveExecutor.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/tools/mutation/MutationErrorTranslator.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/tools/link/BuiltInLinkTools.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/guard/AgentSystemPromptRulesComposer.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/guard/ToolNamePatternProvider.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiAuditEvent.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallOutcome.java

<interfaces>
Current source facts to preserve:
- `AiAuditEvent.getOutcome()` returns `AiToolCallOutcome`; compare to the enum value, not `COMMIT_FAILED.getId()`.
- `ToolUserError` exposes `toDto().error()`; there is no `getCode()`.
- Direct mutation-tool calls need both authentication and static `RunContext`. Plan 11-10 creates `MutationToolTestContext`; use `mutationToolTestContext.withMutationRun("admin", ...)` rather than raw `SystemAuthenticator.withUser(...)`.
- Plan 11-10 creates `MutationTestFixture`, `MutationParentFixture`, `MutationChildFixture`, `MutationLinkedParentFixture`, `MutationLinkedChildFixture`, `MutationToolTestContext`, and `test-main-changelog.xml`; mutation-tool integration tests here reuse those fixtures and set `main.liquibase.change-log=com/vn/agent/test_liquibase/test-main-changelog.xml`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: TEST-12 commit-failed audit and related-write security tests</name>
  <files>
    ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/BuiltInMutationToolsCommitFailedAuditTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/BuiltInMutationToolsRelatedWriteSecurityTest.java
  </files>
  <read_first>
    - ai-agent/ai-agent/src/test/java/com/vn/agent/audit/AuditDurabilityTest.java
    - ai-agent/ai-agent/src/main/java/com/vn/agent/tools/mutation/BuiltInMutationTools.java
    - ai-agent/ai-agent/src/main/java/com/vn/agent/tools/mutation/MutationSaveExecutor.java
    - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiAuditEvent.java
    - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallOutcome.java
  </read_first>
  <action>
1. Create `BuiltInMutationToolsCommitFailedAuditTest`:
   - Use `@SpringBootTest(properties = {"ai-agent.tools.mutation.enabled=true", "main.liquibase.change-log=com/vn/agent/test_liquibase/test-main-changelog.xml"})`.
   - Do NOT use `@MockitoSpyBean` for TEST-12. A spy can throw before the real transactional method body runs and therefore does not prove the desired boundary.
   - Use the real `MutationSaveExecutor` so the host row commits/returns normally.
   - Do NOT `@MockitoBean MutationIntentRepository`. That would bypass real reservation rows and make status assertions meaningless.
   - Provide a test bean for the package-private Plan 11-05 `MutationIntentFailureProbe` so `beforeMarkCommitted(...)` throws after `MutationSaveExecutor.save(...)` returned but before the idempotency row reaches `COMMITTED`. This simulates the real commit-unknown window while preserving the real repository for reservation/replay/status queries.
   - Call `createRecord` inside `mutationToolTestContext.withMutationRun("admin", ...)`.
   - Assert result contains `"concurrent_modification"` and the expected hint contains `do not retry automatically`.
   - Query the fixture table with `dataManager.unconstrained()` and assert the attempted `MutationTestFixture` row WAS persisted exactly once.
   - Query `AiAuditEvent` with `dataManager.unconstrained()` and assert exactly one newest row for `eventName='create_record'`.
   - Assert `auditRows.getFirst().getOutcome()).isEqualTo(AiToolCallOutcome.COMMIT_FAILED)`.
   - Do not compare `getOutcome()` to `AiToolCallOutcome.COMMIT_FAILED.getId()`.
   - Query `AiMutationIntent` with `UnconstrainedDataManager` and assert the status is `COMMIT_UNKNOWN` (preferred) or remains `PENDING` if the test intentionally makes agentstore finalization fail; it must never be `FAILED`.
   - Retry the same tool call with the same idempotency key and assert `MutationSaveExecutor.save(...)` is not invoked a second time; response is `concurrent_modification`/commit-failed guidance, not a duplicate create.
   - Add a separate known-rollback test where `MutationSaveExecutor.save(...)` throws `DataIntegrityViolationException` or `ConstraintViolationException` before returning. Assert the tool result is `validation_failed`, the audit outcome is `ERROR` (not `COMMIT_FAILED`), and the intent is `FAILED` so an exact same-hash retry can proceed.
   - Add a separate post-COMMITTED audit-failure regression: let `markCommitted(...)` succeed, then make `AuditWriter.writeToolCall(...)` throw. Assert the initial tool response is still `SUCCESS`, the intent row remains `COMMITTED` (not `COMMIT_UNKNOWN`/`FAILED`), an exact retry returns `IDEMPOTENT_REPLAY`, and no second host save occurs.

2. Create `BuiltInMutationToolsRelatedWriteSecurityTest`:
   - Use the concrete `MutationParentFixture.children` / `MutationChildFixture.parent` composition relationship from Plan 11-10 for rejection coverage.
   - Use the concrete `MutationLinkedParentFixture.linkedChildren` / `MutationLinkedChildFixture.linkedParent` non-composition relationship from Plan 11-10 for positive related-write coverage.
   - Add successful `add_related_record` and `remove_related_record` tests against the non-composition fixtures; assert `SUCCESS`, parent/child ids in result, child inverse set/cleared in the database, and exactly one success audit row with full `argumentsJson`.
   - Cover `add_related_record` and `remove_related_record` authorization shortcuts.
   - Mock/answer AccessManager contexts to deny one of: parent update, relationship attribute modify, child read/update, inverse attribute modify.
   - Add a separate exposure-denial variant where Jmix `AccessManager` allows the child but `LlmExposurePolicy` hides it; assert `"access_denied"` and no executor call.
   - Assert denial returns `"access_denied"` and `MutationSaveExecutor` is not called.
   - For composition/orphanRemoval removal, assert the tool returns `validation_failed` before any host save/remove and verify `mutationSaveExecutor.save(...)` and `saveAll(...)` are never called.
   - For successful create/update/related-write audit rows, parse `argumentsJson` and assert it includes full LLM arguments (`entityName`, `id` when applicable, `relationship`, `relatedId`, `idempotencyKey`, and nested `attributes`) with sensitive attribute values hashed.
   - Add one unsupported-shape assertion if a fixture or metadata stub exposes it; unsupported many-to-many/unidirectional/collection-inverse metadata returns `"validation_failed"` and does not call `MutationSaveExecutor`.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:test --tests "com.vn.agent.tools.mutation.BuiltInMutationToolsCommitFailedAuditTest" --tests "com.vn.agent.tools.mutation.BuiltInMutationToolsRelatedWriteSecurityTest"</automated>
  </verify>
  <acceptance_criteria>
    - Commit-failed audit test uses a real `MutationSaveExecutor` and a `MutationIntentFailureProbe` test bean to inject failure at `markCommitted(...)` after the host save has returned but before the idempotency row reaches `COMMITTED`; it does not mock/spy `MutationIntentRepository` and does not use `@MockitoSpyBean`.
    - Commit-failed audit test asserts the host fixture row is persisted exactly once.
    - Commit-failed audit test compares `AiAuditEvent.getOutcome()` to `AiToolCallOutcome.COMMIT_FAILED`.
    - Commit-failed audit test asserts exactly one audit row for the mutation call.
    - Commit-failed audit test asserts the `AiMutationIntent` status is non-reclaimable (`COMMIT_UNKNOWN` or retained `PENDING`) and never `FAILED`.
    - Commit-failed audit test retries the same idempotency key and proves no second host save occurs.
    - Post-COMMITTED audit-failure regression proves `safeWriteAudit` absorbs `AuditWriter.writeToolCall(...)` failure after successful `markCommitted(...)`, leaves `AiMutationIntent.status=COMMITTED`, returns initial `SUCCESS`, and exact retry returns `IDEMPOTENT_REPLAY`.
    - Known rollback test asserts save-time `ConstraintViolationException`/`DataIntegrityViolationException` maps to `validation_failed`, audit outcome `ERROR`, and intent status `FAILED`, not `COMMIT_UNKNOWN`.
    - Related-write success tests use `MutationLinkedParentFixture` and `MutationLinkedChildFixture` and prove both related tools have a working success path.
    - Related-write security/rejection tests use `MutationParentFixture` and `MutationChildFixture`.
    - Related-write security test covers parent, relationship attribute, child LLM exposure, child read/update, inverse attribute, and composition/orphanRemoval rejection gates.
    - Related-write security test covers unsupported relationship metadata fail-closed as `validation_failed`.
    - Audit arguments test asserts full tool args are present in `AiAuditEvent.argumentsJson`, not only the attributes map.
    - Related-write security test verifies denied paths do not call `MutationSaveExecutor`.
    - No test file contains `@MockBean`, `@SpyBean`, `@MockitoSpyBean`, `@MockitoBean MutationIntentRepository`, or `.getCode()`.
    - Targeted Gradle tests exit 0.
  </acceptance_criteria>
  <done>TEST-12 and related-write security review blockers are locked by focused integration tests.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Link opacity, translator, prompt-rule, and tool-name scanner tests</name>
  <files>
    ai-agent/ai-agent/src/test/java/com/vn/agent/tools/link/BuiltInLinkToolsOpacityTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/MutationErrorTranslatorTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/guard/AgentSystemPromptRulesComposerTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/guard/ToolNamePatternProviderMutationToolsTest.java
  </files>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/tools/link/BuiltInLinkTools.java
    - ai-agent/ai-agent/src/main/java/com/vn/agent/tools/mutation/MutationErrorTranslator.java
    - ai-agent/ai-agent/src/main/java/com/vn/agent/tools/ToolUserError.java
    - ai-agent/ai-agent/src/main/java/com/vn/agent/guard/AgentSystemPromptRulesComposer.java
    - ai-agent/ai-agent/src/main/java/com/vn/agent/guard/ToolNamePatternProvider.java
  </read_first>
  <action>
1. Create `BuiltInLinkToolsOpacityTest`:
   - Use `@MockitoBean LlmExposurePolicy`.
   - Stub the actual read gate method (`canReadEntity` in current source) to false.
   - Assert hidden entity returns `"unknown_entity"` and never `"access_denied"`.

2. Create `MutationErrorTranslatorTest`:
   - Plain unit test.
   - Cover the six stable codes: `access_denied`, `not_found`, `parameter_conversion_error`, `validation_failed`, `concurrent_modification`, `idempotency_violation`.
   - Assert `translator.translate(new ToolUserError("invalid_literal", ...), ...)` returns a `ToolUserError` whose `toDto().error()` is `parameter_conversion_error`.
   - Assert the same remap for `unsupported_type`.
   - Assert the same remap for `invalid_id` so blank/malformed mutation ids cannot leak a non-Phase-11 error code.
   - Assert unknown exception fallback uses `toDto().error()` equal to `validation_failed` and does not echo raw exception text or PII.

3. Create `AgentSystemPromptRulesComposerTest`:
   - Default config: `effectiveRules()` contains baseline `PROMPT_RULES` and does not contain mutation-only phrases.
   - Mutation enabled config: `effectiveRules()` contains `idempotencyKey`, `access_denied`, `parameter_conversion_error`, `concurrent_modification`, and `generate_entity_detail_link`.
   - Assert neither default nor enabled rules contain `prepare_form_draft`.

4. Create `ToolNamePatternProviderMutationToolsTest`:
   - Assert the pattern/provider catches all 12 built-in names: six read, two link, four mutation.
   - Assert it does not include `delete_record`.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:test --tests "com.vn.agent.tools.link.BuiltInLinkToolsOpacityTest" --tests "com.vn.agent.tools.mutation.MutationErrorTranslatorTest" --tests "com.vn.agent.guard.AgentSystemPromptRulesComposerTest" --tests "com.vn.agent.guard.ToolNamePatternProviderMutationToolsTest"</automated>
  </verify>
  <acceptance_criteria>
    - Link opacity test asserts `"unknown_entity"` and not `"access_denied"`.
    - Translator test uses `toDto().error()` for every error-code assertion.
    - Translator test covers remap from `invalid_literal`, `unsupported_type`, and `invalid_id` to `parameter_conversion_error`.
    - Prompt composer test asserts mutation rules are conditional and `prepare_form_draft` is absent.
    - Tool-name scanner test covers link and mutation tool names and excludes `delete_record`.
    - No test file contains `@MockBean`, `@SpyBean`, `@MockitoSpyBean`, or `.getCode()`.
    - Targeted Gradle tests exit 0.
  </acceptance_criteria>
  <done>Supporting regressions for link opacity, translator mapping, prompt rules, and scanner coverage are green.</done>
</task>

</tasks>

<verification>
- `./gradlew :ai-agent:compileTestJava` exits 0.
- Targeted Gradle commands listed in each task exit 0.
- `./gradlew :ai-agent:test` exits 0.
- JetBrains MCP `get_file_problems` on every new Java test file: zero ERROR-level problems.
- Manual grep: `rg -n "@MockBean|@SpyBean|@MockitoSpyBean|getCode\\(" ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation ai-agent/ai-agent/src/test/java/com/vn/agent/tools/link ai-agent/ai-agent/src/test/java/com/vn/agent/guard` returns no matches.
</verification>

<success_criteria>
- TEST-12 green: commit-unknown mutation writes exactly one `AiAuditEvent` with `AiToolCallOutcome.COMMIT_FAILED`; save rollback writes durable `ERROR`; post-COMMITTED audit-write failure is absorbed by `safeWriteAudit` and leaves the idempotency row `COMMITTED`.
- Related-write security tests prove denied related writes do not save/remove host data and composition/orphanRemoval removal is rejected before any delete-capable operation.
- BuiltInLinkToolsOpacityTest green: hidden entity returns `unknown_entity`, not `access_denied`.
- MutationErrorTranslatorTest covers all six stable codes plus legacy converter-code remapping and PII-no-leak fallback.
- AgentSystemPromptRulesComposerTest proves mutation prompt rules are conditional and do not reference `prepare_form_draft`.
- ToolNamePatternProviderMutationToolsTest proves output scanner covers read, link, and mutation tool names while excluding `delete_record`.
</success_criteria>

<output>
After completion, create `.planning/phases/11-mutation-capable-built-in-tools/11-11-SUMMARY.md`.
</output>
