---
phase: 11-mutation-capable-built-in-tools
plan: 10
type: execute
wave: 10
depends_on:
  - 11-09-PLAN.md
files_modified:
  - ai-agent/ai-agent/src/test/java/com/vn/agent/AITestConfiguration.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/fixture/MutationTestFixture.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/fixture/MutationParentFixture.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/fixture/MutationChildFixture.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/fixture/MutationLinkedParentFixture.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/fixture/MutationLinkedChildFixture.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/MutationToolTestContext.java
  - ai-agent/ai-agent/src/test/resources/com/vn/agent/test_liquibase/010-mutation-test-fixture.xml
  - ai-agent/ai-agent/src/test/resources/com/vn/agent/test_liquibase/test-main-changelog.xml
  - ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/fixture/MutationTestFixtureTestRole.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/AgentToolCallbacksDefaultConfigTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/AgentToolCallbacksMutationEnabledTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/AgentToolCallbacksMutationAuditOwnershipTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/BuiltInMutationToolsAccessGatingTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/BuiltInMutationToolsMassAssignmentTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/BuiltInMutationToolsRelationshipExposureTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/BuiltInMutationToolsGuardReceivesCoercedAttributesTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/BuiltInMutationToolsAuditArgumentsTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/BuiltInMutationToolsIdempotencyReplayTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/BuiltInMutationToolsIdempotencyViolationTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/MutationIntentRepositoryReservationTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/MutationRequestHashCanonicalizationTest.java
autonomous: true
requirements:
  - TEST-10
  - TEST-11
  - TEST-13
must_haves:
  truths:
    - "TEST-10 asserts per-attribute denial, hidden relationship target exposure, and mass-assignment attributes (id/version/audit fields) block before MutationSaveExecutor.save/saveAll is called"
    - "A MutationGuard capture test proves MutationIntent.attributes contains post-coercion typed values, not raw LLM strings"
    - "A mutation audit arguments test proves argumentsJson contains full tool args (entityName, id/idempotencyKey, attributes or relationship/relatedId) with sensitive attributes hashed"
    - "TEST-11 asserts same idempotencyKey and same request hash returns a fresh replay DTO with outcome=IDEMPOTENT_REPLAY and exactly one AiMutationIntent row"
    - "Idempotency tests run without an outer test transaction; replay is observed after the first tool invocation has returned and finalized the reservation"
    - "A same idempotencyKey with a different request shape returns error=idempotency_violation before host save"
    - "MutationIntentRepositoryReservationTest proves a reserved PENDING row makes a duplicate reservation return PENDING instead of allowing a second host write"
    - "Request hash tests prove canonicalization is key-order independent and stable for nested maps, numeric values, booleans, and nulls"
    - "TEST-13 asserts AgentToolCallbacks.forCurrentUser exposes 8 callbacks by default, 12 when mutation is enabled, and never exposes delete_record"
    - "AgentToolCallbacksMutationAuditOwnershipTest asserts mutation callbacks are not wrapped by ToolCallbackAuditDecorator and are wrapped by MutationToolCallbackBoundaryDecorator"
    - "MutationTestFixture plus concrete composition MutationParentFixture/MutationChildFixture and non-composition MutationLinkedParentFixture/MutationLinkedChildFixture relationship fixtures are registered in Jmix metadata for TEST-10..12"
    - "All direct BuiltInMutationTools tests use MutationToolTestContext to establish both SystemAuthenticator and RunContext before invoking a tool"
    - "All tests use ToolUserError.toDto().error(); ToolUserError has no getCode() method"
    - "Mockito test doubles use Spring's @MockitoBean, not deprecated @MockBean / @SpyBean; tests avoid @MockitoSpyBean"
  artifacts:
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/fixture/MutationTestFixture.java"
      provides: "Concrete @JmixEntity fixture used by TEST-10..12"
      contains: "@JmixEntity"
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/fixture/MutationParentFixture.java"
      provides: "Concrete composition parent entity used to test delete-capable removal rejection"
      contains: "@Composition"
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/fixture/MutationChildFixture.java"
      provides: "Concrete composition child entity with child-side to-one inverse used by rejection tests"
      contains: "MutationParentFixture"
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/fixture/MutationLinkedParentFixture.java"
      provides: "Concrete non-composition parent entity used by successful related-write tests"
      contains: "MutationLinkedChildFixture"
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/fixture/MutationLinkedChildFixture.java"
      provides: "Concrete non-composition child entity with child-side to-one inverse used by successful related-write tests"
      contains: "MutationLinkedParentFixture"
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/AITestConfiguration.java"
      provides: "Test Jmix module registration includes the mutation fixture package"
      contains: "MutationTestFixture"
    - path: "ai-agent/ai-agent/src/test/resources/com/vn/agent/test_liquibase/test-main-changelog.xml"
      provides: "Main-store test changelog for TEST_MUTATION_* host fixture tables only"
      contains: "010-mutation-test-fixture.xml"
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/BuiltInMutationToolsIdempotencyReplayTest.java"
      provides: "TEST-11 replay proof"
      contains: "IDEMPOTENT_REPLAY"
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/AgentToolCallbacksDefaultConfigTest.java"
      provides: "TEST-13 default-off callback proof"
      contains: "forCurrentUser"
  key_links:
    - from: "TEST-10..12 direct tool calls"
      to: "CurrentAuthentication + RunContext"
      via: "MutationToolTestContext.withMutationRun(\"admin\", ...)"
      pattern: "withMutationRun"
    - from: "TEST-11"
      to: "MutationIntentRepository.reserveOrReplay / markCommitted"
      via: "two separate non-transactional tool invocations"
      pattern: "reserveOrReplay"
---

<objective>
Land the core Phase 11 regression tests after incorporating REVIEWS.md blockers. This plan covers the test fixture, access gating, idempotency reservation/replay/violation, callback exposure, and callback audit ownership. Plan 11-11 covers TEST-12 and supporting regressions so each test execution chunk stays within GSD scope budget.

Purpose: these tests are the first execution gate for the mutation tool surface. They must prove the corrected reservation-based idempotency design, single callback audit ownership, operation-specific access gating, and default-off callback behavior. They must also compile against the current codebase APIs: `ToolUserError.toDto().error()`, static `RunContext` accessors, and Spring's current Mockito bean override annotations.

Output: a test-only Jmix entity fixture and focused tests under `ai-agent/ai-agent/src/test/java`, all runnable with Gradle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-mutation-capable-built-in-tools/11-CONTEXT.md
@.planning/phases/11-mutation-capable-built-in-tools/11-RESEARCH.md
@.planning/phases/11-mutation-capable-built-in-tools/11-PATTERNS.md
@.planning/phases/11-mutation-capable-built-in-tools/11-REVIEWS.md

@ai-agent/ai-agent/src/test/resources/com/vn/agent/test-app.properties
@ai-agent/ai-agent/src/test/java/com/vn/agent/tools/BuiltInDataToolsReadOnlyTest.java
@ai-agent/ai-agent/src/test/java/com/vn/agent/audit/AuditDurabilityTest.java
@ai-agent/ai-agent/src/test/java/com/vn/agent/exposure/LlmExposurePolicyIntegrationTest.java
@ai-agent/ai-agent/src/test/java/com/vn/agent/filter/StructuredFilterConditionMapperTest.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/tools/ToolUserError.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/tools/ToolErrorDto.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiAuditEvent.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallOutcome.java

<interfaces>
Existing facts verified during review:
- `src/test/resources/com/vn/agent/test-app.properties` wires the agentstore changelog separately.
- Test fixture tables must not replace or duplicate the agentstore changelog. Use a test main-store changelog for host fixture tables only, and set only `main.liquibase.change-log` for TEST-10..12 classes. Leave `agentstore.liquibase.change-log` on the existing agentstore changelog path.
- Direct tool tests must run under a Jmix authentication context. Follow `SystemAuthenticator.withUser(...)` patterns from existing integration tests.
- `ToolUserError` exposes `toDto()`; assertions use `toolUserError.toDto().error()`, not `getCode()`.
- `AiAuditEvent.getOutcome()` returns `AiToolCallOutcome`; assertions compare enum values or use `getOutcomeRaw()` if a raw string is required.
- Spring test doubles use `org.springframework.test.context.bean.override.mockito.MockitoBean` and `MockitoSpyBean`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 0: Create or reuse mutation test fixture and wire main-store test Liquibase safely</name>
  <files>
    ai-agent/ai-agent/src/test/java/com/vn/agent/AITestConfiguration.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/fixture/MutationTestFixture.java,
    ai-agent/ai-agent/src/test/resources/com/vn/agent/test_liquibase/010-mutation-test-fixture.xml,
    ai-agent/ai-agent/src/test/resources/com/vn/agent/test_liquibase/test-main-changelog.xml,
    ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/fixture/MutationTestFixtureTestRole.java
  </files>
  <read_first>
    - ai-agent/ai-agent/src/test/java/com/vn/agent/AITestConfiguration.java (current test Jmix module; it currently registers the add-on module and must explicitly see the fixture entity)
    - ai-agent/ai-agent/src/test/resources/com/vn/agent/test-app.properties (do not lose existing agentstore changelog wiring)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/exposure/AiExposureRule.java (Jmix entity shape)
    - ai-agent/ai-agent/src/test/java/com/vn/agent/exposure/LlmExposurePolicyIntegrationTest.java (SystemAuthenticator and role setup patterns)
    - Search first: `rg -n "@JmixEntity" ai-agent/ai-agent/src/test/java`
  </read_first>
  <action>
1. Search for existing test entities first. Reuse only if they already provide scalar mutation fields, a composition relationship for rejection coverage, and a non-composition parent/child relationship matching the Phase 11 v1.1 supported related-write matrix. If not, create these five entities under `com.vn.agent.tools.mutation.fixture`:

   `MutationTestFixture`:
   - `@JmixEntity`
   - `@Entity(name = "test_MutationTestFixture")`
   - UUID id with `@JmixGeneratedValue`
   - `@Version`
   - `@InstanceName`
   - string attributes `name`, `secret`
   - integer attribute `priority` so guard tests can distinguish raw `"7"` from coerced `Integer 7`

   `MutationParentFixture`:
   - `@JmixEntity`
   - `@Entity(name = "test_MutationParentFixture")`
   - UUID id with `@JmixGeneratedValue`, `@Version`, `@InstanceName`
   - string attribute `name`
   - collection relationship `children` to `MutationChildFixture`
   - `@Composition` + JPA `@OneToMany(mappedBy = "parent", orphanRemoval = true)` so composition removal rejection is testable

   `MutationChildFixture`:
   - `@JmixEntity`
   - `@Entity(name = "test_MutationChildFixture")`
   - UUID id with `@JmixGeneratedValue`, `@Version`, `@InstanceName`
   - string attribute `name`
   - child-side to-one inverse `parent` with `@ManyToOne(fetch = FetchType.LAZY)` to `MutationParentFixture`

   `MutationLinkedParentFixture`:
   - `@JmixEntity`
   - `@Entity(name = "test_MutationLinkedParentFixture")`
   - UUID id with `@JmixGeneratedValue`, `@Version`, `@InstanceName`
   - string attribute `name`
   - non-composition collection relationship `linkedChildren` to `MutationLinkedChildFixture`
   - JPA `@OneToMany(mappedBy = "linkedParent")` WITHOUT `@Composition`, cascade remove, or orphanRemoval

   `MutationLinkedChildFixture`:
   - `@JmixEntity`
   - `@Entity(name = "test_MutationLinkedChildFixture")`
   - UUID id with `@JmixGeneratedValue`, `@Version`, `@InstanceName`
   - string attribute `name`
   - child-side to-one inverse `linkedParent` with `@ManyToOne(fetch = FetchType.LAZY)` to `MutationLinkedParentFixture`

   These fixtures are deliberately concrete: related-write tests must not mock relationship metadata, because Phase 11's risk is metadata-driven relationship handling.

2. Ensure the fixture is registered in Jmix metadata, not only as a JPA table:
   - Update `AITestConfiguration` or add a small imported test Jmix module configuration so the package containing `MutationTestFixture` is included in Jmix scanning.
   - Do not rely on Liquibase alone; `DataManager.create(...)` and `metadata.getClass(...)` must work for all five fixture entities.
   - Add explicit smoke assertions in the first mutation integration test (or a tiny fixture test) before using the tool:
     `metadata.getClass("test_MutationTestFixture")`, `metadata.getClass("test_MutationParentFixture")`, `metadata.getClass("test_MutationChildFixture")`, `metadata.getClass("test_MutationLinkedParentFixture")`, and `metadata.getClass("test_MutationLinkedChildFixture")`.

3. Create `010-mutation-test-fixture.xml` with all five fixture tables:
   - table `TEST_MUTATION_FIXTURE` columns `ID`, `VERSION`, `NAME`, `SECRET`, `PRIORITY`
   - table `TEST_MUTATION_PARENT_FIXTURE` columns `ID`, `VERSION`, `NAME`
   - table `TEST_MUTATION_CHILD_FIXTURE` columns `ID`, `VERSION`, `NAME`, `PARENT_ID`
   - table `TEST_MUTATION_LINKED_PARENT_FIXTURE` columns `ID`, `VERSION`, `NAME`
   - table `TEST_MUTATION_LINKED_CHILD_FIXTURE` columns `ID`, `VERSION`, `NAME`, `LINKED_PARENT_ID`
   - FK `FK_TEST_MUT_CHILD_PARENT` from child `PARENT_ID` to parent `ID`
   - FK `FK_TEST_MUT_LINKED_CHILD_PARENT` from linked child `LINKED_PARENT_ID` to linked parent `ID`

4. Create `MutationToolTestContext` test helper:
   - wraps `SystemAuthenticator.withUser(user, ...)`
   - sets a deterministic `RunContext` with run id, conversation id, and root audit id before each direct tool call
   - clears/restores `RunContext` in `finally`
   - exposes `withMutationRun(String username, Supplier<T> action)` so tests cannot forget either context

5. Create `test-main-changelog.xml` that includes ONLY the fixture changelog for the main store. Do not include the agentstore changelog here; `agentstore.liquibase.change-log` already runs separately.

```xml
<databaseChangeLog ...>
    <include file="com/vn/agent/test_liquibase/010-mutation-test-fixture.xml"/>
</databaseChangeLog>
```

6. Do not edit `test-app.properties` unless the current repo convention requires it. Prefer adding this property only to TEST-10..12 classes:

```java
@SpringBootTest(properties = {
        "ai-agent.tools.mutation.enabled=true",
        "main.liquibase.change-log=com/vn/agent/test_liquibase/test-main-changelog.xml"
})
```

7. Add `MutationTestFixtureTestRole` only if a non-admin test user is used. If tests run as `admin`, still keep the role as documentation and a future non-admin fixture.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:compileTestJava</automated>
  </verify>
  <acceptance_criteria>
    - `MutationTestFixture.java`, `MutationParentFixture.java`, and `MutationChildFixture.java` exist or reused fixtures are named in TEST-10..12 comments.
    - Every fixture has `@JmixEntity`, UUID `@JmixGeneratedValue`, `@Version`, and `@InstanceName`.
    - Scalar fixture exposes at least `name` and `secret` string attributes and `priority` integer attribute.
    - Parent/child fixtures expose a concrete composition parent collection + child to-one inverse; parent relationship includes `@Composition` and JPA `orphanRemoval=true` so tests can prove v1.1 rejects delete-capable relationship removal before host save/remove.
    - Linked parent/child fixtures expose a concrete non-composition parent collection + child to-one inverse; related-write success tests use this fixture for successful `add_related_record` and `remove_related_record`.
    - `MutationToolTestContext.java` exists and contains `withMutationRun`.
    - `AITestConfiguration.java` or a test-only Jmix module configuration references all fixture classes / their package so the entities are in the Jmix metamodel.
    - At least one TEST-10..12 class asserts all five metadata classes before invoking mutation tools.
    - `test-main-changelog.xml` includes `010-mutation-test-fixture.xml`.
    - `test-main-changelog.xml` does NOT include `agentstore-changelog.xml`.
    - `010-mutation-test-fixture.xml` creates all five fixture tables and both parent-child foreign keys.
    - TEST-10..12 set `main.liquibase.change-log=com/vn/agent/test_liquibase/test-main-changelog.xml` without overriding `agentstore.liquibase.change-log`.
    - `./gradlew :ai-agent:compileTestJava` exits 0.
  </acceptance_criteria>
  <done>Mutation tests have a real Jmix entity and a main-store table without breaking agentstore schema setup.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 1: TEST-13 callback shape plus mutation audit ownership</name>
  <files>
    ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/AgentToolCallbacksDefaultConfigTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/AgentToolCallbacksMutationEnabledTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/AgentToolCallbacksMutationAuditOwnershipTest.java
  </files>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/tools/AgentToolCallbacks.java
    - ai-agent/ai-agent/src/main/java/com/vn/agent/audit/ToolCallbackAuditDecorator.java
    - ai-agent/ai-agent/src/main/java/com/vn/agent/audit/MutationToolCallbackBoundaryDecorator.java
    - ai-agent/ai-agent/src/main/java/com/vn/agent/tools/link/BuiltInLinkTools.java
    - ai-agent/ai-agent/src/main/java/com/vn/agent/tools/mutation/BuiltInMutationTools.java
    - .planning/phases/11-mutation-capable-built-in-tools/11-09-PLAN.md
  </read_first>
  <action>
Create three Spring tests:

1. `AgentToolCallbacksDefaultConfigTest`
   - Do not set `ai-agent.tools.mutation.enabled`.
   - Call `agentToolCallbacks.forCurrentUser()`.
   - Extract names using the actual Spring AI callback accessor used by the repo (`callback.getToolDefinition().name()` unless source shows a different accessor).
   - Assert size 8 only if the local app context has no extra `ToolContributor` callbacks. Also assert names include the six read tools and two link tools.
   - Assert names do not contain `create_record`, `update_record`, `add_related_record`, `remove_related_record`, or `delete_record`.

2. `AgentToolCallbacksMutationEnabledTest`
   - Set `ai-agent.tools.mutation.enabled=true`.
   - Assert size 12 only if no local contributors are present.
   - Assert names include six read tools, two link tools, and four mutation tools.
   - Assert names do not contain `delete_record`.

3. `AgentToolCallbacksMutationAuditOwnershipTest`
   - Set `ai-agent.tools.mutation.enabled=true`.
   - Call `forCurrentUser()`.
   - Assert callbacks named `create_record`, `update_record`, `add_related_record`, and `remove_related_record` are not instances of `ToolCallbackAuditDecorator`.
   - Assert those same mutation callbacks are instances of `MutationToolCallbackBoundaryDecorator`.
   - Invoke one mutation callback through the wrapper with missing/malformed arguments that fail before the method body can self-audit; assert exactly one audit row is written with outcome `ERROR`.
   - Assert at least one non-mutation callback is a `ToolCallbackAuditDecorator`, proving the test can detect wrapping.

Keep these tests name-based first, with count assertions as secondary diagnostics. This keeps the TEST-13 contract while making contributor changes easier to diagnose.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:test --tests "com.vn.agent.tools.mutation.AgentToolCallbacksDefaultConfigTest" --tests "com.vn.agent.tools.mutation.AgentToolCallbacksMutationEnabledTest" --tests "com.vn.agent.tools.mutation.AgentToolCallbacksMutationAuditOwnershipTest"</automated>
  </verify>
  <acceptance_criteria>
    - Default-config test calls `agentToolCallbacks.forCurrentUser()`.
    - Default-config test contains `assertThat(names).doesNotContain("create_record", "update_record", "add_related_record", "remove_related_record", "delete_record")`.
    - Enabled test contains `assertThat(names).contains("create_record", "update_record", "add_related_record", "remove_related_record")`.
    - Enabled test contains `assertThat(names).doesNotContain("delete_record")`.
    - Audit ownership test references `ToolCallbackAuditDecorator`.
    - Audit ownership test references `MutationToolCallbackBoundaryDecorator`.
    - Audit ownership test verifies malformed callback-level invocation writes exactly one `AiAuditEvent` row with outcome `ERROR`.
    - Audit ownership test asserts mutation audit wrappers are absent and streaming-event wrappers are present.
    - No test file contains `@MockBean`, `@SpyBean`, or `@MockitoSpyBean`.
    - Targeted Gradle tests exit 0.
  </acceptance_criteria>
  <done>TEST-13 and the no-duplicate-audit review blocker are locked by callback-level tests.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: TEST-10 access gating and TEST-11 idempotency replay/violation/reservation tests</name>
  <files>
    ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/BuiltInMutationToolsAccessGatingTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/BuiltInMutationToolsMassAssignmentTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/BuiltInMutationToolsRelationshipExposureTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/BuiltInMutationToolsGuardReceivesCoercedAttributesTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/BuiltInMutationToolsAuditArgumentsTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/BuiltInMutationToolsIdempotencyReplayTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/BuiltInMutationToolsIdempotencyViolationTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/MutationIntentRepositoryReservationTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/MutationRequestHashCanonicalizationTest.java
  </files>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/tools/mutation/BuiltInMutationTools.java
    - ai-agent/ai-agent/src/main/java/com/vn/agent/tools/mutation/MutationIntentRepository.java
    - ai-agent/ai-agent/src/main/java/com/vn/agent/tools/mutation/MutationSaveExecutor.java
    - ai-agent/ai-agent/src/main/java/com/vn/agent/tools/ToolUserError.java
    - ai-agent/ai-agent/src/test/java/com/vn/agent/exposure/LlmExposurePolicyIntegrationTest.java
  </read_first>
  <action>
Create nine tests:

1. `BuiltInMutationToolsAccessGatingTest`
   - Use `@SpringBootTest(properties = {"ai-agent.tools.mutation.enabled=true", "main.liquibase.change-log=com/vn/agent/test_liquibase/test-main-changelog.xml"})`.
   - Use `@MockitoBean AccessManager` and `@MockitoBean MutationSaveExecutor`; a mock is enough because this test verifies the executor is never called.
   - Run the tool call inside `mutationToolTestContext.withMutationRun("admin", () -> ...)`, not raw `systemAuthenticator.withUser(...)`, so `RunContext` is also present.
   - Configure the `AccessManager` answer so `EntityAttributeContext` for attribute `secret` denies modify and other contexts allow the operation unless the test intentionally denies them.
   - Call `createRecord("test_MutationTestFixture", Map.of("secret", "value"), idempotencyKey)`.
   - Assert JSON contains `"access_denied"`.
   - Verify `mutationSaveExecutor.save(...)` and `saveAll(...)` are never called.

2. `BuiltInMutationToolsMassAssignmentTest`
   - Call `createRecord("test_MutationTestFixture", Map.of("id", uuid, "name", "Alice"), key)` and assert `"validation_failed"`.
   - Repeat for `version`, `createdBy`, and a read-only/transient/calculated fixture property if present.
   - Verify `mutationSaveExecutor.save(...)` and `saveAll(...)` are never called.
   - Assert the error guidance points to writable attributes / `describe_entity`.

3. `BuiltInMutationToolsRelationshipExposureTest`
   - Configure `LlmExposurePolicy` / exposure rules so the child/target fixture entity is hidden from the LLM while Jmix `AccessManager` would otherwise allow read/update.
   - Call a to-one relationship assignment through `create_record` or `add_related_record`.
   - Assert JSON contains `"access_denied"` and the executor is never called.
   - This test proves relationship target/child entities are gated by `LlmExposurePolicy`, not only by Jmix `AccessManager`.

4. `BuiltInMutationToolsGuardReceivesCoercedAttributesTest`
   - Register/mock a `MutationGuard` that captures `MutationIntent`.
   - Call `createRecord("test_MutationTestFixture", Map.of("priority", "7"), key)`.
   - Assert the captured `MutationIntent.attributes().get("priority")` is an `Integer` with value `7`, not the raw String `"7"`.
   - Verify the guard runs before `MutationSaveExecutor.save(...)`.

5. `BuiltInMutationToolsIdempotencyReplayTest`
   - Do not annotate the test method or class with `@Transactional`; do not use `@Commit`.
   - Invoke `createRecord` once, wait for the method to return, then invoke it again with the same idempotency key and same attributes.
   - Parse JSON with `ObjectMapper`.
   - Assert first outcome is `SUCCESS`.
   - Assert second outcome is `IDEMPOTENT_REPLAY`.
   - Assert second `entityId` equals the first `entityId`.
   - Use `UnconstrainedDataManager` to query `AiMutationIntent` and assert exactly one row for the `(toolName, idempotencyKey, userUsername)` tuple.
   - Clean persisted fixture rows and dedup rows in `@AfterEach` with `UnconstrainedDataManager`.

6. `BuiltInMutationToolsIdempotencyViolationTest`
   - First call succeeds with idempotency key `K` and attributes `{"name":"Alice"}`.
   - Second call uses the same key `K` but attributes `{"name":"Bob"}`.
   - Assert second response contains `"idempotency_violation"`.
   - Verify no second fixture row was created and the dedup row count remains one.
   - Assert the correction guidance says to use a fresh idempotency key when changing values.

7. `MutationIntentRepositoryReservationTest`
   - Call `reserveOrReplay("create_record", key, "admin", conversationId, requestHash, ttl)` once and assert `ReservationState.RESERVED`.
   - Call it again with the same key/hash before `markCommitted` and assert `ReservationState.PENDING`.
   - Call it with the same key but different hash and assert `ReservationState.VIOLATION`.
   - Add a real race test with two threads, `CountDownLatch`, and the same `(toolName, idempotencyKey, userUsername, requestHash)`: exactly one call returns `RESERVED`, the loser returns `PENDING`, and only one `AiMutationIntent` row exists. This proves the TransactionTemplate unique-race handling from Plan 11-05 catches commit-time duplicate-key failures.

8. `MutationRequestHashCanonicalizationTest`
   - Exercise the same request-hash helper used by `BuiltInMutationTools`.
   - Assert `{"name":"Alice","priority":7}` and `{"priority":7,"name":"Alice"}` produce the same hash.
   - Assert nested map key order is stable.
   - Assert semantically different numeric/string values (`7` vs `"7"`) do not collapse unless the helper intentionally hashes post-coercion values; if post-coercion values are used, assert both paths are converted before hashing.
   - Assert nulls and booleans are represented deterministically.

9. `BuiltInMutationToolsAuditArgumentsTest`
   - For successful create/update and one related-write call, parse `AiAuditEvent.argumentsJson`.
   - Assert it includes the full tool argument envelope (`entityName`, `id` when applicable, `idempotencyKey`, nested `attributes`, `relationship`, and `relatedId`) and hashes sensitive `secret` values.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:test --tests "com.vn.agent.tools.mutation.BuiltInMutationToolsAccessGatingTest" --tests "com.vn.agent.tools.mutation.BuiltInMutationToolsMassAssignmentTest" --tests "com.vn.agent.tools.mutation.BuiltInMutationToolsRelationshipExposureTest" --tests "com.vn.agent.tools.mutation.BuiltInMutationToolsGuardReceivesCoercedAttributesTest" --tests "com.vn.agent.tools.mutation.BuiltInMutationToolsAuditArgumentsTest" --tests "com.vn.agent.tools.mutation.BuiltInMutationToolsIdempotencyReplayTest" --tests "com.vn.agent.tools.mutation.BuiltInMutationToolsIdempotencyViolationTest" --tests "com.vn.agent.tools.mutation.MutationIntentRepositoryReservationTest" --tests "com.vn.agent.tools.mutation.MutationRequestHashCanonicalizationTest"</automated>
  </verify>
  <acceptance_criteria>
    - Access gating test contains `@MockitoBean` and does NOT contain `@MockitoSpyBean`.
    - Access gating test contains `mutationToolTestContext.withMutationRun` and does NOT call `systemAuthenticator.withUser` directly.
    - Access gating test verifies `mutationSaveExecutor.save` and `saveAll` are never called.
    - Mass-assignment test asserts `id`, `version`, and audit/system fields return `validation_failed` before any executor call.
    - Relationship exposure test hides a child/target entity through `LlmExposurePolicy` and asserts `access_denied` before any executor call.
    - Guard capture test asserts `MutationIntent.attributes()` contains typed/coerced values.
    - Replay test contains no `@Transactional` and no `@Commit`.
    - Replay test contains `objectMapper.readTree`.
    - Replay test asserts `SUCCESS`, `IDEMPOTENT_REPLAY`, equal `entityId`, and `assertThat(dedupRows).hasSize(1)`.
    - Violation test asserts `idempotency_violation`.
    - Reservation test asserts `ReservationState.RESERVED`, `ReservationState.PENDING`, and `ReservationState.VIOLATION`.
    - Reservation race test uses two concurrent callers and proves one `RESERVED`, one `PENDING`, and one database row.
    - Request-hash canonicalization test covers key-order independence, nested maps, numbers, booleans, and nulls.
    - A repository or tool test asserts COMMIT_UNKNOWN/PENDING does not allow a duplicate host write.
    - A success-path audit test parses `AiAuditEvent.argumentsJson` and asserts it includes full tool args: `entityName`, `idempotencyKey`, and nested `attributes` for create/update or `relationship` + `relatedId` for related writes.
    - No test file calls `ToolUserError.getCode()`.
    - Targeted Gradle tests exit 0.
  </acceptance_criteria>
  <done>TEST-10 and TEST-11 prove the corrected gating and idempotency semantics from REVIEWS.md.</done>
</task>

</tasks>

<verification>
- `./gradlew :ai-agent:compileTestJava` exits 0.
- Targeted Gradle commands listed in each task exit 0.
- JetBrains MCP `get_file_problems` on every new Java test file: zero ERROR-level problems.
- Manual grep: `rg -n "@Commit|@MockBean|@SpyBean|@MockitoSpyBean|getCode\\(" ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation ai-agent/ai-agent/src/test/java/com/vn/agent/tools/link ai-agent/ai-agent/src/test/java/com/vn/agent/guard` returns no matches.
</verification>

<success_criteria>
- TEST-10 green: per-attribute denial, hidden relationship target exposure, and mass-assignment rejection block before any host mutation save/saveAll.
- TEST-11 green: replay returns the same result with `IDEMPOTENT_REPLAY`; one dedup row only; same-key/different-shape returns `idempotency_violation`; pending or COMMIT_UNKNOWN duplicate reservation returns `PENDING`/`concurrent_modification` and never runs a second host write.
- TEST-13 green: callback set is default-off for mutation, enabled only with config, never exposes `delete_record`, and mutation callbacks are wrapped only by `MutationToolCallbackBoundaryDecorator`, not `ToolCallbackAuditDecorator`.
</success_criteria>

<output>
After completion, create `.planning/phases/11-mutation-capable-built-in-tools/11-10-SUMMARY.md`.
</output>
