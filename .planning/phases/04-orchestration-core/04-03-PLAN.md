---
phase: 04-orchestration-core
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditWriter.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditListenerFanOut.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditAdvisor.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/audit/ToolCallbackAuditDecorator.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/audit/ToolCallAdvisorBuilderProbe.java
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties
autonomous: true
requirements: [AUD-01, AUD-02, AUD-04, AUD-05, SPI-06]
must_haves:
  truths:
    - "AuditWriter is the only @Transactional(REQUIRES_NEW) surface; advisor + decorator never self-invoke transactional methods"
    - "AuditAdvisor sits at Ordered.HIGHEST_PRECEDENCE and writes pre/post chat audit rows, capturing latency and errorClass; reads runId from advisor context key audit.runId (set by service layer) and only generates a new UUID as a defensive fallback (B8)"
    - "ToolCallbackAuditDecorator wraps every ToolCallback and writes a per-tool-call AiToolCallAudit row that survives tool-method rollback"
    - "AuditListenerFanOut iterates AuditListener beans inside afterCommit; one throwing listener does not block others or fail the chat"
    - "ToolCallAdvisor.Builder method for disabling internal conversation history is verified against the Spring AI 1.1.4 jar before wiring (OQ-1 closed)"
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditWriter.java"
      provides: "REQUIRES_NEW audit writes + afterCommit fan-out registration"
      contains: "REQUIRES_NEW"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditListenerFanOut.java"
      provides: "Per-listener try/catch fan-out to AuditListener beans"
      contains: "List<AuditListener>"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditAdvisor.java"
      provides: "CallAdvisor that writes chat-level pre/post audit and seeds RunContext"
      contains: "implements CallAdvisor"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/audit/ToolCallbackAuditDecorator.java"
      provides: "ToolCallback wrapper that writes per-tool-call audit rows"
      contains: "implements ToolCallback"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/audit/ToolCallAdvisorBuilderProbe.java"
      provides: "OQ-1 closure: documents verified Builder method name for disabling internal memory"
      contains: "ToolCallAdvisor"
  key_links:
    - from: "AuditAdvisor"
      to: "AuditWriter"
      via: "constructor injection (NOT this.-self-call)"
      pattern: "auditWriter\\.writeChat(Pre|Post)"
    - from: "AuditWriter.writeXxx"
      to: "AuditListenerFanOut.fireToolCallAudited"
      via: "TransactionSynchronizationManager.registerSynchronization → afterCommit"
      pattern: "registerSynchronization"
    - from: "ToolCallbackAuditDecorator"
      to: "RunContext"
      via: "thread-local read of current runId"
      pattern: "RunContext\\.get\\(\\)"
---

<objective>
Build the Phase 4 audit pipeline: a single proxied AuditWriter bean that owns all REQUIRES_NEW audit writes; a chat-level AuditAdvisor (outermost CallAdvisor at Ordered.HIGHEST_PRECEDENCE) that records pre/post rows and seeds RunContext; a ToolCallbackAuditDecorator that wraps every MethodToolCallback to write per-tool-call audit rows that survive tool-side rollback; and an AuditListenerFanOut that fires AuditListener notifications inside afterCommit with per-listener exception suppression.

Purpose: Implements AUD-01 (chat pre/post audit), AUD-02 (REQUIRES_NEW durability), AUD-04 (per-tool audit rows), AUD-05 (afterCommit listener fan-out), SPI-06 (AuditListener fan-out contract). Closes RESEARCH.md OQ-1 by verifying the ToolCallAdvisor.Builder memory-disable method name against the actual 1.1.4 jar before any wiring depends on it.

Output: Four production classes under com.vn.agent.audit + a probe class documenting the OQ-1 resolution + i18n keys for any audit-side error messages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-orchestration-core/04-CONTEXT.md
@.planning/phases/04-orchestration-core/04-RESEARCH.md
@.planning/phases/04-orchestration-core/04-AI-SPEC.md
@.planning/phases/04-orchestration-core/04-PATTERNS.md

@ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallAudit.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/spi/AuditListener.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/tools/AgentToolCallbacks.java

<interfaces>
<!-- Existing entity (extended in 04-01) -->
com.vn.agent.entity.AiToolCallAudit fields after 04-01 (VERIFIED FROM SOURCE 2026-04-20):
  Phase 2 baseline (DO NOT use legacy field names — these are the real ones):
  - id (UUID), version (Integer)
  - conversation (AiConversation, @ManyToOne LAZY) — NOT a UUID; resolve via DataManager.load(AiConversation.class).id(uuid).one()
  - userUsername (String, 255) — caller identity (NOT userId, NOT createdBy)
  - toolName (String, 128, @NotNull)
  - argumentsJson (String, @Lob) — NOT inputJson
  - resultSummary (String, @Lob) — NOT outputJson
  - outcome (String, 16, @NotNull) — coerced via setOutcome(AiToolCallOutcome enum); values: SUCCESS, BLOCKED, ERROR
  - denialReason (String, 512)
  - latencyMs (Long)
  - startedAt (OffsetDateTime, @NotNull) — NOT createdAt
  - finishedAt (OffsetDateTime)
  Phase 4 additions from 04-01:
  - runId (UUID)
  - kind (String, max 8): "CHAT" | "TOOL"
  - phase (String, max 8): "PRE" | "POST"
  - promptHash (String, 64)
  - errorClass (String, 255)
  NO fields named userId / createdBy / createdAt / inputJson / outputJson / success / errorMessage exist.
  Setters present: setConversation(AiConversation), setUserUsername(String), setToolName(String),
                   setArgumentsJson(String), setResultSummary(String), setOutcome(AiToolCallOutcome),
                   setDenialReason(String), setLatencyMs(Long), setStartedAt(OffsetDateTime),
                   setFinishedAt(OffsetDateTime), setRunId(UUID), setKind(String), setPhase(String),
                   setPromptHash(String), setErrorClass(String).
  
com.vn.agent.entity.AiConversation (VERIFIED 2026-04-20):
  Ownership field is `createdBy` (String, @NotNull). NO `userId` field exists. JPQL alias:
  `select c from ai_AiConversation c where c.createdBy = :owner`.

com.vn.agent.entity.AiToolCallOutcome (Phase 2 enum):
  Values: SUCCESS("SUCCESS"), BLOCKED("BLOCKED"), ERROR("ERROR"). EnumClass<String>.

com.vn.agent.spi.AuditListener:
  void onToolCallAudited(UUID auditId);

com.vn.agent.orchestration.RunContext (from 04-02):
  static void set(UUID runId);
  static UUID get();           // may return null outside a chat call
  static void clear();

Spring AI 1.1.4 advisor surface (verified):
  org.springframework.ai.chat.client.advisor.api.CallAdvisor extends Advisor
    ChatClientResponse adviseCall(ChatClientRequest req, CallAdvisorChain chain);
  org.springframework.core.Ordered
    int getOrder();   // HIGHEST_PRECEDENCE = Integer.MIN_VALUE
  org.springframework.ai.tool.ToolCallback
    ToolDefinition getToolDefinition();
    String call(String toolInput);
    String call(String toolInput, ToolContext toolContext);   // overload may exist; verify both
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify OQ-1 (ToolCallAdvisor memory-disable method) and document resolution</name>
  <files>ai-agent/ai-agent/src/main/java/com/vn/agent/audit/ToolCallAdvisorBuilderProbe.java</files>
  <read_first>
    - .planning/phases/04-orchestration-core/04-RESEARCH.md (OQ-1 section, Pitfall #1, Assumption A1)
    - .planning/phases/04-orchestration-core/04-AI-SPEC.md (advisor assembly snippet)
  </read_first>
  <action>
    OQ-1 is already RESOLVED by the planner (RESEARCH.md §"Open Questions (RESOLVED)" → OQ-1 RESOLUTION block, dated 2026-04-20). This task codifies the resolution into a tiny in-source probe class so downstream wiring (Plan 04-04 ChatClientFactory) references the verified API surface symbolically.

    **VERIFIED API SURFACE (from javap on `spring-ai-client-chat-1.1.4.jar`, class `org.springframework.ai.chat.client.advisor.ToolCallAdvisor$Builder`):**
    ```
    public T disableMemory();                                 // ← canonical disable for internal history
    public T conversationHistoryEnabled(boolean);             // ← equivalent (alias)
    public T advisorOrder(int);                               // ← order setter (NOT order(int))
    public T toolCallingManager(ToolCallingManager);
    public T streamToolCallResponses(boolean);
    public T suppressToolCallStreaming();
    public ToolCallAdvisor build();
    ```
    Internal field on `ToolCallAdvisor` itself: `private final boolean conversationHistoryEnabled` — `AdvisorOrderStructuralTest` (Plan 04-05) reflects on this field to assert the disabled state.

    **Create `ai-agent/ai-agent/src/main/java/com/vn/agent/audit/ToolCallAdvisorBuilderProbe.java`** — documentation-only class, NO runtime behavior:

    ```java
    package com.vn.agent.audit;

    /**
     * OQ-1 closure constants. RESOLVED 2026-04-20 via javap inspection of
     * spring-ai-client-chat-1.1.4.jar (class
     * org.springframework.ai.chat.client.advisor.ToolCallAdvisor$Builder).
     *
     * <p>This class has no runtime behavior. Plan 04-04 ChatClientFactory references
     * {@link #RESOLVED_BUILDER_METHOD} symbolically so any future API drift surfaces
     * as a compile-time failure rather than a silent change.
     */
    public final class ToolCallAdvisorBuilderProbe {

        /** Verified FQN of ToolCallAdvisor in 1.1.4. */
        public static final String TOOL_CALL_ADVISOR_FQN =
                "org.springframework.ai.chat.client.advisor.ToolCallAdvisor";

        /** Verified Builder method that disables the advisor's internal conversation history. */
        public static final String RESOLVED_BUILDER_METHOD = "disableMemory";

        /** Verified internal field name (used by AdvisorOrderStructuralTest reflection). */
        public static final String INTERNAL_FLAG_FIELD = "conversationHistoryEnabled";

        /** Verified order-setter method name (NOT "order"). */
        public static final String ORDER_SETTER_METHOD = "advisorOrder";

        // VERIFIED 2026-04-20 javap output (org/springframework/ai/chat/client/advisor/ToolCallAdvisor$Builder.class):
        //   public T disableMemory();
        //   public T conversationHistoryEnabled(boolean);
        //   public T advisorOrder(int);
        //   public org.springframework.ai.chat.client.advisor.ToolCallAdvisor build();

        private ToolCallAdvisorBuilderProbe() {}
    }
    ```

    **Optional Windows-bash-compatible re-verification** (only if a future BOM bump invalidates the constants — not required for this task, but documented here per Warning 13):
    ```bash
    # Glob jar from Gradle cache (works under Git Bash on Windows)
    JAR=$(find ~/.gradle/caches/modules-2/files-2.1/org.springframework.ai/spring-ai-client-chat -name 'spring-ai-client-chat-1.1.4.jar' | head -1)
    TMP=$(mktemp -d) && cd "$TMP" && unzip -q "$JAR" 'org/springframework/ai/chat/client/advisor/ToolCallAdvisor*.class'
    "/c/Program Files/Java/jdk-21/bin/javap.exe" -p 'org/springframework/ai/chat/client/advisor/ToolCallAdvisor$Builder.class' | grep -E 'public T (disableMemory|conversationHistoryEnabled|advisorOrder)'
    ```
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:compileJava -q</automated>
  </verify>
  <acceptance_criteria>
    - File exists at ai-agent/ai-agent/src/main/java/com/vn/agent/audit/ToolCallAdvisorBuilderProbe.java.
    - Class declares `public static final String RESOLVED_BUILDER_METHOD = "disableMemory";`.
    - Class declares `public static final String ORDER_SETTER_METHOD = "advisorOrder";` and `INTERNAL_FLAG_FIELD = "conversationHistoryEnabled";`.
    - Javadoc contains the literal substring `OQ-1` and the verified FQN of ToolCallAdvisor.
    - A `// VERIFIED 2026-04-20` comment block lists the javap-derived method signatures.
    - `./gradlew :ai-agent:ai-agent:compileJava -q` exits 0.
  </acceptance_criteria>
  <done>OQ-1 is closed in code (constant + javadoc) so Plan 04-04 wires the verified method name without re-investigation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement AuditWriter (sole REQUIRES_NEW surface) + AuditListenerFanOut</name>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditWriter.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditListenerFanOut.java,
    ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties,
    ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties
  </files>
  <read_first>
    - .planning/phases/04-orchestration-core/04-RESEARCH.md (Pattern 4, Pitfall #3, D-11/D-13/D-14)
    - .planning/phases/04-orchestration-core/04-AI-SPEC.md (§4 audit shape)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallAudit.java (post-04-01 fields)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/spi/AuditListener.java
  </read_first>
  <action>
    Create `com.vn.agent.audit.AuditListenerFanOut` (per D-13):
      - Annotated `@Component`.
      - Constructor-injected `List<AuditListener> listeners` (Spring auto-collects all beans; empty list when none registered).
      - SLF4J logger `Logger log = LoggerFactory.getLogger(AuditListenerFanOut.class)`.
      - Method `public void fireToolCallAudited(UUID auditId)`:
          for (AuditListener listener : listeners) {
              try { listener.onToolCallAudited(auditId); }
              catch (Throwable t) { log.warn("AuditListener {} threw on auditId={}; suppressed", listener.getClass().getName(), auditId, t); }
          }
      - JavaDoc references SPI-06 and D-13 (per-listener try/catch swallows Throwable so one bad listener cannot block others or fail the chat).

    Create `com.vn.agent.audit.AuditWriter` (per D-11/D-14, Pattern 4 verbatim shape):
      - `@Component` (proxied bean — REQUIRES_NEW works because callers use injected reference).
      - Constructor-injected: `DataManager dataManager`, `Metadata metadata`, `AuditListenerFanOut fanOut`.
      - **CRITICAL — entity field shape (VERIFIED FROM SOURCE 2026-04-20):** see `<interfaces>` block above. The entity uses `conversation` (ManyToOne to AiConversation, NOT UUID), `userUsername` (NOT userId), `argumentsJson`/`resultSummary` (NOT inputJson/outputJson), `outcome` enum (NOT success+errorMessage), `startedAt`/`finishedAt` (NOT createdAt). `toolName` and `outcome` are `@NotNull` at the DB level — for chat-level rows that have no tool, **set sentinel values**: `toolName = "<chat>"` and outcome = `AiToolCallOutcome.SUCCESS` for PRE rows; outcome = `(errorClass == null ? SUCCESS : ERROR)` for POST rows. We chose sentinels over a Liquibase changeset that drops the NOT NULL — sentinels keep the schema additive-only and avoid coupling Phase 4 to a 030-changelog rewrite.
      - Three public methods, each `@Transactional(propagation = Propagation.REQUIRES_NEW)`:

        `public UUID writeChatPre(UUID runId, String userUsername, UUID conversationId, String promptHash)`
          1. AiToolCallAudit row = metadata.create(AiToolCallAudit.class)
          2. row.setRunId(runId); row.setKind("CHAT"); row.setPhase("PRE"); row.setPromptHash(promptHash);
          3. row.setUserUsername(userUsername);
          4. if (conversationId != null) {
                 AiConversation conv = dataManager.load(AiConversation.class).id(conversationId).optional().orElse(null);
                 if (conv != null) row.setConversation(conv);
                 // If null, leave conversation unset — FK column is nullable; row is still useful for diagnostics.
             }
          5. row.setToolName("<chat>");                    // sentinel — entity @NotNull
          6. row.setOutcome(AiToolCallOutcome.SUCCESS);    // sentinel — entity @NotNull; revisited by writeChatPost
          7. row.setStartedAt(OffsetDateTime.now());       // @NotNull
          8. dataManager.save(row); return row.getId();

        `public void writeChatPost(UUID runId, String userUsername, UUID conversationId, long latencyMs, String errorClass)`
          1. row = metadata.create(AiToolCallAudit.class)
          2. row.setRunId(runId); row.setKind("CHAT"); row.setPhase("POST");
             row.setLatencyMs(latencyMs); row.setErrorClass(errorClass);
          3. row.setUserUsername(userUsername);
          4. **Re-resolve and set conversation** (per Blocker 2 — the audit query in Plan 04-05 filters by `a.conversation.id`; POST rows MUST carry it for correlation by conversation, in addition to runId-based correlation):
             if (conversationId != null) {
                 AiConversation conv = dataManager.load(AiConversation.class).id(conversationId).optional().orElse(null);
                 if (conv != null) row.setConversation(conv);
             }
          5. row.setToolName("<chat>");
          6. row.setOutcome(errorClass == null ? AiToolCallOutcome.SUCCESS : AiToolCallOutcome.ERROR);
          7. row.setStartedAt(OffsetDateTime.now());
          8. row.setFinishedAt(OffsetDateTime.now());
          9. dataManager.save(row);
          (No fan-out for chat-level rows in MVP — listeners observe tool calls only per SPI-06.)

        `public UUID writeToolCall(UUID runId, String userUsername, UUID conversationId, String toolName,
                                   String argumentsJson, String resultSummary, long latencyMs,
                                   AiToolCallOutcome outcome, String denialReason, String errorClass,
                                   String phase /* "PRE" | "POST" */)`
          1. row = metadata.create(AiToolCallAudit.class)
          2. row.setRunId(runId); row.setKind("TOOL"); row.setPhase(phase);
             row.setUserUsername(userUsername);
             row.setToolName(toolName);                      // real tool name — @NotNull satisfied
             row.setArgumentsJson(argumentsJson);
             row.setResultSummary(resultSummary);
             row.setLatencyMs(latencyMs);
             row.setOutcome(outcome != null ? outcome : AiToolCallOutcome.SUCCESS);  // @NotNull
             row.setDenialReason(denialReason);
             row.setErrorClass(errorClass);
             row.setStartedAt(OffsetDateTime.now());
             if ("POST".equals(phase)) row.setFinishedAt(OffsetDateTime.now());
          3. if (conversationId != null) {
                 AiConversation conv = dataManager.load(AiConversation.class).id(conversationId).optional().orElse(null);
                 if (conv != null) row.setConversation(conv);
             }
          4. dataManager.save(row);
          5. final UUID auditId = row.getId();
          5. Register afterCommit synchronization (D-14):
             if (TransactionSynchronizationManager.isSynchronizationActive()) {
                 TransactionSynchronizationManager.registerSynchronization(new TransactionSynchronization() {
                     @Override public void afterCommit() { fanOut.fireToolCallAudited(auditId); }
                 });
             } else {
                 // Defensive — REQUIRES_NEW should always be active here
                 log.warn("No active synchronization for runId={} auditId={}; firing inline", runId, auditId);
                 fanOut.fireToolCallAudited(auditId);
             }
          6. return auditId

      - SLF4J logger declared.
      - Class JavaDoc: cite D-11 (REQUIRES_NEW lives ONLY here — never on AuditAdvisor/ToolCallbackAuditDecorator), D-14 (afterCommit fan-out), Pitfall #3 (proxy self-invocation).

    Append i18n keys to BOTH locale files:
      messages.properties:
        com.vn.agent.audit/AuditWriteFailed=Failed to record audit row
      messages_vi.properties:
        com.vn.agent.audit/AuditWriteFailed=Không ghi được dòng audit
      (Reserved for future use; ensures locale parity per CLAUDE.md.)
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:compileJava -q</automated>
  </verify>
  <acceptance_criteria>
    - AuditWriter has exactly three public methods, each annotated `@Transactional(propagation = Propagation.REQUIRES_NEW)`.
    - AuditWriter has NO `@Transactional` annotation at the class level (only on methods, so no inherited propagation surprises).
    - AuditListenerFanOut iterates `List<AuditListener>` with a per-iteration `catch (Throwable)`.
    - AuditWriter.writeToolCall registers a TransactionSynchronization whose `afterCommit()` invokes `fanOut.fireToolCallAudited(auditId)`.
    - AuditWriter does NOT call `this.writeXxx(...)` from any of its own methods (no self-invocation).
    - Both locale files contain `com.vn.agent.audit/AuditWriteFailed`.
    - AuditWriter setters use ONLY real entity field setters: `setConversation`, `setUserUsername`, `setToolName`, `setArgumentsJson`, `setResultSummary`, `setOutcome`, `setDenialReason`, `setLatencyMs`, `setStartedAt`, `setFinishedAt`, `setRunId`, `setKind`, `setPhase`, `setPromptHash`, `setErrorClass`. NO calls to imaginary setters `setUserId`, `setCreatedBy`, `setCreatedAt`, `setInputJson`, `setOutputJson`, `setSuccess`, `setErrorMessage`, `setConversationId(UUID)` (the latter does not exist; conversation is set via the resolved entity).
    - Chat-level rows (PRE and POST) set sentinel `toolName="<chat>"` and a non-null `outcome` to satisfy entity `@NotNull` constraints.
    - Chat POST rows re-resolve and set the `conversation` association (Plan 04-05 audit query relies on `a.conversation.id` JPQL path).
    - `./gradlew :ai-agent:ai-agent:compileJava -q` exits 0.
  </acceptance_criteria>
  <done>AuditWriter and AuditListenerFanOut are the only audit-data-layer classes; advisor + decorator (Task 3) inject AuditWriter and never re-declare REQUIRES_NEW.</done>
</task>

<task type="auto">
  <name>Task 3: Implement AuditAdvisor (outermost CallAdvisor) + ToolCallbackAuditDecorator</name>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditAdvisor.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/audit/ToolCallbackAuditDecorator.java
  </files>
  <read_first>
    - .planning/phases/04-orchestration-core/04-RESEARCH.md (Pattern 2 verbatim, Pitfall #1, Pitfall #3, advisor ordering table)
    - .planning/phases/04-orchestration-core/04-AI-SPEC.md (§4 Core Pattern)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditWriter.java (just created in Task 2)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/audit/ToolCallAdvisorBuilderProbe.java (OQ-1 closure from Task 1)
  </read_first>
  <action>
    Create `com.vn.agent.audit.AuditAdvisor` implementing `org.springframework.ai.chat.client.advisor.api.CallAdvisor`:
      - Annotated `@Component`.
      - Constructor-injected: `AuditWriter auditWriter`, `CurrentAuthentication currentAuthentication` (Jmix).
      - `getName()` returns `"AuditAdvisor"`.
      - `getOrder()` returns `Ordered.HIGHEST_PRECEDENCE` (outermost — wraps everything including memory + tool advisors per D-02).
      - `adviseCall(ChatClientRequest request, CallAdvisorChain chain)`:
          1. // B8 FIX: prefer pre-allocated runId from service layer (DefaultChatServiceImpl puts it in advisor context).
             // This eliminates the lifecycle race where the service tried to read RunContext.get() AFTER the call
             // returned, but the finally-block below had already cleared it. Generate a fresh UUID only as a defensive
             // fallback when the advisor is invoked without service-layer pre-allocation (e.g., in tests).
             Object preAllocated = request.context().get("audit.runId");
             UUID runId = (preAllocated instanceof UUID) ? (UUID) preAllocated : UUID.randomUUID();
          2. RunContext.set(runId);
          3. String userId = resolveUserId(); // CurrentAuthentication.getUser().getKey() defensively cast to String; "anonymous" fallback if null
          4. UUID conversationId = extractConversationId(request); // Read from request.context() under "chat_memory_conversation_id" or null
          5. String promptHash = hashPrompt(request); // SHA-256 hex of last user message text (or full prompt if no user message); first 64 hex chars
          6. long startNanos = System.nanoTime();
          7. String errorClass = null;
          8. try {
                 auditWriter.writeChatPre(runId, userId, conversationId, promptHash);
                 // userId here is the userUsername string passed to AuditWriter (Phase 4 calls it userId for symmetry with Jmix CurrentAuthentication.getUser().getUsername()).
                 ChatClientResponse response = chain.nextCall(request);
                 return response;
             } catch (Throwable t) {
                 errorClass = t.getClass().getName();
                 throw t;
             } finally {
                 long latencyMs = (System.nanoTime() - startNanos) / 1_000_000L;
                 try { auditWriter.writeChatPost(runId, userId, conversationId, latencyMs, errorClass); }
                 catch (Throwable t2) { log.warn("writeChatPost failed for runId={}", runId, t2); }
                 RunContext.clear();
             }
      - Private helpers:
          - `resolveUserId()` — try `currentAuthentication.getUser().getKey()` via reflection (returns Object), `String.valueOf(...)`; on any exception return "anonymous".
          - `extractConversationId(ChatClientRequest)` — read `request.context().get("chat_memory_conversation_id")` (constant from Spring AI ChatMemory); coerce to UUID if non-null String/UUID; null otherwise.
          - `hashPrompt(ChatClientRequest)` — SHA-256 of `request.prompt().getInstructions().getLast().getText()` (or full prompt toString if empty); return first 64 hex chars.
      - Class JavaDoc: cite ORCH-02 (advisor ordering), AUD-01 (chat pre/post), Pitfall #3 ("AuditWriter is INJECTED — advisor never self-invokes any @Transactional method").

    Create `com.vn.agent.audit.ToolCallbackAuditDecorator` implementing `org.springframework.ai.tool.ToolCallback`:
      - NOT a Spring `@Component` — instantiated per-callback by AgentToolCallbacks (Plan 04-04). Plain class with public constructor `ToolCallbackAuditDecorator(ToolCallback delegate, AuditWriter auditWriter, CurrentAuthentication currentAuthentication)`.
      - `getToolDefinition()` returns `delegate.getToolDefinition()`.
      - `call(String toolInput)`:
          1. UUID runId = RunContext.get(); // may be null if invoked outside chat (defensive)
          2. String userId = resolveUserId();
          3. UUID conversationId = null; // not directly known here — chat-level row carries it; tool row correlates by runId
          4. String toolName = delegate.getToolDefinition().name();
          5. long startNanos = System.nanoTime();
          6. // PRE-row written eagerly so it survives even if delegate throws AND its tx rolls back
             try {
                 auditWriter.writeToolCall(runId, userId, conversationId, toolName,
                     toolInput, /*resultSummary*/ null, 0L,
                     com.vn.agent.entity.AiToolCallOutcome.SUCCESS,  // sentinel for PRE — POST overwrites with real outcome
                     /*denialReason*/ null, /*errorClass*/ null, "PRE");
             } catch (Throwable t) { log.warn("Tool PRE audit failed runId={} tool={}", runId, toolName, t); }
          7. boolean success = false;
             String outputJson = null;
             String errorMessage = null;
             String errorClass = null;
             try {
                 outputJson = delegate.call(toolInput);
                 success = true;
                 return outputJson;
             } catch (Throwable t) {
                 errorMessage = t.getMessage();
                 errorClass = t.getClass().getName();
                 throw t;
             } finally {
                 long latencyMs = (System.nanoTime() - startNanos) / 1_000_000L;
                 try {
                     com.vn.agent.entity.AiToolCallOutcome outcome = success
                             ? com.vn.agent.entity.AiToolCallOutcome.SUCCESS
                             : com.vn.agent.entity.AiToolCallOutcome.ERROR;
                     // resultSummary truncates outputJson to keep the LOB column reasonable; full JSON
                     // can be reconstructed from the chat memory or a future spillover table.
                     String resultSummary = outputJson == null ? errorMessage
                             : (outputJson.length() > 4096 ? outputJson.substring(0, 4096) : outputJson);
                     auditWriter.writeToolCall(runId, userId, conversationId, toolName,
                         toolInput, resultSummary, latencyMs, outcome,
                         /*denialReason*/ null, errorClass, "POST");
                 } catch (Throwable t2) {
                     log.warn("Tool POST audit failed runId={} tool={}", runId, toolName, t2);
                 }
             }
      - If the `ToolCallback` interface in 1.1.4 declares an overload `call(String toolInput, ToolContext toolContext)`, override that too and forward (audit logic identical, just pass toolContext to delegate).
      - Private `resolveUserId()` matches AuditAdvisor's helper (extract to a small package-private utility if duplication bothers — `AuditUserResolver` — otherwise inline is fine).
      - Class JavaDoc cites AUD-04 (per-tool audit row), AUD-02 (REQUIRES_NEW survives tool rollback because AuditWriter is the proxied bean, not this decorator).
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:compileJava -q</automated>
  </verify>
  <acceptance_criteria>
    - AuditAdvisor implements `org.springframework.ai.chat.client.advisor.api.CallAdvisor` and returns `Ordered.HIGHEST_PRECEDENCE` from `getOrder()`.
    - AuditAdvisor's `adviseCall` calls `auditWriter.writeChatPre` BEFORE `chain.nextCall(...)` and `auditWriter.writeChatPost` in `finally`.
    - AuditAdvisor reads `request.context().get("audit.runId")` first (B8) and falls back to `UUID.randomUUID()` only when the value is absent or not a UUID.
    - AuditAdvisor sets RunContext before chain.nextCall and clears it in finally.
    - AuditAdvisor has NO `@Transactional` annotation and makes NO `this.writeXxx(...)` self-call (Pitfall #3 closed).
    - ToolCallbackAuditDecorator implements `org.springframework.ai.tool.ToolCallback` and writes both PRE and POST tool rows via `auditWriter.writeToolCall(...)`.
    - ToolCallbackAuditDecorator has NO `@Transactional` annotation.
    - ToolCallbackAuditDecorator's POST write happens inside `finally` so it executes even when `delegate.call(...)` throws.
    - `./gradlew :ai-agent:ai-agent:compileJava -q` exits 0.
  </acceptance_criteria>
  <done>The audit pipeline is fully built; Plan 04-04 wires AuditAdvisor into ChatClient.defaultAdvisors and ToolCallbackAuditDecorator into AgentToolCallbacks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Chat caller → AuditAdvisor | Untrusted prompt text enters audit pipeline; promptHash protects against logging raw secrets while preserving correlation. |
| Tool delegate → ToolCallbackAuditDecorator | Tool may throw arbitrary Throwable; audit row must persist regardless. |
| AuditWriter → AuditListener (host SPI) | Host code can register listeners; one bad listener must not break audit or chat. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Information Disclosure | AuditAdvisor.hashPrompt | mitigate | Hash (SHA-256, first 64 hex) of prompt — never store raw prompt text in chat-level audit row. Raw text already lives in AiMessage (separately governed by Jmix entity ACLs). |
| T-04-03-02 | Tampering / Repudiation | AuditWriter | mitigate | REQUIRES_NEW boundary on AuditWriter ensures audit row commits independently of caller tx; tool rollback cannot retroactively erase the row (AUD-02). |
| T-04-03-03 | Denial of Service | AuditListenerFanOut | mitigate | Per-listener try/catch on `Throwable` prevents one host listener from crashing the chat thread or blocking other listeners (D-13). |
| T-04-03-04 | Elevation of Privilege | AuditAdvisor.resolveUserId | accept | Reflective `getUser().getKey()` extraction is read-only and falls back to "anonymous" on failure; no privilege change possible. |
| T-04-03-05 | Repudiation | ToolCallbackAuditDecorator | mitigate | PRE row written before tool execution AND POST row in `finally` — both rows survive tool throwable due to AuditWriter REQUIRES_NEW. |
</threat_model>

<verification>
- `./gradlew :ai-agent:ai-agent:compileJava -q` exits 0.
- Grep confirms no `@Transactional` annotation on AuditAdvisor or ToolCallbackAuditDecorator.
- Grep confirms `Propagation.REQUIRES_NEW` appears exactly three times in AuditWriter (one per public method).
- Grep confirms AuditAdvisor calls `auditWriter.writeChatPre` and `auditWriter.writeChatPost` (NOT `this.writeChatPre`).
- ToolCallAdvisorBuilderProbe.RESOLVED_BUILDER_METHOD constant is non-empty (or documented as "no toggle needed").
</verification>

<success_criteria>
- AuditWriter is the sole REQUIRES_NEW surface in the audit pipeline.
- AuditAdvisor sits at Ordered.HIGHEST_PRECEDENCE and seeds RunContext.
- ToolCallbackAuditDecorator wraps a delegate ToolCallback and writes PRE+POST rows that survive delegate throws.
- AuditListenerFanOut suppresses per-listener Throwable.
- OQ-1 is closed in code (ToolCallAdvisorBuilderProbe).
- Compile succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/04-orchestration-core/04-03-SUMMARY.md` documenting:
- AuditWriter REQUIRES_NEW surface (method names + signatures)
- AuditAdvisor advisor ordering + RunContext lifecycle
- ToolCallbackAuditDecorator tool-row shape (PRE + POST)
- AuditListenerFanOut SPI fan-out behavior
- OQ-1 resolution: verified ToolCallAdvisor.Builder method name (or "no toggle present" finding)
- Files created + Liquibase changes (none in this plan; 04-01 owns DDL)
</output>
