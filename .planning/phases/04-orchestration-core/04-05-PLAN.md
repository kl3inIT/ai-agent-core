---
phase: 04-orchestration-core
plan: 05
type: execute
wave: 4
depends_on: [04-03, 04-04]
files_modified:
  - ai-agent/ai-agent/src/test/java/com/vn/agent/orchestration/OrchestrationIntegrationTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/orchestration/AdvisorOrderStructuralTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/orchestration/OwnershipOpacityTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/audit/AuditDurabilityTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/orchestration/DualLayerParityTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/audit/AuditListenerFanOutTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/live/ChatServiceLiveSemanticTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/test_support/StubChatModelConfiguration.java
autonomous: true
requirements: [TEST-02, TEST-03, TEST-05]
must_haves:
  truths:
    - "Advisor chain ordering is asserted structurally (AuditAdvisor < MessageChatMemoryAdvisor < ToolCallAdvisor by getOrder())"
    - "Cross-user conversation probe returns ConversationNotFoundException with identical message regardless of whether row exists for another user or does not exist at all"
    - "When a tool throws, the per-tool audit row still commits (REQUIRES_NEW durability proven)"
    - "After ChatService.ask, JdbcChatMemoryRepository.findByConversationId and DataManager.load(AiMessage) return the same number of rows in the same order (dual-layer parity)"
    - "An AuditListener that throws does not block other listeners or fail the chat call"
    - "Live test (tagged 'live', skipped by default) calls real OpenRouter and asserts a non-empty assistant response"
  artifacts:
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/orchestration/OrchestrationIntegrationTest.java"
      provides: "End-to-end happy-path with stub ChatModel"
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/orchestration/AdvisorOrderStructuralTest.java"
      provides: "Structural assertion on ChatClient bean's advisor list"
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/orchestration/OwnershipOpacityTest.java"
      provides: "D-09 cross-user opacity proof"
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/audit/AuditDurabilityTest.java"
      provides: "AUD-02 REQUIRES_NEW survival proof"
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/orchestration/DualLayerParityTest.java"
      provides: "D-08 dual-layer write parity proof"
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/audit/AuditListenerFanOutTest.java"
      provides: "SPI-06 / D-13 per-listener exception suppression proof"
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/live/ChatServiceLiveSemanticTest.java"
      provides: "TEST-05 live semantic check (gated by @Tag('live'))"
  key_links:
    - from: "OrchestrationIntegrationTest"
      to: "StubChatModelConfiguration"
      via: "@TestConfiguration providing deterministic ChatModel"
      pattern: "StubChatModelConfiguration"
    - from: "AdvisorOrderStructuralTest"
      to: "ChatClient bean advisor list"
      via: "reflection on ChatClient internals OR Spring AI public accessor if available"
      pattern: "getOrder"
---

<objective>
Implement the Phase 4 verification suite proving the orchestration core works as designed: advisor ordering is structurally correct, ownership opacity holds across users, audit rows survive tool rollback, dual-layer projection stays in lockstep with primary memory, AuditListener fan-out is fault-isolated, and a tagged live test exercises the real OpenRouter path.

Purpose: Implements TEST-02 (orchestration integration tests), TEST-03 (audit + ownership coverage), TEST-05 (gated live test). Provides the mechanical evidence that locked decisions D-01..D-14 actually hold in code.

Output: Seven test classes plus one shared StubChatModelConfiguration helper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-orchestration-core/04-CONTEXT.md
@.planning/phases/04-orchestration-core/04-RESEARCH.md
@.planning/phases/04-orchestration-core/04-AI-SPEC.md
@.planning/phases/04-orchestration-core/04-PATTERNS.md

@ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditAdvisor.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditWriter.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/audit/ToolCallbackAuditDecorator.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/ChatClientFactory.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/ConversationGateway.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/ProjectingChatMemoryRepository.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stub ChatModel + AdvisorOrderStructuralTest + OrchestrationIntegrationTest</name>
  <files>
    ai-agent/ai-agent/src/test/java/com/vn/agent/test_support/StubChatModelConfiguration.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/orchestration/AdvisorOrderStructuralTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/orchestration/OrchestrationIntegrationTest.java
  </files>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/ChatClientFactory.java (advisor wiring)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditAdvisor.java (advisor name + order)
    - .planning/phases/04-orchestration-core/04-RESEARCH.md (AdvisorOrderStructuralTest design notes)
    - .planning/phases/04-orchestration-core/04-PATTERNS.md (existing @SpringBootTest analogs from Phase 1-3)
  </read_first>
  <action>
    Create `com.vn.agent.test_support.StubChatModelConfiguration` (`@TestConfiguration`):
      - Provides `@Bean @Primary ChatModel stubChatModel()` that returns a deterministic ChatModel:
            return new ChatModel() {
                @Override public ChatResponse call(Prompt prompt) {
                    return new ChatResponse(List.of(new Generation(new AssistantMessage("STUB:" + prompt.getInstructions().getLast().getText()))));
                }
                @Override public ChatOptions getDefaultOptions() { return OpenAiChatOptions.builder().model("stub/model").build(); }
            };
      - Reuse the same ChatModel instance for all tests in this plan (avoids real OpenRouter calls).

    Create `com.vn.agent.orchestration.AdvisorOrderStructuralTest`:
      - `@SpringBootTest` against the test app harness (jmix-app or a minimal test boot class — match Phase 3 conventions).
      - `@Import(StubChatModelConfiguration.class)`.
      - `@Autowired ChatClient chatClient;`
      - One test method `verifyAdvisorChainOrder()`:
          1. Reflectively read the advisor list from chatClient (Spring AI's ChatClient stores defaults in `DefaultChatClient.defaultChatClientRequest.advisors`; if a public accessor exists in 1.1.4 use it, else use reflection — `getDeclaredField` on the appropriate path, document the field name in a comment).
          2. Filter to CallAdvisor instances and sort by `getOrder()`.
          3. Assert exactly 3 advisors present.
          4. Assert advisors[0] instanceof AuditAdvisor && advisors[0].getOrder() == Ordered.HIGHEST_PRECEDENCE.
          5. Assert advisors[1].getClass().getSimpleName().equals("MessageChatMemoryAdvisor") && advisors[1].getOrder() == Integer.MIN_VALUE + 200.
          6. Assert advisors[2].getClass().getSimpleName().equals("ToolCallAdvisor") && advisors[2].getOrder() == Integer.MIN_VALUE + 300.
          7. Reflectively assert ToolCallAdvisor's internal-memory flag is the disabled state (per ToolCallAdvisorBuilderProbe — read the field name from the Probe javadoc; if no toggle exists, skip this assertion with a comment citing OQ-1 closure).

    Create `com.vn.agent.orchestration.OrchestrationIntegrationTest`:
      - `@SpringBootTest`, `@Import(StubChatModelConfiguration.class)`.
      - `@Autowired ChatService chatService;`
      - `@Autowired DataManager dataManager;`
      - Test 1 `askWithoutConversationIdCreatesConversation()`:
          1. ChatResponseDto resp = chatService.ask("user-A", null, "hello");
          2. assertThat(resp.conversationId()).isNotNull();
          3. assertThat(resp.content()).startsWith("STUB:");
          4. assertThat(resp.runId()).isNotNull();
          5. AiConversation conv = dataManager.load(AiConversation.class).id(resp.conversationId()).one();
          6. assertThat(conv.getUserId()).isEqualTo("user-A");
      - Test 2 `askTwiceSameConversationProducesTwoChatAuditPairs()`:
          1. ChatResponseDto first = chatService.ask("user-A", null, "msg1");
          2. ChatResponseDto second = chatService.ask("user-A", first.conversationId(), "msg2");
          3. List<AiToolCallAudit> chatRows = dataManager.load(AiToolCallAudit.class)
                .query("select a from ai_AiToolCallAudit a where a.kind = 'CHAT' and a.conversationId = :cid order by a.createdAt")
                .parameter("cid", first.conversationId()).list();
          4. assertThat(chatRows).hasSize(4);  // PRE+POST per ask × 2 asks  — note: writeChatPost does not set conversationId in the spec; if the test assertion fails for that reason, count by runId via two separate queries instead. Use whichever expression compiles and reflects the actual AuditWriter behavior from Plan 04-03.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.orchestration.AdvisorOrderStructuralTest" --tests "com.vn.agent.orchestration.OrchestrationIntegrationTest" -q</automated>
  </verify>
  <acceptance_criteria>
    - StubChatModelConfiguration provides a deterministic, in-memory ChatModel with no network access.
    - AdvisorOrderStructuralTest asserts 3 advisors with the exact (Audit, Memory, Tool) order and (-MIN, -MIN+200, -MIN+300) order constants.
    - OrchestrationIntegrationTest creates a conversation when conversationId is null AND verifies stable conversationId across two asks.
    - Both tests pass via the gradle command above.
  </acceptance_criteria>
  <done>Structural and happy-path coverage of ChatClient + ChatService is in place.</done>
</task>

<task type="auto">
  <name>Task 2: OwnershipOpacityTest + AuditDurabilityTest + DualLayerParityTest + AuditListenerFanOutTest</name>
  <files>
    ai-agent/ai-agent/src/test/java/com/vn/agent/orchestration/OwnershipOpacityTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/audit/AuditDurabilityTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/orchestration/DualLayerParityTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/audit/AuditListenerFanOutTest.java
  </files>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/ConversationGateway.java
    - ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/ConversationNotFoundException.java
    - ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditWriter.java
    - ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditListenerFanOut.java
    - ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/ProjectingChatMemoryRepository.java
    - ai-agent/ai-agent/src/main/java/com/vn/agent/spi/AuditListener.java
  </read_first>
  <action>
    Create `com.vn.agent.orchestration.OwnershipOpacityTest` (`@SpringBootTest`, `@Import(StubChatModelConfiguration.class)`):
      - Inject ChatService + DataManager.
      - Test method `crossUserProbeReturnsSameExceptionAsMissingId()`:
          1. ChatResponseDto userA = chatService.ask("user-A", null, "hello");
          2. UUID nonExistent = UUID.randomUUID();
          3. ConversationNotFoundException eA = catchThrowableOfType(
                 () -> chatService.ask("user-B", userA.conversationId(), "probe"),
                 ConversationNotFoundException.class);
          4. ConversationNotFoundException eB = catchThrowableOfType(
                 () -> chatService.ask("user-B", nonExistent, "probe"),
                 ConversationNotFoundException.class);
          5. assertThat(eA).isNotNull();
          6. assertThat(eB).isNotNull();
          7. assertThat(eA.getClass()).isEqualTo(eB.getClass());
          8. assertThat(eA.getMessage()).isEqualTo(eB.getMessage());  // both equal DEFAULT_MESSAGE — D-09 opacity

    Create `com.vn.agent.audit.AuditDurabilityTest` (`@SpringBootTest`):
      - Inject AuditWriter directly + DataManager + (optional) PlatformTransactionManager for outer-tx wrapping.
      - Test `toolAuditRowSurvivesOuterRollback()`:
          1. UUID runId = UUID.randomUUID();
          2. UUID convId = UUID.randomUUID();
          3. Use TransactionTemplate with PROPAGATION_REQUIRED:
                txTemplate.execute(status -> {
                    auditWriter.writeToolCall(runId, "user-A", convId, "echo",
                        "{\"in\":1}", "{\"out\":1}", 5L, true, null, null, "POST");
                    status.setRollbackOnly();
                    return null;
                });
          4. Outside the rolled-back tx, query: dataManager.load(AiToolCallAudit.class)
                .query("select a from ai_AiToolCallAudit a where a.runId = :rid")
                .parameter("rid", runId).list();
          5. Assert the list contains exactly one row (REQUIRES_NEW committed independently — AUD-02 proven).
          6. assertThat(row.getKind()).isEqualTo("TOOL");
          7. assertThat(row.getPhase()).isEqualTo("POST");

    Create `com.vn.agent.orchestration.DualLayerParityTest` (`@SpringBootTest`, `@Import(StubChatModelConfiguration.class)`):
      - Inject ChatService + DataManager + JdbcChatMemoryRepository (the raw delegate, not the projecting decorator — annotate `@Qualifier` if needed; alternative: inject ChatMemory and call `get(conversationId)`).
      - Test `afterAskBothLayersHaveSameMessageCount()`:
          1. ChatResponseDto resp = chatService.ask("user-A", null, "hello");
          2. List<Message> springRows = jdbcChatMemoryRepository.findByConversationId(resp.conversationId().toString());
          3. List<AiMessage> jmixRows = dataManager.load(AiMessage.class)
                 .query("select m from ai_AiMessage m where m.conversation.id = :cid order by m.createdAt")
                 .parameter("cid", resp.conversationId()).list();
          4. assertThat(jmixRows).hasSize(springRows.size());
          5. For each i: assertThat(jmixRows.get(i).getRole()).isEqualTo(springRows.get(i).getMessageType().getValue());
          6. For each i: assertThat(jmixRows.get(i).getContent()).isEqualTo(springRows.get(i).getText());

    Create `com.vn.agent.audit.AuditListenerFanOutTest` (`@SpringBootTest` + `@TestConfiguration` registering two AuditListener beans — one throwing, one recording):
      - `@TestConfiguration` static inner class:
            @Bean AuditListener throwingListener() {
                return id -> { throw new RuntimeException("boom"); };
            }
            @Bean AuditListener recordingListener(AtomicReference<UUID> ref) {
                return id -> ref.set(id);
            }
            @Bean AtomicReference<UUID> recordedRef() { return new AtomicReference<>(); }
      - Test `throwingListenerDoesNotBlockOthers()`:
          1. Inject AuditWriter + recordedRef + DataManager.
          2. Wrap call in a TransactionTemplate so afterCommit fires:
                txTemplate.execute(status -> {
                    auditWriter.writeToolCall(UUID.randomUUID(), "user-A", UUID.randomUUID(), "echo",
                        "{}", "{}", 1L, true, null, null, "POST");
                    return null;
                });
          3. // afterCommit synchronization fired — recording listener should have been invoked despite throwing listener
          4. assertThat(recordedRef.get()).isNotNull();
          5. assertThat(recordedRef.get()).isInstanceOf(UUID.class);
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.orchestration.OwnershipOpacityTest" --tests "com.vn.agent.audit.AuditDurabilityTest" --tests "com.vn.agent.orchestration.DualLayerParityTest" --tests "com.vn.agent.audit.AuditListenerFanOutTest" -q</automated>
  </verify>
  <acceptance_criteria>
    - OwnershipOpacityTest asserts identical exception class AND identical message for cross-user probe vs nonexistent ID (D-09).
    - AuditDurabilityTest sets `status.setRollbackOnly()` on outer tx and asserts the audit row still exists (AUD-02).
    - DualLayerParityTest asserts message counts match between JdbcChatMemoryRepository and AiMessage rows (D-08).
    - AuditListenerFanOutTest registers a throwing listener AND a recording listener and asserts the recording listener fired (D-13).
    - All four tests pass via the gradle command above.
  </acceptance_criteria>
  <done>Critical correctness invariants from Phase 4 are mechanically enforced.</done>
</task>

<task type="auto">
  <name>Task 3: ChatServiceLiveSemanticTest (gated by @Tag("live"))</name>
  <files>ai-agent/ai-agent/src/test/java/com/vn/agent/live/ChatServiceLiveSemanticTest.java</files>
  <read_first>
    - .planning/phases/04-orchestration-core/04-CONTEXT.md (D-15 / D-16 if any decisions about live tests)
    - jmix-app/src/main/resources/application.properties (existing OpenRouter config keys, if any)
    - .planning/REQUIREMENTS.md (TEST-05 definition)
  </read_first>
  <action>
    Create `com.vn.agent.live.ChatServiceLiveSemanticTest`:
      - `@SpringBootTest` (NO StubChatModelConfiguration — uses real ChatModel from spring-ai-starter-model-openai).
      - `@Tag("live")` — JUnit 5 tag; gradle test task excludes "live" tag by default. Add a comment block at the top documenting:
            // RUN MANUALLY: ./gradlew :ai-agent:ai-agent:test --tests "*ChatServiceLiveSemanticTest" -DincludeTags=live
            // Requires environment variables: SPRING_AI_OPENAI_API_KEY (OpenRouter key)
            //                                 SPRING_AI_OPENAI_BASE_URL=https://openrouter.ai/api
            // Reads model from active AiParameters or jmix.ai-agent.defaults.model.
      - `@EnabledIfEnvironmentVariable(named = "SPRING_AI_OPENAI_API_KEY", matches = ".+")` — JUnit 5 conditional that further protects against accidental runs without a key.
      - Inject ChatService.
      - Test `livePingProducesNonEmptyResponse()`:
          1. ChatResponseDto resp = chatService.ask("test-live-user", null, "Reply with the single word: pong");
          2. assertThat(resp.content()).isNotBlank();
          3. assertThat(resp.runId()).isNotNull();
          4. assertThat(resp.conversationId()).isNotNull();
          5. // Soft semantic assertion — accepts "pong" or any non-empty reply (LLMs may add punctuation/casing variance)
             // Per RESEARCH.md TEST-05 design: prefer .containsIgnoringCase("pong") AND fall back to .isNotBlank() if model is non-deterministic
             assertThat(resp.content().toLowerCase()).containsAnyOf("pong", "yes", "ok", "sure");
             // The disjunction tolerates model drift; the test's PRIMARY value is proving the wire works, not specific output.
      - Configure gradle to exclude `live` tag by default. Add to `ai-agent/ai-agent/build.gradle` (read first to confirm test task config):
            tasks.named('test') {
                useJUnitPlatform {
                    excludeTags 'live'
                }
            }
        If existing build.gradle already has this exclusion or equivalent, DO NOT duplicate — verify with grep first.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test -q</automated>
  </verify>
  <acceptance_criteria>
    - ChatServiceLiveSemanticTest is annotated `@Tag("live")` AND `@EnabledIfEnvironmentVariable(named = "SPRING_AI_OPENAI_API_KEY", matches = ".+")`.
    - Default `./gradlew :ai-agent:ai-agent:test` run does NOT execute this test (excluded by tag).
    - The test class compiles and is discovered (verify by running `./gradlew :ai-agent:ai-agent:test --tests "*ChatServiceLiveSemanticTest" -q --info` and confirming the test is reported as skipped or not selected, not as a missing class).
    - All other tests from Tasks 1-2 still pass in the default gradle test run.
  </acceptance_criteria>
  <done>Phase 4 verification suite is complete: 6 deterministic tests run on every CI invocation, 1 live test is opt-in for manual smoke checks against OpenRouter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → ChatService | Tests are trusted; no input validation needed. |
| Live test → OpenRouter | Real API call; gated by env var to prevent accidental cost. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Information Disclosure | Live test API key | mitigate | Key sourced from environment variable, never committed; test gated by `@EnabledIfEnvironmentVariable`. |
| T-04-05-02 | Denial of Service / Cost | Accidental live test run in CI | mitigate | Gradle excludes `live` tag by default; conditional annotation provides second gate. |
</threat_model>

<verification>
- `./gradlew :ai-agent:ai-agent:test -q` exits 0 with all 6 deterministic tests passing.
- `./gradlew :ai-agent:ai-agent:test --tests "*ChatServiceLiveSemanticTest" -q` reports the test as either skipped (no key) or excluded (tag filter), never as a compile error.
- Test report at `ai-agent/ai-agent/build/reports/tests/test/index.html` shows the 6 deterministic test classes as PASSED.
</verification>

<success_criteria>
- 6 deterministic test classes (Stub config + Advisor structural + Orchestration integration + Ownership opacity + Audit durability + Dual-layer parity + Listener fan-out) all pass on `./gradlew :ai-agent:ai-agent:test`.
- 1 live test class compiles and is gated by tag + env-var conditional.
- Phase 4 success criteria from REQUIREMENTS.md (TEST-02, TEST-03, TEST-05) are mechanically demonstrated.
</success_criteria>

<output>
After completion, create `.planning/phases/04-orchestration-core/04-05-SUMMARY.md` documenting:
- Test inventory (class name, requirement IDs, what it proves)
- Stub vs live test split
- Tag-based exclusion configuration
- Test report location for human review
- Final Phase 4 close-out checklist (all 17 requirement IDs covered: 04-01 → AUD-03; 04-02 → SPI-02/03/ORCH-06; 04-03 → AUD-01/02/04/05/SPI-06; 04-04 → ORCH-01..05; 04-05 → TEST-02/03/05)
</output>
