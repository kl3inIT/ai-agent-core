---
phase: 04-orchestration-core
plan: 04
type: execute
wave: 3
depends_on: [04-02, 04-03]
files_modified:
  - ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/ChatResponseDto.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/ConversationGateway.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/ProjectingChatMemoryRepository.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/ChatClientFactory.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/ChatService.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/tools/AgentToolCallbacks.java
  - ai-agent/ai-agent-starter/src/main/java/com/vn/autoconfigure/agent/AIAutoConfiguration.java
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties
autonomous: true
requirements: [ORCH-01, ORCH-02, ORCH-03, ORCH-04, ORCH-05]
must_haves:
  truths:
    - "ChatClient is built once at startup with verified advisor order: AuditAdvisor (HIGHEST_PRECEDENCE) -> MessageChatMemoryAdvisor (HIGHEST_PRECEDENCE+200) -> ToolCallAdvisor (HIGHEST_PRECEDENCE+300, internal memory disabled via .disableMemory())"
    - "Each chat call uses the cached ChatClient with per-request .prompt() (no per-request rebuild) per D-01"
    - "ConversationGateway enforces user-scoped ownership using AiConversation.createdBy field with same opaque ConversationNotFoundException for missing-vs-not-yours per D-09"
    - "ConversationGateway.loadOrCreate accepts firstMessage; on auto-create, sets title = firstMessage truncated to 80 chars per D-08"
    - "ProjectingChatMemoryRepository decorates JdbcChatMemoryRepository and writes AiMessage rows in the SAME REQUIRED transaction"
    - "ProjectingChatMemoryRepository sets AiMessage.createdDate (NOT createdAt - field is named createdDate on the entity)"
    - "Every ToolCallback assembled by AgentToolCallbacks is wrapped by ToolCallbackAuditDecorator before reaching the chat client"
    - "ChatService.ask returns a ChatResponseDto carrying conversationId, content, runId, model, latencyMs"
    - "DefaultChatServiceImpl pre-allocates runId BEFORE chatClient.prompt() and passes it via advisor context key 'audit.runId' so AuditAdvisor reads it instead of generating a new one (B8 fix - prevents RunContext lifecycle race)"
    - "DefaultChatServiceImpl injects BaselineContextProvider (D-15) and prepends baselineText to the per-request system prompt"
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/ChatClientFactory.java"
      provides: "@Bean ChatClient with verified advisor ordering"
      contains: "defaultAdvisors"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/ConversationGateway.java"
      provides: "loadOrCreate(userId, conversationId, firstMessage) with opaque ownership check + title from first message"
      contains: "ConversationNotFoundException"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/ProjectingChatMemoryRepository.java"
      provides: "Decorator writing AiMessage in same REQUIRED tx"
      contains: "implements ChatMemoryRepository"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java"
      provides: "Cached ChatClient + per-request .prompt() execution (D-01); pre-allocates runId; composes baseline+profile system prompt"
  key_links:
    - from: "DefaultChatServiceImpl.ask"
      to: "ConversationGateway.loadOrCreate"
      via: "ownership check before any chat call"
      pattern: "conversationGateway\\.loadOrCreate"
    - from: "DefaultChatServiceImpl.ask"
      to: "BaselineContextProvider.compose"
      via: "compose baseline before chat call (D-15)"
      pattern: "baselineContextProvider\\.compose"
    - from: "DefaultChatServiceImpl.ask"
      to: "AuditAdvisor (via advisor context)"
      via: ".advisors(spec -> spec.param(\"audit.runId\", runId))"
      pattern: "audit\\.runId"
    - from: "ChatClientFactory"
      to: "AuditAdvisor + MessageChatMemoryAdvisor + ToolCallAdvisor"
      via: ".defaultAdvisors(...)"
      pattern: "defaultAdvisors"
    - from: "AgentToolCallbacks.callbacksFor"
      to: "ToolCallbackAuditDecorator"
      via: "wrap each ToolCallback before returning"
      pattern: "new ToolCallbackAuditDecorator"
    - from: "ProjectingChatMemoryRepository.saveAll"
      to: "DataManager.save(AiMessage)"
      via: "same REQUIRED transaction as JdbcChatMemoryRepository.saveAll"
      pattern: "dataManager\\.save"
---

<objective>
Wire the orchestration layer: a ChatClientFactory that builds a single cached ChatClient with the verified advisor order; a ConversationGateway enforcing user-scoped ownership opacity (D-09) using the real AiConversation.createdBy field, and applying D-08 title rule (firstMessage truncated to 80 chars on auto-create); a ProjectingChatMemoryRepository decorator that writes AiMessage rows in the same REQUIRED transaction as JdbcChatMemoryRepository (dual-layer per D-08); a rewritten DefaultChatServiceImpl that uses the cached client with per-request `.prompt()` (D-01), pre-allocates runId before the call (B8 fix), composes baseline + profile system prompt via BaselineContextProvider (D-15), and an AgentToolCallbacks update that wraps every ToolCallback in ToolCallbackAuditDecorator before they reach the chat client.

Purpose: Implements ORCH-01..ORCH-05. Closes the wiring loop between Plans 04-02 (foundations) and 04-03 (audit pipeline) so a single `ChatService.ask(userId, conversationId, message)` call exercises advisor ordering, ownership, dual-layer persistence, audit, and tool execution end-to-end.

Output: Production orchestration package + updated autoconfig + rewritten ChatService implementation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-orchestration-core/04-CONTEXT.md
@.planning/phases/04-orchestration-core/04-RESEARCH.md
@.planning/phases/04-orchestration-core/04-AI-SPEC.md
@.planning/phases/04-orchestration-core/04-PATTERNS.md

@ai-agent/ai-agent/src/main/java/com/vn/agent/ChatService.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/ChatResponse.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiConversation.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiMessage.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/tools/AgentToolCallbacks.java
@ai-agent/ai-agent-starter/src/main/java/com/vn/autoconfigure/agent/AIAutoConfiguration.java

<interfaces>
<!-- From Plan 04-02 -->
com.vn.agent.orchestration.AiAgentDefaultsProperties (record)
  - String model, Double temperature, Double topP, Integer maxTokens, String systemPrompt
com.vn.agent.orchestration.RunContext
  - static void set/get/clear(UUID)
com.vn.agent.orchestration.ConversationNotFoundException extends RuntimeException
  - public static final String MESSAGE_KEY = "com.vn.agent.orchestration/ConversationNotFound";
  - public static final String DEFAULT_MESSAGE = "Conversation not found";
com.vn.agent.orchestration.AiParametersResolver
  - AiParameters resolveActive();
  - String effectiveModel(AiParameters p);  // validates "/" slash
  - Double effectiveTemperature(AiParameters p);
  - Double effectiveTopP(AiParameters p);
  - Integer effectiveMaxTokens(AiParameters p);
  - String effectiveSystemPrompt(AiParameters p);
com.vn.agent.orchestration.BaselineContextProvider
  - String compose(UUID conversationId);   // D-15: returns baseline text (user, roles, locale, conversation id) to prepend to system prompt

<!-- From Plan 04-03 -->
com.vn.agent.audit.AuditAdvisor implements CallAdvisor
  - reads `audit.runId` from ChatClientRequest.context() if present (B8); falls back to UUID.randomUUID() only if null
com.vn.agent.audit.ToolCallbackAuditDecorator implements ToolCallback
  - constructor: (ToolCallback delegate, AuditWriter, CurrentAuthentication)
com.vn.agent.audit.AuditWriter (proxied @Component)
  - writeChatPre(UUID runId, String userUsername, UUID conversationId, String promptHash)
  - writeChatPost(UUID runId, String userUsername, UUID conversationId, long latencyMs, String errorClass)
  - writeToolCall(UUID runId, String userUsername, UUID conversationId, String toolName, String argumentsJson, String resultSummary, long latencyMs, AiToolCallOutcome outcome, String denialReason, String errorClass, String phase)
com.vn.agent.audit.ToolCallAdvisorBuilderProbe
  - public static final String RESOLVED_BUILDER_METHOD = "disableMemory";
  - public static final String INTERNAL_FLAG_FIELD = "conversationHistoryEnabled";
  - public static final String ORDER_SETTER_METHOD = "advisorOrder";

<!-- Spring AI 1.1.4 (verified by javap probe 2026-04-20 - see RESEARCH.md OQ-1 RESOLUTION) -->
org.springframework.ai.chat.client.ChatClient
  - static ChatClient.Builder builder(ChatModel chatModel);
  - ChatClient.Builder defaultAdvisors(Advisor... advisors);
  - ChatClient.Builder defaultSystem(String text);
  - ChatClient build();
  - ChatClientRequestSpec prompt();
org.springframework.ai.chat.memory.ChatMemory
  - org.springframework.ai.chat.memory.MessageWindowChatMemory.builder()
        .chatMemoryRepository(repo).maxMessages(int).build()
org.springframework.ai.chat.memory.ChatMemoryRepository
  - List<Message> findByConversationId(String conversationId);
  - void saveAll(String conversationId, List<Message> messages);
  - void deleteByConversationId(String conversationId);
  - List<String> findConversationIds();
org.springframework.ai.chat.memory.repository.jdbc.JdbcChatMemoryRepository
  - static Builder builder() { jdbcTemplate(JdbcTemplate).build() }
org.springframework.ai.chat.client.advisor.MessageChatMemoryAdvisor
  - static Builder builder(ChatMemory) { conversationId(String).order(int).build() }
  - HIGHEST_PRECEDENCE+200 per D-02
  - ORDER setter is .order(int)
org.springframework.ai.chat.client.advisor.ToolCallAdvisor   [VERIFIED via javap]
  - static Builder builder()
  - Builder.disableMemory() -> Builder         (literal method name from javap)
  - Builder.conversationHistoryEnabled(boolean) -> Builder (alternative toggle)
  - Builder.advisorOrder(int) -> Builder       (NOTE: NOT .order(int) - the order setter on this builder is named advisorOrder)
  - HIGHEST_PRECEDENCE+300 per D-02

<!-- Existing Jmix entities - VERIFIED FIELD NAMES -->
com.vn.agent.entity.AiConversation
  - UUID id, Integer version
  - String title (length 250)
  - String createdBy   (@CreatedBy String, NOT NULL - this IS the ownership field; NO separate userId field exists)
  - OffsetDateTime createdDate (@CreatedDate)
  - String lastModifiedBy, OffsetDateTime lastModifiedDate
  - List<AiMessage> messages
  - SETTERS: setTitle(String), setCreatedBy(String), setCreatedDate(OffsetDateTime)
com.vn.agent.entity.AiMessage
  - UUID id, Integer version
  - AiConversation conversation (@NotNull ManyToOne)
  - Integer seq
  - String role
  - String content (@Lob)
  - OffsetDateTime createdDate   (NOT createdAt - the field is named createdDate)
  - SETTERS: setConversation(AiConversation), setSeq(Integer), setRole(String), setContent(String), setCreatedDate(OffsetDateTime)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ChatResponseDto + ConversationGateway (with createdBy + firstMessage title) + ChatService signature update</name>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/ChatResponseDto.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/ConversationGateway.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/ChatService.java,
    ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties,
    ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties
  </files>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiConversation.java (CONFIRMED ownership field is `createdBy`, NOT `userId`)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/ChatService.java (current signature)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/ChatResponse.java (existing record - to be superseded by DTO)
    - .planning/phases/04-orchestration-core/04-CONTEXT.md D-08 (title rule: first user message truncated to 80 chars)
    - .planning/phases/04-orchestration-core/04-CONTEXT.md D-09 (ownership opacity)
    - .planning/phases/04-orchestration-core/04-CONTEXT.md D-05/D-06 (conversation creation flow)
  </read_first>
  <action>
    Create `com.vn.agent.orchestration.ChatResponseDto` as a Java record:
      ```java
      public record ChatResponseDto(
          UUID conversationId,
          UUID runId,
          String content,
          String model,
          long latencyMs
      ) {}
      ```

    Create `com.vn.agent.orchestration.ConversationGateway` (`@Component`):
      - Constructor-injected: `DataManager dataManager`, `Metadata metadata`.
      - Public method `AiConversation loadOrCreate(String userId, UUID conversationId, String firstMessage)`:
          1. Validate `userId != null && !userId.isBlank()` - throw IllegalArgumentException("userId must not be null or blank") otherwise.
          2. If `conversationId == null` (auto-create branch per D-05/D-06):
                AiConversation fresh = metadata.create(AiConversation.class);
                fresh.setCreatedBy(userId);                       // B3: real field is createdBy, NOT userId
                fresh.setCreatedDate(OffsetDateTime.now());       // explicit set; @CreatedDate may also auto-fill
                // D-08 title rule: first user message truncated to 80 chars (B4 fix)
                if (firstMessage != null && !firstMessage.isBlank()) {
                    String title = firstMessage.length() > 80
                            ? firstMessage.substring(0, 80)
                            : firstMessage;
                    fresh.setTitle(title);
                } else {
                    fresh.setTitle(null);
                }
                dataManager.save(fresh);
                return fresh;
          3. Else (lookup branch): load by id with createdBy predicate in the SAME query (D-09 opacity - never reveal "exists but not yours"):
                Optional<AiConversation> opt = dataManager.load(AiConversation.class)
                        .query("select c from ai_AiConversation c where c.id = :id and c.createdBy = :owner")
                        .parameter("id", conversationId)
                        .parameter("owner", userId)
                        .optional();
                return opt.orElseThrow(() -> new ConversationNotFoundException(
                        ConversationNotFoundException.DEFAULT_MESSAGE));
                // SAME exception class + message regardless of whether row missing or owned by another user.
      - Class JavaDoc cites D-08 (title rule), D-09 (opacity), and ORCH-04.

    Update `com.vn.agent.ChatService` interface:
      - REPLACE existing `ChatResponse ask(String message, UUID conversationId, String userKey)` method.
      - NEW signature:
          `ChatResponseDto ask(String userId, UUID conversationId, String message)`
        - userId first (mandatory), conversationId nullable (auto-create), message last.
      - Mark old `ChatResponse` record as `@Deprecated` (do NOT delete - left for migration window; Plan 04-04 callers move to DTO).
      - JavaDoc: documents that conversationId == null triggers auto-creation per D-05; throws ConversationNotFoundException per D-09.

    Append to BOTH locale files:
      messages.properties:
        com.vn.agent.orchestration/ConversationNotFound=Conversation not found
        com.vn.agent.orchestration/InvalidUserId=User identifier must not be blank
      messages_vi.properties:
        com.vn.agent.orchestration/ConversationNotFound=Khong tim thay hoi thoai
        com.vn.agent.orchestration/InvalidUserId=Ma nguoi dung khong duoc de trong
      (ConversationNotFound key may already exist from 04-02 - if so, leave it; do not duplicate.)
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:compileJava -q</automated>
  </verify>
  <acceptance_criteria>
    - ChatResponseDto is a record with exactly 5 components: conversationId, runId, content, model, latencyMs.
    - ConversationGateway.loadOrCreate signature is `(String userId, UUID conversationId, String firstMessage)` - 3 args.
    - ConversationGateway uses `setCreatedBy(userId)` (NOT setUserId) on the new entity.
    - ConversationGateway JPQL predicate is `c.id = :id and c.createdBy = :owner` (NOT c.userId).
    - On auto-create, when firstMessage is non-blank, title is set to firstMessage.substring(0, min(80, length)).
    - Both branches (missing row, owned-by-other) throw `ConversationNotFoundException(DEFAULT_MESSAGE)` - identical class and message.
    - ChatService.ask signature is `ChatResponseDto ask(String userId, UUID conversationId, String message)`.
    - Both locale files contain `com.vn.agent.orchestration/ConversationNotFound` and `com.vn.agent.orchestration/InvalidUserId`.
    - `./gradlew :ai-agent:ai-agent:compileJava -q` exits 0.
  </acceptance_criteria>
  <done>Ownership-opaque conversation gateway is in place using real entity field (createdBy); D-08 title rule honored; ChatService surface is on the new DTO contract.</done>
</task>

<task type="auto">
  <name>Task 2: ProjectingChatMemoryRepository (createdDate) + ChatClientFactory (.disableMemory + advisorOrder) + AgentToolCallbacks audit wrap</name>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/ProjectingChatMemoryRepository.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/ChatClientFactory.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/tools/AgentToolCallbacks.java
  </files>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/audit/ToolCallAdvisorBuilderProbe.java (OQ-1 resolved: RESOLVED_BUILDER_METHOD="disableMemory", ORDER_SETTER_METHOD="advisorOrder")
    - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiMessage.java (CONFIRMED fields: role, content, createdDate (NOT createdAt), conversation @NotNull, seq)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/tools/AgentToolCallbacks.java (current MethodToolCallbackProvider assembly)
    - .planning/phases/04-orchestration-core/04-RESEARCH.md OQ-1 RESOLUTION (verified Builder API)
    - .planning/phases/04-orchestration-core/04-CONTEXT.md D-08 (dual-layer projection), D-02 (advisor order)
  </read_first>
  <action>
    Create `com.vn.agent.orchestration.ProjectingChatMemoryRepository` implementing `org.springframework.ai.chat.memory.ChatMemoryRepository`:
      - `@Component` and annotated `@Primary` (overrides default JdbcChatMemoryRepository injection at the ChatMemory builder site).
      - Constructor-injected: `JdbcChatMemoryRepository delegate`, `DataManager dataManager`, `Metadata metadata`.
      - All methods declared `@Transactional` (default propagation REQUIRED - joins outer tx so projection commits/rolls back atomically with delegate per D-08):
          - `findByConversationId(String)` -> returns `delegate.findByConversationId(conversationId)` (read-through; AiMessage projection is one-way write, reads come from SPRING_AI_CHAT_MEMORY).
          - `saveAll(String conversationId, List<Message> messages)`:
                1. delegate.saveAll(conversationId, messages);   // primary store
                2. UUID convUuid = UUID.fromString(conversationId);
                3. AiConversation conv = dataManager.load(AiConversation.class).id(convUuid).one();  // throws if missing - caller bug
                4. for (Message m : messages) {
                       AiMessage row = metadata.create(AiMessage.class);
                       row.setConversation(conv);
                       row.setRole(m.getMessageType().getValue());  // "user" | "assistant" | "system" | "tool"
                       row.setContent(m.getText());
                       row.setCreatedDate(OffsetDateTime.now());    // NOTE: setCreatedDate, NOT setCreatedAt - field is createdDate
                       dataManager.save(row);
                   }
          - `deleteByConversationId(String)`:
                1. delegate.deleteByConversationId(conversationId);
                2. UUID convUuid = UUID.fromString(conversationId);
                3. dataManager.load(AiMessage.class)
                        .query("select m from ai_AiMessage m where m.conversation.id = :cid")
                        .parameter("cid", convUuid)
                        .list()
                        .forEach(dataManager::remove);
          - `findConversationIds()` -> `delegate.findConversationIds()`.
      - JavaDoc cites D-08 (dual-layer) and ORCH-03.

    Create `com.vn.agent.orchestration.ChatClientFactory` (`@Configuration` class with one `@Bean ChatClient`):
      - Constructor-injected: `ChatModel chatModel` (Spring AI auto-config provides this from spring-ai-starter-model-openai), `ChatMemory chatMemory` (defined in AIAutoConfiguration - see Task 3), `AuditAdvisor auditAdvisor`, `AiParametersResolver parametersResolver`.
      - `@Bean ChatClient defaultChatClient()`:
            AiParameters active = parametersResolver.resolveActive();
            String systemPrompt = parametersResolver.effectiveSystemPrompt(active);

            MessageChatMemoryAdvisor memoryAdvisor = MessageChatMemoryAdvisor.builder(chatMemory)
                    .order(BaseAdvisor.HIGHEST_PRECEDENCE + 200)        // MessageChatMemoryAdvisor uses .order(int)
                    .build();

            // OQ-1 RESOLVED 2026-04-20 via javap on spring-ai-client-chat-1.1.4.jar:
            //   - Builder method to disable internal memory: .disableMemory()
            //   - Order setter on this builder is .advisorOrder(int) (NOT .order(int))
            ToolCallAdvisor toolCallAdvisor = ToolCallAdvisor.builder()
                    .disableMemory()                                      // B7: literal verified method
                    .advisorOrder(BaseAdvisor.HIGHEST_PRECEDENCE + 300)   // B7: this builder uses advisorOrder, NOT order
                    .build();

            return ChatClient.builder(chatModel)
                    .defaultSystem(systemPrompt != null ? systemPrompt : "You are a helpful assistant.")
                    .defaultAdvisors(auditAdvisor, memoryAdvisor, toolCallAdvisor)
                    .build();
      - Cite ORCH-01, ORCH-02, D-01, D-02 in class JavaDoc.

    Update `com.vn.agent.tools.AgentToolCallbacks`:
      - Constructor-inject `AuditWriter auditWriter` and `CurrentAuthentication currentAuthentication` (in addition to whatever it already takes).
      - In the method that returns the `ToolCallback[]` for a given request (likely named `callbacksFor(...)` or similar - verify in read_first):
          1. Get the existing array of MethodToolCallback instances from MethodToolCallbackProvider.
          2. Wrap each: `new ToolCallbackAuditDecorator(callback, auditWriter, currentAuthentication)`.
          3. Return the wrapped array.
      - Do NOT modify MethodToolCallbackProvider assembly itself - wrap at the boundary where callbacks leave AgentToolCallbacks.
      - Cite AUD-04 in JavaDoc on the wrapping method.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:compileJava -q</automated>
  </verify>
  <acceptance_criteria>
    - ProjectingChatMemoryRepository implements ChatMemoryRepository, is `@Primary @Component`, all mutating methods `@Transactional`.
    - ProjectingChatMemoryRepository.saveAll calls `delegate.saveAll` AND `dataManager.save(AiMessage)` in the same method body (no separate transactional context).
    - ProjectingChatMemoryRepository.saveAll calls `row.setCreatedDate(...)` (NOT setCreatedAt - grep must confirm zero occurrences of `setCreatedAt` in the new file).
    - ChatClientFactory.defaultChatClient() registers exactly three default advisors in the order: AuditAdvisor, MessageChatMemoryAdvisor, ToolCallAdvisor.
    - ChatClientFactory uses `.order(BaseAdvisor.HIGHEST_PRECEDENCE + 200)` for MessageChatMemoryAdvisor.
    - ChatClientFactory uses `.advisorOrder(BaseAdvisor.HIGHEST_PRECEDENCE + 300)` for ToolCallAdvisor (NOT .order - this Builder uses a different setter name).
    - ChatClientFactory ToolCallAdvisor builder chain contains literal `.disableMemory()` (no placeholder, no reflection).
    - AgentToolCallbacks wraps every returned ToolCallback in ToolCallbackAuditDecorator before returning.
    - `./gradlew :ai-agent:ai-agent:compileJava -q` exits 0.
  </acceptance_criteria>
  <done>ChatClient is the single cached bean with verified advisor order and confirmed Spring AI 1.1.4 Builder API; tool callbacks are audited; dual-layer projection commits atomically with primary store using the correct AiMessage.createdDate field.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite DefaultChatServiceImpl (B5 baseline + B8 runId pre-allocation) + update AIAutoConfiguration beans</name>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java,
    ai-agent/ai-agent-starter/src/main/java/com/vn/autoconfigure/agent/AIAutoConfiguration.java
  </files>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java (current Phase 1 stub)
    - ai-agent/ai-agent-starter/src/main/java/com/vn/autoconfigure/agent/AIAutoConfiguration.java (current ChatClient stub @Bean)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/BaselineContextProvider.java (D-15 - signature: String compose(UUID conversationId))
    - .planning/phases/04-orchestration-core/04-CONTEXT.md D-01 (cached client + per-request prompt), D-08 (dual-layer), D-15 (baseline context)
    - .planning/phases/04-orchestration-core/04-AI-SPEC.md (ChatService body sketch)
  </read_first>
  <action>
    Update `com.vn.autoconfigure.agent.AIAutoConfiguration`:
      - REMOVE the existing stub `@Bean ChatClient` method (ChatClient is now provided by ChatClientFactory in the ai-agent module).
      - ADD `@Bean @ConditionalOnMissingBean ChatMemory chatMemory(ChatMemoryRepository chatMemoryRepository)`:
            return MessageWindowChatMemory.builder()
                    .chatMemoryRepository(chatMemoryRepository)
                    .maxMessages(20)
                    .build();
      - ADD `@Bean @ConditionalOnMissingBean(type = "org.springframework.ai.chat.memory.repository.jdbc.JdbcChatMemoryRepository") JdbcChatMemoryRepository jdbcChatMemoryRepository(JdbcTemplate jdbcTemplate)`:
            return JdbcChatMemoryRepository.builder().jdbcTemplate(jdbcTemplate).build();
      - The `@Primary` annotation on ProjectingChatMemoryRepository ensures the ChatMemory builder picks up the projecting decorator over the raw JdbcChatMemoryRepository.
      - Component-scan note: ensure `@ConfigurationPropertiesScan` (already present per AIConfiguration:22) covers `com.vn.agent.orchestration` so AiAgentDefaultsProperties from 04-02 is bound.

    REWRITE `com.vn.agent.DefaultChatServiceImpl` (replace the Phase 1 stub entirely):
      - `@Service` (or `@Component` - match existing annotation).
      - Constructor-injected (NOTE B5: BaselineContextProvider added):
          - `ChatClient chatClient` (the singleton from ChatClientFactory - D-01)
          - `ConversationGateway conversationGateway`
          - `AgentToolCallbacks toolCallbacks`
          - `AiParametersResolver parametersResolver`
          - `BaselineContextProvider baselineContextProvider`   // B5: D-15 baseline composition
      - SLF4J logger.
      - Implements `ChatService.ask(String userId, UUID conversationId, String message)`:
            // B4 fix: pass `message` as firstMessage so auto-created conversation gets a title
            1. AiConversation conv = conversationGateway.loadOrCreate(userId, conversationId, message);
            2. UUID convId = conv.getId();
            3. AiParameters active = parametersResolver.resolveActive();
            4. String model = parametersResolver.effectiveModel(active);
            5. String profileSystemPrompt = parametersResolver.effectiveSystemPrompt(active);
            6. // B5: Compose baseline (user, roles, locale, conversationId per D-15) + profile prompt for THIS request
               String baselineText = baselineContextProvider.compose(convId);
               String composedSystemPrompt = baselineText
                       + (profileSystemPrompt != null && !profileSystemPrompt.isBlank()
                            ? "\n\n" + profileSystemPrompt
                            : "");
            7. // B8 fix: allocate runId BEFORE the chat call so AuditAdvisor.before() reads it from advisor context
               // instead of generating a fresh one. Eliminates the race where AuditAdvisor's finally-block clears
               // RunContext before we can read it back at the service layer.
               UUID runId = UUID.randomUUID();
            8. long start = System.nanoTime();
            9. ChatResponse springResponse = chatClient.prompt()
                       .system(composedSystemPrompt)                                  // B5: per-request system, overrides defaultSystem
                       .user(message)
                       .toolCallbacks(toolCallbacks.callbacksFor(userId, convId))     // wrapped with audit decorator
                       .advisors(advisorSpec -> advisorSpec
                               .param(ChatMemory.CONVERSATION_ID, convId.toString())
                               .param("audit.runId", runId))                          // B8: hand runId to AuditAdvisor
                       .options(OpenAiChatOptions.builder()
                               .model(model)
                               .temperature(parametersResolver.effectiveTemperature(active))
                               .topP(parametersResolver.effectiveTopP(active))
                               .maxTokens(parametersResolver.effectiveMaxTokens(active))
                               .build())
                       .call()
                       .chatResponse();
           10. long latencyMs = (System.nanoTime() - start) / 1_000_000L;
           11. String content = springResponse.getResult().getOutput().getText();
           12. return new ChatResponseDto(convId, runId, content, model, latencyMs);
      - Wrap nothing in try/catch at this layer - exceptions propagate; AuditAdvisor's `finally` records errorClass.
      - JavaDoc cites ORCH-01 (cached client per D-01), ORCH-05 (per-request options binding), D-15 (baseline composition), B8 (runId pre-allocation).

    Verify the ChatService interface marker - the OLD `ChatResponse ask(String message, UUID conversationId, String userKey)` method MAY still be on the interface if Plan 04-04 Task 1 left it for migration. If so, default-implement it as:
      `default ChatResponse ask(String message, UUID conversationId, String userKey) { throw new UnsupportedOperationException("Use ask(userId, conversationId, message) returning ChatResponseDto"); }`
      OR delete the old method entirely if no other module references it (grep: `grep -r "chatService.ask(" ai-agent jmix-app -l`). Pick the option that compiles cleanly.

    CROSS-PLAN COORDINATION (informational - implemented in Plan 04-03):
      AuditAdvisor.before() must:
        1. Read runId from `request.context().get("audit.runId")` first.
        2. If null (defensive fallback only), call `UUID.randomUUID()`.
        3. Set RunContext.set(runId) so ToolCallbackAuditDecorator threading works.
      This eliminates the B8 race where service-layer code reading RunContext.get() AFTER the call always saw null because AuditAdvisor's finally-block had already cleared it. With pre-allocation we no longer need to read RunContext.get() in DefaultChatServiceImpl at all.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:compileJava :ai-agent:ai-agent-starter:compileJava -q</automated>
  </verify>
  <acceptance_criteria>
    - AIAutoConfiguration declares `@Bean ChatMemory` and `@Bean JdbcChatMemoryRepository` with `@ConditionalOnMissingBean`.
    - AIAutoConfiguration NO LONGER declares `@Bean ChatClient` (now owned by ChatClientFactory).
    - DefaultChatServiceImpl injects `ChatClient` (single cached instance) - does NOT call `ChatClient.builder(...)` anywhere in its method bodies.
    - DefaultChatServiceImpl constructor includes `BaselineContextProvider baselineContextProvider` parameter (B5).
    - DefaultChatServiceImpl.ask calls `baselineContextProvider.compose(convId)` and prepends to system prompt before `.user(message)`.
    - DefaultChatServiceImpl.ask allocates `UUID runId = UUID.randomUUID()` BEFORE `chatClient.prompt()` (B8).
    - DefaultChatServiceImpl.ask passes runId via `.advisors(spec -> spec.param("audit.runId", runId))` (B8).
    - DefaultChatServiceImpl.ask body does NOT call `RunContext.get()` (B8 - obsolete; runId is now local).
    - DefaultChatServiceImpl.ask passes `message` as 3rd arg to `conversationGateway.loadOrCreate(userId, conversationId, message)` (B4).
    - DefaultChatServiceImpl.ask calls `conversationGateway.loadOrCreate(...)` BEFORE `chatClient.prompt()`.
    - DefaultChatServiceImpl returns `new ChatResponseDto(convId, runId, content, model, latencyMs)`.
    - Grep check: `grep -n "RunContext.get" ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java` returns zero matches.
    - Grep check: `grep -n "audit.runId" ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java` returns at least one match.
    - Grep check: `grep -n "baselineContextProvider" ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java` returns at least one match.
    - `./gradlew :ai-agent:ai-agent:compileJava :ai-agent:ai-agent-starter:compileJava -q` exits 0.
  </acceptance_criteria>
  <done>End-to-end orchestration is wired with B5 (baseline composition) and B8 (runId pre-allocation) fixes applied. A single ChatService.ask call exercises ownership check (using createdBy + firstMessage title) -> baseline+profile system prompt -> cached client -> advisor chain (audit reading pre-allocated runId, memory, tool with .disableMemory()) -> audited tool callbacks -> dual-layer projection (with createdDate). Plan 04-05 verifies behavior with integration tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller (User-A) -> ChatService.ask | userId from authenticated session is the sole authority for conversation ownership. |
| ChatService -> ConversationGateway | conversationId may be attacker-controlled (probe for other users' IDs) - ownership check is mandatory and opaque. |
| ChatClient -> tool callbacks | Tool inputs come from the LLM (potentially adversarial) - wrapping with ToolCallbackAuditDecorator ensures every call is logged. |
| ProjectingChatMemoryRepository -> AiMessage | Projection joins outer tx so a Jmix policy violation rolls back BOTH the AiMessage and the SPRING_AI_CHAT_MEMORY row. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Information Disclosure | ConversationGateway.loadOrCreate | mitigate | D-09 opacity: identical exception class + message for missing-vs-not-yours; predicate combines id AND createdBy in the SAME query (no probe channel via timing or error type). |
| T-04-04-02 | Tampering | ProjectingChatMemoryRepository | mitigate | All mutating methods `@Transactional` REQUIRED - projection rolls back atomically with delegate; cannot half-write. |
| T-04-04-03 | Spoofing | ChatService.ask userId parameter | accept | Caller (Phase 4 has no UI) is responsible for passing authenticated userId; Phase 5+ UI integration must pass `currentAuthentication.getUser().getKey()`. Documented in JavaDoc. |
| T-04-04-04 | Denial of Service | ChatClientFactory single bean | accept | One ChatClient per app instance - no per-request build cost. Spring AI ChatClient is thread-safe per docs. |
| T-04-04-05 | Information Disclosure | DefaultChatServiceImpl error propagation | mitigate | Exceptions propagate raw to caller; AuditAdvisor records errorClass (FQN only, not message) - caller responsible for sanitizing before user-facing display. |
| T-04-04-06 | Information Disclosure | Conversation title from firstMessage (D-08) | accept | First-user-message titles may contain sensitive substrings; truncation to 80 chars limits exposure surface, and titles are gated by the same createdBy ownership predicate as the conversation itself. |
</threat_model>

<verification>
- `./gradlew :ai-agent:ai-agent:compileJava :ai-agent:ai-agent-starter:compileJava -q` exits 0.
- Grep: `grep -rn "ChatClient.builder" ai-agent/ai-agent/src/main/java` returns ONLY ChatClientFactory.java (no per-request rebuilds).
- Grep: `grep -rn "@Transactional" ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/ProjectingChatMemoryRepository.java` shows `@Transactional` on saveAll + deleteByConversationId.
- Grep: `grep -n "c.createdBy = :owner" ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/ConversationGateway.java` matches the ownership-predicate query (B3).
- Grep: `grep -n "setCreatedBy" ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/ConversationGateway.java` matches at least once (B3 - ownership field is createdBy not userId).
- Grep: `grep -n "setCreatedDate" ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/ProjectingChatMemoryRepository.java` matches at least once on AiMessage row (NOT setCreatedAt).
- Grep: `grep -n "setCreatedAt" ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/ProjectingChatMemoryRepository.java` returns ZERO matches.
- Grep: `grep -n "\.disableMemory()" ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/ChatClientFactory.java` matches at least once (B7 - literal verified method name).
- Grep: `grep -n "\.advisorOrder(" ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/ChatClientFactory.java` matches at least once (B7 - ToolCallAdvisor uses advisorOrder not order).
- Grep: `grep -n "audit.runId" ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java` matches at least once (B8).
- Grep: `grep -n "baselineContextProvider" ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java` matches at least once (B5).
- Grep: `grep -n "RunContext.get" ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java` returns ZERO matches (B8 - service no longer reads RunContext).
- Grep: `grep -n "loadOrCreate(.*,.*,.*)" ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java` matches once - call passes 3 args including the message (B4).
- Grep: `grep -n "new ToolCallbackAuditDecorator" ai-agent/ai-agent/src/main/java/com/vn/agent/tools/AgentToolCallbacks.java` matches at least once.
</verification>

<success_criteria>
- One cached ChatClient bean with three default advisors in verified order (B7: literal `.disableMemory()` and `.advisorOrder(...)` for ToolCallAdvisor).
- ConversationGateway enforces opaque ownership using AiConversation.createdBy (B3) and applies D-08 title rule from firstMessage (B4).
- ProjectingChatMemoryRepository decorates with @Primary, projects in same REQUIRED tx, and uses AiMessage.createdDate (NOT createdAt).
- DefaultChatServiceImpl returns ChatResponseDto with conversationId, runId, content, model, latencyMs.
- DefaultChatServiceImpl pre-allocates runId before chatClient.prompt() and passes via advisor context (B8).
- DefaultChatServiceImpl injects BaselineContextProvider and composes baseline + profile system prompt per request (B5/D-15).
- AgentToolCallbacks wraps every callback with audit decorator.
- Compile succeeds across ai-agent + ai-agent-starter modules.
</success_criteria>

<output>
After completion, create `.planning/phases/04-orchestration-core/04-04-SUMMARY.md` documenting:
- Final advisor wiring (FQN + order constants; note advisorOrder vs order setter difference)
- ChatService.ask flow (ownership-by-createdBy -> baseline composition -> client -> advisors with pre-allocated runId -> audit -> projection-by-createdDate)
- AIAutoConfiguration bean inventory after Phase 4 (what's @ConditionalOnMissingBean vs always-on)
- Dual-layer write semantics confirmed
- Files created/modified
- Confirmation that B1-B8 fixes from checker review are landed: B1 (audit field names in 04-03), B2 (conversationId via conversation.id), B3 (createdBy), B4 (title from firstMessage), B5 (baseline composition), B6 (orchestration package consolidation), B7 (verified disableMemory + advisorOrder), B8 (runId pre-allocation).
</output>
