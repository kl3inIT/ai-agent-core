---
phase: 12-configurable-chat-surfaces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiChatSurface.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiUiSettings.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/AiUiSettingsService.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/exposure/AiInternalEntityNames.java
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog.xml
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/080-ai-ui-settings.xml
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties
  - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/AiUiSettingsServiceSingletonTest.java
autonomous: true
requirements:
  - SURF-01
  - SURF-02
  - SURF-06
  - SURF-07
  - SURF-10
  - ENT-06
must_haves:
  truths:
    - "Phase 12 planning docs are amended first: ROADMAP.md and REQUIREMENTS.md describe the locked 2-surface scope from 12-CONTEXT.md, not the stale FULL_ROUTE/SIDEBAR/FLOATING shape. This records D-01..D-08 scope before code execution starts."
    - "AiChatSurface is a Jmix EnumClass<String> with exactly FULL_ROUTE and HEADER_BUTTON; SIDEBAR and FLOATING do not exist in production code."
    - "AiUiSettings is a @JmixEntity @Store(name=\"agentstore\") entity with UUID id, @Version, @InstanceName, enabledSurfaceIds stored in a Jmix-safe text column, defaultSurface stored as an enum id string, fixed SINGLETON_ID, and no JavaBean property named enabledSurfaces that Jmix could inspect as an enum collection."
    - "The `080-ai-ui-settings.xml` Liquibase changelog is included from the parent `agentstore-changelog.xml`; creating the child changelog alone is not sufficient."
    - "AiUiSettingsService.loadCurrent() is no-cache and uses UnconstrainedDataManager for singleton ensure-default/load because normal users have no AiUiSettings entity policy. This covers D-04 and the project security-data rule."
    - "Default settings enable FULL_ROUTE and HEADER_BUTTON with defaultSurface=FULL_ROUTE; no dialog size/position fields are introduced, preserving D-06."
    - "SURF-06/SURF-07/SURF-10 are reshaped or deferred in REQUIREMENTS.md exactly as 12-CONTEXT.md says: no bottom-right raw Vaadin floating launcher, no P-21 stacking mitigation, no compact-mode work in v1.1."
    - "AiInternalEntityNames always excludes ai_AiUiSettings from LLM-visible metadata."
    - "Both locale bundles contain entity captions, field captions, and AiChatSurface enum captions."
    - "Review HIGH 12-01/12-02 is resolved: the persisted setting is named `enabledSurfaceIds`; typed helpers are named `getEnabledSurfaceSet()` / `setEnabledSurfaceSet(...)`; UI/controller state may use `enabledSurfaces`, but the Jmix entity does not expose it as a bean property."
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiChatSurface.java"
      provides: "Two-value surface enum"
      contains: "HEADER_BUTTON"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiUiSettings.java"
      provides: "Single-row Jmix entity in agentstore"
      contains: "SINGLETON_ID"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/AiUiSettingsService.java"
      provides: "No-cache settings loader and default row creator"
      contains: "UnconstrainedDataManager"
    - path: "ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/080-ai-ui-settings.xml"
      provides: "AI_UI_SETTINGS table DDL"
      contains: "AI_UI_SETTINGS"
    - path: "ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog.xml"
      provides: "Parent changelog inclusion"
      contains: "080-ai-ui-settings.xml"
  key_links:
    - from: "AiUiSettingsService.loadCurrent"
      to: "ChatSurfaceMounter and ChatView gate"
      via: "fresh per-navigation read"
      pattern: "loadCurrent\\("
---

<objective>
Prepare the phase-12 foundation before any UI surface work lands: rewrite the active planning docs to the approved two-surface contract, add the `AiChatSurface` enum, create the singleton `AiUiSettings` data model and service, wire the Liquibase table, exclude the settings entity from LLM-visible metadata, and prove the default row behavior.

This plan intentionally removes the stale three-surface assumptions from ROADMAP/REQUIREMENTS as executable work. It is the guard against accidentally building `SIDEBAR`, `FLOATING`, raw Vaadin bottom-right launchers, P-21 dialog stacking mitigation, or `setCompactMode(boolean)` in phase 12.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-configurable-chat-surfaces/12-SPEC.md
@.planning/phases/12-configurable-chat-surfaces/12-CONTEXT.md
@.planning/phases/12-configurable-chat-surfaces/12-RESEARCH.md
@.planning/phases/12-configurable-chat-surfaces/12-AI-SPEC.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiParameters.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiMessageRole.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/exposure/AiInternalEntityNames.java
@ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog.xml
@ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/070-ai-mutation-intent.xml
@ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties
@ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties

<verified_docs>
- Context7 `/jmix-framework/jmix-context7`: Jmix entities require the normal JPA + `@JmixEntity` metadata shape and Jmix Flow UI/i18n expects root bundle captions.
- Local Jmix skills used: `jmix-entities`, `jmix-enums`, `jmix-liquibase`, `jmix-services`, `jmix-i18n`.
</verified_docs>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Amend roadmap and requirements to the locked 2-surface scope</name>
  <files>
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - .planning/phases/12-configurable-chat-surfaces/12-CONTEXT.md (Phase Boundary, In scope, Out of scope)
    - .planning/phases/12-configurable-chat-surfaces/12-SPEC.md (Requirements 1, 5, 9 and acceptance checklist)
  </read_first>
  <action>
Rewrite only the Phase 12/SURF sections:
- ROADMAP Phase 12 goal: `FULL_ROUTE` + `HEADER_BUTTON`, not `FULL_ROUTE` + `SIDEBAR` + `FLOATING`.
- ROADMAP success criteria: admin toggles FULL_ROUTE/HEADER_BUTTON, header button opens top-right Jmix `DialogWindow`, one conversation id survives route/dialog switches, no raw Vaadin launcher.
- REQUIREMENTS SURF-01: two surfaces over the same backend.
- REQUIREMENTS SURF-02: `AiUiSettings` fields are `enabledSurfaceIds` text-backed `FULL_ROUTE,HEADER_BUTTON` id list plus `defaultSurface`; controller/view-model state may call this enabled surfaces, but the entity must not expose a JavaBean `enabledSurfaces` collection property; no `AiParameters` bundling.
- REQUIREMENTS SURF-06/SURF-07: replace bottom-right launcher/z-index language with Jmix non-modal `DialogWindow` top-right and note P-21 is moot.
- REQUIREMENTS SURF-10: mark compact mode deferred because the full fragment layout is used in both surfaces.
Keep requirement IDs stable so the coverage gate remains traceable.
  </action>
  <verify>
    <automated>rg -n "SIDEBAR|FLOATING|bottom-right|P-21|setCompactMode" .planning/ROADMAP.md .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP Phase 12 mentions `HEADER_BUTTON`.
    - ROADMAP Phase 12 no longer claims three active surfaces.
    - REQUIREMENTS SURF-01 lists only `FULL_ROUTE` and `HEADER_BUTTON`.
    - REQUIREMENTS SURF-02 names the persisted field `enabledSurfaceIds`, not `enabledSurfaces`.
    - REQUIREMENTS SURF-06/SURF-07 no longer require raw Vaadin `Dialog` or P-21 mitigation.
    - Any remaining `SIDEBAR`, `FLOATING`, `bottom-right`, `P-21`, or `setCompactMode` match is explicitly marked deferred/out-of-scope/future.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add AiChatSurface enum and AiUiSettings entity + Liquibase</name>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiChatSurface.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiUiSettings.java,
    ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog.xml,
    ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/080-ai-ui-settings.xml,
    ai-agent/ai-agent/src/main/java/com/vn/agent/exposure/AiInternalEntityNames.java,
    ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties,
    ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties
  </files>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiParameters.java
    - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiMessageRole.java
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/070-ai-mutation-intent.xml
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog.xml
    - C:/Users/admin/.codex/skills/jmix-entities/SKILL.md
    - C:/Users/admin/.codex/skills/jmix-enums/SKILL.md
  </read_first>
  <action>
Create `AiChatSurface` using the standard `EnumClass<String>` + `fromId` pattern:
- `FULL_ROUTE("FULL_ROUTE")`
- `HEADER_BUTTON("HEADER_BUTTON")`
- `@Nullable static fromId(String id)`

Create `AiUiSettings`:
- package `com.vn.agent.entity`
- `@Store(name = "agentstore")`, `@JmixEntity`, `@Entity(name = "ai_AiUiSettings")`, `@Table(name = "AI_UI_SETTINGS")`
- UUID id with `@JmixGeneratedValue`, `Integer version` with `@Version`
- `@InstanceName` on a stable name field or method
- `enabledSurfaceIds` persisted as deterministic comma-separated enum ids in one text/varchar column; expose typed helpers `getEnabledSurfaceSet()` / `setEnabledSurfaceSet(Set<AiChatSurface>)`; do not add `getEnabledSurfaces()` or `setEnabledSurfaces(...)` because Jmix/JavaBean inspection may treat that as an unsupported enum collection entity property
- `defaultSurface` persisted as a string id with enum getter/setter
- fixed `SINGLETON_ID` UUID constant
- defaults: both surfaces enabled, `FULL_ROUTE` default
- no `@ElementCollection` for enum set

Create `080-ai-ui-settings.xml` in `agentstore-changelog` with UUID id, VERSION, ENABLED_SURFACE_IDS, DEFAULT_SURFACE, CREATED_BY, CREATED_DATE, LAST_MODIFIED_BY, and LAST_MODIFIED_DATE columns matching existing add-on audit conventions. Use `${uuid.type}` and explicit varchar/text types.

Update the parent `agentstore-changelog.xml` to include the new child changelog. Treat this as a blocking acceptance item: without the parent include, the table is not created on startup.

Add `"ai_AiUiSettings"` to `AiInternalEntityNames`.

Add captions to both bundles:
- entity and fields for `AiUiSettings`
- enum and values for `AiChatSurface`
  </action>
  <verify>
    <automated>./gradlew :ai-agent:compileJava</automated>
  </verify>
  <acceptance_criteria>
    - `AiChatSurface.java` contains exactly `FULL_ROUTE("FULL_ROUTE")` and `HEADER_BUTTON("HEADER_BUTTON")`.
    - `AiUiSettings.java` contains `@Store(name = "agentstore")`, `@JmixEntity`, `@JmixGeneratedValue`, `@Version`, and `@InstanceName`.
    - `AiUiSettings.java` does not contain `@ElementCollection`.
    - `AiUiSettings.java` contains `SINGLETON_ID`.
    - `AiUiSettings.java` contains `enabledSurfaceIds`, `getEnabledSurfaceSet()`, and `setEnabledSurfaceSet(`.
    - `AiUiSettings.java` does not contain `getEnabledSurfaces(` or `setEnabledSurfaces(`.
    - `080-ai-ui-settings.xml` creates `AI_UI_SETTINGS` with `ID`, `VERSION`, `ENABLED_SURFACE_IDS`, `DEFAULT_SURFACE`, `CREATED_BY`, `CREATED_DATE`, `LAST_MODIFIED_BY`, and `LAST_MODIFIED_DATE`.
    - `agentstore-changelog.xml` includes `agentstore-changelog/080-ai-ui-settings.xml`.
    - `AiInternalEntityNames.java` contains `"ai_AiUiSettings"`.
    - Both locale bundles contain captions for `AiUiSettings.enabledSurfaceIds`, `AiUiSettings.defaultSurface`, `AiChatSurface.FULL_ROUTE`, and `AiChatSurface.HEADER_BUTTON`.
    - `./gradlew :ai-agent:compileJava` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add AiUiSettingsService singleton loader</name>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/AiUiSettingsService.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/AiUiSettingsServiceSingletonTest.java
  </files>
  <read_first>
    - .planning/phases/12-configurable-chat-surfaces/12-CONTEXT.md (D-04 and Claude's Discretion)
    - C:/Users/admin/.codex/skills/jmix-services/SKILL.md
    - ai-agent/ai-agent/src/test/java/com/vn/agent/parameters/DefaultParamsSeederTest.java
  </read_first>
  <action>
Create `AiUiSettingsService` as a Spring component with constructor injection:
- inject `DataManager` for entity creation and `UnconstrainedDataManager` for trusted load/save
- `loadCurrent()` loads `AiUiSettings.SINGLETON_ID`; on miss creates via `dataManager.create(AiUiSettings.class)`, sets id to `SINGLETON_ID`, sets default values, saves with `unconstrainedDataManager`
- first-create race handling: wrap the create/save path so duplicate-key or optimistic insert conflicts are caught and followed by a reload of `AiUiSettings.SINGLETON_ID`; concurrent first UI initialization must not fail or create a second row
- no cache, no static mutable state, no real-time UI push
- no raw JPQL `loadValue`; if raw JPQL is introduced later, it must call `.store("agentstore")`

Add a focused `@SpringBootTest` with admin authentication that verifies:
- first `loadCurrent()` creates exactly one row
- repeated `loadCurrent()` returns the same id without duplicates
- defaults are both surfaces enabled and `FULL_ROUTE` default
  </action>
  <verify>
    <automated>./gradlew :ai-agent:test --tests "com.vn.agent.view.chat.AiUiSettingsServiceSingletonTest"</automated>
  </verify>
  <acceptance_criteria>
    - `AiUiSettingsService` uses constructor injection.
    - `AiUiSettingsService` injects `UnconstrainedDataManager`.
    - `loadCurrent()` creates the singleton row on first miss.
    - Test asserts there is exactly one `AiUiSettings` row after two sequential loads and after a simulated concurrent first-load race.
    - Focused test exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin settings UI/service -> agentstore | Admin-controlled surface settings influence which chat entry points render for all users. |
| LLM metadata surface -> internal settings entity | `AiUiSettings` is operational configuration and must never be exposed as a chat-queryable business entity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Mitigation Plan |
|-----------|----------|-----------|----------|-----------------|
| T-12-01-01 | Elevation of Privilege | AiUiSettingsService | HIGH | Only trusted service code uses `UnconstrainedDataManager`; user-facing updates are added in Plan 12-02 behind `AiAgentAdminRole`. |
| T-12-01-02 | Information Disclosure | AiInternalEntityNames | HIGH | Add `ai_AiUiSettings` to the always-excluded set and test it. |
| T-12-01-03 | Tampering | Planning docs | MEDIUM | Update ROADMAP/REQUIREMENTS before implementation so stale three-surface requirements cannot drive execution. |
</threat_model>

<verification>
- `./gradlew :ai-agent:compileJava`
- `./gradlew :ai-agent:test --tests "com.vn.agent.view.chat.AiUiSettingsServiceSingletonTest"`
- JetBrains MCP `get_file_problems` on all new/modified Java files.
</verification>

<success_criteria>
- ROADMAP and REQUIREMENTS reflect the two-surface phase-12 contract.
- `AiChatSurface` and `AiUiSettings` compile and are localized.
- Liquibase creates `AI_UI_SETTINGS`.
- `AiUiSettingsService.loadCurrent()` creates and reloads the singleton through `UnconstrainedDataManager`.
- `AiUiSettings` is excluded from LLM-visible internal entity discovery.
</success_criteria>

<output>
After completion, create `.planning/phases/12-configurable-chat-surfaces/12-01-SUMMARY.md`.
</output>
