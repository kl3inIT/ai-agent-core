---
phase: 10-ai-specific-llm-exposure-policy
plan: 10
type: execute
wave: 6
depends_on: [10-04, 10-05, 10-06, 10-07, 10-08, 10-09]
files_modified:
  - ai-agent/ai-agent/src/test/java/com/vn/agent/exposure/LlmExposurePolicyIntegrationTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/rag/RetrievalFilterBuilderDenylistTest.java
autonomous: true
requirements:
  - TEST-09
  - EXP-09

must_haves:
  truths:
    - "LlmExposurePolicyIntegrationTest seeds an AiExposureRule for a deterministic known entity chosen at test setup (not 'first non-system entity' dynamic selection) — reuses the target entity pattern from FilteredSchemaAndExecutionDenialTest or equivalent existing test"
    - "LlmExposurePolicyIntegrationTest builds Authentication principals using the same helper/pattern as FilteredSchemaAndExecutionDenialTest (or equivalent) — no ad-hoc authority string guesses"
    - "LlmExposurePolicyIntegrationTest asserts the entity does NOT appear in list_entities result"
    - "LlmExposurePolicyIntegrationTest asserts the entity does NOT appear in agent.entities baseline"
    - "LlmExposurePolicyIntegrationTest asserts find_records(denylisted entity) returns 'unknown_entity' NOT 'access_denied'"
    - "LlmExposurePolicyIntegrationTest includes end-to-end assertion: when an AiExposureRule is enabled, a RAG retrieval request via RetrievalFilterBuilder produces a Filter.Expression containing source_entity NOT IN <denied> — mocked VectorStore captures the SearchRequest filter"
    - "RetrievalFilterBuilderDenylistTest uses a deterministic seeded entity name (e.g. 'OrderEntity' or known test-app entity) — not dynamic runtime selection"
    - "RetrievalFilterBuilderDenylistTest includes assertion that a chunk with NO source_entity metadata key is NOT excluded by the filter for any non-empty denylist (D-06 Fix R6 cross-link)"
    - "Both tests pass in ./gradlew test"
  artifacts:
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/exposure/LlmExposurePolicyIntegrationTest.java"
      provides: "@SpringBootTest integration test — 3 opacity assertion paths (list_entities, agent.entities, find_records) + 1 RAG filter clause assertion"
      contains: "@SpringBootTest"
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/rag/RetrievalFilterBuilderDenylistTest.java"
      provides: "Unit test asserting nin clause in built Filter.Expression + null-key visibility assertion"
      contains: "getDenylistedEntityNames"
  key_links:
    - from: "LlmExposurePolicyIntegrationTest.seedDenylistRule()"
      to: "dataManager.save(rule)"
      via: "UnconstrainedDataManager seeding AiExposureRule"
      pattern: "dataManager\\.save"
    - from: "RetrievalFilterBuilderDenylistTest"
      to: "RetrievalFilterBuilder.buildFor()"
      via: "Mockito mock for LlmExposurePolicy.getDenylistedEntityNames()"
      pattern: "getDenylistedEntityNames"
---

<objective>
Implement TEST-09: integration test asserting uniform `unknown_entity` opacity across all four assertion paths (`list_entities`, `agent.entities`, `find_records`, RAG filter clause) plus a dedicated unit test (`RetrievalFilterBuilderDenylistTest`) asserting the `Filter.Expression` shape.

Fix R8 applied:
- Use a deterministic seeded entity name (not dynamic "first non-system entity" which causes nondeterminism).
- Build Authentication principals exactly as `FilteredSchemaAndExecutionDenialTest` (or the closest existing integration test) — no ad-hoc authority strings.
- Add end-to-end assertion: when an `AiExposureRule` is enabled, `RetrievalFilterBuilder.buildFor()` produces a `Filter.Expression` that contains the `source_entity NOT IN <denied>` clause. Use a mocked `VectorStore` to capture the `SearchRequest` and assert the filter clause shape.
- Add unit-test assertion (Fix R6 cross-link): a chunk with NO `source_entity` metadata key is NOT excluded by the filter for any non-empty denylist (D-06 legacy-doc contract).

The RAG assertion path (fourth opacity check) is covered by `RetrievalFilterBuilderDenylistTest` as a unit test (not a full integration test with a live vector store) per RESEARCH Open Question 2 recommendation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-ai-specific-llm-exposure-policy/10-CONTEXT.md
@.planning/phases/10-ai-specific-llm-exposure-policy/10-RESEARCH.md

<interfaces>
<!-- Source: RESEARCH.md TEST-09 skeleton code -->
<!-- Source: FilteredSchemaAndExecutionDenialTest.java — template for @SpringBootTest + SystemAuthenticator.withUser -->
<!-- Source: RESEARCH.md Pattern 1: LlmExposurePolicy DenylistTest — Mockito mock approach -->

<!-- Fix R8 — deterministic entity choice: -->
// DO NOT use: metadata.getSession().getClasses().stream().filter(...).findFirst()
// USE: a hardcoded entity name known to exist in the test application's metamodel.
// Check FilteredSchemaAndExecutionDenialTest (or closest test) for which entity it uses.
// If that test uses e.g. "test_Order" or "sample_Customer", reuse the same entity.
// If the test app does not have a fixed entity, pick the host-application entity most
// analogous to "Customer" or "Order" from the @Entity(name=...) registry.
// Store as: private static final String TEST_ENTITY_NAME = "chosen_entity_name";

<!-- Fix R8 — authentication helper: -->
// DO NOT construct: new UsernamePasswordAuthenticationToken("user", ..., List.of(new SimpleGrantedAuthority("ROLE_X")))
// DO: read FilteredSchemaAndExecutionDenialTest and use the EXACT same authentication helper.
// Examples of what may exist:
//   SystemAuthenticator.withUser("testuser", () -> { ... });
//   OR a test helper that creates a Spring Security Authentication with the right Jmix UserDetails

<!-- Fix R8 — RAG filter clause assertion: -->
// End-to-end: seed an AiExposureRule, build the filter via RetrievalFilterBuilder.buildFor(auth),
// assert the resulting Filter.Expression string contains ChunkMetadata.SOURCE_ENTITY and NIN/NOT_IN.
// This can be a standalone @Test in LlmExposurePolicyIntegrationTest OR a separate test.
// Using a mocked VectorStore is the cleanest approach (no live pgvector needed):
//   RetrievalFilterBuilder builder = ... (Spring context injection or constructed);
//   Filter.Expression expr = builder.buildFor(auth);
//   assertThat(expr.toString()).contains(ChunkMetadata.SOURCE_ENTITY);
//   assertThat(expr.toString().toUpperCase()).containsAnyOf("NIN", "NOT IN", "NOT_IN");
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RetrievalFilterBuilderDenylistTest unit test (including null-key Fix R6 assertion)</name>
  <files>
    ai-agent/ai-agent/src/test/java/com/vn/agent/rag/RetrievalFilterBuilderDenylistTest.java
  </files>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/rag/RetrievalFilterBuilder.java (buildFor() method — admin bypass check, filter composition, constructor params)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/exposure/LlmExposurePolicy.java (getDenylistedEntityNames() — public)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/rag/ChunkMetadata.java (SOURCE_ENTITY constant)
    - (find existing rag test if any — for import patterns and Mockito setup)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/security/AiAgentAdminRole.java (CODE constant — for admin bypass test)
  </read_first>
  <behavior>
    - Test 1: buildFor() with non-empty denylist {"OrderEntity"} — result filter expression string/toString contains SOURCE_ENTITY and either "NIN" or "NOT_IN" (case-insensitive)
    - Test 2: buildFor() with empty denylist {} — result filter expression does NOT contain SOURCE_ENTITY (no overhead)
    - Test 3: admin user — buildFor() returns null (bypass) even when denylist non-empty
    - Test 4 (Fix R6 cross-link / D-06): when denylist is {"OrderEntity"}, a Document with NO "source_entity" metadata key is NOT excluded — verify by checking the Filter.Expression does not exclude null-key documents (assert the expression uses nin semantics that preserve missing-key visibility OR assert defensive nullable form if that was chosen in Plan 05)
  </behavior>
  <action>
Read the existing RAG tests and `RetrievalFilterBuilder` constructor to understand the exact parameter types before writing the test.

Create `RetrievalFilterBuilderDenylistTest.java` in `com.vn.agent.rag` test package. Use Mockito for `LlmExposurePolicy`, `AiAgentRagProperties`, `AiAgentEmbeddingProperties` (or whatever the actual constructor params are — read the file first).

Key test cases:

1. `whenDenylistNonEmpty_thenFilterContainsNinClause()` — mock getDenylistedEntityNames() returning Set.of("OrderEntity"); non-admin auth; assert expr.toString() contains ChunkMetadata.SOURCE_ENTITY AND contains "NIN" or "NOT_IN" (upper-cased).

2. `whenDenylistEmpty_thenNoSourceEntityClause()` — mock getDenylistedEntityNames() returning empty set; non-admin auth; if expr non-null, assert expr.toString() does NOT contain ChunkMetadata.SOURCE_ENTITY.

3. `whenAdminUser_thenBuildForReturnsNull()` — denylist non-empty; admin authority; assert result is null.

4. `whenDenylistNonEmptyAndChunkHasNoSourceEntityKey_thenChunkRemainsVisible()` (Fix R6 / D-06) — verify that the Filter.Expression's NIN semantics preserve null-key visibility:
   - If Plan 05 chose bare `b.nin(SOURCE_ENTITY, denied)`: assert the expression does NOT include an explicit null-exclude clause (relying on pgvector missing-key = visible semantics).
   - If Plan 05 chose defensive `b.or(b.isNull(SOURCE_ENTITY), b.nin(...))`: assert the expression contains either isNull or the OR structure.
   - At minimum: assert that the Filter.Expression string does NOT contain a clause that would match/exclude a document with missing source_entity (e.g., not `source_entity == null` in an AND-exclude context).
   - Document the assertion rationale in a comment above the test.

Use `assertThat(...).containsAnyOf("NIN", "NOT IN", "NOT_IN")` for case-insensitive NIN detection.

Build the authentication mock exactly as needed for RetrievalFilterBuilder's admin bypass check. Read `RetrievalFilterBuilder.buildFor()` to see how it extracts authority strings and compare with `AiAgentAdminRole.CODE` to construct the correct authority.

After creating the file, run JetBrains MCP `get_file_problems` and triage.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.rag.RetrievalFilterBuilderDenylistTest" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - All four tests pass
    - `grep -c "getDenylistedEntityNames" ai-agent/ai-agent/src/test/java/com/vn/agent/rag/RetrievalFilterBuilderDenylistTest.java` returns at least 2
    - `grep -c "SOURCE_ENTITY\|source_entity" ai-agent/ai-agent/src/test/java/com/vn/agent/rag/RetrievalFilterBuilderDenylistTest.java` returns at least 2
    - `grep -c "D-06\|null.*key\|legacy.*doc\|missing.*key\|source_entity.*null\|null.*source_entity" ai-agent/ai-agent/src/test/java/com/vn/agent/rag/RetrievalFilterBuilderDenylistTest.java` returns at least 1 (Fix R6 test present with rationale comment)
    - No @SpringBootTest on this test (pure unit test with Mockito)
  </acceptance_criteria>
  <done>RetrievalFilterBuilderDenylistTest passes all four tests. Confirms nin clause present for non-empty denylist, absent for empty denylist, null for admin, and D-06 null-key visibility preserved (Fix R6).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LlmExposurePolicyIntegrationTest — uniform opacity across 3 paths + RAG filter clause assertion</name>
  <files>
    ai-agent/ai-agent/src/test/java/com/vn/agent/exposure/LlmExposurePolicyIntegrationTest.java
  </files>
  <read_first>
    - (find and read) the existing @SpringBootTest integration test that uses SystemAuthenticator or equivalent authentication — e.g. FilteredSchemaAndExecutionDenialTest.java or its equivalent in the codebase. READ IT IN FULL. Extract: test config annotations, authentication helper usage, test entity name, cleanup pattern.
    - ai-agent/ai-agent/src/main/java/com/vn/agent/tools/BuiltInDataTools.java (findRecords method signature — entityName param name and return type)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/BaselineContextProvider.java (compose(UUID) return type and agent.entities key)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/rag/RetrievalFilterBuilder.java (buildFor(Authentication) signature)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/rag/ChunkMetadata.java (SOURCE_ENTITY constant)
    - .planning/phases/10-ai-specific-llm-exposure-policy/10-RESEARCH.md (TEST-09 skeleton code)
    - .planning/phases/10-ai-specific-llm-exposure-policy/10-CONTEXT.md (D-09 test harness shape)
  </read_first>
  <behavior>
    - Test: denylisted entity does NOT appear in list_entities result
    - Test: denylisted entity does NOT appear in agent.entities baseline
    - Test: find_records(denylisted entity name) returns "unknown_entity" and does NOT contain "access_denied"
    - Test: RAG filter clause — when AiExposureRule is enabled, RetrievalFilterBuilder.buildFor(auth) produces a Filter.Expression containing ChunkMetadata.SOURCE_ENTITY in a NIN/NOT_IN clause
  </behavior>
  <action>
CRITICAL (Fix R8): Before writing the test, READ the existing @SpringBootTest integration test (FilteredSchemaAndExecutionDenialTest or equivalent) to extract:
1. Exact test configuration annotations (e.g., @SpringBootTest, @AutoConfigureTestDatabase, custom test config class)
2. How authentication is set up (e.g., SystemAuthenticator.withUser("admin", ...) or custom SecurityContext setup)
3. Which entity the existing test uses (extract that entity name — use the SAME entity for this test)
4. How data is seeded and cleaned up (follow the exact same pattern)

Create `LlmExposurePolicyIntegrationTest.java` in package `com.vn.agent.exposure`:

1. Class annotations: IDENTICAL to the existing integration test (same @SpringBootTest config, same test profile if any)
2. Fields: follow same injection pattern as existing test
3. `private static final String TEST_ENTITY_NAME` — hardcoded to the known test entity (from step 3 above)
4. `@BeforeEach void seedDenylistRule()` — create AiExposureRule with `entityName = TEST_ENTITY_NAME`, `enabled = true`, `mode = EXCLUDE`. Use UnconstrainedDataManager.save(). The entity must be readable by the test user WITHOUT the denylist rule.
5. `@AfterEach void cleanUp()` — delete the seeded rule to avoid test pollution
6. Test methods:

```java
@Test
void denylistedEntityNotInListEntities() {
    // use the same auth setup as FilteredSchemaAndExecutionDenialTest
    // assert: builtInDataTools.listEntities() result does NOT contain TEST_ENTITY_NAME
}

@Test
void denylistedEntityNotInAgentEntities() {
    // use the same auth setup
    // assert: baselineContextProvider.compose(UUID.randomUUID())
    //         .get("agent.entities").toString() does NOT contain TEST_ENTITY_NAME
}

@Test
void findRecordsDenylistedEntityReturnsUnknownEntityNotAccessDenied() {
    // use the same auth setup
    // assert result contains "unknown_entity"
    // assert result does NOT contain "access_denied"
    // Note: method signature may be findRecords(String entityName, ...) — read BuiltInDataTools
}

@Test
void ragFilterContainsDenylistNinClause() {
    // No live VectorStore needed — just call RetrievalFilterBuilder.buildFor(auth) directly
    // Use the same authentication as above (non-admin user that would normally see TEST_ENTITY_NAME)
    // Get auth from test context or construct via the same helper
    Filter.Expression expr = retrievalFilterBuilder.buildFor(auth);
    assertThat(expr).isNotNull();
    assertThat(expr.toString()).contains(ChunkMetadata.SOURCE_ENTITY);
    assertThat(expr.toString().toUpperCase()).containsAnyOf("NIN", "NOT IN", "NOT_IN");
}
```

Inject `@Autowired RetrievalFilterBuilder retrievalFilterBuilder` alongside the other beans.

After creating the file, run JetBrains MCP `get_file_problems` and triage.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.exposure.LlmExposurePolicyIntegrationTest" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - All four test methods pass
    - `grep -c "@SpringBootTest" ai-agent/ai-agent/src/test/java/com/vn/agent/exposure/LlmExposurePolicyIntegrationTest.java` returns 1
    - `grep -c "unknown_entity" ai-agent/ai-agent/src/test/java/com/vn/agent/exposure/LlmExposurePolicyIntegrationTest.java` returns at least 1
    - `grep -c "access_denied" ai-agent/ai-agent/src/test/java/com/vn/agent/exposure/LlmExposurePolicyIntegrationTest.java` returns at least 1 (the doesNotContain assertion)
    - `grep -c "doesNotContain" ai-agent/ai-agent/src/test/java/com/vn/agent/exposure/LlmExposurePolicyIntegrationTest.java` returns at least 2
    - `grep -c "SOURCE_ENTITY\|source_entity\|ragFilter\|NIN\|NOT_IN" ai-agent/ai-agent/src/test/java/com/vn/agent/exposure/LlmExposurePolicyIntegrationTest.java` returns at least 1 (RAG filter clause assertion present — Fix R8)
    - `grep -c "TEST_ENTITY_NAME\|static final.*String" ai-agent/ai-agent/src/test/java/com/vn/agent/exposure/LlmExposurePolicyIntegrationTest.java` returns at least 1 (deterministic entity name — Fix R8)
    - `./gradlew :ai-agent:ai-agent:test` — full test suite passes (no regressions)
  </acceptance_criteria>
  <done>LlmExposurePolicyIntegrationTest passes all four assertion paths. Denylisted entity absent from list_entities and agent.entities. find_records returns unknown_entity not access_denied. RAG filter clause confirmed to include SOURCE_ENTITY NIN clause. Deterministic test entity (Fix R8). Authentication helpers match existing test pattern (Fix R8). Full test suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test assertions | Tests enforce the security contract: unknown_entity opacity, no access_denied leakage, RAG filter clause shape |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Information Disclosure | uniform unknown_entity opacity (regression gate) | mitigate | LlmExposurePolicyIntegrationTest.findRecordsDenylistedEntityReturnsUnknownEntityNotAccessDenied() is a regression gate. If the opacity contract breaks, this test fails CI. |
| T-10-02 | Information Disclosure | agent.entities denylist leak (regression gate) | mitigate | LlmExposurePolicyIntegrationTest.denylistedEntityNotInAgentEntities() is a regression gate for the baseline prompt. |
| T-10-03 | Information Disclosure | RAG filter clause (regression gate) | mitigate | LlmExposurePolicyIntegrationTest.ragFilterContainsDenylistNinClause() and RetrievalFilterBuilderDenylistTest together gate the RAG filter shape. |
</threat_model>

<verification>
1. `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.rag.RetrievalFilterBuilderDenylistTest"` — passes
2. `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.exposure.LlmExposurePolicyIntegrationTest"` — passes
3. `./gradlew :ai-agent:ai-agent:test` — full suite passes (no regressions)
4. Deterministic entity name confirmed (no "first non-system entity" dynamic selection)
</verification>

<success_criteria>
- RetrievalFilterBuilderDenylistTest: 4 tests pass (nin clause present, absent for empty denylist, null for admin, D-06 null-key visibility)
- LlmExposurePolicyIntegrationTest: 4 tests pass (list_entities opacity, agent.entities opacity, find_records unknown_entity not access_denied, RAG filter clause)
- Deterministic test entity names (Fix R8)
- Authentication principals match existing integration test pattern (Fix R8)
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/10-ai-specific-llm-exposure-policy/10-10-SUMMARY.md`
</output>
