---
phase: 16-admin-settings-model-picker-config-knob-migration
plan: 03
type: execute
wave: 1
depends_on: [01]
files_modified:
  - ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/ChatModelCatalogProperties.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/AdminSecretPatternProperties.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/ChatModelCatalog.java
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/module.properties
autonomous: true
requirements: [MODEL-01, MODEL-03, TEST-20]
requirements_addressed: [MODEL-01, MODEL-03, TEST-20]
tags: [spring-boot, configuration-properties]

must_haves:
  truths:
    - "ChatModelCatalogProperties record binds `jmix.ai-agent.models.catalog[*]` with components (id, labelMessageKey, isDefault) per D-06."
    - "AdminSecretPatternProperties record binds `ai-agent.admin.secret-property-patterns` with the locked default list (`*.api-key`, `*.password`, `*.secret`, `*.token`) per D-02 / SPEC criterion 4."
    - "ChatModelCatalog @Component validates exactly-one-default at @PostConstruct (boot-fast on misconfiguration)."
    - "ChatModelCatalog.SELF_HOSTABLE_OPEN_WEIGHTS_ALLOWLIST is a static immutable Set<String>; comment references `project_self_hostable_models_only.md`."
    - "Default catalog seed in `module.properties` contains exactly one isDefault=true entry whose id equals the value of `model` in `default-params.yaml` (drift gate per SPEC criterion 4)."
    - "TEST-20 (`ChatModelCatalogAllowlistTest`) flips green: catalog ⊆ allowlist + exactly-one-default + drift-equals-default-params-yaml."
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/ChatModelCatalogProperties.java"
      provides: "@ConfigurationProperties record for `jmix.ai-agent.models.catalog`"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/ChatModelCatalog.java"
      provides: "@Component with entries(), defaultEntry(), findById(), and SELF_HOSTABLE_OPEN_WEIGHTS_ALLOWLIST constant"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/AdminSecretPatternProperties.java"
      provides: "@ConfigurationProperties record for `ai-agent.admin.secret-property-patterns`"
    - path: "ai-agent/ai-agent/src/main/resources/com/vn/agent/module.properties"
      provides: "Default catalog seed (≥4 entries, one default-marked, matches default-params.yaml.model)"
  key_links:
    - from: "ParametersDetailView (Plan 05)"
      to: "ChatModelCatalog.entries()"
      via: "modelField.setItems(catalog.entries().stream().map(Entry::id).toList())"
      pattern: "catalog\\.entries\\(\\)"
    - from: "AiUiSettingsDetailView Secrets section (Plan 06)"
      to: "AdminSecretPatternProperties.resolvedPatterns()"
      via: "Environment key match against patterns"
      pattern: "secret-property-patterns"
---

<objective>
Stand up the curated open-weights model catalog (`ChatModelCatalogProperties` + `ChatModelCatalog` + `SELF_HOSTABLE_OPEN_WEIGHTS_ALLOWLIST`), the secret-pattern configuration record (`AdminSecretPatternProperties`), seed the default catalog in `module.properties` (≥4 self-hostable entries with `qwen/qwen3.6-35b-a3b` marked default to match `default-params.yaml.model`), and flip `ChatModelCatalogAllowlistTest` (TEST-20) green.

Purpose: Plan 05 consumes `ChatModelCatalog.entries()` for the ComboBox; Plan 06 consumes `AdminSecretPatternProperties.resolvedPatterns()` for the secrets-mask. Pre-shipping both keeps the model-picker and the Tier-3 mask independently parallel-buildable.

Output: 3 new config classes + module.properties seed + TEST-20 flipped green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-admin-settings-model-picker-config-knob-migration/16-SPEC.md
@.planning/phases/16-admin-settings-model-picker-config-knob-migration/16-CONTEXT.md
@.planning/phases/16-admin-settings-model-picker-config-knob-migration/16-RESEARCH.md
@.planning/phases/16-admin-settings-model-picker-config-knob-migration/16-PATTERNS.md
@ai-agent/ai-agent-starter/src/main/resources/default-params.yaml
@ai-agent/ai-agent/src/main/resources/com/vn/agent/module.properties

<interfaces>
From `default-params.yaml`: `model: qwen/qwen3.6-35b-a3b` (line 1) — drift gate target.

Catalog data shape (D-06, RESEARCH §7):
```
jmix.ai-agent.models.catalog[0].id=qwen/qwen3.6-35b-a3b
jmix.ai-agent.models.catalog[0].label-message-key=chatModelCatalog.qwen3_35b
jmix.ai-agent.models.catalog[0].is-default=true
jmix.ai-agent.models.catalog[1].id=meta-llama/llama-3.3-70b-instruct
...
```

SELF_HOSTABLE_OPEN_WEIGHTS_ALLOWLIST seed (RESEARCH §7):
- `qwen/qwen3.6-35b-a3b` (Apache 2.0)
- `meta-llama/llama-3.3-70b-instruct` (Llama 3.x Community License — flag for confirmation; see Assumption A2)
- `mistralai/mistral-small-3.1-24b-instruct` (Apache 2.0)
- `deepseek/deepseek-v3.1` (MIT)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create ChatModelCatalogProperties + AdminSecretPatternProperties @ConfigurationProperties records</name>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/ChatModelCatalogProperties.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/AdminSecretPatternProperties.java
  </files>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/rag/config/AiAgentRagProperties.java (Pattern F — record + nested-record + `resolved*()` defaulting)
    - .planning/phases/16-admin-settings-model-picker-config-knob-migration/16-CONTEXT.md D-02 + D-06 (locked field shapes)
    - .planning/phases/16-admin-settings-model-picker-config-knob-migration/16-RESEARCH.md §1.5 (ChatModelCatalogProperties shape) and §1.13 (AdminSecretPatternProperties shape)
  </read_first>
  <action>
    File 1 — `ChatModelCatalogProperties.java` in package `com.vn.agent.admin.config`:
    - `@ConfigurationProperties("jmix.ai-agent.models")` Java record.
    - Single component: `List<Entry> catalog`.
    - Nested public record `Entry(String id, String labelMessageKey, Boolean isDefault)` (Boolean — nullable; treat `null` as `false`).
    - Register via `@ConfigurationPropertiesScan` if present in autoconfig OR add the record class to the existing scan; verify by reading `AIAutoConfiguration.java` whether scan is class-list-based or package-scan-based.

    File 2 — `AdminSecretPatternProperties.java` in package `com.vn.agent.admin.config`:
    - `@ConfigurationProperties("ai-agent.admin")` Java record.
    - Single component: `List<String> secretPropertyPatterns`.
    - Public method `List<String> resolvedPatterns()` returning `secretPropertyPatterns == null || secretPropertyPatterns.isEmpty() ? List.of("*.api-key", "*.password", "*.secret", "*.token") : secretPropertyPatterns` (locked defaults per D-02 / SPEC criterion 4).
    - Javadoc references `feedback_jmix_loadvalue_store` — irrelevant here, but note the package memory `feedback_pragmatic_modules` rules against speculative SPI fields.

    Both records carry `@KnobMetadata(tier = TIER_2, requiresRestart = true, displayMessageKey = "...")` on their components per Plan 06 annotation-pass invariant (Plan 06 may revisit, but emit baseline classification here so Plan 06's annotation-pass is purely additive and not retroactive).
  </action>
  <verify>
    <automated>cd ai-agent && ./gradlew :ai-agent:compileJava 2>&1 | grep -E "BUILD SUCCESSFUL|error:"</automated>
    <source>grep -c "@ConfigurationProperties\\(\"jmix.ai-agent.models\"\\)" ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/ChatModelCatalogProperties.java outputs 1.</source>
    <source>grep -c "@ConfigurationProperties\\(\"ai-agent.admin\"\\)" ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/AdminSecretPatternProperties.java outputs 1.</source>
    <source>grep -c "List.of(\"\\*.api-key\", \"\\*.password\", \"\\*.secret\", \"\\*.token\")" ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/AdminSecretPatternProperties.java outputs 1 (locked default list).</source>
  </verify>
  <done>
    Both records compile. `ChatModelCatalogProperties` exposes `List<Entry> catalog()` with nested `Entry(id, labelMessageKey, isDefault)` shape. `AdminSecretPatternProperties` exposes `resolvedPatterns()` returning the locked 4-element list when unset. Both records are reachable by Spring's config-properties binder (verify by examining whether they appear in `@ConfigurationPropertiesScan` reach).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create ChatModelCatalog @Component + SELF_HOSTABLE_OPEN_WEIGHTS_ALLOWLIST + module.properties seed</name>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/ChatModelCatalog.java,
    ai-agent/ai-agent/src/main/resources/com/vn/agent/module.properties
  </files>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/AiParametersResolver.java (singleton @Component pattern; resolveActive() try/catch resilience shape — Pattern C)
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/module.properties (current contents; placement for new `jmix.ai-agent.models.catalog[N].*` keys + existing `jmix.ai-agent.defaults.model` if present)
    - ai-agent/ai-agent-starter/src/main/resources/default-params.yaml (`model:` line is the drift-gate target value)
    - .planning/phases/16-admin-settings-model-picker-config-knob-migration/16-PATTERNS.md `### ChatModelCatalog.java + ChatModelCatalogProperties.java`
    - .planning/phases/16-admin-settings-model-picker-config-knob-migration/16-RESEARCH.md §7 (default seed table)
    - .planning/phases/16-admin-settings-model-picker-config-knob-migration/16-RESEARCH.md `### Pattern 2: ChatModelCatalog with allowlist constant` (lines 691-740 — verbatim copyable shape)
  </read_first>
  <action>
    File 1 — `ChatModelCatalog.java` in package `com.vn.agent.admin.config`:
    - `@Component` class with constructor injecting `ChatModelCatalogProperties properties` and `AiAgentDefaultsProperties defaults`.
    - Public static `Set<String> SELF_HOSTABLE_OPEN_WEIGHTS_ALLOWLIST = Set.of(...)` with EXACTLY these four ids (per RESEARCH §7 — open-weights, Apache-2.0+/MIT/Llama Community per memory `project_self_hostable_models_only`):
      - `"qwen/qwen3.6-35b-a3b"`
      - `"meta-llama/llama-3.3-70b-instruct"`
      - `"mistralai/mistral-small-3.1-24b-instruct"`
      - `"deepseek/deepseek-v3.1"`
    - Block comment ABOVE the constant: `// Self-hostable open-weights allowlist per project memory project_self_hostable_models_only. Apache 2.0 / MIT / Llama 3.x Community License only. Proprietary models (Claude, GPT-4o, Gemini Pro) reachable ONLY via the custom-entry escape hatch (SPEC criterion 4). TEST-20 enforces catalog ⊆ allowlist.`
    - Public nested record `Entry(String id, String labelMessageKey, boolean isDefault)` (note: primitive boolean here — internal type after validation has rejected null).
    - `@PostConstruct void validate()`:
      - Read `properties.catalog()`; if null/empty, throw `IllegalStateException("ChatModelCatalog requires at least one entry under jmix.ai-agent.models.catalog[*] — module.properties seeds the default 4.")` (boot-fast — module.properties always seeds).
      - Reject any entry whose `id()` is NOT in `SELF_HOSTABLE_OPEN_WEIGHTS_ALLOWLIST` UNLESS host explicitly overrides via property `jmix.ai-agent.models.allow-out-of-allowlist=true` (defensive; lets a host extend the allowlist by also extending the constant via subclass-replace — but default fails boot to catch drift).
      - Count entries with `Boolean.TRUE.equals(e.isDefault())`. If != 1, throw `IllegalStateException("ChatModelCatalog must have exactly one default entry; found N=" + count)`.
      - Drift check: the single default entry's `id()` MUST equal `defaults.model()` (read from `AiAgentDefaultsProperties` which deserialises `default-params.yaml`). If mismatched, throw `IllegalStateException("ChatModelCatalog default '" + defaultId + "' must equal default-params.yaml.model '" + defaults.model() + "' (drift gate)")`.
      - Materialise `this.entries = ...immutable list...` and `this.defaultEntry = ...`.
    - Public accessors: `List<Entry> entries()`, `Entry defaultEntry()`, `Entry findById(String id)` returning `null` for unknown ids (used by the ComboBox label generator in Plan 05 to render custom-entered values verbatim).

    File 2 — append to `module.properties` (do NOT remove or modify existing keys; verify by `git diff` showing only additions). New keys (D-06 catalog seed; 4 entries, the first marked default — drift-matches `default-params.yaml.model`):
    ```
    # Phase 16 D-06 curated model catalog (open-weights / self-hostable only).
    # Marked-default entry MUST match default-params.yaml.model (TEST-20 drift gate).
    jmix.ai-agent.models.catalog[0].id=qwen/qwen3.6-35b-a3b
    jmix.ai-agent.models.catalog[0].label-message-key=chatModelCatalog.qwen3_35b
    jmix.ai-agent.models.catalog[0].is-default=true
    jmix.ai-agent.models.catalog[1].id=meta-llama/llama-3.3-70b-instruct
    jmix.ai-agent.models.catalog[1].label-message-key=chatModelCatalog.llama3_3_70b
    jmix.ai-agent.models.catalog[1].is-default=false
    jmix.ai-agent.models.catalog[2].id=mistralai/mistral-small-3.1-24b-instruct
    jmix.ai-agent.models.catalog[2].label-message-key=chatModelCatalog.mistral_small_3_1
    jmix.ai-agent.models.catalog[2].is-default=false
    jmix.ai-agent.models.catalog[3].id=deepseek/deepseek-v3.1
    jmix.ai-agent.models.catalog[3].label-message-key=chatModelCatalog.deepseek_v3_1
    jmix.ai-agent.models.catalog[3].is-default=false
    ```

    Drift safety: the marked-default id (`qwen/qwen3.6-35b-a3b` at line 1) MUST exactly equal `default-params.yaml` line 1 value. If a later phase bumps `default-params.yaml.model`, ChatModelCatalogAllowlistTest's drift gate fires at build time — Plan 03 explicitly does NOT bake a hardcoded model string into Java; both sides resolve via Spring config beans.
  </action>
  <verify>
    <automated>cd ai-agent && ./gradlew :ai-agent:compileJava 2>&1 | grep -E "BUILD SUCCESSFUL|error:"</automated>
    <source>grep -c "qwen/qwen3.6-35b-a3b\\|meta-llama/llama-3.3-70b-instruct\\|mistralai/mistral-small-3.1-24b-instruct\\|deepseek/deepseek-v3.1" ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/ChatModelCatalog.java outputs 4.</source>
    <source>grep -c "^jmix\\.ai-agent\\.models\\.catalog\\[" ai-agent/ai-agent/src/main/resources/com/vn/agent/module.properties outputs 12 (4 entries × 3 keys).</source>
    <source>grep -E "^jmix\\.ai-agent\\.models\\.catalog\\[0\\]\\.id=qwen/qwen3.6-35b-a3b$" ai-agent/ai-agent/src/main/resources/com/vn/agent/module.properties outputs 1 line.</source>
    <source>grep -E "^jmix\\.ai-agent\\.models\\.catalog\\[0\\]\\.is-default=true$" ai-agent/ai-agent/src/main/resources/com/vn/agent/module.properties outputs 1 line.</source>
    <automated>cd ai-agent && ./gradlew :ai-agent:test --tests "*ApplicationContextTest*" 2>&1 | grep -E "BUILD SUCCESSFUL|FAILED" (boot succeeds → ChatModelCatalog.@PostConstruct validates).</automated>
  </verify>
  <done>
    `ChatModelCatalog` compiles and boots; `@PostConstruct` validates the 4-entry seed against `SELF_HOSTABLE_OPEN_WEIGHTS_ALLOWLIST` and the drift gate. `module.properties` contains the 4-entry seed with `qwen/qwen3.6-35b-a3b` marked default (matching `default-params.yaml.model`). Any `@SpringBootTest` boots without `IllegalStateException` from the catalog validator.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Flip ChatModelCatalogAllowlistTest (TEST-20) green</name>
  <files>ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/ChatModelCatalogAllowlistTest.java</files>
  <behavior>
    - `catalogSubsetOfAllowlist()`: every `ChatModelCatalog.entries().get(i).id()` is contained in `ChatModelCatalog.SELF_HOSTABLE_OPEN_WEIGHTS_ALLOWLIST`. Fail if any id is missing.
    - `exactlyOneDefault()`: count of `e -> e.isDefault()` over `catalog.entries()` equals exactly 1.
    - `defaultMatchesDefaultParamsYaml()`: `catalog.defaultEntry().id()` equals `aiAgentDefaultsProperties.model()` (the value from `default-params.yaml.model`).
    - Comment in test class references `project_self_hostable_models_only.md` per SPEC criterion 4.
  </behavior>
  <read_first>
    - ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/ChatModelCatalogAllowlistTest.java (scaffold from Plan 01 — currently `fail()`)
    - ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/TtlConfigTest.java (Pattern I — `@SpringBootTest` boot stack with `@ImportAutoConfiguration`)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/ChatModelCatalog.java (just created — read SELF_HOSTABLE_OPEN_WEIGHTS_ALLOWLIST + entries() signature)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/AiParametersResolver.java (find the existing `AiAgentDefaultsProperties` bean import / accessor for `model()`)
  </read_first>
  <action>
    Replace all three `fail("Wave 0 scaffold")` test bodies with real assertions. Test class shape mirrors `TtlConfigTest` boot stack (Pattern I). Inject:
    - `@Autowired ChatModelCatalog catalog;`
    - `@Autowired AiAgentDefaultsProperties defaults;`

    Method bodies:
    - `catalogSubsetOfAllowlist()`: iterate `catalog.entries()`, assert each `entry.id()` is in `ChatModelCatalog.SELF_HOSTABLE_OPEN_WEIGHTS_ALLOWLIST` via `assertThat(SELF_HOSTABLE_OPEN_WEIGHTS_ALLOWLIST).contains(entry.id())`. Add Javadoc on the method linking to `project_self_hostable_models_only.md`.
    - `exactlyOneDefault()`: assert `catalog.entries().stream().filter(Entry::isDefault).count() == 1L`.
    - `defaultMatchesDefaultParamsYaml()`: assert `catalog.defaultEntry().id().equals(defaults.model())`. Add explanatory comment: "Drift gate — if `default-params.yaml.model` is bumped without updating the catalog's `is-default=true` entry, this assertion fails and the build blocks (SPEC Acceptance Criterion 4 + RESEARCH §7 drift-gate)."

    Remove every `fail()` call. Class-level Javadoc updated: `TEST-20 — implemented per Plan 16-03 Task 3.`
  </action>
  <verify>
    <automated>cd ai-agent && ./gradlew :ai-agent:test --tests "com.vn.agent.admin.config.ChatModelCatalogAllowlistTest" 2>&1 | grep -E "BUILD SUCCESSFUL|FAILED"</automated>
    <source>grep -v '^#' ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/ChatModelCatalogAllowlistTest.java | grep -c "fail\\(" outputs 0.</source>
    <source>grep -c "project_self_hostable_models_only" ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/ChatModelCatalogAllowlistTest.java outputs ≥ 1 (memory reference per SPEC criterion 4).</source>
  </verify>
  <done>
    `ChatModelCatalogAllowlistTest` exits with all 3 methods green. The drift gate is provably enforced: a deliberate `module.properties` change (mark `meta-llama/llama-3.3-70b-instruct` as `is-default=true` instead) would fail the test — this is the intended invariant.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `module.properties` seed → `ChatModelCatalog.entries()` → admin ComboBox | Catalog content crosses from a deployable config file into runtime UI; T-16-06 (catalog drift vs `default-params.yaml.model`) is mitigated by the drift gate in `@PostConstruct` plus TEST-20 method 3. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-06 | Tampering | Catalog seed in `module.properties` | mitigate | Drift gate: `ChatModelCatalog.@PostConstruct` fails boot if marked-default ≠ `default-params.yaml.model`; `ChatModelCatalogAllowlistTest.defaultMatchesDefaultParamsYaml()` fails build. |
| T-16-04 | Information Disclosure | `SELF_HOSTABLE_OPEN_WEIGHTS_ALLOWLIST` constant | accept | Allowlist is a public taxonomy tag; not a secret. Memory `project_self_hostable_models_only` is the audit policy. |
</threat_model>

<verification>
- `./gradlew :ai-agent:test --tests "com.vn.agent.admin.config.ChatModelCatalogAllowlistTest"` exits 0.
- `./gradlew :ai-agent:test --tests "*ApplicationContextTest*"` boots cleanly (ChatModelCatalog @PostConstruct passes).
- No existing test fails.
- `git diff module.properties` shows ONLY additions (no existing key removed/edited).
</verification>

<success_criteria>
- ChatModelCatalogProperties record binds `jmix.ai-agent.models.catalog[*]` (id, labelMessageKey, isDefault).
- AdminSecretPatternProperties record binds `ai-agent.admin.secret-property-patterns` with the locked 4-element default.
- ChatModelCatalog @Component validates exactly-one-default + drift + allowlist at boot.
- module.properties contains 4 catalog entries; default-marked == `default-params.yaml.model`.
- TEST-20 green.
</success_criteria>

<output>
After completion, create `.planning/phases/16-admin-settings-model-picker-config-knob-migration/16-03-SUMMARY.md`.
</output>
