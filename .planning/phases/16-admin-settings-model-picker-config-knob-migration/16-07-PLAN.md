---
phase: 16-admin-settings-model-picker-config-knob-migration
plan: 07
type: execute
wave: 3
depends_on: [01, 02, 03, 05, 06]
# 06 added to depends_on to serialise the parallel write of messages_en/vi.properties
# in Wave 3. Plans 06 and 07 add disjoint key sets to the same two files; sequencing
# 07 after 06 eliminates a file-level git race during autonomous wave execution.
files_modified:
  - ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/AiParametersResolver.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties
  - ai-agent/ai-agent/src/test/java/com/vn/agent/DefaultChatServiceImplModelValidationFallbackTest.java
  # AuditWriter.java is NO LONGER in this file list — consensus HIGH Concern #1: the
  # kind-parameter overload (writeAuditEvent) was moved into Plan 01 Wave 0. Plan 07
  # only calls the existing-by-this-point auditWriter.writeAuditEvent(AuditKind.MODEL_VALIDATION_FAILURE, ...).
autonomous: true
requirements: [MODEL-02, MODEL-03]
requirements_addressed: [MODEL-02, MODEL-03]
tags: [spring-ai, audit, chat-service]

must_haves:
  truths:
    - "executeBlockingTurn wraps the ChatClient.call(...) site in a classifier-guarded try/catch (D-05) that catches RuntimeException (broadest viable type) and applies the classifier to the cause chain (codex HIGH Concern #8 — pre-review only caught NonTransientAiException, missing direct RestClientResponseException). Classifier accepts EITHER (a) `RestClientResponseException` anywhere in cause chain, OR (b) `org.springframework.ai.retry.NonTransientAiException` anywhere in cause chain — both AND status ∈ {400, 404, 422} AND body/message contains 'model' substring. Other RuntimeExceptions are rethrown unchanged."
    - "Reissue uses defaults.model() ONLY — read via a new AiParametersResolver.fallbackModel() method that bypasses the active profile (avoids the loop hazard documented in RESEARCH Open Question 1). Plan ALSO guards `fallbackModel().equals(offendingModel)` — if the fallback IS the offending model, do not reissue; propagate the original exception (defensive guard per codex suggestion)."
    - "5xx and non-classified exceptions propagate unchanged (Pitfall 6 — no false-positive fallback on transient network errors)."
    - "Two audit rows per failed-then-recovered turn share the same runId: row 1 is MODEL_VALIDATION_FAILURE (offending model id + sanitised provider fingerprint — HTTP status + class name, NOT raw body); row 2 is the recovered turn under the existing CHAT/TOOL kind taxonomy. Row 1 is written via `auditWriter.writeAuditEvent(AuditKind.MODEL_VALIDATION_FAILURE, ...)` — the overload now exists from Plan 01 Wave 0 (consensus HIGH Concern #1)."
    - "The saved AiParameters.bodyYaml.model is NEVER mutated by the fallback (admin's saved profile is left as-is so the next admin save can correct the typo)."
    - "MODEL-02 user-visible fallback notification is wired IN THIS PHASE (codex HIGH Concern #9 — pre-review optionalized this; consensus rejected): ChatPanelFragment surfaces `chat.notice.modelFallbackApplied` as a Vaadin Notification on the user-facing chat panel after a successful reissue. NO 'defer to next phase' escape. The wire is in `files_modified` and tested explicitly."
    - "DefaultChatServiceImplModelValidationFallbackTest flips green: 7 methods covering happy path, 5xx-doesn't-fallback, runId correlation, profile-unchanged-after-failure, non-bad-model-propagates, directRestClientResponseException-also-triggers-reissue (codex Concern #8), userVisibleFallbackNotificationFires (codex Concern #9)."
    - "Operator-visible locale keys land in both bundles: chat.error.modelValidationFailure + chat.notice.modelFallbackApplied."
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java"
      provides: "Classifier-guarded catch + reissue at executeBlockingTurn (Phase 13.1 BLK-01 chokepoint)"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/AiParametersResolver.java"
      provides: "New fallbackModel() method reading defaults.model() directly"
  key_links:
    - from: "executeBlockingTurn try/catch"
      to: "AuditWriter.writeToolCall with AuditKind.MODEL_VALIDATION_FAILURE"
      via: "audit row append (sanitised body)"
      pattern: "writeToolCall.*MODEL_VALIDATION_FAILURE"
    - from: "executeBlockingTurn reissue"
      to: "AiParametersResolver.fallbackModel()"
      via: "method call returning defaults.model()"
      pattern: "fallbackModel\\(\\)"
---

<objective>
Land the MODEL-02 catch + one-shot reissue inside `DefaultChatServiceImpl.executeBlockingTurn(...)` (the Phase 13.1 BLK-01 chokepoint) — classify provider errors as "bad model" via status code ∈ {400, 404, 422} AND case-insensitive "model" substring on body/message, reissue against `defaults.model()` via a new `AiParametersResolver.fallbackModel()` accessor (NOT re-resolving via the active profile — avoid the loop hazard from RESEARCH Open Question 1), write 2 audit rows under the same runId (`MODEL_VALIDATION_FAILURE` + recovered turn), and add the operator-visible locale keys.

Purpose: MODEL-02 + MODEL-03 — at-first-use validation with localised notification + one-shot fallback + audit emission. The saved profile is NEVER mutated.

Output: 1 modified service method (with helper extraction), 1 resolver accessor add, 2 locale keys × 2 bundles, 1 test flipped green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-admin-settings-model-picker-config-knob-migration/16-SPEC.md
@.planning/phases/16-admin-settings-model-picker-config-knob-migration/16-CONTEXT.md
@.planning/phases/16-admin-settings-model-picker-config-knob-migration/16-RESEARCH.md
@.planning/phases/16-admin-settings-model-picker-config-knob-migration/16-PATTERNS.md
@ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/AiParametersResolver.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/spi/AuditKind.java

<interfaces>
From `DefaultChatServiceImpl.java` (Phase 13.1 BLK-01 chokepoint — lines 358-449 `executeBlockingTurn`):
- Current `chatClient.prompt()...call().chatClientResponse()` chain at lines 383-412 includes `.options(ChatOptions.builder().model(model).temperature(...).topP(...).maxTokens(...).build())`.
- `runId` is in scope as a local variable.
- `AuditWriter.writeToolCall(...)` API per Pattern K: signature `(parentId, runId, userUsername, conversationId, toolName, argumentsJson, resultSummary, latencyMs, outcome, denialReason, errorClass)` and it accepts the kind through a separate channel — verify exact signature; `AuditKind.MODEL_VALIDATION_FAILURE` is the new kind constant added in Plan 01.

Classification rule (RESEARCH §3 + Pitfall 6 + codex HIGH Concern #8 — broadened catch surface, BOTH RestClientResponseException AND NonTransientAiException):
```
Bad-model exception IFF:
  cause chain (max depth 5) contains EITHER
    org.springframework.web.client.RestClientResponseException
    OR org.springframework.ai.retry.NonTransientAiException
  AND (the same exception OR its inner-cause-chain RestClientResponseException) has status ∈ {400, 404, 422}
  AND (body or message) contains "model" case-insensitive substring

Catch site: `RuntimeException` (broadest viable type) — not `NonTransientAiException` alone.
Non-matching RuntimeExceptions are rethrown unchanged after the classifier returns false.
```

Reissue model source (RESEARCH Open Question 1 — locked decision): `defaults.model()` via a new `AiParametersResolver.fallbackModel()` method that returns `aiAgentDefaultsProperties.model()` directly — NOT `parametersResolver.effectiveModel(parametersResolver.resolveActive())` which would just return the bad model again.

Sanitisation (P-22 — RESEARCH §1.13 + Specific Idea #4): audit row body carries HTTP status + sanitised exception class name (`badModel.getClass().getSimpleName()`), NOT the raw response body. Offending model id is admin-input (not user-input) → verbatim is acceptable per the Specific Ideas section.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add AiParametersResolver.fallbackModel() accessor</name>
  <files>ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/AiParametersResolver.java</files>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/AiParametersResolver.java (find the existing `effectiveModel(...)` method signature around lines 109-185; the new `fallbackModel()` lives as a sibling)
    - .planning/phases/16-admin-settings-model-picker-config-knob-migration/16-RESEARCH.md Open Question 1 (locked: separate accessor returning defaults.model() directly)
  </read_first>
  <action>
    Add a public method `String fallbackModel()` to `AiParametersResolver`:
    ```
    /**
     * Returns the seed model from default-params.yaml (via AiAgentDefaultsProperties).
     * Used by DefaultChatServiceImpl.executeBlockingTurn(...) to reissue a turn whose
     * active-profile model was rejected by the provider as invalid (D-05).
     *
     * Bypasses the active-profile read-through chain — calling effectiveModel(resolveActive())
     * after a bad-model failure would return the same bad model and loop. The saved
     * AiParameters.bodyYaml.model is NEVER mutated by this path (admin's saved typo stays
     * until the admin corrects it via the ComboBox).
     */
    public String fallbackModel() {
        return defaults.model();
    }
    ```

    The method has no parameters and reads only the injected `AiAgentDefaultsProperties defaults` (already a constructor-injected field on `AiParametersResolver`). No new injection needed.
  </action>
  <verify>
    <automated>cd ai-agent && ./gradlew :ai-agent:compileJava 2>&1 | grep -E "BUILD SUCCESSFUL|error:"</automated>
    <source>grep -c "public String fallbackModel\\(\\)" ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/AiParametersResolver.java outputs 1.</source>
    <source>grep -A 3 "public String fallbackModel" ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/AiParametersResolver.java | grep -c "defaults.model\\(\\)" outputs 1 (delegation, no other logic).</source>
  </verify>
  <done>
    `AiParametersResolver.fallbackModel()` exists, returns `defaults.model()`, has Javadoc explaining the loop-avoidance rationale.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wrap executeBlockingTurn ChatClient.call site with classifier-guarded catch + one-shot reissue + 2 audit rows + locale keys</name>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java,
    ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties,
    ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties,
    ai-agent/ai-agent/src/test/java/com/vn/agent/DefaultChatServiceImplModelValidationFallbackTest.java
  </files>
  <behavior>
    DefaultChatServiceImplModelValidationFallbackTest:
      - `badModelExceptionTriggersOneShotReissue()`: stub `ChatClient` so the first `.call()` throws a wrapped `RestClientResponseException` with HTTP 404 + body `{"error":{"message":"Model invalid/foo is not available","code":404}}`; assert `executeBlockingTurn` (or the public turn API) returns successfully (reissue uses fallback model and the second call succeeds).
      - `bothAuditRowsShareRunId()`: capture audit rows written during the failed-then-recovered turn; assert two rows with kinds `{MODEL_VALIDATION_FAILURE, CHAT}` (or `TOOL` — whichever matches the recovered-turn audit semantics in the current implementation) and identical `runId`.
      - `savedAiParametersBodyYamlModelIsNotMutated()`: pre-load active AiParameters with `bodyYaml.model = "invalid/foo"`; trigger the bad-model turn + reissue; reload the AiParameters row; assert `bodyYaml` still contains the unmutated `"invalid/foo"` value.
      - `nonBadModelExceptionPropagates()`: stub `ChatClient` to throw `RestClientResponseException` with HTTP 503 + body `"upstream timeout"`; assert the exception propagates (no reissue, no MODEL_VALIDATION_FAILURE audit row written).
      - `_5xxResponseDoesNotTriggerReissue()`: stub HTTP 502 with body containing the word "model"; assert NO reissue (Pitfall 6 — status code is the structural filter; substring alone is insufficient).
  </behavior>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java (entire `executeBlockingTurn` method — read lines 358-449 carefully; identify `runId`, `userId`/`userUsername`, `convId`, `model` local variables in scope at the call site)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditWriter.java (find the `writeToolCall` signature + any sibling method for writing a typed-kind audit row; the current Pattern K extension may need a new overload or use existing API with a `kind` parameter override — scout)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/spi/AuditKind.java (just-extended with MODEL_VALIDATION_FAILURE in Plan 01 — confirm the constant value before referencing)
    - ai-agent/ai-agent/src/test/java/com/vn/agent/DefaultChatServiceImplModelValidationFallbackTest.java (Plan 01 scaffold)
    - .planning/phases/16-admin-settings-model-picker-config-knob-migration/16-RESEARCH.md `### 3. Spring AI error classification` + Pitfall 6 (false-positive guard)
    - .planning/phases/16-admin-settings-model-picker-config-knob-migration/16-PATTERNS.md `### DefaultChatServiceImpl.executeBlockingTurn(...)` (minimal-diff sketch lines 444-501) + Pattern K (AuditWriter.writeToolCall idiom)
    - any existing sibling test like `DefaultChatServiceImplStreamFallbackTest.java` for stub configuration shape (Pattern matches at end of PATTERNS.md table)
  </read_first>
  <action>
    Step A — Refactor + catch inside `executeBlockingTurn(...)`:

    1. Extract the existing `chatClient.prompt()...call().chatClientResponse()` chain (lines 383-412) into a private helper:
       ```
       private ChatClientResponse buildAndCallChatClient(String chosenModel, /* all prompt-shape args */) {
           return chatClient.prompt()
                   .system(systemPromptWithDocs)
                   .user(u -> { u.text(message); if (!resolvedMedia.media().isEmpty()) u.media(...); })
                   .toolCallbacks(toolCallbacks.callbacksFor(userId, convId, toolSurfaceIntentId))
                   .toolContext(auditToolContext(runId, convId))
                   .advisors(advisorSpec -> { ... })
                   .options(ChatOptions.builder()
                           .model(chosenModel)
                           .temperature(parametersResolver.effectiveTemperature(active))
                           .topP(parametersResolver.effectiveTopP(active))
                           .maxTokens(parametersResolver.effectiveMaxTokens(active))
                           .build())
                   .call()
                   .chatClientResponse();
       }
       ```

    2. Replace the original call site with the try/catch (codex HIGH Concern #8 — broadened catch type):
       ```
       ChatClientResponse clientResp;
       boolean fallbackApplied = false;
       try {
           clientResp = buildAndCallChatClient(model, ...);
       } catch (RuntimeException providerEx) {
           if (!isBadModelException(providerEx)) throw providerEx;
           String fallback = parametersResolver.fallbackModel();
           // Defensive guard: if the configured fallback IS the offending model
           // (admin set defaults.model = same bad id), do NOT loop — rethrow.
           if (fallback == null || fallback.equals(model)) throw providerEx;
           writeModelValidationFailureAudit(runId, userUsername, convId, model, providerEx);
           clientResp = buildAndCallChatClient(fallback, ...);
           fallbackApplied = true;
           // The recovered-turn audit row is written by the existing post-call audit code
           // (writeChatFinish / writeChatTurn) AS LONG AS that code uses the same runId,
           // which it already does. The two rows correlate via runId by construction.
       }
       // After successful (initial or reissued) call, if fallbackApplied is true,
       // a downstream notification surface must inform the user — see ChatPanelFragment
       // wire below (codex HIGH Concern #9). The fallbackApplied flag is surfaced
       // either via a return-value extension to executeBlockingTurn, OR via a thread-local
       // / per-request bean / Vaadin push notification — pick the lightest path that
       // fits the existing turn-completion code; scout reveals which is wired today.
       ```

    3. Add package-private static classifier `isBadModelException(Throwable t)` (extracted as package-private static for unit-test isolation per opencode Suggestion #4):
       ```
       private static final Set<Integer> BAD_MODEL_STATUS_CODES = Set.of(400, 404, 422);

       // Package-private static for unit-test isolation (opencode Suggestion #4) +
       // catches BOTH RestClientResponseException directly AND NonTransientAiException
       // wrapping it (codex HIGH Concern #8). RuntimeException catch in the call site
       // delegates here for classification.
       static boolean isBadModelException(Throwable t) {
           Throwable cursor = t;
           for (int depth = 0; cursor != null && depth < 5; depth++, cursor = cursor.getCause()) {
               // Case A: direct RestClientResponseException (Spring AI 1.x bare-bones provider path)
               if (cursor instanceof org.springframework.web.client.RestClientResponseException rcre) {
                   if (matchesBadModelShape(rcre)) return true;
                   continue;
               }
               // Case B: NonTransientAiException wrapping a RestClientResponseException
               // (Spring AI 1.x normalized provider error path). Walk the wrapped cause.
               if (cursor instanceof org.springframework.ai.retry.NonTransientAiException) {
                   Throwable inner = cursor.getCause();
                   int innerDepth = 0;
                   while (inner != null && innerDepth < 5) {
                       if (inner instanceof org.springframework.web.client.RestClientResponseException rcre) {
                           if (matchesBadModelShape(rcre)) return true;
                           break;
                       }
                       inner = inner.getCause();
                       innerDepth++;
                   }
                   // If NonTransientAiException has no RestClientResponseException cause but
                   // its own message mentions "model" — still NOT a bad-model signal without
                   // a status code we can structurally trust (Pitfall 6 false-positive guard).
               }
           }
           return false;
       }

       private static boolean matchesBadModelShape(org.springframework.web.client.RestClientResponseException rcre) {
           int status = rcre.getRawStatusCode();
           if (!BAD_MODEL_STATUS_CODES.contains(status)) return false;
           String body = rcre.getResponseBodyAsString();
           if (body != null && body.toLowerCase().contains("model")) return true;
           String message = rcre.getMessage();
           return message != null && message.toLowerCase().contains("model");
       }
       ```

    4. Add the audit-emission helper `writeModelValidationFailureAudit(...)` — uses the kind-parameter overload `auditWriter.writeAuditEvent(String kind, ...)` which was added in Plan 01 Wave 0 (consensus HIGH Concern #1 resolved at Wave 0):
       ```
       private void writeModelValidationFailureAudit(UUID runId, String userUsername, UUID convId, String offendingModel, Throwable cause) {
           // P-22 sanitisation: HTTP status + sanitised class name only. NEVER the raw response body.
           int status = -1;
           Throwable cursor = cause;
           for (int depth = 0; cursor != null && depth < 5; depth++, cursor = cursor.getCause()) {
               if (cursor instanceof RestClientResponseException rcre) { status = rcre.getRawStatusCode(); break; }
           }
           String errorClass = cause.getClass().getSimpleName();
           String resultSummary = String.format("model=%s status=%d error=%s", offendingModel, status, errorClass);
           // Plan 01 added the kind-parameter overload on AuditWriter — see consensus HIGH Concern #1
           // in 16-REVIEWS. Plan 07 only CALLS the overload; no AuditWriter edits in this plan.
           auditWriter.writeAuditEvent(
               AuditKind.MODEL_VALIDATION_FAILURE,
               /* parentId */ null,
               runId,
               userUsername,
               convId,
               /* toolName */ "model_validation",  // sentinel name; not a real @Tool method
               /* argumentsJson */ "{\"model\":\"" + offendingModel + "\"}",
               resultSummary,
               /* latencyMs */ 0L,
               /* outcome */ AiToolCallOutcome.FAILED,
               /* denialReason */ null,
               errorClass);
       }
       ```

    Step B — Locale keys (D-09):
    - `chat.error.modelValidationFailure` (en: "The configured chat model is not available; this turn was retried with the default model. The Parameters profile was not changed." / vi: "Mô hình chat đã cấu hình không khả dụng; lượt này được thử lại với mô hình mặc định. Hồ sơ Parameters không bị thay đổi.")
    - `chat.notice.modelFallbackApplied` (en: "Used default model for this turn." / vi: "Đã sử dụng mô hình mặc định cho lượt này.")

    Add to BOTH `messages_en.properties` AND `messages_vi.properties`.

    Step C — Wire user-visible fallback notification in `ChatPanelFragment` (codex HIGH Concern #9 — MODEL-02 REQUIRES this in THIS phase; pre-review plan's "defer to next phase if no wire exists" is REJECTED by consensus):

    1. Surface a `fallbackApplied` signal from `DefaultChatServiceImpl.executeBlockingTurn(...)` to the caller. The lightest path is a return-value extension: change the return shape from the existing turn-result to include a `boolean modelFallbackApplied`. If that ripples too widely, alternative B: publish a Spring `ApplicationEvent` (`ChatModelFallbackAppliedEvent(this, runId, conversationId, offendingModel, fallbackModel)`) AFTER the successful reissue, and have `ChatPanelFragment` listen via `@EventListener` scoped to the active conversation. Execution-time choice: pick the path that has fewer ripple edits; document choice in commit body.

    2. In `ChatPanelFragment`, after a turn completes, if `fallbackApplied == true` (or the event fires for the current conversation), call:
       ```
       Notifications.create(
           messages.getMessage("chat.notice.modelFallbackApplied"))
           .withType(Notifications.Type.WARNING)
           .withDuration(8000)
           .show();
       ```
       Plus a persistent (chat-history-row-style) banner using `chat.error.modelValidationFailure` IF the chat panel surfaces a status-bar / error-row component (scout for existing analog). If no persistent surface exists, the Notification suffices — the audit row is the durable record.

    3. Test `userVisibleFallbackNotificationFires()` in `DefaultChatServiceImplModelValidationFallbackTest`: assert the `fallbackApplied` flag (or the published event) is observable to the test after a bad-model + reissue turn completes successfully. A Vaadin UI integration test that asserts the Notification DOM is rendered is OPTIONAL (covered by phase-level UAT); the contract-level test asserts the signal exists in the service layer.

    4. ChatPanelFragment.java is added to `files_modified` for this plan. The change is additive (one new `@EventListener` method OR one new return-value branch) — Plan 12 fragment shape is preserved.

    Step C — Flip `DefaultChatServiceImplModelValidationFallbackTest` per `<behavior>`. Stub `ChatClient` via a Mockito `@MockBean` OR a fake `StubChatModel` that throws on the first call and succeeds on the second. Use Pattern I boot stack. Confirm Phase 11 mutation tests + Phase 13.1 task-file tests still pass (no regression).
  </action>
  <verify>
    <automated>cd ai-agent && ./gradlew :ai-agent:test --tests "com.vn.agent.DefaultChatServiceImplModelValidationFallbackTest" 2>&1 | grep -E "BUILD SUCCESSFUL|FAILED"</automated>
    <automated>cd ai-agent && ./gradlew :ai-agent:test --tests "com.vn.agent.DefaultChatServiceImplStreamFallbackTest" 2>&1 | grep -E "BUILD SUCCESSFUL|FAILED" (Phase 13.1 BLK-01 invariant test still green; the streaming->blocking fallback path is preserved).</automated>
    <automated>cd ai-agent && ./gradlew :ai-agent:test --tests "com.vn.agent.tools.mutation.*" 2>&1 | grep -E "BUILD SUCCESSFUL|FAILED" (Phase 11 mutation suite unaffected).</automated>
    <source>grep -c "isBadModelException\\|writeModelValidationFailureAudit" ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java outputs ≥ 2.</source>
    <source>grep -c "parametersResolver.fallbackModel\\(\\)" ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java outputs ≥ 1.</source>
    <source>grep -c "Set.of(400, 404, 422)" ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java outputs ≥ 1 (status filter; rejects 5xx Pitfall 6).</source>
    <source>grep -c "^chat.error.modelValidationFailure\\|^chat.notice.modelFallbackApplied" ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties outputs 2.</source>
    <source>grep -c "^chat.error.modelValidationFailure\\|^chat.notice.modelFallbackApplied" ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties outputs 2.</source>
    <source>grep -v '^#' ai-agent/ai-agent/src/test/java/com/vn/agent/DefaultChatServiceImplModelValidationFallbackTest.java | grep -c "fail\\(" outputs 0.</source>
    <source>grep -v '^#' ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java | grep -c "getResponseBodyAsString" outputs ≤ 1 (only used inside the classifier — body NEVER touches audit row body).</source>
  </verify>
  <done>
    `executeBlockingTurn` wraps the call site in a classifier-guarded try/catch; classifier filters by status ∈ {400, 404, 422} + "model" substring; reissue uses `parametersResolver.fallbackModel()`; two audit rows correlate via runId; saved profile is unmutated; locale keys present in both bundles; test green; Phase 11/12/13/13.1/14 suites pass unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider HTTP error body → audit row | Sanitisation gate: only HTTP status + sanitised exception class name reach the audit row body. Raw response body NEVER persisted. (T-16-04 mitigation.) |
| Bad-model classifier → reissue path | Classifier ONLY triggers on status ∈ {400, 404, 422}; 5xx falls through to existing retry-advisor path. Prevents transient-network reissue loops. (T-16-03 mitigation.) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | DoS | Reissue path | mitigate | `fallbackModel()` reads `defaults.model()` directly — never `effectiveModel(resolveActive())` — so the reissue uses a DIFFERENT model than the failing one. Classifier filters status code so 5xx loops don't fire. Test `_5xxResponseDoesNotTriggerReissue` enforces. |
| T-16-04 | Information Disclosure | MODEL_VALIDATION_FAILURE audit row | mitigate | Body carries HTTP status + sanitised class name only (P-22 precedent). `grep -c "getResponseBodyAsString"` ≤ 1 (used by classifier, never by writer). |
| T-16-05 | Elevation of Privilege | Per-request model override | accept | The model selection comes from `AiParameters.bodyYaml.model` set by an admin-only view (Plan 05). End users cannot inject a model via chat input. |
| Twin-publisher (R2) | Tampering | DefaultChatServiceImpl event publication | mitigate | This plan does NOT inject `ApplicationEventPublisher` for `AiSettingsChangedEvent`. The recovery does not publish a settings-change event — only the audit row. Plan 06's SecretRedactionInvariantsTest enforces. |
</threat_model>

<verification>
- `./gradlew :ai-agent:test --tests "com.vn.agent.DefaultChatServiceImplModelValidationFallbackTest"` exits 0.
- `./gradlew :ai-agent:test --tests "com.vn.agent.DefaultChatServiceImplStreamFallbackTest"` exits 0 (BLK-01 streaming-fallback path preserved).
- `./gradlew :ai-agent:test --tests "com.vn.agent.tools.mutation.*"` exits 0 (no regression).
- `./gradlew :ai-agent:test` full suite exits 0 (phase-level smoke).
- Locale parity diff returns empty for the 2 new chat keys.
</verification>

<success_criteria>
- `AiParametersResolver.fallbackModel()` returns `defaults.model()` directly.
- `executeBlockingTurn` catches RuntimeException (codex HIGH Concern #8 — broadened from NonTransientAiException); classifier walks cause chain for BOTH RestClientResponseException AND NonTransientAiException; reissues exactly once against `fallbackModel()` (with `fallback != offendingModel` guard); writes 2 audit rows sharing runId via the kind-parameter overload added in Plan 01 (consensus HIGH Concern #1).
- 5xx + non-classified exceptions propagate unchanged.
- Saved AiParameters.bodyYaml.model is NEVER mutated by the fallback path.
- USER-VISIBLE FALLBACK NOTIFICATION IS WIRED IN THIS PHASE (codex HIGH Concern #9 — non-negotiable per consensus): ChatPanelFragment surfaces `chat.notice.modelFallbackApplied` after a successful reissue. No deferral.
- 2 new locale keys in both bundles.
- DefaultChatServiceImplModelValidationFallbackTest green (7 methods, including `directRestClientResponseExceptionTriggersReissue` and `userVisibleFallbackNotificationFires`).
- `isBadModelException` is package-private static and has dedicated unit test coverage (opencode Suggestion #4 — adopted).
- AuditWriter is NOT modified by this plan (Plan 01 owns the overload).
- Phase 9/10/11/12/13/13.1/14/15 test suites pass UNCHANGED.
</success_criteria>

<output>
After completion, create `.planning/phases/16-admin-settings-model-picker-config-knob-migration/16-07-SUMMARY.md`.
</output>
