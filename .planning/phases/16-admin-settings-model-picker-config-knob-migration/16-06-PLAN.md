---
phase: 16-admin-settings-model-picker-config-knob-migration
plan: 06
type: execute
wave: 3
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/KnobInventory.java
  - ai-agent/ai-agent-starter/src/main/java/com/vn/autoconfigure/agent/KnobInventoryScanner.java
  - ai-agent/ai-agent-starter/src/main/java/com/vn/autoconfigure/agent/KnobInventoryAutoConfiguration.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/rag/config/AiAgentRagProperties.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/tools/mutation/AiAgentMutationProperties.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/AiAgentAuditProperties.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/AiAgentGuardProperties.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/prompt/AiAgentPromptProperties.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/conversation/AiAgentTitleProperties.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/parameters/AiAgentDefaultsProperties.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/taskfile/AiTaskFileProperties.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/rag/AiAgentEmbeddingProperties.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/AiExtractionProperties.java
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/view/uisettings/ai-ui-settings-detail-view.xml
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/uisettings/AiUiSettingsDetailView.java
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties
  - ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/KnobInventoryClassificationTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/SecretRedactionInvariantsTest.java
autonomous: true
requirements: [CFG-01, CFG-02, CFG-03, SEC-08]
requirements_addressed: [CFG-01, CFG-02, CFG-03, SEC-08]
tags: [jmix-views, jmix-i18n, spring-boot, reflection, security]

must_haves:
  truths:
    - "KnobMetadata annotation is present on every component/setter of all 10 @ConfigurationProperties records, classifying each as TIER_1/TIER_2/TIER_3 (D-02 + RESEARCH §10 record inventory)."
    - "KnobInventoryScanner runs at ApplicationReadyEvent (Pitfall 4) and walks ConfigurationPropertiesBean.getAll(applicationContext), reflecting @KnobMetadata + applying the Tier-3 pattern mask defense-in-depth."
    - "KnobInventory @Component exposes tier1() / tier2() / tier3() immutable views populated by the scanner."
    - "AiUiSettingsDetailView gains a tier1KnobsTab (form fields bound to 11 new columns), a bootConfigTab (dataGrid of Tier-2 rows with `requires restart` badge), and a secretsTab (dataGrid of Tier-3 rows with `configured: yes/no` only — no raw value rendered)."
    - "SEC-08 SecretRedactionInvariantsTest flips green (3 legs: no secret bound editable, no @ConditionalOnProperty bound editable, twin-publisher source-scan)."
    - "KnobInventoryClassificationTest flips green: every @ConfigurationProperties record has @KnobMetadata coverage; Tier-3 pattern catches unannotated secrets; Tier-2 toggles carry requiresRestart=true."
    - "Tier-1 form-field saves trigger AiUiSettingsEntityListener (Plan 05) — verified via reuse of the AiSettingsChangedEventListenerInvariantTest (no new test needed)."
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/KnobInventory.java"
      provides: "Immutable holder with tier1()/tier2()/tier3() accessors"
    - path: "ai-agent/ai-agent-starter/src/main/java/com/vn/autoconfigure/agent/KnobInventoryScanner.java"
      provides: "@EventListener(ApplicationReadyEvent.class) scanner using ConfigurationPropertiesBean.getAll"
    - path: "ai-agent/ai-agent/src/main/resources/com/vn/agent/view/uisettings/ai-ui-settings-detail-view.xml"
      provides: "tier1KnobsTab + bootConfigTab + secretsTab"
  key_links:
    - from: "KnobInventoryScanner"
      to: "@KnobMetadata annotations on @ConfigurationProperties records"
      via: "ConfigurationPropertiesBean.getAll + reflection"
      pattern: "ConfigurationPropertiesBean\\.getAll"
    - from: "AiUiSettingsDetailView secretsTab"
      to: "Environment.getProperty(key) non-blank check"
      via: "rendered Span value 'yes' or 'no' — NEVER the raw secret value"
      pattern: "configured: yes/no"
---

<objective>
Annotate every @ConfigurationProperties record with `@KnobMetadata`, ship the `KnobInventoryScanner` in the starter autoconfig package (Pitfall 4 — ApplicationReadyEvent), expose the immutable `KnobInventory` to the admin view, expand `AiUiSettingsDetailView` with three tabs (Tier-1 form, Tier-2 read-only grid with requires-restart badge, Tier-3 secrets indicator), and flip `KnobInventoryClassificationTest` + `SecretRedactionInvariantsTest` green.

Purpose: Completes CFG-02 (boot toggles read-only with badge), the Tier-3 secret-indicator UI (SPEC criterion 6), and SEC-08's source-scan invariant. The Tier-1 form fields here are the user-facing surface of Plan 02's schema + Plan 04's resolver — saving fires Plan 05's listener.

Output: 10 records annotated, 3 new starter classes (scanner + autoconfig + KnobInventory holder), 1 view + descriptor expanded, 2 tests flipped green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-admin-settings-model-picker-config-knob-migration/16-SPEC.md
@.planning/phases/16-admin-settings-model-picker-config-knob-migration/16-CONTEXT.md
@.planning/phases/16-admin-settings-model-picker-config-knob-migration/16-RESEARCH.md
@.planning/phases/16-admin-settings-model-picker-config-knob-migration/16-PATTERNS.md
@ai-agent/ai-agent/src/main/java/com/vn/agent/view/uisettings/AiUiSettingsDetailView.java
@ai-agent/ai-agent/src/main/resources/com/vn/agent/view/uisettings/ai-ui-settings-detail-view.xml
@ai-agent/ai-agent-starter/src/main/java/com/vn/autoconfigure/agent/AIAutoConfiguration.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/parameters/DefaultParamsSeeder.java

<interfaces>
From Plan 01: `KnobMetadata.Tier { TIER_1, TIER_2, TIER_3 }` annotation with `tier()`, `requiresRestart()`, `displayMessageKey()`.

From `DefaultParamsSeeder.java` (Pattern E — `@EventListener(ApplicationReadyEvent.class)`).

From Plan 03: `AdminSecretPatternProperties.resolvedPatterns()` returns the configurable secret-mask regex list.

Tier classification per RESEARCH §10 record-inventory table (LOCKED):
- AiAgentRagProperties: topK Tier-1 (already in bodyYaml), splitter Tier-2, ingestExecutor Tier-2, upload.maxFileSizeBytes Tier-1, sampleIngester.enabled Tier-2
- AiAgentMutationProperties: enabled Tier-2 boot toggle, allowDelete Tier-2, confirmationRequired Tier-1, idempotencyTtl Tier-1, bulkMaxRows Tier-1
- AiAgentAuditProperties: all Tier-2
- AiAgentGuardProperties: all Tier-2 (rate-limit/token-breaker/iteration-cap, output-scanner.patterns + pack toggles)
- AiAgentPromptProperties: entityInventory.limit Tier-1
- AiAgentTitleProperties: enabled Tier-2, modelId Tier-2, maxContextMessages Tier-1, minAssistantMessagesTrigger Tier-1, executor.* Tier-2
- AiAgentDefaultsProperties: all Tier-2 (used by resolver fallback; not directly admin-editable)
- AiTaskFileProperties: all 4 Tier-1 (ttlSeconds, maxFileSizeBytes, perTurnMaxFiles, perTurnMaxTotalBytes)
- AiAgentEmbeddingProperties: all Tier-2 (model + dimensions pinned to pgvector schema)
- AiExtractionProperties: all Tier-2

ConfigurationPropertiesBean API (RESEARCH §2):
- `static Map<String, ConfigurationPropertiesBean> getAll(ApplicationContext)`
- per-bean: `getInstance()` / `getType()` / `getAnnotation()` (the @ConfigurationProperties)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Annotate all 10 @ConfigurationProperties records with @KnobMetadata + create KnobInventory + KnobInventoryScanner</name>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/rag/config/AiAgentRagProperties.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/tools/mutation/AiAgentMutationProperties.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/AiAgentAuditProperties.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/AiAgentGuardProperties.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/prompt/AiAgentPromptProperties.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/conversation/AiAgentTitleProperties.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/parameters/AiAgentDefaultsProperties.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/taskfile/AiTaskFileProperties.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/rag/AiAgentEmbeddingProperties.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/AiExtractionProperties.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/KnobInventory.java,
    ai-agent/ai-agent-starter/src/main/java/com/vn/autoconfigure/agent/KnobInventoryScanner.java,
    ai-agent/ai-agent-starter/src/main/java/com/vn/autoconfigure/agent/KnobInventoryAutoConfiguration.java
  </files>
  <read_first>
    - .planning/phases/16-admin-settings-model-picker-config-knob-migration/16-RESEARCH.md `## @ConfigurationProperties Records Inventory (Confirmed)` (the 10-row LOCKED table — Tier disposition per record)
    - .planning/phases/16-admin-settings-model-picker-config-knob-migration/16-RESEARCH.md `### 2. Spring Boot ConfigurationPropertiesBean.getAll(ApplicationContext)` (API contract)
    - .planning/phases/16-admin-settings-model-picker-config-knob-migration/16-PATTERNS.md Pattern E (`@EventListener(ApplicationReadyEvent.class)` precedent) + Pattern F (record + resolved() shape) + `### @KnobMetadata annotation pass on all 10 @ConfigurationProperties records (additive)`
    - ai-agent/ai-agent/src/main/java/com/vn/agent/parameters/DefaultParamsSeeder.java (Pattern E — `ApplicationReadyEvent` listener idiom; lines 31-63)
    - ai-agent/ai-agent-starter/src/main/java/com/vn/autoconfigure/agent/AIAutoConfiguration.java (where the new `KnobInventoryAutoConfiguration` sits as a sibling)
    - Each of the 10 @ConfigurationProperties files listed in files_modified (scout exact component names — records use `record Foo(Integer topK, ...)`; classes use `setTopK(Integer)`)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/KnobMetadata.java (Plan 01 — annotation shape; targets RECORD_COMPONENT/FIELD/METHOD)
  </read_first>
  <action>
    Step A — Annotation pass on all 10 records (pure additive, no shape change):

    For each of the 10 records, add `@KnobMetadata(tier = ..., requiresRestart = ..., displayMessageKey = "...")` to every component (record-component for records, setter or field for classes). Tier assignment LOCKED per RESEARCH §10 table. `displayMessageKey` value is `bootConfig.knob.<beanPrefix>.<componentName>` (e.g. `bootConfig.knob.rag.topK`) — Plan 06 Task 3 adds these to the locale bundles.

    Concrete tier assignments (component name → tier → requiresRestart):
    - `AiAgentRagProperties.topK` → TIER_1, false (already in bodyYaml; annotated as Tier-1 for completeness)
    - `AiAgentRagProperties.similarityThreshold` → TIER_1, false
    - `AiAgentRagProperties.splitter.*` → TIER_2, true (chunk size at runtime creates inconsistent index — RESEARCH out-of-scope rationale)
    - `AiAgentRagProperties.ingestExecutor.*` → TIER_2, true (thread-pool resize hazard)
    - `AiAgentRagProperties.upload.maxFileSizeBytes` → TIER_1, false (migrated to AiUiSettings.UPLOAD_MAX_FILE_SIZE_BYTES)
    - `AiAgentRagProperties.sampleIngester.enabled` → TIER_2, true (@ConditionalOnProperty consumer)
    - `AiAgentRagProperties.adminBypass` → TIER_2, true
    - `AiAgentMutationProperties.enabled` → TIER_2, true (@ConditionalOnProperty consumer — Phase 11)
    - `AiAgentMutationProperties.allowDelete` → TIER_2, true
    - `AiAgentMutationProperties.confirmationRequired` → TIER_1, false (migrated)
    - `AiAgentMutationProperties.idempotencyTtl` → TIER_1, false (migrated; Duration coerced to seconds in resolver)
    - `AiAgentMutationProperties.bulkMaxRows` → TIER_1, false (migrated)
    - `AiAgentAuditProperties.*` → TIER_2, true (all)
    - `AiAgentGuardProperties.*` → TIER_2, true (all)
    - `AiAgentPromptProperties.entityInventory.limit` → TIER_1, false (migrated)
    - `AiAgentTitleProperties.enabled` → TIER_2, true (@ConditionalOnProperty)
    - `AiAgentTitleProperties.modelId` → TIER_2, true (advisor wiring)
    - `AiAgentTitleProperties.maxContextMessages` → TIER_1, false (migrated)
    - `AiAgentTitleProperties.minAssistantMessagesTrigger` → TIER_1, false (migrated)
    - `AiAgentTitleProperties.executor.*` → TIER_2, true (thread-pool hazard)
    - `AiAgentDefaultsProperties.*` → TIER_2, true (all — fallback values not directly admin-editable)
    - `AiTaskFileProperties.ttlSeconds` → TIER_1, false (migrated)
    - `AiTaskFileProperties.maxFileSizeBytes` → TIER_1, false (migrated)
    - `AiTaskFileProperties.perTurnMaxFiles` → TIER_1, false (migrated)
    - `AiTaskFileProperties.perTurnMaxTotalBytes` → TIER_1, false (migrated)
    - `AiAgentEmbeddingProperties.*` → TIER_2, true (dimensions pinned to pgvector schema)
    - `AiExtractionProperties.*` → TIER_2, true

    NO secrets exist in these records (per RESEARCH §10 — Tier-3 lives outside the records, caught by the pattern mask).

    Step B — Create `KnobInventory.java` in `com.vn.agent.admin.config`:
    - `@Component` class with three immutable accessors: `List<KnobRow> tier1()`, `List<KnobRow> tier2()`, `List<SecretIndicatorRow> tier3()`.
    - Backed by `AtomicReference<State>` set by the scanner at `ApplicationReadyEvent`.
    - `KnobRow` nested record: `(String key, String resolvedValue, String displayMessageKey, boolean requiresRestart)`.
    - `SecretIndicatorRow` nested record: `(String key, String displayMessageKey, boolean configured)` — NO `resolvedValue` field (defense-in-depth — the value never leaves Environment).
    - Public setter `setState(State state)` callable only from the scanner (package-private if scanner is in same package; otherwise public with @VisibleForTesting Javadoc note).

    Step C — Create `KnobInventoryScanner.java` in `com.vn.autoconfigure.agent` (starter autoconfig classpath per RESEARCH §10):
    - `@Component` with constructor injection of `ApplicationContext applicationContext`, `Environment environment`, `KnobInventory knobInventory`, `AdminSecretPatternProperties secretPatterns`.
    - `@EventListener(ApplicationReadyEvent.class) public void scan()` method:
      - Call `Map<String, ConfigurationPropertiesBean> beans = ConfigurationPropertiesBean.getAll(applicationContext)`.
      - For each bean: get `getInstance()` + `getType()` + `getAnnotation()` (the @ConfigurationProperties). Walk record components (or fields for non-record classes) reflectively. For each component:
        - If `@KnobMetadata` present, classify by `tier()`.
        - If absent, default to TIER_2 (host-extension safety per RESEARCH §1.6).
        - Resolve current value via `environment.getProperty(prefix + "." + componentName)` (handle camelCase → kebab-case mapping using Spring's `RelaxedNames` or manual transform).
      - Build two `List<KnobRow>` (tier1, tier2) and one `List<SecretIndicatorRow>` (tier3).
      - Tier-3 logic: walk ALL `Environment` `MutablePropertySources` (`((ConfigurableEnvironment) environment).getPropertySources()`), collect property keys (EnumerablePropertySource only), filter by patterns from `secretPatterns.resolvedPatterns()` (convert `*.api-key` glob to regex `.*\\.api-key$`). For each match: emit `SecretIndicatorRow(key, displayMessageKey="bootConfig.knob.secret."+sanitisedKey, configured = environment.getProperty(key) != null && !environment.getProperty(key).isBlank())`.
      - Call `knobInventory.setState(new State(tier1, tier2, tier3))`.
    - Class-level Javadoc: `Defense-in-depth: a host @ConfigurationProperties without @KnobMetadata still appears in Tier-2 with requiresRestart=true; a property name matching the Tier-3 pattern is masked even without annotation (RESEARCH §1.6 + Pitfall guidance).`

    Step D — Create `KnobInventoryAutoConfiguration.java` in `com.vn.autoconfigure.agent`:
    - `@AutoConfiguration`
    - `@EnableConfigurationProperties({AdminSecretPatternProperties.class, ChatModelCatalogProperties.class})` (if not already enabled elsewhere — verify).
    - `@Import({KnobInventory.class, KnobInventoryScanner.class})` OR register them as `@Bean` methods.
    - Add the class to `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` in the starter (verify file path with `find`).
  </action>
  <verify>
    <automated>cd ai-agent && ./gradlew :ai-agent:compileJava :ai-agent-starter:compileJava 2>&1 | grep -E "BUILD SUCCESSFUL|error:"</automated>
    <source>grep -c "@KnobMetadata" ai-agent/ai-agent/src/main/java/com/vn/agent/rag/config/AiAgentRagProperties.java outputs ≥ 5 (top-level components annotated).</source>
    <source>grep -c "@KnobMetadata" ai-agent/ai-agent/src/main/java/com/vn/agent/tools/mutation/AiAgentMutationProperties.java outputs ≥ 5.</source>
    <source>grep -c "@KnobMetadata" ai-agent/ai-agent/src/main/java/com/vn/agent/taskfile/AiTaskFileProperties.java outputs ≥ 4.</source>
    <source>grep -c "ConfigurationPropertiesBean.getAll" ai-agent/ai-agent-starter/src/main/java/com/vn/autoconfigure/agent/KnobInventoryScanner.java outputs ≥ 1.</source>
    <source>grep -c "ApplicationReadyEvent" ai-agent/ai-agent-starter/src/main/java/com/vn/autoconfigure/agent/KnobInventoryScanner.java outputs ≥ 1.</source>
  </verify>
  <done>
    All 10 records annotated. KnobInventory + scanner + autoconfig compile. Scanner runs at ApplicationReadyEvent (proxy verified by any @SpringBootTest succeeding). Tier-3 pattern matching pulls from `AdminSecretPatternProperties.resolvedPatterns()`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Expand AiUiSettingsDetailView XML + Java with tier1KnobsTab + bootConfigTab + secretsTab</name>
  <files>
    ai-agent/ai-agent/src/main/resources/com/vn/agent/view/uisettings/ai-ui-settings-detail-view.xml,
    ai-agent/ai-agent/src/main/java/com/vn/agent/view/uisettings/AiUiSettingsDetailView.java
  </files>
  <read_first>
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/view/uisettings/ai-ui-settings-detail-view.xml (current 42-line flat formLayout — needs wrapping in tabSheet)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/uisettings/AiUiSettingsDetailView.java (current 117-line controller — find `@Subscribe onInit`, `@Subscribe onReady`, `onBeforeSave`, the `rejectSave` helper, and the `Messages messages` injection)
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/view/parameters/parameters-detail-view.xml (tabSheet structure precedent — wrap pattern)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/audit/AiAuditEventListView.java (Pattern D — `@Supply(to="grid.col", subject="renderer")` for the badge column; lines 51-68)
    - .planning/phases/16-admin-settings-model-picker-config-knob-migration/16-PATTERNS.md `### AiUiSettingsDetailView.java + XML`
    - .planning/phases/16-admin-settings-model-picker-config-knob-migration/16-CONTEXT.md D-07 (tab placement) and D-09 (locale keys)
  </read_first>
  <action>
    Step A — XML expansion (`ai-ui-settings-detail-view.xml`):

    Wrap the existing `<formLayout id="settingsForm">` (current entire body) inside `<tabSheet id="editTabs">` → `<tab id="generalTab" label="msg:///aiUiSettings.tab.general">`. The existing form's content stays byte-identical inside this tab (existing chat-surface controls untouched).

    Add 3 new sibling tabs inside the same tabSheet:

    `<tab id="tier1KnobsTab" label="msg:///aiUiSettings.tab.tier1Knobs">`:
    - Five sectioned formLayouts (one per cluster) using `<section helperText="msg:///aiUiSettings.section.<cluster>">`. Inside each section, bind the 11 new columns via `property="<camelCaseFieldName>"`:
      - taskFile section: `<integerField property="taskFileTtlSeconds"/>`, `<integerField property="taskFilePerTurnMaxFiles"/>`, `<integerField property="taskFilePerTurnMaxTotalBytes"/>` (use `<bigIntegerField>` if available for Long; otherwise `<integerField>` — verify Jmix component availability; for Long values prefer `<numberField>` if `<integerField>` rejects Long binding)
      - mutation section: `<checkbox property="mutationConfirmationRequired"/>`, `<integerField property="mutationIdempotencyTtlSeconds"/>`, `<integerField property="mutationBulkMaxRows"/>`
      - promptTools section: `<integerField property="promptEntityInventoryLimit"/>`, `<integerField property="toolsMaxFilterDepth"/>`
      - title section: `<integerField property="titleMaxContextMessages"/>`, `<integerField property="titleMinAssistantMessagesTrigger"/>`
      - upload section: `<integerField property="uploadMaxFileSizeBytes"/>`
    - Each field carries `label="msg:///aiUiSettings.field.<camelCaseFieldName>"` matching the Plan 02 locale keys.
    - Flat columns bind zero-glue via `property=` (Plan 02 + Pitfall 1 — field-name-matching setters).

    `<tab id="bootConfigTab" label="msg:///aiUiSettings.tab.bootConfig">`:
    - `<dataGrid id="bootConfigGrid" dataContainer="bootConfigDc" width="100%">` with columns:
      - `<column key="key" header="msg:///aiUiSettings.bootConfig.column.key"/>`
      - `<column key="resolvedValue" header="msg:///aiUiSettings.bootConfig.column.value"/>`
      - `<column key="badge" header="msg:///aiUiSettings.bootConfig.column.badge"/>`

    `<tab id="secretsTab" label="msg:///aiUiSettings.tab.secrets">`:
    - `<dataGrid id="secretsGrid" dataContainer="secretsDc" width="100%">` with columns:
      - `<column key="key" header="msg:///aiUiSettings.secrets.column.key"/>`
      - `<column key="configured" header="msg:///aiUiSettings.secrets.column.configured"/>`

    Under the existing `<data>` element, add two in-memory collection containers:
    ```xml
    <collection id="bootConfigDc" class="com.vn.agent.admin.config.KnobInventory$KnobRow"/>
    <collection id="secretsDc" class="com.vn.agent.admin.config.KnobInventory$SecretIndicatorRow"/>
    ```

    Step B — Controller (`AiUiSettingsDetailView.java`):

    - Inject `@Autowired KnobInventory knobInventory;` + `@Autowired UiComponents uiComponents;` (UiComponents already standard).
    - Add `@ViewComponent private CollectionContainer<KnobInventory.KnobRow> bootConfigDc;` and `@ViewComponent private CollectionContainer<KnobInventory.SecretIndicatorRow> secretsDc;` (Jmix XML data-container injection).
    - Add `@ViewComponent` fields for the 11 new Tier-1 form fields with the same naming as the XML property attributes (e.g., `private JmixIntegerField taskFileTtlField;` — exact component type may be `TypedTextField` if Jmix lacks a Long-aware integer field; use `<numberField>` + `BigDecimalField` if necessary — scout Jmix 2.8 Flow component list).
    - Extend `onInit(InitEvent event)` AFTER the existing `enabledSurfacesField.setItems(...)`:
      ```
      bootConfigDc.getMutableItems().addAll(knobInventory.tier2());
      secretsDc.getMutableItems().addAll(knobInventory.tier3());
      ```
    - Add the bootConfig badge renderer per Pattern D (`@Supply(to="bootConfigGrid.badge", subject="renderer")`):
      ```
      @Supply(to = "bootConfigGrid.badge", subject = "renderer")
      private Renderer<KnobInventory.KnobRow> bootConfigBadgeRenderer() {
          return new ComponentRenderer<>(row -> {
              Span badge = uiComponents.create(Span.class);
              badge.setText(messages.getMessage(row.requiresRestart()
                      ? "aiUiSettings.bootConfig.badge.requiresRestart"
                      : "aiUiSettings.bootConfig.badge.tier1Editable"));
              badge.getElement().getThemeList().add("badge");
              return badge;
          });
      }
      ```
    - Add the secrets `configured` renderer per Pattern D:
      ```
      @Supply(to = "secretsGrid.configured", subject = "renderer")
      private Renderer<KnobInventory.SecretIndicatorRow> secretsConfiguredRenderer() {
          return new ComponentRenderer<>(row -> {
              Span span = uiComponents.create(Span.class);
              span.setText(messages.getMessage(row.configured()
                      ? "aiUiSettings.secrets.indicator.yes"
                      : "aiUiSettings.secrets.indicator.no"));
              // NEVER call row.getValue() or environment.getProperty(row.key()) here —
              // the raw value MUST NOT touch the DOM (SEC-08).
              return span;
          });
      }
      ```
    - Crucial SEC-08 invariant: the secrets renderer NEVER reads a raw secret value into the DOM — it only displays the boolean `configured` flag as a localised "yes"/"no" Span.
    - `onBeforeSave` extension: bean-validation runs through Jmix's standard JPA flush; the `rejectSave(...)` helper at line 102 of the existing controller already handles `ConstraintViolationException`. Verify no extension needed; if Tier-1 validation messages are not surfaced through the standard path, add a catch + `rejectSave(messages.getMessage("aiUiSettings.validation.<knob>.range"))` block.
  </action>
  <verify>
    <automated>cd ai-agent && ./gradlew :ai-agent:compileJava 2>&1 | grep -E "BUILD SUCCESSFUL|error:"</automated>
    <source>grep -c "<tab id=\"tier1KnobsTab\"\\|<tab id=\"bootConfigTab\"\\|<tab id=\"secretsTab\"" ai-agent/ai-agent/src/main/resources/com/vn/agent/view/uisettings/ai-ui-settings-detail-view.xml outputs 3.</source>
    <source>grep -c "@Supply(to = \"bootConfigGrid.badge\"\\|@Supply(to = \"secretsGrid.configured\"" ai-agent/ai-agent/src/main/java/com/vn/agent/view/uisettings/AiUiSettingsDetailView.java outputs 2.</source>
    <source>grep -v '^#' ai-agent/ai-agent/src/main/java/com/vn/agent/view/uisettings/AiUiSettingsDetailView.java | grep -c "getColumnByKey.*setRenderer" outputs 0 (Pattern D — never programmatic setRenderer).</source>
    <source>grep -c "property=\"taskFileTtlSeconds\"\\|property=\"taskFilePerTurnMaxFiles\"\\|property=\"taskFilePerTurnMaxTotalBytes\"\\|property=\"mutationConfirmationRequired\"\\|property=\"mutationIdempotencyTtlSeconds\"\\|property=\"mutationBulkMaxRows\"\\|property=\"promptEntityInventoryLimit\"\\|property=\"toolsMaxFilterDepth\"\\|property=\"titleMaxContextMessages\"\\|property=\"titleMinAssistantMessagesTrigger\"\\|property=\"uploadMaxFileSizeBytes\"" ai-agent/ai-agent/src/main/resources/com/vn/agent/view/uisettings/ai-ui-settings-detail-view.xml outputs 11.</source>
    <source>grep -v '^#' ai-agent/ai-agent/src/main/java/com/vn/agent/view/uisettings/AiUiSettingsDetailView.java | grep -c "environment.getProperty\\|getRawSecret\\|secretValue" outputs 0 (SEC-08 — no raw-value read in the controller).</source>
    <automated>cd ai-agent && ./gradlew :ai-agent:test --tests "*AiUiSettings*" 2>&1 | grep -E "BUILD SUCCESSFUL|FAILED" (existing AiUiSettings tests still pass; new behavior covered by Plan 04/06 tests).</automated>
  </verify>
  <done>
    Three new tabs in the XML descriptor; controller injects KnobInventory; two `@Supply` renderers render badge and configured indicators; 11 Tier-1 form fields bind via `property=`. NO raw secret value reaches the DOM. Existing chat-surface controls in generalTab untouched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add D-09 boot-config/secrets locale keys + flip KnobInventoryClassificationTest + SecretRedactionInvariantsTest green</name>
  <files>
    ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties,
    ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties,
    ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/KnobInventoryClassificationTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/SecretRedactionInvariantsTest.java
  </files>
  <behavior>
    Locale keys added (D-09 — both bundles, identical key set):
      - `aiUiSettings.tab.general`, `.tab.tier1Knobs`, `.tab.bootConfig`, `.tab.secrets`
      - `aiUiSettings.bootConfig.column.key`, `.value`, `.badge`
      - `aiUiSettings.bootConfig.badge.requiresRestart`, `.tier1Editable`
      - `aiUiSettings.secrets.column.key`, `.configured`
      - `aiUiSettings.secrets.indicator.yes`, `.no`

    KnobInventoryClassificationTest:
      - `everyConfigurationPropertiesRecordHasKnobMetadataCoverage()`: reflective walk through `ConfigurationPropertiesBean.getAll(applicationContext)`, assert every component of every record annotated under the 10 phase-targeted records has @KnobMetadata.
      - `tier3SecretPatternMaskAppliesToUnannotatedKey()`: set a host-style property `someHost.custom.api-key=value123` via `@TestPropertySource`, restart context, assert `knobInventory.tier3()` contains a `SecretIndicatorRow` for that key with `configured=true`.
      - `tier2BootToggleCarriesRequiresRestartTrue()`: assert `AiAgentMutationProperties#enabled` (and `AiAgentTitleProperties#enabled`, `AiAgentRagProperties.sampleIngester#enabled`) appear in `knobInventory.tier2()` with `requiresRestart=true`.
      - `tier1MigratedKnobCarriesRequiresRestartFalse()`: assert `AiTaskFileProperties#ttlSeconds` appears in tier1 (or as Tier-1 in tier2 collection — depending on how scanner partitions; spec says Tier-1 surface lives on the AiUiSettings form, so tier1 list is for completeness) with `requiresRestart=false`.

    SecretRedactionInvariantsTest:
      - `noSecretBoundEditable()`: scan all `*-view.xml` under `ai-agent/ai-agent/src/main/resources`, parse XML, find every element of name `textField|comboBox|checkbox|integerField|numberField|bigIntegerField|bigDecimalField|datePicker|checkboxGroup|radioButtonGroup` with `property="..."` attribute; cross-reference property names against `AdminSecretPatternProperties.resolvedPatterns()` (glob → regex `.*\\.(api-key|password|secret|token)`); assert NO matches.
      - `noConditionalOnPropertyToggleBoundEditable()`: classpath walk for `*.class` files under `ai-agent/ai-agent/src/main/java` (compiled output), parse `@ConditionalOnProperty(name="...")` keys; cross-reference against XML `property="..."` bindings; assert NO matches. (Alternative implementation: source walk under `src/main/java` looking for `@ConditionalOnProperty(name = "..."` declarations.)
      - `singlePublishSiteForAiSettingsChangedEvent()`: walk `ai-agent/ai-agent/src/main/java` for the regex `publishEvent\\s*\\(\\s*new\\s+AiSettingsChangedEvent`; assert the matching file set is exactly `{AiParametersEntityListener.java, AiUiSettingsEntityListener.java}` (duplicates Plan 05's same-named test method — this leg is identical for the SEC-08 contract).
  </behavior>
  <read_first>
    - ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/KnobInventoryClassificationTest.java (Plan 01 scaffold)
    - ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/SecretRedactionInvariantsTest.java (Plan 01 scaffold)
    - .planning/phases/16-admin-settings-model-picker-config-knob-migration/16-CONTEXT.md D-09 (full key list — boot-config + secrets groups)
    - .planning/phases/16-admin-settings-model-picker-config-knob-migration/16-RESEARCH.md `### Pattern Map @ConfigurationProperties Records Inventory` (Tier classification table)
  </read_first>
  <action>
    Step A — Append D-09 locale keys to BOTH `messages_en.properties` AND `messages_vi.properties` (root bundle, identical key set per Pitfall 7):
    - `aiUiSettings.tab.general` (en: "General" / vi: "Chung")
    - `aiUiSettings.tab.tier1Knobs` (en: "Operator knobs" / vi: "Tham số vận hành")
    - `aiUiSettings.tab.bootConfig` (en: "Boot config (read-only)" / vi: "Cấu hình khởi động (chỉ đọc)")
    - `aiUiSettings.tab.secrets` (en: "Secrets" / vi: "Bí mật")
    - `aiUiSettings.bootConfig.column.key` (en: "Property" / vi: "Thuộc tính")
    - `aiUiSettings.bootConfig.column.value` (en: "Current value" / vi: "Giá trị hiện tại")
    - `aiUiSettings.bootConfig.column.badge` (en: "Classification" / vi: "Phân loại")
    - `aiUiSettings.bootConfig.badge.requiresRestart` (en: "property only — requires restart" / vi: "chỉ thuộc tính — cần khởi động lại")
    - `aiUiSettings.bootConfig.badge.tier1Editable` (en: "editable above" / vi: "có thể chỉnh ở trên")
    - `aiUiSettings.secrets.column.key` (en: "Property" / vi: "Thuộc tính")
    - `aiUiSettings.secrets.column.configured` (en: "Configured" / vi: "Đã cấu hình")
    - `aiUiSettings.secrets.indicator.yes` (en: "yes" / vi: "có")
    - `aiUiSettings.secrets.indicator.no` (en: "no" / vi: "không")

    Step B — Flip `KnobInventoryClassificationTest` per `<behavior>`. Boot stack Pattern I. Inject `@Autowired KnobInventory knobInventory; @Autowired ApplicationContext context;`. Use `ConfigurationPropertiesBean.getAll(context)` directly in the test for the coverage assertion. Remove all `fail()` bodies.

    Step C — Flip `SecretRedactionInvariantsTest` per `<behavior>`. NO Spring context needed for the source-scan legs — pure file-system walks via `java.nio.file.Files.walk`. The third leg (publish-site invariant) duplicates Plan 05's source-scan but is required by SEC-08's locked contract; both tests assert the same invariant for redundancy. Remove all `fail()` bodies.
  </action>
  <verify>
    <automated>cd ai-agent && ./gradlew :ai-agent:test --tests "com.vn.agent.admin.config.KnobInventoryClassificationTest" 2>&1 | grep -E "BUILD SUCCESSFUL|FAILED"</automated>
    <automated>cd ai-agent && ./gradlew :ai-agent:test --tests "com.vn.agent.admin.config.SecretRedactionInvariantsTest" 2>&1 | grep -E "BUILD SUCCESSFUL|FAILED"</automated>
    <source>grep -c "^aiUiSettings\\.tab\\.\\|^aiUiSettings\\.bootConfig\\.\\|^aiUiSettings\\.secrets\\." ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties outputs ≥ 13.</source>
    <source>diff <(grep -oE "^aiUiSettings\\.(tab|bootConfig|secrets)\\.[a-zA-Z.]+" ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties | sort -u) <(grep -oE "^aiUiSettings\\.(tab|bootConfig|secrets)\\.[a-zA-Z.]+" ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties | sort -u) outputs no lines (key parity).</source>
    <source>grep -v '^#' ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/SecretRedactionInvariantsTest.java | grep -c "fail\\(" outputs 0.</source>
  </verify>
  <done>
    Both tests green. Locale keys present in both bundles. SEC-08's three-legged invariant enforced at build time.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Environment property store → secrets grid Span | Tier-3 path: only the `configured: yes/no` boolean reaches the DOM; raw value never read into a renderable component. T-16-01 mitigation. |
| XML view descriptor `property=` binding → `@ConfigurationProperties` field name | SEC-08 source-scan rejects any binding whose property name matches `*.api-key|*.password|*.secret|*.token` OR matches a `@ConditionalOnProperty` key. T-16-01 + T-16-02 mitigations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Information Disclosure | Tier-3 secrets surface | mitigate | `SecretIndicatorRow.configured` is boolean-only; renderer outputs only "yes"/"no" Span; controller has zero `environment.getProperty(secretKey)` calls; `noSecretBoundEditable()` source-scan blocks future regressions. |
| T-16-02 | Tampering | Boot-toggle Vaadin editable | mitigate | `noConditionalOnPropertyToggleBoundEditable()` source-scan asserts no `@ConditionalOnProperty(name=...)` key maps to an XML `property=` binding; Tier-2 entries render through `bootConfigGrid` (Span-only, never HasValue). |
| Twin-publisher (R2) | Tampering | settings-change event source-scan | mitigate | `singlePublishSiteForAiSettingsChangedEvent()` enforces the invariant a third time (Plan 05 + this plan); harder to slip past. |
| T-16-05 | Elevation of Privilege | bootConfigTab + secretsTab visibility | mitigate | View id `AiAgent_AiUiSettings.detail` is in `AiAgentAdminRole.@ViewPolicy` only; non-admin login lacks the route entirely (existing role gate). |
</threat_model>

<verification>
- `./gradlew :ai-agent:test --tests "com.vn.agent.admin.config.KnobInventoryClassificationTest"` exits 0.
- `./gradlew :ai-agent:test --tests "com.vn.agent.admin.config.SecretRedactionInvariantsTest"` exits 0.
- `./gradlew :ai-agent:test --tests "*AiUiSettings*"` passes (existing tests + Plan 02 bean-validation green).
- Locale parity diff returns empty.
- `./gradlew :ai-agent-starter:compileJava` succeeds (KnobInventoryScanner + autoconfig compile in the starter module).
</verification>

<success_criteria>
- All 10 @ConfigurationProperties records carry @KnobMetadata on every component.
- KnobInventoryScanner + KnobInventory + KnobInventoryAutoConfiguration ship in the starter.
- AiUiSettingsDetailView has 4 tabs: general, tier1Knobs (11 form fields), bootConfig (dataGrid with badge renderer), secrets (dataGrid with configured indicator).
- 13 new locale keys in both bundles.
- KnobInventoryClassificationTest + SecretRedactionInvariantsTest flip green.
- SEC-08's three legs enforced.
</success_criteria>

<output>
After completion, create `.planning/phases/16-admin-settings-model-picker-config-knob-migration/16-06-SUMMARY.md`.
</output>
