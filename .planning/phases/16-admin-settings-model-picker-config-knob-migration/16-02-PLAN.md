---
phase: 16-admin-settings-model-picker-config-knob-migration
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiUiSettings.java
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/120-ai-ui-settings-tier1-knobs.xml
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties
  - ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/AiUiSettingsBeanValidationTest.java
autonomous: true
requirements: [CFG-01, CFG-03]
requirements_addressed: [CFG-01, CFG-03]
tags: [jmix-entities, jmix-liquibase, jmix-i18n]

must_haves:
  truths:
    - "AiUiSettings entity carries 12 new nullable Tier-1 columns (D-01 list + TASK_FILE_MAX_FILE_SIZE_BYTES added per codex HIGH Concern #4 — task-file chat upload max size is a distinct knob from RAG/KB upload max size) plus existing enabledSurfaceIds and defaultSurface — null means fall through to module.properties default (per CFG-01 resolution-order contract)."
    - "Each new column has a Jakarta bean-validation annotation per RESEARCH §8 bounds table; saving an out-of-range value throws ConstraintViolationException."
    - "Sentinel value -1 is permitted on TASK_FILE_TTL_SECONDS, TASK_FILE_PER_TURN_MAX_FILES, TASK_FILE_PER_TURN_MAX_TOTAL_BYTES per Phase 13.1 contract (preserved through @Min(-1) annotation). TASK_FILE_MAX_FILE_SIZE_BYTES does NOT take a sentinel (always positive)."
    - "Liquibase changelog 120-ai-ui-settings-tier1-knobs.xml adds the 12 columns AND widens AI_AUDIT_EVENT.KIND to varchar(32) (Pitfall 5)."
    - "080-ai-ui-settings.xml is byte-identical to its pre-phase content (Phase 12 D-15 invariant)."
    - "Locale parity: every new aiUiSettings.field.* / aiUiSettings.section.* / aiUiSettings.validation.* key in messages_en.properties is also in messages_vi.properties (D-09)."
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiUiSettings.java"
      provides: "12 new nullable typed columns + getters/setters with field-name-matching setters (Pitfall 1); TASK_FILE_MAX_FILE_SIZE_BYTES added per codex HIGH Concern #4"
    - path: "ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/120-ai-ui-settings-tier1-knobs.xml"
      provides: "Additive DDL: 12 columns on AI_UI_SETTINGS + KIND column widening on AI_AUDIT_EVENT"
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/AiUiSettingsBeanValidationTest.java"
      provides: "Per-knob out-of-range save → ConstraintViolationException"
  key_links:
    - from: "ChatModelCatalogAllowlistTest (Plan 01 scaffold)"
      to: "AI_UI_SETTINGS columns"
      via: "@Column field on AiUiSettings"
      pattern: "@Column\\(name = \"TASK_FILE_TTL_SECONDS\"\\)"
    - from: "AiUiSettingsResolver (Plan 03)"
      to: "AiUiSettings getters"
      via: "getTaskFileTtlSeconds() etc."
      pattern: "getter returning boxed Long/Integer/Boolean — null = fall-through signal"
---

<objective>
Expand the AiUiSettings singleton with 12 nullable Tier-1 columns per D-01 + codex HIGH Concern #4 (TASK_FILE_MAX_FILE_SIZE_BYTES added; chat task-file upload cap is a DISTINCT knob from RAG/KB upload cap) with bean-validation bounds from RESEARCH §8, land the additive Liquibase changelog `120-ai-ui-settings-tier1-knobs.xml` (column adds + AI_AUDIT_EVENT.KIND widening to varchar(32) per Pitfall 5), add the locale message keys for the new form fields per D-09, and flip the `AiUiSettingsBeanValidationTest` scaffold green.

Purpose: This is the schema lock for the entire phase. Plan 03 (resolver) and Plan 06 (view) both consume the column types and bean-validation annotations. The KIND-column widening is co-located here because the same changelog file is the only DDL touchpoint in Phase 16.

Output: 12 new entity columns, one new changelog file, 12 sets of locale keys × 2 bundles, one bean-validation test class flipped green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-admin-settings-model-picker-config-knob-migration/16-SPEC.md
@.planning/phases/16-admin-settings-model-picker-config-knob-migration/16-CONTEXT.md
@.planning/phases/16-admin-settings-model-picker-config-knob-migration/16-RESEARCH.md
@.planning/phases/16-admin-settings-model-picker-config-knob-migration/16-PATTERNS.md
@ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiUiSettings.java
@ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/080-ai-ui-settings.xml
@ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/030-ai-audit-event.xml
@ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/110-ai-extraction-draft.xml

<interfaces>
From `AiUiSettings.java` (lines 38-53, existing flat-column precedent):
```java
@NotNull
@Column(name = "ENABLED_SURFACE_IDS", nullable = false)
private String enabledSurfaceIds = toEnabledSurfaceIds(EnumSet.allOf(AiChatSurface.class));

@NotNull
@Column(name = "DEFAULT_SURFACE", nullable = false, length = 64)
private String defaultSurface = AiChatSurface.FULL_ROUTE.getId();
```

EclipseLink weaver invariant (lines 102-108 docstring): setter name MUST match field name; no `@Transient` bridges.

Existing changelog parent `agentstore-changelog.xml` uses `<includeAll path="/com/vn/agent/liquibase/agentstore-changelog" errorIfMissingOrEmpty="false"/>` — new file lands as sibling.

RESEARCH §8 bean-validation bounds table (locked):
| Column | Type | Annotation | Sentinel |
| TASK_FILE_TTL_SECONDS | Long | @Min(-1) @Max(604800) | -1 disables |
| TASK_FILE_PER_TURN_MAX_FILES | Integer | @Min(-1) @Max(100) | -1 disables |
| TASK_FILE_PER_TURN_MAX_TOTAL_BYTES | Long | @Min(-1) @Max(524288000) | -1 disables |
| MUTATION_CONFIRMATION_REQUIRED | Boolean | (no bounds) | — |
| MUTATION_IDEMPOTENCY_TTL_SECONDS | Long | @Min(60) @Max(604800) | — |
| MUTATION_BULK_MAX_ROWS | Integer | @Min(1) @Max(500) | — |
| PROMPT_ENTITY_INVENTORY_LIMIT | Integer | @Min(1) @Max(500) | — |
| TOOLS_MAX_FILTER_DEPTH | Integer | @Min(1) @Max(10) | — |
| TITLE_MAX_CONTEXT_MESSAGES | Integer | @Min(1) @Max(50) | — |
| TITLE_MIN_ASSISTANT_MESSAGES_TRIGGER | Integer | @Min(1) @Max(10) | — |
| UPLOAD_MAX_FILE_SIZE_BYTES | Long | @Min(1024) @Max(524288000) | — (KB/RAG upload cap) |
| TASK_FILE_MAX_FILE_SIZE_BYTES | Long | @Min(1024) @Max(524288000) | — (NEW per codex HIGH Concern #4 — chat task-file upload cap, distinct from RAG upload cap) |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add 12 nullable Tier-1 columns to AiUiSettings with bean-validation bounds + setter-name-matches-field-name (TASK_FILE_MAX_FILE_SIZE_BYTES added per codex HIGH Concern #4)</name>
  <files>ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiUiSettings.java</files>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiUiSettings.java (current entity — read lines 1-175 to see existing column shape, sentinel handling, and the EclipseLink-weaver docstring at lines 90-108)
    - .planning/phases/16-admin-settings-model-picker-config-knob-migration/16-RESEARCH.md `### 8. Bean-validation bounds per Tier-1 knob` (the 11-row table is the authoritative spec for this task)
    - .planning/phases/16-admin-settings-model-picker-config-knob-migration/16-PATTERNS.md Pattern G (Pitfall 1 — flat-column setter-name-matches-field-name)
  </read_first>
  <action>
    Add 12 nullable boxed-typed columns to `AiUiSettings.java` mirroring the existing `defaultSurface` flat-column shape (precedent at lines 51-53, 102-108). For each column emit: (a) Jakarta bean-validation annotation, (b) `@Column(name = "<UPPER_SNAKE>")` WITHOUT `nullable=false` (every Tier-1 column is nullable so `null` = fall-through), (c) the private field (boxed type — `Long`, `Integer`, `Boolean` — never primitive, because `null` is the load-bearing sentinel for "use module.properties default"), (d) public getter and setter with the setter name exactly matching the field name (Pattern G — Pitfall 1).

    Exact column inventory (UPPER_SNAKE column name → field name → boxed type → annotation):
    - `TASK_FILE_TTL_SECONDS` → `taskFileTtlSeconds` → `Long` → `@Min(-1) @Max(604_800L)` (sentinel -1; 7-day cap)
    - `TASK_FILE_PER_TURN_MAX_FILES` → `taskFilePerTurnMaxFiles` → `Integer` → `@Min(-1) @Max(100)` (sentinel -1)
    - `TASK_FILE_PER_TURN_MAX_TOTAL_BYTES` → `taskFilePerTurnMaxTotalBytes` → `Long` → `@Min(-1L) @Max(524_288_000L)` (sentinel -1; 500 MB cap)
    - `TASK_FILE_MAX_FILE_SIZE_BYTES` → `taskFileMaxFileSizeBytes` → `Long` → `@Min(1024L) @Max(524_288_000L)` (NEW per codex HIGH Concern #4 — chat task-file per-file upload cap, distinct from RAG upload cap; 1 KiB floor – 500 MB ceiling; no sentinel)
    - `MUTATION_CONFIRMATION_REQUIRED` → `mutationConfirmationRequired` → `Boolean` → (no bounds)
    - `MUTATION_IDEMPOTENCY_TTL_SECONDS` → `mutationIdempotencyTtlSeconds` → `Long` → `@Min(60L) @Max(604_800L)` (1 min–7 days; no sentinel — idempotency must be meaningful)
    - `MUTATION_BULK_MAX_ROWS` → `mutationBulkMaxRows` → `Integer` → `@Min(1) @Max(500)` (Phase 13 D-02 DoS guard)
    - `PROMPT_ENTITY_INVENTORY_LIMIT` → `promptEntityInventoryLimit` → `Integer` → `@Min(1) @Max(500)`
    - `TOOLS_MAX_FILTER_DEPTH` → `toolsMaxFilterDepth` → `Integer` → `@Min(1) @Max(10)`
    - `TITLE_MAX_CONTEXT_MESSAGES` → `titleMaxContextMessages` → `Integer` → `@Min(1) @Max(50)`
    - `TITLE_MIN_ASSISTANT_MESSAGES_TRIGGER` → `titleMinAssistantMessagesTrigger` → `Integer` → `@Min(1) @Max(10)`
    - `UPLOAD_MAX_FILE_SIZE_BYTES` → `uploadMaxFileSizeBytes` → `Long` → `@Min(1024L) @Max(524_288_000L)` (KB/RAG upload cap, 1 KiB floor – 500 MB cap; distinct knob from `TASK_FILE_MAX_FILE_SIZE_BYTES`)

    Setter contract (Pattern G / Pitfall 1): each setter is `public void setTaskFileTtlSeconds(Long taskFileTtlSeconds) { this.taskFileTtlSeconds = taskFileTtlSeconds; }` — name MATCHES field name, direct field assignment is tracked by EclipseLink weaver. Do NOT introduce `@Transient` getter bridges; do NOT compose/coerce inside the entity. Place the new fields below the existing `defaultSurface` field (around line 53) in the order listed above so the file diff is contiguous. Insert one block comment ABOVE the new fields: `// Phase 16 D-01 Tier-1 runtime knobs — null = fall through to module.properties default (resolved by AiUiSettingsResolver). Setter names MUST match field names (EclipseLink weaver — see setDefaultSurface docstring lines 102-108).`
  </action>
  <verify>
    <automated>cd ai-agent && ./gradlew :ai-agent:compileJava 2>&1 | grep -E "BUILD SUCCESSFUL|error:"</automated>
    <source>(Select-String -Path ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiUiSettings.java -Pattern "@Column\(name = `"(TASK_FILE_TTL_SECONDS|TASK_FILE_PER_TURN_MAX_FILES|TASK_FILE_PER_TURN_MAX_TOTAL_BYTES|TASK_FILE_MAX_FILE_SIZE_BYTES|MUTATION_CONFIRMATION_REQUIRED|MUTATION_IDEMPOTENCY_TTL_SECONDS|MUTATION_BULK_MAX_ROWS|PROMPT_ENTITY_INVENTORY_LIMIT|TOOLS_MAX_FILTER_DEPTH|TITLE_MAX_CONTEXT_MESSAGES|TITLE_MIN_ASSISTANT_MESSAGES_TRIGGER|UPLOAD_MAX_FILE_SIZE_BYTES)`"").Count -eq 12 (codex MEDIUM fix — PowerShell-safe + codex HIGH Concern #4 — 12 columns now)</source>
    <source>grep -v '^#' ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiUiSettings.java | grep -c "@Transient" outputs 0 (Pitfall 1 — no transient bridges on Tier-1 columns).</source>
  </verify>
  <done>
    All 12 columns present with bean-validation annotations matching RESEARCH §8 table + codex Concern #4 extension. Setter names match field names. Compile succeeds under `./gradlew :ai-agent:compileJava`. No `@Transient` bridges on the new fields. The class still compiles against existing consumers (AiUiSettingsService, AiUiSettingsDetailView) — Plan 03 wires the new getters into the resolver, Plan 06 wires them into the view.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create Liquibase changelog 120-ai-ui-settings-tier1-knobs.xml (12 column adds + KIND widening; +1 column per codex HIGH Concern #4)</name>
  <files>ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/120-ai-ui-settings-tier1-knobs.xml</files>
  <read_first>
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/080-ai-ui-settings.xml (column-type precedent for varchar / etc. on AI_UI_SETTINGS table)
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/110-ai-extraction-draft.xml (sibling shape — same parent, latest filename slot before 120)
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/071-widen-ai-audit-outcome.xml (`<modifyDataType>` precedent for widening an existing varchar column on AI_AUDIT_EVENT — directly applicable to the KIND widening)
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/030-ai-audit-event.xml (current AI_AUDIT_EVENT.KIND column definition — confirm current varchar width before widening)
    - .planning/phases/16-admin-settings-model-picker-config-knob-migration/16-RESEARCH.md Pitfall 5 (KIND width fix)
    - .planning/phases/16-admin-settings-model-picker-config-knob-migration/16-PATTERNS.md Pattern H (Liquibase additive via includeAll)
  </read_first>
  <action>
    Create new file `120-ai-ui-settings-tier1-knobs.xml` under `ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/` using the same `<databaseChangeLog>` root + namespace declarations as `110-ai-extraction-draft.xml`. The parent `agentstore-changelog.xml` uses `<includeAll>` (Pattern H) — DO NOT edit the parent.

    Contents — two `<changeSet>` blocks:

    `<changeSet id="1" author="ai-agent">` — adds 12 nullable columns to `AI_UI_SETTINGS`:
    - `<addColumn tableName="AI_UI_SETTINGS">` containing 12 `<column>` children:
      - `<column name="TASK_FILE_TTL_SECONDS" type="bigint"/>` (Long → bigint)
      - `<column name="TASK_FILE_PER_TURN_MAX_FILES" type="int"/>` (Integer → int)
      - `<column name="TASK_FILE_PER_TURN_MAX_TOTAL_BYTES" type="bigint"/>`
      - `<column name="TASK_FILE_MAX_FILE_SIZE_BYTES" type="bigint"/>` (NEW per codex HIGH Concern #4 — chat task-file per-file upload cap)
      - `<column name="MUTATION_CONFIRMATION_REQUIRED" type="boolean"/>`
      - `<column name="MUTATION_IDEMPOTENCY_TTL_SECONDS" type="bigint"/>`
      - `<column name="MUTATION_BULK_MAX_ROWS" type="int"/>`
      - `<column name="PROMPT_ENTITY_INVENTORY_LIMIT" type="int"/>`
      - `<column name="TOOLS_MAX_FILTER_DEPTH" type="int"/>`
      - `<column name="TITLE_MAX_CONTEXT_MESSAGES" type="int"/>`
      - `<column name="TITLE_MIN_ASSISTANT_MESSAGES_TRIGGER" type="int"/>`
      - `<column name="UPLOAD_MAX_FILE_SIZE_BYTES" type="bigint"/>` (KB/RAG upload cap — distinct from TASK_FILE_MAX_FILE_SIZE_BYTES per codex Concern #4)
    - NO `defaultValue` (null = fall-through to module.properties).
    - NO `<constraints nullable="false"/>` (all 12 are nullable by contract).

    `<changeSet id="2" author="ai-agent">` — widens `AI_AUDIT_EVENT.KIND` to `varchar(32)` per Pitfall 5:
    - `<modifyDataType tableName="AI_AUDIT_EVENT" columnName="KIND" newDataType="varchar(32)"/>` (additive widening — PG/HSQL/MSSQL all support it). Pattern precedent: `071-widen-ai-audit-outcome.xml` does the same thing for the OUTCOME column.

    DO NOT modify `080-ai-ui-settings.xml` (Phase 12 D-15 invariant — verify byte-identicality with `git diff 080-ai-ui-settings.xml` showing zero changes after Plan 02 commit).
  </action>
  <verify>
    <automated>cd ai-agent && ./gradlew :ai-agent:test --tests "*LiquibaseChangelogTest*" 2>&1 | grep -E "BUILD SUCCESSFUL|FAILED"</automated>
    <source>test -f ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/120-ai-ui-settings-tier1-knobs.xml outputs file present.</source>
    <source>(Select-String -Path ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/120-ai-ui-settings-tier1-knobs.xml -Pattern "<column name=`"(TASK_FILE_TTL_SECONDS|TASK_FILE_PER_TURN_MAX_FILES|TASK_FILE_PER_TURN_MAX_TOTAL_BYTES|TASK_FILE_MAX_FILE_SIZE_BYTES|MUTATION_CONFIRMATION_REQUIRED|MUTATION_IDEMPOTENCY_TTL_SECONDS|MUTATION_BULK_MAX_ROWS|PROMPT_ENTITY_INVENTORY_LIMIT|TOOLS_MAX_FILTER_DEPTH|TITLE_MAX_CONTEXT_MESSAGES|TITLE_MIN_ASSISTANT_MESSAGES_TRIGGER|UPLOAD_MAX_FILE_SIZE_BYTES)`"").Count -eq 12 (PowerShell-safe + codex HIGH Concern #4)</source>
    <automated>cd ai-agent && ./gradlew :ai-agent:bootRun -Pliquibase.dryRun=true 2>&1 | findstr /C:"BUILD SUCCESSFUL" /C:"FAILED" (codex MEDIUM fix — direct Liquibase apply-on-boot test; alt: rely on any existing @SpringBootTest below)</automated>
    <source>grep -c "modifyDataType.*AI_AUDIT_EVENT.*KIND.*varchar(32)" ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/120-ai-ui-settings-tier1-knobs.xml outputs ≥ 1.</source>
    <source>git diff --stat HEAD -- ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/080-ai-ui-settings.xml shows zero changes (Phase 12 D-15 invariant — sibling unchanged).</source>
    <source>git diff --stat HEAD -- ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog.xml shows zero changes (D-08 invariant — parent file untouched; new changelog is discovered via existing `<includeAll>`).</source>
    <automated>cd ai-agent && ./gradlew :ai-agent:test --tests "*ApplicationContextTest*" 2>&1 | grep -E "BUILD SUCCESSFUL|FAILED" (boot succeeds → Liquibase migration applies cleanly against the test DB).</automated>
  </verify>
  <done>
    File exists; one `<changeSet id="1">` with 11 nullable column adds; one `<changeSet id="2">` widening AI_AUDIT_EVENT.KIND to varchar(32); sibling `080-ai-ui-settings.xml` byte-identical AND parent `agentstore-changelog.xml` byte-identical (`git diff` shows no change on either — D-08 + Phase 12 D-15 invariants); Spring Boot test boot succeeds (Liquibase migration applies cleanly) — proxy verified by any `@SpringBootTest` class booting under `./gradlew :ai-agent:test --tests "*ApplicationContextTest*"` or the existing `TtlConfigTest`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add locale message keys for new Tier-1 form fields + flip AiUiSettingsBeanValidationTest green</name>
  <files>
    ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties,
    ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties,
    ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/AiUiSettingsBeanValidationTest.java
  </files>
  <behavior>
    - Save AiUiSettings with `taskFileTtlSeconds = -2` → throws `ConstraintViolationException` (below @Min(-1)).
    - Save AiUiSettings with `taskFileTtlSeconds = -1` → succeeds (sentinel preserved).
    - Save AiUiSettings with `taskFileTtlSeconds = 604_801` → throws `ConstraintViolationException` (above @Max(604_800)).
    - Save AiUiSettings with `mutationBulkMaxRows = 0` → throws (below @Min(1)).
    - Save AiUiSettings with `mutationBulkMaxRows = 501` → throws (above @Max(500)).
    - Save AiUiSettings with `uploadMaxFileSizeBytes = 1023L` → throws (below @Min(1024L)).
    - Locale parity: every `aiUiSettings.field.*` key emitted in messages_en.properties exists at the identical key in messages_vi.properties.
  </behavior>
  <read_first>
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties (search for existing `aiUiSettingsDetail.field.*` block — lines 271-279 — for placement and key shape)
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties (find the same block by line number proximity — Vi bundle mirrors En key set)
    - .planning/phases/16-admin-settings-model-picker-config-knob-migration/16-CONTEXT.md D-09 (full key list)
    - .planning/phases/16-admin-settings-model-picker-config-knob-migration/16-RESEARCH.md `### 8. Bean-validation bounds per Tier-1 knob` (one validation msg key per knob)
    - ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/AiUiSettingsBeanValidationTest.java (scaffold from Plan 01 — currently `fail()`)
    - ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/TtlConfigTest.java (Pattern I — `@SpringBootTest` boot shape for the integration test)
  </read_first>
  <action>
    Step A — Append D-09 key groups to BOTH `messages_en.properties` AND `messages_vi.properties` in identical key order. Key list (every key MUST appear in both bundles per Pitfall 7 — key parity, not value parity):

    Section labels:
    - `aiUiSettings.section.taskFile`
    - `aiUiSettings.section.mutation`
    - `aiUiSettings.section.promptTools`
    - `aiUiSettings.section.title`
    - `aiUiSettings.section.upload`

    Field labels (one per Tier-1 column, 12 keys):
    - `aiUiSettings.field.taskFileTtlSeconds`
    - `aiUiSettings.field.taskFilePerTurnMaxFiles`
    - `aiUiSettings.field.taskFilePerTurnMaxTotalBytes`
    - `aiUiSettings.field.taskFileMaxFileSizeBytes` (NEW per codex HIGH Concern #4)
    - `aiUiSettings.field.mutationConfirmationRequired`
    - `aiUiSettings.field.mutationIdempotencyTtlSeconds`
    - `aiUiSettings.field.mutationBulkMaxRows`
    - `aiUiSettings.field.promptEntityInventoryLimit`
    - `aiUiSettings.field.toolsMaxFilterDepth`
    - `aiUiSettings.field.titleMaxContextMessages`
    - `aiUiSettings.field.titleMinAssistantMessagesTrigger`
    - `aiUiSettings.field.uploadMaxFileSizeBytes`

    Validation messages (one per Tier-1 knob with bounds — 11 keys, mutationConfirmationRequired has no bounds so no validation key):
    - `aiUiSettings.validation.taskFileTtl.range`
    - `aiUiSettings.validation.taskFilePerTurnMaxFiles.range`
    - `aiUiSettings.validation.taskFilePerTurnMaxTotalBytes.range`
    - `aiUiSettings.validation.taskFileMaxFileSizeBytes.range` (NEW per codex HIGH Concern #4)
    - `aiUiSettings.validation.mutationIdempotencyTtl.range`
    - `aiUiSettings.validation.mutationBulkMaxRows.range`
    - `aiUiSettings.validation.promptEntityInventoryLimit.range`
    - `aiUiSettings.validation.toolsMaxFilterDepth.range`
    - `aiUiSettings.validation.titleMaxContextMessages.range`
    - `aiUiSettings.validation.titleMinAssistantMessagesTrigger.range`
    - `aiUiSettings.validation.uploadMaxFileSizeBytes.range`

    English values use the field name in a human-readable form (e.g., `aiUiSettings.field.taskFileTtlSeconds=Task file TTL (seconds, -1 disables cleanup)`); Vietnamese values use Vietnamese translations. Sentinel-bearing keys MUST mention "-1" in both languages so admins know the disable affordance. Do NOT translate the keys (`feedback_jmix_messages_over_spring` — root bundle, identical key set across locales).

    Step B — Flip `AiUiSettingsBeanValidationTest` green:

    Test class shape mirrors `TtlConfigTest` (Pattern I — `@SpringBootTest(classes = AITestConfiguration.class) @ImportAutoConfiguration({AIAutoConfiguration.class, SpiDefaultsAutoConfiguration.class}) @Import({...stub configs...})`). Test methods:
    - `taskFileTtlBelowMinusOneRejected()`: set `taskFileTtlSeconds = -2L`, `dataManager.save(settings)`, assert `ConstraintViolationException` thrown (catch via JUnit 5 `assertThrows`).
    - `taskFileTtlMinusOneSentinelAccepted()`: set `taskFileTtlSeconds = -1L`, save succeeds, reload row, assert value == -1L.
    - `taskFileTtlAboveMaxRejected()`: set `taskFileTtlSeconds = 604_801L`, assert throws.
    - `mutationBulkMaxRowsBelowMinRejected()`: set `mutationBulkMaxRows = 0`, assert throws.
    - `mutationBulkMaxRowsAboveMaxRejected()`: set `mutationBulkMaxRows = 501`, assert throws.
    - `uploadMaxFileSizeBytesBelowFloorRejected()`: set `uploadMaxFileSizeBytes = 1023L`, assert throws.
    - `taskFileMaxFileSizeBytesBelowFloorRejected()`: set `taskFileMaxFileSizeBytes = 1023L`, assert throws (codex HIGH Concern #4 — new column bound check).
    - `mutationConfirmationRequiredAcceptsAnyBooleanIncludingNull()`: set null, save, reload, value == null; set false, save, reload, value == false. (No bounds.)

    Use `unconstrainedDataManager` to load/save the singleton (Pitfall 3) so the test is not blocked by row-level security. Remove every `fail("Wave 0 scaffold")` body and replace with real assertions.
  </action>
  <verify>
    <automated>cd ai-agent && ./gradlew :ai-agent:test --tests "com.vn.agent.admin.config.AiUiSettingsBeanValidationTest" 2>&1 | grep -E "BUILD SUCCESSFUL|FAILED"</automated>
    <source>Locale parity proxy: `diff <(grep -oE "^aiUiSettings\\.(field|section|validation)\\.[a-zA-Z.]+" ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties | sort -u) <(grep -oE "^aiUiSettings\\.(field|section|validation)\\.[a-zA-Z.]+" ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties | sort -u)` outputs no differences.</source>
    <source>(Select-String -Path ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties -Pattern "^aiUiSettings\.field\.").Count -eq 12 (codex HIGH Concern #4 — 12 field keys)</source>
    <source>(Select-String -Path ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties -Pattern "^aiUiSettings\.field\.").Count -eq 12</source>
  </verify>
  <done>
    `AiUiSettingsBeanValidationTest` passes (7 test methods green). Both locale bundles contain the same 11 `aiUiSettings.field.*` keys, 5 `aiUiSettings.section.*` keys, and 10 `aiUiSettings.validation.*` keys. Locale parity diff outputs zero rows. (Plans 05 and 06 add the remaining D-09 key groups: `parametersDetail.modelField.*`, `aiUiSettings.tab.*`, `aiUiSettings.bootConfig.*`, `aiUiSettings.secrets.*`, `chat.error.modelValidationFailure`, `chat.notice.modelFallbackApplied`.)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin save form input → JPA entity → DB column | Bean-validation runs at JPA flush before DB write. T-16-02 (boot-toggle becomes runtime-editable) is mitigated: this plan adds ONLY Tier-1 columns; Tier-2 boot toggles remain `@ConditionalOnProperty` consumers, never JPA columns. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02 | Tampering | AiUiSettings schema | mitigate | This plan adds ONLY the 11 Tier-1 knobs listed in D-01 — no `@ConditionalOnProperty` toggle gets a JPA column. SEC-08 test in Plan 06 cross-checks. |
| T-16-04 | Information Disclosure | AI_AUDIT_EVENT.KIND widening | accept | Widening a varchar column from 16 to 32 is a low-risk additive DDL; existing rows are preserved unchanged (per `071-widen-ai-audit-outcome.xml` precedent). |
| T-16-05 | Elevation of Privilege | New Tier-1 columns | accept | The columns themselves carry no end-user-facing secret. Admin-only access is enforced by `@EntityPolicy` (existing `AiAgentAdminRole` READ+UPDATE on AiUiSettings) and the singleton-entity ViewPolicy — verified separately in Plan 06. |
</threat_model>

<verification>
- `./gradlew :ai-agent:compileJava` and `:ai-agent:compileTestJava` exit 0.
- `./gradlew :ai-agent:test --tests "com.vn.agent.admin.config.AiUiSettingsBeanValidationTest"` exits 0.
- `git diff` on `080-ai-ui-settings.xml` shows zero changes.
- Locale parity diff command returns empty output.
- A `@SpringBootTest` from another package (e.g., `TtlConfigTest`) still boots cleanly → Liquibase migration applied cleanly against the test DB.
</verification>

<success_criteria>
- 12 nullable columns added to AiUiSettings with Jakarta bean-validation bounds matching RESEARCH §8 + codex HIGH Concern #4 extension (TASK_FILE_MAX_FILE_SIZE_BYTES is its own column, separate from UPLOAD_MAX_FILE_SIZE_BYTES).
- Setter names match field names (no Pitfall 1 weaver trap).
- Changelog `120-ai-ui-settings-tier1-knobs.xml` exists with two changeSets (12 column adds + KIND widening).
- `080-ai-ui-settings.xml` is byte-identical (Phase 12 D-15).
- New locale keys added to BOTH bundles with identical key set (12 field keys + 11 validation keys).
- `AiUiSettingsBeanValidationTest` flips green (8 methods including the new `taskFileMaxFileSizeBytesBelowFloorRejected`).
- Existing tests still pass (no regression).
- Boot-time Liquibase migration applies cleanly (codex MEDIUM fix — direct boot assertion).
</success_criteria>

<output>
After completion, create `.planning/phases/16-admin-settings-model-picker-config-knob-migration/16-02-SUMMARY.md`.
</output>
