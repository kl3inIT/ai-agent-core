---
phase: 16-admin-settings-model-picker-config-knob-migration
plan: 05
type: execute
wave: 2
depends_on: [01, 03]
files_modified:
  - ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/AiParametersEntityListener.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/AiUiSettingsEntityListener.java
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/view/parameters/parameters-detail-view.xml
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/parameters/ParametersDetailView.java
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties
  - ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/AiSettingsChangedEventListenerInvariantTest.java
autonomous: true
requirements: [MODEL-01, MODEL-03, CFG-03]
requirements_addressed: [MODEL-01, MODEL-03, CFG-03]
tags: [jmix-views, jmix-i18n, event-publication]

must_haves:
  truths:
    - "AiSettingsChangedEvent.Kind enum is consumed by both entity listeners (single publish site per entity — Plan 10-06 R2 invariant)."
    - "AiParametersEntityListener publishes AiSettingsChangedEvent(kind=PARAMETERS) IFF the saved row's active=true (CREATED/UPDATED), or the deleted row's previous active=true."
    - "AiUiSettingsEntityListener publishes AiSettingsChangedEvent(kind=UI_SETTINGS) on every save of the singleton id."
    - "Neither listener saves data; both inject ApplicationEventPublisher only — no DataManager mutation."
    - "modelField in parameters-detail-view.xml is now <comboBox allowCustomValue=\"true\">; populated with ChatModelCatalog.entries() ids; label generator appends `(default)` for the default-marked entry; custom-entered values persist verbatim via CustomValueSetEvent."
    - "AiSettingsChangedEventListenerInvariantTest flips green: active save fires once, inactive save fires zero, UI-settings save fires once, only the 2 listener classes reference the publish path (source-scan)."
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/AiParametersEntityListener.java"
      provides: "Single publish site for kind=PARAMETERS (active=true guard)"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/AiUiSettingsEntityListener.java"
      provides: "Single publish site for kind=UI_SETTINGS (every save)"
    - path: "ai-agent/ai-agent/src/main/resources/com/vn/agent/view/parameters/parameters-detail-view.xml"
      provides: "modelField as <comboBox allowCustomValue>"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/view/parameters/ParametersDetailView.java"
      provides: "modelField setItems + setItemLabelGenerator + onCustomValueSet wire"
  key_links:
    - from: "Plan 06 KnobInventoryScanner.@EventListener"
      to: "AiSettingsChangedEvent"
      via: "Spring event bus"
      pattern: "@EventListener.*AiSettingsChangedEvent"
    - from: "Plan 07 DefaultChatServiceImpl reissue path"
      to: "ChatModelCatalog (for label resolution in fallback notification)"
      via: "indirect (via parametersResolver / Messages)"
      pattern: "not direct — fallback uses defaults.model() only"
---

<objective>
Ship the model-picker UI swap (textField → `<comboBox allowCustomValue>` per D-06 + RESEARCH Pattern 4) AND the two entity listeners that publish `AiSettingsChangedEvent` per D-04. These two pieces ship together because they are both "consumer-facing wires" for the schema and event types locked in Plans 01/02/03 — and because flipping `AiSettingsChangedEventListenerInvariantTest` green requires both listeners to exist.

Purpose: MODEL-01 + MODEL-03 (admin picks model from curated catalog or types custom) and the publish half of CFG-03 (settings-change event ready for Phase 18 cache eviction).

Output: 2 entity listeners, modelField as ComboBox with custom-value support, locale keys for the new model-picker labels, AiSettingsChangedEventListenerInvariantTest flipped green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-admin-settings-model-picker-config-knob-migration/16-SPEC.md
@.planning/phases/16-admin-settings-model-picker-config-knob-migration/16-CONTEXT.md
@.planning/phases/16-admin-settings-model-picker-config-knob-migration/16-RESEARCH.md
@.planning/phases/16-admin-settings-model-picker-config-knob-migration/16-PATTERNS.md
@ai-agent/ai-agent/src/main/java/com/vn/agent/exposure/AiExposureRuleEntityListener.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/exposure/LlmExposureChangedEvent.java
@ai-agent/ai-agent/src/main/resources/com/vn/agent/view/parameters/parameters-detail-view.xml
@ai-agent/ai-agent/src/main/java/com/vn/agent/view/parameters/ParametersDetailView.java

<interfaces>
From `AiExposureRuleEntityListener.java` (Pattern A — entire class is the verbatim shape to copy):
```java
@Component
public class AiExposureRuleEntityListener {
    private final ApplicationEventPublisher eventPublisher;
    public AiExposureRuleEntityListener(ApplicationEventPublisher eventPublisher) {
        this.eventPublisher = eventPublisher;
    }
    @EventListener
    public void onExposureRuleChanged(EntityChangedEvent<AiExposureRule> event) {
        eventPublisher.publishEvent(new LlmExposureChangedEvent(this));
    }
}
```

From Plan 01: `AiSettingsChangedEvent(Object source, Kind kind)` with `Kind { PARAMETERS, UI_SETTINGS }`.

From `parameters-detail-view.xml` lines 25-28 (CURRENT shape):
```xml
<textField id="modelField"
           label="msg:///parametersDetail.field.model"
           required="true"
           requiredMessage="msg:///parametersDetail.validation.modelRequired"/>
```

From `ParametersDetailView.java` lines 138-141 (existing value-change wire):
```java
@Subscribe("modelField")
public void onModelFieldChange(final AbstractField.ComponentValueChangeEvent<?, ?> event) {
    refreshYamlPreview();
}
```

ComboBox + custom-value contract (RESEARCH Pattern 4 / D-06):
- `setItems(catalog.entries().stream().map(Entry::id).toList())`
- `setItemLabelGenerator(id -> { entry=findById(id); return entry==null ? id : (entry.isDefault() ? label+" (default suffix)" : label); })`
- `@Subscribe("modelField") onCustomValueSet(ComboBoxBase.CustomValueSetEvent<...>) { modelField.setValue(event.getDetail()); }`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create AiParametersEntityListener + AiUiSettingsEntityListener (single publish sites per entity)</name>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/AiParametersEntityListener.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/AiUiSettingsEntityListener.java
  </files>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/exposure/AiExposureRuleEntityListener.java (Pattern A — verbatim 35-line shape; the new UI-settings listener is a 1:1 copy with class/event substitutions)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/AiSettingsChangedEvent.java (Plan 01 — confirm Kind enum constants)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiParameters.java (to find `getActive()` getter signature; nullable Boolean per existing schema)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiUiSettings.java (singleton id constant `SINGLETON_ID`)
    - .planning/phases/16-admin-settings-model-picker-config-knob-migration/16-RESEARCH.md `### 4. Jmix EntityChangedEvent entity listener pattern (D-04)` (the active-guard pattern with getOldValue/getChanges)
    - .planning/phases/16-admin-settings-model-picker-config-knob-migration/16-PATTERNS.md Pattern A
  </read_first>
  <action>
    File 1 — `AiUiSettingsEntityListener.java` in `com.vn.agent.admin.config`:
    - Verbatim copy of `AiExposureRuleEntityListener.java` shape with substitutions:
      - Class name → `AiUiSettingsEntityListener`
      - `EntityChangedEvent<AiExposureRule>` → `EntityChangedEvent<AiUiSettings>`
      - Method name → `onAiUiSettingsChanged`
      - Publish payload → `new AiSettingsChangedEvent(this, AiSettingsChangedEvent.Kind.UI_SETTINGS)`
    - No guard — every save of the singleton fires the event (D-04 — singleton id is sealed; admin-edit is the only write path).
    - Class-level Javadoc (load-bearing for SEC-08): `Single publish site for AiSettingsChangedEvent(kind=UI_SETTINGS) — views/services NEVER inject ApplicationEventPublisher for this event (Plan 10-06 R2 invariant + SecretRedactionInvariantsTest enforces).`

    File 2 — `AiParametersEntityListener.java` in `com.vn.agent.admin.config`:
    - Same shape PLUS the active-guard pattern (RESEARCH §4).
    - Inject `ApplicationEventPublisher eventPublisher` AND `DataManager dataManager` (via constructor — needed to re-read the row for CREATED/UPDATED active check).
    - Method `onAiParametersChanged(EntityChangedEvent<AiParameters> event)`:
      - If `event.getType() == EntityChangedEvent.Type.DELETED`: read `Boolean oldActive = event.getChanges().getOldValue("active")`; publish IFF `Boolean.TRUE.equals(oldActive)`.
      - Else (CREATED/UPDATED): `AiParameters current = dataManager.load(AiParameters.class).id(event.getEntityId()).optional().orElse(null)`; publish IFF `current != null && Boolean.TRUE.equals(current.getActive())`.
      - Publish payload: `new AiSettingsChangedEvent(this, AiSettingsChangedEvent.Kind.PARAMETERS)`.
    - Class-level Javadoc same form as the UI-settings listener.

    Both listeners are pure publishers — NO DataManager save calls, NO downstream service references, NO Vaadin imports. They live in `com.vn.agent.admin.config` (D-04 + D-02 package).
  </action>
  <verify>
    <automated>cd ai-agent && ./gradlew :ai-agent:compileJava 2>&1 | grep -E "BUILD SUCCESSFUL|error:"</automated>
    <source>grep -c "publishEvent\\(new AiSettingsChangedEvent" ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/AiParametersEntityListener.java outputs 1.</source>
    <source>grep -c "publishEvent\\(new AiSettingsChangedEvent" ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/AiUiSettingsEntityListener.java outputs 1.</source>
    <source>grep -v '^#' ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/AiParametersEntityListener.java | grep -c "dataManager\\.save\\|dataManager.remove" outputs 0 (listener does NOT mutate data).</source>
    <source>grep -E "Kind\\.PARAMETERS" ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/AiParametersEntityListener.java outputs ≥ 1.</source>
    <source>grep -E "Kind\\.UI_SETTINGS" ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/AiUiSettingsEntityListener.java outputs ≥ 1.</source>
  </verify>
  <done>
    Both listeners compile. Each is the sole publisher of `AiSettingsChangedEvent` with its respective Kind. Active-guard logic in AiParametersEntityListener handles all three EntityChangedEvent.Type values (CREATED/UPDATED/DELETED).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Swap modelField textField → ComboBox + wire setItems/labelGenerator/CustomValueSetEvent + add D-09 model-picker locale keys</name>
  <files>
    ai-agent/ai-agent/src/main/resources/com/vn/agent/view/parameters/parameters-detail-view.xml,
    ai-agent/ai-agent/src/main/java/com/vn/agent/view/parameters/ParametersDetailView.java,
    ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties,
    ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties
  </files>
  <read_first>
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/view/parameters/parameters-detail-view.xml (entire file — see current `<textField id="modelField"/>` at lines 25-28 and surrounding fields)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/parameters/ParametersDetailView.java (entire file — read current `@ViewComponent private TypedTextField<String> modelField;` declaration and the existing `@Subscribe("modelField") onModelFieldChange` at lines 138-141 and `onInit` block at lines 112-123 to understand how `enabledToolsField.setItems(...)` is wired today; also find the `bodyYaml` write path that the controller uses on save — likely an `onBeforeSave` that composes the YAML from in-memory fields)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/ChatModelCatalog.java (just-shipped catalog — read `entries()`, `findById()`, `defaultEntry()` signatures)
    - .planning/phases/16-admin-settings-model-picker-config-knob-migration/16-RESEARCH.md `### 5. Jmix Flow UI <comboBox allowCustomValue=true>` + `### Pattern 4: ComboBox with custom-value support` (verbatim XML + Java)
    - .planning/phases/16-admin-settings-model-picker-config-knob-migration/16-PATTERNS.md `### parameters-detail-view.xml` and `### ParametersDetailView.java`
    - .planning/phases/16-admin-settings-model-picker-config-knob-migration/16-CONTEXT.md D-09 (model-picker key list)
  </read_first>
  <action>
    Step A — XML: in `parameters-detail-view.xml` replace the `<textField id="modelField" ...>` element (currently lines 25-28) with:
    ```xml
    <comboBox id="modelField"
              label="msg:///parametersDetail.field.model"
              allowCustomValue="true"
              required="true"
              requiredMessage="msg:///parametersDetail.validation.modelRequired"
              helperText="msg:///parametersDetail.modelField.customValueHint"/>
    ```
    Do NOT add `property=` (model lives inside `bodyYaml`, controller composes on save — preserve existing pattern). Do NOT alter neighbouring fields. Verify diff is contained to the 3-4 line element swap.

    Step B — Java controller `ParametersDetailView.java`:
    - Change the field declaration from `private TypedTextField<String> modelField;` to `private JmixComboBox<String> modelField;` (import: `io.jmix.flowui.component.combobox.JmixComboBox`). Keep `@ViewComponent` annotation.
    - Inject `ChatModelCatalog catalog` via field with `@Autowired` (CLAUDE.md rule: `@Autowired` for Spring beans in views — Messages is already injected the same way).
    - In `onInit(InitEvent event)` add (after existing `enabledToolsField.setItems(...)`):
      ```
      List<String> catalogIds = catalog.entries().stream().map(ChatModelCatalog.Entry::id).toList();
      modelField.setItems(catalogIds);
      modelField.setItemLabelGenerator(id -> {
          ChatModelCatalog.Entry entry = catalog.findById(id);
          if (entry == null) return id;
          String label = messages.getMessage(entry.labelMessageKey());
          return entry.isDefault()
                  ? label + " " + messages.getMessage("parametersDetail.modelField.defaultSuffix")
                  : label;
      });
      ```
    - Add `@Subscribe("modelField")` method `onModelFieldCustomValueSet(ComboBoxBase.CustomValueSetEvent<ComboBox<String>> event) { modelField.setValue(event.getDetail()); }` (RESEARCH Pattern 4 — verbatim).
    - Keep the existing `onModelFieldChange(ComponentValueChangeEvent)` method untouched (`refreshYamlPreview()` continues to fire on both catalog selection and `setValue` after custom-value-set).
    - On save (existing `onBeforeSave` or analog YAML-write path — scout to confirm): no change required IF the existing path reads `modelField.getValue()` into `bodyYaml.model` (the getter returns the same String for catalog and custom values). If the existing path reads via `modelField.getTypedValue()` or another `TypedTextField`-specific API, replace with `modelField.getValue()`.

    Step C — locale keys: append to BOTH `messages_en.properties` AND `messages_vi.properties` (root bundle, identical key set per D-09):
    - `parametersDetail.modelField.customValueHint` (en: "Pick a model from the curated catalog, or type any custom model id." / vi: "Chọn một mô hình từ danh mục được tuyển chọn, hoặc nhập id mô hình tùy ý.")
    - `parametersDetail.modelField.defaultSuffix` (en: "(default)" / vi: "(mặc định)")
    - `chatModelCatalog.qwen3_35b` (en: "Qwen3.6 35B A3B" / vi: "Qwen3.6 35B A3B")
    - `chatModelCatalog.llama3_3_70b` (en: "Llama 3.3 70B Instruct" / vi: "Llama 3.3 70B Instruct")
    - `chatModelCatalog.mistral_small_3_1` (en: "Mistral Small 3.1 24B" / vi: "Mistral Small 3.1 24B")
    - `chatModelCatalog.deepseek_v3_1` (en: "DeepSeek V3.1" / vi: "DeepSeek V3.1")

    Model names are proper nouns — Vietnamese values are identical to English except for `defaultSuffix` and `customValueHint` body text.
  </action>
  <verify>
    <automated>cd ai-agent && ./gradlew :ai-agent:compileJava 2>&1 | grep -E "BUILD SUCCESSFUL|error:"</automated>
    <source>grep -c "<comboBox id=\"modelField\"" ai-agent/ai-agent/src/main/resources/com/vn/agent/view/parameters/parameters-detail-view.xml outputs 1.</source>
    <source>grep -c "allowCustomValue=\"true\"" ai-agent/ai-agent/src/main/resources/com/vn/agent/view/parameters/parameters-detail-view.xml outputs 1.</source>
    <source>grep -c "<textField id=\"modelField\"" ai-agent/ai-agent/src/main/resources/com/vn/agent/view/parameters/parameters-detail-view.xml outputs 0 (old element fully removed).</source>
    <source>grep -c "JmixComboBox<String> modelField\\|JmixComboBox modelField" ai-agent/ai-agent/src/main/java/com/vn/agent/view/parameters/ParametersDetailView.java outputs ≥ 1.</source>
    <source>grep -c "setItemLabelGenerator" ai-agent/ai-agent/src/main/java/com/vn/agent/view/parameters/ParametersDetailView.java outputs ≥ 1.</source>
    <source>grep -c "CustomValueSetEvent" ai-agent/ai-agent/src/main/java/com/vn/agent/view/parameters/ParametersDetailView.java outputs ≥ 1.</source>
    <source>grep -c "^parametersDetail\\.modelField\\.defaultSuffix\\|^parametersDetail\\.modelField\\.customValueHint\\|^chatModelCatalog\\." ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties outputs ≥ 6.</source>
    <source>grep -c "^parametersDetail\\.modelField\\.defaultSuffix\\|^parametersDetail\\.modelField\\.customValueHint\\|^chatModelCatalog\\." ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties outputs ≥ 6.</source>
    <automated>cd ai-agent && ./gradlew :ai-agent:test --tests "*Parameters*" 2>&1 | grep -E "BUILD SUCCESSFUL|FAILED" (any existing parameters tests pass)</automated>
  </verify>
  <done>
    XML element swapped to ComboBox with allowCustomValue. Controller declares `JmixComboBox<String>` and wires `setItems` + `setItemLabelGenerator` + `CustomValueSetEvent` handler. Six new locale keys present in both bundles. Existing parameters test suite (if any) still passes. Manual verification at http://localhost:8088 deferred to phase-level UAT.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Flip AiSettingsChangedEventListenerInvariantTest green</name>
  <files>ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/AiSettingsChangedEventListenerInvariantTest.java</files>
  <behavior>
    - `activeParametersSavePublishesPARAMETERS()`: save `AiParameters{active=true}`, capture `AiSettingsChangedEvent`s via `@RecordApplicationEvents`/`ApplicationEvents`, assert exactly 1 event with `getKind() == PARAMETERS`.
    - `inactiveParametersSavePublishesZero()`: save `AiParameters{active=false}`, assert 0 events captured.
    - `inactiveDeletedRowOldActiveTrueStillPublishes()`: load an existing active row, delete it, assert 1 event published (active-was-true path).
    - `uiSettingsSavePublishesUI_SETTINGS()`: load `AiUiSettings.SINGLETON_ID`, update a column, save, assert exactly 1 event with `getKind() == UI_SETTINGS`.
    - `singlePublishSiteSourceScan()`: walk `ai-agent/ai-agent/src/main/java` looking for the regex `publishEvent\\s*\\(\\s*new\\s+AiSettingsChangedEvent`. Assert the file set is exactly `{AiParametersEntityListener.java, AiUiSettingsEntityListener.java}`. Fail with the offending file path otherwise (Plan 10-06 R2 single-publish-site invariant).
  </behavior>
  <read_first>
    - ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/AiSettingsChangedEventListenerInvariantTest.java (Plan 01 scaffold — currently fail())
    - ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/TtlConfigTest.java (Pattern I — boot stack)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/AiParametersEntityListener.java + AiUiSettingsEntityListener.java (just-shipped — confirm publish path)
    - .planning/phases/16-admin-settings-model-picker-config-knob-migration/16-RESEARCH.md `### Wave 0 Gaps` AiSettingsChangedEventPublicationTest
  </read_first>
  <action>
    Replace all `fail("Wave 0 scaffold")` bodies with real assertions per `<behavior>`. Boot stack mirrors `TtlConfigTest` (Pattern I). Use Spring's `@RecordApplicationEvents` on the test class and inject `ApplicationEvents events` per JUnit 5 Spring extension.

    Method bodies:
    - `activeParametersSavePublishesPARAMETERS()`: load/build an `AiParameters` instance via `unconstrainedDataManager.create(AiParameters.class)`, `setActive(true)`, save; assert `events.stream(AiSettingsChangedEvent.class).filter(e -> e.getKind() == Kind.PARAMETERS).count() == 1L`. Cleanup: delete the test row in `@AfterEach`.
    - `inactiveParametersSavePublishesZero()`: same setup with `setActive(false)`; assert count == 0.
    - `inactiveDeletedRowOldActiveTrueStillPublishes()`: pre-seed an active row in `@BeforeEach`, then `dataManager.remove(row)`; assert count == 1.
    - `uiSettingsSavePublishesUI_SETTINGS()`: load `AiUiSettings.SINGLETON_ID` via UnconstrainedDataManager, mutate a column (e.g., set `taskFileTtlSeconds = 3600L`), save; assert exactly 1 event with `Kind.UI_SETTINGS`.
    - `singlePublishSiteSourceScan()`: use `java.nio.file.Files.walk(Path.of("../ai-agent/src/main/java"))` (or resolve relative to the test working dir), collect `.java` files containing the regex `publishEvent\\s*\\(\\s*new\\s+AiSettingsChangedEvent`, assert the resulting set equals `Set.of("AiParametersEntityListener.java", "AiUiSettingsEntityListener.java")` (compare by simple file name). Fail with a clear diff message if mismatched.

    Class-level Javadoc: `Enforces Plan 10-06 R2 single-publish-site invariant for AiSettingsChangedEvent. Plans 02-07 MUST NOT inject ApplicationEventPublisher for this event in views/services.`
  </action>
  <verify>
    <automated>cd ai-agent && ./gradlew :ai-agent:test --tests "com.vn.agent.admin.config.AiSettingsChangedEventListenerInvariantTest" 2>&1 | grep -E "BUILD SUCCESSFUL|FAILED"</automated>
    <source>grep -v '^#' ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/AiSettingsChangedEventListenerInvariantTest.java | grep -c "fail\\(" outputs 0.</source>
  </verify>
  <done>
    Test exits green with all 5 methods passing. The source-scan method specifically asserts that NO other file in `ai-agent/src/main/java` references the publish path — a future plan trying to inject `ApplicationEventPublisher` for `AiSettingsChangedEvent` in a view or service will fail this test at build time.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin ComboBox value → `AiParameters.bodyYaml.model` (controller-composed YAML on save) | The ComboBox value (catalog id OR custom string) is admin-input only (admin-only view); persisted verbatim. No SSRF surface — the value flows to Spring AI's `ChatOptions.model(...)` which routes through OpenRouter's slug-validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | DoS | Bad-model reissue loop | mitigate (Plan 07) | The catch+reissue path (Plan 07) uses `defaults.model()`, NOT re-resolving via the active profile. Plan 05's UI swap does not change the call path — Plan 07 owns the loop guard. |
| T-16-05 | Elevation of Privilege | ParametersDetailView | mitigate | View is admin-only (existing `AiAgentAdminRole.@ViewPolicy`). End users have no surface to navigate here. SPEC criterion 3. |
| T-16-04 | Information Disclosure | AiSettingsChangedEvent payload | accept | Event carries only `Kind` enum (no PII / no model id). Plan 18 cache eviction reads `getKind()` only. |
| Twin-publisher (R2) | Tampering | Settings-change event source-scan | mitigate | `singlePublishSiteSourceScan()` test fails the build if ANY view/service injects `ApplicationEventPublisher` for `AiSettingsChangedEvent`. |
</threat_model>

<verification>
- `./gradlew :ai-agent:test --tests "com.vn.agent.admin.config.AiSettingsChangedEventListenerInvariantTest"` exits 0.
- `./gradlew :ai-agent:test --tests "*Parameters*"` passes (any existing parameters tests).
- Locale-parity check (`diff <(grep -oE "^(parametersDetail\\.modelField|chatModelCatalog)\\." en) <(grep -oE "^(parametersDetail\\.modelField|chatModelCatalog)\\." vi)`) returns empty.
- `git diff` on `parameters-detail-view.xml` shows ONLY the modelField element swap (no other elements touched).
</verification>

<success_criteria>
- AiParametersEntityListener publishes Kind.PARAMETERS only when active=true at save time (or was-active for deletes).
- AiUiSettingsEntityListener publishes Kind.UI_SETTINGS on every singleton save.
- modelField is `<comboBox allowCustomValue>` with catalog items + (default) suffix + custom-value persistence.
- 6 new locale keys in both bundles.
- AiSettingsChangedEventListenerInvariantTest green (5 methods including single-publish-site source scan).
</success_criteria>

<output>
After completion, create `.planning/phases/16-admin-settings-model-picker-config-knob-migration/16-05-SUMMARY.md`.
</output>
