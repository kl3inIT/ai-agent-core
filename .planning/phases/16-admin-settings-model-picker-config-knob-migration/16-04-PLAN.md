---
phase: 16-admin-settings-model-picker-config-knob-migration
plan: 04
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/AiUiSettingsResolver.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/taskfile/AiTaskFileCleanupJob.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/taskfile/AiTaskFileMediaResolver.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/tools/mutation/BuiltInMutationTools.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/tools/mutation/MutationIntentRepository.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/tools/mutation/MutationSaveExecutor.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/BaselineContextProvider.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/tools/BuiltInDataTools.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/tools/ToolEntityResolver.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/conversation/AiConversationTitleService.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/rag/KnowledgeDocumentUploadService.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/AiUiSettingsResolverReadThroughTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/TtlConfigSentinelSurvivesAiUiSettingsTest.java
autonomous: true
requirements: [CFG-01]
requirements_addressed: [CFG-01]
tags: [jmix-services, orchestration]

must_haves:
  truths:
    - "AiUiSettingsResolver @Component exposes 12 typed `resolveXxx()` methods mirroring AiParametersResolver shape (D-03 caller list + codex HIGH Concern #5 split: resolveTaskFileMaxFileSizeBytes for chat task-file upload and resolveRagUploadMaxFileSizeBytes for KB/RAG upload are DISTINCT methods)."
    - "Each resolveXxx() loads AiUiSettings.SINGLETON_ID via UnconstrainedDataManager (Pitfall 3 — non-admin chat turns must succeed), returns column value if non-null, otherwise returns the @ConfigurationProperties resolved value (DB → props → constant chain)."
    - "Resolver catches RuntimeException on the singleton load and logs at WARN, returning the property fallback so a transient DB hiccup never breaks a chat turn (Pattern C)."
    - "10 caller sites swap their direct `props.resolvedXxx()` calls for `aiUiSettingsResolver.resolveXxx()` (fully additive — properties stay as the fallback). EXACT caller census locked at execution time before edits: BuiltInDataTools AND ToolEntityResolver are BOTH inspected (codex MEDIUM Concern #5 — replace 'and/or' with the exact file list)."
    - "AiUiSettingsResolverReadThroughTest flips green (per-cluster fall-through: DB → properties → constant)."
    - "TtlConfigSentinelSurvivesAiUiSettingsTest flips green: -1 sentinel set via AiUiSettings.taskFileTtlSeconds disables cleanup identically to the property-source path (Phase 13.1 invariant preserved)."
    - "MutationIntentRepository idempotency-TTL coercion is verified — resolver returns long seconds; the caller's existing comparison logic (likely `Duration.toMillis()` or `System.currentTimeMillis()` arithmetic) is read FIRST, then coerced via `Duration.ofSeconds(seconds)` on the consumption side with a code comment per opencode MEDIUM Concern #4."
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/AiUiSettingsResolver.java"
      provides: "12 typed resolveXxx() methods + UnconstrainedDataManager-based singleton load (split per codex HIGH Concern #5: resolveTaskFileMaxFileSizeBytes vs resolveRagUploadMaxFileSizeBytes)"
  key_links:
    - from: "AiTaskFileCleanupJob"
      to: "AiUiSettingsResolver.resolveTaskFileTtl()"
      via: "constructor injection"
      pattern: "aiUiSettingsResolver\\.resolveTaskFileTtl"
    - from: "BuiltInMutationTools / MutationIntentRepository / MutationSaveExecutor"
      to: "AiUiSettingsResolver.resolveMutationBulkMaxRows() etc."
      via: "constructor injection"
      pattern: "aiUiSettingsResolver\\.resolveMutation"
---

<objective>
Build the sibling `AiUiSettingsResolver` per D-03 (mirroring `AiParametersResolver` shape, loading the singleton via `UnconstrainedDataManager`, exposing 12 typed `resolveXxx()` methods that return DB-column → @ConfigurationProperties default — TASK_FILE_MAX_FILE_SIZE_BYTES is its own resolver method, distinct from UPLOAD_MAX_FILE_SIZE_BYTES per codex HIGH Concern #5), wire the 10 caller sites listed in CONTEXT D-03 to consult the resolver instead of the property beans directly (additive — properties beans stay as the fallback layer inside the resolver), pre-scout the `MutationIntentRepository` Duration coercion site per opencode MEDIUM Concern #4 BEFORE editing, and flip the read-through fall-through test + the Phase 13.1 sentinel-survives test green.

Purpose: This is the runtime half of CFG-01. Plan 02 added the columns; this plan makes them reachable per-turn without restart. The 10 caller swaps are mechanical and zero-behavior-change when the DB column is null (resolver returns the property default which is the original value). The Concern #5 split prevents a real-runtime CFG-01 violation where chat task-file upload limits and RAG upload limits would have collapsed into a single knob.

Output: 1 new resolver (12 methods), 10 caller injections + call-site swaps (including disambiguated chat-task-file vs RAG-upload paths), 2 tests flipped green + 1 new resolver-test for the second upload-resolver method.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-admin-settings-model-picker-config-knob-migration/16-SPEC.md
@.planning/phases/16-admin-settings-model-picker-config-knob-migration/16-CONTEXT.md
@.planning/phases/16-admin-settings-model-picker-config-knob-migration/16-RESEARCH.md
@.planning/phases/16-admin-settings-model-picker-config-knob-migration/16-PATTERNS.md
@ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/AiParametersResolver.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/AiUiSettingsService.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiUiSettings.java

<interfaces>
From `AiParametersResolver.java` (the resolver shape to mirror):
- `@Component` with `@Slf4j`
- Constructor injects `DataManager` + `Metadata` + `AiAgentDefaultsProperties`
- `resolveActive()` wraps DB query in try/catch RuntimeException; on failure logs WARN + returns `buildFallback()`
- `effectiveTopK(params)` style: read body value, null/<=0 → defaultValue

From `AiUiSettingsService.loadCurrent()` (Pitfall 3 — Pattern B): use `UnconstrainedDataManager` + `.id(AiUiSettings.SINGLETON_ID).optional().orElseGet(this::createDefaultSettings)`.

New columns (from Plan 02) — accessors:
- `getTaskFileTtlSeconds() : Long`
- `getTaskFilePerTurnMaxFiles() : Integer`
- `getTaskFilePerTurnMaxTotalBytes() : Long`
- `getTaskFileMaxFileSizeBytes() : Long` (NEW per codex HIGH Concern #4 — chat task-file per-file upload cap)
- `getMutationConfirmationRequired() : Boolean`
- `getMutationIdempotencyTtlSeconds() : Long`
- `getMutationBulkMaxRows() : Integer`
- `getPromptEntityInventoryLimit() : Integer`
- `getToolsMaxFilterDepth() : Integer`
- `getTitleMaxContextMessages() : Integer`
- `getTitleMinAssistantMessagesTrigger() : Integer`
- `getUploadMaxFileSizeBytes() : Long` (RAG/KB upload cap)

Property fallback beans (already on classpath — verify exact `resolved*()` method names per file):
- `AiTaskFileProperties` (ttl-seconds, per-turn-max-files, per-turn-max-total-bytes, max-file-size-bytes — the LAST property is the chat task-file upload cap, per codex HIGH Concern #4)
- `AiAgentMutationProperties` (confirmation-required, idempotency-ttl, bulk-max-rows)
- `AiAgentPromptProperties` (entity-inventory.limit)
- `AiAgentToolsProperties` OR similar (max-filter-depth — scout for the exact bean name)
- `AiAgentTitleProperties` (max-context-messages, min-assistant-messages-trigger)
- `AiAgentRagProperties.upload()` nested record (max-file-size-bytes — the KB/RAG upload cap, distinct knob from AiTaskFileProperties.maxFileSizeBytes per codex HIGH Concern #5)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create AiUiSettingsResolver with 11 typed resolveXxx() methods (DB→props→constant fallback)</name>
  <files>ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/AiUiSettingsResolver.java</files>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/AiParametersResolver.java (Pattern C — resolveActive() resilience + effectiveXxx() defaulting shape; lines 49-185)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/AiUiSettingsService.java (Pattern B — UnconstrainedDataManager singleton-load with `.optional().orElseGet(...)`; lines 23-44)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiUiSettings.java (post-Plan-02 — read the new getters for boxed Long/Integer/Boolean return types)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/taskfile/AiTaskFileProperties.java (find exact `resolvedTtlSeconds()`/`resolvedPerTurnMaxFiles()` method names — confirm during read)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/tools/mutation/AiAgentMutationProperties.java (find exact resolvedXxx() names)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/prompt/AiAgentPromptProperties.java OR equivalent (entity-inventory limit)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/conversation/AiAgentTitleProperties.java (title knob property bean)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/rag/config/AiAgentRagProperties.java (upload.maxFileSizeBytes)
    - .planning/phases/16-admin-settings-model-picker-config-knob-migration/16-PATTERNS.md Pattern B + Pattern C
  </read_first>
  <action>
    Create `AiUiSettingsResolver.java` in package `com.vn.agent.orchestration` (same package as `AiParametersResolver` — D-03 sibling rule). Class shape:

    - `@Component` + `@Slf4j` (mirror `AiParametersResolver` declaration block lines 38-46).
    - Constructor injects (final fields):
      - `UnconstrainedDataManager unconstrainedDataManager` (Pitfall 3 — system-internal read path; non-admin chat turn must succeed).
      - `AiTaskFileProperties taskFileProperties` (3 fallback getters)
      - `AiAgentMutationProperties mutationProperties` (3 fallback getters)
      - `AiAgentPromptProperties promptProperties` (1 fallback getter)
      - `AiAgentTitleProperties titleProperties` (2 fallback getters)
      - `AiAgentRagProperties ragProperties` (1 fallback getter via `.upload().resolvedMaxFileSizeBytes()` or equivalent — scout exact accessor)
      - The bean owning `max-filter-depth` (likely `AiAgentToolsProperties` — scout)
    - Private `AiUiSettings loadSingleton()` helper:
      - Wrap `unconstrainedDataManager.load(AiUiSettings.class).id(AiUiSettings.SINGLETON_ID).optional().orElse(null)` in try/catch `RuntimeException` (Pattern C). On exception, log WARN with message and return `null` (callers fall through to property defaults).
    - 11 public `resolveXxx()` methods, one per Tier-1 column. Each method follows the exact pattern:
      ```
      public long resolveTaskFileTtlSeconds() {
          AiUiSettings settings = loadSingleton();
          if (settings != null && settings.getTaskFileTtlSeconds() != null) {
              return settings.getTaskFileTtlSeconds();
          }
          return taskFileProperties.resolvedTtlSeconds(); // SCOUT exact method name
      }
      ```
      The return type matches the consuming caller's expectation — primitive `long`/`int`/`boolean` where the column is nullable Long/Integer/Boolean, because the resolver itself never returns null (DB null = property fallback). Sentinel -1 passes through verbatim.

    Method signatures (return type → method name → fallback path) — 12 methods total, codex HIGH Concern #5 split:
    - `long resolveTaskFileTtlSeconds()` → `taskFileProperties.resolvedTtlSeconds()` (sentinel -1 honored)
    - `int resolveTaskFilePerTurnMaxFiles()` → `taskFileProperties.resolvedPerTurnMaxFiles()` (sentinel -1 honored)
    - `long resolveTaskFilePerTurnMaxTotalBytes()` → `taskFileProperties.resolvedPerTurnMaxTotalBytes()` (sentinel -1 honored)
    - `long resolveTaskFileMaxFileSizeBytes()` → `taskFileProperties.resolvedMaxFileSizeBytes()` (chat task-file per-file upload cap — distinct knob, codex HIGH Concern #4+#5; consumed by ChatPanelFragment task-file dropzone)
    - `boolean resolveMutationConfirmationRequired()` → `mutationProperties.resolvedConfirmationRequired()`
    - `long resolveMutationIdempotencyTtlSeconds()` → `mutationProperties.resolvedIdempotencyTtlSeconds()` (convert from `Duration` if the property carries `Duration` — coerce to seconds inside the resolver per D-01 entity-side note: stored as seconds to avoid JPA Duration mapping. See Task 2 step note re: MutationIntentRepository consumer-side coercion)
    - `int resolveMutationBulkMaxRows()` → `mutationProperties.resolvedBulkMaxRows()`
    - `int resolvePromptEntityInventoryLimit()` → `promptProperties.resolvedEntityInventoryLimit()`
    - `int resolveToolsMaxFilterDepth()` → `toolsProperties.resolvedMaxFilterDepth()` (scout for actual bean)
    - `int resolveTitleMaxContextMessages()` → `titleProperties.resolvedMaxContextMessages()`
    - `int resolveTitleMinAssistantMessagesTrigger()` → `titleProperties.resolvedMinAssistantMessagesTrigger()`
    - `long resolveRagUploadMaxFileSizeBytes()` → `ragProperties.upload().resolvedMaxFileSizeBytes()` (KB/RAG upload cap; consumed by KnowledgeDocumentUploadService. Renamed from `resolveUploadMaxFileSizeBytes` per codex HIGH Concern #5 to make the disambiguation explicit at every call site.)

    Class-level Javadoc (load-bearing for SEC-08 source-scan in Plan 06): "Sibling resolver to `AiParametersResolver` for operator-runtime knobs persisted on `AiUiSettings` (D-03). Read fresh per turn — Phase 18 memoization slot lives in its own future class subscribing to `AiSettingsChangedEvent(kind=UI_SETTINGS)`. NEVER inject `ApplicationEventPublisher` here — only `AiUiSettingsEntityListener` publishes (Plan 10-06 R2 invariant)."
  </action>
  <verify>
    <automated>cd ai-agent && ./gradlew :ai-agent:compileJava 2>&1 | grep -E "BUILD SUCCESSFUL|error:"</automated>
    <source>grep -c "public .* resolve\\(TaskFile\\|Mutation\\|Prompt\\|Tools\\|Title\\|Upload\\)" ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/AiUiSettingsResolver.java outputs 11.</source>
    <source>grep -c "unconstrainedDataManager" ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/AiUiSettingsResolver.java outputs ≥ 2 (constructor + loadSingleton).</source>
    <source>grep -v '^#' ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/AiUiSettingsResolver.java | grep -c "ApplicationEventPublisher" outputs 0 (Plan 10-06 R2 invariant — resolver does NOT publish events).</source>
  </verify>
  <done>
    `AiUiSettingsResolver` compiles. All 11 `resolveXxx()` methods exist with primitive return types. Singleton load goes through `UnconstrainedDataManager` (Pitfall 3). No `ApplicationEventPublisher` injected (R2 invariant). Try/catch wraps the DB load (Pattern C resilience).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Inject AiUiSettingsResolver into 10 caller sites and swap property-bean calls to resolver calls (additive)</name>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/taskfile/AiTaskFileCleanupJob.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/taskfile/AiTaskFileMediaResolver.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/tools/mutation/BuiltInMutationTools.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/tools/mutation/MutationIntentRepository.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/tools/mutation/MutationSaveExecutor.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/BaselineContextProvider.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/tools/BuiltInDataTools.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/tools/ToolEntityResolver.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/conversation/AiConversationTitleService.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/rag/KnowledgeDocumentUploadService.java
  </files>
  <read_first>
    - .planning/phases/16-admin-settings-model-picker-config-knob-migration/16-CONTEXT.md D-03 (the 10-caller list with the exact knob each consumes)
    - .planning/phases/16-admin-settings-model-picker-config-knob-migration/16-RESEARCH.md `### Files to MODIFY (existing)` Caller injection plan
    - .planning/phases/16-admin-settings-model-picker-config-knob-migration/16-PATTERNS.md `### Caller-injection callers (additive — 10 files)` (Pattern: constructor-injection precedent from `AiParametersResolver`)
    - Each of the 10 caller files listed in files_modified — scout for the existing `props.resolvedXxx()` call site that will be swapped
  </read_first>
  <action>
    For each of the 10 caller classes (11 files in `files_modified` — `BuiltInDataTools` and `ToolEntityResolver` may both consume `max-filter-depth`; either both swap or only one does — scout to confirm):

    Step 1: Add `private final AiUiSettingsResolver aiUiSettingsResolver;` field.
    Step 2: Add `AiUiSettingsResolver aiUiSettingsResolver` to the constructor signature (append at the end of the parameter list to minimise diff). Assign in body. Constructor injection only — never `@Autowired` field injection (CLAUDE.md rule).
    Step 3: At the existing call site that reads `taskFileProperties.resolvedTtlSeconds()` (or analog), swap to `aiUiSettingsResolver.resolveTaskFileTtlSeconds()`. Do NOT remove the property bean injection; it stays as a back-stop for any rare code path the resolver doesn't yet cover (and the resolver itself injects the property bean for fall-through).

    PRE-EDIT CALLER CENSUS (codex MEDIUM Concern #5 — replace "and/or" with the exact file list): BEFORE running any caller-site edits, open every file in `files_modified` and locate the EXACT existing call site (`props.resolvedXxx()`-style call) that will be swapped. For `BuiltInDataTools` and `ToolEntityResolver`, the planner's pre-execution scout MUST determine whether one, the other, or both consume `max-filter-depth`. Document the finding in the per-task commit message body. If only one consumes it, the other does NOT get the resolver injection (avoid dead injections).

    PRE-EDIT COERCION CENSUS (opencode MEDIUM Concern #4 — MutationIntentRepository idempotency-TTL coercion): BEFORE editing `MutationIntentRepository`, read the file end-to-end and identify how the current `mutationProperties.resolvedIdempotencyTtl()` (Duration) result is consumed. If it's compared via `.toMillis()` against `System.currentTimeMillis()` arithmetic, the new long-seconds value must be coerced via `Duration.ofSeconds(seconds).toMillis()` (or equivalent) at the comparison site. Write the discovered comparison shape in the commit body BEFORE the swap edit. Add an inline code comment at the coercion site per opencode Suggestion #5: `// Phase 16 D-01: AiUiSettings stores idempotency TTL as seconds (Long column) to avoid JPA Duration mapping. Resolver coerces to Duration for existing comparison logic.`

    Per-file knob mapping (concrete swaps):
    - `AiTaskFileCleanupJob`: swap `taskFileProperties.resolvedTtlSeconds()` → `aiUiSettingsResolver.resolveTaskFileTtlSeconds()`. (Phase 13.1 sentinel -1 contract — resolver passes -1 through verbatim.)
    - `AiTaskFileMediaResolver`: swap per-turn caps to `aiUiSettingsResolver.resolveTaskFilePerTurnMaxFiles()` and `.resolveTaskFilePerTurnMaxTotalBytes()`.
    - `ChatPanelFragment`: swap chat task-file per-file upload cap (`taskFileProperties.resolvedMaxFileSizeBytes()`) → `aiUiSettingsResolver.resolveTaskFileMaxFileSizeBytes()` (codex HIGH Concern #5 — this is the chat task-file dropzone path, NOT the RAG upload path; the two were conflated in the pre-review plan).
    - `BuiltInMutationTools`: swap `mutationProperties.resolvedConfirmationRequired()` → `aiUiSettingsResolver.resolveMutationConfirmationRequired()`.
    - `MutationIntentRepository`: swap `mutationProperties.resolvedIdempotencyTtl()` (Duration) → `aiUiSettingsResolver.resolveMutationIdempotencyTtlSeconds()` (long seconds — coerce on consumption: `Duration.ofSeconds(seconds)`). MUST follow the PRE-EDIT COERCION CENSUS finding above; the inline comment is mandatory.
    - `MutationSaveExecutor`: swap `mutationProperties.resolvedBulkMaxRows()` → `aiUiSettingsResolver.resolveMutationBulkMaxRows()`.
    - `BaselineContextProvider`: swap `promptProperties.resolvedEntityInventoryLimit()` → `aiUiSettingsResolver.resolvePromptEntityInventoryLimit()`.
    - `BuiltInDataTools` AND/OR `ToolEntityResolver` per CALLER CENSUS: swap `toolsProperties.resolvedMaxFilterDepth()` → `aiUiSettingsResolver.resolveToolsMaxFilterDepth()` ONLY in the files identified by the census. Do NOT inject the resolver into a class that doesn't actually consume max-filter-depth.
    - `AiConversationTitleService`: swap `titleProperties.resolvedMaxContextMessages()` and `.resolvedMinAssistantMessagesTrigger()` to the resolver equivalents.
    - `KnowledgeDocumentUploadService`: swap RAG/KB upload size to `aiUiSettingsResolver.resolveRagUploadMaxFileSizeBytes()` (the renamed method — distinct from chat task-file dropzone path).

    Behavior contract: when `AiUiSettings` columns are all null (production state after Plan 02 ships), every resolver call returns the property bean's `resolvedXxx()` value, so behavior is byte-identical to pre-Plan-04. The first edit in the admin UI (Plan 06) is what activates the new code path.
  </action>
  <verify>
    <automated>cd ai-agent && ./gradlew :ai-agent:compileJava 2>&1 | grep -E "BUILD SUCCESSFUL|error:"</automated>
    <automated>cd ai-agent && ./gradlew :ai-agent:test --tests "com.vn.agent.taskfile.*" 2>&1 | grep -E "BUILD SUCCESSFUL|FAILED" (Phase 13.1 task-file tests pass unchanged — null columns fall through to property defaults).</automated>
    <automated>cd ai-agent && ./gradlew :ai-agent:test --tests "com.vn.agent.tools.mutation.*" 2>&1 | grep -E "BUILD SUCCESSFUL|FAILED" (Phase 11 mutation tests pass unchanged).</automated>
    <source>grep -l "aiUiSettingsResolver" ai-agent/ai-agent/src/main/java/com/vn/agent/taskfile/AiTaskFileCleanupJob.java ai-agent/ai-agent/src/main/java/com/vn/agent/taskfile/AiTaskFileMediaResolver.java ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java ai-agent/ai-agent/src/main/java/com/vn/agent/tools/mutation/BuiltInMutationTools.java ai-agent/ai-agent/src/main/java/com/vn/agent/tools/mutation/MutationIntentRepository.java ai-agent/ai-agent/src/main/java/com/vn/agent/tools/mutation/MutationSaveExecutor.java ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/BaselineContextProvider.java ai-agent/ai-agent/src/main/java/com/vn/agent/conversation/AiConversationTitleService.java ai-agent/ai-agent/src/main/java/com/vn/agent/rag/KnowledgeDocumentUploadService.java outputs ≥ 9 of the 10 caller files (one of `BuiltInDataTools`/`ToolEntityResolver` may not consume max-filter-depth).</source>
  </verify>
  <done>
    All 10 callers inject `AiUiSettingsResolver` via constructor and swap their primary property call to the resolver. No `@Autowired` field injection introduced. Phase 11 mutation suite + Phase 13.1 task-file suite + Phase 12 chat-surface suite pass UNCHANGED — proves behavior is byte-identical when DB columns are null (the production state after Plan 02 ships).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Flip AiUiSettingsResolverReadThroughTest + TtlConfigSentinelSurvivesAiUiSettingsTest green</name>
  <files>
    ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/AiUiSettingsResolverReadThroughTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/TtlConfigSentinelSurvivesAiUiSettingsTest.java
  </files>
  <behavior>
    AiUiSettingsResolverReadThroughTest — for each cluster (task-file, mutation, prompt, tools, title, upload):
      - `<cluster>DbWinsOverProperty()`: set column to value X via `unconstrainedDataManager.save(settings)`; assert `aiUiSettingsResolver.resolve<cluster>Xxx() == X`.
      - `<cluster>NullColumnFallsThroughToProperty()`: set column to null; override property via `@TestPropertySource` value Y; assert resolver returns Y.
      - `<cluster>PropertyAbsentFallsThroughToConstant()`: column null + property unset; assert resolver returns the documented default constant (per RESEARCH §8 or the existing resolved*() defaulting in the property bean).

    TtlConfigSentinelSurvivesAiUiSettingsTest:
      - `sentinelMinusOneInUiSettingsRowSkipsCleanup()`: set `aiUiSettings.taskFileTtlSeconds = -1L`, save; insert a task-file row with `expiresAt` in the past; call `cleanupJob.deleteExpiredTaskFiles()` twice; assert the row still exists in the DB.
      - `repositoryDeleteAllExpiredReturnsZeroUnderSentinel()`: under the same -1 column, `repository.deleteAllExpired(OffsetDateTime.now())` returns 0.
      - `mediaResolverReturnsNonEmptyUnderSentinel()`: `resolver.resolveActive(conversationId)` returns non-empty media (Phase 13.1 invariant — sentinel disables active-row expiry filtering).
  </behavior>
  <read_first>
    - ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/AiUiSettingsResolverReadThroughTest.java (Plan 01 scaffold)
    - ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/TtlConfigSentinelSurvivesAiUiSettingsTest.java (Plan 01 scaffold)
    - ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/TtlConfigTest.java (Pattern I — find the existing `TtlConfigSentinelSkipsCleanupTest` sibling class to mirror; the new test uses identical setup but sets the sentinel via AiUiSettings column instead of `@SpringBootTest properties`)
    - .planning/phases/16-admin-settings-model-picker-config-knob-migration/16-RESEARCH.md §9 (`TtlConfigSentinelSkipsCleanupTest` current assertions — the new test mirrors them with the column source)
  </read_first>
  <action>
    Replace all `fail("Wave 0 scaffold")` bodies with real assertions per `<behavior>`. ALSO remove the class-level `@Disabled` and `@Tag("phase-16-scaffold")` annotations (Plan 01 strategy).

    For `AiUiSettingsResolverReadThroughTest`:
    - Use `@SpringBootTest` + `@ImportAutoConfiguration({AIAutoConfiguration.class, SpiDefaultsAutoConfiguration.class})` (Pattern I).
    - Inject `@Autowired UnconstrainedDataManager unconstrainedDataManager; @Autowired AiUiSettingsResolver resolver;`.
    - Each cluster test mutates the singleton row in-place (load → setXxx → save), reads back via the resolver, asserts the expected return value. Use `@DirtiesContext` per method OR reset to null in `@AfterEach` so test isolation holds.
    - Minimum 7 cluster tests × 3 cases = 21 assertions (codex HIGH Concern #5 — add explicit `taskFileMaxFileSizeBytes` cluster ≠ `ragUploadMaxFileSizeBytes` cluster: two distinct DbWinsOverProperty / NullColumnFallsThroughToProperty pairs assert that swapping one column does NOT affect the other resolver method's output). Alternatively, use `@ParameterizedTest` with one entry per knob — equivalent coverage.
    - Add one consumer-level integration test per actual upload path (codex MEDIUM Concern #5 — verify the two upload knobs route through their distinct call sites): `chatPanelFragmentRespectsTaskFileMaxFileSizeBytes()` and `knowledgeDocumentUploadServiceRespectsRagUploadMaxFileSizeBytes()`. These are NEW methods on this test class; both lock the two-knob split end-to-end.

    For `TtlConfigSentinelSurvivesAiUiSettingsTest`:
    - Boot stack mirrors `TtlConfigSentinelSkipsCleanupTest` exactly (same `@Tag("integration")`, same `@SpringBootTest(classes = AITestConfiguration.class)`, same `@ImportAutoConfiguration`, same `@Import` test configs).
    - Crucially: do NOT pass `properties = {"ai-agent.task-file.ttl-seconds=-1"}` — leave the property at its default. Instead, in `@BeforeEach`: load the singleton, `settings.setTaskFileTtlSeconds(-1L)`, save. This proves the DB column path delivers the same behavior as the property path.
    - Three assertion methods (per `<behavior>`).
  </action>
  <verify>
    <automated>cd ai-agent && ./gradlew :ai-agent:test --tests "com.vn.agent.admin.config.AiUiSettingsResolverReadThroughTest" 2>&1 | grep -E "BUILD SUCCESSFUL|FAILED"</automated>
    <automated>cd ai-agent && ./gradlew :ai-agent:test --tests "com.vn.agent.taskfile.TtlConfigSentinelSurvivesAiUiSettingsTest" 2>&1 | grep -E "BUILD SUCCESSFUL|FAILED"</automated>
    <automated>cd ai-agent && ./gradlew :ai-agent:test --tests "com.vn.agent.taskfile.TtlConfigSentinelSkipsCleanupTest" 2>&1 | grep -E "BUILD SUCCESSFUL|FAILED" (existing test still passes — both paths green)</automated>
    <source>grep -v '^#' ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/AiUiSettingsResolverReadThroughTest.java | grep -c "fail\\(" outputs 0.</source>
    <source>grep -v '^#' ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/TtlConfigSentinelSurvivesAiUiSettingsTest.java | grep -c "fail\\(" outputs 0.</source>
  </verify>
  <done>
    Both tests flip green. Existing `TtlConfigSentinelSkipsCleanupTest` (property-source path) also still green — both source paths now deliver identical sentinel behavior.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Non-admin chat turn → resolver → AiUiSettings singleton | Resolver uses `UnconstrainedDataManager` so non-admin chat turns are not blocked by row-level security on `AiUiSettings` (Pitfall 3). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Elevation of Privilege | `AiUiSettingsResolver.loadSingleton()` | accept | The `UnconstrainedDataManager` is intentional — read-only system-internal path. End users cannot READ `AiUiSettings` via DataManager (existing row-level security stays). Resolver only EXPOSES knob values as primitives, never the entity. |
| T-16-04 | Information Disclosure | Resolver method results | accept | Tier-1 knobs are operational tuning values (not secrets). They flow into ChatOptions / cleanup jobs / mutation guards — no PII surface. |
</threat_model>

<verification>
- `./gradlew :ai-agent:compileJava` exits 0.
- Phase 9, 10, 11, 12, 13, 13.1, 14, 15 test suites pass UNCHANGED (additive injection, no behavior change when DB columns are null).
- `AiUiSettingsResolverReadThroughTest` + `TtlConfigSentinelSurvivesAiUiSettingsTest` green.
- `TtlConfigSentinelSkipsCleanupTest` (property-source path) still green.
</verification>

<success_criteria>
- AiUiSettingsResolver exposes 12 typed resolveXxx() methods reading DB → properties → constant (codex HIGH Concern #5 — resolveTaskFileMaxFileSizeBytes and resolveRagUploadMaxFileSizeBytes are DISTINCT methods).
- Singleton load goes through UnconstrainedDataManager + try/catch resilience.
- 10 caller sites inject the resolver and swap their primary property call; ChatPanelFragment uses `resolveTaskFileMaxFileSizeBytes`, KnowledgeDocumentUploadService uses `resolveRagUploadMaxFileSizeBytes`.
- MutationIntentRepository coercion site verified pre-edit per opencode Concern #4; inline comment added on the consumption site.
- BuiltInDataTools/ToolEntityResolver caller census documented in commit body (codex Concern #5).
- No ApplicationEventPublisher in the resolver (Plan 10-06 R2).
- 2 test scaffolds flip green PLUS 2 new consumer-level integration tests in AiUiSettingsResolverReadThroughTest verify the upload-knob split; all existing test suites pass unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/16-admin-settings-model-picker-config-knob-migration/16-04-SUMMARY.md`.
</output>
