---
phase: 16-admin-settings-model-picker-config-knob-migration
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/AiSettingsChangedEvent.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/KnobMetadata.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/spi/AuditKind.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditWriter.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/ChatModelCatalogAllowlistTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/SecretRedactionInvariantsTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/AiSettingsChangedEventListenerInvariantTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/AiUiSettingsResolverReadThroughTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/AiUiSettingsBeanValidationTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/KnobInventoryClassificationTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/TtlConfigSentinelSurvivesAiUiSettingsTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/DefaultChatServiceImplModelValidationFallbackTest.java
autonomous: true
requirements: [MODEL-02, CFG-03, SEC-08, TEST-20]
requirements_addressed: [MODEL-02, CFG-03, SEC-08, TEST-20]
tags: [jmix, spring-boot, scaffolding]

must_haves:
  truths:
    - "AiSettingsChangedEvent class exists with enum Kind {PARAMETERS, UI_SETTINGS} and is publishable as ApplicationEvent (D-04)."
    - "KnobMetadata annotation exists with enum Tier {TIER_1, TIER_2, TIER_3} and is targetable at RECORD_COMPONENT, FIELD, METHOD (D-02)."
    - "AuditKind.MODEL_VALIDATION_FAILURE constant exists with literal string value MODEL_VALIDATION_FAILURE (D-05, Pitfall 5)."
    - "AuditWriter exposes a kind-parameter overload writeAuditEvent(String kind, ...) (or equivalent name) that stamps an arbitrary kind constant on the row — Plan 07 calls this overload with AuditKind.MODEL_VALIDATION_FAILURE without further AuditWriter changes (consensus HIGH fix from 16-REVIEWS opencode+codex)."
    - "Eight Wave-0 test scaffolds compile and are MARKED @Disabled(\"Phase 16 Wave 0 scaffold — Plan <NN> removes @Disabled and fills in body\") AND tagged @Tag(\"phase-16-scaffold\") so the branch is GREEN between plans (codex HIGH fix — no committed failing tests on the branch). Downstream plans remove @Disabled + fill body to flip the test green. CI must not run the phase-16-scaffold tag during the inter-plan window."
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/AiSettingsChangedEvent.java"
      provides: "Typed ApplicationEvent with Kind enum"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/KnobMetadata.java"
      provides: "Runtime annotation with Tier enum + requiresRestart + displayMessageKey"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/spi/AuditKind.java"
      provides: "MODEL_VALIDATION_FAILURE constant"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditWriter.java"
      provides: "writeAuditEvent(String kind, ...) kind-parameter overload (additive — existing writeToolCall unchanged) so Plan 07 can stamp MODEL_VALIDATION_FAILURE without further AuditWriter edits"
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/AiUiSettingsBeanValidationTest.java"
      provides: "Plan 02 scaffold (added to Plan 01 inventory per codex MEDIUM fix — align scaffold drift with later plans)"
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/ChatModelCatalogAllowlistTest.java"
      provides: "TEST-20 scaffold"
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/SecretRedactionInvariantsTest.java"
      provides: "SEC-08 scaffold"
  key_links:
    - from: "Plan 04 entity listeners"
      to: "AiSettingsChangedEvent"
      via: "ApplicationEventPublisher.publishEvent"
      pattern: "publishEvent\\(new AiSettingsChangedEvent\\("
    - from: "Plan 06 KnobInventoryScanner"
      to: "KnobMetadata"
      via: "reflective scan"
      pattern: "@KnobMetadata"
    - from: "Plan 07 DefaultChatServiceImpl"
      to: "AuditKind.MODEL_VALIDATION_FAILURE"
      via: "AuditWriter.writeAuditEvent(kind, ...) overload added in this plan"
      pattern: "AuditKind\\.MODEL_VALIDATION_FAILURE"
    - from: "Plan 07 writeModelValidationFailureAudit helper"
      to: "AuditWriter.writeAuditEvent(String kind, ...) overload"
      via: "direct call — additive API contract; existing writeToolCall callers untouched"
      pattern: "auditWriter\\.writeAuditEvent\\("
---

<objective>
Lay down the cross-wave foundation: the AiSettingsChangedEvent typed event, the KnobMetadata annotation that lets every @ConfigurationProperties record declare its tier, the AuditKind.MODEL_VALIDATION_FAILURE constant the catch+reissue wave will write, an additive AuditWriter.writeAuditEvent(String kind, ...) overload so Plan 07 can stamp MODEL_VALIDATION_FAILURE without further writer edits (consensus HIGH fix — Concern #1 from 16-REVIEWS), and eight @Disabled-tagged test scaffolds that subsequent waves flip green by removing @Disabled and replacing fail() with real assertions. Plans 02-07 reference these symbols by name — nothing else in this wave is functional, the goal is to publish stable type signatures and test class names so later waves do not have to invent them.

Purpose: Honor Plan 10-06 R2 single-publish-site invariant from the start (a typed event + reflective gate test before any publisher exists), satisfy `<deep_work_rules>` requirement that every later task has an automated verify by pre-creating the test classes those tasks will fill in, AND keep CI green between plans (codex HIGH fix — Concern #3) by gating scaffolds behind @Disabled + @Tag("phase-16-scaffold").

Output: 3 new production classes (event + annotation + constant addition), 1 modified production class (AuditWriter additive overload), 8 @Disabled test scaffolds (added AiUiSettingsBeanValidationTest to Plan 01's inventory per codex MEDIUM — align scaffold drift with Plan 02), zero behavior change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/16-admin-settings-model-picker-config-knob-migration/16-SPEC.md
@.planning/phases/16-admin-settings-model-picker-config-knob-migration/16-CONTEXT.md
@.planning/phases/16-admin-settings-model-picker-config-knob-migration/16-RESEARCH.md
@.planning/phases/16-admin-settings-model-picker-config-knob-migration/16-PATTERNS.md
@.planning/phases/16-admin-settings-model-picker-config-knob-migration/16-VALIDATION.md
@ai-agent/ai-agent/src/main/java/com/vn/agent/exposure/LlmExposureChangedEvent.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/spi/AuditKind.java

<interfaces>
From `ai-agent/ai-agent/src/main/java/com/vn/agent/exposure/LlmExposureChangedEvent.java` (precedent — Plan 10-06 R2):
```java
public class LlmExposureChangedEvent extends ApplicationEvent {
    public LlmExposureChangedEvent(Object source) { super(source); }
}
```

From `ai-agent/ai-agent/src/main/java/com/vn/agent/spi/AuditKind.java` (lines 1-16):
```java
public final class AuditKind {
    public static final String CHAT = "CHAT";
    public static final String TOOL = "TOOL";
    public static final String RETRIEVAL = "RETRIEVAL";
    private AuditKind() {}
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create AiSettingsChangedEvent + KnobMetadata annotation + AuditKind constant</name>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/AiSettingsChangedEvent.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/KnobMetadata.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/spi/AuditKind.java
  </files>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/exposure/LlmExposureChangedEvent.java (PATTERNS.md analog for the event shape)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/spi/AuditKind.java (file being modified — see current `CHAT`/`TOOL`/`RETRIEVAL` constants + 16-char width comment)
    - .planning/phases/16-admin-settings-model-picker-config-knob-migration/16-CONTEXT.md D-02 and D-04 (decision text for tier enum + event-kind enum)
    - .planning/phases/16-admin-settings-model-picker-config-knob-migration/16-RESEARCH.md Pitfall 5 (24-char `MODEL_VALIDATION_FAILURE` exceeds 16-char column — width fix lives in Plan 02 changelog, NOT here)
  </read_first>
  <action>
    Create three artifacts in lock-step per D-02, D-04, D-05.

    File 1 — `AiSettingsChangedEvent.java` in package `com.vn.agent.admin.config`:
    - Public class extending `org.springframework.context.ApplicationEvent`.
    - Public nested `enum Kind { PARAMETERS, UI_SETTINGS }` (exact identifiers, order locked).
    - Private final `Kind kind` field; public constructor `AiSettingsChangedEvent(Object source, Kind kind)` invoking `super(source)` and assigning the field.
    - Public `Kind getKind()` accessor.
    - Javadoc on the class MUST contain the literal sentence "Publish sites: AiParametersEntityListener and AiUiSettingsEntityListener ONLY — views/services NEVER inject ApplicationEventPublisher for this event (Plan 10-06 R2 invariant; SEC-08 second leg enforces)." (per D-04). This docstring is read by the SecretRedactionInvariantsTest and AiSettingsChangedEventListenerInvariantTest in Wave 0 scaffolds.

    File 2 — `KnobMetadata.java` in package `com.vn.agent.admin.config`:
    - `@Target({ElementType.RECORD_COMPONENT, ElementType.FIELD, ElementType.METHOD})` (three targets — RESEARCH §10 records vs setter classes both supported).
    - `@Retention(RetentionPolicy.RUNTIME)`.
    - `@Documented`.
    - Public nested `enum Tier { TIER_1, TIER_2, TIER_3 }`.
    - Members: `Tier tier()` (no default — required), `boolean requiresRestart() default false`, `String displayMessageKey() default ""`.
    - Javadoc references project memory `feedback_jmix_messages_over_spring` (root bundle keys only).

    File 3 — extend `AuditKind.java` additively:
    - Add `public static final String MODEL_VALIDATION_FAILURE = "MODEL_VALIDATION_FAILURE";` as a sibling constant of `CHAT`/`TOOL`/`RETRIEVAL` (exact 24-char string literal per D-05).
    - Update the existing `varchar(16)` docstring on the class with an inline note: "Plan 16-02 widens KIND to varchar(32) to fit MODEL_VALIDATION_FAILURE (24 chars)."
    - Do NOT shorten the constant value; the width fix lives in the Plan 02 changelog (Pitfall 5).
  </action>
  <verify>
    <automated>cd ai-agent && ./gradlew :ai-agent:compileJava 2>&1 | grep -E "error|BUILD SUCCESSFUL"</automated>
    <source>grep -E "enum Kind" ai-agent/ai-agent/src/main/java/com/vn/agent/admin/config/AiSettingsChangedEvent.java | grep -v '^#' | wc -l outputs ≥ 1</source>
    <source>grep -E "MODEL_VALIDATION_FAILURE" ai-agent/ai-agent/src/main/java/com/vn/agent/spi/AuditKind.java outputs a `public static final String` line.</source>
  </verify>
  <done>
    All three files exist and compile under `./gradlew :ai-agent:compileJava`. `AiSettingsChangedEvent.Kind.PARAMETERS` and `AiSettingsChangedEvent.Kind.UI_SETTINGS` are accessible as nested enums. `AuditKind.MODEL_VALIDATION_FAILURE.equals("MODEL_VALIDATION_FAILURE") == true`. No production callers added in this task — symbols are dangling on purpose; Plans 04/06/07 wire them.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create eight Wave-0 test scaffolds (all @Disabled + @Tag("phase-16-scaffold") to keep CI green between plans) that Plans 02-07 fill in</name>
  <files>
    ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/ChatModelCatalogAllowlistTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/SecretRedactionInvariantsTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/AiSettingsChangedEventListenerInvariantTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/AiUiSettingsResolverReadThroughTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/AiUiSettingsBeanValidationTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/KnobInventoryClassificationTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/TtlConfigSentinelSurvivesAiUiSettingsTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/DefaultChatServiceImplModelValidationFallbackTest.java
  </files>
  <read_first>
    - ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/TtlConfigTest.java (Pattern I — `@SpringBootTest` + `@ImportAutoConfiguration` boot stack; the new sentinel-survives test is a sibling class shape)
    - .planning/phases/16-admin-settings-model-picker-config-knob-migration/16-VALIDATION.md `## Wave 0 Requirements` (the seven scaffold files this task creates)
    - .planning/phases/16-admin-settings-model-picker-config-knob-migration/16-RESEARCH.md `## Validation Architecture` and `### Phase Requirements → Test Map` (per-test required behaviors)
    - .planning/phases/16-admin-settings-model-picker-config-knob-migration/16-PATTERNS.md Pattern I (test class shape)
  </read_first>
  <action>
    Create eight test classes (was seven — `AiUiSettingsBeanValidationTest` added per codex MEDIUM fix to align with Plan 02's inventory). Each test class:
    - Is annotated at the CLASS level with `@org.junit.jupiter.api.Disabled("Phase 16 Wave 0 scaffold — Plan <NN> removes @Disabled and fills in body")` AND `@org.junit.jupiter.api.Tag("phase-16-scaffold")` (codex HIGH fix — Concern #3: branch stays green between plans; CI must filter out `phase-16-scaffold` tag during the inter-plan window, or rely on the @Disabled which JUnit honors regardless of tag config).
    - Contains JUnit 5 `@Test`-annotated methods whose body is `org.junit.jupiter.api.Assertions.fail("Phase 16 Wave 0 scaffold — implemented by Plan <NN>")`. The fail() bodies stay present (defense-in-depth: if a future plan accidentally removes @Disabled without filling in the body, the test FAILS rather than silently passing).
    - Each downstream plan that flips a scaffold green removes the class-level `@Disabled`, removes the `@Tag("phase-16-scaffold")`, removes each `fail()` call, and replaces with real assertions.
    - Each test class carries a Javadoc header naming the plan that will implement it AND a one-line note: `Disabled until Plan <NN> fills in the body — see Phase 16 inter-plan branch-green strategy (16-REVIEWS codex HIGH concern #3).`

    File 1 — `ChatModelCatalogAllowlistTest` (Plan 02 fills in):
      - Methods (each `fail()`-bodied): `catalogSubsetOfAllowlist()`, `exactlyOneDefault()`, `defaultMatchesDefaultParamsYaml()` (drift gate per D-06 / SPEC criterion 4).
      - No Spring context needed — pure static-list checks against the constant `ChatModelCatalog.SELF_HOSTABLE_OPEN_WEIGHTS_ALLOWLIST` (Plan 02 creates the catalog).

    File 2 — `SecretRedactionInvariantsTest` (Plan 06 fills in, SEC-08):
      - Methods: `noSecretBoundEditable()` (XML scan for property names matching `.*\\.api-key|.*\\.password|.*\\.secret|.*\\.token`), `noConditionalOnPropertyToggleBoundEditable()` (classpath scan for `@ConditionalOnProperty` keys cross-referenced with XML `property=` attributes), `noPublisherOutsideTwoEntityListeners()` (third leg: `ApplicationEventPublisher.publishEvent(new AiSettingsChangedEvent(...))` substring appears only in `AiParametersEntityListener` and `AiUiSettingsEntityListener` source files).
      - Use `java.nio.file.Files.walk(Paths.get("ai-agent/ai-agent/src/main"))` shape; no Reflections dependency.

    File 3 — `AiSettingsChangedEventListenerInvariantTest` (Plan 04 fills in):
      - Methods: `activeParametersSavePublishesPARAMETERS()`, `inactiveParametersSavePublishesZero()`, `uiSettingsSavePublishesUI_SETTINGS()`, `singlePublishSiteSourceScan()`.
      - Boot stack per Pattern I; use `@RecordApplicationEvents` and `ApplicationEvents` injection to capture events.

    File 4 — `AiUiSettingsResolverReadThroughTest` (Plan 03 fills in):
      - Methods: one `*DbWinsOverProperty()` + one `*NullColumnFallsThroughToProperty()` + one `*PropertyAbsentFallsThroughToConstant()` per knob cluster (task-file, mutation, prompt/tools, title, upload — 5 clusters × 3 = 15 methods, or one parameterised test per cluster — planner's choice; minimum 6 methods to keep coverage explicit).
      - Boot stack per Pattern I.

    File 5 — `KnobInventoryClassificationTest` (Plan 06 fills in):
      - Methods: `everyConfigurationPropertiesRecordHasKnobMetadata()` (reflective scan), `tier3SecretPatternMaskAppliesToUnannotatedKey()`, `tier2BootToggleHasRequiresRestartTrue()`.

    File 6 — `TtlConfigSentinelSurvivesAiUiSettingsTest` in package `com.vn.agent.taskfile` (Plan 03 fills in):
      - Methods (mirroring existing `TtlConfigSentinelSkipsCleanupTest` in same file/package): `sentinelMinusOneInUiSettingsRowSkipsCleanup()`, `nullColumnFallsThroughToPropertyValue()`, `nullColumnFallsThroughToPropertySentinel()`.
      - Boot stack per Pattern I; sentinel set via `AiUiSettings.TASK_FILE_TTL_SECONDS = -1` (Plan 02 creates this column).

    File 6b — `AiUiSettingsBeanValidationTest` in package `com.vn.agent.admin.config` (Plan 02 fills in — added per codex MEDIUM fix to remove scaffold drift between Plan 01 and Plan 02):
      - Methods: `taskFileTtlBelowMinusOneRejected()`, `taskFileTtlMinusOneSentinelAccepted()`, `taskFileTtlAboveMaxRejected()`, `mutationBulkMaxRowsBelowMinRejected()`, `mutationBulkMaxRowsAboveMaxRejected()`, `uploadMaxFileSizeBytesBelowFloorRejected()`, `mutationConfirmationRequiredAcceptsAnyBooleanIncludingNull()`, `taskFileMaxFileSizeBytesBelowFloorRejected()` (last method covers the NEW task-file max-size column added per codex HIGH Concern #4 — Plan 02 schema).
      - Boot stack mirrors `TtlConfigTest` (Pattern I).

    File 7 — `DefaultChatServiceImplModelValidationFallbackTest` in package `com.vn.agent` (Plan 07 fills in):
      - Methods: `badModelExceptionTriggersOneShotReissue()`, `bothAuditRowsShareRunId()`, `savedAiParametersBodyYamlModelIsNotMutated()`, `nonBadModelExceptionPropagates()`, `_5xxResponseDoesNotTriggerReissue()` (Pitfall 6 false-positive guard), `directRestClientResponseExceptionTriggersReissue()` (codex HIGH Concern #8 — classifier must catch RestClientResponseException directly, not only NonTransientAiException), `userVisibleFallbackNotificationFires()` (codex HIGH Concern #9 — MODEL-02 fallback notification surface MUST be wired in Plan 07, not deferred).
      - Boot stack mirrors `DefaultChatServiceImplStreamFallbackTest` if present; otherwise Pattern I.

    Every test class MUST start with a Javadoc block: `/** Wave 0 scaffold — Plan 16-<NN> implements the bodies. @Disabled at class level until that plan; each method currently calls fail() as a defense-in-depth gate. */`.
  </action>
  <verify>
    <powershell>Push-Location ai-agent; ./gradlew ":ai-agent:compileTestJava"; Pop-Location  # exits 0 (codex MEDIUM fix — PowerShell-safe verify command)</powershell>
    <powershell>Push-Location ai-agent; ./gradlew ":ai-agent:test" "--tests" "com.vn.agent.admin.config.ChatModelCatalogAllowlistTest"; Pop-Location  # SKIPPED (class-level @Disabled — branch stays green)</powershell>
    <source>(Select-String -Path ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/*.java,ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/TtlConfigSentinelSurvivesAiUiSettingsTest.java,ai-agent/ai-agent/src/test/java/com/vn/agent/DefaultChatServiceImplModelValidationFallbackTest.java -Pattern "Phase 16 Wave 0 scaffold" -List).Count -ge 8</source>
    <source>(Select-String -Path ai-agent/ai-agent/src/test/java/com/vn/agent/admin/config/*.java,ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/TtlConfigSentinelSurvivesAiUiSettingsTest.java,ai-agent/ai-agent/src/test/java/com/vn/agent/DefaultChatServiceImplModelValidationFallbackTest.java -Pattern "@Disabled\(.*phase-16.*scaffold|Phase 16 Wave 0 scaffold" -List).Count -ge 8 (every scaffold is class-level @Disabled)</source>
    <automated>cd ai-agent && ./gradlew :ai-agent:test 2>&1 | grep -E "BUILD SUCCESSFUL|FAILED" (full test run is GREEN — all 8 scaffolds skipped via @Disabled; codex HIGH fix Concern #3)</automated>
  </verify>
  <done>
    Eight test classes compile under `./gradlew :ai-agent:compileTestJava`. Every class has a class-level `@Disabled("Phase 16 Wave 0 scaffold — Plan <NN> ...")` AND `@Tag("phase-16-scaffold")`. Full `./gradlew :ai-agent:test` run is GREEN (codex HIGH fix Concern #3 — branch stays green between plans). Plans 02-07 each remove `@Disabled` + `@Tag("phase-16-scaffold")` + `fail()` calls and add real assertions to flip their owned tests green.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add AuditWriter.writeAuditEvent(String kind, ...) kind-parameter overload (consensus HIGH fix — Concern #1 from 16-REVIEWS)</name>
  <files>ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditWriter.java</files>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditWriter.java (entire file — identify the existing `writeToolCall(...)` method's row-build helper; the new overload reuses the same helper but accepts an arbitrary `String kind` parameter)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/spi/AuditKind.java (just-extended with MODEL_VALIDATION_FAILURE in Task 1 — confirm the constant is reachable)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiAuditEvent.java (the audit row entity — confirm `setKind(String)` accepts the new 24-char value once Plan 02 widens the column to varchar(32))
    - .planning/phases/16-admin-settings-model-picker-config-knob-migration/16-REVIEWS.md (Concern #1 — kind-parameter overload must exist before Plan 07 needs it)
    - .planning/phases/16-admin-settings-model-picker-config-knob-migration/16-RESEARCH.md `### Pattern K (AuditWriter.writeToolCall idiom)`
  </read_first>
  <action>
    Add a new public method to `AuditWriter.java` — purely additive, no existing callers modified:

    ```
    /**
     * Phase 16 D-05 — writes an audit row stamped with the caller-supplied kind constant
     * (e.g., AuditKind.MODEL_VALIDATION_FAILURE). Reuses the same row-build path as
     * writeToolCall(...) but does not assume kind == TOOL.
     *
     * Plan 07's DefaultChatServiceImpl.writeModelValidationFailureAudit calls this overload
     * with kind = AuditKind.MODEL_VALIDATION_FAILURE so the failed-then-recovered turn emits
     * two correlated audit rows under the same runId (MODEL_VALIDATION_FAILURE + recovered
     * CHAT/TOOL row from the existing post-call audit code).
     *
     * Note: the column AI_AUDIT_EVENT.KIND is widened from varchar(16) to varchar(32) by
     * Plan 02's changelog (Pitfall 5 — MODEL_VALIDATION_FAILURE is 24 chars).
     */
    public void writeAuditEvent(String kind,
                                UUID parentId,
                                UUID runId,
                                String userUsername,
                                UUID conversationId,
                                String toolName,
                                String argumentsJson,
                                String resultSummary,
                                long latencyMs,
                                AiToolCallOutcome outcome,
                                String denialReason,
                                String errorClass) {
        // Delegates to the existing row-build private helper, then setKind(kind) before save.
        // Implementation MUST mirror writeToolCall(...) exactly — same DataManager path,
        // same JPA flush semantics, same redaction guarantees — differing ONLY in the
        // kind value stamped on the row.
        // Concrete shape: extract the body of writeToolCall(...) into a private helper
        //   private void writeAuditRow(String kind, UUID parentId, ..., String errorClass)
        // and have BOTH writeToolCall (passing AuditKind.TOOL) and writeAuditEvent (passing
        // the caller-supplied kind) delegate to it. Zero behavior change for existing
        // writeToolCall callers (Phases 11/12/13/13.1/14/15 audit tests must pass unchanged).
    }
    ```

    Locked invariants:
    - The new method is **additive only**. Existing `writeToolCall(...)` callers do NOT change.
    - The shared private helper (`writeAuditRow` or equivalent name) MUST sanitize identically to the existing path — no new redaction surface introduced (T-16-04 mitigation preserved).
    - Method signature parameters mirror `writeToolCall` exactly (same parameter names, same types, same order) so the call site in Plan 07 is a 1-line swap.
    - The method does NOT validate `kind` against AuditKind constants — host extensions may emit custom kinds with the column-widening as the only schema constraint. The existing in-tree usage is restricted to `AuditKind.*` constants per code review.
  </action>
  <verify>
    <powershell>Push-Location ai-agent; ./gradlew ":ai-agent:compileJava"; Pop-Location  # exits 0</powershell>
    <source>(Select-String -Path ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditWriter.java -Pattern "public void writeAuditEvent\(String kind").Count -eq 1</source>
    <source>(Select-String -Path ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditWriter.java -Pattern "public void writeToolCall").Count -eq 1 (existing method still present, unchanged)</source>
    <automated>cd ai-agent && ./gradlew :ai-agent:test --tests "*AuditWriter*" 2>&1 | grep -E "BUILD SUCCESSFUL|FAILED" (any existing audit-writer tests pass — additive change)</automated>
  </verify>
  <done>
    `AuditWriter` exposes both `writeToolCall(...)` (unchanged) and `writeAuditEvent(String kind, ...)` (new, additive). Both delegate to the same private row-build helper. Existing audit-writer tests pass unchanged. Plan 07 can call `auditWriter.writeAuditEvent(AuditKind.MODEL_VALIDATION_FAILURE, ...)` without further AuditWriter edits — Plan 07's `files_modified` no longer needs to include AuditWriter.java (codex HIGH Concern #1 closed at Wave 0).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin save → entity listener → event consumer (Phase 18 cache) | Settings-change event crosses the publish boundary; T-16-05 disposition lives in Plan 02 schema + Plan 04 listener, but the event TYPE shape must be locked here to prevent twin types. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02 | Tampering | `KnobMetadata` annotation | mitigate | Three-tier enum (TIER_1/TIER_2/TIER_3) is the type-safe SoT; SEC-08 source-scan in Plan 06 cross-checks `@ConditionalOnProperty` keys against editable XML bindings. This task defines the Tier enum shape. |
| T-16-04 | Information Disclosure | `AuditKind.MODEL_VALIDATION_FAILURE` constant | mitigate | Constant value is a public taxonomy tag (no PII / no secret); the sanitisation of the audit ROW body happens in Plan 07's writer. This task only locks the kind string. |
</threat_model>

<verification>
- `./gradlew :ai-agent:compileJava` exits 0.
- `./gradlew :ai-agent:compileTestJava` exits 0.
- `./gradlew :ai-agent:test` exits 0 (full suite GREEN — all 8 scaffolds skipped via class-level `@Disabled`; codex HIGH fix Concern #3 — branch stays green between plans).
- `Select-String -Path ai-agent/ai-agent/src/main/java -Pattern "AiSettingsChangedEvent|KnobMetadata|MODEL_VALIDATION_FAILURE" -Recurse` finds the new symbols and ONLY these usages plus the new `writeAuditEvent` overload site (no premature wiring; codex MEDIUM fix — PowerShell-safe command).
- `Select-String -Path ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditWriter.java -Pattern "public void writeAuditEvent\(String kind"` outputs ≥ 1 (consensus HIGH fix Concern #1 — overload exists before Plan 07 needs it).
</verification>

<success_criteria>
- AiSettingsChangedEvent.Kind enum exposes PARAMETERS and UI_SETTINGS constants in source order matching D-04.
- KnobMetadata.Tier enum exposes TIER_1, TIER_2, TIER_3.
- AuditKind class compiles with the new constant + updated docstring.
- AuditWriter exposes `writeAuditEvent(String kind, ...)` overload as a sibling of `writeToolCall`; both delegate to the same private row-build helper (consensus HIGH Concern #1).
- All 8 Wave-0 test scaffolds compile, are class-level `@Disabled`, AND tagged `@Tag("phase-16-scaffold")` so `./gradlew :ai-agent:test` is GREEN (codex HIGH Concern #3).
- Scaffolds include `AiUiSettingsBeanValidationTest` (codex MEDIUM — Plan 01 inventory aligned with Plan 02) and the two new test methods in `DefaultChatServiceImplModelValidationFallbackTest` (`directRestClientResponseExceptionTriggersReissue`, `userVisibleFallbackNotificationFires` per codex HIGH Concerns #8 + #9).
- No production code outside these 4 files (AiSettingsChangedEvent, KnobMetadata, AuditKind, AuditWriter) references the new symbols (callers come in Plans 04/06/07).
</success_criteria>

<output>
After completion, create `.planning/phases/16-admin-settings-model-picker-config-knob-migration/16-01-SUMMARY.md`.
</output>
