---
phase: 17-mutation-internals-hardening-phase-11-follow-up
plan: 02
type: execute
wave: 1
depends_on: ["17-01"]
files_modified:
  - ai-agent/ai-agent/src/main/java/com/vn/agent/tools/mutation/RelatedWriteMetadataResolver.java
autonomous: true
requirements: [MUT-17]
must_haves:
  truths:
    - "The metamodel walk in RelatedWriteMetadataResolver runs exactly once per distinct (parentEntityName, relationshipName) key across repeated calls — for supported AND unsupported keys."
    - "A cached rejection rethrows a FRESHLY built unsupportedRelationship() (never the cached throwable), byte-identical in code/message/hints."
    - "All add_related_record / remove_related_record behavior is byte-for-byte identical to v1.1 (existing resolver + related-write suites pass unchanged)."
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/tools/mutation/RelatedWriteMetadataResolver.java"
      provides: "Memoized (parentMetaClass, relationshipName) -> descriptor with package-private compute seam"
      contains: "ConcurrentHashMap"
  key_links:
    - from: "RelatedWriteMetadataResolver.resolveSupportedRelatedWriteRelationship"
      to: "cache.computeIfAbsent"
      via: "ConcurrentHashMap keyed on record Key(String parentEntityName, String relationshipName)"
      pattern: "computeIfAbsent"
    - from: "RelatedWriteMetadataResolver.computeSupported"
      to: "RelatedWriteMetadataMemoTest.CountingResolver"
      via: "package-private compute seam overridden by the call-count proxy"
      pattern: "computeSupported"
---

<objective>
Memoize `RelatedWriteMetadataResolver.resolveSupportedRelatedWriteRelationship` with a plain
`ConcurrentHashMap` keyed on a `record Key(String parentEntityName, String relationshipName)`, caching BOTH
outcomes (supported descriptor AND rejection) via a small `Result` holder, never caching a live throwable.
Extract the existing metamodel-walk body into a package-private `computeSupported(...)` seam so the Wave-0
call-count proxy (Plan 01 Task 2) can override and count it.

This is the lowest-parity-risk refactor (MUT-17): the file is otherwise untouched, the metamodel is immutable
at runtime (no eviction), and every caller (`add_related_record`/`remove_related_record` via the chain) is
unchanged.

Purpose: turn the per-call metamodel walk into a walk-once-per-key cache — per D-10/D-11/D-12.
Output: one modified file; the Plan 01 memo test turns GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17-mutation-internals-hardening-phase-11-follow-up/17-SPEC.md
@.planning/phases/17-mutation-internals-hardening-phase-11-follow-up/17-CONTEXT.md
@.planning/phases/17-mutation-internals-hardening-phase-11-follow-up/17-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Memoize resolveSupportedRelatedWriteRelationship with ConcurrentHashMap + Result holder + compute seam</name>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/tools/mutation/RelatedWriteMetadataResolver.java (the file being modified; the full current walk `resolveSupportedRelatedWriteRelationship` lines 110-172, `SupportedRelatedRelationship` record lines 98-101, `unsupportedRelationship()` factory lines 307-314 — note it is a `private static` no-arg factory with NO LLM data, so a fresh rebuild is byte-identical)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/rag/CancellationRegistry.java (house ConcurrentHashMap precedent: `private final Map<...> = new ConcurrentHashMap<>()` + `computeIfAbsent`, no eviction — lines 33-37, 63-66)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/StreamingSinkHolder.java (second ConcurrentHashMap house precedent)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/StreamingEvent.java (sealed-interface + nested-record idiom for the Result holder)
    - ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/RelatedWriteMetadataMemoTest.java (Plan 01 Task 2 — the seam name it overrides MUST match `computeSupported`)
    - 17-CONTEXT.md D-10 (ConcurrentHashMap, no eviction), D-11 (record Key on parentMetaClass.getName()), D-12 (cache both outcomes via Result holder; never cache the throwable; rethrow fresh), D-13 (package-private compute seam)
    - 17-PATTERNS.md "NEW Result holder + record Key", "MODIFY RelatedWriteMetadataResolver.java memoization"
  </read_first>
  <behavior>
    - resolve(parent, "linkedChildren") called twice → metamodel walk executes once; both calls return the same supported descriptor shape.
    - resolve(parent, "<unsupported>") called twice → walk executes once; both calls throw ToolUserError("validation_failed", ...); the two thrown instances are distinct objects (fresh rebuild, not the cached throwable).
    - Two distinct keys → walk executes twice.
    - Blank/null relationshipName still throws unsupportedRelationship() WITHOUT polluting the cache with a null-key entry (preserve the existing early-throw before key construction OR key safely).
  </behavior>
  <action>
    Add `import java.util.Map;` and `import java.util.concurrent.ConcurrentHashMap;`. Add a nested
    `record Key(String parentEntityName, String relationshipName) {}` (D-11; built from
    `parentMetaClass.getName()` — never the raw MetaClass) and a private final
    `Map<Key, Result> cache = new ConcurrentHashMap<>();`.

    Define a small package-private `Result` holder carrying EITHER a supported descriptor OR a rejected
    marker — a sealed nested type or a 2-field record (`SupportedRelatedRelationship relationship` non-null
    XOR a `boolean rejected`) per the StreamingEvent sealed idiom; expose `rejected()` and `relationship()`
    accessors. NEVER store a Throwable in it (D-12).

    Extract the ENTIRE current validation walk body (current lines 113-171, everything after the
    `Objects.requireNonNull` + blank-name guard) into a NEW package-private method
    `Result computeSupported(MetaClass parentMetaClass, String relationshipName)` that returns
    `Result.rejected()` instead of `throw unsupportedRelationship()` at each rejection point, and
    `Result.of(new SupportedRelatedRelationship(...))` at the success return. (Convert the internal `throw`s
    to `return Result.rejected()`.) Keep this method package-private so `RelatedWriteMetadataMemoTest.CountingResolver`
    can override it.

    Rewrite the public `resolveSupportedRelatedWriteRelationship` to:
    `Objects.requireNonNull(parentMetaClass, "parentMetaClass")`; keep the
    `if (relationshipName == null || relationshipName.isBlank()) throw unsupportedRelationship();` guard
    BEFORE key construction so blank keys never enter the cache; build
    `Key key = new Key(parentMetaClass.getName(), relationshipName)`; then
    `Result r = cache.computeIfAbsent(key, k -> computeSupported(parentMetaClass, relationshipName));`
    then `if (r.rejected()) throw unsupportedRelationship(); return r.relationship();` — the `throw` builds a
    FRESH `unsupportedRelationship()` each time (byte-identical; D-12).

    Do NOT change `unsupportedRelationship()`, `wireInverseReference`, `clearInverseReference`,
    `ensureInverseClearable`, `childBelongsToParent`, `childBelongsToDifferentParent`,
    `isCompositionOrDeleteCapable`, or the `SupportedRelatedRelationship` record. No eviction logic (immutable
    metamodel, D-10). No new dependency.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.tools.mutation.RelatedWriteMetadataMemoTest" --tests "com.vn.agent.tools.mutation.RelatedWriteMetadataResolverTest"</automated>
  </verify>
  <acceptance_criteria>
    - `RelatedWriteMetadataResolver.java` contains a `ConcurrentHashMap` field, a nested `record Key(String parentEntityName, String relationshipName)`, a `Result` holder (sealed or record, carrying NO Throwable), and a package-private `computeSupported(MetaClass, String)` method.
    - The public method uses `cache.computeIfAbsent(...)` and rethrows a FRESH `unsupportedRelationship()` on a cached rejection (the source contains `throw unsupportedRelationship();` AFTER the cache lookup, not a stored exception).
    - `--tests "*RelatedWriteMetadataMemoTest"` exits 0 (walk-once for supported + unsupported, distinct-keys-twice, fresh-rethrow `isNotSameAs` all green).
    - `--tests "*RelatedWriteMetadataResolverTest"` exits 0 with ZERO edits to that test body (MUT-18 parity — the resolver's externally observable verdicts and error are byte-identical).
    - No eviction method/listener added; no new import beyond `java.util.Map` + `java.util.concurrent.ConcurrentHashMap`.
  </acceptance_criteria>
  <done>The resolver memoizes both outcomes, the walk runs once per key, rejections rethrow fresh, and the existing resolver test plus all related-write suites pass unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → related-write resolver | `relationshipName` is LLM-supplied; resolved against the immutable Jmix metamodel. The cache key is `(parentEntityName, relationshipName)` — derived from already-validated/visible inputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | Elevation / Information Disclosure | RelatedWriteMetadataResolver memo | mitigate | The memo caches ONLY metamodel-relationship-support facts (whether a `@OneToMany(mappedBy)` non-composition shape with a single-valued inverse exists). The Jmix metamodel is immutable at runtime and security-INDEPENDENT — NO `AccessManager`/`LlmExposurePolicy`/row-level decision is consulted or cached here (those gates run in the chain BEFORE/AROUND this resolver, unchanged). Confirmed: the metamodel relationship-support decision is permission-independent, so caching it across security contexts cannot leak a permission-dependent outcome. The walk-once test asserts this without varying security context because none is read. |
| T-17-04 | Tampering (error-message drift) | unsupportedRelationship() rethrow | mitigate | Per D-12 the cache stores a rejected MARKER, never the thrown `ToolUserError`; on a cached rejection a FRESH `unsupportedRelationship()` is built — byte-identical code/message/hints. The memo test asserts the two thrown instances are distinct objects, locking "fresh rebuild" so no stale stack/suppressed state leaks. |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | No package installs (no new dependencies — SPEC). `ConcurrentHashMap` is JDK 21. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.tools.mutation.RelatedWriteMetadataMemoTest"` — green.
- `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.tools.mutation.RelatedWriteMetadataResolverTest"` — green, test body unchanged.
- `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.tools.mutation.*"` — all mutation suites green (related-write add/remove behavior byte-identical).
</verification>

<success_criteria>
- Walk runs once per distinct key (supported + unsupported), proven by the Plan 01 memo test.
- Rejections rethrow a fresh canned error (no cached throwable).
- No eviction logic; no new dependency.
- All Phase 9/10/11 mutation suites pass unchanged (MUT-18 parity for the related-write path).
</success_criteria>

<output>
Create `.planning/phases/17-mutation-internals-hardening-phase-11-follow-up/17-02-SUMMARY.md` when done.
</output>
