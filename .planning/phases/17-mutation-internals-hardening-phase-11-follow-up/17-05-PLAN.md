---
phase: 17-mutation-internals-hardening-phase-11-follow-up
plan: 05
type: execute
wave: 3
depends_on: ["17-02", "17-03", "17-04"]
files_modified: []
autonomous: true
requirements: [MUT-18]
must_haves:
  truths:
    - "Every Phase 9/10/11 mutation test suite + AgentToolCallbacksDefaultConfigTest passes with ZERO test-body edits."
    - "git diff confirms no Phase 9/10/11 mutation test source file was modified except the additive @Test methods on MutationToolInvariantsTest (MUT-15) created in Plan 01."
    - "The three structural proxies (gate-order invariant, FK 1-query slope, memo walk-once) are green, proving the refactor did something; the unchanged behavioral suites prove it changed nothing observable."
  artifacts: []
  key_links:
    - from: "full :ai-agent:ai-agent:test"
      to: "Phase 9/10/11 mutation suites + AgentToolCallbacksDefaultConfigTest"
      via: "byte-for-byte parity regression gate"
      pattern: "AgentToolCallbacksDefaultConfigTest"
---

<objective>
Final BLOCKING parity gate for the phase (MUT-18). Prove byte-for-byte behavior parity: every Phase 9/10/11
mutation suite (`MutationToolInvariantsTest`, gating-order, audit-row, `MutationErrorTranslator`, host-guard-veto,
row-level mutation-security, idempotency-replay, bulk partial-failure) and the default-config zero-mutation-callback
boot test (`AgentToolCallbacksDefaultConfigTest`) pass UNCHANGED — with no edits to any existing test body. The
only additive test changes permitted in the entire phase are the new @Test methods on `MutationToolInvariantsTest`
(MUT-15) created in Plan 01.

This plan writes NO production code. It is the explicit verification gate that the three refactors (MUT-15
chain extraction, MUT-16 FK batch-load, MUT-17 memoization) are observably invisible.

Purpose: lock the refactor as behavior-frozen before `/gsd-verify-work`.
Output: a green full suite + a clean git-diff audit of the test tree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17-mutation-internals-hardening-phase-11-follow-up/17-SPEC.md
@.planning/phases/17-mutation-internals-hardening-phase-11-follow-up/17-VALIDATION.md
</context>

<tasks>

<task type="auto" gate="blocking">
  <name>Task 1 [BLOCKING]: Full-suite parity regression + zero-test-edit git-diff audit</name>
  <read_first>
    - .planning/phases/17-mutation-internals-hardening-phase-11-follow-up/17-VALIDATION.md (the per-requirement test map + sampling rate + "zero test edits" gate)
    - .planning/phases/17-mutation-internals-hardening-phase-11-follow-up/17-SPEC.md Acceptance Criteria (the 8 pass/fail checks)
    - ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/MutationToolInvariantsTest.java (the ONE test file allowed to have additive @Test methods — MUT-15; its pre-Phase-17 body must be otherwise unchanged)
  </read_first>
  <action>
    Run the FULL included-build module test suite: `./gradlew :ai-agent:ai-agent:test` (root has no
    `:ai-agent:test` task — STATE.md Phase 12 decision). Confirm green:
    - the three new structural proxies (`MutationToolInvariantsTest` MUT-15 assertions,
      `MutationFkBatchLoadQueryCountTest` slope, `RelatedWriteMetadataMemoTest` walk-once);
    - the Phase 9/10/11 behavioral suites (`MutationToolInvariantsTest` original assertions, gating-order,
      audit-row, `MutationErrorTranslatorTest`, host-guard-veto, row-level mutation-security,
      idempotency-replay, `BuiltInMutationToolsBulkSavePartialFailureTest`,
      `RelatedWriteMetadataResolverTest`);
    - `AgentToolCallbacksDefaultConfigTest` (zero-mutation-callback boot).

    Then run a git-diff audit proving NO existing test body was edited:
    `git diff --stat origin/main -- ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation
    ai-agent/ai-agent/src/test/java/com/vn/agent/performance` and confirm the only test-source changes are
    (a) ADDITIVE @Test methods on `MutationToolInvariantsTest` (MUT-15), and (b) the NEW Wave-0 test files +
    agentstore FK fixture from Plan 01 — no edits to the body of any pre-existing Phase 9/10/11 mutation test.

    If the known atmosphere-runtime / `agentstoreEntityManagerFactory` @SpringBootTest boot regression affects
    a module-level Spring-context test (as it did in Phase 11/13/13.1), record it in the SUMMARY exactly as
    Phase 13.1 Plan 06 did (reproduced on pre-Phase-17 code, NOT introduced by this phase) and confirm the
    affected test is source-correct + compileTestJava-green; it does not block the parity verdict for tests
    that DO boot.

    If ANY pre-existing mutation/default-config test FAILS or shows a behavioral diff: STOP, do not patch the
    test, and report the failing suite + the production-code regression it reveals (this is a parity break in
    Plan 02/03/04 — fix the production code, never the test). This task is BLOCKING — the phase does not
    complete until the suite is green with zero test-body edits.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew :ai-agent:ai-agent:test` exits 0 (full module suite green), OR any non-green test is solely the documented pre-existing boot regression (reproduced on pre-Phase-17 code) with the failing test source-correct + compileTestJava-green and behaviorally unchanged.
    - `git diff origin/main -- ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation ai-agent/ai-agent/src/test/java/com/vn/agent/performance` shows ONLY: additive @Test methods on `MutationToolInvariantsTest`, plus the new `RelatedWriteMetadataMemoTest` / `MutationFkBatchLoadQueryCountTest` / agentstore FK fixture files — NO edits to the body of any pre-existing Phase 9/10/11 mutation test.
    - The three structural proxies are green (gate order, FK slope, memo walk-once); the behavioral suites + `AgentToolCallbacksDefaultConfigTest` are green/unchanged.
    - The SUMMARY records the exact suites verified and the git-diff result.
  </acceptance_criteria>
  <done>The full mutation suite + default-config boot test pass with zero test-body edits, the three structural proxies are green, and the git-diff audit confirms behavior-frozen parity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| refactored production code → existing behavioral test suites | The Phase 9/10/11 suites are the externally-observable behavior contract; this gate confirms the refactor did not cross any of those boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-10 | Tampering | silent behavior regression | mitigate | The full Phase 9/10/11 mutation suite + `AgentToolCallbacksDefaultConfigTest` run UNCHANGED is the byte-for-byte parity gate (MUT-18): any change to gating order/outcomes, exception classification, `MutationErrorTranslator` outputs, audit rows (incl. rollback), or idempotency semantics fails a locked test. Failures are fixed in PRODUCTION code, never by editing the test (enforced by the git-diff audit). |
| T-17-11 | Tampering | test-weakening to pass | mitigate | The `git diff origin/main` audit on the test tree fails the gate if any pre-existing test body was edited — preventing a regression from being hidden by relaxing a locked assertion. Only the MUT-15 additive @Test methods + Wave-0 new files are permitted. |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | No package installs (no new dependencies — SPEC). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `./gradlew :ai-agent:ai-agent:test` — full module suite green (or only the documented pre-existing boot regression, source-correct).
- `git diff origin/main -- ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation ai-agent/ai-agent/src/test/java/com/vn/agent/performance` — additive-only test changes.
</verification>

<success_criteria>
- Phase 9/10/11 mutation suites + default-config boot test pass with zero test-body edits.
- The three structural proxies (gate order, FK 1-query slope, memo walk-once) are green.
- git-diff audit confirms behavior-frozen parity (additive test changes only).
</success_criteria>

<output>
Create `.planning/phases/17-mutation-internals-hardening-phase-11-follow-up/17-05-SUMMARY.md` when done.
</output>
