---
phase: 17-mutation-internals-hardening-phase-11-follow-up
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/MutationToolInvariantsTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/RelatedWriteMetadataMemoTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/fixture/MutationStoreLinkedParentFixture.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/fixture/MutationStoreLinkedChildFixture.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/performance/MutationFkBatchLoadQueryCountTest.java
autonomous: true
requirements: [MUT-15, MUT-16, MUT-17]
must_haves:
  truths:
    - "A source/reflection invariant test asserts the MutationGateChain gate order, save-after-all-gates, and @Transactional absence on the chain (red until Plan 04)."
    - "A source-scan invariant asserts the FK-load path in MutationAttributeBinder uses no UnconstrainedDataManager and no raw JPQL (red until Plan 03)."
    - "A pure-JUnit call-count test asserts the metamodel walk runs once per distinct (entity,relationship) key for supported AND unsupported keys (red until Plan 02)."
    - "A datasource-proxy SELECT-count test asserts the bulk FK load slope is ~0 as K grows 10->100 (red until Plan 03)."
  artifacts:
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/MutationToolInvariantsTest.java"
      provides: "Extended source/reflection invariants for MUT-15 gate-order + MUT-16 forbidden-token scan"
      contains: "MutationGateChain"
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/RelatedWriteMetadataMemoTest.java"
      provides: "MUT-17 walk-once call-count seam (pure JUnit + AtomicInteger)"
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/performance/MutationFkBatchLoadQueryCountTest.java"
      provides: "MUT-16 SELECT-count 1-query-not-N proxy (narrowed-boot + datasource-proxy)"
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/fixture/MutationStoreLinkedChildFixture.java"
      provides: "agentstore-backed @ManyToOne FK fixture so the agentstore-wrapping counting harness counts the FK .ids() SELECT (Open Q1 option a)"
      contains: "@Store"
  key_links:
    - from: "MutationFkBatchLoadQueryCountTest"
      to: "QueryCountingDataSourceConfiguration"
      via: "@Import + agentstoreDataSource wrap"
      pattern: "QueryCountingDataSourceConfiguration"
    - from: "RelatedWriteMetadataMemoTest"
      to: "RelatedWriteMetadataResolver.computeSupported"
      via: "package-private compute seam override + AtomicInteger"
      pattern: "AtomicInteger"
---

<objective>
Create the four structural test seams that prove the three refactors (MUT-15/16/17) "did something",
plus the agentstore FK fixture (Open Question 1 option a) the SELECT-count proxy requires. These tests
are written FIRST so the implementation in Plans 02/03/04 has explicit targets to satisfy.

This plan resolves Open Question 1 (SELECT-count target store) by locking option (a): add an
`@Store("agentstore")` FK fixture pair so the EXISTING agentstore-wrapping `QueryCountingDataSourceConfiguration`
counts the batch FK `.ids()` SELECT without widening the shared counting config (lower divergence per RESEARCH).

Purpose: Wave 0 seams. The new structural tests are intentionally RED until their implementing plan lands
(memo test red until 02, FK tests red until 03, gate-order test red until 04). They prove the refactor changed
structure; the unchanged Phase 9/10/11 suites (Plan 05) prove it changed nothing observable.
Output: 1 extended test, 2 new tests, 1 new agentstore FK fixture pair.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17-mutation-internals-hardening-phase-11-follow-up/17-SPEC.md
@.planning/phases/17-mutation-internals-hardening-phase-11-follow-up/17-CONTEXT.md
@.planning/phases/17-mutation-internals-hardening-phase-11-follow-up/17-PATTERNS.md
@.planning/phases/17-mutation-internals-hardening-phase-11-follow-up/17-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend MutationToolInvariantsTest with MUT-15 gate-order/reflection + MUT-16 forbidden-token invariants</name>
  <read_first>
    - ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/MutationToolInvariantsTest.java (the file being modified; house idiom: `Files.readString` + `indexOf` + `countOccurrences` lines 161-169, `resolveModuleRoot()` lines 176-195, `Files.walk(MUTATION_PACKAGE)` lines 68-72)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/tools/mutation/BuiltInMutationTools.java (current inline gate sequence — RESEARCH lines 232-243; the ordered token list the chain must mirror)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/tools/mutation/MutationSaveExecutor.java (sole @Transactional bean; the save token the chain delegates to)
    - 17-PATTERNS.md "Pattern: Pure-JUnit source/reflection invariant" (the exact assertion idiom) and "NEW MutationGateChain.java" section (D-14 reflection assertion)
    - 17-CONTEXT.md D-14 (gate-ORDER via strictly-increasing indexOf of distinct gate tokens; @Transactional absence by REFLECTION, not regex) and D-09 (constrained-DM only)
  </read_first>
  <action>
    Add new @Test methods to `MutationToolInvariantsTest`, reusing the existing `MODULE_ROOT`/`MUTATION_PACKAGE`
    fields, `countOccurrences` helper, and `resolveModuleRoot()`. Add a constant
    `MUTATION_GATE_CHAIN_FILE = MUTATION_PACKAGE.resolve("MutationGateChain.java")` and
    `MUTATION_ATTRIBUTE_BINDER_FILE = MUTATION_PACKAGE.resolve("MutationAttributeBinder.java")`.

    (a) `mutationGateChain_gatesAppearInCanonicalOrder`: read `MutationGateChain.java`; locate the
    `indexOf` of the eight distinct ordered gate-method-call tokens in the `execute` dispatch spine
    (`enforceRole`, `resolve`, `authorize`, `reserve`, `coerce`, `guard`, `save`, `finalize` — use the
    actual private-method invocation tokens Plan 04 commits, e.g. `enforceRole(`); assert each token's
    index is strictly greater than the previous (strictly-increasing). Guard the test against the file not
    yet existing by asserting `Files.exists(MUTATION_GATE_CHAIN_FILE)` first (this is the intentional RED
    until Plan 04).

    (b) `mutationGateChain_saveTokenAppearsAfterAllGateTokens`: assert `indexOf("mutationSaveExecutor.")`
    (the save/saveAll/bulkSave proxy-crossing call) is greater than the index of every gate token from (a)
    — proves fail-closed "every gate throws before the transactional save".

    (c) `mutationGateChain_carriesNoTransactionalAnnotation`: by REFLECTION (NOT regex per D-14) load
    `Class.forName("com.vn.agent.tools.mutation.MutationGateChain")`; assert
    `clazz.isAnnotationPresent(org.springframework.transaction.annotation.Transactional.class)` is false AND
    no declared method (`clazz.getDeclaredMethods()`) carries `@Transactional`. Catch `ClassNotFoundException`
    and `fail(...)` with a message naming Plan 04 as the producer (intentional RED until Plan 04).

    (d) `mutationAttributeBinder_fkPathUsesNoUnconstrainedDataManagerOrRawJpql`: read
    `MutationAttributeBinder.java`; assert `doesNotContain("UnconstrainedDataManager")` and
    `doesNotContain("loadValues(")` and `doesNotContain("loadValue(")` and `doesNotContain(".query(")`
    (raw-JPQL markers — the FK path must use only `dataManager.load(class).ids(...)` / `.id(...)`). This is
    GREEN today and must STAY green after Plan 03 (MUT-16 forbidden-token scan).

    Keep all existing test methods unchanged (they must continue to pass — MUT-18 parity).
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.tools.mutation.MutationToolInvariantsTest" ; echo "EXPECTED: existing tests + (d) GREEN; (a)(b)(c) RED until Plan 04 — confirm the failures name MutationGateChain.java"</automated>
  </verify>
  <acceptance_criteria>
    - `MutationToolInvariantsTest.java` source contains the literal token `MutationGateChain` and `MutationAttributeBinder` file references and four new @Test methods named exactly: `mutationGateChain_gatesAppearInCanonicalOrder`, `mutationGateChain_saveTokenAppearsAfterAllGateTokens`, `mutationGateChain_carriesNoTransactionalAnnotation`, `mutationAttributeBinder_fkPathUsesNoUnconstrainedDataManagerOrRawJpql`.
    - The @Transactional-absence test uses `Class.forName` + reflection (`isAnnotationPresent` / `getDeclaredMethods`), NOT a regex/string scan of the source (D-14).
    - `--tests "*MutationToolInvariantsTest"`: the four ORIGINAL methods pass; method (d) passes; methods (a)(b)(c) FAIL with a message that names `MutationGateChain.java`/`MutationGateChain` as the missing producer (Plan 04).
    - No edits to any Phase 9/10/11 mutation suite other than additive @Test methods on this invariant test (MUT-18).
  </acceptance_criteria>
  <done>The invariant test compiles, original assertions stay green, the MUT-16 forbidden-token scan is green, and the three MUT-15 assertions are red pointing at Plan 04.</done>
</task>

<task type="auto">
  <name>Task 2: New RelatedWriteMetadataMemoTest — pure-JUnit AtomicInteger walk-once seam (MUT-17)</name>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/tools/mutation/RelatedWriteMetadataResolver.java (the resolver to be memoized; `resolveSupportedRelatedWriteRelationship` lines 110-172, `SupportedRelatedRelationship` record lines 98-101, `unsupportedRelationship()` factory lines 307-314)
    - ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/RelatedWriteMetadataResolverTest.java (concrete-metamodel resolver test analog — how it obtains a real MetaClass + supported/unsupported relationship names)
    - ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/BuiltInMutationToolsBulkSavePartialFailureTest.java (AtomicInteger counting idiom analog, line ~193)
    - 17-CONTEXT.md D-13 (package-private compute/walk seam), D-16 (counting subclass/wrapper + AtomicInteger; no Spring, no Mockito), D-17 (no Mockito InOrder)
    - 17-PATTERNS.md "Pattern: AtomicInteger call-counting seam"
  </read_first>
  <action>
    Create `RelatedWriteMetadataMemoTest` (package `com.vn.agent.tools.mutation`, pure JUnit 5 + AssertJ, no
    Spring, no Mockito). The test wraps/overrides the package-private compute seam that Plan 02 will extract
    (named `computeSupported(MetaClass, String)` — coordinate the exact name with Plan 02; assume
    `computeSupported`). Define a test subclass `CountingResolver extends RelatedWriteMetadataResolver` that
    overrides `computeSupported(...)` to `walkCount.incrementAndGet()` then `super.computeSupported(...)`,
    where `walkCount` is an `AtomicInteger`.

    Obtain a real `MetaClass` + relationship name the same way `RelatedWriteMetadataResolverTest` does
    (concrete metamodel via the resolver test's existing setup — reuse the supported `(parent, relationshipName)`
    pair it uses for a supported case, and an unsupported relationshipName for the rejected case).

    Test `walkRunsOnceForRepeatedSupportedKey`: call `resolve(parent, "linkedChildren")` twice; assert
    `walkCount.get() == 1` and both returns are non-null `SupportedRelatedRelationship`.

    Test `walkRunsOnceForRepeatedUnsupportedKey`: call `resolve(parent, "<unsupportedName>")` twice; both
    calls must throw `ToolUserError` with code `validation_failed` (assert via `assertThatThrownBy`), and
    `walkCount.get() == 1` — proving the rejected outcome is cached AND that a FRESH `unsupportedRelationship()`
    is rethrown (D-12: never the cached throwable). Assert the two thrown instances are NOT the same object
    identity (`isNotSameAs`) to lock "fresh rethrow".

    Test `distinctKeysEachWalkOnce`: call two distinct supported/unsupported keys; assert `walkCount.get() == 2`.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.tools.mutation.RelatedWriteMetadataMemoTest" ; echo "EXPECTED: RED until Plan 02 — failures should name the missing computeSupported seam / lack of memoization"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `src/test/java/com/vn/agent/tools/mutation/RelatedWriteMetadataMemoTest.java`, pure JUnit 5 + AssertJ, zero Spring (`@SpringBootTest` absent), zero Mockito imports.
    - Contains a `CountingResolver` test subclass overriding the package-private `computeSupported` seam + an `AtomicInteger walkCount`.
    - Contains @Test methods: `walkRunsOnceForRepeatedSupportedKey`, `walkRunsOnceForRepeatedUnsupportedKey` (asserts `isNotSameAs` on the two thrown instances), `distinctKeysEachWalkOnce`.
    - `--tests "*RelatedWriteMetadataMemoTest"` FAILS today (compile error on the not-yet-extracted `computeSupported` seam, OR assertion `walkCount==1` fails because the un-memoized resolver walks twice) — the RED state is intentional and resolved by Plan 02.
  </acceptance_criteria>
  <done>The memo test compiles against the resolver's intended seam (or fails clearly naming the missing seam) and is red, pending Plan 02's memoization.</done>
</task>

<task type="auto">
  <name>Task 3: agentstore FK fixture pair + new MutationFkBatchLoadQueryCountTest (Open Q1 option a, MUT-16)</name>
  <read_first>
    - ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/fixture/MutationLinkedChildFixture.java (the @ManyToOne FK shape to mirror — copy structure, add @Store("agentstore"))
    - ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/fixture/MutationLinkedParentFixture.java (parent target shape to mirror)
    - ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/MutationFixturePersistenceTestConfiguration.java (@JmixModule test-module registration pattern)
    - ai-agent/ai-agent/src/test/java/com/vn/agent/performance/ToolQueryCountBaselineTest.java (narrowed-boot recipe lines 50-58; `safeSelectCount()` lines 101-104; `measureSteadyStateSelects` lines 113-118; the N-scaling slope probe lines 265-295)
    - ai-agent/ai-agent/src/test/java/com/vn/agent/performance/QueryCountingDataSourceConfiguration.java (wraps `agentstoreDataSource` ONLY — lines 40, 47; this is WHY the FK fixture must be @Store("agentstore"))
    - ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/MutationToolTestUsersConfiguration.java (test users for mutation gating — import per RESEARCH A3)
    - 17-RESEARCH.md Open Question 1 (agentstore-vs-main store decision; option (a) locked here) and A1/A3 (the .ids() Collection overload — VERIFIED present in jmix-core 2.8.1: `FluentLoader.ids(java.util.Collection<?>)` — and the narrowed-boot requirement)
    - 17-PATTERNS.md "Pattern: Narrowed-boot SELECT-count harness" + "Pattern: Test-only Jmix module for fixture persistence"
  </read_first>
  <action>
    Resolve Open Question 1 as option (a): add an agentstore-backed FK fixture so the existing
    agentstore-wrapping counting harness counts the batch FK SELECT without touching the shared
    `QueryCountingDataSourceConfiguration`.

    (1) Create `MutationStoreLinkedParentFixture` (package `...tools.mutation.fixture`): mirror
    `MutationLinkedParentFixture` exactly (UUID @JmixGeneratedValue id, @Version, @InstanceName String name)
    but add `@Store("agentstore")` (io.jmix.core.metamodel.annotation.Store) and a distinct
    `@Entity(name="mutationTest_MutationStoreLinkedParentFixture")` + `@Table(name="MUTATION_STORE_LINKED_PARENT_FIXTURE")`.
    Omit the `@OneToMany` collection (not needed — the FK fan-out is on the child side).

    (2) Create `MutationStoreLinkedChildFixture`: mirror `MutationLinkedChildFixture` exactly (id, @Version,
    @InstanceName label, `@ManyToOne(fetch=LAZY) @JoinColumn(name="STORE_LINKED_PARENT_ID")
    MutationStoreLinkedParentFixture storeLinkedParent`) but add `@Store("agentstore")` and distinct
    entity/table names (`MUTATION_STORE_LINKED_CHILD_FIXTURE`). The FK must be a writable to-one (NOT
    composition) so `bulk_save_records` can set it through `coerceAttributes`.

    Register both fixtures for agentstore persistence — confirm whether `MutationFixturePersistenceTestConfiguration`'s
    @JmixModule already sweeps the fixture package for agentstore entities; if it contributes only the main
    store, add the agentstore mapping the same way the agentstore audit entities are contributed (follow the
    existing agentstore store-configuration pattern referenced in STATE.md "dataManager.load(EntityClass).query()
    auto-resolves store from @Store"). Do NOT alter main-store fixture wiring.

    (3) Create `MutationFkBatchLoadQueryCountTest` (package `com.vn.agent.performance`): copy the
    `ToolQueryCountBaselineTest` narrowed-boot recipe VERBATIM (`@SpringBootTest(classes=AITestConfiguration.class)`
    + `@ImportAutoConfiguration({AIAutoConfiguration, SpiDefaultsAutoConfiguration, AiToolsAutoConfiguration})`
    + `@Import({StubChatModelConfiguration, StubVectorStoreConfiguration, QueryCountingDataSourceConfiguration})`).
    Add `@Import(MutationToolTestUsersConfiguration.class)` and the property `ai-agent.tools.mutation.enabled=true`
    via `@SpringBootTest(properties=...)` (as the gating tests do). Reuse `safeSelectCount()` /
    `measureSteadyStateSelects` / the `QueryCountHolder.clear()` `@BeforeEach`.

    Seed K `MutationStoreLinkedChildFixture` rows each referencing the SAME `MutationStoreLinkedParentFixture`
    via `systemAuthenticator.withUser(...)`. Build a `bulk_save_records` call for a K-row batch where every row
    sets `storeLinkedParent` to that one parent id. Write the slope probe: measure SELECTs for K=10 vs K=100,
    assert `countLarge - countSmall <= 1` (constant — one FK SELECT per target class, NOT K). This is the
    "1 query not N" proxy.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.performance.MutationFkBatchLoadQueryCountTest" ; echo "EXPECTED: compiles + boots (or surfaces the known module boot regression); slope assertion RED until Plan 03 because today's per-reference path issues K FK SELECTs"</automated>
  </verify>
  <acceptance_criteria>
    - `MutationStoreLinkedParentFixture.java` and `MutationStoreLinkedChildFixture.java` exist under `.../tools/mutation/fixture/`, each carries `@Store("agentstore")` and a `@ManyToOne` FK on the child to the parent (writable, non-composition).
    - The agentstore FK fixtures are registered for persistence without altering main-store fixture wiring (`MutationLinkedChildFixture`/`MutationLinkedParentFixture` unchanged).
    - `MutationFkBatchLoadQueryCountTest.java` exists under `com.vn.agent.performance`, uses the narrowed-boot recipe (NOT bare full-autoconfig `@SpringBootTest`), imports `QueryCountingDataSourceConfiguration` + `MutationToolTestUsersConfiguration`, sets `ai-agent.tools.mutation.enabled=true`, and contains a K=10-vs-K=100 slope probe asserting `countLarge - countSmall <= 1`.
    - `--tests "*MutationFkBatchLoadQueryCountTest"`: the slope assertion FAILS today (per-reference path issues ~K FK SELECTs → slope ≈ 90), intentionally RED until Plan 03. If the known atmosphere-runtime boot regression blocks the @SpringBootTest, record it in the SUMMARY exactly as Phase 13.1 Plan 06 did and keep the test compileTestJava-green and source-correct.
  </acceptance_criteria>
  <done>The agentstore FK fixture pair exists and is registered, the SELECT-count test compiles against the narrowed-boot recipe, and its slope assertion is red pending Plan 03's batch load.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → agentstore | New @Store("agentstore") fixtures persist via the constrained test datasource; they are test-only and never ship in the addon jar (test source tree). |
| invariant/scan tests → production source | Source/reflection tests read production `.java` files read-only; no mutation of runtime behavior. |

This plan adds NO production code — it is test seams only. The security-relevant threat is that a WEAK seam
fails to catch a future regression of the fail-closed gates.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | MUT-15 gate-order invariant | mitigate | The reflection-based @Transactional-absence check + strictly-increasing-indexOf gate-order assertion catch a future reordering that lets a save occur before role/exposure/AccessManager/idempotency checks. Asserts "save token after ALL gate tokens" so fail-closed ordering is structurally enforced (D-14). |
| T-17-02 | Information Disclosure / Elevation | MUT-16 forbidden-token scan | mitigate | `mutationAttributeBinder_fkPathUsesNoUnconstrainedDataManagerOrRawJpql` fails the build if the FK batch path ever switches to `UnconstrainedDataManager`/raw JPQL (`loadValue`/`loadValues`/`.query(`), which would bypass row-level security (D-09). |
| T-17-03 | Elevation | MUT-17 memo (permission-independence) | accept | The memo key is `(parentEntityName, relationshipName)` over the IMMUTABLE Jmix metamodel — relationship-support is a pure metamodel fact, security-independent (no AccessManager/exposure decision is cached). The memo test confirms walk-once; it deliberately does NOT vary security context because none is consulted. Documented as security-independent per the phase threat-model requirement. |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (no new dependencies — SPEC constraint); `datasource-proxy`/JUnit/AssertJ already `testImplementation`. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.tools.mutation.MutationToolInvariantsTest"` — original methods green, MUT-16 scan green, three MUT-15 assertions red (name Plan 04).
- `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.tools.mutation.RelatedWriteMetadataMemoTest"` — red (names the missing memo seam; Plan 02).
- `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.performance.MutationFkBatchLoadQueryCountTest"` — compiles; slope assertion red (Plan 03) OR documents the known boot regression like Phase 13.1 Plan 06.
- `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.tools.mutation.*"` — every PRE-EXISTING Phase 9/10/11 mutation test still green (no parity regression introduced by additive test code).
</verification>

<success_criteria>
- Four seams exist (1 extended invariant test, 1 memo test, 1 FK SELECT-count test, 1 agentstore FK fixture pair).
- Open Question 1 resolved in source: agentstore FK fixture (option a), shared counting config untouched.
- Each new structural assertion is RED in a way that names its implementing plan (02/03/04).
- Zero edits to existing Phase 9/10/11 mutation test bodies (additive @Test methods on the invariant test are allowed and required by MUT-15).
</success_criteria>

<output>
Create `.planning/phases/17-mutation-internals-hardening-phase-11-follow-up/17-01-SUMMARY.md` when done.
</output>
