---
phase: 07.2-redesign-audit-schema-tree-lite
plan: 04
type: execute
wave: 4
depends_on: [07.2-01]
files_modified:
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/audit/AiAuditEventListView.java
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/view/audit/ai-audit-event-list-view.xml
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/audit/AiAuditEventDetailDialog.java
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/view/audit/ai-audit-event-detail-dialog.xml
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/menu.xml
  - ai-agent/ai-agent/src/main/java/com/vn/agent/security/AiAgentAdminRole.java
autonomous: true
requirements: [AUD-03]
tags: [jmix-flowui, tree-grid, view-rename, i18n]
must_haves:
  truths:
    - "/ai-agent/audit URL renders a tree: root CHAT rows expand to show TOOL + RETRIEVAL children"
    - "Detail dialog surfaces all new retrieval columns (queryText, topK, hitCount, topScore, filtersJson)"
    - "AiAgent_AiAuditEvent.list is the registered view id — AiAgent_ToolCallAudit.list is gone"
    - "menu.xml + AiAgentAdminRole reference the new view id"
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/view/audit/AiAuditEventListView.java"
      provides: "Flow UI list view for AiAuditEvent with tree rendering"
      contains: "StandardListView<AiAuditEvent>"
    - path: "ai-agent/ai-agent/src/main/resources/com/vn/agent/view/audit/ai-audit-event-list-view.xml"
      provides: "XML descriptor with hierarchical data container + children fetchPlan"
      contains: "children"
    - path: "ai-agent/ai-agent/src/main/resources/com/vn/agent/menu.xml"
      provides: "Menu entry pointing to AiAgent_AiAuditEvent.list"
      contains: "AiAgent_AiAuditEvent.list"
  key_links:
    - from: "menu.xml ai-agent.audit.list"
      to: "AiAgent_AiAuditEvent.list view controller"
      via: "view id attribute"
      pattern: "AiAgent_AiAuditEvent.list"
    - from: "AiAgentAdminRole @SpecificPolicy"
      to: "AiAgent_AiAuditEvent.list"
      via: "resources array entry"
      pattern: "AiAgent_AiAuditEvent.list"
    - from: "ai-audit-event-list-view.xml dataGrid"
      to: "AiAuditEvent.children @Composition"
      via: "fetchPlan + hierarchyProperty children"
      pattern: "hierarchyProperty=\"children\"|property name=\"children\""
---

<objective>
Rename the existing `ToolCallAuditListView` + `ToolCallAuditDetailDialog` (Java + XML pairs) to `AiAuditEventListView` / `AiAuditEventDetailDialog` and switch the list grid to tree rendering via Jmix `@Composition` fetch plan (D-11). Update `menu.xml` + `AiAgentAdminRole` single-attribute references. Preserve the user-facing URL `/ai-agent/audit` (stakeholder-facing — MUST NOT change).

Purpose: Surface the tree-lite audit schema (Plan 01 + 02) through the existing admin list view without breaking the URL or the role catalog. Implements D-11 literally (Jmix-first XML over raw Vaadin, `@Subscribe` wiring, `@Supply` renderers preserved).
Output: Admin-gated tree grid showing one root per chat turn, expandable to TOOL + RETRIEVAL children; detail dialog exposes the new retrieval columns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.2-redesign-audit-schema-tree-lite/07.2-CONTEXT.md
@.planning/phases/07.2-redesign-audit-schema-tree-lite/07.2-RESEARCH.md
@.planning/phases/07.2-redesign-audit-schema-tree-lite/07.2-PATTERNS.md
@.planning/phases/07.2-redesign-audit-schema-tree-lite/07.2-01-SUMMARY.md
@CLAUDE.md

<interfaces>
Memory notes governing Jmix UI (MUST follow):
- feedback_jmix_first_ui: Jmix XML over raw Vaadin.
- feedback_jmix_view_listeners: @Subscribe / @Install for all wiring.
- feedback_jmix_datagrid_renderer: @Supply(to="grid.col", subject="renderer") + UiComponents.create; NEVER programmatic setRenderer in onInit.
- feedback_jmix_action_column_renderer: per-row icon actions via @Supply renderer + DataGridRenderers.buildActionsColumn + EnumSet<ActionColumnType>.
- feedback_jmix_messages_over_spring: io.jmix.core.Messages (NOT Spring MessageSource); root-bundle messages only.
- feedback_jmix_query_parameters_event: Jmix View.QueryParametersChangeEvent + getSingleParameter for query-param reading (not Vaadin BeforeEnterObserver).
- feedback_jmix_list_item_tracking: list_itemTracking action inside <dataGrid> + action="grid.id" button binding (not addSelectionListener).

From Plan 01 — AiAuditEvent entity (fields available to the view):
- parent, children (tree), conversation, userUsername, runId, kind, eventName, outcome, startedAt, finishedAt, latencyMs, queryText, topK, hitCount, topScore, filtersJson, argumentsJson, resultSummary, denialReason, errorClass, promptHash.

From Plan 01 — i18n keys already defined in messages_en.properties + messages_vi.properties:
- `com.vn.agent.entity/AiAuditEvent.*`
- `auditList.column.kind`, `auditList.column.eventName`
- `auditList.detail.kind`, `.eventName`, `.queryText`, `.topK`, `.hitCount`, `.topScore`, `.filtersJson`
- KEPT unchanged: `auditList.title`, `auditList.column.startedAt`, `auditList.column.outcome`, etc.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rename + rewrite AiAuditEventListView (Java + XML) with hierarchical grid</name>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/audit/ToolCallAuditListView.java (full file — all current @ViewComponent/@Subscribe/@Supply wiring that must be preserved verbatim except for entity reference + column property names)
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/view/audit/tool-call-audit-list-view.xml (full file — data container, filters, export bar, grid)
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/*.xml (or any existing @Composition fetchPlan analog in the repo — grep for `<property name=` inside `<fetchPlan` to find patterns)
    - .planning/phases/07.2-redesign-audit-schema-tree-lite/07.2-PATTERNS.md §view/audit/AiAuditEventListView.java + §view/audit/ai-audit-event-list-view.xml
    - .planning/phases/07.2-redesign-audit-schema-tree-lite/07.2-RESEARCH.md §Example 4 lines 623-663 + §Pattern 1 lines 268-272 (fetch plan one level deep)
  </read_first>
  <behavior>
    - `/ai-agent/audit` URL still resolves (Route unchanged — stakeholder-facing URL preserved).
    - View id is `AiAgent_AiAuditEvent.list`.
    - Tree grid renders one expandable row per root CHAT event; expand reveals TOOL + RETRIEVAL children sorted by startedAt.
    - Existing filter bar + row-click detail-dialog opener + outcome-badge renderer + action-column renderer all continue to work.
    - Base query fetches ROOTS ONLY: `select e from ai_AiAuditEvent e where e.parent is null`.
  </behavior>
  <action>
    **1a. Create Java controller `ai-agent/ai-agent/src/main/java/com/vn/agent/view/audit/AiAuditEventListView.java`** by copying `ToolCallAuditListView.java` verbatim and applying the following changes (PATTERNS §view/audit/AiAuditEventListView.java lines 642-683):

    - Class name: `ToolCallAuditListView` → `AiAuditEventListView`.
    - Generic: `StandardListView<AiToolCallAudit>` → `StandardListView<AiAuditEvent>`.
    - Annotations:
      ```java
      @Route(value = "ai-agent/audit", layout = DefaultMainViewParent.class)   // URL PRESERVED — stakeholder-facing
      @ViewController(id = "AiAgent_AiAuditEvent.list")
      @ViewDescriptor(path = "ai-audit-event-list-view.xml")
      ```
    - Import swap: `com.vn.agent.entity.AiToolCallAudit` → `com.vn.agent.entity.AiAuditEvent`.
    - Update the `BASE_QUERY` constant (current line 70-73 per PATTERNS) to root-only:
      ```java
      private static final String BASE_QUERY = "select e from ai_AiAuditEvent e where e.parent is null";
      ```
    - Update the distinct-event-name query used by the filter combo loader (current line 177 per PATTERNS):
      ```java
      "select distinct e.eventName from ai_AiAuditEvent e where e.eventName is not null order by e.eventName"
      ```
    - Field/property renames in JPQL and wherever the view references the old column:
      - `e.toolName` → `e.eventName`
      - Any reference to `e.phase` — DELETE entirely (phase column is gone).
      - If a `toolFilter` @ViewComponent / @Subscribe exists, rename the method pair's binding to `eventNameFilter` OR keep the Java field name `toolFilter` if the XML id stays `toolFilter` (minimize churn); at minimum the XML `@ComboBoxLoader` query and JPQL property name swap to `eventName`.
    - PRESERVE verbatim:
      - All `@ViewComponent` injections.
      - Row-click detail-dialog opener (`@Subscribe("auditsDataGrid") DataGrid.ItemClickEvent`).
      - `@Supply(to="auditsDataGrid.outcome", subject="renderer")` outcome-badge renderer.
      - `@Supply` action-column renderer using `DataGridRenderers.buildActionsColumn` + `EnumSet<ActionColumnType>` per `feedback_jmix_action_column_renderer`.
      - Export-bar, genericFilter, filter-change subscribers (`@Subscribe("toolFilter") onToolFilterChange`, etc.).
      - `io.jmix.core.Messages` injection (per `feedback_jmix_messages_over_spring`).

    **1b. DELETE** the old `ToolCallAuditListView.java` after copying — the new file replaces it entirely (no thin-wrapper pattern per CONTEXT.md §Specifics).

    **1c. Create XML descriptor `ai-agent/ai-agent/src/main/resources/com/vn/agent/view/audit/ai-audit-event-list-view.xml`** by copying `tool-call-audit-list-view.xml` verbatim and applying (PATTERNS §view/audit/ai-audit-event-list-view.xml lines 707-720 + RESEARCH §Example 4):

    - Title: unchanged (`msg:///auditList.title`).
    - Data container class: `com.vn.agent.entity.AiToolCallAudit` → `com.vn.agent.entity.AiAuditEvent`.
    - Fetch plan: MUST include `children` one level deep (D-11 — tree rendering via Jmix @Composition fetch plan, no client-side grouping):
      ```xml
      <fetchPlan extends="_base">
          <property name="children" fetchPlan="_base"/>
      </fetchPlan>
      ```
      Do NOT nest `children` recursively — children of children are not rendered in v1 (current kinds are all leaf under CHAT, per CONTEXT deferred idea "Deeper trees").
    - Loader query: `select e from ai_AiAuditEvent e where e.parent is null order by e.startedAt desc`.
    - Grid element: switch `<dataGrid>` to a hierarchical tree grid per RESEARCH §Example 4. A4 ASSUMED — Jmix 2.8 Flow UI `<treeDataGrid>` with `hierarchyProperty="children"`:
      ```xml
      <treeDataGrid id="auditsDataGrid"
                    dataContainer="auditsDc"
                    hierarchyProperty="children"
                    width="100%" minHeight="20em" themeNames="compact">
          <actions>
              <!-- list_itemTracking action preserved verbatim from old XML -->
          </actions>
          <columns resizable="true">
              <column property="startedAt" header="msg:///auditList.column.startedAt"/>
              <column property="kind" header="msg:///auditList.column.kind"/>
              <column property="eventName" header="msg:///auditList.column.eventName"/>
              <column key="outcome" header="msg:///auditList.column.outcome"/>
              <column property="latencyMs" header="msg:///auditList.column.latencyMs"/>
              <column key="actions" header="msg:///common.column.actions"/>
          </columns>
      </treeDataGrid>
      ```
      **A4 fallback if `<treeDataGrid>` does not exist in Jmix 2.8 Flow UI schema:** verify by attempting a `./gradlew :jmix-app:bootRun` and reading the XSD/startup log. If the element is rejected, fall back to raw Vaadin `TreeGrid` programmatically wired in the Java controller with a Jmix `HierarchicalContainer`/`ContainerHierarchicalDataProvider` — note explicit divergence from `feedback_jmix_first_ui` in 07.2-04-SUMMARY.md with WebFetch evidence from https://docs.jmix.io/jmix/flow-ui/vc/components/dataGrid.html. Preferred resolution order: (i) `<treeDataGrid>`, (ii) `<dataGrid>` with `<hierarchyConfig>`, (iii) raw Vaadin TreeGrid.
    - Drop entirely the `<column property="toolName" .../>` and `<column property="phase" .../>` entries.
    - Filter bar: keep the existing filter fields. If a `toolFilter` combo exists, rename its label key to `auditList.filter.eventName` OR keep `auditList.filter.tool` if the label stays (planner's call — minimal-churn option: keep id `toolFilter`, change its underlying JPQL condition column from `e.toolName` to `e.eventName`). Drop any `phaseFilter` field entirely.
    - Action-column `key="actions"` and `@Supply` renderer (`feedback_jmix_action_column_renderer`) preserved verbatim — the renderer is on the Java side.

    **1d. DELETE** the old `tool-call-audit-list-view.xml` file.
  </action>
  <verify>
    <automated>grep -c "com.vn.agent.entity.AiAuditEvent" ai-agent/ai-agent/src/main/resources/com/vn/agent/view/audit/ai-audit-event-list-view.xml</automated>
  </verify>
  <done>
    `AiAuditEventListView.java` exists with `@ViewController(id = "AiAgent_AiAuditEvent.list")` and `extends StandardListView<AiAuditEvent>`. XML contains `<property name="children"` inside `<fetchPlan>`. XML contains `<treeDataGrid hierarchyProperty="children"` OR (fallback) documented divergence in SUMMARY. Old Java + XML deleted. `./gradlew :ai-agent:ai-agent:compileJava` succeeds. JPQL queries in Java + XML reference `ai_AiAuditEvent` and `e.parent is null`; zero references to `ai_AiToolCallAudit` or `e.phase`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rename + rewrite AiAuditEventDetailDialog (Java + XML) with retrieval fields</name>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/audit/ToolCallAuditDetailDialog.java (full file)
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/view/audit/tool-call-audit-detail-dialog.xml (full file)
    - .planning/phases/07.2-redesign-audit-schema-tree-lite/07.2-PATTERNS.md §view/audit/AiAuditEventDetailDialog.java + .xml (lines 744-759)
  </read_first>
  <behavior>
    - Dialog id is `AiAgent_AiAuditEvent.detailDialog`.
    - Opened by the list-view row-click handler.
    - For CHAT rows, displays: kind, eventName (likely empty), runId, userUsername, conversationId, promptHash, startedAt, finishedAt, latencyMs, outcome, errorClass.
    - For TOOL rows, displays: parent reference, kind, eventName (tool name), argumentsJson, resultSummary, outcome, denialReason, latencyMs, errorClass.
    - For RETRIEVAL rows, displays: parent reference, kind, queryText, topK, hitCount, topScore, filtersJson, latencyMs, outcome, errorClass.
    - Field visibility need NOT be conditional on `kind` — unpopulated fields render empty and are inoffensive (low-complexity; follow existing pattern of always showing all fields).
  </behavior>
  <action>
    **2a. Create Java controller `AiAuditEventDetailDialog.java`** by copying `ToolCallAuditDetailDialog.java` verbatim and applying:

    - Class name: `ToolCallAuditDetailDialog` → `AiAuditEventDetailDialog`.
    - Extends: `StandardView` (unchanged).
    - Annotations (PATTERNS lines 747-753):
      ```java
      @ViewController("AiAgent_AiAuditEvent.detailDialog")
      @ViewDescriptor("ai-audit-event-detail-dialog.xml")
      @DialogMode(width = "var(--lumo-size-xl-plus, 960px)", draggable = true, resizable = true, closeOnEsc = true)
      public class AiAuditEventDetailDialog extends StandardView { ... }
      ```
    - Replace every import/reference `AiToolCallAudit` → `AiAuditEvent`.
    - In the `setAudit(AiAuditEvent audit)` / `@Subscribe View.InitEvent` method body (current lines 63-76 per PATTERNS):
      - Drop the `phaseField.setValue(...)` binding.
      - Keep `kindField`, `eventNameField` (rename `toolField` → `eventNameField` if a `toolField` exists), `runIdField`, `conversationField`, `userUsernameField`, `promptHashField`, `startedAtField`, `finishedAtField`, `latencyMsField`, `outcomeField`, `errorClassField`, `argumentsJsonField`, `resultSummaryField`, `denialReasonField`.
      - Add NEW bindings (all using `Objects.toString(audit.getXxx(), "")` pattern observed at PATTERNS line 65-74):
        ```java
        if (queryTextField != null) queryTextField.setValue(Objects.toString(audit.getQueryText(), ""));
        if (topKField != null) topKField.setValue(Objects.toString(audit.getTopK(), ""));
        if (hitCountField != null) hitCountField.setValue(Objects.toString(audit.getHitCount(), ""));
        if (topScoreField != null) topScoreField.setValue(Objects.toString(audit.getTopScore(), ""));
        if (filtersJsonField != null) filtersJsonField.setValue(Objects.toString(audit.getFiltersJson(), ""));
        if (parentField != null) parentField.setValue(audit.getParent() == null ? "" : audit.getParent().getId().toString());
        ```
    - Preserve VERBATIM the outcome-badge @Supply renderer (lines 78-101 per PATTERNS) + close button.

    **2b. Create XML `ai-audit-event-detail-dialog.xml`** by copying `tool-call-audit-detail-dialog.xml` verbatim and:
    - Drop `<field id="phaseField" …/>` or equivalent entirely.
    - Rename `<field id="toolField" …/>` → `<field id="eventNameField" …/>` (or keep id, update the label key to `msg:///auditList.detail.eventName`).
    - Add new read-only fields (one `<textField readOnly="true">` or `<textArea>` per new attribute — mirror the existing field pattern):
      ```xml
      <textField id="kindField" label="msg:///auditList.detail.kind" readOnly="true"/>
      <textField id="eventNameField" label="msg:///auditList.detail.eventName" readOnly="true"/>
      <textField id="parentField" label="msg:///com.vn.agent.entity/AiAuditEvent.parent" readOnly="true"/>
      <textArea id="queryTextField" label="msg:///auditList.detail.queryText" readOnly="true" minRows="2"/>
      <textField id="topKField" label="msg:///auditList.detail.topK" readOnly="true"/>
      <textField id="hitCountField" label="msg:///auditList.detail.hitCount" readOnly="true"/>
      <textField id="topScoreField" label="msg:///auditList.detail.topScore" readOnly="true"/>
      <textArea id="filtersJsonField" label="msg:///auditList.detail.filtersJson" readOnly="true" minRows="2"/>
      ```
      Layout the new fields in a section (or group) below the existing ones — preserve the existing `<formLayout>` / `<vbox>` structure.

    **2c. DELETE** the old Java + XML files.
  </action>
  <verify>
    <automated>grep -c "queryTextField" ai-agent/ai-agent/src/main/resources/com/vn/agent/view/audit/ai-audit-event-detail-dialog.xml</automated>
  </verify>
  <done>
    Dialog Java + XML exist under new names with @ViewController id `AiAgent_AiAuditEvent.detailDialog`. XML contains `queryTextField`, `topKField`, `hitCountField`, `topScoreField`, `filtersJsonField`, `parentField`. Zero references to `phaseField` or `AiToolCallAudit`. Old files deleted. Compile succeeds.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update menu.xml + AiAgentAdminRole — single-attribute rename</name>
  <read_first>
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/menu.xml line 13 (existing view attribute)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/security/AiAgentAdminRole.java line 43 (adminViews() @SpecificPolicy resources array)
    - .planning/phases/07.2-redesign-audit-schema-tree-lite/07.2-PATTERNS.md §security/AiAgentAdminRole.java + §menu.xml
  </read_first>
  <behavior>
    - menu.xml still has an entry with id `ai-agent.audit.list` (menu id unchanged — stakeholder-facing key preserved).
    - The `view=` attribute now points to `AiAgent_AiAuditEvent.list`.
    - AiAgentAdminRole @SpecificPolicy resources array contains `"AiAgent_AiAuditEvent.list"` and NOT `"AiAgent_ToolCallAudit.list"`.
  </behavior>
  <action>
    **3a. Edit `menu.xml`** — find the line where `view="AiAgent_ToolCallAudit.list"` appears (PATTERNS cites line 13). Change ONLY that single attribute value to `view="AiAgent_AiAuditEvent.list"`. Keep:
    - `id="ai-agent.audit.list"` (menu id unchanged).
    - The existing message-bundle key for the menu label.

    **3b. Edit `AiAgentAdminRole.java`** — find the `@SpecificPolicy(resources = {...})` annotation at line 43 (PATTERNS §security/AiAgentAdminRole.java). Change the single array entry `"AiAgent_ToolCallAudit.list"` → `"AiAgent_AiAuditEvent.list"`. Keep all other entries in the resources array untouched.

    Verification: `grep -rn "AiAgent_ToolCallAudit" ai-agent/` MUST return zero matches in `main/` (test sourceSet may still have one hit until Plan 05 closes).
  </action>
  <verify>
    <automated>grep "AiAgent_AiAuditEvent.list" ai-agent/ai-agent/src/main/resources/com/vn/agent/menu.xml ai-agent/ai-agent/src/main/java/com/vn/agent/security/AiAgentAdminRole.java</automated>
  </verify>
  <done>
    menu.xml contains `AiAgent_AiAuditEvent.list`. AiAgentAdminRole.java @SpecificPolicy contains `AiAgent_AiAuditEvent.list`. Zero `AiAgent_ToolCallAudit` references in `ai-agent/ai-agent/src/main/`. compile succeeds.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin user vs audit log contents | New queryText + filtersJson columns contain user-originated data; access gated by AiAgentAdminRole |
| Route / view id | Stakeholder-facing `/ai-agent/audit` URL must remain valid post-rename |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.2-12 | Elevation of Privilege | Non-admin user accesses audit list | mitigate | AiAgentAdminRole @SpecificPolicy updated to the new view id in Task 3; existing entity/attribute policies from Phase 2 UI-10 cover row-level visibility (unchanged). |
| T-07.2-13 | Information Disclosure | queryText/filtersJson leak to non-admin users if view id rename is botched | mitigate | Task 3 acceptance criterion: grep confirms NEW view id in role AND removal of old view id — no dangling reference that would permit a non-admin access path. |
| T-07.2-14 | Denial (broken URL) | Stakeholder link to /ai-agent/audit returns 404 after rename | mitigate | `@Route(value = "ai-agent/audit")` preserved verbatim in Task 1; URL-preservation called out explicitly. |
</threat_model>

<verification>
- `./gradlew :ai-agent:ai-agent:compileJava` succeeds.
- `grep -rn "AiAgent_ToolCallAudit\|AiToolCallAudit\|tool-call-audit" ai-agent/ai-agent/src/main` returns zero matches.
- The XML descriptor contains a `<fetchPlan>` with a nested `<property name="children" …/>`.
- Manual verification path (optional smoke, not required for plan completion): `./gradlew :jmix-app:bootRun` + login as admin + navigate to `/ai-agent/audit` + verify one expandable CHAT row per chat turn + click row opens the detail dialog showing retrieval-specific fields when clicking a RETRIEVAL child. If `<treeDataGrid>` element is unsupported (A4 fallback), grid renders flat and SUMMARY documents the divergence.
</verification>

<success_criteria>
- Phase 7.2 success criterion #4 ("Jmix list view /ai-agent/audit renders tree via @Composition fetch plan — no client-side grouping") SATISFIED.
- AUD-03: all schema fields (now including queryText/topK/hitCount/topScore/filtersJson) are surfaced in the detail dialog.
- URL preserved.
- Admin role updated.
</success_criteria>

<output>
Create .planning/phases/07.2-redesign-audit-schema-tree-lite/07.2-04-SUMMARY.md documenting: (1) resolved Jmix tree-grid element name (`<treeDataGrid>` success, OR A4 fallback with Vaadin TreeGrid + justification), (2) list of deleted files, (3) list of new view ids, (4) grep evidence of zero legacy `AiAgent_ToolCallAudit` references in `src/main`.
</output>
