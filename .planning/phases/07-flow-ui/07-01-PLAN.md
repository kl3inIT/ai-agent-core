---
phase: 07-flow-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-agent/ai-agent/ai-agent.gradle
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/MarkdownRenderer.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/push/AiAgentAppShell.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/push/DocumentStatusChangedEvent.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/StreamingEvent.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/security/AiAgentAdminRole.java
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/menu.xml
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties
autonomous: true
requirements: [UI-08, UI-09, UI-10]
tags: [jmix, flowui, vaadin, push, markdown, i18n, security-role]
must_haves:
  truths:
    - "Flexmark + OWASP sanitizer + gridexport + spring-boot-starter-cache dependencies resolve at build time"
    - "MarkdownRenderer bean converts untrusted markdown to sanitized HTML (no <script>, no javascript: URIs)"
    - "Vaadin @Push is activated globally by ai-agent add-on under a Spring-conditional switch (jmix.ai-agent.flowui.push-autoconfigure)"
    - "DocumentStatusChangedEvent Java record exists for ingestion-status fan-out"
    - "StreamingEvent sealed interface carries Content/ToolCall/ToolResult/Citation/Final/Error variants"
    - "AiAgentAdminRole gains adminViews() method with @MenuPolicy + @ViewPolicy for Parameters/KB/Audit"
    - "menu.xml enumerates 5 aiAgent.* items under the existing AI root, each with msg:// title"
    - "messages.properties + messages_vi.properties have identical keys covering every UI-SPEC copywriting key (en+vi parity)"
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/MarkdownRenderer.java"
      provides: "Thread-safe Flexmark + OWASP pipeline; toSafeHtml(String) -> sanitized HTML"
      contains: "Sanitizers.FORMATTING"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/push/AiAgentAppShell.java"
      provides: "AppShellConfigurator implementing @Push(transport = Transport.WEBSOCKET_XHR)"
      contains: "WEBSOCKET_XHR"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/push/DocumentStatusChangedEvent.java"
      provides: "Spring ApplicationEvent record (documentId, AiKnowledgeDocumentStatus)"
      contains: "record DocumentStatusChangedEvent"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/StreamingEvent.java"
      provides: "sealed interface StreamingEvent permits Content, ToolCall, ToolResult, Citation, Final, Error"
      contains: "sealed interface StreamingEvent"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/security/AiAgentAdminRole.java"
      provides: "Existing interface extended with adminViews() @MenuPolicy + @ViewPolicy"
      contains: "adminViews"
    - path: "ai-agent/ai-agent/src/main/resources/com/vn/agent/menu.xml"
      provides: "aiAgent.* namespaced menu items (chat, conversations, parameters.list, knowledge.list, audit.list)"
      contains: "aiAgent.chat"
    - path: "ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties"
      provides: "Full UI-SPEC key coverage (chatView.*, conversationList.*, parametersDetail.*, knowledgeBase.*, auditList.*, menu.*)"
      contains: "chatView.title"
    - path: "ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties"
      provides: "Vietnamese parity for every key in messages.properties"
      contains: "chatView.title"
  key_links:
    - from: "ai-agent/ai-agent/ai-agent.gradle"
      to: "com.vladsch.flexmark:flexmark:0.64.8, com.googlecode.owasp-java-html-sanitizer:owasp-java-html-sanitizer:20220608.1, io.jmix.gridexport:jmix-gridexport-flowui-starter"
      via: "implementation dependencies in a dedicated // Plan 07-01 block"
      pattern: "flexmark|owasp-java-html-sanitizer|gridexport"
    - from: "ai-agent/ai-agent/src/main/resources/com/vn/agent/menu.xml"
      to: "AiAgentAdminRole.java @MenuPolicy(menuIds=...)"
      via: "menu item id strings match role annotation values verbatim"
      pattern: "aiAgent\\.parameters\\.list|aiAgent\\.knowledge\\.list|aiAgent\\.audit\\.list"
---

<objective>
Wave 1 foundation plan for Phase 7. Lands all dependency additions, add-on-wide plumbing (Push, markdown, events, streaming DTO), security role extensions, namespaced menu, and complete bilingual message bundles so Wave 2 view plans can run in parallel without sharing files or message-bundle write conflicts.

Purpose:
- Every downstream view task must use msg:// keys. Pre-seeding both bundles with the full UI-SPEC key set (§Copywriting Contract) eliminates cross-plan serialization on messages*.properties.
- MarkdownRenderer and StreamingEvent are shared primitives consumed by 07-02 (backend) and 07-03 (ChatView). Defining them here gives both downstream plans stable contracts.
- AppShellConfigurator must be one class, classpath-scanned by Vaadin — belongs in the foundation plan, not in a view plan.
- menu.xml and AiAgentAdminRole admin-gating list EVERY admin view id — both edited once here rather than five times across view plans.

Output: Gradle deps, 4 new Java files under com.vn.agent.push/orchestration/view.chat, 1 modified Java file (role), 3 modified resource files (menu + 2 bundles).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-flow-ui/07-CONTEXT.md
@.planning/phases/07-flow-ui/07-RESEARCH.md
@.planning/phases/07-flow-ui/07-PATTERNS.md
@.planning/phases/07-flow-ui/07-UI-SPEC.md
@ai-agent/ai-agent/ai-agent.gradle
@ai-agent/ai-agent/src/main/java/com/vn/agent/security/AiAgentAdminRole.java
@ai-agent/ai-agent/src/main/resources/com/vn/agent/menu.xml
@ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties
@ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties

<interfaces>
Existing interface — do not change, extend in place:

From com/vn/agent/security/AiAgentAdminRole.java (current 29 lines, already has @ResourceRole + 5 @EntityPolicy on adminAccess()):
```java
@ResourceRole(name = "AI Agent Admin", code = AiAgentAdminRole.CODE)
public interface AiAgentAdminRole {
    String CODE = "ai-agent-admin";
    // existing @EntityPolicy(...) stack on adminAccess() stays verbatim
    void adminAccess();
    // Plan 07-01 ADDS the adminViews() method below.
}
```

From com/vn/agent/entity/AiKnowledgeDocumentStatus.java: enum values PENDING, PROCESSING, READY, FAILED, CANCELLED (used by DocumentStatusChangedEvent).

Key imports the new files need:
- `io.jmix.security.role.annotation.MenuPolicy` + `ViewPolicy`
- `com.vaadin.flow.component.page.AppShellConfigurator`
- `com.vaadin.flow.component.page.Push` + `com.vaadin.flow.shared.communication.PushMode` + `com.vaadin.flow.shared.ui.Transport`
- `org.springframework.boot.autoconfigure.condition.ConditionalOnProperty`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gradle deps + MarkdownRenderer + StreamingEvent DTO</name>
  <read_first>
    - ai-agent/ai-agent/ai-agent.gradle (FULL — extend the dependencies { } block; do NOT reorder existing Plan 0X-0Y blocks)
    - .planning/phases/07-flow-ui/07-RESEARCH.md §Standard Stack / §Pattern 7 (Flexmark + OWASP)
    - .planning/phases/07-flow-ui/07-PATTERNS.md §No Analog Found (MarkdownRenderer fallback)
  </read_first>
  <files>
    ai-agent/ai-agent/ai-agent.gradle,
    ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/MarkdownRenderer.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/StreamingEvent.java
  </files>
  <action>
    1. Append a new labeled block to `dependencies { }` in ai-agent/ai-agent/ai-agent.gradle after the existing Plan 06-03 block:

       ```gradle
       // Plan 07-01 (D-07, D-20): markdown rendering + XSS sanitizer for assistant output;
       // Jmix gridexport add-on for ToolCallAuditListView Excel+JSON actions (D-20).
       implementation 'com.vladsch.flexmark:flexmark:0.64.8'
       implementation 'com.vladsch.flexmark:flexmark-ext-gfm-tables:0.64.8'
       implementation 'com.vladsch.flexmark:flexmark-ext-autolink:0.64.8'
       implementation 'com.googlecode.owasp-java-html-sanitizer:owasp-java-html-sanitizer:20220608.1'
       implementation 'io.jmix.gridexport:jmix-gridexport-flowui-starter'
       ```

       Do NOT modify the `configurations.implementation { exclude ... hilla ... copilot }` block — those excludes must survive.

    2. Create `ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/MarkdownRenderer.java` as a `@Component` Spring bean:
       - package `com.vn.agent.view.chat;`
       - Fields: `private final Parser parser;` `private final HtmlRenderer renderer;` `private final PolicyFactory sanitizer;` (all final, initialized in constructor).
       - Constructor builds `MutableDataSet opts; opts.set(Parser.EXTENSIONS, List.of(TablesExtension.create(), AutolinkExtension.create()));` then `parser = Parser.builder(opts).build(); renderer = HtmlRenderer.builder(opts).build();` then `sanitizer = Sanitizers.FORMATTING.and(Sanitizers.BLOCKS).and(Sanitizers.LINKS).and(Sanitizers.TABLES);`
       - Public method `public String toSafeHtml(String markdown)`: null/empty → return `""`; else parse → render → sanitize and return. 
       - Class-level Javadoc explicitly notes: "Thread-safe after construction (Flexmark Parser/HtmlRenderer + OWASP PolicyFactory are immutable). Strips <script>, javascript: URIs, data: URIs, on* event handlers via the sanitizer policy." Reference RESEARCH.md Pitfall #5 and Pitfall #6.

    3. Create `ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/StreamingEvent.java`:
       - package `com.vn.agent.orchestration;`
       - `public sealed interface StreamingEvent permits StreamingEvent.Content, StreamingEvent.ToolCall, StreamingEvent.ToolResult, StreamingEvent.Citation, StreamingEvent.Final, StreamingEvent.Error { ... }` with the six nested records from RESEARCH.md §Example 1 VERBATIM:
         - `record Content(String markdownChunk) implements StreamingEvent {}`
         - `record ToolCall(UUID toolCallId, String toolName, String argsJson) implements StreamingEvent {}`
         - `record ToolResult(UUID toolCallId, String summary, AiToolCallOutcome outcome) implements StreamingEvent {}`
         - `record Citation(int index, UUID documentId, String snippet) implements StreamingEvent {}`
         - `record Final(UUID runId, long latencyMs, int promptTokens, int completionTokens) implements StreamingEvent {}`
         - `record Error(String messageKey, Map<String, Object> params) implements StreamingEvent {}`
       - Import `com.vn.agent.entity.AiToolCallOutcome` and `java.util.UUID` + `java.util.Map`.
       - Javadoc: "Phase 7 streaming DTO consumed by ChatView. Bridges Spring AI Flux<ChatResponse> content chunks with out-of-band tool and citation events emitted via Sinks.Many (D-01, RESEARCH Pattern 1)."
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:compileJava</automated>
  </verify>
  <done>
    - `./gradlew :ai-agent:ai-agent:compileJava` succeeds.
    - `./gradlew :ai-agent:ai-agent:dependencies --configuration compileClasspath | grep -E "flexmark|owasp-java-html-sanitizer|jmix-gridexport"` prints three matches.
    - `grep -c "toSafeHtml" ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/MarkdownRenderer.java` prints `1` or more.
    - `grep -c "sealed interface StreamingEvent" ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/StreamingEvent.java` prints `1`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Push plumbing — AppShellConfigurator + DocumentStatusChangedEvent + AiAgentAdminRole.adminViews</name>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/security/AiAgentAdminRole.java (FULL)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiKnowledgeDocumentStatus.java (enum values)
    - .planning/phases/07-flow-ui/07-RESEARCH.md §Pattern 2 (AppShell) + §Pattern 3 (event broker)
    - .planning/phases/07-flow-ui/07-PATTERNS.md §Pattern: role interface extension
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/push/AiAgentAppShell.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/push/DocumentStatusChangedEvent.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/security/AiAgentAdminRole.java
  </files>
  <action>
    1. Create `ai-agent/ai-agent/src/main/java/com/vn/agent/push/AiAgentAppShell.java`:
       - package `com.vn.agent.push;`
       - Class header:
         ```java
         @Push(transport = Transport.WEBSOCKET_XHR)
         @ConditionalOnProperty(
             name = "jmix.ai-agent.flowui.push-autoconfigure",
             havingValue = "true",
             matchIfMissing = true)
         public class AiAgentAppShell implements AppShellConfigurator {
             // marker — @Push annotation carries the config
         }
         ```
       - Imports: `com.vaadin.flow.component.page.AppShellConfigurator`, `com.vaadin.flow.component.page.Push`, `com.vaadin.flow.shared.ui.Transport`, `org.springframework.boot.autoconfigure.condition.ConditionalOnProperty`.
       - Class Javadoc: explain that Vaadin scans the classpath for AppShellConfigurator implementations. Host apps that already ship their own AppShellConfigurator must set `jmix.ai-agent.flowui.push-autoconfigure=false` in application.properties to avoid the "more than one AppShellConfigurator" Vaadin startup error (RESEARCH Pitfall #9). The @ConditionalOnProperty gates Spring bean registration; if Vaadin honors Spring conditionality for classpath scanning (A2 in RESEARCH Assumptions Log) the class is skipped on opt-out. NOTE: record both interpretations in the SUMMARY if the two-AppShellConfigurator collision actually surfaces at bootRun — fall back to documentation-only (delete this class) if the conditional does not prevent classpath scanning.

    2. Create `ai-agent/ai-agent/src/main/java/com/vn/agent/push/DocumentStatusChangedEvent.java`:
       - package `com.vn.agent.push;`
       - `public record DocumentStatusChangedEvent(UUID documentId, AiKnowledgeDocumentStatus status, String errorMessage) {}`
       - Imports: `com.vn.agent.entity.AiKnowledgeDocumentStatus`, `java.util.UUID`.
       - Javadoc: "Fired by IngestionStatusWriter via TransactionSynchronization.afterCommit (Plan 07-02 wires the publisher). Subscribed by KnowledgeBaseView per-UI listener (Plan 07-06) to drive Vaadin Push refresh. errorMessage non-null only when status=FAILED."

    3. Modify `ai-agent/ai-agent/src/main/java/com/vn/agent/security/AiAgentAdminRole.java` in place — add imports and a new policy-annotated method ALONGSIDE the existing `adminAccess()`:
       - Add imports:
         ```java
         import io.jmix.security.role.annotation.MenuPolicy;
         import io.jmix.security.role.annotation.ViewPolicy;
         ```
       - After the existing `adminAccess()` method add:
         ```java
         @MenuPolicy(menuIds = {
                 "aiAgent.parameters.list",
                 "aiAgent.knowledge.list",
                 "aiAgent.audit.list"})
         @ViewPolicy(viewIds = {
                 "AiAgent_Parameters.list", "AiAgent_Parameters.detail",
                 "AiAgent_KnowledgeBase.list",
                 "AiAgent_ToolCallAudit.list"})
         void adminViews();
         ```
       - Do NOT remove or alter the existing `@ResourceRole` annotation, `CODE` constant, or `adminAccess()` method. Do NOT touch `AiAgentUserRole` or `AiAgentUserRowLevelRole` — Chat/Conversation views are user-level per UI-SPEC §Admin gating.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:compileJava</automated>
  </verify>
  <done>
    - compileJava succeeds.
    - `grep -c "@Push(transport = Transport.WEBSOCKET_XHR)" ai-agent/ai-agent/src/main/java/com/vn/agent/push/AiAgentAppShell.java` prints `1`.
    - `grep -c "record DocumentStatusChangedEvent" ai-agent/ai-agent/src/main/java/com/vn/agent/push/DocumentStatusChangedEvent.java` prints `1`.
    - `grep -c "adminViews" ai-agent/ai-agent/src/main/java/com/vn/agent/security/AiAgentAdminRole.java` prints `1` or more.
    - `grep -c "adminAccess" ai-agent/ai-agent/src/main/java/com/vn/agent/security/AiAgentAdminRole.java` prints `1` or more (existing method preserved).
  </done>
</task>

<task type="auto">
  <name>Task 3: menu.xml + full bilingual messages bundles</name>
  <read_first>
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/menu.xml (FULL — 7 lines)
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties (FULL — 108 lines, existing keys preserved)
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties (FULL)
    - .planning/phases/07-flow-ui/07-UI-SPEC.md §Copywriting Contract (AUTHORITATIVE — every key below must match this section verbatim)
    - .planning/phases/07-flow-ui/07-PATTERNS.md §`menu.xml` extension (in place)
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/resources/com/vn/agent/menu.xml,
    ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties,
    ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties
  </files>
  <action>
    1. Rewrite `ai-agent/ai-agent/src/main/resources/com/vn/agent/menu.xml` to:

       ```xml
       <?xml version="1.0" encoding="UTF-8" standalone="no"?>
       <menu-config xmlns="http://jmix.io/schema/flowui/menu">

           <menu id="AI" title="msg://com.vn.agent/menu.addon" opened="true">
               <item id="aiAgent.chat" view="AiAgent_Chat"
                     title="msg://com.vn.agent/menu.chat"/>
               <item id="aiAgent.conversations" view="AiAgent_Conversation.list"
                     title="msg://com.vn.agent/menu.conversations"/>
               <item id="aiAgent.parameters.list" view="AiAgent_Parameters.list"
                     title="msg://com.vn.agent/menu.parameters"/>
               <item id="aiAgent.knowledge.list" view="AiAgent_KnowledgeBase.list"
                     title="msg://com.vn.agent/menu.knowledge"/>
               <item id="aiAgent.audit.list" view="AiAgent_ToolCallAudit.list"
                     title="msg://com.vn.agent/menu.audit"/>
           </menu>

       </menu-config>
       ```

       The three admin-view ids under `aiAgent.parameters.list`, `aiAgent.knowledge.list`, `aiAgent.audit.list` MUST match exactly the strings in AiAgentAdminRole.@MenuPolicy (Task 2).

    2. Append ALL the following keys to `messages.properties` (do NOT delete or reorder the existing 108 lines). Use a `# ======= Phase 07 Flow UI (Plan 07-01) =======` separator. Keys come VERBATIM from 07-UI-SPEC.md §Copywriting Contract. Include every row of these tables:
       - Global menu + view titles table (6 keys: com.vn.agent/menu.addon, menu.chat, menu.conversations, menu.parameters, menu.knowledge, menu.audit)
       - ChatView table (24 keys from chatView.title through chatView.confirm.newChat.body)
       - ConversationListView/DetailView table (10 keys)
       - ParametersListView/DetailView table (23 keys)
       - KnowledgeBaseView table (22 keys)
       - ToolCallAuditListView table (25 keys)
       Use the EN column verbatim as the value on the right of `=`. Preserve MessageFormat placeholders `{0}`, `{1}` exactly as written in UI-SPEC.

       NOTE on existing parameters keys: the existing bundle already contains `ai-agent.parameters.model.required`, `ai-agent.parameters.maxTokens.min`, `ai-agent.parameters.maxTokens.max` (Phase 6 Jakarta validation). Do NOT duplicate those — the Phase 7 additions use the `parametersDetail.*` namespace (different keys).

    3. Append the same key set to `messages_vi.properties` using the VI column VERBATIM. Every key in messages.properties that was added in step 2 MUST appear in messages_vi.properties with the same key and the Vietnamese value from UI-SPEC (parity is enforced by LocaleParityTest in Plan 07-07).
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:processResources</automated>
  </verify>
  <done>
    - processResources succeeds (schema-valid menu.xml).
    - `grep -c "^aiAgent\.chat\|^aiAgent\.audit\.list" ai-agent/ai-agent/src/main/resources/com/vn/agent/menu.xml` prints at least 2 matches.
    - `grep -c "^chatView\.title=" ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties` prints `1`.
    - `grep -c "^chatView\.title=" ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties` prints `1`.
    - `grep -c "^auditList\." ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties` prints `25` or more (audit table size).
    - Key parity sanity: `diff <(grep -oE '^[^#=][^=]+' ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties | sort -u) <(grep -oE '^[^#=][^=]+' ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties | sort -u)` prints nothing.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → ChatView Html | Assistant markdown rendered in browser DOM — untrusted text crosses here |
| Browser → Vaadin @Push channel | WebSocket payloads arrive from authenticated client |
| Host AppShellConfigurator ↔ ai-agent AiAgentAppShell | Two competing classpath-scanned classes may collide |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering/Elevation | MarkdownRenderer.toSafeHtml | mitigate | OWASP PolicyFactory composed of Sanitizers.FORMATTING+BLOCKS+LINKS+TABLES; Flexmark output fed through sanitizer BEFORE embedding in Vaadin Html (Pitfall #5). Unit test in Plan 07-07 asserts `<script>` and `javascript:` URIs stripped. |
| T-07-02 | DoS | AiAgentAppShell + host AppShellConfigurator | mitigate | @ConditionalOnProperty with `matchIfMissing=true` + documented opt-out `jmix.ai-agent.flowui.push-autoconfigure=false`. Pattern 2 in RESEARCH. If Vaadin ignores Spring conditionality, executor deletes the class and falls back to documented snippet (RESEARCH Open Q#3). |
| T-07-03 | Information Disclosure | messages.properties (locale bundles) | accept | Message bundle content is intentionally user-facing text; no secrets or runtime data. |
| T-07-04 | Elevation | AiAgentAdminRole menu/view policy | mitigate | @MenuPolicy + @ViewPolicy tie menu item ids and view ids to the ai-agent-admin role code. Plan 07-07 AdminViewAccessTest probes a non-admin user and asserts denial. |
</threat_model>

<verification>
- `./gradlew :ai-agent:ai-agent:compileJava` green
- `./gradlew :ai-agent:ai-agent:processResources` green
- 4 new Java files present under `com.vn.agent.push` (2) and `com.vn.agent.view.chat` (1) and `com.vn.agent.orchestration` (1)
- AiAgentAdminRole has BOTH `adminAccess` and `adminViews` methods
- menu.xml has 5 item children with msg:// titles
- Both messages bundles contain the full UI-SPEC key set with identical key sets (parity)
</verification>

<success_criteria>
Foundation primitives landed so Wave 2 view plans (07-03, 07-04, 07-05, 07-06) can be executed in parallel with zero shared-file conflicts and every msg:// reference they emit already resolves.
</success_criteria>

<output>
After completion, create `.planning/phases/07-flow-ui/07-01-SUMMARY.md` capturing:
- Dependency resolution evidence (BOM-resolved version of jmix-gridexport, confirmed Flexmark + OWASP versions)
- Whether @ConditionalOnProperty effectively prevents Vaadin from scanning AiAgentAppShell when disabled (if unknown at static-compile time, note for manual bootRun verification)
- Total messages keys added per locale, with the parity diff result
</output>
