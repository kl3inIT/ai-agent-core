---
phase: 07-flow-ui
plan: 06
type: execute
wave: 2
depends_on: [07-01, 07-02]
files_modified:
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/knowledge/KnowledgeBaseView.java
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/view/knowledge/knowledge-base-view.xml
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/audit/ToolCallAuditListView.java
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/view/audit/tool-call-audit-list-view.xml
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/audit/ToolCallAuditDetailDialog.java
autonomous: true
requirements: [UI-05, UI-06]
tags: [flowui, knowledge, upload, push, audit, gridexport]
must_haves:
  truths:
    - "KnowledgeBaseView shows uploaded AiKnowledgeDocument rows with a Badge-rendered Status column"
    - "Jmix <upload> component accepts multi-file PDF/MD/TXT/HTML drops; each file triggers KnowledgeDocumentUploadService with PENDING row created immediately"
    - "DocumentStatusChangedEvent (emitted by IngestionStatusWriter in 07-02) refreshes the affected row via UI.access() — no polling"
    - "KB row actions Reingest (confirm dialog) and Delete (confirm dialog, destructive theme) are wired to KnowledgeDocumentService"
    - "Optional documentId query param pre-filters KB to a single document (citation deep-link from ChatView)"
    - "ToolCallAuditListView shows all AiToolCallAudit rows with typed filter bar (user, tool, outcome, date range) + Jmix GenericFilter"
    - "Audit grid has two gridexport actions (grdexp_excelExport + grdexp_jsonExport) bound to Export Excel + Export JSON buttons"
    - "Row click opens a modal ToolCallAuditDetailDialog with the full audit fields (runId, argsJson, resultSummary, errorClass, denialReason, flagged)"
    - "Outcome column renders as Vaadin Badge with theme per UI-SPEC (success/error/warning/contrast)"
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/view/knowledge/KnowledgeBaseView.java"
      provides: "StandardListView<AiKnowledgeDocument> with upload + status badge + event listener + row actions"
      contains: "DocumentStatusChangedEvent"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/view/audit/ToolCallAuditListView.java"
      provides: "StandardListView<AiToolCallAudit> with typed filters + gridexport actions + detail dialog trigger"
      contains: "grdexp_excelExport"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/view/audit/ToolCallAuditDetailDialog.java"
      provides: "Modal Dialog with full audit row detail (D-21)"
      contains: "extends Dialog"
  key_links:
    - from: "KnowledgeBaseView.@EventListener(DocumentStatusChangedEvent)"
      to: "DataGrid.getDataProvider().refreshItem(doc)"
      via: "per-UI UI.access() broadcast"
      pattern: "@EventListener.*DocumentStatusChanged"
    - from: "ToolCallAuditListView.dataGrid"
      to: "grdexp_excelExport + grdexp_jsonExport"
      via: "XML action declarations bound to Export buttons"
      pattern: "grdexp_excelExport|grdexp_jsonExport"
---

<objective>
Wave 2 plan. Ships UI-05 (KnowledgeBaseView) + UI-06 (ToolCallAuditListView + detail dialog) together because both views are admin-only, both render entity grids with Badge columns, and neither has cross-file dependencies on 07-03/04/05. Bundles them to keep Wave 2 to four plans max while maintaining zero file conflicts.

Purpose:
- KnowledgeBaseView wires Jmix `<upload>` (D-15), Badge status column (D-17), Reingest + Delete row actions (D-18), and the push refresh listener (D-16 — subscribing to DocumentStatusChangedEvent emitted by IngestionStatusWriter in 07-02).
- ToolCallAuditListView wires typed filter bar + GenericFilter (D-19), gridexport add-on actions (D-20), Outcome Badge (D-22), and the detail dialog (D-21). Uses the gridexport dependency added in 07-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-flow-ui/07-CONTEXT.md
@.planning/phases/07-flow-ui/07-UI-SPEC.md
@.planning/phases/07-flow-ui/07-PATTERNS.md
@ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiKnowledgeDocument.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiKnowledgeDocumentStatus.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallAudit.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallOutcome.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/rag/KnowledgeDocumentUploadService.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/rag/KnowledgeDocumentService.java

<interfaces>
From Phase 5:
- `AiKnowledgeDocument`: id, filename (String), mimeType (String), status (AiKnowledgeDocumentStatus enum), uploadedDate, errorMessage (String nullable), chunkCount (Integer), allowedRoles (List<String>).
- `KnowledgeDocumentUploadService.upload(InputStream stream, String filename, String mimeType)` — returns created document id. Creates PENDING row, kicks off async ingestion.
- `KnowledgeDocumentService.delete(UUID id)` — atomic row + vector chunk removal.
- `KnowledgeDocumentService.reingest(UUID id)` — transitions READY/FAILED row back to PENDING and re-enqueues.

From Phase 4:
- `AiToolCallAudit`: id, runId (UUID), conversationId, userId, toolName, kind, phase, createdDate, outcome (AiToolCallOutcome enum), latencyMs (Long), argsJson (String), resultSummary (String), errorClass (String), denialReason (String), flagged (Boolean), promptHash (String).

From Plan 07-01:
- `DocumentStatusChangedEvent(UUID documentId, AiKnowledgeDocumentStatus status, String errorMessage)` record.
- gridexport dependency resolved; actions `grdexp_excelExport` + `grdexp_jsonExport` usable in XML.

From Plan 07-02:
- IngestionStatusWriter publishes DocumentStatusChangedEvent via afterCommit in all 5 mutation methods.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: KnowledgeBaseView + upload + Badge status column + event-driven refresh + row actions</name>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiKnowledgeDocument.java (FULL)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiKnowledgeDocumentStatus.java (enum values)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/rag/KnowledgeDocumentUploadService.java
    - ai-agent/ai-agent/src/main/java/com/vn/agent/rag/KnowledgeDocumentService.java
    - .planning/phases/07-flow-ui/07-RESEARCH.md §Pattern 3 (event broker + UI.access)
    - .planning/phases/07-flow-ui/07-CONTEXT.md §D-15..D-18
    - .planning/phases/07-flow-ui/07-UI-SPEC.md §KnowledgeBaseView
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/view/knowledge/KnowledgeBaseView.java,
    ai-agent/ai-agent/src/main/resources/com/vn/agent/view/knowledge/knowledge-base-view.xml
  </files>
  <action>
    1. Create `knowledge-base-view.xml`:

       ```xml
       <?xml version="1.0" encoding="UTF-8"?>
       <view xmlns="http://jmix.io/schema/flowui/view"
             title="msg://knowledgeBase.title">
           <data>
               <collection id="documentsDc" class="com.vn.agent.entity.AiKnowledgeDocument">
                   <fetchPlan extends="_base"/>
                   <loader id="documentsDl" readOnly="true">
                       <query>
                           <![CDATA[select e from AiAgent_AiKnowledgeDocument e order by e.uploadedDate desc]]>
                       </query>
                   </loader>
               </collection>
           </data>
           <facets><dataLoadCoordinator auto="true"/></facets>
           <layout padding="true" spacing="true">
               <upload id="documentUpload" acceptedFileTypes="application/pdf,text/markdown,text/plain,text/html"
                       maxFiles="10" dropLabel="msg://knowledgeBase.upload.placeholder"
                       uploadText="msg://knowledgeBase.upload.button"/>
               <dataGrid id="documentsDataGrid" dataContainer="documentsDc" width="100%" themeNames="compact">
                   <actions>
                       <action id="reingest" text="msg://knowledgeBase.action.reingest"/>
                       <action id="delete" text="msg://knowledgeBase.action.delete" type="list_remove"/>
                   </actions>
                   <columns>
                       <column property="filename" header="msg://knowledgeBase.column.filename"/>
                       <column property="mimeType" header="msg://knowledgeBase.column.mimeType"/>
                       <column key="status" header="msg://knowledgeBase.column.status"/>
                       <column property="uploadedDate" header="msg://knowledgeBase.column.uploadedDate"/>
                   </columns>
               </dataGrid>
           </layout>
       </view>
       ```

       Rule 1: Jmix `<upload>` attribute names — consult skill `jmix-fragments`/docs or an existing Jmix view with upload; adapt `acceptedFileTypes`, `maxFiles`, `dropLabel` attribute names if actual names differ.

    2. Create `KnowledgeBaseView.java`:
       - `@Route(value = "ai-agent/knowledge", layout = DefaultMainViewParent.class) @ViewController("AiAgent_KnowledgeBase.list") @ViewDescriptor("knowledge-base-view.xml") public class KnowledgeBaseView extends StandardListView<AiKnowledgeDocument> implements HasUrlParameter<String> { ... }` (or `BeforeEnterObserver`, per 07-03 pattern).

       - Fields:
         - `@ViewComponent DataGrid<AiKnowledgeDocument> documentsDataGrid;`
         - `@ViewComponent Upload documentUpload;`
         - `@ViewComponent CollectionLoader<AiKnowledgeDocument> documentsDl;`
         - `@ViewComponent CollectionContainer<AiKnowledgeDocument> documentsDc;`
         - `@Autowired KnowledgeDocumentUploadService uploadService;`
         - `@Autowired KnowledgeDocumentService documentService;`
         - `@Autowired Dialogs dialogs;`
         - `@Autowired Notifications notifications;`
         - `@Autowired MessageSource messages;`
         - `private UI ownerUi;`  // captured onAttach for UI.access in event listener

       - `@Subscribe public void onInit(InitEvent event)`:
         - Custom status column renderer:
           ```java
           documentsDataGrid.getColumnByKey("status").setRenderer(new ComponentRenderer<>(doc -> {
               Span badge = new Span(messages.getMessage("knowledgeBase.status." + doc.getStatus().name().toLowerCase(), null, UI.getCurrent().getLocale()));
               badge.getElement().getThemeList().add("badge " + statusTheme(doc.getStatus()));
               if (doc.getStatus() == AiKnowledgeDocumentStatus.FAILED && doc.getErrorMessage() != null) {
                   badge.getElement().setAttribute("title", doc.getErrorMessage());
               }
               return badge;
           }));
           ```
         - `private String statusTheme(AiKnowledgeDocumentStatus s)`: PENDING/PROCESSING → "contrast", READY → "success", FAILED → "error", CANCELLED → "contrast tertiary" (UI-SPEC mapping).

         - Wire upload:
           ```java
           MemoryBuffer buffer = new MemoryBuffer();
           documentUpload.setReceiver(buffer);
           documentUpload.addSucceededListener(e -> {
               try (InputStream in = buffer.getInputStream()) {
                   uploadService.upload(in, e.getFileName(), e.getMIMEType());
                   notifications.create(messages.getMessage("knowledgeBase.toast.uploadStarted", new Object[]{e.getFileName()}, UI.getCurrent().getLocale())).show();
                   documentsDl.load();
               } catch (IOException ex) {
                   notifications.create(messages.getMessage("knowledgeBase.upload.rejected", new Object[]{e.getFileName()}, UI.getCurrent().getLocale())).withThemeVariant(NotificationVariant.LUMO_ERROR).show();
               }
           });
           ```
           Rule 1: Jmix `<upload>` Java type may differ from plain Vaadin `Upload` — use Jmix's typed component and adapt listener method names.

         - Wire row actions:
           - Reingest action: `documentsDataGrid.getAction("reingest").addActionPerformedListener(e -> onReingestClick());`
           - onReingestClick: `documentsDataGrid.getSingleSelectedItem().ifPresent(doc -> dialogs.createOptionDialog().withHeader(messages.getMessage("knowledgeBase.confirm.reingest.title", new Object[]{doc.getFilename()}, UI.getCurrent().getLocale())).withText(messages.getMessage("knowledgeBase.confirm.reingest.body", null, UI.getCurrent().getLocale())).withActions(DialogAction.Type.OK.handler(ev -> { documentService.reingest(doc.getId()); documentsDl.load(); }), DialogAction.Type.CANCEL).open());`
           - Delete action (list_remove type handles confirm natively, but D-18 wants custom text): intercept RemoveAction preConfirm to set the custom message keys; or override with a custom action using dialogs.createOptionDialog exactly like Reingest but theming button as `primary error`.

       - `@Subscribe public void onAttach(AttachEvent event) { ownerUi = getUI().orElse(null); }`
       - `@Subscribe public void onDetach(DetachEvent event) { ownerUi = null; }`

       - Event listener for push refresh:
         ```java
         @EventListener
         public void onDocumentStatusChanged(DocumentStatusChangedEvent event) {
             UI ui = this.ownerUi;
             if (ui == null) return;
             ui.access(() -> {
                 documentsDc.getItems().stream()
                         .filter(d -> d.getId().equals(event.documentId()))
                         .findFirst()
                         .ifPresent(doc -> {
                             doc.setStatus(event.status());
                             if (event.status() == AiKnowledgeDocumentStatus.FAILED) doc.setErrorMessage(event.errorMessage());
                             documentsDc.replaceItem(doc);
                         });
             });
         }
         ```
         Rule 1: Jmix 2.8 CollectionContainer refresh API — may be `replaceItem(entity)` or `getMutableItems().replaceAll(...)`; adapt. If the entity might have been deleted, wrap the replace in a try-catch.

       - Route parameter handling — if `documentId` query param present, apply JPQL filter: `documentsDl.setQuery("... where e.id = :docId ..."); documentsDl.setParameter("docId", UUID.fromString(paramValue));` on beforeEnter.

       - Javadoc: "UI-05. D-15 Jmix upload, D-16 push refresh via DocumentStatusChangedEvent + UI.access, D-17 Badge column, D-18 confirm dialogs on Reingest + Delete. Admin-only per role policy in 07-01."
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:compileJava :ai-agent:ai-agent:processResources</automated>
  </verify>
  <done>
    - compileJava + processResources green.
    - `grep -c "AiAgent_KnowledgeBase.list" ai-agent/ai-agent/src/main/java/com/vn/agent/view/knowledge/KnowledgeBaseView.java` prints `1`.
    - `grep -c "DocumentStatusChangedEvent\\|@EventListener" ai-agent/ai-agent/src/main/java/com/vn/agent/view/knowledge/KnowledgeBaseView.java` prints `3` or more.
    - `grep -c "ui.access\\|UI.access" ai-agent/ai-agent/src/main/java/com/vn/agent/view/knowledge/KnowledgeBaseView.java` prints `1` or more.
    - All XML `text=`/`header=`/`title=`/`dropLabel=`/`uploadText=` attributes use `msg://`.
  </done>
</task>

<task type="auto">
  <name>Task 2: ToolCallAuditListView + ToolCallAuditDetailDialog + gridexport actions</name>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallAudit.java (FULL — confirm field names including runId, kind, phase, errorClass, denialReason, flagged, promptHash)
    - .planning/phases/07-flow-ui/07-CONTEXT.md §D-19..D-22
    - .planning/phases/07-flow-ui/07-UI-SPEC.md §ToolCallAuditListView
    - Jmix gridexport docs via Context7 or skill
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/view/audit/ToolCallAuditListView.java,
    ai-agent/ai-agent/src/main/resources/com/vn/agent/view/audit/tool-call-audit-list-view.xml,
    ai-agent/ai-agent/src/main/java/com/vn/agent/view/audit/ToolCallAuditDetailDialog.java
  </files>
  <action>
    1. Create `tool-call-audit-list-view.xml`:

       ```xml
       <?xml version="1.0" encoding="UTF-8"?>
       <view xmlns="http://jmix.io/schema/flowui/view"
             xmlns:grdexp="http://jmix.io/schema/gridexport/flowui"
             title="msg://auditList.title">
           <data>
               <collection id="auditsDc" class="com.vn.agent.entity.AiToolCallAudit">
                   <fetchPlan extends="_base"/>
                   <loader id="auditsDl" readOnly="true">
                       <query>
                           <![CDATA[select e from AiAgent_AiToolCallAudit e order by e.createdDate desc]]>
                       </query>
                   </loader>
               </collection>
           </data>
           <facets><dataLoadCoordinator auto="true"/></facets>
           <layout padding="true" spacing="true">
               <hbox id="filterBar" spacing="true" padding="false">
                   <textField id="userFilter" placeholder="msg://auditList.filter.user"/>
                   <comboBox id="toolFilter" placeholder="msg://auditList.filter.tool"/>
                   <comboBox id="outcomeFilter" placeholder="msg://auditList.filter.outcome"/>
                   <dateTimePicker id="dateFromFilter" placeholder="msg://auditList.filter.dateRange"/>
                   <dateTimePicker id="dateToFilter"/>
               </hbox>
               <genericFilter id="auditsGenericFilter" dataLoader="auditsDl"/>
               <hbox id="exportBar" spacing="true">
                   <button id="excelExportBtn" text="msg://auditList.action.exportExcel"
                           themeNames="primary" action="auditsDataGrid.excelExport"/>
                   <button id="jsonExportBtn" text="msg://auditList.action.exportJson"
                           action="auditsDataGrid.jsonExport"/>
               </hbox>
               <dataGrid id="auditsDataGrid" dataContainer="auditsDc" width="100%" height="100%"
                         themeNames="compact" selectionMode="MULTI">
                   <actions>
                       <action id="excelExport" type="grdexp_excelExport"/>
                       <action id="jsonExport" type="grdexp_jsonExport"/>
                   </actions>
                   <columns>
                       <column property="createdDate" header="msg://auditList.column.createdDate"/>
                       <column property="userId" header="msg://auditList.column.user"/>
                       <column property="toolName" header="msg://auditList.column.tool"/>
                       <column property="phase" header="msg://auditList.column.phase"/>
                       <column key="outcome" header="msg://auditList.column.outcome"/>
                       <column property="latencyMs" header="msg://auditList.column.latencyMs"/>
                   </columns>
               </dataGrid>
           </layout>
       </view>
       ```

       Rule 1: gridexport XML namespace URI and action `type=` exact strings — confirm via Context7 on `io.jmix.gridexport:jmix-gridexport-flowui-starter`. If `grdexp_excelExport` / `grdexp_jsonExport` type ids differ from documentation, adapt. The button `action="auditsDataGrid.excelExport"` binding is the Jmix `list-action` indirection — follow existing Jmix patterns.

       Rule 1: `comboBox` vs `entityComboBox` vs `select` — pick whichever Jmix 2.8 form controls natively allow setting an items list at runtime.

    2. Create `ToolCallAuditDetailDialog.java`:
       - `public class ToolCallAuditDetailDialog extends Dialog { ... }`
       - Constructor: `public ToolCallAuditDetailDialog(AiToolCallAudit audit, MessageSource messages)`.
       - Content: a `FormLayout` with one row per field: runId, argsJson (inside `<Pre>` monospace), resultSummary (<Pre>), errorClass, denialReason, flagged (as a Badge), outcome (Badge colored per UI-SPEC), latencyMs.
       - Size: `setWidth("var(--lumo-size-xl-plus)");` (≈ 960px per UI-SPEC).
       - Header: `setHeaderTitle(messages.getMessage("auditList.detail.title", null, UI.getCurrent().getLocale()));`
       - All labels via msg keys `auditList.detail.*`.

    3. Create `ToolCallAuditListView.java`:
       - `@Route(value = "ai-agent/audit", layout = DefaultMainViewParent.class) @ViewController("AiAgent_ToolCallAudit.list") @ViewDescriptor("tool-call-audit-list-view.xml") public class ToolCallAuditListView extends StandardListView<AiToolCallAudit> { ... }`
       - Fields for every filter control + the grid + `@Autowired DataManager dataManager; @Autowired MessageSource messages;`.

       - `@Subscribe public void onInit(InitEvent event)`:
         - Populate filter comboboxes:
           - `toolFilter.setItems(collectDistinctToolNames());` — query via `dataManager.loadValue("select distinct e.toolName from AiAgent_AiToolCallAudit e", String.class).list();`
           - `outcomeFilter.setItems(Arrays.asList(AiToolCallOutcome.values())); outcomeFilter.setItemLabelGenerator(o -> messages.getMessage("auditList.outcome." + o.name().toLowerCase(), null, UI.getCurrent().getLocale()));`
         - Attach valueChangeListener to each filter: `rebuildJpqlQuery();` — compose a JPQL with `where` clauses for each active filter, reload `auditsDl`. (Alternative: use Jmix `Condition` composition via `LogicalCondition.and(...)` on the loader.)

         - Outcome column badge renderer:
           ```java
           auditsDataGrid.getColumnByKey("outcome").setRenderer(new ComponentRenderer<>(row -> {
               Span b = new Span(messages.getMessage("auditList.outcome." + row.getOutcome().name().toLowerCase(), null, UI.getCurrent().getLocale()));
               b.getElement().getThemeList().add("badge " + outcomeTheme(row.getOutcome()));
               return b;
           }));
           ```
           Mapping: SUCCESS→success, FAILED→error, DENIED→warning, TIMEOUT→contrast, CANCELLED→contrast tertiary (UI-SPEC §Color).

         - Row click:
           ```java
           auditsDataGrid.addItemClickListener(e -> new ToolCallAuditDetailDialog(e.getItem(), messages).open());
           ```

       - Javadoc: "UI-06. D-19 typed filter bar + genericFilter, D-20 gridexport Excel+JSON (via 07-01 dependency), D-21 row-click detail dialog, D-22 Outcome Badge. Admin-only per role policy in 07-01."
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:compileJava :ai-agent:ai-agent:processResources</automated>
  </verify>
  <done>
    - compileJava + processResources green.
    - `grep -c "AiAgent_ToolCallAudit.list" ai-agent/ai-agent/src/main/java/com/vn/agent/view/audit/ToolCallAuditListView.java` prints `1`.
    - `grep -c "grdexp_excelExport\\|grdexp_jsonExport" ai-agent/ai-agent/src/main/resources/com/vn/agent/view/audit/tool-call-audit-list-view.xml` prints `2`.
    - `grep -c "ToolCallAuditDetailDialog" ai-agent/ai-agent/src/main/java/com/vn/agent/view/audit/ToolCallAuditListView.java` prints `2` or more.
    - Every `text=`/`header=`/`placeholder=`/`title=` starts with `msg://` in both audit XML files.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Uploaded file bytes → disk/vector store | MIME sniffing + size limit enforced upstream by KnowledgeDocumentUploadService (Phase 5) |
| Rendered errorMessage on FAILED badges | Backend-sanitized string (IngestionStatusWriter truncates); safe to display |
| DocumentStatusChangedEvent listener cross-tenant fan-out | ApplicationEvent broadcasts to ALL UIs; per-UI listener filters by ownerUi attachment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-17 | DoS | File upload size/count | mitigate | acceptedFileTypes + maxFiles attributes clamp client-side; KnowledgeDocumentUploadService enforces server-side size cap (Phase 5 invariant). |
| T-07-18 | Information Disclosure | FAILED badge tooltip | accept | errorMessage is backend-sanitized in IngestionStatusWriter.truncate() (Phase 5). Admin-only view; minimal disclosure risk. |
| T-07-19 | Tampering | gridexport output after filter bypass | mitigate | Jmix gridexport add-on honors the DataProvider's current filter + sort automatically (D-20); no separate export path that could skip filters. |
| T-07-20 | Elevation | Audit direct URL | mitigate | @ViewPolicy in 07-01 gates AiAgent_ToolCallAudit.list. |
</threat_model>

<verification>
- compileJava + processResources green
- KnowledgeBaseView reacts to DocumentStatusChangedEvent with UI.access
- Audit grid declares both grdexp_excelExport + grdexp_jsonExport actions
- All visible strings via msg://
- View ids match 07-01 menu + role policy
</verification>

<success_criteria>
UI-05 + UI-06 delivered: upload + status badges + reactive push refresh + row actions; audit filter bar + genericFilter + gridexport Excel/JSON + detail dialog + outcome badges. All admin-only.
</success_criteria>

<output>
After completion, create `.planning/phases/07-flow-ui/07-06-SUMMARY.md` capturing:
- Actual Jmix 2.8 `<upload>` component API (attribute names + Java type)
- Confirmed gridexport namespace URI and action type ids from Context7
- Whether CollectionContainer.replaceItem exists or if a different refresh API was used
- Observed AiToolCallAudit field list (for SUMMARY cross-ref with Phase 4)
</output>
