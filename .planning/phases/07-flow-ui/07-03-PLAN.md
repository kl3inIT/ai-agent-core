---
phase: 07-flow-ui
plan: 03
type: execute
wave: 2
depends_on: [07-01, 07-02]
files_modified:
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/ChatView.java
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/chat-view.xml
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/fragment/chat-panel-fragment.xml
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/MessageBubbleComponent.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ToolCallCardComponent.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/CitationDialog.java
autonomous: true
requirements: [UI-01, UI-02]
tags: [flowui, chat, streaming, push, fragment, markdown]
must_haves:
  truths:
    - "User can open /ai-agent/chat and see an empty-state heading + message input"
    - "User typing a message + Send triggers ChatService.stream and the assistant bubble renders tokens as they arrive"
    - "While a stream is in flight, Send button hides and Stop button is visible; clicking Stop disposes the Flux and freezes the bubble"
    - "Stream completion resets the input (Send visible, Stop hidden) and Final event is received"
    - "Tool calls appear as collapsed badge rows that expand on click to show args + result + outcome badge"
    - "Citations appear as inline [n] superscripts; clicking opens a dialog with chunk text + Open in Knowledge base button"
    - "Query param ?conversationId={uuid} pre-loads that conversation; invalid/non-owned id falls through to new chat with a Notification"
    - "New chat button confirms before discarding an in-flight stream; empty conversation skips the dialog"
    - "ChatPanelFragment is the reusable unit (D-29 substrate for v2 floating launcher) — ChatView composes it + a breadcrumb + New chat only"
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/ChatView.java"
      provides: "@Route(\"ai-agent/chat\") StandardView hosting ChatPanelFragment"
      contains: "ChatPanelFragment"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java"
      provides: "Reusable Jmix Fragment owning message list + input + streaming driver"
      contains: "Fragment"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/MessageBubbleComponent.java"
      provides: "Role-styled message bubble backed by MarkdownRenderer"
      contains: "MarkdownRenderer"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ToolCallCardComponent.java"
      provides: "Collapsed-badge-to-expanded Details component (D-08, reusable with ConversationDetailView)"
      contains: "Details"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/CitationDialog.java"
      provides: "Dialog showing chunk text + Open in KB navigation button"
      contains: "Dialog"
  key_links:
    - from: "ChatPanelFragment.onSendClick"
      to: "ChatService.stream(userId, conversationId, message, Overrides)"
      via: "Flux subscription with .subscribe(event -> ui.access(() -> render(event)))"
      pattern: "chatService\\.stream"
    - from: "ChatPanelFragment.onDetach"
      to: "active Flux Disposable"
      via: "disposable.dispose() on Vaadin detach to prevent orphan streams (Pitfall #8)"
      pattern: "disposable\\.dispose"
    - from: "CitationDialog.onOpenInKb"
      to: "KnowledgeBaseView"
      via: "UI.getCurrent().navigate(KnowledgeBaseView.class, QueryParameters.of(\"documentId\", uuid))"
      pattern: "navigate.*KnowledgeBaseView"
---

<objective>
Wave 2 plan. Ships ChatView (UI-01, UI-02) as a thin route host composing ChatPanelFragment — the reusable chat substrate (D-29). Wires ChatService.stream into Vaadin Push with UI.access, renders tokens through MarkdownRenderer, and exposes Stop via CancellationRegistry. Parallel-safe with 07-04/05/06 because this plan only writes files under com.vn.agent.view.chat/**.

Purpose:
- ChatView itself is deliberately tiny (D-29): the Fragment carries the logic so v2 floating launcher embeds the same substrate.
- MessageBubbleComponent + ToolCallCardComponent + CitationDialog are reused by Plan 07-04 ConversationDetailView (read-only mode). Building them here as standalone classes (not inner classes) is a D-25 requirement.
- Streaming driver lives entirely in the Fragment: 20 Hz debounce, UI.access for Push, dispose-on-detach.

Output: 2 new Java view classes (ChatView + ChatPanelFragment) + 3 reusable UI component classes + 2 XML descriptors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-flow-ui/07-CONTEXT.md
@.planning/phases/07-flow-ui/07-RESEARCH.md
@.planning/phases/07-flow-ui/07-PATTERNS.md
@.planning/phases/07-flow-ui/07-UI-SPEC.md
@.planning/phases/07-flow-ui/07-01-PLAN.md
@.planning/phases/07-flow-ui/07-02-PLAN.md
@ai-agent/ai-agent/src/main/java/com/vn/agent/ChatService.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/ConversationGateway.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/ChatResponseDto.java

<interfaces>
From Plan 07-01 (ALREADY LANDED before this plan runs):
- `com.vn.agent.view.chat.MarkdownRenderer` Spring bean: `String toSafeHtml(String markdown)` — XSS-safe.
- `com.vn.agent.orchestration.StreamingEvent` sealed interface — variants: Content(String markdownChunk), ToolCall(UUID toolCallId, String toolName, String argsJson), ToolResult(UUID toolCallId, String summary, AiToolCallOutcome outcome), Citation(int index, UUID documentId, String snippet), Final(UUID runId, long latencyMs, int promptTokens, int completionTokens), Error(String messageKey, Map<String,Object> params).
- `com.vn.agent.push.AiAgentAppShell` — enables @Push globally.
- `messages.properties` chatView.* keys — ALL already seeded by 07-01.

From Plan 07-02:
- `ChatService.stream(String userId, UUID conversationId, String message, Overrides overrides) -> Flux<StreamingEvent>`
- Stream emits: zero-or-more Content/ToolCall/ToolResult/Citation events, exactly one Final, or exactly one Error.
- CancellationRegistry wired inside DefaultChatServiceImpl; caller disposes the subscription to cancel.

From Phase 4 (existing):
- `ConversationGateway.loadOrCreate(userId, conversationId, firstMessage)` — loader with ownership check; returns `AiConversation`.
- `AiConversation` entity — id/title/createdBy fields.
- `AiMessage` entity — role/content/toolCallsJson fields for transcript replay.

CurrentAuthentication (Jmix): `io.jmix.core.security.CurrentAuthentication.getUser().getUsername()` — use for userId parameter to stream().
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MessageBubbleComponent + ToolCallCardComponent + CitationDialog</name>
  <read_first>
    - .planning/phases/07-flow-ui/07-UI-SPEC.md §ChatView + §Color (bubble left-border tokens) + §Spacing (bubble padding)
    - .planning/phases/07-flow-ui/07-CONTEXT.md §D-06 / D-08 / D-09 / D-25
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/MarkdownRenderer.java (FULL — provides toSafeHtml)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/StreamingEvent.java (record shapes)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallOutcome.java (enum values — SUCCESS, FAILED, DENIED, TIMEOUT, CANCELLED)
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/MessageBubbleComponent.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ToolCallCardComponent.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/CitationDialog.java
  </files>
  <action>
    1. Create `MessageBubbleComponent.java` (package `com.vn.agent.view.chat.fragment`):
       - `public class MessageBubbleComponent extends Composite<Div>` (Vaadin Flow Composite pattern).
       - Constructor: `public MessageBubbleComponent(MessageBubbleComponent.Role role, MarkdownRenderer markdownRenderer)`.
       - Nested enum `public enum Role { USER, ASSISTANT, SYSTEM }`.
       - Private `Html htmlContent` holding the rendered markdown; private `StringBuilder buffer = new StringBuilder();` for streaming.
       - Public methods:
         - `public void appendMarkdown(String chunk)` — appends to buffer, re-renders via `htmlContent.setHtmlContent(markdownRenderer.toSafeHtml(buffer.toString()))`.
         - `public void setMarkdown(String full)` — replaces buffer wholesale (used for replay of completed messages).
         - `public void markStopped()` — appends a muted `<span class="ai-agent-stopped">— stopped</span>` suffix (css class referenced from UI-SPEC Small type).
       - Initial theming in constructor: `Div root = getContent(); root.getStyle().setPadding("var(--lumo-space-m) var(--lumo-space-m)"); root.getStyle().setBackground("var(--lumo-contrast-5pct)"); root.getStyle().setBorderRadius("var(--lumo-border-radius-m)");`
         For role=ASSISTANT add `root.getStyle().setBorderLeft("2px solid var(--lumo-primary-color)");`
         For role=USER add `root.getStyle().setMarginLeft("auto"); root.getStyle().setBackground("var(--lumo-primary-color-10pct)");`
         Do NOT hardcode px — use Lumo vars (UI-SPEC §Spacing/§Color).
       - Initial `htmlContent = new Html("<div></div>"); root.add(htmlContent);`
       - Javadoc: "D-06 role-styled bubble. Server-side markdown rendering with streaming buffer. Reused by ChatPanelFragment live path AND by ConversationDetailView (Plan 07-04) read-only replay via setMarkdown()."

    2. Create `ToolCallCardComponent.java`:
       - `public class ToolCallCardComponent extends Composite<Details>` where `Details` is `com.vaadin.flow.component.details.Details`.
       - Constructor: `public ToolCallCardComponent(UUID toolCallId, String toolName, String argsJson, MessageSource messages)`.
       - Build summary row in constructor: `HorizontalLayout summary = new HorizontalLayout(new Icon(VaadinIcon.TOOLS), new Span(toolName), new Span("(" + shortSummaryOf(argsJson) + ")"));` — use UI-SPEC key `chatView.toolCard.collapsed = {0} ({1})` via MessageFormat formatting (locale-aware) rather than manual concatenation. Set `details.setSummary(summary);`
       - Body content (expanded state) in constructor: a `VerticalLayout` containing:
         - A `Span(msg("chatView.toolCard.argsHeading"))` label.
         - A `Pre` (monospace) containing `argsJson` pretty-printed (use `new ObjectMapper().readerFor(JsonNode.class).readValue(argsJson).toPrettyString()` or similar — handle `JsonProcessingException` by falling back to the raw string).
         - A `Span(msg("chatView.toolCard.resultHeading"))` label + a `Pre` for the result summary.
         - A `Span outcomeBadge` with `element.getThemeList().add("badge " + themeFor(outcome))`.
       - Public methods:
         - `public void setResult(String resultSummary, AiToolCallOutcome outcome)` — updates the result Pre and outcome badge theme.
       - Private helper `private String themeFor(AiToolCallOutcome outcome)`: map per UI-SPEC §Color badge table (SUCCESS->success, FAILED->error, DENIED->warning, TIMEOUT->contrast, CANCELLED->contrast tertiary).
       - Private helper `private String shortSummaryOf(String argsJson)`: first 40 chars of single-line-collapsed JSON; ellipsis on overflow.
       - `private String msg(String key)`: `messages.getMessage(key, null, UI.getCurrent().getLocale())` using the injected `MessageSource`.
       - Javadoc: "D-08, D-25 — shared between ChatView live-streaming and ConversationDetailView replay. Collapsed-badge → expand-on-click via Vaadin Details."

    3. Create `CitationDialog.java`:
       - `public class CitationDialog extends Dialog` (`com.vaadin.flow.component.dialog.Dialog`).
       - Constructor: `public CitationDialog(int index, UUID documentId, String documentName, String snippetText, MessageSource messages)`.
       - Set header via `setHeaderTitle(MessageFormat.format(msg("chatView.citation.dialogTitle"), documentName));` (key: "Source: {0}").
       - Content: a `VerticalLayout` with a `Pre` (or a plain `Div` with `white-space: pre-wrap`) showing `snippetText`. Width: `setWidth("var(--lumo-size-xl-plus)")` per UI-SPEC §Interaction Contracts (Audit detail dialog width reused).
       - Footer via `getFooter().add(...)`: two buttons:
         - `Button openInKb = new Button(msg("chatView.citation.openInKb"), e -> { UI.getCurrent().navigate(KnowledgeBaseView.class, QueryParameters.of("documentId", documentId.toString())); close(); });` — forward reference to KnowledgeBaseView (Plan 07-06) is compile-time resolved AFTER 07-06 lands; for THIS plan's compile target, use `"com.vn.agent.view.knowledge.KnowledgeBaseView"` via `Class.forName(...)`-free approach: import the class. **Rule 1 tolerance:** if 07-06 has NOT yet compiled when this plan runs, either (a) accept the compile error and add this import in a follow-up, or (b) stub with `Notification.show(msg("chatView.citation.openInKb"))` and leave a TODO(07-06) comment — executor picks whichever is consistent with wave-2 execution order. Prefer (b) because waves 07-03..07-06 run in parallel.
         - `Button close = new Button(msg("actions.cancel"), e -> close());` — msg key already in base bundle.
       - `private String msg(String key)`: identical helper as above.
       - Javadoc: "D-09 — citation chunk preview + KB navigation. Dialog width matches Audit detail dialog (UI-SPEC)."

    Shared rule: all three components use ONLY `msg://` key values fetched via injected `MessageSource` or the parent Fragment's MessageBundle. NO hardcoded visible strings. NO hex colors.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:compileJava</automated>
  </verify>
  <done>
    - compileJava succeeds.
    - `grep -c "extends Composite<Div>" ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/MessageBubbleComponent.java` prints `1`.
    - `grep -c "extends Composite<Details>" ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ToolCallCardComponent.java` prints `1`.
    - `grep -c "extends Dialog" ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/CitationDialog.java` prints `1`.
    - No hex literals in new files: `grep -E "#[0-9a-fA-F]{3,8}" ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/*.java` prints nothing.
  </done>
</task>

<task type="auto">
  <name>Task 2: ChatPanelFragment (streaming driver) + XML descriptor</name>
  <read_first>
    - .planning/phases/07-flow-ui/07-RESEARCH.md §Pattern 1 (full reference code, lines 220-285) + §Pitfall #1, #4, #7, #8
    - .planning/phases/07-flow-ui/07-UI-SPEC.md §Interaction Contracts §Chat streaming + cancel
    - .planning/phases/07-flow-ui/07-CONTEXT.md §D-01..D-05, D-29
    - Existing pattern: any existing view descriptor under ai-agent/ai-agent/src/main/resources/com/vn/agent/view/** for Jmix Flow UI XML shape (if none, use jmix-app views/*.xml as reference via `ls ai-agent/jmix-app/src/main/resources/com/company/app/view 2>/dev/null`).
    - D:/Study materials spring 2026/EXE101/ai/jmix-ai-backend/src/main/java/io/jmix/ai/backend/view/chat/ChatView.java (reference implementation — D-05)
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java,
    ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/fragment/chat-panel-fragment.xml
  </files>
  <action>
    1. Create descriptor `ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/fragment/chat-panel-fragment.xml`:

       ```xml
       <?xml version="1.0" encoding="UTF-8"?>
       <fragment xmlns="http://jmix.io/schema/flowui/fragment">
           <content>
               <vbox id="rootLayout" width="100%" height="100%" padding="false">
                   <scroller id="messageScroller" width="100%" height="100%">
                       <vbox id="messageList" padding="true" spacing="true" width="100%"/>
                   </scroller>
                   <div id="emptyStatePanel" visible="true">
                       <h3 text="msg://chatView.empty.heading"/>
                       <span text="msg://chatView.empty.body"/>
                   </div>
                   <hbox id="inputBar" width="100%" alignItems="END" padding="true" spacing="true">
                       <textArea id="messageInput" width="100%" placeholder="msg://chatView.input.placeholder"
                                 minHeight="3em" maxHeight="10em"/>
                       <button id="sendButton" text="msg://chatView.action.send"
                               themeNames="primary"/>
                       <button id="stopButton" text="msg://chatView.action.stop"
                               themeNames="tertiary" visible="false"/>
                   </hbox>
               </vbox>
           </content>
       </fragment>
       ```

       Rule 1 tolerance: Jmix 2.8 fragment element/namespace exact syntax — if `<fragment>` root is not recognized, use the exact form from Jmix skill `jmix-fragments` or from an existing fragment in the repo (grep for `<fragment ` under ai-agent/). Adapt attribute names to whatever Jmix 2.8 Flow UI actually accepts; the INTENT is: 1 scroller → vbox message list + empty state + input bar with send/stop buttons.

    2. Create `ChatPanelFragment.java`:
       - `@FragmentDescriptor("chat-panel-fragment.xml") public class ChatPanelFragment extends Fragment<VerticalLayout>` — adapt exact Jmix base type per skill `jmix-fragments`. Fallback if Jmix 2.8 Fragment API shape differs: `public class ChatPanelFragment extends Composite<VerticalLayout>` with XML loaded via `UiComponents.create(...)` and a `@ViewDescriptor`-like annotation that the skill documents.
       - Fields (all `@ViewComponent` unless noted):
         - `@ViewComponent VerticalLayout messageList;`
         - `@ViewComponent Scroller messageScroller;`
         - `@ViewComponent Div emptyStatePanel;`
         - `@ViewComponent TextArea messageInput;`
         - `@ViewComponent JmixButton sendButton;`
         - `@ViewComponent JmixButton stopButton;`
         - `@Autowired private ChatService chatService;`
         - `@Autowired private CurrentAuthentication currentAuthentication;`
         - `@Autowired private MarkdownRenderer markdownRenderer;`
         - `@Autowired private MessageSource messages;`
         - `private UUID conversationId;`   // null until set via setConversationId
         - `private Disposable activeStream;`
         - `private MessageBubbleComponent activeAssistantBubble;`
         - `private final Map<UUID, ToolCallCardComponent> toolCardsByCallId = new HashMap<>();`
         - `private long lastRenderNanos = 0L;`   // for 20 Hz debounce

       - `@Subscribe public void onInit(Fragment.InitEvent event) { ... }`:
         - Wire: `sendButton.addClickListener(e -> onSendClick());`
         - Wire: `stopButton.addClickListener(e -> onStopClick());`
         - Wire: `messageInput.addKeyPressListener(Key.ENTER, e -> { if (!e.isShiftDown()) onSendClick(); });` — adapt to Jmix 2.8 TextArea key-listener API (Rule 1).

       - Public lifecycle:
         - `public void setConversationId(UUID conversationId) { this.conversationId = conversationId; if (conversationId != null) loadTranscript(); }`
         - `public void loadTranscript()` — load `AiConversation` via ConversationGateway, render existing AiMessage rows into MessageBubbleComponent instances (USER + ASSISTANT alternating). Hide `emptyStatePanel`. **Rule 1 tolerance:** if ConversationGateway only exposes `loadOrCreate`, add a new `loadOwned(userId, conversationId)` method in a TINY follow-up or just call `loadOrCreate(userId, id, null)` and accept the early message-insert side effect as acceptable only for this plan. Document deviation in SUMMARY. Preferred: add a read-only loader to ConversationGateway as part of this task IF it takes <5 lines; otherwise fall back.

       - `private void onSendClick()`:
         - Capture `String text = messageInput.getValue();` — if blank, return.
         - Clear input, disable, hide `sendButton`, show `stopButton`, hide `emptyStatePanel`.
         - Append a USER MessageBubbleComponent with the text.
         - Append a new ASSISTANT MessageBubbleComponent; assign to `activeAssistantBubble`.
         - `UUID userId;` — use `currentAuthentication.getUser().getUsername()` (convert to String as ChatService expects String userId).
         - Subscribe: `activeStream = chatService.stream(userId, conversationId, text, null).subscribe(event -> handleEvent(event), error -> handleError(error), () -> finishStream());`
         - Do NOT block the servlet thread — `.subscribe()` runs off the subscribeOn scheduler from Plan 07-02.

       - `private void handleEvent(StreamingEvent event)` — must wrap all UI mutations in `getUI().ifPresent(ui -> ui.access(() -> { ... }))` because Push callbacks come off the streaming scheduler (Pitfall #7):
         - `Content` → throttle: if `System.nanoTime() - lastRenderNanos < 50_000_000L` (20 Hz), coalesce via simple "append to buffer now, render on next tick" — simplest correct impl: always call `activeAssistantBubble.appendMarkdown(content.markdownChunk())` then `lastRenderNanos = System.nanoTime();`. Executor may add a Flux `.sample(Duration.ofMillis(50))` operator in the subscribe chain instead — either works, pick the simpler path and note in SUMMARY.
         - `ToolCall` → create `new ToolCallCardComponent(tc.toolCallId(), tc.toolName(), tc.argsJson(), messages)`, put in `toolCardsByCallId`, add to `messageList` after `activeAssistantBubble`.
         - `ToolResult` → `toolCardsByCallId.get(tr.toolCallId()).setResult(tr.summary(), tr.outcome());`
         - `Citation` → currently: render inline `[n]` marker by appending to assistant bubble; clicking opens `new CitationDialog(c.index(), c.documentId(), documentName, c.snippet(), messages).open()`. Document name lookup deferred — use `c.documentId().toString()` as the header title fallback. Note in SUMMARY.
         - `Final` → no-op in handleEvent (stream completion triggers `finishStream()` via the `.subscribe(onComplete)` arg).
         - `Error` → call `Notification.show(messages.getMessage(e.messageKey(), null, UI.getCurrent().getLocale()))` with theme `ERROR`; then `finishStream()`.

       - `private void handleError(Throwable t)`:
         - `getUI().ifPresent(ui -> ui.access(() -> { Notification.show(messages.getMessage("chatView.error.generic", null, ui.getLocale())).addThemeVariants(NotificationVariant.LUMO_ERROR); finishStream(); }));`

       - `private void finishStream()`:
         - `getUI().ifPresent(ui -> ui.access(() -> { sendButton.setVisible(true); stopButton.setVisible(false); messageInput.setEnabled(true); activeAssistantBubble = null; activeStream = null; }));`

       - `private void onStopClick()`:
         - `if (activeStream != null) { activeStream.dispose(); }`
         - `if (activeAssistantBubble != null) { activeAssistantBubble.markStopped(); }`
         - `finishStream();`

       - `@Subscribe public void onDetach(DetachEvent event)` (Vaadin lifecycle — adapt to Jmix Fragment detach hook):
         - `if (activeStream != null && !activeStream.isDisposed()) { activeStream.dispose(); }` — Pitfall #8.

       - Class-level Javadoc: "D-29 substrate. Composed by ChatView (full route) in v1; v2 floating launcher embeds the SAME fragment unchanged. All UI mutations under UI.access (RESEARCH Pitfall #7). Stream disposed on detach (Pitfall #8)."

    3. Rule 1 tolerances to carry forward:
       - Jmix 2.8 Fragment base class and annotation (`@FragmentDescriptor`, `Fragment<T>` generic parameter) — consult skill `jmix-fragments` during execution; adapt if needed.
       - `TextArea` key-press listener API in Jmix Flow UI 2.8 may be `addKeyPressListener` or use `Shortcuts.addShortcutListener(component, action, Key.ENTER)` — adapt.
       - `Fragment.InitEvent` vs `AttachEvent` — executor picks whichever the Jmix 2.8 Fragment API actually publishes.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:compileJava :ai-agent:ai-agent:processResources</automated>
  </verify>
  <done>
    - compileJava + processResources succeed.
    - `grep -c "ChatService" ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java` prints `2` or more (import + field).
    - `grep -c "ui.access\|UI.access\|getUI().ifPresent" ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java` prints `3` or more (handleEvent + handleError + finishStream).
    - `grep -c "activeStream\.dispose\|activeStream.dispose()" ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java` prints `2` or more (Stop + onDetach).
    - `grep -c "msg://chatView\." ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/fragment/chat-panel-fragment.xml` prints `5` or more.
    - No hardcoded visible strings in the XML (every `text=` or `placeholder=` attribute starts with `msg://`).
  </done>
</task>

<task type="auto">
  <name>Task 3: ChatView route host + XML descriptor + conversationId routing</name>
  <read_first>
    - .planning/phases/07-flow-ui/07-UI-SPEC.md §Routing contract (D-26)
    - .planning/phases/07-flow-ui/07-CONTEXT.md §D-26, D-29
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java (just created)
    - Jmix skill `jmix-views` for @Route + QueryParameters pattern
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/ChatView.java,
    ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/chat-view.xml
  </files>
  <action>
    1. Create `chat-view.xml`:

       ```xml
       <?xml version="1.0" encoding="UTF-8"?>
       <view xmlns="http://jmix.io/schema/flowui/view"
             title="msg://chatView.title">
           <layout padding="true" spacing="true">
               <hbox id="headerBar" width="100%" alignItems="CENTER" justifyContent="END" padding="false">
                   <button id="newChatButton" text="msg://chatView.action.newChat"
                           themeNames="tertiary"/>
               </hbox>
               <fragment id="chatPanel" class="com.vn.agent.view.chat.fragment.ChatPanelFragment" width="100%" height="100%"/>
           </layout>
       </view>
       ```

       Rule 1 tolerance: if Jmix 2.8 XML does not allow `<fragment class="...">` declaration in view descriptors, fall back to programmatic composition in `onInit()`: instantiate the Fragment via `UiComponents.create(ChatPanelFragment.class)` and `getContent().add(fragment)`.

    2. Create `ChatView.java`:
       - `@Route(value = "ai-agent/chat", layout = DefaultMainViewParent.class) @ViewController("AiAgent_Chat") @ViewDescriptor("chat-view.xml") public class ChatView extends StandardView implements HasUrlParameter<String> { ... }` — or equivalent Jmix route-parameter idiom. If `HasUrlParameter` isn't the right Jmix 2.8 hook, use `BeforeEnterObserver.beforeEnter(BeforeEnterEvent)` and read QueryParameters from the event. Pick whichever Jmix 2.8 actually supports.
       - Fields:
         - `@ViewComponent private ChatPanelFragment chatPanel;`
         - `@ViewComponent private JmixButton newChatButton;`
         - `@Autowired private DialogWindows dialogWindows;` (for confirm dialog)
         - `@Autowired private MessageSource messages;`
         - `@Autowired private Notifications notifications;`

       - `@Subscribe public void onInit(InitEvent event) { newChatButton.addClickListener(e -> onNewChatClick()); }`

       - Route parameter handling — whichever hook is correct:
         ```java
         @Override
         public void setParameter(BeforeEvent event, @OptionalParameter String unused) {
             QueryParameters params = event.getLocation().getQueryParameters();
             List<String> idValues = params.getParameters().getOrDefault("conversationId", List.of());
             if (!idValues.isEmpty()) {
                 try {
                     UUID id = UUID.fromString(idValues.get(0));
                     chatPanel.setConversationId(id);
                 } catch (IllegalArgumentException ex) {
                     notifications.create(messages.getMessage("chatView.error.generic", null, UI.getCurrent().getLocale())).show();
                     // Fall through: chatPanel remains in new-chat mode.
                 }
             }
         }
         ```
         Catch `ConversationNotFoundException` (thrown by ConversationGateway.loadOwned when the id is not owned by the current user) in a `try/catch` around `chatPanel.setConversationId(id)` — on catch, show the UI-SPEC "Conversation not available" notification (use key `chatView.error.generic` as fallback if a more specific key isn't in the bundle) and continue with a new chat.

       - `private void onNewChatClick()`:
         - If the chat panel has no messages yet, just reset.
         - Otherwise show a confirm dialog via `dialogWindows.confirmDialog(this).withHeader(msg("chatView.confirm.newChat.title")).withText(msg("chatView.confirm.newChat.body")).withActions(DialogAction.Type.OK.handler(e -> resetToNewChat()), DialogAction.Type.CANCEL).open();` — adapt to actual Jmix 2.8 confirm-dialog API.
         - `resetToNewChat()` calls `chatPanel.setConversationId(null)` and clears its message list.

       - Class-level Javadoc: "D-29 composition point. ChatView owns only the route + page chrome (New chat button). All chat logic lives in ChatPanelFragment so v2 floating launcher embeds the same substrate."
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:compileJava :ai-agent:ai-agent:processResources</automated>
  </verify>
  <done>
    - compileJava + processResources green.
    - `grep -c "AiAgent_Chat" ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/ChatView.java` prints `1` or more (matches @ViewController id used by menu.xml in 07-01).
    - `grep -c "ai-agent/chat" ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/ChatView.java` prints `1` (matches D-26 route).
    - `grep -c "ChatPanelFragment" ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/ChatView.java` prints `2` or more.
    - `grep -c "conversationId" ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/ChatView.java` prints `1` or more (query param handled).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM stream chunks → Html render | Assistant markdown crosses into browser DOM — MUST go through MarkdownRenderer.toSafeHtml |
| Query param conversationId → ConversationGateway | User-supplied UUID; ownership enforced at gateway |
| Streaming scheduler thread → Vaadin UI | Must cross via UI.access() — Pitfall #7 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Tampering / XSS | MessageBubbleComponent.appendMarkdown | mitigate | All HTML produced via MarkdownRenderer.toSafeHtml (OWASP sanitizer). Plan 07-07 MarkdownRendererXssTest asserts `<script>` + `javascript:` + `onerror=` stripped. |
| T-07-09 | Elevation | ChatView conversationId param | mitigate | ConversationGateway enforces ownership (Phase 4 invariant). Invalid/non-owned id → Notification + fallthrough; raw gateway exception NEVER bubbled to UI. |
| T-07-10 | DoS | orphan Flux subscription on view detach | mitigate | onDetach disposes activeStream (Pitfall #8). Plan 07-07 ChatViewStreamTest verifies no subscription leaks after navigation-away. |
| T-07-11 | Information Disclosure | Error notification text | mitigate | Only i18n keys from bundle — raw exception text never shown (ties to Plan 07-02 mapToStreamingError). |
</threat_model>

<verification>
- compileJava + processResources green
- Zero hex color literals in new Java files
- Every visible string in the XML descriptors uses msg:// prefix
- ChatPanelFragment.onDetach disposes activeStream
- ChatView route id matches menu.xml entry `AiAgent_Chat` (07-01 invariant)
</verification>

<success_criteria>
UI-01 + UI-02 deliverables: streaming chat with Stop, New chat, tool-call cards, citation dialog, conversation-resume via query param — all landed as a reusable Fragment substrate (D-29) ready for v2 embedding.
</success_criteria>

<output>
After completion, create `.planning/phases/07-flow-ui/07-03-SUMMARY.md` capturing:
- Jmix 2.8 Fragment API shape actually used (Fragment<T> vs Composite<T>) and why
- How 20 Hz debounce was implemented (manual timestamp check vs Flux.sample)
- Any Rule 1 deviations (ConversationGateway read-only loader shape, citation document name lookup path)
- Whether `<fragment class="...">` in view XML was accepted, or fell back to programmatic composition
</output>
