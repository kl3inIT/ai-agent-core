---
phase: 14-intent-driven-extraction-form-prefill
plan: 04
type: execute
wave: 3
depends_on: ["02", "03"]
files_modified:
  - ai-agent/ai-agent/src/main/java/com/vn/agent/ChatService.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/tools/AgentToolCallbacks.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/guard/AgentSystemPromptRulesComposer.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/RunContext.java
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties
autonomous: true
requirements:
  - EXTRACT-01
  - EXTRACT-03
  - EXTRACT-05
  - EXTRACT-06
tags:
  - chat-service
  - tool-gating
  - prompt-contract
  - streaming
must_haves:
  truths:
    - "D-05 - named-intent turns expose only prepare_form_draft; Auto turns retain the full tool surface"
    - "D-06 - named-intent system prompt suffix instructs the assistant to call prepare_form_draft and ask for missing information instead of inventing values"
    - "D-18 - chat plumbing passes all active AiTaskFile media into the extraction input context"
    - "D-19 - named-intent turn may have zero files and rely on the userMessage text"
    - "Named intent selection is per turn; no global/session tool-surface mutation is introduced"
    - "Review MEDIUM - streaming fallback preserves the already-resolved Phase 13 BLK-01 media/context path and does not double-call prepare_form_draft"
    - "Review MEDIUM - stale/unknown intent ids and prepare_form_draft callback misconfiguration fail closed with localized user-visible errors before LLM invocation"
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/ChatService.java"
      provides: "Intent-aware overloads for blocking and streaming chat"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/tools/AgentToolCallbacks.java"
      provides: "Per-turn callback filtering for named intents"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/guard/AgentSystemPromptRulesComposer.java"
      provides: "Named-intent rule composer"
---

<objective>
Thread the selected intent through the chat pipeline, make tool gating structural, and add the named-intent prompt rule. After this plan, a named-intent chat turn can only call `prepare_form_draft`, while Auto turns continue to use the existing read/link/mutation/contributor tool surface.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-intent-driven-extraction-form-prefill/14-SPEC.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-CONTEXT.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-AI-SPEC.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-RESEARCH.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add intent-aware ChatService overloads</name>
  <read_first>
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/ChatService.java`
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java`
    - callers of `chatService.ask` and `chatService.stream`
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/ChatService.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java
  </files>
  <action>
    1. Add overloads rather than breaking existing callers:
       - `ChatResponseDto ask(String userId, UUID conversationId, String message, Overrides overrides, String intentId)`
       - `Flux<StreamingEvent> stream(String userId, UUID conversationId, String message, Overrides overrides, String intentId)`
    2. Existing overloads delegate with `intentId = null`.
    3. Normalize blank/Auto intent ids to null at the service boundary.
    4. Preserve all existing guard order: rate limit before conversation creation, token budget after conversation load, iteration counter lifecycle, RAG filter setup, media resolution.
    5. Pass normalized `intentId` into blocking and streaming internal helpers.
  </action>
  <verify>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/java/com/vn/agent/ChatService.java' -Pattern 'intentId').Count -ge 2"</automated>
    <automated>./gradlew :ai-agent:ai-agent:compileJava</automated>
  </verify>
  <done>ChatService supports per-turn intent id without breaking existing callers.</done>
</task>

<task type="auto">
  <name>Task 2: Filter callbacks to prepare_form_draft for named intents</name>
  <read_first>
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/tools/AgentToolCallbacks.java`
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/ExtractionToolBridge.java`
    - Spring AI `ToolCallback` name accessors available in the current version
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/tools/AgentToolCallbacks.java
  </files>
  <action>
    1. Add `callbacksFor(String userId, UUID conversationId, String intentId)`.
    2. Existing `callbacksFor(userId, conversationId)` delegates with null.
    3. When intent id is null, return the same full callback list as today.
    4. When intent id is non-null, build callbacks and filter to exactly callback name `prepare_form_draft`.
    5. Fail closed if zero or more than one matching callback is found by throwing a stable configuration exception before LLM invocation. Map it to localized chat error/toast copy such as `chatView.intent.configurationError`; do not return a stack trace and do not let the model run with the full tool surface.
    6. Keep audit decorators intact around `prepare_form_draft`.
    7. Add tests ensuring mutation/read/link/contributor tools are absent for named-intent turns.
  </action>
  <verify>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/java/com/vn/agent/tools/AgentToolCallbacks.java' -Pattern 'prepare_form_draft','intentId','callbacksFor').Count -ge 3"</automated>
  </verify>
  <done>Named-intent callback list contains only the audited `prepare_form_draft` callback.</done>
</task>

<task type="auto">
  <name>Task 3: Add named-intent prompt rule composition</name>
  <read_first>
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/guard/AgentSystemPromptRulesComposer.java`
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/guard/AgentSystemPromptRules.java`
    - `ai-agent/ai-agent/src/test/java/com/vn/agent/guard/AgentSystemPromptRulesComposerTest.java`
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/guard/AgentSystemPromptRulesComposer.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java
  </files>
  <action>
    1. Add an overload like `effectiveRules(IntentOption selectedIntent)` or `effectiveRules(String intentId, String label)`.
    2. Preserve the existing no-arg behavior.
    3. Append named-intent rules only when intent id is non-null:
       - The assistant MUST call `prepare_form_draft("<intentId>", contextRefs)`.
       - The assistant MUST call it at most once for the turn.
       - If values are incomplete or ambiguous, ask for missing information instead of inventing values.
       - Draft promotion happens only after the user clicks the Jmix detail view Save.
    4. Resolve the selected intent through `IntentRegistry` in `DefaultChatServiceImpl` before composing the prompt. If the id is stale/unknown for the current user, return a localized stable error such as `chatView.intent.unknownIntent`, do not call the LLM, and do not expose any callbacks.
    5. Resolve intent label through `IntentRegistry` in `DefaultChatServiceImpl` if needed, not in the view.
    6. Ensure prompt text does not include raw file content or payloadJson.
  </action>
  <verify>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/java/com/vn/agent/guard/AgentSystemPromptRulesComposer.java' -Pattern 'prepare_form_draft','intentId','effectiveRules').Count -ge 3"</automated>
  </verify>
  <done>System prompt carries named-intent rules only for named-intent turns.</done>
</task>

<task type="auto">
  <name>Task 4: Wire intent into blocking and streaming model calls</name>
  <read_first>
    - intent-aware overloads from Task 1
    - callback filtering from Task 2
    - prompt composer from Task 3
    - `DefaultChatServiceImpl.executeBlockingTurn(...)` and streaming path
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/RunContext.java,
    ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties,
    ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties
  </files>
  <action>
    1. Thread `intentId` through blocking and streaming helper methods.
    2. Use intent-aware system prompt rules before `SystemPromptComposer.compose(...)`.
    3. Call `toolCallbacks.callbacksFor(userId, convId, intentId)`.
    4. If `RunContext` needs intent id for audit/tool body lookup, add set/clear methods and ensure `finally/doFinally` clears it.
    5. Do not re-run `AiTaskFileMediaResolver`; retain the existing Phase 13 BLK-01 "resolve once per turn" invariant and pass the already-resolved media/context references into fallback code.
    6. Preserve streaming fallback path: if streaming is unsupported, pass the same `intentId` and already-resolved media/context to `executeBlockingTurn(...)`.
    7. Guard named-intent turns so the same assistant turn cannot invoke `prepare_form_draft` twice, including the streaming-unsupported fallback path.
    8. Add `chatView.intent.configurationError` and `chatView.intent.unknownIntent` to both root locale bundles for the fail-closed paths above.
  </action>
  <verify>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java' -Pattern 'callbacksFor\\(userId, convId, intentId\\)|effectiveRules\\(.*intent|executeBlockingTurn\\(.*intent').Count -ge 2"</automated>
    <automated>powershell -Command "$keys=@('chatView.intent.configurationError','chatView.intent.unknownIntent'); foreach($f in @('ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties','ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties')){ foreach($k in $keys){ if(-not (Select-String -Path $f -Pattern ('^'+[regex]::Escape($k)+'=') -Quiet)){ throw \"$f missing $k\" } } }"</automated>
    <automated>./gradlew :ai-agent:ai-agent:compileJava</automated>
  </verify>
  <done>Intent id affects both prompt rules and tool callback list across blocking, streaming, and fallback paths.</done>
</task>

<task type="auto">
  <name>Task 5: Add chat plumbing tests</name>
  <read_first>
    - `ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/AgentToolCallbacksDefaultConfigTest.java`
    - `ai-agent/ai-agent/src/test/java/com/vn/agent/DefaultChatServiceImplStreamFallbackTest.java`
    - `ai-agent/ai-agent/src/test/java/com/vn/agent/guard/AgentSystemPromptRulesComposerTest.java`
  </read_first>
  <files>
    ai-agent/ai-agent/src/test/java/com/vn/agent/extraction/AgentToolCallbacksIntentGatingTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/guard/AgentSystemPromptRulesComposerIntentTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/DefaultChatServiceIntentRoutingTest.java
  </files>
  <action>
    1. Assert Auto/null intent returns baseline callbacks.
    2. Assert named intent returns exactly one callback named `prepare_form_draft`.
    3. Assert composer output contains named-intent rule only for named intent.
    4. Assert missing or duplicate `prepare_form_draft` callbacks surface localized fail-closed errors and do not call the LLM.
    5. Assert stale/unknown intent ids surface localized errors and do not call the LLM.
    6. Assert streaming fallback preserves intent id when delegating to blocking helper and does not double-call `prepare_form_draft`.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "*IntentGatingTest" --tests "*ComposerIntentTest" --tests "*ChatServiceIntentRoutingTest"</automated>
  </verify>
  <done>Tool gating and prompt routing are locked under tests.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Component | Risk | Mitigation |
|-----------|-----------|------|------------|
| T-14-13 | Tool callbacks | Named turn exposes mutation/read tools | Exact callback-name filtering and tests |
| T-14-14 | Prompt rules | Model ignores selected intent | System-prompt suffix with must-call instruction |
| T-14-15 | Thread context | Intent leaks into next request | RunContext cleared in existing finally/doFinally lifecycle |
| T-14-16 | Fallback path | Non-streaming model bypasses tool gating | Intent passed into blocking fallback helper |
</threat_model>

<verification>
- `./gradlew :ai-agent:ai-agent:compileJava`
- `./gradlew :ai-agent:ai-agent:test --tests "*Intent*"`
- Existing chat service and callback tests still pass.
- JetBrains file-problem checks on touched Java files.
</verification>

<success_criteria>
1. Existing chat callers compile unchanged.
2. Named-intent callers can pass an intent id.
3. Named-intent turns expose only `prepare_form_draft`.
4. Auto turns retain the existing tool surface.
5. Named-intent prompt rule appears only for named-intent turns.
</success_criteria>

<output>
Create `.planning/phases/14-intent-driven-extraction-form-prefill/14-04-SUMMARY.md` with API signature changes, callback counts, prompt suffix text, and test results.
</output>
