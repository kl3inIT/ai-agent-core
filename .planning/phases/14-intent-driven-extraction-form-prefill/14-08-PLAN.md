---
phase: 14-intent-driven-extraction-form-prefill
plan: 08
type: execute
wave: 4
depends_on: ["01", "02", "03", "04", "05", "06", "07"]
files_modified:
  - ai-agent/ai-agent/src/test/java/com/vn/agent/guard/ToolNavigationLeakScannerTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/extraction/DraftSetValueBypassScannerTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/extraction/CoreCustomerImportScannerTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/extraction/ExtractionEvaluationContractTest.java
  - ai-agent/ai-agent/src/test/resources/eval/extraction-fixtures.yaml
autonomous: true
requirements:
  - EXTRACT-01
  - EXTRACT-02
  - EXTRACT-03
  - EXTRACT-04
  - EXTRACT-05
  - EXTRACT-06
  - EXTRACT-07
  - EXTRACT-08
  - EXTRACT-09
  - EXTRACT-10
  - ENT-08
  - SPI-12
  - TEST-15
  - SEC-06
tags:
  - verification
  - scanners
  - evals
  - security
must_haves:
  truths:
    - "D-03 - scanners/evals verify collection and non-modifiable attrs are not extracted/applied"
    - "D-04 - eval contract treats schema parse failure as FAILED audit and no confirm button"
    - "D-05 - callback scanner verifies named intent has only prepare_form_draft"
    - "D-09 - TEST-15 verifies tool-bearing classes do not navigate"
    - "D-10 - UI tests verify confirm row appears in the message-list area"
    - "D-11 - stale/expired draft test disables confirm row instead of loading a different draft"
    - "D-12 - audit tests verify two append-only TOOL rows for successful apply"
    - "D-13 - audit tests verify denied-attributes list cap"
    - "D-14 - exposure denial test verifies one DENIED row and no draft"
    - "D-15 - final verification follows D-12 two-row audit, not SPEC's older single-row wording"
    - "D-20 - scanner verifies Customer reference code stays out of ai-agent core"
    - "D-21 - reference eval verifies name/email/phone only"
    - "D-22 - config test verifies customer-reference property can disable the bean"
  artifacts:
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/guard/ToolNavigationLeakScannerTest.java"
      provides: "TEST-15 guard against ViewNavigators/navigation in tool-bearing classes"
    - path: "ai-agent/ai-agent/src/test/resources/eval/extraction-fixtures.yaml"
      provides: "Reference extraction eval fixtures and expected fields"
---

<objective>
Add the final cross-cutting verification gates for Phase 14. This plan does not introduce new runtime features; it closes TEST-15, raw-prefill bypass risk, host/core boundary risk, audit/eval coverage, locale parity, and end-to-end build verification.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-intent-driven-extraction-form-prefill/14-SPEC.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-CONTEXT.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-AI-SPEC.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-UI-SPEC.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-RESEARCH.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-01-PLAN.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-02-PLAN.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-03-PLAN.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-04-PLAN.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-05-PLAN.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-06-PLAN.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-07-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement TEST-15 navigation leak scanner</name>
  <read_first>
    - `ai-agent/ai-agent/src/test/java/com/vn/agent/guard/ToolNameLeakScannerTest.java`
    - all `@Tool`-bearing classes under `ai-agent/ai-agent/src/main/java`
    - all `ToolContributor` implementations in add-on tests if scanner covers contributors
  </read_first>
  <files>
    ai-agent/ai-agent/src/test/java/com/vn/agent/guard/ToolNavigationLeakScannerTest.java
  </files>
  <action>
    1. Add a source scanner or reflection-based scanner for all `@Tool`-bearing classes.
    2. Fail if any scanned class imports `io.jmix.flowui.ViewNavigators`, references `ViewNavigators`, or calls `.navigate()`.
    3. Include `ExtractionToolBridge` explicitly in the test.
    4. Include a clear failure message listing offending files/classes.
    5. Do not flag `OpenFormWithDraftHandler`; it is not tool-bearing and is the allowed navigation owner.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "*ToolNavigationLeakScannerTest"</automated>
  </verify>
  <done>TEST-15 scanner fails fast on tool-side navigation leakage.</done>
</task>

<task type="auto">
  <name>Task 2: Add raw setValue and core Customer import scanners</name>
  <read_first>
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/DraftLoader.java`
    - `jmix-app/src/main/java/com/vn/jmixapp/entity/Customer.java`
  </read_first>
  <files>
    ai-agent/ai-agent/src/test/java/com/vn/agent/extraction/DraftSetValueBypassScannerTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/extraction/CoreCustomerImportScannerTest.java
  </files>
  <action>
    1. Add scanner verifying `EntityValues.setValue` under `com.vn.agent.extraction` appears only in `DraftLoader.setValueIfPermitted`.
    2. Add scanner verifying `ai-agent/ai-agent/src/main/java` contains no `com.vn.jmixapp.entity.Customer` import/reference.
    3. Add scanner verifying `StreamEventRenderer` contains no `ViewNavigators` or `.navigate()`.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "*DraftSetValueBypassScannerTest" --tests "*CoreCustomerImportScannerTest"</automated>
  </verify>
  <done>Source-level boundaries for draft prefill and host reference isolation are enforced.</done>
</task>

<task type="auto">
  <name>Task 3: Add extraction evaluation fixture contract</name>
  <read_first>
    - `.planning/phases/14-intent-driven-extraction-form-prefill/14-AI-SPEC.md` evaluation dimensions
    - `ai-agent/ai-agent/src/test/resources/eval/structured-output-fixtures.yaml`
    - existing eval-style tests under `ai-agent/ai-agent/src/test/java/com/vn/agent/live` and `guard`
  </read_first>
  <files>
    ai-agent/ai-agent/src/test/resources/eval/extraction-fixtures.yaml,
    ai-agent/ai-agent/src/test/java/com/vn/agent/extraction/ExtractionEvaluationContractTest.java
  </files>
  <action>
    1. Create YAML fixtures for:
       - happy customer extraction: expected `name`, `email`, `phone`.
       - schema discipline failure: markdown fenced JSON or extra keys.
       - source faithfulness failure: fabricated value not in source.
       - denied attribute payload: one permitted and one denied field.
       - expired draft click.
       - exposure denied entity.
    2. Tests may be deterministic/non-live: assert local service/schema/audit behavior against fixture payloads instead of calling a real model.
    3. Record known v1.1 gap for deduplication awareness in test comments; do not fail the phase for no dedupe.
    4. Ensure no raw PII values from fixtures are written to audit result summaries.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "*ExtractionEvaluationContractTest"</automated>
  </verify>
  <done>AI-SPEC critical failure modes are represented in deterministic tests/fixtures.</done>
</task>

<task type="auto">
  <name>Task 4: Run full Phase 14 verification suite and fix regressions</name>
  <read_first>
    - Summaries from Plans 01-07 if present
    - `14-AI-SPEC.md` Guardrails and Production Monitoring sections
    - `14-UI-SPEC.md` acceptance criteria
  </read_first>
  <files>
    ai-agent/ai-agent/src/test/java/com/vn/agent/i18n/LocaleParityTest.java
  </files>
  <action>
    1. Run all Phase 14 targeted tests:
       - `*AiExtractionDraft*`
       - `*IntentRegistry*`
       - `*MetaClassDtoSynthesizer*`
       - `*ExtractionService*`
       - `*ExtractionToolBridge*`
       - `*IntentGating*`
       - `*DraftLoader*`
       - `*OpenFormWithDraft*`
       - `*IntentCardRow*`
       - `*CustomerDraft*`
       - `*ToolNavigationLeakScannerTest`
    2. Run locale parity tests.
    3. Run module tests:
       - `./gradlew :ai-agent:ai-agent:test`
       - `./gradlew :jmix-app:test`
    4. Run `./gradlew build` if module tests pass.
    5. Fix regressions within Phase 14 files only unless a failure proves a required upstream contract mismatch.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test</automated>
    <automated>./gradlew :jmix-app:test</automated>
    <automated>./gradlew build</automated>
  </verify>
  <done>Targeted, module, and build verification pass or every residual failure is documented with owner and reason.</done>
</task>

<task type="auto">
  <name>Task 5: Manual UAT checklist for UI flow</name>
  <read_first>
    - `14-UI-SPEC.md`
    - `14-AI-SPEC.md`
    - local app run instructions in AGENTS.md
  </read_first>
  <files>
    .planning/phases/14-intent-driven-extraction-form-prefill/14-UAT-CHECKLIST.md
  </files>
  <action>
    1. Create a short UAT checklist covering:
       - start app and log in as `admin/admin`.
       - Customer intent card visible.
       - Auto default selected.
       - attach fixture customer document or type customer text.
       - select Customer intent and send.
       - confirm button appears.
       - click opens Customer detail view prefilled.
       - edit and Save.
       - draft row deleted.
       - expired draft button disables.
       - permission-denied user sees notification.
    2. Include exact expected visible messages using message keys, not hardcoded implementation prose.
  </action>
  <verify>
    <automated>powershell -Command "Test-Path '.planning/phases/14-intent-driven-extraction-form-prefill/14-UAT-CHECKLIST.md'"</automated>
  </verify>
  <done>Manual UI verification is explicit for execute-phase/UAT.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Component | Risk | Mitigation |
|-----------|-----------|------|------------|
| T-14-29 | Tool layer | Navigation primitive leaks to LLM | TEST-15 scanner |
| T-14-30 | Draft apply | Raw `setValue` bypass | Source scanner plus DraftLoader tests |
| T-14-31 | Host/core boundary | Core imports demo Customer | Source scanner |
| T-14-32 | Eval coverage | Critical AI failures untested | Deterministic eval fixtures from AI-SPEC |
</threat_model>

<verification>
- `./gradlew :ai-agent:ai-agent:test`
- `./gradlew :jmix-app:test`
- `./gradlew build`
- JetBrains file-problem checks on final touched Java/XML/properties files.
</verification>

<success_criteria>
1. TEST-15 scanner passes.
2. Draft setValue bypass scanner passes.
3. Core Customer import scanner passes.
4. Extraction eval contract test covers AI-SPEC critical failures.
5. Locale parity passes.
6. Module tests and build pass, or residual failures are documented as pre-existing/non-Phase-14.
</success_criteria>

<output>
Create `.planning/phases/14-intent-driven-extraction-form-prefill/14-08-SUMMARY.md` with scanner results, eval fixture coverage, module test results, build result, and any residual risks.
</output>
