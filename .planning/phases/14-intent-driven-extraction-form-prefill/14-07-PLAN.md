---
phase: 14-intent-driven-extraction-form-prefill
plan: 07
type: execute
wave: 4
depends_on: ["02", "03", "04", "05", "06"]
files_modified:
  - jmix-app/src/main/java/com/vn/jmixapp/ai/CustomerDraftIntentExtractor.java
  - jmix-app/src/main/resources/application.properties
  - jmix-app/src/main/resources/com/vn/jmixapp/messages_en.properties
  - jmix-app/src/main/resources/com/vn/jmixapp/messages_vi.properties
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties
autonomous: true
requirements:
  - EXTRACT-02
  - EXTRACT-03
  - EXTRACT-04
  - EXTRACT-05
  - EXTRACT-06
  - EXTRACT-07
  - EXTRACT-08
  - EXTRACT-09
tags:
  - host-reference
  - spring-ai
  - customer
  - e2e
must_haves:
  truths:
    - "D-20 - CustomerDraftIntentExtractor lives in jmix-app/src/main/java/com/vn/jmixapp/ai, not ai-agent core"
    - "D-21 - reference Customer schema covers name, email, phone and excludes recommendedProducts"
    - "D-22 - ai-agent.intents.customer-reference.enabled=true controls reference extractor registration"
    - "D-01 - reference extractor uses Map<String,Object> structured output before narrowing to Customer"
    - "D-04 - reference extractor uses prompt-only strict JSON; no provider-native strict schema flag"
    - "D-18 - extractor passes all active media into one Spring AI call"
    - "D-19 - extractor also supports zero-file userMessage extraction"
  artifacts:
    - path: "jmix-app/src/main/java/com/vn/jmixapp/ai/CustomerDraftIntentExtractor.java"
      provides: "Host demo implementation of IntentExtractor<Customer>"
---

<objective>
Ship one real host-side reference intent proving the generic engine against `jmixapp_Customer`. This plan stays outside `ai-agent` core except for shared message keys as needed. It uses Spring AI structured output, the generic schema synthesizer, active model configuration, and the existing Customer detail view to exercise the full workflow.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-intent-driven-extraction-form-prefill/14-SPEC.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-CONTEXT.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-AI-SPEC.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-RESEARCH.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-02-PLAN.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement CustomerDraftIntentExtractor in jmix-app</name>
  <read_first>
    - `jmix-app/src/main/java/com/vn/jmixapp/entity/Customer.java`
    - `jmix-app/src/main/java/com/vn/jmixapp/view/customer/CustomerDetailView.java`
    - `jmix-app/src/main/java/com/vn/jmixapp/ai/OrderSummaryToolContributor.java`
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/MetaClassDtoSynthesizer.java`
    - Spring AI structured output Context7 notes in `14-RESEARCH.md`
  </read_first>
  <files>
    jmix-app/src/main/java/com/vn/jmixapp/ai/CustomerDraftIntentExtractor.java
  </files>
  <action>
    1. Create `@Component CustomerDraftIntentExtractor implements IntentExtractor<Customer>`.
    2. Add `@ConditionalOnProperty(prefix = "ai-agent.intents.customer-reference", name = "enabled", havingValue = "true", matchIfMissing = true)` or equivalent.
    3. Return stable metadata:
       - `intentId()` = `customer-from-document` or `customer-from-pdf`; use the same id in tests and messages.
       - `label()` fallback = "Customer draft".
       - `description()` fallback = "Extract a customer name, email, and phone into a draft form."
       - `targetType()` = `Customer.class`.
       - `entityName()` = `jmixapp_Customer`.
    4. Inject `ChatClient`, `MetaClassDtoSynthesizer`, `ObjectMapper`, `Validator`.
    5. Build prompt text containing user message, strict JSON instruction, and schema from `MetaClassDtoSynthesizer`.
    6. Call Spring AI with lambda user form:
       `chatClient.prompt().user(u -> { u.text(promptText); if media not empty u.media(...); }).call().entity(new ParameterizedTypeReference<Map<String,Object>>() {})`.
    7. Convert the returned map to `Customer` with Jackson and validate Bean Validation annotations.
    8. On parse/validation failure, throw `ExtractionSchemaException` with failure code and property paths only, no raw values.
    9. Ensure no code in `ai-agent` imports `com.vn.jmixapp.entity.Customer`.
  </action>
  <verify>
    <automated>powershell -Command "Test-Path 'jmix-app/src/main/java/com/vn/jmixapp/ai/CustomerDraftIntentExtractor.java'"</automated>
    <automated>powershell -Command "(Select-String -Path 'jmix-app/src/main/java/com/vn/jmixapp/ai/CustomerDraftIntentExtractor.java' -Pattern 'IntentExtractor<Customer>','ParameterizedTypeReference<Map<String, Object>>','MetaClassDtoSynthesizer','Customer.class','jmixapp_Customer').Count -ge 5"</automated>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/java/**/*.java' -Pattern 'com.vn.jmixapp.entity.Customer').Count -eq 0"</automated>
  </verify>
  <done>Host reference extractor compiles and core remains Customer-free.</done>
</task>

<task type="auto">
  <name>Task 2: Add reference intent configuration and message keys</name>
  <read_first>
    - `jmix-app/src/main/resources/application.properties`
    - `jmix-app/src/main/resources/com/vn/jmixapp/messages_en.properties`
    - `jmix-app/src/main/resources/com/vn/jmixapp/messages_vi.properties`
    - `ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties`
    - `ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties`
  </read_first>
  <files>
    jmix-app/src/main/resources/application.properties,
    jmix-app/src/main/resources/com/vn/jmixapp/messages_en.properties,
    jmix-app/src/main/resources/com/vn/jmixapp/messages_vi.properties,
    ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties,
    ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties
  </files>
  <action>
    1. Add `ai-agent.intents.customer-reference.enabled=true` to `jmix-app/src/main/resources/application.properties` if a default explicit property is preferred for operator visibility.
    2. Add `chatView.intent.customer-from-document.label` and `.description` keys to bundles that `IntentRegistry` reads from at runtime. If the chat UI resolves labels from the add-on bundle, place keys in `ai-agent` bundles; if host messages are resolved through `Messages`, duplicate in `jmix-app` bundles if needed.
    3. Keep en/vi parity.
  </action>
  <verify>
    <automated>powershell -Command "(Select-String -Path 'jmix-app/src/main/resources/application.properties' -Pattern 'ai-agent.intents.customer-reference.enabled=true').Count -eq 1"</automated>
  </verify>
  <done>Reference extractor is enabled by default and localized.</done>
</task>

<task type="auto">
  <name>Task 3: Add host integration and workflow tests</name>
  <read_first>
    - `jmix-app/src/test/java/com/vn/jmixapp/ai/ChatServiceToolIntegrationTest.java`
    - `jmix-app/src/test/java/com/vn/jmixapp/ai/CustomerMutationToolIntegrationTest.java`
    - `jmix-app/src/test/java/com/vn/jmixapp/test_support/AuthenticatedAsAdmin.java`
    - `ai-agent/ai-agent/src/test/resources/eval/structured-output-fixtures.yaml`
  </read_first>
  <files>
    jmix-app/src/test/java/com/vn/jmixapp/ai/CustomerDraftIntentExtractorTest.java,
    jmix-app/src/test/java/com/vn/jmixapp/ai/CustomerDraftWorkflowTest.java
  </files>
  <action>
    1. Mock/stub `ChatClient` where possible so tests do not require a live model.
    2. Test the extractor maps fixture JSON to a `Customer` with `name`, `email`, `phone`.
    3. Test `recommendedProducts` is absent from the generated schema.
    4. Test zero-file input with userMessage only.
    5. Test one fixture task-file/media input is passed to the Spring AI call.
    6. Add an integration workflow test: select/reference intent path -> prepare draft -> render/open handler -> Customer detail view prefilled -> Save -> draft deleted. If full UI path is too slow/flaky, split into service + handler integration tests and document the manual UAT command in the summary.
  </action>
  <verify>
    <automated>./gradlew :jmix-app:test --tests "*CustomerDraftIntentExtractorTest" --tests "*CustomerDraftWorkflowTest"</automated>
  </verify>
  <done>Reference Customer intent is covered in the host module without live LLM dependency.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Component | Risk | Mitigation |
|-----------|-----------|------|------------|
| T-14-25 | Host extractor | Core depends on sample Customer | Source scanner rejects Customer imports in `ai-agent` |
| T-14-26 | Model output | Extra keys or wrong types | Map conversion + Jackson narrowing + Bean Validation |
| T-14-27 | PII | Validation/audit logs raw values | Exception codes/property paths only |
| T-14-28 | Operator config | Unwanted reference intent in host app | Conditional property disables the bean |
</threat_model>

<verification>
- `./gradlew :jmix-app:compileJava`
- `./gradlew :jmix-app:test --tests "*CustomerDraft*"`
- Source grep: no `com.vn.jmixapp.entity.Customer` import under `ai-agent/ai-agent/src/main/java`.
- JetBrains file-problem checks on touched Java/properties files.
</verification>

<success_criteria>
1. Reference extractor lives in `jmix-app/ai`.
2. Core add-on has zero Customer imports.
3. Customer schema includes only `name`, `email`, `phone`.
4. Extractor supports media and zero-file userMessage.
5. Host tests prove draft workflow against Customer.
</success_criteria>

<output>
Create `.planning/phases/14-intent-driven-extraction-form-prefill/14-07-SUMMARY.md` with extractor id, config flag, schema fields, host/core boundary scan, and test results.
</output>
