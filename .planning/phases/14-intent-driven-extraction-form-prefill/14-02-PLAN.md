---
phase: 14-intent-driven-extraction-form-prefill
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-agent/ai-agent/src/main/java/com/vn/agent/spi/IntentExtractor.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/ExtractionInput.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/ExtractionResult.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/IntentOption.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/IntentRegistry.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/MetaClassDtoSynthesizer.java
autonomous: true
requirements:
  - SPI-12
  - EXTRACT-02
  - EXTRACT-03
  - EXTRACT-05
tags:
  - spi
  - schema
  - spring-ai
  - access-control
must_haves:
  truths:
    - "D-01 - structured-output target is Map<String,Object>; schema is prompt text, not runtime DTO bytecode"
    - "D-02 - to-one foreign keys are represented as UUID strings in the schema"
    - "D-03 - schema excludes system/audit, non-modifiable, collection, and computed-without-setter attributes"
    - "D-04 - strict mode is prompt-only; no provider-native json_schema strict flag is introduced"
    - "D-08 - IntentRegistry eligibility filters named intents against LLM exposure; Auto is a UI option, not an extractor"
    - "UI-SPEC - named intent options are sorted by locale-resolved label with intentId as a deterministic tiebreaker"
    - "D-18 - ExtractionInput carries intentId, conversationId, nullable userMessage, taskFileIds, and taskFileMedia"
    - "D-19 - zero-file named-intent turns are allowed and use userMessage text"
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/spi/IntentExtractor.java"
      provides: "Host SPI for named extraction intents"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/IntentRegistry.java"
      provides: "Deterministic eligible intent lookup"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/MetaClassDtoSynthesizer.java"
      provides: "Entity-generic JSON-schema prompt instruction builder"
---

<objective>
Introduce the host extension point and schema synthesis layer. This plan gives later extraction and UI plans a stable registry of eligible named intents, a repository-toolchain-compatible `ExtractionInput` contract, and a `MetaClass`-driven schema builder filtered by Jmix security and Phase 10 exposure rules.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-intent-driven-extraction-form-prefill/14-SPEC.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-CONTEXT.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-AI-SPEC.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define IntentExtractor SPI and extraction DTO records</name>
  <read_first>
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/spi/ToolContributor.java`
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/taskfile/AiTaskFileMediaResolver.java`
    - Spring AI `Media` / project media usage in `DefaultChatServiceImpl`
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/spi/IntentExtractor.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/ExtractionInput.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/ExtractionResult.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/IntentOption.java
  </files>
  <action>
    1. Create `IntentExtractor<T>` in `com.vn.agent.spi`:
       ```java
       String intentId();
       String label();
       String description();
       Class<T> targetType();
       String entityName();
       T extract(ExtractionInput input);
       ```
    2. Create `ExtractionInput` record in `com.vn.agent.extraction` with:
       - `String intentId`
       - `UUID conversationId`
       - `String userMessage`
       - `List<UUID> taskFileIds`
       - `List<Media> taskFileMedia` or the exact local media-content type already returned by `AiTaskFileMediaResolver`
    3. Normalize null lists to empty lists in a compact constructor.
    4. Create `ExtractionResult(UUID draftId, String entityName, String instanceName)`.
    5. Create `IntentOption(String intentId, String label, String description, String entityName, boolean auto)` for UI use. Make Auto a static factory or constant in UI code later, not an `IntentExtractor`.
  </action>
  <verify>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/java/com/vn/agent/spi/IntentExtractor.java' -Pattern 'interface IntentExtractor','intentId','targetType','extract').Count -ge 4"</automated>
    <automated>./gradlew :ai-agent:ai-agent:compileJava</automated>
  </verify>
  <done>SPI and DTO records compile and are independent of host entities.</done>
</task>

<task type="auto">
  <name>Task 2: Implement IntentRegistry with deterministic eligibility</name>
  <read_first>
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/exposure/LlmExposurePolicy.java`
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/exposure/AiInternalEntityNames.java`
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/metadata/CurrentUserSchemaAccess.java`
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/IntentRegistry.java
  </files>
  <action>
    1. Inject `List<IntentExtractor<?>>`, `Metadata`, `LlmExposurePolicy`, `Messages`, and `CurrentAuthentication` if localized label resolution is implemented here.
    2. Expose `Optional<IntentExtractor<?>> find(String intentId)`.
    3. Expose `List<IntentOption> eligibleForCurrentUser()` or equivalent.
    4. For each extractor, resolve its `entityName()` to a `MetaClass`. If unknown or hidden by `LlmExposurePolicy.canReadEntity/canCreate` as appropriate, exclude it.
    5. Resolve each display label from `chatView.intent.{intentId}.label` in the current user's Jmix locale with `IntentExtractor.label()` as fallback, matching UI-SPEC. Sort deterministically by that locale-resolved label, then `intentId` as tiebreaker.
    6. Do not cache eligibility globally; exposure rules and user security are per request.
    7. Throw a stable `UnknownIntentException` for missing ids; do not return null.
  </action>
  <verify>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/IntentRegistry.java' -Pattern 'List<IntentExtractor','eligible','LlmExposurePolicy','Comparator','CurrentAuthentication|Locale').Count -ge 5"</automated>
  </verify>
  <done>Registry resolves and sorts eligible named intents per current user.</done>
</task>

<task type="auto">
  <name>Task 3: Implement MetaClassDtoSynthesizer</name>
  <read_first>
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/BaselineContextProvider.java`
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/tools/DescribeEntityPayloadBuilder.java` or nearest describe-entity schema builder
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/tools/mutation/MutationAttributeBinder.java`
    - Context7 Spring AI structured output Map docs from `14-RESEARCH.md`
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/MetaClassDtoSynthesizer.java
  </files>
  <action>
    1. Build a JSON object schema as text from a `MetaClass` or `Class<?>`.
    2. Include only scalar, enum, date/time, number, boolean, string, and to-one UUID fields that pass both:
       - `LlmExposurePolicy.canReadAttribute(...)` or equivalent current project access primitive.
       - `EntityAttributeContext.canModify()` after `AccessManager.applyRegisteredConstraints(...)`.
    3. Exclude `id`, `version`, `createdBy`, `createdDate`, `lastModifiedBy`, `lastModifiedDate`, `deletedBy`, `deletedDate`.
    4. Exclude `@OneToMany`, `@ManyToMany`, collection-valued properties, and computed `@JmixProperty` properties without a setter.
    5. Render to-one references as JSON schema string with `"format": "uuid"`.
    6. Use message/entity captions as descriptions when cheap and available, but never make localization required for correctness.
    7. Return a stable value object with `schemaJson`, `attributeNames`, and maybe `requiredAttributeNames`; or return string plus helper methods. Keep the public contract simple for the reference extractor.
    8. Do not create runtime DTO classes or bytecode.
  </action>
  <verify>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/MetaClassDtoSynthesizer.java' -Pattern 'EntityAttributeContext','canModify','format','uuid','payload|schema').Count -ge 5"</automated>
  </verify>
  <done>Schema synthesizer emits parseable JSON schema text filtered by exposure and write permissions.</done>
</task>

<task type="auto">
  <name>Task 4: Add SPI and schema unit tests</name>
  <read_first>
    - `ai-agent/ai-agent/src/test/java/com/vn/agent/metadata/CurrentUserSchemaAccessTest.java`
    - `ai-agent/ai-agent/src/test/java/com/vn/agent/orchestration/BaselineContextProviderTest.java`
    - `jmix-app/src/main/java/com/vn/jmixapp/entity/Customer.java`
  </read_first>
  <files>
    ai-agent/ai-agent/src/test/java/com/vn/agent/extraction/IntentRegistryTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/extraction/MetaClassDtoSynthesizerTest.java
  </files>
  <action>
    1. Unit-test registry sorting and unknown intent handling with test extractors.
    2. Unit-test exposure denial excludes an extractor.
    3. Unit-test schema JSON parses with Jackson.
    4. Unit-test Customer-like schema includes `name`, `email`, `phone` and excludes `recommendedProducts`, `id`, `version`, audit fields.
    5. Unit-test denied `EntityAttributeContext.canModify()` attributes are excluded.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "*IntentRegistryTest" --tests "*MetaClassDtoSynthesizerTest"</automated>
  </verify>
  <done>Registry and schema synthesis behavior is covered independently of LLM calls.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Component | Risk | Mitigation |
|-----------|-----------|------|------------|
| T-14-05 | Schema builder | Prompt asks model for fields the user cannot write | `EntityAttributeContext.canModify()` filter |
| T-14-06 | Schema builder | Unbounded relationship/collection prompt expansion | Exclude collections and computed no-setter properties |
| T-14-07 | Registry | Hidden entity appears as an intent option | Filter through `LlmExposurePolicy` per current user |
| T-14-08 | SPI | Host extractor returns unknown target | Registry and service validate entity name against metadata |
</threat_model>

<verification>
- `./gradlew :ai-agent:ai-agent:compileJava`
- `./gradlew :ai-agent:ai-agent:test --tests "*IntentRegistryTest" --tests "*MetaClassDtoSynthesizerTest"`
- JetBrains file-problem checks on touched Java files.
</verification>

<success_criteria>
1. `IntentExtractor<T>` SPI exists in `com.vn.agent.spi`.
2. `ExtractionInput` supports zero-file and multi-file turns.
3. `IntentRegistry` returns deterministic eligible named intents.
4. `MetaClassDtoSynthesizer` emits parseable, security-filtered JSON schema text.
5. Tests prove field inclusion/exclusion and registry filtering.
</success_criteria>

<output>
Create `.planning/phases/14-intent-driven-extraction-form-prefill/14-02-SUMMARY.md` with SPI shape, registry ordering/filtering, schema exclusion list, and test results.
</output>
