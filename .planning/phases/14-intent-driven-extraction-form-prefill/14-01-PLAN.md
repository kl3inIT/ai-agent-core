---
phase: 14-intent-driven-extraction-form-prefill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiExtractionDraft.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/AiExtractionProperties.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/AiExtractionDraftCleanupJob.java
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/110-ai-extraction-draft.xml
  - ai-agent/ai-agent/src/main/java/com/vn/agent/security/AiAgentUserRole.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/security/AiAgentUserRowLevelRole.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/exposure/AiInternalEntityNames.java
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties
autonomous: true
requirements:
  - ENT-08
  - EXTRACT-04
  - EXTRACT-09
  - SEC-06
tags:
  - jmix-entities
  - liquibase
  - security
  - lifecycle
must_haves:
  truths:
    - "D-16 - close-without-save leaves AiExtractionDraft rows for TTL cleanup; no BeforeClose deletion path is introduced"
    - "D-17 - no explicit Cancel button or cancel-specific draft deletion is added in this foundation plan"
    - "AiExtractionDraft is persisted in agentstore as ai_AiExtractionDraft and hidden through AiInternalEntityNames"
    - "New Liquibase file is 110-ai-extraction-draft.xml because 100-ai-task-file-drop-dead-columns.xml already exists"
    - "User-facing draft persistence uses secured DataManager; only cleanup uses UnconstrainedDataManager"
    - "Repository Java level is reconciled before implementation: Gradle toolchains and CLAUDE.md currently say Java 21 while AGENTS.md says Java 17; compile against Gradle's configured toolchain and avoid preview APIs"
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiExtractionDraft.java"
      provides: "Jmix entity with UUID id, Version, InstanceName, owner username, payloadJson, source ids, createdAt, expiresAt, confirmed"
    - path: "ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/110-ai-extraction-draft.xml"
      provides: "AI_EXTRACTION_DRAFT table and indexes"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/security/AiAgentUserRowLevelRole.java"
      provides: "Row-level ownership predicate on AiExtractionDraft.userUsername"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/AiExtractionDraftCleanupJob.java"
      provides: "Scheduled TTL cleanup for expired draft rows"
---

<objective>
Create the persistence, ownership, lifecycle, and i18n foundation for extraction drafts. This plan introduces `AiExtractionDraft`, its agentstore schema, user resource/row-level security, the TTL cleanup configuration/job, and LLM-surface exclusion. No extraction service or UI wiring lands here; downstream plans depend on this stable draft table and entity model.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-SPEC.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-CONTEXT.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AiExtractionDraft entity and Liquibase 110</name>
  <read_first>
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiTaskFile.java`
    - `ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/090-ai-task-file.xml`
    - `ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/100-ai-task-file-drop-dead-columns.xml`
    - `ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog.xml`
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiExtractionDraft.java,
    ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/110-ai-extraction-draft.xml,
    ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties,
    ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties
  </files>
  <action>
    1. Create `AiExtractionDraft` in `com.vn.agent.entity` with:
       - `@Store(name = "agentstore")`, `@JmixEntity`, `@Entity(name = "ai_AiExtractionDraft")`, `@Table(name = "AI_EXTRACTION_DRAFT", indexes = ...)`.
       - UUID `id` with `@JmixGeneratedValue`, `Integer version` with `@Version`.
       - `@InstanceName @NotNull String instanceName`.
       - `@NotNull String userUsername`.
       - `@NotNull String targetEntityName`.
       - `@NotNull String intentId`.
       - `@NotNull @Column(columnDefinition = "text" where locally supported by existing XML style) String payloadJson`.
       - `@NotNull UUID sourceConversationId`.
       - nullable `UUID sourceTaskFileId` for single-file compatibility, even though the runtime input carries all active files.
       - `@NotNull OffsetDateTime createdAt`, `@NotNull OffsetDateTime expiresAt`.
       - `Boolean confirmed = false` or primitive `boolean confirmed`, following existing entity style.
    2. Do not add Lombok, eager relationships, or direct constructors.
    3. Create `110-ai-extraction-draft.xml` with a table `AI_EXTRACTION_DRAFT`; use `${uuid.type}` for UUID columns and `${offsetDateTime.type}` for timestamps.
    4. Include columns `ID`, `VERSION`, `USER_USERNAME`, `TARGET_ENTITY_NAME`, `INTENT_ID`, `PAYLOAD_JSON`, `SOURCE_CONVERSATION_ID`, `SOURCE_TASK_FILE_ID`, `INSTANCE_NAME`, `CREATED_AT`, `EXPIRES_AT`, `CONFIRMED`.
    5. Add indexes at minimum on `USER_USERNAME`, `EXPIRES_AT`, and `SOURCE_CONVERSATION_ID`.
    6. Do not edit `agentstore-changelog.xml`; `includeAll` auto-discovers the new file.
    7. Add entity and attribute captions to both locale bundles.
  </action>
  <verify>
    <automated>powershell -Command "Test-Path 'ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiExtractionDraft.java'"</automated>
    <automated>powershell -Command "Test-Path 'ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/110-ai-extraction-draft.xml'"</automated>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiExtractionDraft.java' -Pattern '@JmixEntity','@Store\\(name = \"agentstore\"\\)','@Version','@InstanceName','payloadJson','expiresAt').Count -ge 6"</automated>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/110-ai-extraction-draft.xml' -Pattern 'AI_EXTRACTION_DRAFT','USER_USERNAME','EXPIRES_AT','\\$\\{uuid.type\\}','\\$\\{offsetDateTime.type\\}').Count -ge 5"</automated>
  </verify>
  <done>Draft entity and agentstore changelog exist with the locked fields and indexes; locale captions exist in both bundles.</done>
</task>

<task type="auto">
  <name>Task 2: Extend user security roles and internal entity denylist</name>
  <read_first>
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/security/AiAgentUserRole.java`
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/security/AiAgentUserRowLevelRole.java`
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/exposure/AiInternalEntityNames.java`
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/security/AiAgentUserRole.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/security/AiAgentUserRowLevelRole.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/exposure/AiInternalEntityNames.java
  </files>
  <action>
    1. Import `AiExtractionDraft` in both security role interfaces.
    2. Add `@EntityPolicy(entityClass = AiExtractionDraft.class, actions = {READ, CREATE, UPDATE, DELETE})` to `AiAgentUserRole.userAccess()`.
    3. Add `@JpqlRowLevelPolicy(entityClass = AiExtractionDraft.class, where = "{E}.userUsername = :current_user_username") void extractionDraft();` to `AiAgentUserRowLevelRole`.
    4. Add `"ai_AiExtractionDraft"` to `AiInternalEntityNames.NAMES`.
    5. Keep admin roles unchanged unless compile requires an import-only adjustment.
  </action>
  <verify>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/java/com/vn/agent/security/AiAgentUserRole.java' -Pattern 'AiExtractionDraft').Count -ge 2"</automated>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/java/com/vn/agent/security/AiAgentUserRowLevelRole.java' -Pattern 'AiExtractionDraft','userUsername = :current_user_username').Count -ge 2"</automated>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/java/com/vn/agent/exposure/AiInternalEntityNames.java' -Pattern 'ai_AiExtractionDraft').Count -eq 1"</automated>
  </verify>
  <done>User resource and row-level policies cover drafts, and drafts are excluded from the LLM-visible entity list.</done>
</task>

<task type="auto">
  <name>Task 3: Add extraction TTL properties and cleanup job</name>
  <read_first>
    - `AGENTS.md`
    - `CLAUDE.md`
    - `ai-agent/build.gradle`
    - `jmix-app/build.gradle`
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/taskfile/AiTaskFileCleanupJob.java`
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/taskfile/AiTaskFileProperties.java`
    - project configuration properties registration patterns
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/AiExtractionProperties.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/AiExtractionDraftCleanupJob.java
  </files>
  <action>
    1. Create `AiExtractionProperties` with `@ConfigurationProperties(prefix = "ai-agent.extraction")`.
    2. Add `ttlSeconds = 3600L` and `cleanupIntervalMs = 3600000L` with getters/setters. First reconcile the local Java contract by reading `AGENTS.md`, `CLAUDE.md`, and the Gradle toolchain declarations; compile against the Gradle toolchain and do not use preview/incubator APIs.
    3. Ensure the properties class is registered the same way existing `AiTaskFileProperties` is registered.
    4. Create `AiExtractionDraftCleanupJob` in `com.vn.agent.extraction`.
    5. Use `@Scheduled(fixedDelayString = "${ai-agent.extraction.cleanup-interval-ms:3600000}")`.
    6. Use `UnconstrainedDataManager` because this is trusted system-internal cleanup.
    7. Delete rows where `expiresAt < now`. Prefer secured entity metadata and `UnconstrainedDataManager.remove(...)`; if raw JPQL `loadValue/loadValues` is needed, call `.store("agentstore")`.
    8. Log a debug count when rows are removed; do not audit cleanup rows.
  </action>
  <verify>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/AiExtractionProperties.java' -Pattern 'ai-agent.extraction','ttlSeconds','cleanupIntervalMs').Count -ge 3"</automated>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/AiExtractionDraftCleanupJob.java' -Pattern '@Scheduled','UnconstrainedDataManager','expiresAt').Count -ge 3"</automated>
    <automated>powershell -Command "Select-String -Path 'ai-agent/build.gradle','jmix-app/build.gradle','AGENTS.md','CLAUDE.md' -Pattern 'JavaLanguageVersion|Java 17|Java 21' | ForEach-Object Line"</automated>
    <automated>./gradlew :ai-agent:ai-agent:compileJava</automated>
  </verify>
  <done>TTL properties bind under `ai-agent.extraction`, and a scheduled cleanup job removes expired drafts through `UnconstrainedDataManager`.</done>
</task>

<task type="auto">
  <name>Task 4: Foundation tests for schema, security, and cleanup</name>
  <read_first>
    - `ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/AiTaskFileCleanupJobTest.java`
    - `ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/TtlConfigTest.java`
    - `ai-agent/ai-agent/src/test/java/com/vn/agent/security/CrossUserConversationAccessTest.java`
    - `ai-agent/ai-agent/src/test/java/com/vn/agent/liquibase/LiquibaseSchemaTest.java`
  </read_first>
  <files>
    ai-agent/ai-agent/src/test/java/com/vn/agent/extraction/AiExtractionDraftModelTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/extraction/AiExtractionDraftCleanupJobTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/security/AiExtractionDraftSecurityTest.java
  </files>
  <action>
    1. Add a Spring Boot model/persistence test that creates `AiExtractionDraft` through `DataManager.create` or `Metadata.create`, saves it, and reloads it from `agentstore`.
    2. Add a cleanup job test that inserts one expired and one active draft, runs the job, and asserts only the expired row is gone.
    3. Add a row-level security test with two users; assert `bob` cannot load `alice`'s draft through secured `DataManager` and `alice` can.
    4. Ensure test cleanup removes test drafts with `SystemAuthenticator` / unconstrained path consistent with local test patterns.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "*AiExtractionDraftModelTest" --tests "*AiExtractionDraftCleanupJobTest" --tests "*AiExtractionDraftSecurityTest"</automated>
  </verify>
  <done>Persistence, TTL cleanup, and owner row-level security are covered before extraction logic lands.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Component | Risk | Mitigation |
|-----------|-----------|------|------------|
| T-14-01 | Draft table | Cross-user draft read | Row-level role on `userUsername`; secured `DataManager` for user paths |
| T-14-02 | Cleanup job | Cleanup bypasses security broadly | `UnconstrainedDataManager` is isolated to expired-row system cleanup only |
| T-14-03 | LLM schema surface | Draft table exposed to LLM tools | `AiInternalEntityNames` excludes `ai_AiExtractionDraft` |
| T-14-04 | Liquibase ordering | Duplicate 100 sequence creates confusing migration order | Use `110-ai-extraction-draft.xml` |
</threat_model>

<verification>
- `./gradlew :ai-agent:ai-agent:compileJava`
- `./gradlew :ai-agent:ai-agent:test --tests "*AiExtractionDraft*"`
- JetBrains file-problem checks on all touched Java/XML/properties files after implementation.
</verification>

<success_criteria>
1. `AiExtractionDraft` exists with all required fields and Jmix annotations.
2. Liquibase creates `AI_EXTRACTION_DRAFT` with UUID/timestamp portability and indexes.
3. `AiAgentUserRole` and `AiAgentUserRowLevelRole` cover the draft entity.
4. `AiInternalEntityNames` hides drafts from LLM schema/tools.
5. TTL cleanup deletes expired drafts only.
6. Foundation tests pass.
</success_criteria>

<output>
Create `.planning/phases/14-intent-driven-extraction-form-prefill/14-01-SUMMARY.md` with entity fields, changelog name, role changes, cleanup behavior, and test results.
</output>
