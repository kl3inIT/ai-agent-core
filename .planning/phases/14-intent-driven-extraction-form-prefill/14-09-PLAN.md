---
phase: 14-intent-driven-extraction-form-prefill
plan: 09
plan_id: "14-09"
type: execute
wave: 7
gap_closure: true
gap_source: 14-VERIFICATION.md
depends_on: ["01", "02", "03", "04", "05", "06", "07", "08"]
files_modified:
  - ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/ExtractionDraftAccess.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/DraftLoader.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/intent/OpenFormWithDraftHandler.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/ExtractionSourceText.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/ExtractionInput.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/RunContext.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/ExtractionToolBridge.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/ExtractionService.java
  - jmix-app/src/main/java/com/vn/jmixapp/ai/CustomerDraftIntentExtractor.java
  - jmix-app/src/main/resources/application.properties
  - jmix-app/.env.example
  - ai-agent/ai-agent/src/test/java/com/vn/agent/extraction/ExtractionDraftAccessTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/extraction/DraftLoaderTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/intent/OpenFormWithDraftHandlerTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/extraction/ExtractionServiceTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/extraction/ExtractionToolBridgeTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/IntentCardRowTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/fragment/ChatPanelFragmentConversationIdTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/DefaultChatServiceIntentRoutingTest.java
  - jmix-app/src/test/java/com/vn/jmixapp/ai/CustomerDraftIntentExtractorTest.java
  - jmix-app/src/test/java/com/vn/jmixapp/ai/CustomerDraftWorkflowTest.java
  - jmix-app/src/test/java/com/vn/jmixapp/config/ApplicationPropertiesSecretsTest.java
autonomous: true
requirements:
  - EXTRACT-01
  - EXTRACT-02
  - EXTRACT-04
  - EXTRACT-05
  - EXTRACT-07
  - EXTRACT-08
  - EXTRACT-09
  - EXTRACT-10
  - ENT-08
  - SPI-12
  - TEST-15
  - SEC-06
tags:
  - gap-closure
  - extraction-draft
  - source-faithfulness
  - chat-guard-ordering
  - credential-hygiene
must_haves:
  truths:
    - "BL-02 - every user-facing draft load path rejects rows where expiresAt <= now OR confirmed = true before applying payloads or navigating."
    - "BL-03 - first-turn streaming submit passes the current nullable conversationId into ChatService.stream; ChatPanelFragment no longer calls conversationGateway.loadOrCreate before service guards run."
    - "BL-04 - ExtractionService persists payloadJson from MetaClassDtoSynthesizer-approved attribute names only; getter-backed Jmix/entity properties such as recommendedProducts, version, class, or audit attrs cannot enter draft payload JSON."
    - "BL-05 - CustomerDraftIntentExtractor rejects extracted String values not supported by textual source evidence from userMessage or Tika-extracted task-file document text before any SUCCESS draft can be persisted."
    - "BL-01 - jmix-app/src/main/resources/application.properties contains no committed database passwords, admin default password, or host-specific database IP; local values move to environment variables or ignored .env."
    - "All new tests are pure JUnit/Mockito/source tests unless an existing targeted CustomerDraft test already runs in jmix-app; do not introduce new broad @SpringBootTest surfaces for these gaps."
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/ExtractionDraftAccess.java"
      provides: "Single secured DataManager load path for open, unexpired, unconfirmed AiExtractionDraft rows."
      contains: "e.id = :draftId and e.expiresAt > :now and e.confirmed = false"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java"
      provides: "Service-owned first-turn conversation creation after rate-limit guard."
      contains: "new StreamingEvent.Final(runId, convId"
    - path: "jmix-app/src/main/java/com/vn/jmixapp/ai/CustomerDraftIntentExtractor.java"
      provides: "Reference extractor source-faithfulness check against textual evidence."
      contains: "assertSourceFaithful"
    - path: "jmix-app/src/main/resources/application.properties"
      provides: "Environment-backed datasource and UI login defaults."
      contains: "MAIN_DATASOURCE_URL"
---

<objective>
Close the five blockers from `14-VERIFICATION.md` without expanding Phase 14 scope.

This is a gap-closure plan for:

- BL-02: expired or confirmed extraction drafts remain loadable by id.
- BL-03: first-turn streaming submit creates a conversation in the UI before service guards run.
- BL-04: the Customer reference intent can reserialize a Jmix entity into draft payload JSON beyond the synthesized schema.
- BL-05: source-faithfulness is only represented by fixtures, not enforced in production.
- BL-01: committed development database/admin defaults remain in `jmix-app/src/main/resources/application.properties`.

The plan adds a single dependent Wave 7 repair pass. It does not add new user-facing features, new AI tools, new Jmix views, new entity tables, new audit kinds, or an AI-specific exposure layer.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-intent-driven-extraction-form-prefill/14-VERIFICATION.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-SPEC.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-CONTEXT.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-AI-SPEC.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-UI-SPEC.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-01-SUMMARY.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-02-SUMMARY.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-03-SUMMARY.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-04-SUMMARY.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-05-SUMMARY.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-06-SUMMARY.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-07-SUMMARY.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-08-SUMMARY.md
@AGENTS.md

<jmix_reference_used>
- Jmix DataManager supports secured entity-class loads with JPQL/property conditions; use `dataManager.load(AiExtractionDraft.class).query(...)`, not `EntityManager`.
- Jmix AccessManager view checks use `UiShowViewContext`, `accessManager.applyRegisteredConstraints(accessContext)`, then `accessContext.isPermitted()`.
- Jmix detail views can react to `StandardDetailView.AfterSaveEvent`; current save-delete wiring may remain but the draft must be loaded through the open-draft guard before apply/open.
</jmix_reference_used>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Centralize live-draft loading and reject expired or confirmed rows</name>
  <read_first>
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiExtractionDraft.java`
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/DraftLoader.java`
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/intent/OpenFormWithDraftHandler.java`
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/AiExtractionDraftCleanupJob.java`
    - `ai-agent/ai-agent/src/test/java/com/vn/agent/extraction/DraftLoaderTest.java`
    - `ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/intent/OpenFormWithDraftHandlerTest.java`
    - `jmix-app/src/test/java/com/vn/jmixapp/ai/CustomerDraftWorkflowTest.java`
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/ExtractionDraftAccess.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/DraftLoader.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/intent/OpenFormWithDraftHandler.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/extraction/ExtractionDraftAccessTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/extraction/DraftLoaderTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/intent/OpenFormWithDraftHandlerTest.java,
    jmix-app/src/test/java/com/vn/jmixapp/ai/CustomerDraftWorkflowTest.java
  </files>
  <action>
    1. Create `com.vn.agent.extraction.ExtractionDraftAccess` as a Spring `@Component` with constructor-injected secured `DataManager`.
    2. Add method `Optional<AiExtractionDraft> loadOpenDraft(UUID draftId)`.
    3. `loadOpenDraft` MUST return `Optional.empty()` for `null` ids.
    4. `loadOpenDraft` MUST use this exact secured entity-class DataManager predicate:
       `dataManager.load(AiExtractionDraft.class).query("e.id = :draftId and e.expiresAt > :now and e.confirmed = false").parameter("draftId", draftId).parameter("now", OffsetDateTime.now()).optional()`.
    5. Do NOT use `UnconstrainedDataManager` in `ExtractionDraftAccess`; the cleanup job remains the only unconstrained draft path.
    6. Replace `DraftLoader.loadDraft(UUID)` with the new access component. If `loadOpenDraft` is empty, throw the existing `DraftNotFoundException(draftId)`.
    7. Replace `OpenFormWithDraftHandler.loadDraft(UUID)` with the new access component. If empty, return `OpenResult.expired()` and show `chatView.intent.draftExpired`.
    8. Leave `OpenFormWithDraftHandler.confirmAndDeleteDraft(UUID)` on `DataManager` by id; successful save may still set `confirmed=true` and remove the row. The open/apply paths are the ones that must reject confirmed rows.
    9. Update constructors and tests for the new dependency without using field injection.
    10. Add tests that cover all three non-open states:
        - missing draft id -> expired/not found behavior.
        - existing draft with `expiresAt` in the past -> `DraftLoader.apply(...)` throws `DraftNotFoundException` and `OpenFormWithDraftHandler.open(...)` returns `EXPIRED`.
        - existing draft with `confirmed=true` -> same behavior.
    11. Keep existing permitted-field, denied-field, relationship, permission-denial, and save-delete tests green.
  </action>
  <verify>
    <automated>./gradlew --no-daemon :ai-agent:ai-agent:test --tests "*ExtractionDraftAccessTest" --tests "*DraftLoaderTest" --tests "*OpenFormWithDraftHandlerTest"</automated>
    <automated>./gradlew --no-daemon :jmix-app:test --tests "*CustomerDraftWorkflowTest"</automated>
    <also>rg -n "e.id = :draftId and e.expiresAt > :now and e.confirmed = false" ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/ExtractionDraftAccess.java</also>
    <also>rg -n "UnconstrainedDataManager" ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/ExtractionDraftAccess.java must return zero matches</also>
  </verify>
  <done>
    BL-02 is closed: stale and confirmed draft rows cannot open a form or apply a payload even if the cleanup job has not deleted them yet.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Make first-turn streaming conversation creation service-owned</name>
  <read_first>
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java`
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java`
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/ChatService.java`
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/StreamingEvent.java`
    - `ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/fragment/ChatPanelFragmentConversationIdTest.java`
    - `ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/IntentCardRowTest.java`
    - `ai-agent/ai-agent/src/test/java/com/vn/agent/DefaultChatServiceIntentRoutingTest.java`
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/fragment/ChatPanelFragmentConversationIdTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/IntentCardRowTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/DefaultChatServiceIntentRoutingTest.java
  </files>
  <action>
    1. In `ChatPanelFragment.onSubmit(...)`, replace the eager submit path:
       `final UUID targetConversationId = ensureConversationIdForSubmit(userId, text);`
       with:
       `final UUID targetConversationId = conversationId;`
    2. Keep the existing service call shape, now passing the nullable id:
       `chatService.stream(userId, targetConversationId, text, null, selectedIntentId)`.
    3. Keep the existing `StreamingEvent.Final` consumer that assigns `conversationId = f.conversationId()` and calls `updateSessionConversationId(conversationId)` when the service returns a newly created conversation id.
    4. After final-event conversation id assignment, keep task-file loader sync by setting `taskFilesDl` parameter to the resolved id and loading it when non-null.
    5. Delete `ensureConversationIdForSubmit(...)` if no production code needs it after step 1. If deletion creates too much churn in tests, keep it package-private but add a source test proving `onSubmit(...)` no longer calls it.
    6. Do NOT change `ensureConversationIdForUpload()`: uploads still need an eager conversation FK before a file row can be persisted.
    7. Update `IntentCardRowTest.selectedNamedTurnIsSentAndThenResetToAuto` to assert the nullable-current-id path instead of the old eager helper.
    8. Update `ChatPanelFragmentConversationIdTest` so it no longer asserts submit-time creation. Add a source-level regression test that the source does NOT contain `ensureConversationIdForSubmit(userId, text)` and DOES contain `final UUID targetConversationId = conversationId`.
    9. Add/adjust a service-level Mockito test in `DefaultChatServiceIntentRoutingTest` proving `stream(USER_ID, null, MESSAGE, Overrides.NONE, INTENT_ID)` still emits a `StreamingEvent.Final` whose `conversationId()` is the id returned by `conversationGateway.loadOrCreate(...)`.
    10. Add/adjust a service-level guard-order test proving a rate-limit denial on `stream(USER_ID, null, MESSAGE, ...)` does not call `conversationGateway.loadOrCreate(...)`.
  </action>
  <verify>
    <automated>./gradlew --no-daemon :ai-agent:ai-agent:test --tests "*ChatPanelFragmentConversationIdTest" --tests "*IntentCardRowTest" --tests "*DefaultChatServiceIntentRoutingTest"</automated>
    <also>rg -n "ensureConversationIdForSubmit\\(userId, text\\)" ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java must return zero matches</also>
    <also>rg -n "final UUID targetConversationId = conversationId" ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java</also>
  </verify>
  <done>
    BL-03 is closed: on first-turn streaming submits without uploads, the UI no longer persists an `AiConversation` before `DefaultChatServiceImpl.stream(...)` runs rate-limit and token-budget guard ordering.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Persist only schema-approved draft payload attributes</name>
  <read_first>
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/ExtractionService.java`
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/ExtractionJsonSupport.java`
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/MetaClassDtoSynthesizer.java`
    - `jmix-app/src/main/java/com/vn/jmixapp/ai/CustomerDraftIntentExtractor.java`
    - `ai-agent/ai-agent/src/test/java/com/vn/agent/extraction/ExtractionServiceTest.java`
    - `jmix-app/src/test/java/com/vn/jmixapp/ai/CustomerDraftIntentExtractorTest.java`
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/ExtractionService.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/extraction/ExtractionServiceTest.java,
    jmix-app/src/test/java/com/vn/jmixapp/ai/CustomerDraftIntentExtractorTest.java
  </files>
  <action>
    1. Inject `MetaClassDtoSynthesizer` into `ExtractionService` via constructor injection.
    2. After `ExtractionJsonSupport.toPayloadMap(...)`, build the current user's schema with `schemaSynthesizer.buildSchema(metaClass)`.
    3. Add a private helper in `ExtractionService` named `filterPayloadToSchema(LinkedHashMap<String, Object> payloadMap, MetaClassDtoSynthesizer.SynthesizedSchema schema)`.
    4. `filterPayloadToSchema` MUST create a new `LinkedHashMap` in schema attribute order and copy only keys present in `schema.attributeNames()`.
    5. Use the filtered map for `payloadJson`, `extractedFieldCount`, `extractedAttributes`, and draft persistence.
    6. Do NOT include unsupported keys in audit `resultSummary`; raw values must remain absent from audit summaries.
    7. Keep unknown raw payload keys out of `payloadJson` even when the extractor returned a Jmix entity whose Jackson conversion exposes getter-backed fields.
    8. Add a test in `ExtractionServiceTest` where the mocked extractor returns a map containing:
       `name`, `email`, `recommendedProducts`, `version`, `class`.
       Stub `schemaSynthesizer.buildSchema(metaClass)` to allow only `name`, `email`, `phone`.
       Assert the captured `AiExtractionDraft.payloadJson` contains `name` and `email`, and does NOT contain `recommendedProducts`, `version`, or `class`.
    9. Add an audit assertion that `resultSummary` reports `extractedFieldCount:2` and `extractedAttributes:["name","email"]`, not the unsupported keys.
    10. Keep existing schema parse failure and exposure denied tests green.
  </action>
  <verify>
    <automated>./gradlew --no-daemon :ai-agent:ai-agent:test --tests "*ExtractionServiceTest"</automated>
    <also>rg -n "filterPayloadToSchema" ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/ExtractionService.java</also>
    <also>rg -n "schemaSynthesizer.buildSchema\\(metaClass\\)" ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/ExtractionService.java</also>
  </verify>
  <done>
    BL-04 is closed at the generic service boundary: no extractor can persist draft payload keys outside the synthesized schema for the current user.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Enforce source faithfulness on the reference Customer intent</name>
  <read_first>
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/taskfile/AiTaskFileMediaResolver.java`
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/ExtractionInput.java`
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/RunContext.java`
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java`
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/ExtractionToolBridge.java`
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/ExtractionService.java`
    - `jmix-app/src/main/java/com/vn/jmixapp/ai/CustomerDraftIntentExtractor.java`
    - `jmix-app/src/test/java/com/vn/jmixapp/ai/CustomerDraftIntentExtractorTest.java`
    - `ai-agent/ai-agent/src/test/java/com/vn/agent/extraction/ExtractionToolBridgeTest.java`
    - `ai-agent/ai-agent/src/test/java/com/vn/agent/DefaultChatServiceIntentRoutingTest.java`
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/ExtractionSourceText.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/ExtractionInput.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/RunContext.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/ExtractionToolBridge.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/ExtractionService.java,
    jmix-app/src/main/java/com/vn/jmixapp/ai/CustomerDraftIntentExtractor.java,
    jmix-app/src/test/java/com/vn/jmixapp/ai/CustomerDraftIntentExtractorTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/extraction/ExtractionToolBridgeTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/DefaultChatServiceIntentRoutingTest.java
  </files>
  <action>
    1. Add `com.vn.agent.extraction.ExtractionSourceText` as a Java record:
       `record ExtractionSourceText(String name, String text, boolean truncated)`.
    2. Extend `ExtractionInput` with a new final component:
       `List<ExtractionSourceText> sourceTexts`.
    3. Preserve backward compatibility by adding a five-argument constructor with the current signature:
       `(String intentId, UUID conversationId, String userMessage, List<UUID> taskFileIds, List<Media> taskFileMedia)`,
       delegating to the six-argument canonical constructor with `List.of()` source texts.
    4. In the canonical constructor, null-normalize `sourceTexts` to `List.of()` and copy it with `List.copyOf(...)`.
    5. Extend `RunContext.setExtractionTurn(...)` to carry `sourceTexts`, and add an overload with the old five-argument signature delegating to `List.of()` so existing tests can migrate incrementally.
    6. Add `RunContext.getSourceTexts()` and clear its ThreadLocal in `RunContext.clear()`.
    7. In `DefaultChatServiceImpl.executeBlockingTurn(...)` and the streaming path, convert `resolvedMedia.documentTexts()` into `ExtractionSourceText` values and pass them into `RunContext.setExtractionTurn(...)`.
       Use `DocumentText.filename()`, `DocumentText.text()`, and `DocumentText.truncated()`.
    8. In `ExtractionToolBridge.runScopedInput(...)`, pass `RunContext.getSourceTexts()` into the six-argument `ExtractionInput`.
    9. In `ExtractionService.prepare(String intentId, Map<String,Object> contextRefs)`, when resolving active task files directly, pass the same converted document texts into `ExtractionInput`.
    10. In `CustomerDraftIntentExtractor`, add `assertSourceFaithful(LinkedHashMap<String,Object> narrowedPayload, ExtractionInput input)` and call it after `validatePayload(...)` and before `convertToCustomer(...)`.
    11. `assertSourceFaithful` must build textual evidence from:
        - `input.userMessage()` when non-blank.
        - each `ExtractionSourceText.text()` when non-blank.
    12. If there is no textual evidence at all (for example image-only media), do not invent OCR. Preserve the existing prompt-based path and document this branch in a test name/comment. This plan closes the production gap for pasted text and Tika-extracted documents, which are the Phase 14 PDF/text source path.
    13. For every non-null, non-blank extracted String value:
        - `email`: lower-case exact containment in normalized evidence.
        - `phone`: digit-only value must be contained in digit-only evidence.
        - `name`: lower-case whitespace-normalized exact containment in normalized evidence.
        - any other String field in the reference Customer schema: lower-case whitespace-normalized exact containment.
    14. On unsupported evidence, throw `ExtractionSchemaException.validationFailure(INTENT_ID, ENTITY_NAME, narrowedPayload.size())`. Do not include the unsupported value in the exception message.
    15. Add `CustomerDraftIntentExtractorTest` cases:
        - `fabricatedEmailRejectedWhenSourceTextDoesNotContainIt`: source text contains name + phone but no email; model payload contains email; expect `ExtractionSchemaException.CODE_VALIDATION_FAILURE` and no raw email in message.
        - `documentSourceTextSupportsExtractedValues`: source text comes from `new ExtractionSourceText("customer.txt", "...", false)`; payload values appear in that text; extractor returns Customer.
        - `imageOnlyInputUsesExistingPromptPathWhenNoTextEvidenceExists`: no userMessage, no sourceTexts, one image `Media`; payload still maps as before.
    16. Update `ExtractionEvaluationContractTest` or its fixture only if needed to assert source-faithfulness is now enforced by a production test, not only represented as a fixture.
  </action>
  <verify>
    <automated>./gradlew --no-daemon :jmix-app:test --tests "*CustomerDraftIntentExtractorTest"</automated>
    <automated>./gradlew --no-daemon :ai-agent:ai-agent:test --tests "*ExtractionToolBridgeTest" --tests "*DefaultChatServiceIntentRoutingTest" --tests "*ExtractionEvaluationContractTest"</automated>
    <also>rg -n "assertSourceFaithful" jmix-app/src/main/java/com/vn/jmixapp/ai/CustomerDraftIntentExtractor.java</also>
    <also>rg -n "getSourceTexts|setExtractionTurn" ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/RunContext.java</also>
  </verify>
  <done>
    BL-05 is closed for production textual evidence: the reference Customer extractor cannot promote fabricated string values when the user message or task-file document text does not support them.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Externalize committed development credentials</name>
  <read_first>
    - `jmix-app/src/main/resources/application.properties`
    - `jmix-app/src/main/resources/application-hsqldb.properties`
    - `.gitignore`
    - root `.env` loading comments in `application.properties`
  </read_first>
  <files>
    jmix-app/src/main/resources/application.properties,
    jmix-app/.env.example,
    jmix-app/src/test/java/com/vn/jmixapp/config/ApplicationPropertiesSecretsTest.java
  </files>
  <action>
    1. Replace hardcoded datasource values in `jmix-app/src/main/resources/application.properties` with environment-backed placeholders:
       - `main.datasource.url=${MAIN_DATASOURCE_URL:jdbc:postgresql://localhost:5432/ai-agent}`
       - `main.datasource.username=${MAIN_DATASOURCE_USERNAME:postgres}`
       - `main.datasource.password=${MAIN_DATASOURCE_PASSWORD:}`
       - `agentstore.datasource.url=${AGENTSTORE_DATASOURCE_URL:jdbc:postgresql://localhost:5432/agentstore}`
       - `agentstore.datasource.username=${AGENTSTORE_DATASOURCE_USERNAME:postgres}`
       - `agentstore.datasource.password=${AGENTSTORE_DATASOURCE_PASSWORD:}`
    2. Replace UI login defaults with environment-backed placeholders:
       - `ui.login.defaultUsername=${JMIX_UI_LOGIN_DEFAULT_USERNAME:admin}`
       - `ui.login.defaultPassword=${JMIX_UI_LOGIN_DEFAULT_PASSWORD:}`
    3. Remove the host-specific `10.123.123.174`, `admin123`, and `ui.login.defaultPassword=admin` literals from committed resources.
    4. Keep `spring.config.import=optional:file:.env[.properties],optional:file:../.env[.properties]` so local ignored `.env` files remain the developer override path.
    5. Add `jmix-app/.env.example` with placeholder keys only and no real values:
       `MAIN_DATASOURCE_URL=`, `MAIN_DATASOURCE_USERNAME=`, `MAIN_DATASOURCE_PASSWORD=`, `AGENTSTORE_DATASOURCE_URL=`, `AGENTSTORE_DATASOURCE_USERNAME=`, `AGENTSTORE_DATASOURCE_PASSWORD=`, `JMIX_UI_LOGIN_DEFAULT_USERNAME=`, `JMIX_UI_LOGIN_DEFAULT_PASSWORD=`, `OPENROUTER_API_KEY=`.
    6. Do NOT commit or modify the real root `.env`.
    7. Add `ApplicationPropertiesSecretsTest` as a pure JUnit source/resource test under `jmix-app/src/test/java/com/vn/jmixapp/config/`.
    8. The test must assert:
       - `application.properties` does not contain `10.123.123.174`.
       - `application.properties` does not contain `admin123`.
       - `application.properties` does not contain `ui.login.defaultPassword=admin`.
       - `application.properties` contains all eight placeholder variable names listed above.
       - `jmix-app/.env.example` exists and contains the same keys but no non-placeholder secret values.
  </action>
  <verify>
    <automated>./gradlew --no-daemon :jmix-app:test --tests "*ApplicationPropertiesSecretsTest"</automated>
    <also>rg -n "10\\.123\\.123\\.174|admin123|ui\\.login\\.defaultPassword=admin" jmix-app/src/main/resources/application.properties must return zero matches</also>
    <also>rg -n "MAIN_DATASOURCE_URL|AGENTSTORE_DATASOURCE_URL|JMIX_UI_LOGIN_DEFAULT_PASSWORD" jmix-app/src/main/resources/application.properties jmix-app/.env.example</also>
  </verify>
  <done>
    BL-01 is closed: committed resources no longer contain the known development database/admin defaults.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Run gap-closure regression matrix and write summary</name>
  <read_first>
    - `.planning/phases/14-intent-driven-extraction-form-prefill/14-VERIFICATION.md`
    - `.planning/phases/14-intent-driven-extraction-form-prefill/14-UAT-CHECKLIST.md`
    - `.planning/phases/14-intent-driven-extraction-form-prefill/14-08-SUMMARY.md`
    - `AGENTS.md`
  </read_first>
  <files>
    .planning/phases/14-intent-driven-extraction-form-prefill/14-09-SUMMARY.md
  </files>
  <action>
    1. Run the targeted gap matrix:
       - `./gradlew --no-daemon :ai-agent:ai-agent:test --tests "*ExtractionDraftAccessTest" --tests "*DraftLoaderTest" --tests "*OpenFormWithDraftHandlerTest" --tests "*ExtractionServiceTest" --tests "*ExtractionToolBridgeTest" --tests "*ChatPanelFragmentConversationIdTest" --tests "*IntentCardRowTest" --tests "*DefaultChatServiceIntentRoutingTest" --tests "*ExtractionEvaluationContractTest"`
       - `./gradlew --no-daemon :jmix-app:test --tests "*CustomerDraftIntentExtractorTest" --tests "*CustomerDraftWorkflowTest" --tests "*ApplicationPropertiesSecretsTest"`
    2. Run broader confidence checks already known to be relevant:
       - `./gradlew --no-daemon :ai-agent:ai-agent:test`
       - `./gradlew --no-daemon :jmix-app:test --tests "*CustomerDraft*"`
    3. If the existing broad `:jmix-app:test` or `:ai-agent:ai-agent:check :jmix-app:check --continue` residual failures are re-run, record whether they still fail for the known `User` metaclass/native-memory reasons from `14-VERIFICATION.md`; do not hide them.
    4. Run JetBrains MCP `get_file_problems(..., onlyErrors=false)` on touched Java/properties/XML files if available, per AGENTS.md.
    5. Create `14-09-SUMMARY.md` with:
       - each blocker id (BL-01..BL-05) and the exact task that closed it.
       - key files changed.
       - test commands and PASS/FAIL results.
       - residual broad-module failures, if any, with owner/reason.
       - confirmation that no new AI tool, view, entity, table, audit kind, or exposure layer was added.
  </action>
  <verify>
    <automated>powershell -Command "Test-Path '.planning/phases/14-intent-driven-extraction-form-prefill/14-09-SUMMARY.md'"</automated>
    <also>rg -n "BL-02|BL-03|BL-04|BL-05|BL-01" .planning/phases/14-intent-driven-extraction-form-prefill/14-09-SUMMARY.md</also>
  </verify>
  <done>
    Gap closure is ready for phase-level re-verification through `$gsd-execute-phase 14` followed by `$gsd-verify-work 14` or the project verifier path in use.
  </done>
</task>

</tasks>

<threat_model>
| Threat ID | Source Gap | Component | Risk | Mitigation |
|-----------|------------|-----------|------|------------|
| T-14-33 | BL-02 | Draft apply/open | Expired or confirmed draft can still prefill a form before cleanup deletes it | Central `ExtractionDraftAccess.loadOpenDraft` predicate requires `expiresAt > now` and `confirmed = false` for both `DraftLoader` and `OpenFormWithDraftHandler`. |
| T-14-34 | BL-03 | Chat UI/service boundary | Rate-limited first turn persists an empty conversation before service guard denial | UI passes nullable conversation id; `DefaultChatServiceImpl.stream` owns first-turn creation after rate-limit guard. |
| T-14-35 | BL-04 | Draft payload serialization | Jackson reintroduces Jmix getter-backed fields beyond the schema into `payloadJson` | `ExtractionService` filters converted payloads through `MetaClassDtoSynthesizer.SynthesizedSchema.attributeNames()`. |
| T-14-36 | BL-05 | Reference extractor | Model fabricates syntactically valid values absent from source text | `CustomerDraftIntentExtractor` validates extracted strings against user/document textual evidence before returning a payload. |
| T-14-37 | BL-01 | Configuration | Committed host DB/admin defaults become reusable credentials | Datasource and UI default values move to env placeholders plus ignored `.env`; source test blocks regression. |
</threat_model>

<verification>
- `./gradlew --no-daemon :ai-agent:ai-agent:test --tests "*ExtractionDraftAccessTest" --tests "*DraftLoaderTest" --tests "*OpenFormWithDraftHandlerTest" --tests "*ExtractionServiceTest" --tests "*ExtractionToolBridgeTest" --tests "*ChatPanelFragmentConversationIdTest" --tests "*IntentCardRowTest" --tests "*DefaultChatServiceIntentRoutingTest" --tests "*ExtractionEvaluationContractTest"`
- `./gradlew --no-daemon :jmix-app:test --tests "*CustomerDraftIntentExtractorTest" --tests "*CustomerDraftWorkflowTest" --tests "*ApplicationPropertiesSecretsTest"`
- `./gradlew --no-daemon :ai-agent:ai-agent:test`
- `./gradlew --no-daemon :jmix-app:test --tests "*CustomerDraft*"`
- JetBrains MCP file-problem checks on touched Java/resource files.
</verification>

<success_criteria>
1. `DraftLoader` and `OpenFormWithDraftHandler` both use `ExtractionDraftAccess.loadOpenDraft(...)` for user-facing draft reads.
2. The live-draft query contains `e.id = :draftId and e.expiresAt > :now and e.confirmed = false`.
3. Expired and confirmed draft tests fail closed before payload apply or navigation.
4. `ChatPanelFragment.onSubmit(...)` no longer calls `conversationGateway.loadOrCreate(...)` or `ensureConversationIdForSubmit(...)` before `chatService.stream(...)`.
5. `DefaultChatServiceImpl.stream(..., null, ...)` emits the created conversation id in `StreamingEvent.Final` after guard checks pass.
6. A rate-limit denial with `conversationId == null` does not call `conversationGateway.loadOrCreate(...)`.
7. `ExtractionService` writes draft `payloadJson` from schema-approved keys only.
8. `ExtractionServiceTest` proves unsupported keys (`recommendedProducts`, `version`, `class`) do not appear in draft JSON or audit summaries.
9. `ExtractionInput` carries textual source evidence from user text and task-file document text.
10. `CustomerDraftIntentExtractorTest` proves fabricated email rejection through the real extractor implementation path.
11. `application.properties` contains no `10.123.123.174`, `admin123`, or `ui.login.defaultPassword=admin` literals.
12. `ApplicationPropertiesSecretsTest` blocks future reintroduction of those committed defaults.
13. `14-09-SUMMARY.md` records results for BL-01 through BL-05.
</success_criteria>

<output>
Create `.planning/phases/14-intent-driven-extraction-form-prefill/14-09-SUMMARY.md`.

Mention:
- BL-02, BL-03, BL-04, BL-05, and BL-01 are the only intended gap closures.
- No new AI tool, entity table, audit kind, Jmix view, menu item, or exposure layer was introduced.
- Any broad-module residual failures are inherited only if they match `14-VERIFICATION.md`'s known `User` metaclass or native-memory failures.
- After this plan executes, rerun phase verification and then manual UI UAT from `14-UAT-CHECKLIST.md`.
</output>
