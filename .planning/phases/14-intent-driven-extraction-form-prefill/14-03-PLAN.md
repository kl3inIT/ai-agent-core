---
phase: 14-intent-driven-extraction-form-prefill
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/ExtractionService.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/ExtractionToolBridge.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/ExtractionSchemaException.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/ExtractionDeniedException.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/StreamingEvent.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/audit/ToolCallbackAuditDecorator.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/audit/MutationToolCallbackBoundaryDecorator.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/tools/AgentToolCallbacks.java
autonomous: true
requirements:
  - EXTRACT-03
  - EXTRACT-05
  - EXTRACT-06
  - EXTRACT-10
tags:
  - extraction
  - spring-ai-tools
  - audit
  - security
must_haves:
  truths:
    - "D-01 - extractor path accepts Map<String,Object> structured output from Spring AI; no runtime DTO synthesis"
    - "D-04 - schema violations fail/audit; no provider-native strict schema flag or automatic retry in v1.1"
    - "D-12 - prepare_form_draft audit row is append-only and separate from extraction.draft_applied"
    - "D-13 - audit resultSummary contains counts and bounded attribute-name lists, not raw values"
    - "D-14 - exposure denial writes one DENIED prepare_form_draft row and persists no draft"
    - "D-15 - SPEC REQ-10 audit wording is superseded by the two-row pattern in D-12"
    - "D-18 - ExtractionService builds ExtractionInput from conversation, message, task file ids, and all active media"
    - "ExtractionToolBridge returns only the structured payload; it never imports ViewNavigators or navigates"
    - "Review HIGH - StreamingEvent.ToolResult carries structured payloadJson/toolName for prepare_form_draft so UI never parses a human-readable summary"
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/ExtractionService.java"
      provides: "Transactional orchestration for intent resolution, extraction, draft persistence, and audit"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/ExtractionToolBridge.java"
      provides: "@Tool prepare_form_draft returning open_form_with_draft payload"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/StreamingEvent.java"
      provides: "ToolResult structured payload channel for extraction confirm rendering"
---

<objective>
Build the core extraction orchestration and the single LLM-facing tool. After this plan, a named intent can be invoked programmatically through `prepare_form_draft`, producing exactly one persisted draft and a structured tool payload, with append-only audit rows for success, denial, and schema failures.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-intent-driven-extraction-form-prefill/14-SPEC.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-CONTEXT.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-AI-SPEC.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-RESEARCH.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-01-PLAN.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement extraction exceptions and result-safe JSON helpers</name>
  <read_first>
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/tools/ToolResultFormatter.java`
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/tools/mutation/MutationErrorTranslator.java`
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/audit/DiffSerializer.java`
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/ExtractionSchemaException.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/ExtractionDeniedException.java
  </files>
  <action>
    1. Add stable exception types/codes for unknown intent, exposure denial, schema parse failure, and validation failure.
    2. Keep exception messages safe: no raw payload values, raw model response, or file content.
    3. If helper serializers are needed, keep them package-private in `com.vn.agent.extraction` and deterministic with Jackson/LinkedHashMap.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:compileJava</automated>
  </verify>
  <done>Extraction failure types compile and expose stable codes for audit/user mapping.</done>
</task>

<task type="auto">
  <name>Task 2: Implement ExtractionService.prepare(...)</name>
  <read_first>
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiExtractionDraft.java`
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/IntentRegistry.java`
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/exposure/LlmExposurePolicy.java`
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditWriter.java`
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/RunContext.java`
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/ExtractionService.java
  </files>
  <action>
    1. Create `@Service ExtractionService` with constructor injection only.
    2. Do NOT annotate the whole service or `prepare(...)` with `@Transactional`; the extractor can perform an LLM call and must not hold a database transaction/connection across that call. Keep persistence/audit writes in short `DataManager` operations and rely on `AuditWriter`'s existing transaction boundary.
    3. Resolve the `IntentExtractor<?>` by `intentId`.
    4. Resolve `MetaClass` from extractor `entityName()` and check Phase 10 exposure for the current user before extraction.
    5. If denied, write a single `prepare_form_draft` audit row with `AiToolCallOutcome.DENIED`, stable denial reason, no draft, and no second audit row.
    6. Invoke `extractor.extract(input)`.
    7. Serialize the returned object to `payloadJson`; for typed host objects, convert through Jackson to `Map` first if needed for deterministic payload.
    8. Compute `instanceName` using `MetadataTools.getInstanceName(...)`; if null/blank, fall back to the entity caption plus draft id suffix in the exact format `"{caption} draft {draftId}"` and assert that format in tests.
    9. Create `AiExtractionDraft` through `DataManager.create` or `Metadata.create`, set owner username from `CurrentAuthentication`, `sourceConversationId`, optional `sourceTaskFileId`, `expiresAt = now + ttlSeconds`, `confirmed=false`.
    10. Save through secured `DataManager`.
    11. Write `prepare_form_draft` audit row with `SUCCESS`, arguments containing intent id/context refs only, and result summary containing `draftId`, `entityName`, `instanceName`, `extractedFieldCount`.
    12. On schema/validation failures, write `FAILED` audit row with failure code and counts only.
  </action>
  <verify>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/ExtractionService.java' -Pattern 'AuditWriter','prepare_form_draft','AiExtractionDraft','MetadataTools','DataManager').Count -ge 5"</automated>
    <automated>powershell -Command "if(Select-String -Path 'ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/ExtractionService.java' -Pattern '@Transactional' -Quiet){ throw 'ExtractionService must not be service-level transactional' }"</automated>
  </verify>
  <done>`ExtractionService.prepare` persists drafts and audits success/denial/failure without raw-value leakage.</done>
</task>

<task type="auto">
  <name>Task 3: Implement ExtractionToolBridge and structured ToolResult propagation</name>
  <read_first>
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/tools/mutation/BuiltInMutationTools.java` rich tool descriptions
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/tools/AgentToolCallbacks.java`
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/StreamingEvent.java`
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/audit/ToolCallbackAuditDecorator.java`
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/audit/MutationToolCallbackBoundaryDecorator.java`
    - Spring AI `@Tool` / `@ToolParam` Context7 docs summarized in `14-RESEARCH.md`
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/ExtractionToolBridge.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/StreamingEvent.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/audit/ToolCallbackAuditDecorator.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/audit/MutationToolCallbackBoundaryDecorator.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/tools/AgentToolCallbacks.java
  </files>
  <action>
    1. Create `@Component ExtractionToolBridge` with an `@Tool(name = "prepare_form_draft", description = """...""")` method.
    2. Tool parameters:
       - `String intentId`
       - `Map<String, Object> contextRefs`, optional if Spring AI version supports optional tool params in this project.
    3. Description must follow the rich-tool-description convention: MANDATORY, INPUT CONTRACT, PARAMETER FORMATS, ERROR HANDLING, STRICTNESS + EXAMPLES.
    4. The tool delegates to `ExtractionService.prepare(...)` and returns a `LinkedHashMap<String, Object>` with keys exactly:
       - `action` = `"open_form_with_draft"`
       - `draftId`
       - `entityName`
       - `instanceName`
    5. The class must not import `io.jmix.flowui.ViewNavigators`, `ViewNavigators`, or any Vaadin/Jmix UI navigation type.
    6. Extend `StreamingEvent.ToolResult` to include `toolName` and nullable `payloadJson`, while preserving a backwards-compatible constructor for existing tests/callers that still pass `(toolCallId, summary, outcome)`.
    7. Update `ToolCallbackAuditDecorator` to pass `toolName` on every `ToolResult` and set `payloadJson` to the raw delegate output only when `toolName.equals("prepare_form_draft")` and the output JSON has `action == "open_form_with_draft"`; otherwise pass `null`. Keep resultSummary capped and prompt-injection escaped as today.
    8. Update `MutationToolCallbackBoundaryDecorator` to use the backwards-compatible `ToolResult` constructor or pass `payloadJson=null`.
    9. Register the bridge with `AgentToolCallbacks` by adding the actual `ExtractionToolBridge` bean/object to the same callback assembly used for other `@Tool` beans, unless the current assembly already discovers every `@Tool` bean automatically; in that case, add a test proving `prepare_form_draft` appears in `forCurrentUser()` and document the no-code registration path in the summary.
    10. Do not enable named-intent gating yet; Plan 04 owns gating.
  </action>
  <verify>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/ExtractionToolBridge.java' -Pattern '@Tool\\(name = \"prepare_form_draft\"','open_form_with_draft','LinkedHashMap').Count -ge 3"</automated>
    <automated>powershell -Command "if(Select-String -Path 'ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/ExtractionToolBridge.java' -Pattern 'ViewNavigators|\\.navigate\\(' -Quiet){ throw 'ExtractionToolBridge must not navigate' }"</automated>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/StreamingEvent.java' -Pattern 'payloadJson','toolName').Count -ge 2"</automated>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/java/com/vn/agent/audit/ToolCallbackAuditDecorator.java' -Pattern 'prepare_form_draft','payloadJson|structured').Count -ge 2"</automated>
  </verify>
  <done>Tool bridge returns the locked structured payload, streaming events carry that raw payload for the renderer, and no navigation dependency exists in the tool layer.</done>
</task>

<task type="auto">
  <name>Task 4: Add extraction service/tool tests</name>
  <read_first>
    - `ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/BuiltInMutationToolsAccessGatingTest.java`
    - `ai-agent/ai-agent/src/test/java/com/vn/agent/audit/AuditWriterFieldMappingTest.java`
    - `ai-agent/ai-agent/src/test/java/com/vn/agent/tools/ToolDescriptionInvariantsTest.java`
  </read_first>
  <files>
    ai-agent/ai-agent/src/test/java/com/vn/agent/extraction/ExtractionServiceTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/extraction/ExtractionToolBridgeTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/extraction/ExtractionAuditTest.java
  </files>
  <action>
    1. Add a test `IntentExtractor` fixture that returns a simple map/object without calling an LLM.
    2. Assert `ExtractionService.prepare` creates exactly one `AiExtractionDraft` for success.
    3. Assert the draft owner is current username and `expiresAt` is set from properties.
    4. Assert `prepare_form_draft` output keys equal the locked set and action literal.
    5. Assert exposure denial writes a DENIED audit row and no draft row.
    6. Assert schema failure writes FAILED audit with failure code and does not log raw values.
    7. Assert rich tool description contains required sections.
    8. Assert streaming `ToolResult` emitted for `prepare_form_draft` carries `toolName=prepare_form_draft` and non-null `payloadJson`, while non-extraction tools keep `payloadJson=null`.
    9. Assert `AgentToolCallbacks.forCurrentUser()` discovers or explicitly includes `prepare_form_draft`.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "*ExtractionServiceTest" --tests "*ExtractionToolBridgeTest" --tests "*ExtractionAuditTest"</automated>
  </verify>
  <done>Core extraction tool behavior is covered without UI or host Customer dependency.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Component | Risk | Mitigation |
|-----------|-----------|------|------------|
| T-14-09 | Extraction tool | Tool returns navigation primitive | Payload-only contract and source grep |
| T-14-10 | Audit | Raw PII leaks into resultSummary/error | Counts, ids, paths, and failure codes only |
| T-14-11 | Exposure | Denied entity still gets draft row | Denial check before extractor invocation and persistence |
| T-14-12 | Schema failure | Malformed model output silently opens form | FAILED audit and no draft on parse/validation failure |
</threat_model>

<verification>
- `./gradlew :ai-agent:ai-agent:compileJava`
- `./gradlew :ai-agent:ai-agent:test --tests "*Extraction*"`
- Source grep: no `ViewNavigators` in `ExtractionToolBridge`.
- JetBrains file-problem checks on touched Java files.
</verification>

<success_criteria>
1. `prepare_form_draft` is registered as a Spring AI tool.
2. Successful tool call persists one draft and returns locked payload.
3. Denied tool call persists no draft and writes one DENIED audit row.
4. Schema failure persists no draft and writes one FAILED audit row.
5. Tool bridge has no navigation imports or calls.
</success_criteria>

<output>
Create `.planning/phases/14-intent-driven-extraction-form-prefill/14-03-SUMMARY.md` with service flow, audit shapes, tool output sample, denial behavior, and test results.
</output>
