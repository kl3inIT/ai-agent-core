---
phase: 13.1-chat-attachments-rightpane-and-persistent-context
plan: 04
type: execute
wave: 3
depends_on: [01]
files_modified:
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/fragment/chat-panel-fragment.xml
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/fragment/ai-task-file-card-fragment.xml
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/AiTaskFileCardFragmentRenderer.java
  - ai-agent/ai-agent/src/main/resources/META-INF/resources/frontend/styles/ai-agent-chat.css
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties
autonomous: true
requirements:
  - UI-01
  - I18N-01
  - CONTRACT-01-xml
tags:
  - jmix-views
  - jmix-fragments
  - i18n
  - css
  - crm-port
must_haves:
  truths:
    - "chat-panel-fragment.xml root contains a <split orientation=HORIZONTAL splitterPosition=68> with chatPanel left and attachmentsPanel right"
    - "Right-pane vbox retains id=attachmentsPanel (Phase 12 fragment-contract slot id) and contains attachmentsTitle h3 + attachmentsEmptyState vbox + attachmentsGridLayout + taskFileUpload"
    - "Phase 13 chip-strip vbox content + <hbox attachRow> are removed"
    - "ai-task-file-card-fragment.xml ships verbatim from CRM with the 5 D-B1 adaptations (entity name, h5 property, 2nd button, DOWNLOAD icon, sizeValue replacing source)"
    - "AiTaskFileCardFragmentRenderer extends FragmentRenderer<JmixCard, AiTaskFile> with @RendererItemContainer(\"taskFileDc\") and wires open + delete actions plus per-extension icon resolver"
    - "ai-agent-chat.css has the verbatim-CRM appendix with .ai-conversation-message-list renamed to .ai-agent-chat-panel__messages"
    - "All 12 new chatView.attachments.* keys present in BOTH messages_en.properties and messages_vi.properties"
  artifacts:
    - path: "ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/fragment/chat-panel-fragment.xml"
      provides: "Reshaped fragment with <split> + right-pane attachmentsPanel + taskFilesDc data-section"
      contains: "splitterPosition=\"68\""
    - path: "ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/fragment/ai-task-file-card-fragment.xml"
      provides: "Card fragment with headerPrefix icon + h5 filename + open/delete buttons + meta hbox"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/AiTaskFileCardFragmentRenderer.java"
      provides: "Per-row card renderer with download + delete + per-extension icon"
    - path: "ai-agent/ai-agent/src/main/resources/META-INF/resources/frontend/styles/ai-agent-chat.css"
      provides: "CRM-ported attachments-dropzone + attachment-event + card meta CSS rules"
  key_links:
    - from: "chat-panel-fragment.xml taskFilesDc"
      to: "AiTaskFileCardFragmentRenderer @RendererItemContainer(taskFileDc)"
      via: "<gridLayout itemsContainer=taskFilesDc> + fragmentRenderer class binding"
      pattern: "itemsContainer=\"taskFilesDc\""
    - from: "AiTaskFileCardFragmentRenderer.openButton click"
      to: "Downloader.download(FileRef)"
      via: "Jmix Downloader bean"
      pattern: "downloader\\.download\\("
---

<objective>
Land the verbatim CRM-style right-pane Attachments UI: reshape `chat-panel-fragment.xml` into a horizontal split layout with chat on the left and an Attachments panel on the right; create the new `ai-task-file-card-fragment.xml` + `AiTaskFileCardFragmentRenderer.java` (per-row card with download + delete actions); append the CRM-ported CSS rules to `ai-agent-chat.css`; and add all 12 new `chatView.attachments.*` keys to both locale bundles. Wave-3 prerequisite for the Phase 13.1 UX. Java fragment controller (`ChatPanelFragment.java`) wiring lives in Plan 13.1-05; this plan delivers everything XML/CSS/i18n + the standalone renderer class so the next plan can wire data-loaders + upload-success + NOTICE rendering against a stable surface.

Purpose: separate the verbatim-CRM port (high-volume, low-risk, mechanical) from the Java controller rewiring (lower volume, higher integration risk). Concentrating XML + new renderer + CSS + i18n in one plan keeps the diff coherent and lets the checker verify CRM parity in a single pass.

Output: reshaped fragment XML, new card fragment XML + Java renderer, CSS appendix, full bilingual key set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-SPEC.md
@.planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-CONTEXT.md
@.planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-UI-SPEC.md
@.planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-RESEARCH.md
@.planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-PATTERNS.md

<interfaces>
<!-- Renderer contract — depends on data container id "taskFileDc" inside the card fragment XML. -->

From RESEARCH.md Pattern 1 (target chat-panel-fragment.xml structure):
- Root `<vbox rootLayout>` wraps a `<split id="conversationSplit" orientation="HORIZONTAL" splitterPosition="68">`
- Left `<vbox id="chatPanel">`: titleBar, messageListSlot, messageInputSlot (NO chip strip, NO attachRow)
- Right `<vbox id="attachmentsPanel">`: attachmentsTitle h3, attachmentsEmptyState vbox, attachmentsGridLayout (gridLayout w/ fragmentRenderer), taskFileUpload (with all the existing MIME/maxFiles/maxFileSize attributes)
- New `<data>` block above `<content>` declares `taskFilesDc` collection container + `taskFilesDl` loader with the JPQL `select e from ai_AiTaskFile e where e.conversation.id = :conversationId order by e.createdDate desc`

From RESEARCH.md Pattern 2 / PATTERNS.md `ai-task-file-card-fragment.xml` section:
- `<data readOnly="true"><instance id="taskFileDc" class="com.vn.agent.entity.AiTaskFile"/></data>`
- `<card id="taskFileCard" width="100%" themeNames="outlined">` with headerPrefix/header/headerSuffix/content slots

From RESEARCH.md Pattern 6 (CSS appendix to ai-agent-chat.css):
- 110 lines verbatim from `D:/DTH/jmix-crm/.../ai-conversation-detail-view.css`
- Single rename: `.ai-conversation-message-list` → `.ai-agent-chat-panel__messages`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reshape chat-panel-fragment.xml + new ai-task-file-card-fragment.xml + ai-agent-chat.css appendix</name>
  <read_first>
    - .planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-SPEC.md (REQ-1 UI-01 — DOM acceptance criteria; REQ-7 CONTRACT-01 — `attachmentsPanel` id retained)
    - .planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-UI-SPEC.md (Layout Contract section — verbatim splitterPosition=68; Card slot table; Empty-state spec; Drop-zone props)
    - .planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-RESEARCH.md (Pattern 1 — full target XML; Pattern 6 — full CSS appendix; Pitfall 4 — `<gridLayout gap=>` may need CSS fallback; Pitfall 9 — application.properties path; "Common Pitfalls" Pattern 5 NOTICE rendering substrate)
    - .planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-PATTERNS.md (chat-panel-fragment.xml + ai-task-file-card-fragment.xml + ai-agent-chat.css sections)
    - D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/fragment/chat-panel-fragment.xml (full file, 87 lines — current shape)
    - D:/DTH/jmix-crm/src/main/resources/com/company/crm/ai/view/aiconversation/ai-conversation-detail-view.xml (CRM verbatim source — copy split structure verbatim)
    - D:/DTH/jmix-crm/src/main/resources/com/company/crm/ai/view/aiconversation/ai-attachment-card-fragment.xml (CRM verbatim source for the card)
    - D:/DTH/jmix-crm/src/main/frontend/themes/crm/view/ai-conversation-detail-view.css (CRM verbatim CSS source — port lines 1-110)
    - D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/resources/META-INF/resources/frontend/styles/ai-agent-chat.css (current 34 lines — append-only, do not modify existing)
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/fragment/chat-panel-fragment.xml,
    ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/fragment/ai-task-file-card-fragment.xml,
    ai-agent/ai-agent/src/main/resources/META-INF/resources/frontend/styles/ai-agent-chat.css
  </files>
  <action>
    1. REPLACE the entire content of `chat-panel-fragment.xml` with the structure documented in RESEARCH.md Pattern 1 + PATTERNS.md. Concrete shape (with the existing titleBar buttons preserved verbatim from the current file):
       ```xml
       <?xml version="1.0" encoding="UTF-8" standalone="no"?>
       <fragment xmlns="http://jmix.io/schema/flowui/fragment">
           <data>
               <collection id="taskFilesDc" class="com.vn.agent.entity.AiTaskFile">
                   <fetchPlan extends="_base"/>
                   <loader id="taskFilesDl">
                       <query>
                           <![CDATA[select e from ai_AiTaskFile e where e.conversation.id = :conversationId order by e.createdDate desc]]>
                       </query>
                   </loader>
               </collection>
           </data>
           <content>
               <vbox id="rootLayout" classNames="ai-agent-chat-panel"
                     width="100%" height="100%" padding="false" spacing="false">
                   <split id="conversationSplit" orientation="HORIZONTAL"
                          width="100%" height="100%" splitterPosition="68">
                       <vbox id="chatPanel" width="100%" height="100%"
                             padding="false" spacing="false">
                           <hbox id="titleBar" width="100%" alignItems="CENTER"
                                 classNames="ai-agent-chat-panel__toolbar">
                               <h3 id="conversationTitle"
                                   text="msg:///chatView.action.newChat"
                                   classNames="m-0 ai-agent-chat-panel__title"/>
                               <button id="editConversationTitleButton"
                                       icon="PENCIL" title="msg:///chatView.action.editTitle"
                                       ariaLabel="msg:///chatView.action.editTitle"
                                       themeNames="tertiary-inline small icon"
                                       visible="false"/>
                               <button id="newChatButton"
                                       icon="PLUS" title="msg:///chatView.action.newChat"
                                       ariaLabel="msg:///chatView.action.newChat"
                                       themeNames="tertiary-inline small icon"/>
                               <button id="stopButton"
                                       text="msg:///chatView.action.stop"
                                       themeNames="tertiary"
                                       visible="false"/>
                           </hbox>
                           <vbox id="messageListSlot" width="100%" height="100%"
                                 padding="false" spacing="false"
                                 classNames="ai-agent-chat-panel__messages"/>
                           <vbox id="messageInputSlot" width="100%"
                                 padding="false" spacing="false"
                                 classNames="ai-agent-chat-panel__input"/>
                       </vbox>
                       <vbox id="attachmentsPanel" width="100%" height="100%"
                             spacing="false" padding="true">
                           <h3 id="attachmentsTitle"
                               text="msg:///chatView.attachments.title"
                               classNames="m-0"/>
                           <vbox id="attachmentsEmptyState"
                                 alignItems="CENTER" justifyContent="CENTER"
                                 spacing="true" padding="true"
                                 width="100%" height="100%">
                               <icon icon="vaadin:cloud-upload" size="XL"
                                     classNames="text-tertiary"/>
                               <span text="msg:///chatView.attachments.emptyState"
                                     classNames="text-tertiary text-s"/>
                           </vbox>
                           <gridLayout id="attachmentsGridLayout"
                                       itemsContainer="taskFilesDc"
                                       width="100%">
                               <fragmentRenderer
                                   class="com.vn.agent.view.chat.fragment.AiTaskFileCardFragmentRenderer"/>
                           </gridLayout>
                           <upload id="taskFileUpload"
                                   width="100%"
                                   classNames="attachments-dropzone"
                                   acceptedFileTypes="application/pdf,text/csv,application/msword,application/vnd.openxmlformats-officedocument.wordprocessingml.document,application/vnd.ms-excel,application/vnd.openxmlformats-officedocument.spreadsheetml.sheet,text/html,text/plain,text/markdown,image/png,image/jpeg,image/gif,image/webp,.pdf,.csv,.doc,.docx,.xls,.xlsx,.html,.htm,.txt,.md,.png,.jpg,.jpeg,.gif,.webp"
                                   maxFiles="10"
                                   maxFileSize="104857600"
                                   uploadText="msg:///chatView.attachments.uploadText"
                                   dropLabel="msg:///chatView.attachments.dropLabel"
                                   dropAllowed="true"/>
                       </vbox>
                   </split>
               </vbox>
           </content>
       </fragment>
       ```
       Key invariants:
       - `<split splitterPosition="68">` verbatim (UI-SPEC line 60).
       - `id="attachmentsPanel"` on the right-pane vbox (REQ-7 / Pitfall 6).
       - NO `receiverType=` on `<upload>` (memory `feedback_jmix_upload_receiver_deprecated`).
       - `acceptedFileTypes`, `maxFiles=10`, `maxFileSize=104857600` carried verbatim from the Phase 13 file (preserve the MIME allowlist exactly).
       - Phase 13 chip-strip vbox at lines 52-57 + `<hbox attachRow>` at lines 62-81 are GONE.

    2. CREATE `ai-task-file-card-fragment.xml` verbatim per RESEARCH.md Pattern 2 / PATTERNS.md `ai-task-file-card-fragment.xml`:
       ```xml
       <?xml version="1.0" encoding="UTF-8" standalone="no"?>
       <fragment xmlns="http://jmix.io/schema/flowui/fragment">
           <data readOnly="true">
               <instance id="taskFileDc" class="com.vn.agent.entity.AiTaskFile"/>
           </data>
           <content>
               <card id="taskFileCard" width="100%" themeNames="outlined">
                   <headerPrefix>
                       <icon id="attachmentIcon" icon="FILE_O" size="S"/>
                   </headerPrefix>
                   <header>
                       <h5 id="titleValue" dataContainer="taskFileDc" property="filename"/>
                   </header>
                   <headerSuffix>
                       <hbox spacing="false" alignItems="CENTER">
                           <button id="openButton" icon="DOWNLOAD"
                                   themeNames="tertiary-inline small icon"/>
                           <button id="deleteButton" icon="TRASH"
                                   themeNames="tertiary-inline small icon"/>
                       </hbox>
                   </headerSuffix>
                   <content>
                       <hbox id="metaLayout" spacing="true" alignItems="CENTER"
                             classNames="attachment-card-meta">
                           <span id="timestampValue" dataContainer="taskFileDc"
                                 property="createdDate"
                                 classNames="text-secondary text-s"/>
                           <span id="sizeSeparator" text="|"
                                 classNames="text-secondary text-s attachment-card-meta-separator"/>
                           <span id="sizeValue" classNames="text-secondary text-s"/>
                       </hbox>
                   </content>
               </card>
           </content>
       </fragment>
       ```

    3. APPEND the CSS port to `ai-agent-chat.css`. Source: `D:/DTH/jmix-crm/src/main/frontend/themes/crm/view/ai-conversation-detail-view.css` lines 1-110. Single mechanical rename: every selector `.ai-conversation-message-list` becomes `.ai-agent-chat-panel__messages`. Append the rules verbatim — they are reproduced in RESEARCH.md Pattern 6, full block. Do NOT modify the existing 34 lines of `ai-agent-chat.css`; append only. Add a single header comment delimiter:
       ```
       /* --- Phase 13.1: CRM-ported attachments + NOTICE row styles ----------------- */
       ```

    Implements REQ-1 UI-01 (XML side) + the CSS half of UI-SPEC.
  </action>
  <verify>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/fragment/chat-panel-fragment.xml' -Pattern 'splitterPosition=\"68\"|id=\"attachmentsPanel\"|id=\"attachmentsGridLayout\"|id=\"taskFileUpload\"|attachments-dropzone|fragmentRenderer.*AiTaskFileCardFragmentRenderer').Count -ge 6"</automated>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/fragment/chat-panel-fragment.xml' -Pattern 'attachRow|attachButton|chip-strip|attachmentsPanel.*visible=\"false\"').Count -eq 0"</automated>
    <automated>powershell -Command "Test-Path 'ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/fragment/ai-task-file-card-fragment.xml'"</automated>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/fragment/ai-task-file-card-fragment.xml' -Pattern 'taskFileDc|openButton|deleteButton|attachmentIcon|sizeValue|attachment-card-meta').Count -ge 6"</automated>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/resources/META-INF/resources/frontend/styles/ai-agent-chat.css' -Pattern 'attachments-dropzone|attachment-event|attachment-card-meta|ai-agent-chat-panel__messages.*vaadin-message').Count -ge 4"</automated>
  </verify>
  <done>
    Three files exist with the documented structure. The reshaped fragment uses `splitterPosition=68`, retains the `attachmentsPanel` slot id, and contains zero references to the removed chip-strip elements. The card fragment has the data container `taskFileDc` and the open + delete buttons. CSS appendix selectors all reference the renamed `.ai-agent-chat-panel__messages` class.
  </done>
</task>

<task type="auto">
  <name>Task 2: AiTaskFileCardFragmentRenderer.java + bilingual chatView.attachments.* keys</name>
  <read_first>
    - .planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-CONTEXT.md (D-B1 — card content + actions; D-B2 — delete confirm dialog; D-C1 — empty-state asset)
    - .planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-UI-SPEC.md (Card slot table; Copywriting Contract — exact en/vi strings; Spacing/Typography sections for the renderer styling decisions)
    - .planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-RESEARCH.md (Pattern 2 — full target Java; per-extension icon switch lines 417-425; humanizeSize lines 438-445; Common Operation 4 — Notifications usage)
    - .planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-PATTERNS.md (`AiTaskFileCardFragmentRenderer.java` + messages_en.properties + messages_vi.properties sections)
    - D:/DTH/jmix-crm/src/main/java/com/company/crm/ai/view/aiconversation/AiAttachmentCardFragmentRenderer.java (CRM verbatim source for the renderer)
    - D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties (find existing `chatView.attachments.*` block to place new keys nearby)
    - D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties (same — both bundles must mirror exactly)
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/AiTaskFileCardFragmentRenderer.java,
    ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties,
    ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties
  </files>
  <action>
    1. CREATE `AiTaskFileCardFragmentRenderer.java` per RESEARCH.md Pattern 2 (full target Java listing) — the executor copies the listing verbatim and adapts the package + entity names which are already correct in the listing. Concrete invariants:
       - Class header: `@FragmentDescriptor("ai-task-file-card-fragment.xml")` + `@RendererItemContainer("taskFileDc")` + `extends FragmentRenderer<JmixCard, AiTaskFile>`.
       - Field declarations via `@ViewComponent`: `H5 titleValue`, `Span sizeValue`, `Icon attachmentIcon`, `JmixButton openButton`, `JmixButton deleteButton`, `MessageBundle messageBundle`, `InstanceContainer<AiTaskFile> taskFileDc`.
       - Spring beans via `@Autowired`: `Downloader downloader`, `DataManager dataManager`, `FileStorageLocator fileStorageLocator`, `Dialogs dialogs`.
       - Subscribe handlers: `onReady(ReadyEvent)` (sets aria labels + applies state), `onItemChange(InstanceContainer.ItemChangeEvent<AiTaskFile>)` (re-applies state), `onOpenButtonClick(ClickEvent<JmixButton>)` (calls `downloader.setShowNewWindow(true); downloader.download(row.getStorageRef());`), `onDeleteButtonClick(ClickEvent<JmixButton>)` (opens `dialogs.createOptionDialog().withType(WARNING)` per CONTEXT D-B2).
       - `performDelete(AiTaskFile row)`: capture `FileRef ref` BEFORE calling `dataManager.remove(row)`; on row-remove failure log warn + return; on row-remove success try `fileStorageLocator.getByName(ref.getStorageName()).removeFile(ref)` inside its own try/catch — log warn on blob failure (CONTEXT D-B1 — log-and-continue; orphan blob swept by cleanup job).
       - `applyState(AiTaskFile row)`: titleValue text, attachmentIcon set via `resolveIcon(filename)`, sizeValue text via `humanizeSize(sizeBytes)`, openButton enabled iff `getStorageRef() != null`, deleteButton enabled iff row not null.
       - `resolveIcon(String fileName)` static-style switch returning `VaadinIcon` per RESEARCH.md lines 417-425: csv/xlsx/xls → TABLE; pdf/html/htm/md/txt → FILE_TEXT_O; png/jpg/jpeg/gif/webp → FILE_PICTURE; default → FILE_O.
       - `humanizeSize(Long bytes)` per RESEARCH.md lines 438-445 — `DecimalFormat("#,##0.#")`, units array `{"B","KB","MB","GB","TB"}`, binary 1024 base.
       - Message keys consumed: `chatView.attachments.action.download`, `chatView.attachments.action.delete`, `chatView.attachments.deleteConfirm.title`, `chatView.attachments.deleteConfirm.message`, `chatView.attachments.deleteConfirm.confirm`, `chatView.attachments.deleteConfirm.cancel`, `chatView.attachments.missingFileName`.

    2. APPEND to `messages_en.properties` (English) — place near the existing `chatView.attachments.*` cluster:
       ```
       chatView.attachments.title=Attachments
       chatView.attachments.uploadText=Upload
       chatView.attachments.dropLabel=Drop attachment here
       chatView.attachments.emptyState=There's nothing here yet...
       chatView.attachments.notice=[%s] added attachment "%s"
       chatView.attachments.budgetExceeded=Some attachments were not sent to the model (budget exceeded).
       chatView.attachments.action.download=Download attachment
       chatView.attachments.action.delete=Remove attachment
       chatView.attachments.deleteConfirm.title=Remove attachment
       chatView.attachments.deleteConfirm.message=Remove attachment "%s"? This will also remove it from the agent's context for future turns.
       chatView.attachments.deleteConfirm.confirm=Remove
       chatView.attachments.deleteConfirm.cancel=Cancel
       chatView.attachments.missingFileName=(unnamed file)
       ```

    3. APPEND to `messages_vi.properties` (Vietnamese) — same cluster placement:
       ```
       chatView.attachments.title=Tệp đính kèm
       chatView.attachments.uploadText=Tải lên
       chatView.attachments.dropLabel=Thả tệp vào đây
       chatView.attachments.emptyState=Chưa có tệp nào...
       chatView.attachments.notice=[%s] đã đính kèm "%s"
       chatView.attachments.budgetExceeded=Một số tệp không được gửi cho mô hình (vượt giới hạn).
       chatView.attachments.action.download=Tải tệp về
       chatView.attachments.action.delete=Xoá tệp đính kèm
       chatView.attachments.deleteConfirm.title=Xoá tệp đính kèm
       chatView.attachments.deleteConfirm.message=Xoá tệp "%s"? Tệp sẽ không còn trong ngữ cảnh của trợ lý cho các lượt tiếp theo.
       chatView.attachments.deleteConfirm.confirm=Xoá
       chatView.attachments.deleteConfirm.cancel=Huỷ
       chatView.attachments.missingFileName=(tệp không tên)
       ```

       The two bundles MUST share the exact same set of keys (LocaleParityTest enforces in Plan 13.1-06). Phase 13 keys (`chatView.attachments.button` etc.) are RETAINED — no key deletion.

    Implements REQ-1 UI-01 (renderer side) + REQ-9 I18N-01 (12 new keys × 2 bundles + 1 missing-filename helper).
  </action>
  <verify>
    <automated>powershell -Command "Test-Path 'ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/AiTaskFileCardFragmentRenderer.java'"</automated>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/AiTaskFileCardFragmentRenderer.java' -Pattern 'extends FragmentRenderer<JmixCard, AiTaskFile>|@RendererItemContainer\(\"taskFileDc\"\)|resolveIcon|humanizeSize|downloader\.download').Count -ge 5"</automated>
    <automated>powershell -Command "$keys = @('chatView.attachments.title','chatView.attachments.uploadText','chatView.attachments.dropLabel','chatView.attachments.emptyState','chatView.attachments.notice','chatView.attachments.budgetExceeded','chatView.attachments.action.download','chatView.attachments.action.delete','chatView.attachments.deleteConfirm.title','chatView.attachments.deleteConfirm.message','chatView.attachments.deleteConfirm.confirm','chatView.attachments.deleteConfirm.cancel','chatView.attachments.missingFileName'); $missing = @(); foreach ($k in $keys) { foreach ($f in @('ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties','ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties')) { $kEsc = [regex]::Escape($k); if (-not (Select-String -Path $f -Pattern (\"^\" + $kEsc + \"=\") -Quiet)) { $missing += \"$f :: $k\" } } }; if ($missing.Count -gt 0) { Write-Host 'Missing keys:'; $missing | ForEach-Object { Write-Host (\"  - \" + $_) }; throw ('Locale parity broken: ' + $missing.Count + ' key(s) missing') } else { Write-Host 'All 13 keys present in both bundles.' }"</automated>
    <automated>./gradlew :ai-agent:ai-agent:compileJava</automated>
  </verify>
  <done>
    Renderer compiles with the documented contract. Both locale bundles carry exactly 13 new `chatView.attachments.*` keys (12 from SPEC + 1 missingFileName helper). Library compiles.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File download via Jmix `Downloader` | Authenticated user request to FileStorage; `Downloader` resolves MIME + Content-Disposition from FileRef metadata only — no path traversal vector because `FileRef` is opaque. |
| Card delete | DataManager.remove runs under user's own row-level policy (`AiAgentUserRowLevelRole.taskFile()`) — cross-user delete blocked at the policy layer. |
| Filename rendering in card / NOTICE / dialogs | Filename comes from upload metadata; rendered via Vaadin Span/H5 components which escape HTML by default — no stored XSS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.1-13 | Tampering | Cross-user attachment download via stolen FileRef | mitigate | `Downloader.download(FileRef)` does not skip authorization; `AiAgentUserRowLevelRole` filters the `taskFilesDc` loader so users only see their own rows in the right pane → cannot acquire another user's `FileRef`. |
| T-13.1-14 | Information Disclosure | Filename injection in delete-confirm dialog → stored XSS | mitigate | `messageBundle.formatMessage("chatView.attachments.deleteConfirm.message", row.getFilename())` renders inside Vaadin `<vaadin-confirm-dialog>` Text component; Vaadin Flow's default text-mode escapes HTML — confirmed via Vaadin 24.8 docs. NEVER use `Html` component to render filename. |
| T-13.1-15 | Denial of Service | Per-extension icon `resolveIcon` failing on adversarial filename (e.g. trailing dot) | mitigate | `fileExtension(...)` defensively returns empty string for null/blank or trailing-dot inputs; switch falls through to `FILE_O` default. No exception path. |
| T-13.1-16 | Information Disclosure | Rendering raw `sizeBytes` instead of humanized form leaks an obscure but real fingerprint | accept | Phase 13 already exposed `sizeBytes` via the entity; humanizing reduces fingerprinting surface incidentally. Not a treated risk for v1.1. |
</threat_model>

<verification>
- `./gradlew :ai-agent:ai-agent:compileJava` → BUILD SUCCESSFUL.
- `LocaleParityTest` (Plan 13.1-06) is the regression gate for the 13 new keys.
- `CrmStyleLayoutTest` (Plan 13.1-06) is the regression gate for the XML reshape.
- Manual visual smoke (UAT) verifies CSS appendix renders correctly.
</verification>

<success_criteria>
1. `chat-panel-fragment.xml` carries the `<split>` shape with `splitterPosition="68"` and ZERO chip-strip residue.
2. New `ai-task-file-card-fragment.xml` exists with the documented slot structure.
3. `AiTaskFileCardFragmentRenderer` compiles, extends `FragmentRenderer<JmixCard, AiTaskFile>`, wires `Downloader` for open + `Dialogs` for delete-confirm.
4. `ai-agent-chat.css` appendix shipped verbatim from CRM with the single selector rename — existing 34 lines untouched.
5. 13 new keys land in BOTH locale bundles (12 SPEC keys + missingFileName helper).
6. `id="attachmentsPanel"` retained on the right-pane vbox (REQ-7 zero-diff for ChatSurfaceMounter).
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-04-SUMMARY.md` documenting:
- Final chat-panel-fragment.xml structure (line counts: data section + content section)
- New card fragment XML element list
- Renderer class line count + public method names
- Number of CSS rules appended (count of `}` in the appendix block)
- 13 keys × 2 bundles = 26 new locale entries
- Confirmation that `id="attachmentsPanel"` survives unchanged
</output>
