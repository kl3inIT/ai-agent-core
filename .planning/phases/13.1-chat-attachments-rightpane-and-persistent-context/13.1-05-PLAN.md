---
phase: 13.1-chat-attachments-rightpane-and-persistent-context
plan: 05
type: execute
wave: 4
depends_on: [03, 04]
files_modified:
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java
autonomous: true
requirements:
  - UI-01-controller
  - UX-01-notice-insert
  - UX-01-notice-render
  - AUDIT-01-toast
  - CONTRACT-01-java
tags:
  - jmix-views
  - jmix-data-loader-events
  - notice-rendering
  - upload-handler
must_haves:
  truths:
    - "ChatPanelFragment.attachmentsPanel field type remains VerticalLayout (Phase 12 contract preserved)"
    - "taskFilesDc + taskFilesDl loader bound programmatically with parameter conversationId; reloaded on conversation change and after upload-success / card-delete"
    - "On upload success, ChatPanelFragment inserts an AiMessage with role=NOTICE, formatted via chatView.attachments.notice key; failure logs warn and continues"
    - "Empty-state vbox toggles via @Subscribe(id=\"taskFilesDl\", target=Target.DATA_LOADER) on PostLoadEvent — visible only when getItems().isEmpty()"
    - "messageListSlot now renders USER/ASSISTANT bubbles via MessageBubbleComponent and NOTICE rows via vaadin-message.attachment-event sibling elements (Pitfall 7 Option A)"
    - "On chat send, when ChatResponseDto.budgetExceeded is true, a Notifications WARNING toast fires via ui.access using chatView.attachments.budgetExceeded key"
    - "ChatSurfaceMounter.java, AiUiSettingsService.java, AiUiSettings.java, ChatView.java, ChatDialogView.java have ZERO modifications in this plan"
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java"
      provides: "Reshaped controller wiring: taskFilesDl loader, NOTICE insert + render, budget-exceeded toast"
  key_links:
    - from: "ChatPanelFragment.handleUploadedFile"
      to: "AiMessage role=NOTICE persisted to AI_AGENT_MESSAGE"
      via: "metadataApi.create(AiMessage.class) + dataManager.save"
      pattern: "AiMessageRole\\.NOTICE"
    - from: "ChatPanelFragment ChatResponseDto.budgetExceeded handler"
      to: "Notifications WARNING toast"
      via: "ui.access — fragment is on the UI thread for .ask, scheduler for .stream"
      pattern: "chatView\\.attachments\\.budgetExceeded"
---

<objective>
Wire the Java controller side of the Phase 13.1 reshape: bind `taskFilesDc` / `taskFilesDl` programmatically, replace the chip-strip rendering scaffolding with empty-state visibility toggling, insert the inline NOTICE `AiMessage` row on every successful upload, render mixed USER/ASSISTANT bubbles + NOTICE dividers in the message-list (Pitfall 7 Option A — migrate `messageListSlot` to a `VerticalLayout` of `MessageBubbleComponent` + `vaadin-message.attachment-event` sibling elements), surface the budget-exceeded toast from `ChatResponseDto.budgetExceeded`, and verify `ChatPanelFragment.attachmentsPanel` field type remains `VerticalLayout` so the Phase 12 fragment contract holds. This is the largest single-file rewrite of the phase; expect ~30-50% of context budget in one task.

Purpose: deliver the user-visible behavior — drop-zone live, cards rendered, NOTICE rows inline between bubbles, toast on overflow. After this plan the phase is functionally complete pending tests.

Output: a reshaped `ChatPanelFragment.java` with the chip-strip removed, the NOTICE-insert + render path live, the budget-exceeded toast wired, and the data-loader bound to the conversation lifecycle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-SPEC.md
@.planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-CONTEXT.md
@.planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-RESEARCH.md
@.planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-PATTERNS.md
@.planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-04-SUMMARY.md
@.planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-03-SUMMARY.md

<interfaces>
<!-- Existing controller surface — preserve message-input + send + history-replay logic; only the chip-strip + attachRow + Phase 13 markInjected paths change. -->

From ChatPanelFragment.java (current — load-bearing line numbers):
- Line 13-14: `import com.vaadin.flow.component.messages.MessageList; import com.vaadin.flow.component.messages.MessageListItem;`
- Line 93: `@ViewComponent private VerticalLayout messageListSlot;`
- Line 97: `@ViewComponent private VerticalLayout attachmentsPanel;` ← TYPE STAYS `VerticalLayout`
- Line 105: `Messages messages` is already injected
- Line 111: `Metadata metadataApi` is already injected (used for `metadataApi.create(...)`)
- Line 116: `private MessageList messageList;` ← REMOVED (Pitfall 7 Option A)
- Line 187: `messageListSlot.add(messageList);` ← REMOVED
- Line 211-233: chip-strip wiring `chipStrip` + `attachmentsPanel.add(chipStrip)` ← REMOVED
- Line 245-270: `UploadHandler.toFile(...)` registration on `upload` ← REWIRE on `taskFileUpload` (new id)
- Line 347-410: `setConversationIdInternal(...)` history replay loop ← REWIRE for NOTICE arm + new bubble rendering
- Line 731-790: `handleUploadedFile(...)` upload-success path ← ADD NOTICE-row insert
- Line 939: `attachmentsPanel.setVisible(!chipById.isEmpty());` ← REMOVED (right-pane is always visible; emptyState toggles instead)

From AiTaskFileMediaResolver.Resolved (post Plan 13.1-02): record(media, budgetExceeded).
From ChatResponseDto (post Plan 13.1-03): now carries `boolean budgetExceeded()`.
From AiMessageRole (post Plan 13.1-01): contains `NOTICE("NOTICE")`.

Phase 13 file `MessageBubbleComponent.java` already exists at `ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/MessageBubbleComponent.java` and supports CssImport-driven styling. Its constructor + setters define the bubble's visual contract; reuse them as-is.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Field/import rewire + chip-strip removal + data-loader binding + empty-state toggle</name>
  <read_first>
    - .planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-SPEC.md (REQ-1 UI-01 + REQ-7 CONTRACT-01)
    - .planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-RESEARCH.md (Pitfall 5 taskFilesDl parameter binding programmatic; Pitfall 6 attachmentsPanel field type stays VerticalLayout)
    - .planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-PATTERNS.md (`ChatPanelFragment.java` MODIFY section)
    - D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java (full file — preserve unchanged sections)
    - D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/ChatSurfaceMounter.java (DO NOT MODIFY — read only to confirm field-type contract on `attachmentsPanel`)
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java
  </files>
  <action>
    **1. Field declarations + injection updates:**
       - DELETE `private MessageList messageList;` and `import com.vaadin.flow.component.messages.MessageList;` and `import com.vaadin.flow.component.messages.MessageListItem;`.
       - ADD `@ViewComponent` field declarations (Jmix injects from XML):
         * `private CollectionContainer<AiTaskFile> taskFilesDc;`
         * `private CollectionLoader<AiTaskFile> taskFilesDl;`
         * `private VerticalLayout attachmentsEmptyState;`
         * `private VerticalLayout attachmentsGridLayout;` (Jmix `<gridLayout>` is `VerticalLayout`-compatible — verify the actual binding type; if Jmix exposes a `JmixGridLayout` class use that)
         * `private JmixUpload taskFileUpload;` (matches new XML id `taskFileUpload`)
       - DELETE the prior `@ViewComponent private JmixUpload upload;` field if it referenced the old `id="upload"` element (now removed in XML).
       - KEEP `private VerticalLayout attachmentsPanel;` field declaration EXACTLY as `VerticalLayout` (REQ-7 / Pitfall 6).
       - KEEP all other `@ViewComponent` and `@Autowired` injections.
       - ADD imports: `import com.vn.agent.entity.AiMessageRole;`, `import com.vn.agent.entity.AiTaskFile;`, `import io.jmix.flowui.model.CollectionContainer;`, `import io.jmix.flowui.model.CollectionLoader;`, `import com.vaadin.flow.component.notification.NotificationVariant;` (if not already imported).

    **2. Init / onReady wiring + chip-strip removal:**
       - DELETE the `messageList = new MessageList(); ... messageListSlot.add(messageList);` block (current lines ~183-187).
       - DELETE the entire chip-strip construction block at lines ~211-233 (the `chipStrip` HorizontalLayout + `attachmentsPanel.add(chipStrip)` + `attachmentsPanel.setVisible(false)`).
       - DELETE the `chipById` map field and any chip helper methods (search the file for `chipById` and `addChip` / `removeChip` / similar — remove the entire chip lifecycle helpers).
       - REWIRE the `UploadHandler.toFile(...)` registration: change the call site `upload.setUploadHandler(...)` → `taskFileUpload.setUploadHandler(...)` (the field rename). The handler body (file save → AiTaskFile insert → handleUploadedFile callback) is unchanged.
       - In `onReady` (or wherever the conversation id is first known), add `taskFilesDl.setParameter("conversationId", currentConversationId); taskFilesDl.load();`. If `currentConversationId` is null at `onReady` time, defer the call to `setConversationIdInternal`.
       - REMOVE the `attachmentsPanel.setVisible(...)` line at the previous line 939 — the right pane is always visible; visibility of contents is handled by `attachmentsEmptyState`.

    **3. Empty-state toggle via PostLoadEvent (Jmix typed listener per memory `feedback_jmix_data_loader_events`):**
       - ADD a method:
         ```java
         @Subscribe(id = "taskFilesDl", target = Target.DATA_LOADER)
         public void onTaskFilesPostLoad(final CollectionLoader.PostLoadEvent<AiTaskFile> event) {
             refreshTaskFiles();
         }
         ```
       - ADD a private helper:
         ```java
         private void refreshTaskFiles() {
             boolean empty = taskFilesDc.getItems().isEmpty();
             attachmentsEmptyState.setVisible(empty);
             attachmentsGridLayout.setVisible(!empty);
         }
         ```
       - Call `refreshTaskFiles()` once at the end of `onReady` to set the initial state correctly.

    Implements REQ-1 UI-01 controller-side foundation + REQ-7 CONTRACT-01 (preserves the `attachmentsPanel` field type).
  </action>
  <verify>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java' -Pattern 'private VerticalLayout attachmentsPanel').Count -eq 1"</automated>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java' -Pattern 'taskFilesDl|taskFilesDc|attachmentsEmptyState').Count -ge 3"</automated>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java' -Pattern 'chipStrip|chipById|new MessageList\(\)').Count -eq 0"</automated>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java' -Pattern '@Subscribe\(id = \"taskFilesDl\", target = Target\.DATA_LOADER\)').Count -eq 1"</automated>
  </verify>
  <done>
    `attachmentsPanel` field type stays `VerticalLayout`. Chip-strip + MessageList substrate gone. Data loader bound programmatically. Empty-state toggle handler in place. (Compile may still be red until Tasks 2 + 3 land — full library compile is asserted at end of Task 3.)
  </done>
</task>

<task type="auto">
  <name>Task 2: NOTICE insert (upload-success path) + history-replay rendering substrate migration</name>
  <read_first>
    - .planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-SPEC.md (REQ-5 UX-01)
    - .planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-CONTEXT.md (integration-points "Upload success → NOTICE insert" + "Card delete → taskFilesDl.load()")
    - .planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-UI-SPEC.md (NOTICE row visual spec)
    - .planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-RESEARCH.md (Pattern 5 NOTICE insertion + rendering; Pitfall 2 stale conversation reference; Pitfall 7 Option A locked)
    - D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/MessageBubbleComponent.java (full file — confirm constructor signature for the bubble migration)
    - D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiMessage.java (full file — confirm role + content + seq + createdDate + conversation field accessors)
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java
  </files>
  <action>
    **4. Conversation change wiring (`setConversationIdInternal` and friends) + history-replay substrate migration:**
       - In `setConversationIdInternal(UUID cid)` (current line 347), AFTER setting `this.conversationId = cid`, add:
         ```java
         taskFilesDl.setParameter("conversationId", cid);
         taskFilesDl.load();
         ```
       - REWORK the history-replay loop (currently lines 369-410). Replace any `MessageListItem` construction with a dispatch by role. Iterate the `AiMessage` rows ordered by `seq ASC` and produce children of `messageListSlot` in order:
         * USER / ASSISTANT → `MessageBubbleComponent` (use the existing constructor; align name resolution with current `buildItem` semantics).
         * NOTICE → a Vaadin `com.vaadin.flow.component.messages.Message` element (or a low-level `Element("vaadin-message")`) with class `attachment-event` and `text=row.getContent()`. Use:
           ```java
           com.vaadin.flow.dom.Element msg = new com.vaadin.flow.dom.Element("vaadin-message");
           msg.getClassList().add("attachment-event");
           msg.setProperty("text", row.getContent());
           messageListSlot.getElement().appendChild(msg);
           ```
         * Helper method `appendNoticeRow(String text)` to keep the live-upload path symmetrical.
       - DELETE the existing `buildItem(AiMessage m, ...)` if it returns `MessageListItem`. Replace with `appendBubble(AiMessage m)` that creates a `MessageBubbleComponent` and appends it to `messageListSlot`.
       - In the live `onSubmit` send path, REPLACE the `MessageListItem userItem = new MessageListItem(...)` / `messageList.setItems(items)` calls with `appendBubble(...)` and direct `messageListSlot.add(component)` calls. Persist any `botMsg` accumulator as a `MessageBubbleComponent` reference instead of `MessageListItem`.

    **5. Upload success → NOTICE insert (research Pitfall 2 + Pattern 5):**
       In `handleUploadedFile(...)`, AFTER the existing `dataManager.save(taskFile)` line and BEFORE the success notification, add:
       ```java
       // Phase 13.1 UX-01: bilingual NOTICE row marking the upload event.
       try {
           AiMessage notice = metadataApi.create(AiMessage.class);
           notice.setConversation(conversation);  // reuse the conversation reloaded above for ensureConversationIdForUpload (Pitfall 2)
           notice.setRole(AiMessageRole.NOTICE);
           notice.setContent(messages.formatMessage(ChatPanelFragment.class,
                   "chatView.attachments.notice",
                   currentAuthentication.getUser().getUsername(),
                   taskFile.getFilename()));
           notice.setCreatedDate(OffsetDateTime.now());
           notice.setSeq(nextSeq(conversation.getId()));
           dataManager.save(notice);
           appendNoticeRow(notice.getContent());
       } catch (RuntimeException noticeEx) {
           log.warn("Failed to write NOTICE AiMessage for upload {}", taskFile.getFilename(), noticeEx);
       }
       taskFilesDl.load();   // refresh the right-pane grid + emptyState toggle via PostLoadEvent
       ```
       If a `nextSeq(UUID)` helper already exists, reuse it; else add one that does `select max(m.seq) from ai_AiMessage m where m.conversation.id = :cid` via DataManager loadValue (with `.store("agentstore")` per memory `feedback_jmix_loadvalue_store`) and returns `(maxSeq + 1)`, defaulting to `0` when the conversation has no messages yet.

    Implements REQ-5 UX-01 (NOTICE insert + render).
  </action>
  <verify>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java' -Pattern 'AiMessageRole\.NOTICE|chatView\.attachments\.notice|appendNoticeRow|appendBubble').Count -ge 4"</automated>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java' -Pattern 'MessageListItem').Count -eq 0"</automated>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java' -Pattern 'vaadin-message|attachment-event').Count -ge 2"</automated>
  </verify>
  <done>
    History-replay dispatches by role (USER/ASSISTANT → bubble, NOTICE → vaadin-message divider). `appendBubble` and `appendNoticeRow` helpers in place. Upload-success path persists a NOTICE `AiMessage` and reloads the grid. (Compile asserted at Task 3.)
  </done>
</task>

<task type="auto">
  <name>Task 3: Budget-exceeded toast wire on BOTH paths + Phase 12 contract zero-diff guard + library compile</name>
  <read_first>
    - .planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-CONTEXT.md (D-D1 toast wire — both blocking AND streaming paths MUST surface the WARNING toast)
    - .planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-03-SUMMARY.md (post Plan 13.1-03 — names the exact streaming terminal emission shape locked by Plan 03 step 2e Option A; this task consumes that contract)
    - D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/java/com/vn/agent/ChatResponseDto.java (post Plan 13.1-03 — confirm `budgetExceeded()` accessor name)
    - D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java (post Plan 13.1-03 — confirm streaming terminal emission shape; whether `.stream()` returns `Flux<ChatResponseDto>` or a `Flux<…>` envelope that carries one)
    - D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/ChatSurfaceMounter.java (DO NOT MODIFY)
    - D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/ChatView.java (DO NOT MODIFY)
    - D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/ChatDialogView.java (DO NOT MODIFY)
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java
  </files>
  <action>
    **6. Budget-exceeded toast (CONTEXT D-D1 — wire on BOTH paths; no scope-reduction escape hatch):**

    Plan 13.1-03 step 2e locked **Option A: reuse `ChatResponseDto` shape on both paths**. The streaming terminal emission also carries a `ChatResponseDto` with `budgetExceeded()` populated. Therefore this task wires a SINGLE toast helper invoked from BOTH consumer sites.

       - ADD a private helper:
         ```java
         private void showBudgetExceededToast() {
             getUI().ifPresent(ui -> ui.access(() ->
                 notifications.create(
                         messages.getMessage(ChatPanelFragment.class,
                                 "chatView.attachments.budgetExceeded"))
                     .withThemeVariant(NotificationVariant.LUMO_WARNING)
                     .show()));
         }
         ```
       - **Blocking-path consumer** (`onSubmit` `.ask` callback): after the successful `ChatResponseDto response = ...` line, add:
         ```java
         if (response.budgetExceeded()) {
             showBudgetExceededToast();
         }
         ```
       - **Streaming-path consumer**: locate the streaming subscriber's terminal-event handler (the place where the subscriber receives the final `ChatResponseDto` per Plan 03's Option A — typically inside `.doOnNext(...)` or `.subscribe(next -> ..., error -> ..., complete -> ...)` over the `Flux<ChatResponseDto>` returned by `.stream()`). Add:
         ```java
         if (terminalResponse.budgetExceeded()) {
             showBudgetExceededToast();
         }
         ```
         If the streaming surface emits a wrapper rather than `ChatResponseDto` directly, dereference to the contained DTO. Read the streaming-call-site BEFORE coding to confirm the exact accessor — Plan 13.1-03's SUMMARY documents the chosen shape.

       - `notifications` is already injected at the existing site; reuse the field.

       **Acceptance criteria for this sub-step:**
         * `Select-String ChatPanelFragment.java -Pattern 'showBudgetExceededToast'` returns ≥ 3 matches (1 declaration + 2 invocations — one per path).
         * `Select-String ChatPanelFragment.java -Pattern '\.budgetExceeded\(\)'` returns ≥ 2 matches (one per path's consumer site).
         * `Select-String ChatPanelFragment.java -Pattern 'chatView\.attachments\.budgetExceeded'` returns exactly 1 match (only inside `showBudgetExceededToast`).

    **7. Phase 12 contract zero-diff guard + library compile:**
       - Run `git diff --name-only origin/main` and assert that ChatSurfaceMounter.java, AiUiSettingsService.java, AiUiSettings.java, ChatView.java, ChatDialogView.java are NOT in the diff. If any of them is, FAIL the task.
       - Run `./gradlew :ai-agent:ai-agent:compileJava` and assert BUILD SUCCESSFUL.

    Implements REQ-3 AUDIT-01 (toast UX on both paths) + REQ-7 CONTRACT-01 (zero-diff regression).
  </action>
  <verify>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java' -Pattern 'showBudgetExceededToast').Count -ge 3"</automated>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java' -Pattern '\.budgetExceeded\(\)').Count -ge 2"</automated>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java' -Pattern 'chatView\.attachments\.budgetExceeded').Count -eq 1"</automated>
    <automated>git diff --name-only origin/main -- ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/ChatSurfaceMounter.java ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/AiUiSettingsService.java ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiUiSettings.java ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/ChatView.java ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/ChatDialogView.java | powershell -Command "$input | ForEach-Object { if ($_ -ne '') { throw 'Phase 12 contract files modified: ' + $_ } }"</automated>
    <automated>./gradlew :ai-agent:ai-agent:compileJava</automated>
  </verify>
  <done>
    Budget-exceeded toast fires once per overflow turn from BOTH `.ask` and `.stream` paths, gated by `ChatResponseDto.budgetExceeded()`. Phase 12 contract files have ZERO diff. Library compile passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Upload handler → AiTaskFile + AiMessage save | Authenticated user identity + DataManager row-level policy enforce ownership; both saves run in the user request thread. |
| NOTICE row content (formatted username + filename) | Rendered inside `<vaadin-message>` text property — Vaadin escapes by default. NEVER set the same content via the `Html` element. |
| Budget-exceeded toast `ui.access` | Runs on the UI thread via the access lock; safe for streaming path which lives on the chat scheduler. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.1-17 | Information Disclosure | Stored XSS via filename injected into NOTICE row text | mitigate | NOTICE content rendered via `setProperty("text", ...)` on `<vaadin-message>` element — Vaadin Flow's `Element.setProperty` for `text` property HTML-escapes. Confirmed via `vaadin-message` component spec. NEVER use `Element.setProperty("innerHTML", ...)`. Same applies to delete-confirm dialog (Plan 13.1-04 Task 2). |
| T-13.1-18 | Spoofing | NOTICE row attributed to wrong user (e.g. impersonation via session race) | accept | `currentAuthentication.getUser().getUsername()` is the canonical authority; same accessor used by the row-level policy that filters `taskFilesDc`. Race conditions across UI sessions are out-of-scope (Phase 12 session-state contract handles this). |
| T-13.1-19 | Tampering | NOTICE row inserted with wrong conversation FK if `ensureConversationIdForUpload` was called this turn (Pitfall 2) | mitigate | Action explicitly reuses the freshly loaded `conversation` reference; do NOT cache an earlier load. Confirmed by Pitfall 2 in research. |
| T-13.1-20 | Denial of Service | Toast spam from repeated budget-exceeded events | accept | Toast fires once per turn; users sending many over-cap turns explicitly chose to do so. Lumo notification has built-in dismissal + auto-fade. |
| T-13.1-21 | Repudiation | Surface mounter contract drift (Phase 12 file modified silently) | mitigate | Acceptance check uses `git diff --name-only origin/main` against the 5 Phase 12 contract files; ANY diff fails the plan. Reinforced by Plan 13.1-06 SurfaceMountingTest. |
</threat_model>

<verification>
- `./gradlew :ai-agent:ai-agent:compileJava` → BUILD SUCCESSFUL.
- `git diff --name-only` includes only the documented `files_modified` set.
- `CrmStyleLayoutTest` + `NoticeRenderTest` + `SurfaceMountingTest` (Plan 13.1-06) are the regression gates.
- `boot-only` smoke: run `./gradlew :jmix-app:bootRun` locally and verify the chat view loads — covered by Plan 13.1-06 integration tests.
</verification>

<success_criteria>
1. `attachmentsPanel` field type stays `VerticalLayout`.
2. `taskFilesDl` data loader is bound programmatically with `:conversationId`; reloaded on conversation switch + after upload + after card delete (the renderer in Plan 13.1-04 already triggers the loader via `dataManager.remove`; this plan's `@Subscribe(...PostLoadEvent)` rebinds the empty state).
3. Upload success persists NOTICE `AiMessage` + appends a `<vaadin-message class="attachment-event">` element to `messageListSlot`.
4. History replay dispatches by role: USER/ASSISTANT → `MessageBubbleComponent`; NOTICE → `<vaadin-message class="attachment-event">`.
5. Budget-exceeded toast shows once per overflow turn via `ui.access`.
6. Phase 12 contract files have ZERO modifications in this plan's diff.
7. `./gradlew :ai-agent:ai-agent:compileJava` and `compileTestJava` pass.
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-05-SUMMARY.md` documenting:
- Constructor and field-list deltas (added/removed)
- New @Subscribe handlers
- New private helpers (`refreshTaskFiles`, `appendBubble`, `appendNoticeRow`, `nextSeq`, `showBudgetExceededToast`)
- Confirmation that the 5 Phase 12 contract files have zero diff
- Compile pass log line
</output>
