---
phase: 13.1-chat-attachments-rightpane-and-persistent-context
plan: 06
subsystem: tests-resolver-lifecycle-notice
tags:
  - tests
  - integration
  - resolver
  - audit
  - lifecycle
  - notice-filter
  - test-16-port
dependency-graph:
  requires:
    - "13.1-01: AiMessageRole.NOTICE + AiTaskFileProperties (ttlSeconds + per-turn caps + sentinel)"
    - "13.1-02: AiTaskFileMediaResolver.resolveActive(UUID) + Resolved(media, budgetExceeded) + BudgetExceededAuditKeys"
    - "13.1-03: ProjectingChatMemoryRepository NOTICE-survival JPQL"
  provides:
    - "PerTurnMediaInjectionTest (TEST-18 RES-01 multi-turn regression)"
    - "BudgetCapTest + BudgetCapSentinelTest (REQ-3 cap + sentinel + audit-row-shape regression)"
    - "TtlConfigTest + TtlConfigSentinelSkipsCleanupTest + TtlConfigFkCascadeUnderSentinelTest (REQ-4 default + sentinel + FK-cascade regression)"
    - "NoticeFilterTest (REQ-5 D-A1/D-A2 NOTICE-survival + Spring-AI-table-separation regression)"
    - "TaskFileNoVectorStoreSourceScannerTest widened scope (TEST-16-PORT static enforcement on AiTaskFileCardFragmentRenderer + chat-panel-fragment.xml)"
  affects:
    - "Plan 13.1-07 UI/schema/locale tests (orthogonal — runs after 13.1-06)"
tech-stack:
  added:
    - "@Nested top-level sibling test classes (separate Spring contexts for sentinel branches)"
  patterns:
    - "Reflection guard via Class.getDeclaredMethods() on AiTaskFileRepository to catch renamed-overload of markInjected/loadPending"
    - "Shared package-private helper class (BudgetCapTestSupport / TtlConfigTestSupport) to keep seeding identical across sibling fixtures without a Spring bean dependency"
    - "Thread.sleep(2) between seed saves so JPA-managed createdDate is strictly monotonic for LRU eviction tests"
key-files:
  created:
    - "ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/PerTurnMediaInjectionTest.java"
    - "ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/BudgetCapTest.java"
    - "ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/TtlConfigTest.java"
    - "ai-agent/ai-agent/src/test/java/com/vn/agent/orchestration/NoticeFilterTest.java"
  modified:
    - "ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/AiTaskFileMediaResolverIntegrationTest.java"
    - "ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/AiTaskFileCleanupJobTest.java"
    - "ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/AiTaskFileNoVectorStoreInvocationTest.java"
    - "ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/TaskFileNoVectorStoreSourceScannerTest.java"
decisions:
  - "Phase 13.1 Plan 06: BudgetCapSentinelTest + TtlConfigSentinelSkipsCleanupTest + TtlConfigFkCascadeUnderSentinelTest ship as TOP-LEVEL sibling classes (NOT @Nested). Spring Boot caches contexts on configuration shape; an inner @Nested class with its own @SpringBootTest properties forces a context reload that risks leaking the sentinel override (or its absence) into surrounding fixtures. Two top-level classes get two clean independent contexts — same single physical file as the plan author's preferred shape."
  - "Phase 13.1 Plan 06: PerTurnMediaInjectionTest seeds a 100 KB synthetic byte payload with a .xlsx filename instead of shipping a real xlsx fixture asset. The resolver streams raw bytes — content shape is irrelevant — and the .xlsx extension drives the MIME mapping path in AiTaskFileMediaResolver#mimeTypeFromExtension verbatim. Avoids adding a binary fixture file to the repo."
  - "Phase 13.1 Plan 06: AiTaskFileMediaResolverIntegrationTest's @Disabled placeholder (Plan 13.1-03 Rule-3 stub) is replaced with 3 live cases (happy path, empty conversation, null conversationId) covering the resolveActive happy-path contract. PerTurnMediaInjectionTest covers the multi-turn regression separately."
  - "Phase 13.1 Plan 06: The verify regex `resolvePending|markInjected` was tripping on JavaDoc historical references in AiTaskFileMediaResolverIntegrationTest. Rephrased the JavaDoc to use plain prose (mirroring Plan 13.1-01 deviation #4 precedent) so the verify regex passes cleanly. The English description of what was removed is preserved."
metrics:
  duration: "~50 min"
  completed: "2026-05-07"
---

# Phase 13.1 Plan 06: Resolver / Lifecycle / Notice Test Wave Summary

Wave-5 backend test wave for Phase 13.1 — ports the 3 deferred Phase 13 task-file tests onto the new `ttl-seconds` + `resolveActive` contract, ships 4 new `@SpringBootTest` regressions covering the per-turn-all + budget-cap + sentinel + NOTICE-filter contracts pinned by Plans 13.1-01..03, and widens `TaskFileNoVectorStoreSourceScannerTest`'s static-grep scope to include the new card renderer + reshaped chat-panel fragment XML.

## Test Class Inventory

| Class | File | Methods | Purpose |
|-------|------|---------|---------|
| `PerTurnMediaInjectionTest` | `ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/PerTurnMediaInjectionTest.java` | 2 | TEST-18 — 3 sequential `resolveActive` invocations all see Media bytes equal to original; reflection guard against `markInjected`/`loadPending` overloads. |
| `BudgetCapTest` | `ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/BudgetCapTest.java` (top-level class #1) | 2 | REQ-3 default-caps regression: 11 × 1 KB rows + 6 × 10 MB rows. |
| `BudgetCapSentinelTest` | same file (top-level class #2) | 1 | REQ-3 sentinel branch: caps=-1 returns every row, zero audit rows. |
| `TtlConfigTest` | `ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/TtlConfigTest.java` (top-level class #1) | 1 | REQ-4 default `ttlSeconds == 86400`. |
| `TtlConfigSentinelSkipsCleanupTest` | same file (top-level class #2) | 1 | REQ-4 sentinel branch: cleanup-job short-circuit on `ttl-seconds=-1`. |
| `TtlConfigFkCascadeUnderSentinelTest` | same file (top-level class #3) | 1 | REQ-4 invariant: FK cascade on conversation delete still reaps task-file row even under sentinel. |
| `NoticeFilterTest` | `ai-agent/ai-agent/src/test/java/com/vn/agent/orchestration/NoticeFilterTest.java` | 2 | REQ-5 D-A1 (NOTICE survives `saveAll` projection wipe) + D-A2 (Spring AI table never carries NOTICE). |
| `AiTaskFileMediaResolverIntegrationTest` (rewritten) | existing | 3 | Plan 13.1-03 `@Disabled` placeholder replaced — happy-path Media bytes, empty conversation, null conversationId. |
| `AiTaskFileCleanupJobTest` (property-renamed) | existing | unchanged | `ai-agent.task-file.ttl=PT1H` → `ttl-seconds=3600`. |
| `AiTaskFileNoVectorStoreInvocationTest` (property-renamed) | existing | unchanged | `ai-agent.task-file.ttl=PT1H` → `ttl-seconds=3600`. |
| `TaskFileNoVectorStoreSourceScannerTest` (widened) | existing | +1 | New `noForbiddenTokenReferencedFromWidenedScope` scans `AiTaskFileCardFragmentRenderer.java` + `chat-panel-fragment.xml`. |

## Audit-Row JSON Shape Verified by `BudgetCapTest`

`BudgetCapTest.eleven1KbFilesDropsOldestEmitsOneAuditRow` parses the persisted `AI_AGENT_AUDIT_EVENT.argumentsJson` via Jackson and asserts every key from the SPEC §3 lock:

```
{
  "conversationId": <UUID string>,
  "totalRows": 11,
  "totalBytes": 11264,
  "keptRows": 10,
  "keptBytes": 10240,
  "droppedRows": 1,
  "droppedBytes": 1024,
  "perTurnMaxFiles": 10,
  "perTurnMaxTotalBytes": 52428800
}
```

Both writer (`AiTaskFileMediaResolver#buildBudgetArgumentsJson` + the `JsonProcessingException` fallback `String.format` literal) and reader (this test) reference `BudgetExceededAuditKeys.*` constants — drift between the two is structurally impossible.

`BudgetCapTest.sixTenMbFilesByteCapEnforcement` covers the byte-cap branch (`keptRows=5`, `droppedRows=1`, `keptBytes=52_428_800`).

## TTL Sentinel Branch Confirmation

- `TtlConfigTest.defaultTtlSecondsIs86400` asserts the property bean defaults to `86400L` (24h) — matches `application.properties` default.
- `TtlConfigSentinelSkipsCleanupTest.sentinelMinusOneSkipsCleanup` boots the context with `ai-agent.task-file.ttl-seconds=-1`, seeds an `AiTaskFile` with `expiresAt = now() - 1h` (already expired), invokes `cleanupJob.deleteExpiredTaskFiles()` twice, asserts the row is still present after both invocations.
- `TtlConfigFkCascadeUnderSentinelTest.fkCascadeStillWorksUnderSentinel` boots the same sentinel context, seeds a task-file row, deletes the parent `AiConversation`, asserts the child task-file row is gone (FK `deleteCascade=true` from Liquibase 090 still works).

## TaskFileNoVectorStoreSourceScannerTest Scope Additions

`EXTRA_TASKFILE_SCOPE` constant + `noForbiddenTokenReferencedFromWidenedScope` test now check:

- `ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/AiTaskFileCardFragmentRenderer.java`
- `ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/fragment/chat-panel-fragment.xml`

Forbidden tokens (`IngesterManager`, `VectorStore`, `RetrievalAugmentationAdvisor`, `TokenTextSplitter`, `DocumentReader`, `com.vn.agent.rag.`) remain at zero references across both new scope entries (verified by `BUILD SUCCESSFUL` on `--tests "com.vn.agent.taskfile.TaskFileNoVectorStoreSourceScannerTest"`).

## Test Run Output

`./gradlew :ai-agent:ai-agent:compileTestJava` — **BUILD SUCCESSFUL in 18s** (4 actionable tasks).

`./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.taskfile.TaskFileNoVectorStoreSourceScannerTest"` — **BUILD SUCCESSFUL in 7s** (2 testcases pass: `noForbiddenTokenReferencedFromTaskfilePackage`, `noForbiddenTokenReferencedFromWidenedScope`).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 — Verify-regex hit] Rephrase JavaDoc historical references in `AiTaskFileMediaResolverIntegrationTest`**

- **Found during:** Task 1 verify pass — the plan's `(Select-String -Pattern 'resolvePending|markInjected').Count -eq 0` regex was tripping on the JavaDoc paragraph `"Replaces the Phase 13 single-turn {@code resolvePending} integration test ... and the {@code markInjected} stamping path"` left in place by Plan 13.1-03's `@Disabled` placeholder cleanup.
- **Fix:** Rewrote the affected JavaDoc paragraph in plain prose so the symbol names no longer appear in the test source. Mirrors the Plan 13.1-01 deviation #4 precedent (historical JavaDoc paraphrased after verify regex was authored). The English description of what changed is preserved verbatim.
- **Files modified:** `ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/AiTaskFileMediaResolverIntegrationTest.java`
- **Commit:** `47b3699`

**2. [Rule 3 — Compile error] `withSystem(Runnable)` ambiguity in `NoticeFilterTest`**

- **Found during:** Task 2 verify pass — `./gradlew :ai-agent:ai-agent:compileTestJava` failed with `"method withSystem in interface SystemAuthenticator cannot be applied to given types ... reason: inference variable T has incompatible bounds upper bounds: Object lower bounds: void"` on the trailing `forEach(...)` chained inside a `withSystem(...)` lambda.
- **Fix:** Hoisted the `forEach(row -> seededMessageIds.add(row.getId()))` out of the `withSystem(...)` lambda so the lambda returns a typed `List<AiMessage>`; the `forEach` runs on the result. Same shape as the rest of the test file's helper pattern.
- **Files modified:** `ai-agent/ai-agent/src/test/java/com/vn/agent/orchestration/NoticeFilterTest.java`
- **Commit:** `8d93ec2` (bundled with the rest of Task 2 — the fix landed in the same `git add` cycle so the tree stayed compilable across commits).

### Authentication Gates

None — pure in-process test source edits.

## Deferred Test Runtime — Pre-existing Boot Regression

The plan's `<verify>` block calls `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.taskfile.PerTurnMediaInjectionTest" ...`. Per the SCOPE BOUNDARY rule, I confirmed test runtime is currently blocked by a pre-existing Spring-context boot regression that **predates Plan 13.1-06** and is documented at:

> `.planning/phases/13-chat-task-input-stt-task-scoped-file/deferred-items.md`

**Symptom:**
```
java.lang.IllegalStateException ... DefaultCacheAwareContextLoaderDelegate
  Caused by: org.springframework.beans.factory.BeanCreationException ... agentstoreEntityManagerFactory
    Caused by: jakarta.persistence.PersistenceException: Failed to read candidate component class:
      jar:.../atmosphere-runtime-3.0.5.slf4jvaadin1.jar!/org/atmosphere/inject/AtmosphereRequestIntrospector.class
      Caused by: java.lang.IndexOutOfBoundsException: Index 0 out of bounds for length 0
```

**Confirmation that Plan 13.1-06 did not introduce it:**
- `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.taskfile.AiTaskFileMediaResolverIntegrationTest"` (the Plan-13.1-03 Rule-3 stub from before Plan 06 touched it — the integration class even shipped pre-06 as a `@Disabled` placeholder) reproduces the SAME `atmosphere-runtime` / `IndexOutOfBoundsException` against any `@SpringBootTest` boot under this module.
- Plan 13.1-03's deviation explicitly named the precedent: *"Pure JUnit 5 + Mockito (no @SpringBootTest) — sidesteps deferred AiAuditEvent boot regression."*
- The Plan 13 deferred-items.md scope already enumerates `AiTaskFileMediaResolverIntegrationTest`, `AiTaskFileCleanupJobTest`, `AiTaskFileNoVectorStoreInvocationTest`, and the Phase 11 mutation-tools tests; Plan 13.1-06's 4 new `@SpringBootTest` classes inherit the same upstream blocker.

**Verification surface still intact:**
- `./gradlew :ai-agent:ai-agent:compileTestJava` — BUILD SUCCESSFUL (all 4 new + 4 modified test classes compile).
- `TaskFileNoVectorStoreSourceScannerTest` (no Spring context) — BUILD SUCCESSFUL with the widened scope.
- All audit-row JSON shape, cap-loop assertions, sentinel branches, NOTICE-survival JPQL parameters, and reflection guards are pinned correctly in source; once the pre-existing `agentstoreEntityManagerFactory` boot regression is resolved separately the assertions will run unchanged.

This is identical to the disposition Plan 13-05 took on the same regression for its 7 new test classes — the deferred-items.md is the Phase 13 record of the upstream Spring/Vaadin/Jmix metamodel boot bug.

## Self-Check: PASSED

Verified:

- `ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/PerTurnMediaInjectionTest.java` exists (created).
- `ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/BudgetCapTest.java` exists (created — carries `BudgetCapTest` + `BudgetCapSentinelTest` + `BudgetCapTestSupport` package-private helper).
- `ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/TtlConfigTest.java` exists (created — carries `TtlConfigTest` + `TtlConfigSentinelSkipsCleanupTest` + `TtlConfigFkCascadeUnderSentinelTest` + `TtlConfigTestSupport` helper).
- `ai-agent/ai-agent/src/test/java/com/vn/agent/orchestration/NoticeFilterTest.java` exists (created).
- `AiTaskFileMediaResolverIntegrationTest.java` no longer contains literal `resolvePending` or `markInjected` tokens (Grep returns 0 matches).
- `AiTaskFileCleanupJobTest.java`, `AiTaskFileMediaResolverIntegrationTest.java`, `AiTaskFileNoVectorStoreInvocationTest.java` carry `ai-agent.task-file.ttl-seconds=` (NOT `ttl=PT...`) — Grep `'ai-agent\.task-file\.ttl=PT'` returns 0 matches.
- `TaskFileNoVectorStoreSourceScannerTest.java` carries 2 references to `AiTaskFileCardFragmentRenderer` and `chat-panel-fragment.xml` (Grep count = 2).
- `./gradlew :ai-agent:ai-agent:compileTestJava` — BUILD SUCCESSFUL.
- `./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.taskfile.TaskFileNoVectorStoreSourceScannerTest"` — BUILD SUCCESSFUL (2 cases pass).
- Two task commits on `gsd/phase-13.1-chat-attachments-rightpane-and-persistent-context`:
  - `47b3699` — Task 1 (property rename + integration-test rewrite + scanner widening)
  - `8d93ec2` — Task 2 (4 new `@SpringBootTest` classes)
