---
phase: 13.1-chat-attachments-rightpane-and-persistent-context
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/ProjectingChatMemoryRepository.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/UserMessagePersister.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/DefaultChatServiceImplStreamFallbackTest.java
autonomous: true
requirements:
  - RES-01-wiring
  - UX-01-projection-filter
  - BLK-01-preserve
tags:
  - chat-service
  - memory-projection
  - streaming-fallback
  - service-deletion
must_haves:
  truths:
    - "DefaultChatServiceImpl calls AiTaskFileMediaResolver.resolveActive(convId) on every chat turn (.ask + .stream + streaming-fallback) and never calls resolvePending or markInjected"
    - "ProjectingChatMemoryRepository.saveAll JPQL excludes role=NOTICE from the delete-recreate wipe so NOTICE rows survive every projection"
    - "UserMessagePersister class and its standalone test are deleted (sole caller was DefaultChatServiceImpl two-phase write that no longer exists)"
    - "executeBlockingTurn helper retained as the streaming-fallback target with the userMessageIdAlreadyPersisted parameter dropped — BLK-01 invariant preserved"
    - "Streaming path runs resolveActive INSIDE Flux.defer so blob reads do not block the calling thread before .subscribe()"
    - "DefaultChatServiceImplStreamFallbackTest is rewritten to assert the post-Phase-13.1 contract (resolveActive called once per turn; no markInjected mock)"
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/ProjectingChatMemoryRepository.java"
      provides: "saveAll JPQL with role <> :noticeRole predicate"
      contains: "AiMessageRole.NOTICE.getId()"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java"
      provides: "resolveActive integration; markInjected + UserMessagePersister wiring removed"
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/DefaultChatServiceImplStreamFallbackTest.java"
      provides: "Mockito tests asserting resolveActive contract on streaming-fallback"
  key_links:
    - from: "DefaultChatServiceImpl.ask + DefaultChatServiceImpl.stream"
      to: "AiTaskFileMediaResolver.resolveActive"
      via: "single call per turn — passes Resolved.media into chatClient.prompt().user(...)"
      pattern: "taskFileMediaResolver\\.resolveActive\\("
    - from: "ProjectingChatMemoryRepository.saveAll"
      to: "AiMessage rows with role != NOTICE"
      via: "JPQL parameter binding"
      pattern: "m\\.role <> :noticeRole"
---

<objective>
Wire the new per-turn-all resolver into `DefaultChatServiceImpl`, drop the now-vestigial `markInjected` and `UserMessagePersister` machinery, and add the 1-line JPQL predicate to `ProjectingChatMemoryRepository.saveAll` that keeps NOTICE rows alive across the delete-recreate projection cycle. Rewrite `DefaultChatServiceImplStreamFallbackTest` so its 5 cases reflect the new contract (`resolveActive` called once per turn; no `markInjected` to assert). The Phase-13 BLK-01 invariant (streaming-fallback never double-persists the user message) is preserved by keeping `executeBlockingTurn` as the fallback target — only its `userMessageIdAlreadyPersisted` parameter is dropped because there is nothing to "already persist" anymore.

Purpose: connect Wave-1 resolver work to the chat path and clean up the now-dead two-phase write surface in one focused plan; locks the budget-exceeded propagation contract (`Resolved.budgetExceeded` → `ChatResponseDto.budgetExceeded`) for Plan 13.1-04 (UI fragment) to consume.

Output: rewired chat service, slimmer ChatMemory projection, deleted persister class + test, and a passing rewritten streaming-fallback test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-SPEC.md
@.planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-RESEARCH.md
@.planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-PATTERNS.md
@.planning/phases/13-chat-task-input-stt-task-scoped-file/13-06-SUMMARY.md

<interfaces>
<!-- Pre-Phase-13.1 chat-path surfaces — preserve everything not explicitly listed for change. -->

From AiTaskFileMediaResolver (post Plan 13.1-02):
```java
public Resolved resolveActive(UUID conversationId);
public record Resolved(List<Media> media, boolean budgetExceeded) { ... }
```

From ProjectingChatMemoryRepository.java (current saveAll wipe pass — lines 85-89):
```java
dataManager.load(AiMessage.class)
        .query("select m from ai_AiMessage m where m.conversation.id = :cid")
        .parameter("cid", convUuid)
        .list()
        .forEach(dataManager::remove);
```

From DefaultChatServiceImpl (current — sites that change):
- Constructor at line 163 takes `UserMessagePersister userMessagePersister` — DROP this parameter and the field.
- `taskFileMediaResolver.resolvePending(convId)` at lines 276 + 525 — REPLACE with `resolveActive`.
- `taskFileRepository.markInjected(...)` at lines 388 + 586 — REMOVE these blocks entirely.
- `executeBlockingTurn(...)` helper at lines 338-431 — KEEP but drop the `userMessageIdAlreadyPersisted` parameter; signature update only.

From `ChatResponseDto` (existing record returned by `.ask()`) — extend with `boolean budgetExceeded`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProjectingChatMemoryRepository JPQL filter + DefaultChatServiceImpl resolver wiring + UserMessagePersister deletion</name>
  <read_first>
    - .planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-CONTEXT.md (D-A1 / D-A2 — JPQL change is the only modification needed; Spring AI's `JdbcChatMemoryRepository` is structurally separate)
    - .planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-RESEARCH.md (Pattern 4 verbatim 1-line JPQL fix; Pitfall 8 — preserve `executeBlockingTurn` helper; "Component Responsibilities" — `UserMessagePersister` likely DELETE)
    - .planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-PATTERNS.md (`ProjectingChatMemoryRepository.java`, `DefaultChatServiceImpl.java`, `UserMessagePersister.java` sections)
    - .planning/phases/13-chat-task-input-stt-task-scoped-file/13-06-SUMMARY.md (BLK-01 contract — streaming-fallback delegates to executeBlockingTurn; preserve)
    - D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/ProjectingChatMemoryRepository.java (full file — change ONLY lines 85-89; do NOT touch deleteByConversationId at lines 113-123)
    - D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java (full file — sites at lines 138, 142, 163, 273-287, 338-431, 525-586, 763-781)
    - D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/java/com/vn/agent/UserMessagePersister.java (verify file exists and is the class to delete; confirm there are no other callers via Grep)
    - D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiMessageRole.java (post Plan-01 with `NOTICE` entry)
    - D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/java/com/vn/agent/ChatResponseDto.java (current shape of the response record so the new `budgetExceeded` field lands cleanly)
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/ProjectingChatMemoryRepository.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/UserMessagePersister.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/ChatResponseDto.java
  </files>
  <action>
    1. EDIT `ProjectingChatMemoryRepository.java` (D-A1 — 1-line JPQL fix at lines 85-89):
       - REPLACE the existing 5-line block:
         ```java
         dataManager.load(AiMessage.class)
                 .query("select m from ai_AiMessage m where m.conversation.id = :cid")
                 .parameter("cid", convUuid)
                 .list()
                 .forEach(dataManager::remove);
         ```
         with:
         ```java
         dataManager.load(AiMessage.class)
                 .query("select m from ai_AiMessage m where m.conversation.id = :cid " +
                         "and m.role <> :noticeRole")
                 .parameter("cid", convUuid)
                 .parameter("noticeRole", AiMessageRole.NOTICE.getId())
                 .list()
                 .forEach(dataManager::remove);
         ```
       - ADD import `import com.vn.agent.entity.AiMessageRole;` if missing.
       - DO NOT modify `deleteByConversationId` (lines 113-123) — when the conversation is deleted, NOTICE rows MUST go too.
       - ADD a single-line comment immediately above the modified query: `// Phase 13.1 D-A1: NOTICE rows survive the projection wipe so the upload ledger persists across turns.`

    2. EDIT `DefaultChatServiceImpl.java`:
       a. CONSTRUCTOR (line 163):
          - DROP parameter `UserMessagePersister userMessagePersister` and the field `private final UserMessagePersister userMessagePersister;` (line 142).
          - DROP corresponding constructor body assignment.
          - DROP `import com.vn.agent.UserMessagePersister;` if it becomes unused.

       b. BLOCKING `.ask` PATH (around lines 270-300):
          - REPLACE `taskFileMediaResolver.resolvePending(convId)` with `taskFileMediaResolver.resolveActive(convId)`.
          - DELETE the `userMessagePersister.persistUserMessage(...)` call block (the `try/catch` around lines 282-288).
          - The helper call now becomes `executeBlockingTurn(<arguments without userMessageIdAlreadyPersisted>)`. Adjust call site to drop that argument.

       c. BLOCKING HELPER `executeBlockingTurn` (lines 338-431):
          - DROP the `UUID userMessageIdAlreadyPersisted` parameter from the method signature.
          - DELETE the entire `markInjected` try/catch block (lines 383-394) — the post-send marker no longer exists.
          - LEAVE the rest of the helper (chatClient build, baseline context, audit, error translation) untouched.
          - The `Resolved resolvedMedia` variable now references `Resolved(media, budgetExceeded)` — propagate `budgetExceeded` into the returned `ChatResponseDto` (see step 2e).

       d. STREAMING `.stream` PATH (around lines 510-590):
          - REPLACE `taskFileMediaResolver.resolvePending(convId)` with `taskFileMediaResolver.resolveActive(convId)`.
          - HOIST the `resolveActive` call INSIDE the `Flux.defer(...)` body (research §DefaultChatServiceImpl `Pitfall — streaming path: hoist inside Flux.defer`). The exact mechanic: move the `Resolved resolvedMedia = taskFileMediaResolver.resolveActive(convId);` line so it executes inside the deferred supplier, not on the caller thread before `.subscribe()`.
          - DELETE the `userMessagePersister.persistUserMessage` block (analogous to step 2b).
          - DELETE the `taskFileRepository.markInjected(...)` block at line 586.
          - The streaming path's graceful-fallback `catch(UnsupportedOperationException)` MUST continue to delegate to `executeBlockingTurn(...)` (BLK-01 invariant). Update the call site to drop the `userMessageIdAlreadyPersisted` argument.

       e. BUDGET-EXCEEDED PROPAGATION (research Open Question 6 + CONTEXT D-D1 — **Option A LOCKED: reuse `ChatResponseDto` shape on both paths**):
          - EXTEND `ChatResponseDto` (in `ChatResponseDto.java`) by adding a `boolean budgetExceeded` field at the END of the existing record component list. Keep all existing fields (text, conversationId, latencyMs, …) in their current order. Add a static helper `ChatResponseDto.withBudgetExceeded(ChatResponseDto base, boolean budgetExceeded)` (or rely on Java record canonical-constructor copy) — adopt whichever pattern matches existing helpers in `ChatResponseDto.java` (read first to confirm).
          - **Blocking path (`.ask`)**: every `new ChatResponseDto(...)` call site inside `DefaultChatServiceImpl.executeBlockingTurn` MUST set the new field from `resolvedMedia.budgetExceeded()`. There are no scope-reduction escape hatches — D-D1 mandates the toast on BOTH paths.
          - **Streaming path (`.stream`)**: locate the terminal completion emission in `Flux<…>` returned by `.stream()` — i.e. the final aggregated assistant turn that the subscriber consumes after token streaming completes. This emission MUST also produce a `ChatResponseDto` (or a wrapper that carries one) with `budgetExceeded = resolvedMedia.budgetExceeded()`. Concrete steps:
            1. Identify the streaming surface's terminal type. If `.stream()` already returns `Flux<ChatResponseDto>` or a `ChatStreamEvent` envelope that contains a `ChatResponseDto`, set the field on that final emission.
            2. If the streaming subscriber currently consumes only `Flux<String>` token chunks (no terminal envelope), introduce a `Flux.concat(tokenFlux, Mono.just(buildFinalChatResponseDto(resolvedMedia.budgetExceeded(), ...)))` style emission so the subscriber receives a single terminal `ChatResponseDto` after the last token. Document the chosen emission shape in the SUMMARY.
            3. Read `ChatResponseDto.java` and the streaming-call-site BEFORE coding to pick whichever pattern fits the existing surface with the smallest diff.
          - **Acceptance criteria for this sub-step (graded by BudgetExceededTest cases C + D in Plan 13.1-03 Task 2 and toast-wire grep in Plan 13.1-05):**
            * `Select-String DefaultChatServiceImpl.java -Pattern 'budgetExceeded'` returns ≥ 2 matches (one per path).
            * Both `.ask` AND `.stream` callers downstream see a `ChatResponseDto` with the boolean populated — verified by Plan 13.1-03 Test C (streaming) and Test D (blocking).
            * No streaming-gap "deferred for later" note in the SUMMARY — D-D1 forbids partial coverage.

       f. CHECK there are no remaining references to `taskFileRepository.markInjected`, `resolvePending`, or `userMessagePersister` anywhere in `DefaultChatServiceImpl.java` after the edits.

    3. DELETE `ai-agent/ai-agent/src/main/java/com/vn/agent/UserMessagePersister.java` and its companion test file `ai-agent/ai-agent/src/test/java/com/vn/agent/UserMessagePersisterTest.java` if it exists. Use `git rm <path>` semantics — confirm no other production file imports `UserMessagePersister` BEFORE deletion (`Grep -n UserMessagePersister ai-agent/ai-agent/src/main/java`). If a non-test caller is found, STOP and surface as a checkpoint — Plan should not proceed silently.

    4. VERIFY library compile.

    Implements RES-01 chat-service wiring + UX-01 projection-filter (D-A1) + research-cited removal of vestigial UserMessagePersister.
  </action>
  <verify>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/ProjectingChatMemoryRepository.java' -Pattern 'm\.role <> :noticeRole|AiMessageRole\.NOTICE\.getId\(\)').Count -ge 2"</automated>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java' -Pattern 'resolveActive\(').Count -ge 2 -and (Select-String -Path 'ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java' -Pattern 'resolvePending|markInjected|UserMessagePersister|userMessagePersister').Count -eq 0"</automated>
    <automated>powershell -Command "-not (Test-Path 'ai-agent/ai-agent/src/main/java/com/vn/agent/UserMessagePersister.java')"</automated>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/java/com/vn/agent/ChatResponseDto.java' -Pattern 'budgetExceeded').Count -ge 1"</automated>
    <automated>./gradlew :ai-agent:ai-agent:compileJava</automated>
  </verify>
  <done>
    Projection JPQL excludes NOTICE rows. DefaultChatServiceImpl is free of `resolvePending`, `markInjected`, `UserMessagePersister`. ChatResponseDto carries `budgetExceeded`. Library compile passes (test compile may still be red until Task 2 lands).
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite DefaultChatServiceImplStreamFallbackTest for the post-Phase-13.1 contract</name>
  <read_first>
    - .planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-RESEARCH.md (Open Question 5 — recommendation: rewrite, not delete)
    - .planning/phases/13-chat-task-input-stt-task-scoped-file/13-06-SUMMARY.md (existing test scaffold + Mockito setup; BLK-01 invariant the rewrite must continue to enforce)
    - D:/DTH/ai-agent-core/ai-agent/ai-agent/src/test/java/com/vn/agent/DefaultChatServiceImplStreamFallbackTest.java (full file — 5 cases; mocks `userMessagePersister.persistUserMessage` and `taskFileRepository.markInjected`)
    - D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java (post Task-1 — confirm `executeBlockingTurn` signature)
  </read_first>
  <files>
    ai-agent/ai-agent/src/test/java/com/vn/agent/DefaultChatServiceImplStreamFallbackTest.java
  </files>
  <action>
    1. REWRITE `DefaultChatServiceImplStreamFallbackTest.java` to align with the post-Phase-13.1 contract. The class structure stays pure JUnit 5 + Mockito (no `@SpringBootTest`) per the BLK-01 closure rationale (sidesteps deferred boot regression). Mock the post-edit constructor surface of `DefaultChatServiceImpl` — i.e. drop the `UserMessagePersister` mock and any `markInjected` interaction.

    2. KEEP the test class name and file location. Replace the 5 cases with the following 4 cases (consolidating; cleaner contract):

       - **Test A — `streamingHappyPathCallsResolveActiveExactlyOnce`**: stub `chatClient.prompt().user().stream()` to return a normal `Flux`. Assert `taskFileMediaResolver.resolveActive(convId)` is invoked EXACTLY ONCE per turn (use `verify(resolver, times(1)).resolveActive(any())`). Assert `taskFileRepository.markInjected` mock does not exist on the class (compile-time guarantee).

       - **Test B — `streamingFallbackDelegatesToExecuteBlockingTurnExactlyOnce`**: stub `chatClient.prompt().user().stream()` to throw `UnsupportedOperationException` synchronously (BEFORE subscription, so the `Flux.defer`'d resolveActive call is never reached) so the surrounding `catch (UnsupportedOperationException)` delegates to `executeBlockingTurn`. Assertions (in order):
         1. **Stream throws synchronously**: invoking `defaultChatService.stream(...)` results in the streaming-builder's `chatClient.prompt()...stream()` throwing `UnsupportedOperationException` BEFORE any `Flux.defer`'d body executes. Use `verify(chatClient.prompt().user(...), times(1)).stream();` and confirm via `org.assertj.core.api.Assertions.assertThatThrownBy(...)` only if the streaming entry point is synchronous; otherwise inspect the produced `Flux` and let `.subscribe()` materialise the error signal.
         2. **Pre-fallback resolver no-interaction guard**: BEFORE the catch-block fallback runs, the streaming path must NOT have called `resolveActive` (because `.stream()` exploded before subscription, the defer body never ran). Express this as `Mockito.verifyNoInteractions(resolverDuringStreamingPhaseSpy)` OR — simpler — capture invocations via `Mockito.inOrder(resolver)` and assert no `resolveActive` invocation precedes the first `chatClient.prompt()` call. If using a single `resolver` mock, the equivalent assertion is "after `chatClient.prompt()...stream()` throws, BEFORE `executeBlockingTurn` is invoked, `verify(resolver, never()).resolveActive(any())`" — captured via `InOrder inOrder = inOrder(chatClient, resolver); inOrder.verify(chatClient.prompt().user(...)).stream(); inOrder.verify(resolver, never()).resolveActive(any());` (semantically: nothing on `resolver` between the throw and the fallback).
         3. **Fallback resolves exactly once**: after the `executeBlockingTurn` fallback path completes, `verify(resolver, times(1)).resolveActive(convId);` — the blocking helper performs the ONE and ONLY `resolveActive` call for the entire turn. Combined with assertion 2, this proves total invocations = 1, all of them inside the fallback. BLK-01 invariant holds.

       - **Test C — `streamingHappyPathPropagatesBudgetExceeded`**: stub `resolveActive` to return `Resolved(media=[oneMedia], budgetExceeded=true)`. Assert that the terminal `ChatResponseDto` (or streaming envelope) emitted to the subscriber carries `budgetExceeded=true`.

       - **Test D — `blockingPathPropagatesBudgetExceeded`**: same as Test C but for `.ask(...)` — assert returned `ChatResponseDto.budgetExceeded() == true`.

    3. DELETE any `@MockitoBean private UserMessagePersister ...` field, any `when(userMessagePersister.persistUserMessage(...))` stub, and any `verify(taskFileRepository, ...).markInjected(...)` lines.

    4. UPDATE constructor invocation of `new DefaultChatServiceImpl(...)` (or `@InjectMocks`) to match the post-Task-1 constructor signature (one fewer parameter).

    5. KEEP existing imports for `Mockito`, `JUnit Jupiter`, `reactor.core.publisher.Flux`, `Mockito.when`, etc. ADD `import com.vn.agent.taskfile.AiTaskFileMediaResolver.Resolved;` if not present.

    6. RUN the test class.

    Implements research-cited rewrite of the stream-fallback regression suite for the new contract.
  </action>
  <verify>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/test/java/com/vn/agent/DefaultChatServiceImplStreamFallbackTest.java' -Pattern 'resolveActive|budgetExceeded').Count -ge 4 -and (Select-String -Path 'ai-agent/ai-agent/src/test/java/com/vn/agent/DefaultChatServiceImplStreamFallbackTest.java' -Pattern 'markInjected|UserMessagePersister').Count -eq 0"</automated>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.DefaultChatServiceImplStreamFallbackTest"</automated>
  </verify>
  <done>
    Stream-fallback test compiles, runs green with the 4 cases, and contains zero references to the removed `markInjected` / `UserMessagePersister` surfaces.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Chat-memory projection wipe (saveAll) | Transaction-bounded delete-recreate over `AI_AGENT_MESSAGE` for one conversation; runs on user request thread under user identity. |
| Spring AI `JdbcChatMemoryRepository` | Separate `SPRING_AI_CHAT_MEMORY` table — NOTICE rows are structurally invisible (D-A2). |
| Streaming subscriber (caller's UI thread) | Receives `budgetExceeded` flag for toast display; flag value comes from the resolver's own audit-controlled output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.1-09 | Information Disclosure | NOTICE row leaks into LLM prompt | mitigate | Two layers: (1) JPQL `m.role <> :noticeRole` keeps NOTICE rows from being projected into `SPRING_AI_CHAT_MEMORY` via the wipe-recreate path; (2) Spring AI's `JdbcChatMemoryRepository` reads its own table that NOTICE rows never touch (D-A2 — NOTICE rows live only in `AI_AGENT_MESSAGE`). NoticeFilterTest (Plan 13.1-06) is the regression guard. |
| T-13.1-10 | Information Disclosure | Per-turn-all `Media` re-injection sends previously-attached files to the LLM on every turn — operator may not realise | accept (documented) | Lifecycle TTL caps liability to 24h by default; operator can set per-conversation lifetime via cleanup TTL. NOTICE row in the visible chat ledger reminds the user which files are still in scope. Documented in SPEC §3 Background. |
| T-13.1-11 | Tampering | Streaming-fallback path could re-resolve and re-emit Media (BLK-01 regression) | mitigate | `executeBlockingTurn` is the single fallback target; signature audit + Test B verify exactly one `resolveActive` call across the failed-stream + fallback sequence. |
| T-13.1-12 | Repudiation | Deletion of `UserMessagePersister` removes an attestation surface | accept | The chat-memory advisor's own `saveAll` projection persists the user message (Spring AI standard path). Audit of the user message remains via `AiAuditEvent.eventName=chat_user_message` (existing). |
</threat_model>

<verification>
- `./gradlew :ai-agent:ai-agent:test --tests "DefaultChatServiceImplStreamFallbackTest"` → all 4 tests pass.
- Static-grep `taskFileRepository.markInjected\|UserMessagePersister\|resolvePending` against `ai-agent/ai-agent/src/main/java/` returns zero matches after this plan.
- Plan 13.1-06 `NoticeFilterTest` is the broader regression gate.
</verification>

<success_criteria>
1. `ProjectingChatMemoryRepository.saveAll` JPQL excludes NOTICE rows; `deleteByConversationId` is unchanged.
2. `DefaultChatServiceImpl` references only `resolveActive` (twice — blocking + streaming) and never `resolvePending`, `markInjected`, or `userMessagePersister`.
3. `executeBlockingTurn` helper signature is `(...)` minus `userMessageIdAlreadyPersisted` — BLK-01 contract preserved.
4. `ChatResponseDto.budgetExceeded` field exists and is set from `Resolved.budgetExceeded()`.
5. `UserMessagePersister.java` and any companion test are deleted.
6. `DefaultChatServiceImplStreamFallbackTest` rewritten with 4 cases, all green.
7. `./gradlew :ai-agent:ai-agent:compileJava` and `./gradlew :ai-agent:ai-agent:compileTestJava` pass.
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-03-SUMMARY.md` documenting:
- Exact ProjectingChatMemoryRepository diff
- DefaultChatServiceImpl constructor diff (parameter count delta)
- Final `executeBlockingTurn` signature
- `ChatResponseDto` field added
- File deletions: UserMessagePersister.java + (if existed) UserMessagePersisterTest.java
- 4 test case names with one-line descriptions
</output>
