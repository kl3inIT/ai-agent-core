# Phase 13.1: Chat Attachments — CRM-Style Right-Pane + Persistent Multi-Turn Context — Pattern Map

**Mapped:** 2026-05-07
**Files analyzed:** 19 (2 new + 17 modified)
**Analogs found:** 19 / 19 (every file has either a Phase 13 in-repo ancestor or a CRM verbatim port source)
**Parent phase:** `13-PATTERNS.md` (every Phase 13 file referenced below already shipped 2026-05-06)

---

## File Classification

| File | New / Modified | Role | Data Flow | Closest Analog | Match Quality |
|------|----------------|------|-----------|----------------|---------------|
| `D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/fragment/chat-panel-fragment.xml` | MODIFY | view-xml | UI-layout | `D:/DTH/jmix-crm/src/main/resources/com/company/crm/ai/view/aiconversation/ai-conversation-detail-view.xml` (lines 27, 41, 51) | exact (verbatim port + slot rename) |
| `D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java` | MODIFY | view-controller | event-driven + CRUD | self (Phase 13 — `handleUploadedFile` line 731, `setConversationIdInternal` line 347, chip-strip wiring lines 211–233) + `D:/DTH/jmix-crm/src/main/java/com/company/crm/ai/view/aiconversation/AiConversationDetailView.java` (loader + post-load wiring) | exact (extension + chip-strip removal) |
| `D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/fragment/ai-task-file-card-fragment.xml` | NEW | fragment-xml | UI-layout | `D:/DTH/jmix-crm/src/main/resources/com/company/crm/ai/view/aiconversation/ai-attachment-card-fragment.xml` (lines 1–44) | exact (verbatim port + 3 adaptations) |
| `D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/AiTaskFileCardFragmentRenderer.java` | NEW | view-controller (renderer) | event-driven | `D:/DTH/jmix-crm/src/main/java/com/company/crm/ai/view/aiconversation/AiAttachmentCardFragmentRenderer.java` (lines 1–128) | exact (verbatim port + 4 adaptations) |
| `D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/java/com/vn/agent/taskfile/AiTaskFileMediaResolver.java` | MODIFY | resolver / service | request-response (read) | self (Phase 13 — `resolvePending` lines 101–119, `buildMedia` 121–131, MIME map 46–78) | exact (extension — replace JPQL + add cap loop + audit) |
| `D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/java/com/vn/agent/taskfile/AiTaskFileRepository.java` | MODIFY | repository | CRUD | self (Phase 13 — `markInjected` lines 117–150) | exact (delete method) |
| `D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/java/com/vn/agent/taskfile/AiTaskFileProperties.java` | MODIFY | config | binding | self (Phase 13 — `Duration ttl` lines 32–51) + `tools/mutation/AiAgentMutationProperties.java` (multi-knob shape) | exact (add fields + flip ttl→ttlSeconds) |
| `D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/java/com/vn/agent/taskfile/AiTaskFileCleanupJob.java` | MODIFY | scheduled-job | batch | self (Phase 13 — `@Scheduled` `deleteExpiredTaskFiles` lines 39–45) | exact (extension — sentinel skip) |
| `D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/ProjectingChatMemoryRepository.java` | MODIFY | service / decorator | request-response (write) | self (Phase 4/8 — `saveAll` delete-recreate lines 76–111) | exact (1-line JPQL predicate) |
| `D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiMessageRole.java` | MODIFY | enum | data-shape | self (Phase 1 — enum entries lines 7–12) | exact (+1 entry) |
| `D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiTaskFile.java` | MODIFY | entity | persistence | self (Phase 13) | exact (field deletion) |
| `D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/java/com/vn/agent/DefaultChatServiceImpl.java` | MODIFY | service | streaming + request-response | self (Phase 13 — `resolvePending` + `markInjected` call sites; `auditDenial` audit pattern lines 763–771 / 781) | exact (replace + simplify) |
| `D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/java/com/vn/agent/UserMessagePersister.java` | DELETE (likely) | service | request-response | self (Phase 13 — vestigial after per-turn-all) | n/a |
| `D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/100-ai-task-file-drop-dead-columns.xml` | NEW | liquibase | schema-migration | `D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/090-ai-task-file.xml` (lines 16, 58, 87 — table name + index name conventions) | role-match (no in-repo `dropColumn` precedent yet) |
| `D:/DTH/ai-agent-core/jmix-app/src/main/resources/application.properties` | MODIFY | config / properties | binding | self (Phase 13 — `ai-agent.task-file.ttl=PT1H` line 87) | exact (extension) |
| `D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties` | MODIFY | message-bundle | i18n | self (Phase 13 — `chatView.attachments.button` block) | exact (extension) |
| `D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties` | MODIFY | message-bundle | i18n | self (Phase 13) | exact (extension) |
| `D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/resources/META-INF/resources/frontend/styles/ai-agent-chat.css` | MODIFY (append) | stylesheet | UI-styling | `D:/DTH/jmix-crm/src/main/frontend/themes/crm/view/ai-conversation-detail-view.css` (lines 1–110) | exact (verbatim port + selector rename) |
| `D:/DTH/ai-agent-core/ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/*.java` (TEST-18, BudgetCapTest, TtlConfigTest, NoticeFilterTest, LiquibaseSchemaTest, LocaleParityTest, CrmStyleLayoutTest, NoticeRenderTest, SurfaceMountingTest) | NEW (×9) | test | integration | self (Phase 13 — `AiTaskFileMediaResolverIntegrationTest`, `AiTaskFileCleanupJobTest`, `AiTaskFileNoVectorStoreInvocationTest`) + `BuiltInMutationToolsAuditArgumentsTest` `@SpringBootTest` shape | role-match (Phase 13 shape) |

---

## Pattern Assignments

### `chat-panel-fragment.xml` (view-xml, UI-layout) — RESHAPE

**Primary analog (verbatim port source):** `D:/DTH/jmix-crm/src/main/resources/com/company/crm/ai/view/aiconversation/ai-conversation-detail-view.xml`
**Self analog (preserve):** `D:/DTH/ai-agent-core/.../chat-panel-fragment.xml` titleBar + messageListSlot + messageInputSlot blocks (lines 23–46 / 59–82 — keep, drop chip-strip + attachRow)

**Split + right-pane structure (CRM verbatim, lines 27, 41, 51):**
```xml
<split id="conversationSplit" orientation="HORIZONTAL"
       width="100%" height="100%" splitterPosition="68">
    <vbox id="chatPanel" .../>           <!-- LEFT — chat -->
    <vbox id="attachmentsPanel" width="100%" height="100%"
          spacing="false" padding="true">    <!-- RIGHT — attachments (slot id retained per REQ-7) -->
        <h3 id="attachmentsTitle"
            text="msg://com.company.crm.ai.view.aiconversation/attachmentsPanelTitle"/>
        <gridLayout id="attachmentsGridLayout" .../>
        <upload id="attachmentUpload" classNames="attachments-dropzone"
                dropAllowed="true" .../>
    </vbox>
</split>
```

**Data section to add (collection container + loader for taskFilesDc):**
```xml
<data>
    <collection id="taskFilesDc" class="com.vn.agent.entity.AiTaskFile">
        <fetchPlan extends="_base"/>
        <loader id="taskFilesDl">
            <query><![CDATA[
                select e from ai_AiTaskFile e
                where e.conversation.id = :conversationId
                order by e.createdDate desc
            ]]></query>
        </loader>
    </collection>
</data>
```

**Removed (Phase 13 chip-strip surface in current file):**
- Lines 48–57 (`<vbox id="attachmentsPanel" classNames="ai-agent-chat-panel__attachments" visible="false"/>` — converted to right-pane vbox; classNames retired)
- Lines 62–81 inside `messageInputSlot` (`<hbox id="attachRow">` + `<button id="attachButton" icon="PAPERCLIP">` + `<upload id="upload" .../>`)

**Pitfalls:**
- Slot id `attachmentsPanel` MUST be retained on the new right-pane `<vbox>` (REQ-7 / Pitfall 6).
- Drop the `acceptedFileTypes/maxFiles/maxFileSize` from old `id="upload"` → carry verbatim onto new `id="taskFileUpload"`. MIME allowlist string is locked from Phase 13.
- Do NOT add `receiverType=` declaratively — Java-side `UploadHandler.toFile` continues per memory `feedback_jmix_upload_receiver_deprecated`.
- Title `msg://...` key path becomes the agent-core `msg:///chatView.attachments.title`; the CRM bundle path is illustrative only.

---

### `ai-task-file-card-fragment.xml` (NEW, fragment-xml)

**Verbatim port source:** `D:/DTH/jmix-crm/src/main/resources/com/company/crm/ai/view/aiconversation/ai-attachment-card-fragment.xml` (lines 1–44 — full file).

**Verbatim card shell (lines 8–21 of CRM):**
```xml
<card id="taskFileCard" width="100%" themeNames="outlined">
    <headerPrefix>
        <icon id="attachmentIcon" icon="FILE_O" size="S"/>
    </headerPrefix>
    <header>
        <h5 id="titleValue" dataContainer="taskFileDc" property="filename"/>
    </header>
    <headerSuffix>
        <hbox spacing="false" alignItems="CENTER">
            <button id="openButton"  icon="DOWNLOAD"
                    themeNames="tertiary-inline small icon"/>
            <button id="deleteButton" icon="TRASH"
                    themeNames="tertiary-inline small icon"/>
        </hbox>
    </headerSuffix>
```

**Adaptations from CRM (locked by 13.1-CONTEXT D-B1 + UI-SPEC):**
1. CRM `<instance class="...AiConversationAttachment">` → `<instance class="com.vn.agent.entity.AiTaskFile">`
2. `<header>` `<h5 property="title">` → `<h5 property="filename">` (entity has no `title` field)
3. `<headerSuffix>` adds a SECOND button `id="deleteButton" icon="TRASH"` (CRM has only `openButton`)
4. `<headerSuffix>` `id="openButton" icon="EXTERNAL_LINK"` → `icon="DOWNLOAD"` (Phase 13.1 is download-only; no inline preview)
5. CRM content `sourceIcon` + `sourceValue` (AI vs USER provenance) is REPLACED with a single `id="sizeValue"` span (programmatically populated by renderer with humanized size)

**Verbatim content meta hbox (CRM lines 22–41 with adaptation 5):**
```xml
<content>
    <hbox id="metaLayout" spacing="true" alignItems="CENTER"
          classNames="attachment-card-meta">
        <span id="timestampValue" dataContainer="taskFileDc"
              property="createdDate" classNames="text-secondary text-s"/>
        <span id="sizeSeparator" text="|"
              classNames="text-secondary text-s attachment-card-meta-separator"/>
        <span id="sizeValue" classNames="text-secondary text-s"/>
    </hbox>
</content>
```

**Pitfalls:**
- CSS class names (`attachment-card-meta`, `attachment-card-meta-separator`) MUST be preserved verbatim — the CSS append in `ai-agent-chat.css` selects on these.
- `<data readOnly="true"><instance id="taskFileDc"/></data>` MUST use the `taskFileDc` id — `@RendererItemContainer("taskFileDc")` on the renderer binds by exact name.

---

### `AiTaskFileCardFragmentRenderer.java` (NEW, view-controller / renderer)

**Verbatim port source:** `D:/DTH/jmix-crm/src/main/java/com/company/crm/ai/view/aiconversation/AiAttachmentCardFragmentRenderer.java` (lines 1–128 — full file).

**Class skeleton (CRM lines 24–45 verbatim, package + parameterized type swapped):**
```java
@FragmentDescriptor("ai-task-file-card-fragment.xml")
@RendererItemContainer("taskFileDc")
public class AiTaskFileCardFragmentRenderer
        extends FragmentRenderer<JmixCard, AiTaskFile> {

    @ViewComponent private H5 titleValue;
    @ViewComponent private Span sizeValue;
    @ViewComponent private Icon attachmentIcon;
    @ViewComponent private JmixButton openButton;
    @ViewComponent private JmixButton deleteButton;
    @ViewComponent private MessageBundle messageBundle;
    @ViewComponent private InstanceContainer<AiTaskFile> taskFileDc;

    @Autowired private Downloader downloader;
    @Autowired private DataManager dataManager;
    @Autowired private FileStorageLocator fileStorageLocator;
    @Autowired private Dialogs dialogs;
}
```

**Verbatim ready + item-change subscribers (CRM lines 46–55):**
```java
@Subscribe
public void onReady(final ReadyEvent event) {
    openButton.setAriaLabel(messageBundle.getMessage("chatView.attachments.action.download"));
    deleteButton.setAriaLabel(messageBundle.getMessage("chatView.attachments.action.delete"));
    applyState(taskFileDc.getItemOrNull());
}

@Subscribe(id = "taskFileDc", target = Target.DATA_CONTAINER)
public void onItemChange(final InstanceContainer.ItemChangeEvent<AiTaskFile> event) {
    applyState(event.getItem());
}
```

**Open action (CRM lines 57–65 verbatim, swap `attachment.getFile()` → `row.getStorageRef()`):**
```java
@Subscribe("openButton")
public void onOpenButtonClick(final ClickEvent<JmixButton> event) {
    AiTaskFile row = taskFileDc.getItemOrNull();
    if (row == null || row.getStorageRef() == null) return;
    downloader.setShowNewWindow(true);
    downloader.download(row.getStorageRef());
}
```

**Adaptations from CRM (4 — locked by 13.1-CONTEXT D-B1 / D-B2):**
1. Generic param `AiConversationAttachment` → `AiTaskFile`
2. `@RendererItemContainer("attachmentDc")` → `("taskFileDc")`
3. New `deleteButton` action with `Dialogs.createOptionDialog().withType(WARNING)` confirmation (CRM has no delete) — pattern below
4. `applyState(...)` populates `sizeValue` from `humanizeSize(row.getSizeBytes())` (replaces CRM `applySourceState` AI-vs-USER provenance switch)

**Delete action (NEW — no CRM analog; pattern from 13.1-RESEARCH lines 354–392):**
```java
@Subscribe("deleteButton")
public void onDeleteButtonClick(final ClickEvent<JmixButton> event) {
    AiTaskFile row = taskFileDc.getItemOrNull();
    if (row == null) return;
    dialogs.createOptionDialog()
            .withHeader(messageBundle.getMessage("chatView.attachments.deleteConfirm.title"))
            .withText(messageBundle.formatMessage("chatView.attachments.deleteConfirm.message",
                    row.getFilename()))
            .withActions(
                    new DialogAction(DialogAction.Type.YES)
                            .withVariant(ActionVariant.DANGER)
                            .withText(messageBundle.getMessage("chatView.attachments.deleteConfirm.confirm"))
                            .withHandler(e -> performDelete(row)),
                    new DialogAction(DialogAction.Type.NO)
                            .withText(messageBundle.getMessage("chatView.attachments.deleteConfirm.cancel")))
            .open();
}

private void performDelete(AiTaskFile row) {
    FileRef ref = row.getStorageRef();
    try { dataManager.remove(row); }
    catch (RuntimeException ex) { log.warn("...", ex); return; }
    if (ref != null) {
        try { fileStorageLocator.getByName(ref.getStorageName()).removeFile(ref); }
        catch (RuntimeException blobEx) { log.warn("...", blobEx); }  // log-and-continue per CONTEXT
    }
}
```

**Per-extension icon switch (CRM lines 108–115 verbatim + image case):**
```java
private VaadinIcon resolveIcon(String fileName) {
    String ext = fileExtension(fileName);
    return switch (ext) {
        case "csv", "xlsx", "xls" -> VaadinIcon.TABLE;
        case "pdf", "html", "htm", "md", "txt" -> VaadinIcon.FILE_TEXT_O;
        case "png", "jpg", "jpeg", "gif", "webp" -> VaadinIcon.FILE_PICTURE;  // agent-core only
        default -> VaadinIcon.FILE_O;
    };
}
```

**`fileExtension` helper (CRM lines 117–127 verbatim — no change).**

**Pitfalls:**
- Use `DataManager` (NOT `UnconstrainedDataManager`) — delete must respect user row policy `userUsername = :current_user_username`.
- `fileStorage.removeFile(...)` is best-effort log-and-continue (orphan blob is swept by `AiTaskFileCleanupJob`).
- `dataManager.remove(row)` on a `taskFilesDc`-bound entity auto-fires `CollectionContainer` change events → grid refreshes without explicit `taskFilesDl.load()`. If empty-state visibility doesn't auto-toggle, `ChatPanelFragment.refreshTaskFiles()` runs from `taskFilesDl.PostLoadEvent` (Pitfall 5).

---

### `ChatPanelFragment.java` (MODIFY, view-controller)

**Self-analogs (preserve / extend):**
- Lines 89–115 — `@ViewComponent` field block (extend with new fields)
- Lines 211–233 — chip-strip wiring in `onReady` (REMOVE)
- Lines 245–270 — `UploadHandler.toFile` Java-side registration (PRESERVE — only id ref changes)
- Lines 347–390 — `setConversationIdInternal` history-replay loop (EXTEND with NOTICE arm)
- Lines 731–800 — `handleUploadedFile` (EXTEND with NOTICE `AiMessage` insert after `dataManager.save(row)` at line 781)

**New `@ViewComponent` fields to add (beside line 97 `attachmentsPanel` block — keep that field unchanged per REQ-7):**
```java
// Phase 13.1 UI-01 — right-pane data container + loader for the card grid.
@ViewComponent private CollectionContainer<AiTaskFile> taskFilesDc;
@ViewComponent private CollectionLoader<AiTaskFile> taskFilesDl;
@ViewComponent private VerticalLayout attachmentsEmptyState;
@ViewComponent private GridLayout attachmentsGridLayout;
// Existing field on line 98 renames id binding "upload" → "taskFileUpload" (XML-driven).
@ViewComponent private JmixUpload taskFileUpload;
```

**Remove existing field (line 99):**
```java
@ViewComponent private JmixButton attachButton;     // REMOVE — no separate paperclip button
```

**Existing field at line 97 stays (REQ-7 zero-diff for slot id contract):**
```java
@ViewComponent private VerticalLayout attachmentsPanel;   // KEEP — XML rebinds to right-pane vbox
```

**Loader-bind pattern (NEW — pattern source: `setConversationIdInternal` line 369–374 history-load):**
```java
private void setConversationIdInternal(UUID cid) {
    // ... existing code (line 347+) ...
    taskFilesDl.setParameter("conversationId", cid);
    taskFilesDl.load();
    refreshTaskFiles();
}
```

**Post-load handler pattern (NEW — pattern source: memory `feedback_jmix_data_loader_events`):**
```java
@Subscribe(id = "taskFilesDl", target = Target.DATA_LOADER)
public void onTaskFilesPostLoad(final CollectionLoader.PostLoadEvent<AiTaskFile> event) {
    refreshTaskFiles();
}

private void refreshTaskFiles() {
    boolean empty = taskFilesDc.getItems().isEmpty();
    attachmentsEmptyState.setVisible(empty);
    attachmentsGridLayout.setVisible(!empty);
}
```

**NOTICE row insert in `handleUploadedFile` (insert AFTER existing `dataManager.save(row)` at line 781, BEFORE the success notification):**
```java
// Phase 13.1 UX-01 — bilingual NOTICE row, log-and-continue per CONTEXT integration-points.
try {
    AiMessage notice = metadataApi.create(AiMessage.class);
    notice.setConversation(conversation);                 // reuse the same `conversation` ref loaded above
    notice.setRole(AiMessageRole.NOTICE);
    notice.setContent(messages.formatMessage(
            "chatView.attachments.notice",
            currentAuthentication.getUser().getUsername(), fileName));
    notice.setCreatedDate(OffsetDateTime.now());
    notice.setSeq(nextSeq(conversation.getId()));
    dataManager.save(notice);
} catch (RuntimeException noticeEx) {
    log.warn("Failed to write NOTICE AiMessage for upload {}", fileName, noticeEx);
}
taskFilesDl.load();   // refresh the right-pane grid
```

**NOTICE rendering arm in history loop (in `setConversationIdInternal` near existing line 379 `if (role == AiMessageRole.USER)` arm):**

Two options per 13.1-RESEARCH Pitfall 7 (plan-phase decides):
- **Option A (recommended):** Migrate `messageListSlot` from `MessageList` to `VerticalLayout` of mixed bubbles (`MessageBubbleComponent`) + `vaadin-message.attachment-event` element for NOTICE rows.
- **Option B (smaller diff):** Keep `MessageList`; render NOTICE as a sibling `vaadin-message` element added directly to `messageListSlot` (parent already classed `.ai-agent-chat-panel__messages` per current XML line 46).

**Pitfalls:**
- Line 97 field type MUST stay `VerticalLayout` (REQ-7 / Pitfall 6).
- Re-load `conversation` from `dataManager` BEFORE the NOTICE insert if `ensureConversationIdForUpload` was called this turn (Pitfall 2).
- Use `Messages` + `messages.formatMessage(...)` — NOT Spring `MessageSource` (memory `feedback_jmix_messages_over_spring`). Already injected at line 105.
- `metadataApi.create(AiMessage.class)` (NOT `new AiMessage()`) — CLAUDE.md rule.

---

### `AiTaskFileMediaResolver.java` (MODIFY, resolver)

**Self-analog:** existing `resolvePending` (lines 101–119), `buildMedia` (121–131), MIME map (46–78), helpers (133–189). Preserve all helpers + MIME map verbatim.

**Replace `resolvePending` with `resolveActive` (RES-01 + AUDIT-01):**
```java
public Resolved resolveActive(UUID conversationId) {
    if (conversationId == null) return Resolved.empty();
    List<AiTaskFile> rows = dataManager.load(AiTaskFile.class)
            .query("select e from ai_AiTaskFile e " +
                    "where e.conversation.id = :cid and e.expiresAt > :now " +
                    "order by e.createdDate desc")          // DESC for LRU eviction
            .parameter("cid", conversationId)
            .parameter("now", OffsetDateTime.now())
            .list();
    if (rows.isEmpty()) return Resolved.empty();

    int maxFiles  = taskFileProperties.getPerTurnMaxFiles();         // -1 disables file cap
    long maxBytes = taskFileProperties.getPerTurnMaxTotalBytes();    // -1 disables byte cap

    List<AiTaskFile> kept = new ArrayList<>();
    long keptBytes = 0, droppedBytes = 0, totalBytes = 0;
    int  droppedRows = 0;
    for (AiTaskFile row : rows) {
        long size = row.getSizeBytes() == null ? 0L : row.getSizeBytes();
        totalBytes += size;
        boolean fileCapHit = (maxFiles >= 0) && (kept.size() >= maxFiles);
        boolean byteCapHit = (maxBytes >= 0) && (keptBytes + size > maxBytes);
        if (fileCapHit || byteCapHit) { droppedRows++; droppedBytes += size; continue; }
        kept.add(row); keptBytes += size;
    }

    boolean budgetExceeded = droppedRows > 0;
    if (budgetExceeded) {
        try {
            auditWriter.writeToolCall(
                    /*parentId*/ null,
                    /*runId*/    runContextRunIdOrNew(),
                    /*userUsername*/ currentUsername(),
                    /*conversationId*/ conversationId,
                    "task_file_budget_exceeded",
                    buildBudgetArgumentsJson(conversationId, rows.size(), totalBytes,
                            kept.size(), keptBytes, droppedRows, droppedBytes,
                            maxFiles, maxBytes),
                    /*resultSummary*/ null, 0L,
                    AiToolCallOutcome.SUCCESS,
                    /*denialReason*/ null, /*errorClass*/ null);
        } catch (RuntimeException auditEx) {
            log.warn("Failed to write task_file_budget_exceeded audit row for conv={}",
                    conversationId, auditEx);
        }
    }

    List<Media> media = kept.stream().map(this::buildMedia).toList();
    return new Resolved(media, budgetExceeded);
}

public record Resolved(List<Media> media, boolean budgetExceeded) {
    public static Resolved empty() { return new Resolved(List.of(), false); }
    public boolean isEmpty()       { return media.isEmpty(); }
}
```

**Audit reference call site (precedent):** `DefaultChatServiceImpl.java:763–771`
```java
auditWriter.writeToolCall(RunContext.getRootAuditId(), runId, userUsername, convId,
        GuardedToolCallingManager.CHAT_SENTINEL_TOOL_NAME,
        /*argumentsJson*/ null, /*resultSummary*/ null, 0L,
        AiToolCallOutcome.BLOCKED, denialKey, /*errorClass*/ null);
```

**Pitfalls:**
- Use `DataManager` (NOT `UnconstrainedDataManager`) — user row-level policy `userUsername = :current_user_username` MUST apply (PATTERN 13 RES-01 + memory note).
- DESC ordering is load-bearing: oldest rows are dropped first because the loop walks newest-first (LRU).
- `RunContext.get()` may be null when called from tests/admin — fall back to `UUID.randomUUID()` (Pitfall 3).
- New constructor injection needed: `AuditWriter auditWriter`, `AiTaskFileProperties taskFileProperties`, `CurrentAuthentication currentAuthentication`. Drop `taskFileIds` field from `Resolved` record entirely.
- Forbidden imports unchanged: NO `IngesterManager`, `VectorStore`, `RetrievalAugmentationAdvisor`, `TokenTextSplitter`, `DocumentReader` (TEST-16 invariant).

---

### `AiTaskFileRepository.java` (MODIFY, repository — delete `markInjected`)

**Self-analog:** existing `markInjected` (lines 117–150), `loadExpired` (157–162), `deleteRow` (174–187), `deleteAllExpired` (197–209) — keep the latter three verbatim.

**Action:**
- DELETE method `markInjected(...)` lines 117–150 entirely.
- DELETE method `loadPending(...)` lines 85–97 (resolver no longer delegates to repo for read; resolver loads directly).
- KEEP `loadExpired`, `deleteRow`, `deleteAllExpired`.
- KEEP `agentstoreRequiresNew` template (used by `deleteAllExpired`).
- DROP unused imports: `AiMessage`, `Optional` (if no longer referenced after delete).

**Pitfalls:**
- Constructor remains the same signature (still needs `dataManager`, `unconstrainedDataManager`, `fileStorageLocator`, `agentstoreTransactionManager`).
- Compile-time check (per SPEC §6 acceptance): `grep -r "markInjected\|getInjectedAt\|setInjectedAt\|getMessage()\|setMessage(" ai-agent/.../taskfile ai-agent/.../entity/AiTaskFile.java` returns zero matches.

---

### `AiTaskFileProperties.java` (MODIFY, config)

**Self-analog:** existing `Duration ttl` field (line 32), getters/setters (36–50). Replace with `long ttlSeconds` (per 13.1-RESEARCH Pitfall 1 — Option B locked).

**Target shape:**
```java
@ConfigurationProperties(prefix = "ai-agent.task-file")
public class AiTaskFileProperties {

    /** TTL in seconds. Default 86400 (24h). Sentinel `-1` = no TTL (skip cleanup-job purge). */
    private long ttlSeconds = 86_400L;

    private long maxFileSizeBytes = 104_857_600L;     // unchanged

    /** Per-turn cap on file count. Default 10. Sentinel `-1` disables. */
    private int perTurnMaxFiles = 10;

    /** Per-turn cap on total bytes. Default 50 MB. Sentinel `-1` disables. */
    private long perTurnMaxTotalBytes = 52_428_800L;

    // standard getters/setters
}
```

**Pitfalls:**
- Property key flips: `ai-agent.task-file.ttl=PT1H` → `ai-agent.task-file.ttl-seconds=86400`.
- 3 existing test files reference the old key (`AiTaskFileCleanupJobTest`, `AiTaskFileMediaResolverIntegrationTest`, `AiTaskFileNoVectorStoreInvocationTest`) — must update with the rename.

---

### `AiTaskFileCleanupJob.java` (MODIFY, scheduled-job)

**Self-analog:** existing `deleteExpiredTaskFiles` (lines 39–45). Add sentinel skip path.

**Target shape (extend, do not rewrite):**
```java
@Scheduled(cron = "0 0 * * * *")
public void deleteExpiredTaskFiles() {
    long ttl = taskFileProperties.getTtlSeconds();
    if (ttl == -1L) {
        if (sentinelLoggedOnce.compareAndSet(false, true)) {
            log.info("AiTaskFile TTL disabled (ttl-seconds=-1); cleanup job skipping TTL purge");
        }
        return;
    }
    int removed = repository.deleteAllExpired(OffsetDateTime.now());
    if (removed > 0) log.debug("Removed {} expired AiTaskFile rows", removed);
}
```

**New constructor field:** inject `AiTaskFileProperties taskFileProperties`. Add `private final AtomicBoolean sentinelLoggedOnce = new AtomicBoolean(false);` for once-only INFO log.

**Pitfalls:**
- FK cascade on `AI_TASK_FILE.CONVERSATION_ID` (`deleteCascade="true"` from `090-ai-task-file.xml:28`) handles conversation-delete path independently — sentinel mode does NOT break that.
- Apply same `-1` skip in `DefaultChatServiceImpl` opportunistic per-turn purge if that path exists in current code (verify in plan-phase).

---

### `ProjectingChatMemoryRepository.java` (MODIFY, decorator) — D-A1 1-line JPQL fix

**Self-analog:** existing `saveAll` delete-recreate at lines 76–111. Modify the load query at lines 85–89 only.

**Current (lines 85–89):**
```java
dataManager.load(AiMessage.class)
        .query("select m from ai_AiMessage m where m.conversation.id = :cid")
        .parameter("cid", convUuid)
        .list()
        .forEach(dataManager::remove);
```

**Target (D-A1 — add predicate `m.role <> :noticeRole`):**
```java
dataManager.load(AiMessage.class)
        .query("select m from ai_AiMessage m where m.conversation.id = :cid " +
               "and m.role <> :noticeRole")
        .parameter("cid", convUuid)
        .parameter("noticeRole", AiMessageRole.NOTICE.getId())   // "NOTICE"
        .list()
        .forEach(dataManager::remove);
```

**Pitfalls:**
- DO NOT also modify `deleteByConversationId` (lines 113–123) — when the entire conversation is deleted, NOTICE rows MUST go too. Conversation FK cascade does the work; the explicit query path here is the no-op double-belt that runs if Spring AI calls it.
- Spring AI's `findByConversationId` (line 70–71) reads SPRING_AI_CHAT_MEMORY (separate table). NOTICE rows are structurally invisible to LLM-prompt path (D-A2). NoticeFilterTest is a regression guard.

---

### `AiMessageRole.java` (MODIFY, enum) — +1 entry

**Self-analog:** existing enum entries lines 7–12. Append `NOTICE("NOTICE")`.

**Target (append before `;`):**
```java
public enum AiMessageRole implements EnumClass<String> {
    USER("USER"),
    ASSISTANT("ASSISTANT"),
    SYSTEM("SYSTEM"),
    TOOL("TOOL"),
    NOTICE("NOTICE");                  // Phase 13.1 UX-01
    // ... id field, getId, fromId unchanged ...
}
```

**Pitfalls:**
- `AiMessage.ROLE_` column is `varchar(16)` — `"NOTICE"` (6 chars) fits. NO Liquibase change.
- Add locale entries: `com.vn.agent.entity/AiMessageRole.NOTICE=Notice` (en) and `=Thông báo` (vi).
- `fromId(...)` (line 27) auto-discovers the new value — no method change.

---

### `AiTaskFile.java` (MODIFY, entity) — drop `message` + `injectedAt`

**Self-analog:** Phase 13 `AiTaskFile.java` (the entity already in repo).

**Action:**
- Delete field `private AiMessage message;` and its `@ManyToOne(...)` + `@OnDelete(...)` annotations + getters/setters.
- Delete field `private OffsetDateTime injectedAt;` + getters/setters.
- Remove `IDX_AI_TASK_FILE__ON_MESSAGE` and `IDX_AI_TASK_FILE__INJECTED_AT` from any `@Table(indexes={...})` annotation if present.

**Pitfalls (CRITICAL):**
- ORDER OF MERGE: Liquibase 100 MUST run BEFORE EclipseLink validates the metamodel at boot. Both must land in the SAME commit so the schema and entity stay aligned (per 13.1-RESEARCH "Anti-Patterns to Avoid").
- KEEP all other fields (filename, contentType, sizeBytes, storageRef, conversation, userUsername, expiresAt, createdBy, createdDate, lastModifiedBy, lastModifiedDate, version, id).

---

### `DefaultChatServiceImpl.java` (MODIFY, service)

**Self-analog (Phase 13):** existing `resolvePending` + `markInjected` call sites in `ask` and `stream` paths; `executeBlockingTurn` helper (lines 338–431); `auditDenial` audit pattern (lines 763–771 / 781).

**Action:**
- Replace `resolvePending(...)` → `resolveActive(...)` everywhere it's called.
- REMOVE the `markInjected(...)` call from both `.ask` and `.stream` paths.
- REMOVE `UserMessagePersister` injection if no other caller uses it (likely DELETE the class — see "UserMessagePersister.java" entry).
- KEEP `executeBlockingTurn` helper (Pitfall 8) — DROP its `userMessageIdAlreadyPersisted` parameter.
- ADD budget-exceeded toast: when `Resolved.budgetExceeded`, call `notifications.create(messages.getMessage("chatView.attachments.budgetExceeded")).withType(WARNING).show()` (per 13.1-CONTEXT D-D1). Note: `Notifications` lives on the UI thread — toast may need to fire from `ChatPanelFragment.onSubmit` instead. Plan-phase locks the dispatch site.
- Streaming path: hoist `resolveActive(convId)` INSIDE `Flux.defer(...)` body so blob reads don't run on the calling thread before `.subscribe()` (Phase 13 PATTERNS Pitfall 1).

**Pitfalls:**
- Lambda form `.user(u -> { u.text(message); u.media(resolved.media().toArray(new Media[0])); })` is required — convenience overload `.user(String)` silently drops media (Phase 13 PATTERNS Pitfall 2).
- DropF the `markInjected` call sites entirely — failure to do so means `injectedAt` is set on rows whose column was just dropped → write fails.

---

### `100-ai-task-file-drop-dead-columns.xml` (NEW, liquibase)

**Pattern source:** `D:/DTH/ai-agent-core/.../090-ai-task-file.xml` (table-name + index-name + FK-name conventions).

**Auto-include:** parent `agentstore-changelog.xml:15` already has `<includeAll path=".../agentstore-changelog" errorIfMissingOrEmpty="false"/>` — no parent edit.

**Target file (verbatim from 13.1-RESEARCH Pattern 7):**
```xml
<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!-- Phase 13.1 SCHEMA-01: drop AI_TASK_FILE.MESSAGE_ID + INJECTED_AT
         and their FK + indexes. Per-turn-all resolver no longer needs them.
         Order: drop FK → drop indexes → drop columns. -->
    <changeSet id="1" author="ai-agent">
        <dropForeignKeyConstraint baseTableName="AI_TASK_FILE"
                constraintName="FK_AI_TASK_FILE__ON_MESSAGE"/>
    </changeSet>
    <changeSet id="2" author="ai-agent">
        <dropIndex indexName="IDX_AI_TASK_FILE__ON_MESSAGE" tableName="AI_TASK_FILE"/>
    </changeSet>
    <changeSet id="3" author="ai-agent">
        <dropIndex indexName="IDX_AI_TASK_FILE__INJECTED_AT" tableName="AI_TASK_FILE"/>
    </changeSet>
    <changeSet id="4" author="ai-agent">
        <dropColumn columnName="MESSAGE_ID" tableName="AI_TASK_FILE"/>
    </changeSet>
    <changeSet id="5" author="ai-agent">
        <dropColumn columnName="INJECTED_AT" tableName="AI_TASK_FILE"/>
    </changeSet>
</databaseChangeLog>
```

**Pitfalls:**
- DO NOT touch `FK_AI_TASK_FILE__ON_CONVERSATION` — its `deleteCascade="true"` is a Phase 13 invariant and must survive (Pitfall 10). Only the SET-NULL `FK_AI_TASK_FILE__ON_MESSAGE` is dropped.
- Order matters: FK dropped first, then indexes, then columns. Postgres permits dropping FK before column; reverse fails on some dialects.
- Liquibase `id` values 1–5 within this single changelog are scoped per-file — no collision risk with `090-ai-task-file.xml` `id="1..3"`.

---

### `application.properties` (MODIFY, config)

**Pattern source:** Phase 13 line 87 (`ai-agent.task-file.ttl=PT1H`). VERIFIED PATH: `D:/DTH/ai-agent-core/jmix-app/src/main/resources/application.properties` (NOT the `ai-agent` library module — Pitfall 9).

**Target keys (rename ttl → ttl-seconds; add 2 new caps):**
```properties
ai-agent.task-file.ttl-seconds=86400
ai-agent.task-file.per-turn-max-files=10
ai-agent.task-file.per-turn-max-total-bytes=52428800
# (max-file-size-bytes=104857600 unchanged from Phase 13)
```

**Remove:** old `ai-agent.task-file.ttl=PT1H` line.

**Pitfalls:**
- `spring.servlet.multipart.max-file-size=100MB` and `max-request-size=110MB` from Phase 13 are unchanged.
- Default chat model `qwen/qwen3.6-35b-a3b` is unchanged (Phase 13 lock).

---

### `messages.properties` + `messages_vi.properties` (MODIFY, message-bundle)

**Self-analog:** Phase 13 `chatView.attachments.button` block. NEW keys append below.

**Keys to add in BOTH bundles (per UI-SPEC § Copywriting Contract):**
```properties
chatView.attachments.title=Attachments                                    # vi: Tệp đính kèm
chatView.attachments.uploadText=Upload                                    # vi: Tải lên
chatView.attachments.dropLabel=Drop attachment here                       # vi: Thả tệp vào đây
chatView.attachments.emptyState=There's nothing here yet...               # vi: Chưa có tệp nào...
chatView.attachments.notice=[%s] added attachment "%s"                    # vi: [%s] đã đính kèm "%s"
chatView.attachments.budgetExceeded=Some attachments were not sent to the model (budget exceeded).  # vi: Một số tệp không được gửi cho mô hình (vượt giới hạn).
chatView.attachments.action.download=Download attachment                  # vi: Tải tệp về
chatView.attachments.action.delete=Remove attachment                      # vi: Xoá tệp đính kèm
chatView.attachments.deleteConfirm.title=Remove attachment                # vi: Xoá tệp đính kèm
chatView.attachments.deleteConfirm.message=Remove attachment "%s"? This will also remove it from the agent's context for future turns.   # vi: Xoá tệp "%s"? Tệp sẽ không còn trong ngữ cảnh của trợ lý cho các lượt tiếp theo.
chatView.attachments.deleteConfirm.confirm=Remove                          # vi: Xoá
chatView.attachments.deleteConfirm.cancel=Cancel                           # vi: Huỷ
com.vn.agent.entity/AiMessageRole.NOTICE=Notice                            # vi: Thông báo
```

**Pitfalls:**
- LocaleParityTest fails on any en key absent from vi.
- Phase 13 key `chatView.attachments.button` (and any other paperclip-era keys) RETAINED — cleanup deferred (CONTEXT out-of-scope).
- File name is `messages.properties` (en) — repo uses bare `messages` for the default locale.

---

### `ai-agent-chat.css` (MODIFY, append-only)

**Verbatim port source:** `D:/DTH/jmix-crm/src/main/frontend/themes/crm/view/ai-conversation-detail-view.css` (lines 1–110).

**Single rename:** CRM selector `.ai-conversation-message-list` → agent-core `.ai-agent-chat-panel__messages` (the existing class on `messageListSlot` per current `chat-panel-fragment.xml:46`).

**Append blocks (verbatim from CRM, classes preserved):** see 13.1-RESEARCH Pattern 6 (full rule list — `vaadin-message.attachment-event`, `attachments-dropzone`, `::part(drop-label)`, `::part(primary-buttons)`, `attachment-card-meta`, `attachment-card-meta-separator`).

**Pitfalls:**
- Class names MUST stay exact match to CRM source — the renderer / fragment XML rely on these.
- Append-only — do NOT modify the existing 34 lines of `ai-agent-chat.css`.

---

### Test files (NEW × 9, role-match analogs from Phase 13)

**Pattern sources for `@SpringBootTest` shape:**
- `D:/DTH/ai-agent-core/ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/AiTaskFileMediaResolverIntegrationTest.java` (closest analog — same resolver under test)
- `D:/DTH/ai-agent-core/ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/AiTaskFileCleanupJobTest.java` (closest analog for TtlConfigTest)
- `D:/DTH/ai-agent-core/ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/AiTaskFileNoVectorStoreInvocationTest.java` (TEST-16 invariant port for the widened file scope)
- `BuiltInMutationToolsAuditArgumentsTest.java` `@SpringBootTest` shape (lines 49–80) for budget-exceeded audit row assertion

**`@UiTest` analog for CrmStyleLayoutTest / NoticeRenderTest / SurfaceMountingTest:**
- `D:/DTH/ai-agent-core/ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/...` — locate Phase 12/13 `@UiTest` for chat surface (plan-phase confirms the exact file).

**Pitfalls:**
- Tests writing audit/intent rows: use `UnconstrainedDataManager.save(...)` (memory `feedback_jmix_unconstrained_for_system_writes`).
- `loadValue/loadValues` raw JPQL on agentstore needs `.store("agentstore")`. `dataManager.load(Class).query()` does NOT.
- `@AfterEach` cleanup of seeded rows for concurrent test runs.
- TEST-18 PerTurnMediaInjectionTest: 3 sequential turns × same xlsx → assert all 3 outbound `Prompt` user messages contain a `Media` whose bytes equal the original. Use `StubChatModelConfiguration` to capture prompts.

---

## Shared Patterns

### Authentication + Authorization
**Source:** `DataManager` (NOT `UnconstrainedDataManager`) for resolver + renderer reads/deletes.
**Apply to:** `AiTaskFileMediaResolver.resolveActive`, `AiTaskFileCardFragmentRenderer.performDelete`. Row-level policy `userUsername = :current_user_username` from `AiAgentUserRowLevelRole.taskFile()` filters cross-user reads structurally.
**System-internal exception:** `ProjectingChatMemoryRepository` continues to use `UnconstrainedDataManager` (chat-memory pruning is system-internal — see file-level JavaDoc lines 38–55).

### Audit Reuse (NO new AuditKind)
**Source:** `AuditWriter.writeToolCall(...)` — verified call site `DefaultChatServiceImpl.java:763–771`.
**Apply to:** `AiTaskFileMediaResolver.resolveActive` budget-exceeded path. Event name `task_file_budget_exceeded`. `argumentsJson` shape locked: `{conversationId, totalRows, totalBytes, keptRows, keptBytes, droppedRows, droppedBytes, perTurnMaxFiles, perTurnMaxTotalBytes}`. Outcome `SUCCESS`. Best-effort: log warn on audit failure, do not block resolver.

### TEST-16 No-VectorStore Constraint (widened scope)
**Source:** Phase 13 `package-info.java` forbidden-token list + `AiTaskFileNoVectorStoreInvocationTest`.
**Apply to:** every Phase 13.1-modified or 13.1-added file in `com.vn.agent.taskfile.**` AND the new `AiTaskFileCardFragmentRenderer.java` AND the reshaped `chat-panel-fragment.xml`. Forbidden imports / class references: `IngesterManager`, `VectorStore`, `RetrievalAugmentationAdvisor`, `TokenTextSplitter`, `DocumentReader`, anything from `com.vn.agent.rag.**`.

### Locale Parity (REQ-9)
**Source:** memory `feedback_jmix_messages_over_spring` + CLAUDE.md.
**Apply to:** every key added to `messages.properties` MUST land in `messages_vi.properties`. Inject `io.jmix.core.Messages` (NOT Spring `MessageSource`). Already wired in `ChatPanelFragment.java:105`.

### Phase 12 Fragment Contract (REQ-7 zero-diff)
**Source:** `ChatSurfaceMounter.java`, `AiUiSettingsService.java`, `AiUiSettings` entity, `ChatView.java`, `ChatDialogView.java`.
**Apply to:** ALL files. ZERO source modifications to these 5 files. `ChatPanelFragment.attachmentsPanel` field type stays `VerticalLayout`. Slot id `attachmentsPanel` retained on the new right-pane vbox.

### Upload Receiver Pattern (Vaadin 24.8 deprecation)
**Source:** memory `feedback_jmix_upload_receiver_deprecated` + Phase 13 `ChatPanelFragment.java:245–270` (`UploadHandler.toFile` Java-side wiring).
**Apply to:** `taskFileUpload` element — NO declarative `receiverType=` in XML; Java-side `UploadHandler.toFile(...)` registration in `ChatPanelFragment.onReady` continues unchanged. Only `FileRejectedEvent` is `@Subscribe`-safe.

### Liquibase Auto-Include
**Source:** `agentstore-changelog.xml:15` — `<includeAll path="/com/vn/agent/liquibase/agentstore-changelog" errorIfMissingOrEmpty="false"/>`.
**Apply to:** `100-ai-task-file-drop-dead-columns.xml` — drop into the directory; auto-discovered. No parent file edit.

### Entity Instantiation
**Source:** CLAUDE.md + memory `feedback_jmix_unconstrained_for_system_writes`.
**Apply to:** every new `AiMessage` / `AiTaskFile` instance — `metadataApi.create(Class)` (NEVER `new`). Already wired at `ChatPanelFragment.java:111`.

---

## No Analog Found / Partial Analogs

| File | Role | Reason |
|------|------|--------|
| `100-ai-task-file-drop-dead-columns.xml` | liquibase / DDL drop | No prior `dropColumn` / `dropForeignKeyConstraint` migration in `agentstore-changelog/`. Pattern is mechanical Liquibase tag composition; verbatim from 13.1-RESEARCH Pattern 7 plus `090-ai-task-file.xml` for FK + index name conventions. |
| Inline NOTICE rendering dispatch | view-renderer choice | Phase 13's `MessageList` is `MessageListItem`-only (no centered divider). Two implementation options (Option A: migrate to `VerticalLayout` of `MessageBubbleComponent` + `vaadin-message`; Option B: intersperse `vaadin-message` siblings in `messageListSlot`). Plan-phase locks. CSS works identically for either path. |

---

## Metadata

**Analog search scope:**
- `D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/java/com/vn/agent/**/*.java`
- `D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/resources/com/vn/agent/**/*.{xml,properties,css}`
- `D:/DTH/ai-agent-core/jmix-app/src/main/resources/application.properties`
- `D:/DTH/jmix-crm/src/main/{java,resources,frontend}/com/company/crm/ai/view/aiconversation/*` (4 files — verbatim port source)
- `D:/DTH/ai-agent-core/.planning/phases/13-chat-task-input-stt-task-scoped-file/13-PATTERNS.md` (parent phase analogs)
- `D:/DTH/ai-agent-core/.planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-RESEARCH.md` (researcher's pre-cited analogs)

**Files scanned:** ~24 in-repo (Java/XML/properties/CSS) + 4 jmix-crm reference files + 2 parent-phase planning docs.

**Pattern extraction date:** 2026-05-07

**Verbatim port mandate:** ~85% of Phase 13.1 UI is verbatim CRM port (XML + CSS) + ~10% Jmix built-ins (Downloader, Dialogs, Notifications, Messages, FragmentRenderer); the only hand-roll surfaces are `humanizeSize` (5 lines), per-extension icon switch (5 lines), and budget-cap loop (~20 lines).

## PATTERN MAPPING COMPLETE
