---
milestone: v1.1.0
milestone_name: "Prompt Hardening, Mutation Tools & Configurable Chat Surfaces"
audited: 2026-05-11T00:00:00+07:00
status: tech_debt
scope:
  phases_in_scope: ["9", "10", "11", "12", "13", "13.1", "14"]
  deferred_to_v1_2: ["Phase 15 — Chat Voice Input · Soniox STT (STT-01..06, SPI-11, TEST-17)"]
  backlog_not_in_scope: ["999.1 — Phase 11 mutation hardening follow-ups", "999.2 — Chat Voice Input Soniox STT (= deferred Phase 15)"]
scores:
  requirements: "all delivered REQ-IDs satisfied across 7 phases (Phase 15 STT set deferred)"
  phases: "7/7 phase goals achieved (Phase 10 verification still flagged human_needed — open triage items, see tech_debt)"
  integration: "8/8 cross-phase wiring points WIRED"
  flows: "5/5 E2E user flows PASS"
gaps:
  requirements: []   # no unsatisfied / orphaned requirements
  integration: []    # no broken cross-phase wiring
  flows: []          # no broken E2E flows
warnings:
  - id: "INT-WARNING-AiTaskFile-not-denylisted"
    severity: low
    detail: "ai_AiTaskFile is NOT on AiInternalEntityNames (siblings ai_AiConversation/ai_AiMessage/ai_AiExtractionDraft are). A user with AiAgentUserRole READ on AiTaskFile could have the LLM call find_records('ai_AiTaskFile') and see their own attachment metadata (filename/content_type/size/storageRef). Documented design decision (Phase 13.1 RESEARCH.md): task files are user-scoped data; exposed columns are benign metadata (file bytes live in FileStorage). Flag for reviewer: confirm intent vs. denylist inconsistency."
    requirement: "TASK-01..06 / ENT-07 / SEC-06 (partial — AiTaskFile)"
tech_debt:
  - phase: "10-ai-specific-llm-exposure-policy"
    note: "VERIFICATION.md status is human_needed (stale, 2026-04-28). Phase goal achieved (4/4 ROADMAP criteria, 12/12 requirement IDs satisfied) and phases 11-14 built on it successfully. Open items recorded in its human_verification block were never closed in the doc:"
    items:
      - "REVIEW BLOCKER-01 — AsyncIngestionWorker.enrich NPE on null element in deserialized allowedRoles. Integration check found this ADDRESSED in code (AsyncIngestionWorker.java:288-293 filters null/blank). Re-run /gsd-verify-work 10 to flip status to passed."
      - "REVIEW BLOCKER-02 — Liquibase 060 index name vs JPA @Index name mismatch on AiExposureRule. Found ADDRESSED in code (060-ai-exposure-rule.xml:34-49 matches AiExposureRule.java:38-40)."
      - "REVIEW WARNING-08 — KB upload form lacked allowedRoles field. Found ADDRESSED in code (KnowledgeBaseView.java:127,155-187 has uploadAllowedRolesField)."
      - "REVIEW WARNING-01 — partial-failure window in updatePermissionsAndReingest (RAG KB flow). NOT re-verified line-by-line; out of v1.1 cross-phase scope. Track in hardening backlog."
      - "Visual verification of AiExposureRuleListView/DetailView admin workflow and VectorStoreDebugView pagination/filter — never recorded (Vaadin Flow visual quality)."
  - phase: "all phases except 14"
    note: "Nyquist coverage — only Phase 14 has a *-VALIDATION.md (nyquist_compliant: true). Phases 9, 10, 11, 12, 13, 13.1 are MISSING VALIDATION.md."
    items:
      - "Run /gsd-validate-phase 9 / 10 / 11 / 12 / 13 / 13.1 to backfill Nyquist validation coverage (discovery-only finding; does not block milestone close)."
  - phase: "13.1-chat-attachments-rightpane-and-persistent-context"
    note: "ROADMAP listed '**Plans:** 6/7 plans executed' but all 7 plans show [x] and VERIFICATION.md reports 9/9 must-haves verified. Minor doc inconsistency (the '6/7' looks like a stale typo)."
    items:
      - "Reconcile the plan-count wording in the archived v1.1.0-ROADMAP.md (cosmetic)."
nyquist:
  compliant_phases: ["14"]
  partial_phases: []
  missing_phases: ["9", "10", "11", "12", "13", "13.1"]
  overall: "PARTIAL — only 1/7 phases have VALIDATION.md"
---

# v1.1.0 Milestone Audit — Prompt Hardening, Mutation Tools & Configurable Chat Surfaces

**Audited:** 2026-05-11
**Status:** `tech_debt` — all requirements met, no critical blockers, cross-phase integration verified, all E2E flows complete; accumulated bookkeeping debt (Phase 10 verification still `human_needed`, 6 phases lack `VALIDATION.md`) needs review before/with milestone close.

## Scope

| In scope | Status |
|---|---|
| Phase 9 — Tool-Layer Foundations & Prompt-Contract Hardening | ✓ verified (30/30 must-haves) |
| Phase 10 — AI-Specific LLM Exposure Policy | ✓ goal achieved (4/4 ROADMAP criteria, 12/12 REQ IDs) — VERIFICATION status `human_needed`, open triage items addressed in code (see Tech Debt) |
| Phase 11 — Mutation-Capable Built-In Tools | ✓ verified (5/5 must-haves; passed after 11-12/11-13 gap closure) |
| Phase 12 — Configurable Chat Surfaces | ✓ verified (4/4 criteria, 12/12 REQ IDs, 7/7 UAT) |
| Phase 13 — Chat Task File — Attach + LLM Read + Bulk Save | ✓ verified (9/9 + 6/6 gap-closure must-haves, 4/4 HUMAN-UAT resolved) |
| Phase 13.1 — Chat Attachments CRM-Style Right-Pane + Persistent Multi-Turn Context | ✓ verified (9/9 must-haves) |
| Phase 14 — Intent-Driven Extraction → Form Prefill | ✓ verified (16/16 must-haves; manual chat-to-form UAT accepted; shipped via PR #28) — only phase with VALIDATION.md (nyquist_compliant: true) |

**Deferred to v1.2 (out of scope):** Phase 15 — Chat Voice Input · Soniox STT (`STT-01..06`, `SPI-11`, `TEST-17`) — see ROADMAP `## Backlog` → Phase 999.2 and `REQUIREMENTS.md` "Deferred to v1.2".

**Backlog, not in milestone scope:** Phase 999.1 (Phase 11 mutation hardening follow-ups), Phase 999.2 (= deferred Phase 15).

## 1. Requirements Coverage

**No unsatisfied or orphaned requirements.** Every milestone REQ-ID is verified by its assigned phase:

| Phase | Requirements | Phase VERIFICATION status |
|---|---|---|
| 9 | PROMPT-01..06, TOOL-09..12, SPI-09, TEST-08, AUD-07 (partial — plumbing) | passed (30/30) |
| 10 | EXP-01..10, ENT-05, SEC-05 (partial — AiExposureRule), TEST-09 | human_needed — but 12/12 REQ IDs reported satisfied; goal achieved; downstream phases built on it OK |
| 11 | MUT-01..12, ENT-09, AUD-06, AUD-07 (mutation-specific), SEC-07, SPI-10, TEST-10..13 | passed (5/5) |
| 12 | SURF-01..10, ENT-06, SEC-05 (partial — AiUiSettings), TEST-14 | passed (4/4 + 12/12 + 7/7 UAT) |
| 13 | TASK-01..06, ENT-07, MUT-14, SEC-06 (partial — AiTaskFile), TEST-16 | passed (9/9 + 6/6 gap + 4/4 UAT) |
| 13.1 | UI-01, RES-01, AUDIT-01, LIFE-01, UX-01, SCHEMA-01, CONTRACT-01, TEST-16-PORT, I18N-01 | passed (9/9) |
| 14 | EXTRACT-01..10, ENT-08, SPI-12, SEC-06 (partial — AiExtractionDraft row-level), TEST-15 | pass (16/16 + manual UAT) |

3-source cross-reference (phase VERIFICATION × SUMMARY frontmatter × REQUIREMENTS traceability) shows no `unsatisfied` rows. The only requirements not yet checked off as complete in REQUIREMENTS.md are the **deferred** `STT-01..06` / `SPI-11` / `TEST-17`, which are intentionally moved out of v1.1 — not gaps.

## 2. Cross-Phase Integration (gsd-integration-checker)

**Verdict: PASS** — all 8 cross-phase wiring points WIRED end-to-end; no broken or partially-wired connections.

| # | Wiring point | Status |
|---|---|---|
| 1 | Phase 9 → 10 substitution: `BaselineContextProvider` / `BuiltInDataTools` / `FetchPlanIntersector` call `LlmExposurePolicy`; RAG `RetrievalFilterBuilder` applies the NIN denylist | WIRED |
| 2 | Phase 10 → 11/13: mutation tools (`create_record` …, `bulk_save_records`) gate through `LlmExposurePolicy.canCreate/canUpdate` before `AccessManager` | WIRED |
| 3 | Phase 11 → 13: `bulk_save_records` reuses `MutationSaveExecutor` / `MutationGuard` / `AiMutationIntent` / `MutationErrorTranslator` (one transaction, batch idempotency) | WIRED |
| 4 | Phase 12 → 13/13.1/14: `ChatPanelFragment` slots (`attachmentsPanel` → CRM right-pane; `messageInputSlot`) + `AiChatSessionState` continuity across both surfaces; `ChatSurfaceMounter` contract unchanged | WIRED |
| 5 | Phase 14 → 11: `action:create-now` routes the LLM through the constrained mutation tool surface; `prepare_form_draft`/named-intent turns expose ONLY `prepare_form_draft`; LLM never receives `ViewNavigators` (TEST-15 scanner green) | WIRED |
| 6 | Audit surface: new `eventName`s + new `outcome`s flow through `AuditWriter.writeToolCall`, no new `AuditKind`; mutation callbacks self-audit exactly once, not double-wrapped by `ToolCallbackAuditDecorator` | WIRED |
| 7 | Liquibase 060/061/070/071/080/090/091/100/110 picked up by root `<includeAll>` in filename order, run cleanly | WIRED |
| 8 | Role composition: `AiAgentAdminRole`(AiExposureRule + AiUiSettings) / `AiAgentUserRole` + `AiAgentUserRowLevelRole`(own AiTaskFile + own AiExtractionDraft) / `AiAgentMutationRole` marker | WIRED (see Warning) |

## 3. End-to-End Flows

| Flow | Status |
|---|---|
| A — Admin hides an entity via `AiExposureRule` → it disappears from `list_entities`/`agent.entities`/`find_records`/RAG, surfaced uniformly as `unknown_entity` (never `access_denied`) | PASS |
| B — `mutation.enabled=true` create flow: clarify missing fields → `propose_action_choices` → "Create now" → full fail-closed gating chain → `AiMutationIntent` idempotency → `DataManager.save` → audited; replay → `IDEMPOTENT_REPLAY` | PASS |
| C — Attach xlsx in CRM right-pane → persists as `AiTaskFile` (no `IngesterManager`/`VectorStore`) → multi-turn: turns 2..N still see the file (per-turn-all, budget-capped, LRU eviction + `task_file_budget_exceeded` audit) → `bulk_save_records` → one transaction, batch-audited | PASS |
| D — Switch chat surface mid-session (`ChatView` ↔ `ChatDialogView`) → same `conversationId`, same JDBC chat-memory rows | PASS |
| E — "PDF → customer draft" → `prepare_form_draft` structured payload → "Open form to confirm" → `OpenFormWithDraftHandler` opens prefilled Jmix detail view (permitted attrs only via `setValueIfPermitted`) → Save = normal Jmix validation → draft row deleted; LLM had no `ViewNavigators` at any point | PASS |

## 4. Tech Debt (non-blocking — review before/with close)

**Phase 10 — verification status stale at `human_needed`.** The phase goal is fully achieved and phases 11–14 built on it successfully. Its `human_verification` block lists items that were never closed in the doc; the integration check found the substantive ones already fixed in code:
- BLOCKER-01 (`AsyncIngestionWorker.enrich` NPE on null role element) — **fixed in code** (`AsyncIngestionWorker.java:288-293`). → `/gsd-verify-work 10` would flip the status.
- BLOCKER-02 (Liquibase 060 index name vs JPA `@Index`) — **fixed in code** (`060-ai-exposure-rule.xml:34-49` matches `AiExposureRule.java:38-40`).
- WARNING-08 (KB upload form lacked `allowedRoles` field) — **fixed in code** (`KnowledgeBaseView.java:127,155-187`).
- WARNING-01 (partial-failure window in `updatePermissionsAndReingest`, RAG KB flow) — not re-verified; track in hardening backlog (out of v1.1 cross-phase scope).
- Visual verification of `AiExposureRuleListView`/`DetailView` admin workflow and `VectorStoreDebugView` — never recorded.

**Nyquist coverage — 6 of 7 phases lack `*-VALIDATION.md`.** Only Phase 14 has it (`nyquist_compliant: true`). Phases 9, 10, 11, 12, 13, 13.1 are MISSING. Discovery-only finding — does not block close. Backfill with `/gsd-validate-phase 9` … `13.1` when convenient.

**Integration WARNING (low severity):** `ai_AiTaskFile` is not on the `AiInternalEntityNames` LLM denylist (its siblings `ai_AiConversation`, `ai_AiMessage`, `ai_AiExtractionDraft` are). A user with `AiAgentUserRole` READ on `AiTaskFile` could have the LLM `find_records('ai_AiTaskFile')` and see their own attachment metadata (filename/content type/size/storageRef — file bytes live in `FileStorage`, not the entity). This was a **documented design decision** (Phase 13.1 RESEARCH.md: "task files are user-scoped data, not LLM-tool surface"). Reviewer: confirm intent vs. denylist inconsistency.

**Doc nit:** ROADMAP listed Phase 13.1 as "6/7 plans executed" while all 7 plans are `[x]` and VERIFICATION reports 9/9 — looks like a stale typo; reconcile in the archived ROADMAP.

## 5. Verdict

- **Requirements:** all delivered REQ-IDs satisfied. No unsatisfied, no orphans. (Phase 15 STT set deferred to v1.2 — not a gap.)
- **Phases:** 7/7 phase goals achieved. Phase 10's verification doc is the only one not formally `passed`, and the substantive items behind that are already fixed in code.
- **Integration:** 8/8 cross-phase wiring points WIRED.
- **E2E flows:** 5/5 PASS.
- **No critical blockers.** Tech debt is bookkeeping (re-verify Phase 10, backfill 6 `VALIDATION.md` files) plus one documented low-severity design choice.

**→ Safe to proceed to `/gsd-complete-milestone 1.1`**, accepting the tech debt above (it will be tracked — Phase 10 re-verification + Nyquist backfill can happen in v1.2 or as a hardening pass). Alternatively, run `/gsd-verify-work 10` first to flip Phase 10 to `passed` for a cleaner archive.

---

_Audited 2026-05-11 by /gsd-audit-milestone — phase verifications aggregated + gsd-integration-checker cross-phase + E2E pass._
