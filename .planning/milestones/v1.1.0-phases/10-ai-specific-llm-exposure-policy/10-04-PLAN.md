---
phase: 10-ai-specific-llm-exposure-policy
plan: 04
type: execute
wave: 3
depends_on: [10-02]
files_modified:
  - ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/BaselineContextProvider.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/tools/BuiltInDataTools.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/tools/fetchplan/FetchPlanIntersector.java
autonomous: true
requirements:
  - EXP-03
  - EXP-04
  - EXP-09

must_haves:
  truths:
    - "BaselineContextProvider no longer injects CurrentUserSchemaAccess directly — it injects LlmExposurePolicy"
    - "BuiltInDataTools no longer injects CurrentUserSchemaAccess — it injects LlmExposurePolicy at all 5+ call sites"
    - "FetchPlanIntersector no longer injects CurrentUserSchemaAccess — it injects LlmExposurePolicy"
    - "FetchPlanIntersector routes BOTH canReadAttribute AND canReadEntity calls through LlmExposurePolicy — denylisted entities reachable via relationship hops in host fetch plans are pruned, not returned via nested projection (Fix R5)"
    - "unknown_entity error hint strings in BuiltInDataTools are preserved byte-for-byte (Phase 9 D-14)"
    - "D-14: BuiltInDataTools resolveReadableEntityOrThrow returns unknown_entity for BOTH denylisted and unknown-name cases — access_denied is no longer reachable on this code path. TEST-08 byte-for-byte assertions still pass. UNKNOWN_ENTITY_HINTS strings preserved exactly including em dash U+2014 on hint #3. (Fix R4)"
    - "BuiltInDataTools uses LlmExposurePolicy.canReadEntity(mc) to gate entity access. If false (whether due to Jmix role denial OR denylist), the denial branch constructs ToolErrorDto.unknownEntity(...) — NOT ToolErrorDto.accessDenied. Full uniformity: both Jmix-role-denied and denylist-denied entities surface identically as unknown_entity to the LLM (EXP-09 opacity)."
    - "No behavioral change other than rules being applied — if denylist is empty, behavior is identical to pre-Phase-10"
    - "CurrentUserSchemaAccess class itself is NOT modified — it stays as the Jmix-permission source of truth"
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/BaselineContextProvider.java"
      provides: "LlmExposurePolicy injection replacing CurrentUserSchemaAccess"
      contains: "LlmExposurePolicy"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/tools/BuiltInDataTools.java"
      provides: "All currentUserSchemaAccess references replaced with llmExposurePolicy; denial path returns unknown_entity not access_denied"
      contains: "llmExposurePolicy"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/tools/fetchplan/FetchPlanIntersector.java"
      provides: "LlmExposurePolicy injection replacing CurrentUserSchemaAccess; both canReadAttribute AND canReadEntity routed through policy"
      contains: "LlmExposurePolicy"
  key_links:
    - from: "BaselineContextProvider.compose()"
      to: "LlmExposurePolicy.getReadableSchema()"
      via: "llmExposurePolicy.getReadableSchema()"
      pattern: "llmExposurePolicy\\.getReadableSchema"
    - from: "BuiltInDataTools.listEntities()"
      to: "LlmExposurePolicy.getReadableSchema()"
      via: "llmExposurePolicy.getReadableSchema()"
      pattern: "llmExposurePolicy\\.getReadableSchema"
    - from: "BuiltInDataTools.resolveReadableEntityOrThrow()"
      to: "ToolErrorDto.unknownEntity(...)"
      via: "denial branch — access_denied removed, unknown_entity used for both cases"
      pattern: "ToolErrorDto\\.unknownEntity"
---

<objective>
Mechanical call-site swap: replace `CurrentUserSchemaAccess` injection with `LlmExposurePolicy` in the three consumers — `BaselineContextProvider`, `BuiltInDataTools`, and `FetchPlanIntersector`.

Additionally (Fix R4): migrate `BuiltInDataTools.resolveReadableEntityOrThrow()` denial path from `access_denied` to `unknown_entity` so denylisted entities are indistinguishable from non-existent ones (EXP-09 opacity, Phase 3 D-08).

Additionally (Fix R5): in `FetchPlanIntersector`, swap BOTH `canReadAttribute` AND `canReadEntity` to route through `LlmExposurePolicy` so denylisted entities reachable via relationship hops in nested fetch plans are also pruned.

Purpose: After this plan, all LLM-visible schema and tool responses source through the policy boundary. `agent.entities`, `agent.permissions`, and all tool responses automatically exclude denied entities. Denied entities return `unknown_entity` — never `access_denied`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-ai-specific-llm-exposure-policy/10-CONTEXT.md
@.planning/phases/10-ai-specific-llm-exposure-policy/10-PATTERNS.md

<interfaces>
<!-- Source: BaselineContextProvider.java — exact substitution seam (PATTERNS.md #9) -->
```java
// BEFORE (field declaration, line ~73):
private final CurrentUserSchemaAccess currentUserSchemaAccess;

// BEFORE (constructor param, line ~80):
CurrentUserSchemaAccess currentUserSchemaAccess,

// BEFORE (constructor body, line ~86):
this.currentUserSchemaAccess = currentUserSchemaAccess;

// BEFORE (call site, line ~107):
Map<MetaClass, Set<String>> readableSchema = currentUserSchemaAccess.getReadableSchema();

// AFTER — mechanical rename only (identical call shape):
private final LlmExposurePolicy llmExposurePolicy;
// constructor: LlmExposurePolicy llmExposurePolicy
// body:        this.llmExposurePolicy = llmExposurePolicy;
// call:        Map<MetaClass, Set<String>> readableSchema = llmExposurePolicy.getReadableSchema();
```

<!-- Source: BuiltInDataTools.java — all 5 currentUserSchemaAccess references (PATTERNS.md #10) -->
```
// Lines approximately: 6(import), 55(field), 67(ctor param), 78(ctor body), then call sites:
// line ~92:  currentUserSchemaAccess.getReadableSchema()
// line ~151: currentUserSchemaAccess.getReadableSchema().get(metaClass)
// line ~268: currentUserSchemaAccess.canReadAttribute(rootMetaClass, relationship)
// line ~274: currentUserSchemaAccess.canReadEntity(targetMetaClass)
// line ~336: currentUserSchemaAccess.canReadEntity(metaClass)
// All prefixes: currentUserSchemaAccess → llmExposurePolicy (method signatures identical)
```

<!-- Source: BuiltInDataTools.java — resolveReadableEntityOrThrow() denial path (Fix R4) -->
```
// CURRENT (before this plan): denial branch produces access_denied
//   return ToolErrorDto.accessDenied(entityName, UNKNOWN_ENTITY_HINTS);
// OR similar construction. Read the file to find exact line.
//
// REQUIRED (Fix R4): change to unknown_entity for ALL canReadEntity()==false cases:
//   return ToolErrorDto.unknownEntity(entityName, UNKNOWN_ENTITY_HINTS);
// This makes denylisted AND Jmix-role-denied entities both surface as unknown_entity.
// Full uniformity: the LLM cannot distinguish "no such entity" from "entity exists but denied".
//
// CRITICAL: UNKNOWN_ENTITY_HINTS strings must remain byte-for-byte identical.
// The three hint strings in the expected[] arrays MUST remain identical to what TEST-08 asserts.
// The em dash on hint #3 is U+2014 (—), NOT a hyphen (-). Do NOT change these strings.
//
// After this change: grep -c "access_denied" BuiltInDataTools.java returns 0
// (or only for non-denial legitimate uses if any; verify by reading).
// grep -c "ToolErrorDto.unknownEntity" BuiltInDataTools.java returns at least 1 in the resolve path.
```

<!-- Source: FetchPlanIntersector.java — same mechanical swap (PATTERNS.md #11) + Fix R5 -->
```
// CURRENT: only canReadAttribute is called via currentUserSchemaAccess
// FetchPlanIntersector also calls: currentUserSchemaAccess.canReadEntity(nestedMetaClass)
// for relationship hop checks. This call must ALSO be routed through LlmExposurePolicy.
//
// REQUIRED (Fix R5):
// - currentUserSchemaAccess → llmExposurePolicy (all references)
// - canReadAttribute call-through: unchanged call shape
// - canReadEntity call-through: unchanged call shape
// Both calls now route through policy, so denylisted relationship targets are pruned.
//
// After: grep -c "currentUserSchemaAccess" FetchPlanIntersector.java returns 0
//        grep -c "llmExposurePolicy.canRead" FetchPlanIntersector.java returns at least 2
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Call-site swap in BaselineContextProvider and FetchPlanIntersector (including canReadEntity Fix R5)</name>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/BaselineContextProvider.java
    ai-agent/ai-agent/src/main/java/com/vn/agent/tools/fetchplan/FetchPlanIntersector.java
  </files>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/BaselineContextProvider.java (FULL file read — find exact line numbers for field, ctor, and call site)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/tools/fetchplan/FetchPlanIntersector.java (FULL file read — find ALL currentUserSchemaAccess references including both canReadAttribute AND canReadEntity calls)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/exposure/LlmExposurePolicy.java (method signatures must match call shapes)
  </read_first>
  <action>
In `BaselineContextProvider.java`:
1. Replace import `import com.vn.agent.metadata.CurrentUserSchemaAccess;` with `import com.vn.agent.exposure.LlmExposurePolicy;`
2. Replace field `private final CurrentUserSchemaAccess currentUserSchemaAccess;` with `private final LlmExposurePolicy llmExposurePolicy;`
3. Replace constructor parameter type `CurrentUserSchemaAccess currentUserSchemaAccess` with `LlmExposurePolicy llmExposurePolicy`
4. Replace constructor body `this.currentUserSchemaAccess = currentUserSchemaAccess;` with `this.llmExposurePolicy = llmExposurePolicy;`
5. Replace call site `currentUserSchemaAccess.getReadableSchema()` with `llmExposurePolicy.getReadableSchema()`
Do NOT change any other line. Do NOT remove the existing Javadoc comment referencing the Phase 10 substitution seam (update it to say the substitution is complete).

In `FetchPlanIntersector.java` (Fix R5 — swap BOTH canReadAttribute AND canReadEntity):
1. Replace import for `CurrentUserSchemaAccess` with `LlmExposurePolicy`
2. Replace field declaration `private final CurrentUserSchemaAccess currentUserSchemaAccess` with `private final LlmExposurePolicy llmExposurePolicy`
3. Replace constructor parameter type and assignment
4. Replace ALL call sites:
   - `currentUserSchemaAccess.canReadAttribute(...)` → `llmExposurePolicy.canReadAttribute(...)`
   - `currentUserSchemaAccess.canReadEntity(...)` → `llmExposurePolicy.canReadEntity(...)`
   Both method signatures are identical to LlmExposurePolicy — mechanical rename only.

CRITICAL (Fix R5): Do NOT leave any `currentUserSchemaAccess.canReadEntity` calls unswapped. After this edit, `grep -c "currentUserSchemaAccess" FetchPlanIntersector.java` must return 0 and `grep -c "llmExposurePolicy.canRead" FetchPlanIntersector.java` must return at least 2.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:compileJava 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CurrentUserSchemaAccess" ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/BaselineContextProvider.java` returns 0
    - `grep -c "llmExposurePolicy" ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/BaselineContextProvider.java` returns at least 3
    - `grep -c "CurrentUserSchemaAccess" ai-agent/ai-agent/src/main/java/com/vn/agent/tools/fetchplan/FetchPlanIntersector.java` returns 0
    - `grep -c "currentUserSchemaAccess" ai-agent/ai-agent/src/main/java/com/vn/agent/tools/fetchplan/FetchPlanIntersector.java` returns 0
    - `grep -c "llmExposurePolicy.canRead" ai-agent/ai-agent/src/main/java/com/vn/agent/tools/fetchplan/FetchPlanIntersector.java` returns at least 2 (Fix R5: both canReadAttribute AND canReadEntity swapped)
    - Both files compile cleanly
  </acceptance_criteria>
  <done>BaselineContextProvider and FetchPlanIntersector no longer inject CurrentUserSchemaAccess. All call sites use LlmExposurePolicy. FetchPlanIntersector routes BOTH canReadAttribute AND canReadEntity through the policy (Fix R5). Compilation clean.</done>
</task>

<task type="auto">
  <name>Task 2: Call-site swap in BuiltInDataTools + denial path migration to unknown_entity (Fix R4)</name>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/tools/BuiltInDataTools.java
  </files>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/tools/BuiltInDataTools.java (FULL file — identify ALL currentUserSchemaAccess references; find resolveReadableEntityOrThrow() method and its denial branch; locate UNKNOWN_ENTITY_HINTS array with the exact strings including em dash U+2014 on hint #3)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/exposure/LlmExposurePolicy.java (method signatures)
    - .planning/phases/10-ai-specific-llm-exposure-policy/10-CONTEXT.md (D-03: unknown_entity hint strings byte-for-byte; D-14 from Phase 9 must be preserved)
  </read_first>
  <action>
In `BuiltInDataTools.java`:

Step A — Mechanical call-site swap:
1. Replace import for `CurrentUserSchemaAccess` with `LlmExposurePolicy`
2. Replace field `private final CurrentUserSchemaAccess currentUserSchemaAccess` with `private final LlmExposurePolicy llmExposurePolicy`
3. Replace constructor parameter type `CurrentUserSchemaAccess currentUserSchemaAccess` with `LlmExposurePolicy llmExposurePolicy`
4. Replace constructor body assignment
5. Replace ALL call sites (there are ~5 per PATTERNS.md #10):
   - `currentUserSchemaAccess.getReadableSchema()` → `llmExposurePolicy.getReadableSchema()`
   - `currentUserSchemaAccess.getReadableSchema().get(metaClass)` → `llmExposurePolicy.getReadableSchema().get(metaClass)`
   - `currentUserSchemaAccess.canReadAttribute(rootMetaClass, relationship)` → `llmExposurePolicy.canReadAttribute(rootMetaClass, relationship)`
   - `currentUserSchemaAccess.canReadEntity(targetMetaClass)` → `llmExposurePolicy.canReadEntity(targetMetaClass)`
   - `currentUserSchemaAccess.canReadEntity(metaClass)` → `llmExposurePolicy.canReadEntity(metaClass)`

Step B — Denial path migration (Fix R4):
Read the file carefully to find `resolveReadableEntityOrThrow()` (or equivalent method that gates entity access). Locate the branch where `canReadEntity()` returns false and the method returns/throws an error DTO.

CURRENT behavior: the denial branch constructs `ToolErrorDto.accessDenied(...)` or equivalent.
REQUIRED: change to `ToolErrorDto.unknownEntity(...)` using the same `UNKNOWN_ENTITY_HINTS` strings.

The rationale: Phase 10 requires denylisted entities to surface identically to non-existent entities (EXP-09, Phase 3 D-08). Full uniformity is the preferred approach — both Jmix-role-denied AND denylist-denied entities return `unknown_entity`. The LLM cannot distinguish the two cases, which is correct behavior.

CRITICAL — byte-for-byte hint preservation:
Do NOT modify the `UNKNOWN_ENTITY_HINTS` strings or their array. The exact content (including em dash U+2014 on hint #3) must match what TEST-08 asserts. Only change the DTO constructor call from `accessDenied` to `unknownEntity`.

After this edit:
- `grep -v '^//' BuiltInDataTools.java | grep -c "access_denied\|accessDenied"` returns 0 (no denial error path uses access_denied)
- `grep -c "ToolErrorDto.unknownEntity\|unknownEntity" BuiltInDataTools.java` returns at least 1 in the resolve path

After editing, use JetBrains MCP `get_file_problems("ai-agent/ai-agent/src/main/java/com/vn/agent/tools/BuiltInDataTools.java", onlyErrors=false)` to triage any issues.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:compileJava 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CurrentUserSchemaAccess" ai-agent/ai-agent/src/main/java/com/vn/agent/tools/BuiltInDataTools.java` returns 0
    - `grep -c "currentUserSchemaAccess" ai-agent/ai-agent/src/main/java/com/vn/agent/tools/BuiltInDataTools.java` returns 0
    - `grep -c "llmExposurePolicy" ai-agent/ai-agent/src/main/java/com/vn/agent/tools/BuiltInDataTools.java` returns at least 6 (field + ctor param + ctor body + 5 call sites, some may merge)
    - `grep -c "UNKNOWN_ENTITY_HINTS\|unknown_entity" ai-agent/ai-agent/src/main/java/com/vn/agent/tools/BuiltInDataTools.java` — same count as before this edit (no loss of error path strings)
    - `grep -v '^[[:space:]]*//' ai-agent/ai-agent/src/main/java/com/vn/agent/tools/BuiltInDataTools.java | grep -c "accessDenied\|access_denied"` returns 0 (Fix R4: denial path uses unknownEntity now)
    - `grep -c "unknownEntity\|ToolErrorDto.unknownEntity" ai-agent/ai-agent/src/main/java/com/vn/agent/tools/BuiltInDataTools.java` returns at least 1 (Fix R4 applied)
    - `./gradlew :ai-agent:ai-agent:test` — no test regressions (TEST-08 byte-for-byte hint assertions still pass)
  </acceptance_criteria>
  <done>BuiltInDataTools injects LlmExposurePolicy. All 5 call sites updated. Denial path now returns unknown_entity (not access_denied) for all canReadEntity==false cases. UNKNOWN_ENTITY_HINTS unchanged. Tests pass including TEST-08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BuiltInDataTools → LlmExposurePolicy | All schema queries now go through the policy boundary; denied entities produce unknown_entity (not access_denied) — full opacity |
| BaselineContextProvider → LlmExposurePolicy | agent.entities and agent.permissions cannot include denied entities |
| FetchPlanIntersector → LlmExposurePolicy | Both attribute AND entity hop checks route through policy; denylisted relationship targets pruned from nested fetch plans |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | Information Disclosure | BaselineContextProvider agent.entities | mitigate | getReadableSchema() removes denied entities; agent.entities block cannot include them after this plan. |
| T-10-03 | Information Disclosure | BuiltInDataTools list_entities, find_records | mitigate | canReadEntity() returns false for denied entities → unknown_entity error (not access_denied). Phase 3 D-08 opacity preserved. Full uniformity: both Jmix-role-denied and denylist-denied surface identically. |
| T-10-04 | Information Disclosure | BuiltInDataTools error path | mitigate | unknown_entity text preserved byte-for-byte; access_denied removed from this code path entirely. No error variant reveals the denylist distinction. |
| T-10-05 | Information Disclosure | FetchPlanIntersector relationship hops | mitigate | Both canReadAttribute AND canReadEntity route through LlmExposurePolicy; denylisted entities reachable via relationships are pruned from nested fetch plan projections. |
</threat_model>

<verification>
1. `./gradlew :ai-agent:ai-agent:compileJava` — clean
2. `grep -rn "CurrentUserSchemaAccess" ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/ ai-agent/ai-agent/src/main/java/com/vn/agent/tools/` — zero results (only CurrentUserSchemaAccess.java itself and metadata/ package remain)
3. `grep -v '^[[:space:]]*//' ai-agent/ai-agent/src/main/java/com/vn/agent/tools/BuiltInDataTools.java | grep -c "accessDenied\|access_denied"` returns 0
4. `grep -c "llmExposurePolicy.canRead" ai-agent/ai-agent/src/main/java/com/vn/agent/tools/fetchplan/FetchPlanIntersector.java` returns at least 2
5. `./gradlew :ai-agent:ai-agent:test` — existing tests pass (prompt-contract tests, schema access tests, TEST-08 hint assertions)
</verification>

<success_criteria>
- Zero references to CurrentUserSchemaAccess in BaselineContextProvider, BuiltInDataTools, FetchPlanIntersector
- All replaced with llmExposurePolicy (identical call shape)
- BuiltInDataTools denial path uses unknown_entity, not access_denied (Fix R4)
- UNKNOWN_ENTITY_HINTS byte-for-byte unchanged including em dash U+2014
- FetchPlanIntersector routes both canReadAttribute AND canReadEntity through policy (Fix R5)
- Existing tests pass including TEST-08
</success_criteria>

<output>
After completion, create `.planning/phases/10-ai-specific-llm-exposure-policy/10-04-SUMMARY.md`
</output>
