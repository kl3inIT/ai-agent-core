---
phase: 11-mutation-capable-built-in-tools
plan: 12
type: execute
wave: 12
depends_on:
  - 11-11-PLAN.md
files_modified:
  - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiAuditEvent.java
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/071-widen-ai-audit-outcome.xml
  - ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/BuiltInMutationToolsIdempotencyReplayTest.java
autonomous: true
gap_closure: true
requirements:
  - MUT-04
  - MUT-08
  - AUD-06
  - TEST-11
  - TEST-12
must_haves:
  truths:
    - "AiToolCallOutcome.IDEMPOTENT_REPLAY is 17 characters and must persist safely in AiAuditEvent.outcome without truncation or database constraint failure."
    - "AiAuditEvent.outcome Java metadata and the agentstore Liquibase schema both allow at least 32 characters for OUTCOME."
    - "The existing 030-ai-audit-event.xml historical createTable remains intact; a new ordered changelog widens AI_AGENT_AUDIT_EVENT.OUTCOME for existing databases."
    - "BuiltInMutationToolsIdempotencyReplayTest proves the second exact replay writes a durable AiAuditEvent row whose outcome is AiToolCallOutcome.IDEMPOTENT_REPLAY."
    - "The replay audit regression queries persisted AiAuditEvent rows from agentstore, not only the returned JSON DTO."
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiAuditEvent.java"
      provides: "JPA metadata width for persisted audit outcome"
      contains: "@Column(name = \"OUTCOME\", length = 32)"
    - path: "ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/071-widen-ai-audit-outcome.xml"
      provides: "DDL migration widening AI_AGENT_AUDIT_EVENT.OUTCOME"
      contains: "newDataType=\"varchar(32)\""
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/BuiltInMutationToolsIdempotencyReplayTest.java"
      provides: "Persisted replay audit outcome regression"
      contains: "AiToolCallOutcome.IDEMPOTENT_REPLAY"
  key_links:
    - from: "MutationCommitCoordinator replay path"
      to: "AuditWriter.writeToolCall"
      via: "AiToolCallOutcome.IDEMPOTENT_REPLAY persisted in AiAuditEvent.outcome"
      pattern: "IDEMPOTENT_REPLAY"
---

<objective>
Close the replay-audit durability verification gap. `IDEMPOTENT_REPLAY` must be observable on persisted `AiAuditEvent` rows on strict databases, not only in the mutation tool return DTO.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-mutation-capable-built-in-tools/11-VERIFICATION.md
@.planning/phases/11-mutation-capable-built-in-tools/11-REVIEW.md
@.planning/phases/11-mutation-capable-built-in-tools/11-07C-PLAN.md
@.planning/phases/11-mutation-capable-built-in-tools/11-10-PLAN.md
@.planning/phases/11-mutation-capable-built-in-tools/11-11-PLAN.md

@ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallOutcome.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiAuditEvent.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/tools/mutation/MutationCommitCoordinator.java
@ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/030-ai-audit-event.xml
@ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog.xml
@ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/BuiltInMutationToolsIdempotencyReplayTest.java
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Widen persisted audit outcome metadata</name>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiAuditEvent.java,
    ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/071-widen-ai-audit-outcome.xml
  </files>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallOutcome.java
    - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiAuditEvent.java
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/030-ai-audit-event.xml
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog.xml
  </read_first>
  <action>
1. In `AiAuditEvent`, change the outcome column annotation from `@Column(name = "OUTCOME", length = 16)` to exactly `@Column(name = "OUTCOME", length = 32)`.
2. Add `ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/071-widen-ai-audit-outcome.xml`.
3. The new changelog must contain one changeset with id `071-widen-ai-audit-outcome` and author `ai-agent`.
4. The changeset must run:
   `<modifyDataType tableName="AI_AGENT_AUDIT_EVENT" columnName="OUTCOME" newDataType="varchar(32)"/>`.
5. Do not edit the historical `030-ai-audit-event.xml` create-table width except if a local Liquibase convention explicitly requires baseline DDL updates for new installs. If you do edit the baseline, the new `071` widening changelog must still exist for already-migrated databases.
6. Do not add a manual include for `071-widen-ai-audit-outcome.xml`; `ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog.xml` already uses `<includeAll path="/com/vn/agent/liquibase/agentstore-changelog" errorIfMissingOrEmpty="false"/>`.
  </action>
  <verify>
    <automated>./gradlew -p ai-agent :ai-agent:compileJava</automated>
  </verify>
  <acceptance_criteria>
    - `AiAuditEvent.java` contains `@Column(name = "OUTCOME", length = 32)`.
    - `071-widen-ai-audit-outcome.xml` exists under `ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/`.
    - `071-widen-ai-audit-outcome.xml` contains `id="071-widen-ai-audit-outcome"`.
    - `071-widen-ai-audit-outcome.xml` contains `tableName="AI_AGENT_AUDIT_EVENT"`.
    - `071-widen-ai-audit-outcome.xml` contains `columnName="OUTCOME"`.
    - `071-widen-ai-audit-outcome.xml` contains `newDataType="varchar(32)"`.
    - `./gradlew -p ai-agent :ai-agent:compileJava` exits 0.
  </acceptance_criteria>
  <done>Audit outcome persistence can store all Phase 11 outcome ids, including IDEMPOTENT_REPLAY.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Persisted replay audit regression</name>
  <files>
    ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/BuiltInMutationToolsIdempotencyReplayTest.java
  </files>
  <read_first>
    - ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/BuiltInMutationToolsIdempotencyReplayTest.java
    - ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/MutationToolTestContext.java
    - ai-agent/ai-agent/src/main/java/com/vn/agent/tools/mutation/MutationCommitCoordinator.java
    - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiAuditEvent.java
    - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallOutcome.java
  </read_first>
  <action>
1. Extend `BuiltInMutationToolsIdempotencyReplayTest.sameKeySameAttributes_returnsIdempotentReplayWithSameEntityId_oneDedupRow`.
2. After the second call returns `IDEMPOTENT_REPLAY`, query persisted `AiAuditEvent` rows from `UnconstrainedDataManager` with:
   `select a from ai_AiAuditEvent a where a.eventName = :n and a.argumentsJson like :arg order by a.startedAt desc`.
   Use parameters `n = "create_record"` and `arg = "%" + idempotencyKey + "%"`.
3. Assert exactly two matching rows exist for the idempotency key.
4. Assert the row outcomes contain exactly `AiToolCallOutcome.SUCCESS` and `AiToolCallOutcome.IDEMPOTENT_REPLAY`.
5. Assert the replay audit row has `getOutcomeRaw()` equal to the literal string `"IDEMPOTENT_REPLAY"` so the test proves the 17-character value is persisted, not only enum-mapped in memory.
6. Extend `@AfterEach cleanRows()` to remove matching `AiAuditEvent` rows for this test's `idempotencyKey` after fixture and intent cleanup.
7. Keep the test non-transactional; do not add `@Transactional`, because replay must be observed after the first invocation returns and commits its idempotency reservation.
  </action>
  <verify>
    <automated>./gradlew -p ai-agent :ai-agent:test --tests "com.vn.agent.tools.mutation.BuiltInMutationToolsIdempotencyReplayTest"</automated>
  </verify>
  <acceptance_criteria>
    - `BuiltInMutationToolsIdempotencyReplayTest.java` imports or references `AiAuditEvent`.
    - The replay test contains `AiToolCallOutcome.IDEMPOTENT_REPLAY`.
    - The replay test contains `getOutcomeRaw()`.
    - The replay test asserts two audit rows for the idempotency key.
    - The replay test does not contain `@Transactional`.
    - The replay test cleanup removes `AiAuditEvent` rows associated with the test idempotency key.
    - `./gradlew -p ai-agent :ai-agent:test --tests "com.vn.agent.tools.mutation.BuiltInMutationToolsIdempotencyReplayTest"` exits 0.
  </acceptance_criteria>
  <done>TEST-11 now proves both replay behavior and persisted replay audit observability.</done>
</task>

</tasks>

<verification>
Run:
1. `./gradlew -p ai-agent :ai-agent:test --tests "com.vn.agent.tools.mutation.BuiltInMutationToolsIdempotencyReplayTest"`
2. `./gradlew -p ai-agent :ai-agent:test`
3. JetBrains MCP `get_file_problems(errorsOnly=false)` on:
   - `ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiAuditEvent.java`
   - `ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/BuiltInMutationToolsIdempotencyReplayTest.java`
</verification>

<success_criteria>
- The verifier's replay-audit durability gap is closed: `IDEMPOTENT_REPLAY` fits the Java column metadata and Liquibase schema.
- A strict database can persist a replay audit row without truncating or rejecting the outcome.
- The idempotency replay test proves a persisted `AiAuditEvent` row with raw outcome `IDEMPOTENT_REPLAY`.
- No mutation callback behavior, idempotency reservation semantics, or audit event names change.
</success_criteria>
