---
phase: 13.1-chat-attachments-rightpane-and-persistent-context
plan: 07
type: execute
wave: 5
depends_on: [04, 05]
files_modified:
  - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/CrmStyleLayoutTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/NoticeRenderTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/SurfaceMountingTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/liquibase/LiquibaseSchemaTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/i18n/LocaleParityTest.java
autonomous: true
requirements:
  - UI-01-test
  - UX-01-render-test
  - SCHEMA-01-test
  - I18N-01-test
  - CONTRACT-01-test
tags:
  - tests
  - ui-test
  - liquibase
  - i18n
must_haves:
  truths:
    - "CrmStyleLayoutTest opens ChatView and ChatDialogView; asserts vaadin-split-layout with splitterPosition≈68 and the documented right-pane child ids in BOTH surfaces"
    - "NoticeRenderTest seeds a NOTICE AiMessage row, opens the chat panel, and asserts the message-list DOM contains a vaadin-message.attachment-event element with the formatted ledger text"
    - "SurfaceMountingTest toggles AiUiSettings.surface=FULL_ROUTE↔HEADER_BUTTON and asserts both surfaces render the new layout; git diff against the 5 Phase 12 contract files asserts zero modifications"
    - "LiquibaseSchemaTest boots the application context and asserts MESSAGE_ID + INJECTED_AT columns + their indexes + FK_AI_TASK_FILE__ON_MESSAGE are absent; FK_AI_TASK_FILE__ON_CONVERSATION is present"
    - "LocaleParityTest asserts every chatView.attachments.* key (and AiMessageRole.NOTICE caption) is present in BOTH messages_en.properties and messages_vi.properties"
  artifacts:
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/CrmStyleLayoutTest.java"
      provides: "REQ-1 UI-01 DOM regression for both surfaces"
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/NoticeRenderTest.java"
      provides: "REQ-5 UX-01 DOM regression for inline NOTICE divider"
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/SurfaceMountingTest.java"
      provides: "REQ-7 CONTRACT-01 zero-diff regression for the 5 Phase 12 contract files + functional regression in both surfaces"
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/liquibase/LiquibaseSchemaTest.java"
      provides: "REQ-6 SCHEMA-01 column / index / FK absence regression"
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/i18n/LocaleParityTest.java"
      provides: "REQ-9 I18N-01 key-set diff regression"
  key_links:
    - from: "CrmStyleLayoutTest"
      to: "chat-panel-fragment.xml splitterPosition + slot ids"
      via: "@UiTest Vaadin TestBench / Karibu DOM assertion"
      pattern: "splitterPosition.*68"
    - from: "LiquibaseSchemaTest"
      to: "AI_TASK_FILE schema after Liquibase 100"
      via: "JDBC information_schema query"
      pattern: "FK_AI_TASK_FILE__ON_MESSAGE"
---

<objective>
Land the UI / schema / locale / contract test cluster: CrmStyleLayoutTest (REQ-1), NoticeRenderTest (REQ-5 render side), SurfaceMountingTest (REQ-7), LiquibaseSchemaTest (REQ-6), LocaleParityTest (REQ-9). Together with Plan 13.1-06 these close the 9-test SPEC matrix. After this plan the phase is gate-ready.

Purpose: separate UI tests + schema integration test + locale parity sweep into a single plan focused on Wave-3/4 verification surfaces (post-XML + post-Java rewire). Schema test boots the full Spring context so its prerequisites are Plans 01-05 having all landed cleanly.

Output: 5 new test classes, all green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-SPEC.md
@.planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-RESEARCH.md
@.planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-PATTERNS.md
@.planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-04-SUMMARY.md
@.planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-05-SUMMARY.md

<interfaces>
<!-- UI test infrastructure to use. -->

Phase 13.1 ships under Jmix 2.8 with `@UiTest` (Jmix Flow UI test scaffold) for in-memory Vaadin Flow UI assertions. Locate an existing `@UiTest` example in the repo via `Grep -rn "@UiTest"` to confirm scaffolding (test base class, ApplicationContext bring-up). Phase 12 likely shipped the closest analog at `ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/...`.

If the repo prefers Karibu UI Test or Vaadin TestBench scaffolding, use whichever is already in use (do NOT introduce a new framework). The DOM assertions can be performed via the chosen framework's component-tree query API.

For LiquibaseSchemaTest, use a `@SpringBootTest` + Spring `JdbcTemplate` injected against the agentstore DataSource. Query patterns:
```sql
SELECT column_name FROM information_schema.columns WHERE table_name = 'AI_TASK_FILE' AND column_name IN ('MESSAGE_ID','INJECTED_AT')
SELECT constraint_name FROM information_schema.table_constraints WHERE table_name = 'AI_TASK_FILE' AND constraint_type = 'FOREIGN KEY'
SELECT index_name FROM information_schema.statistics WHERE table_name = 'AI_TASK_FILE'  -- MySQL/H2
```
For PostgreSQL the `pg_indexes`/`pg_constraint` catalog queries differ; use the dialect already in use (check `application.properties` `spring.datasource.url` for the driver).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CrmStyleLayoutTest + NoticeRenderTest + SurfaceMountingTest (UI tests)</name>
  <read_first>
    - .planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-SPEC.md (REQ-1 + REQ-5 render + REQ-7 acceptance)
    - .planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-RESEARCH.md (Phase Requirements → Test Map; UI test entries)
    - .planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-PATTERNS.md (Test files section — `@UiTest` analog)
    - D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/fragment/chat-panel-fragment.xml (post Plan 13.1-04 — confirm slot ids the test asserts on)
    - D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/ChatView.java (DO NOT MODIFY — test reads it to verify FULL_ROUTE surface)
    - D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/ChatDialogView.java (DO NOT MODIFY — test opens the dialog to verify HEADER_BUTTON surface)
    - D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/ChatSurfaceMounter.java (DO NOT MODIFY — SurfaceMountingTest asserts zero diff)
    - Locate an existing `@UiTest` analog in the repo. Run `grep -rn "@UiTest" ai-agent/ai-agent/src/test/java` and `grep -rn "@UiTest" jmix-app/src/test/java`. Read the analog's class header to copy the test scaffolding (ApplicationContext bring-up, test base class).
  </read_first>
  <files>
    ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/CrmStyleLayoutTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/NoticeRenderTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/SurfaceMountingTest.java
  </files>
  <action>
    Use the discovered `@UiTest` scaffold (whatever Phase 12 shipped — likely class-level `@UiTest` annotation + `@Autowired ViewNavigators` + a `User` setup helper).

    **A. CrmStyleLayoutTest:**
       - **Test `chatViewRendersCrmStyleSplitWithRightPane`:**
         1. Navigate to `ChatView` via `ViewNavigators`.
         2. Locate the rendered fragment via the test framework's component-tree query.
         3. Assert the root contains a Vaadin `vaadin-split-layout` element. Read its `splitterPosition` style/property and assert it equals `68` (or the documented value).
         4. Assert the right-pane vbox has id `attachmentsPanel` and contains children with ids `attachmentsTitle`, `attachmentsEmptyState`, `attachmentsGridLayout`, `taskFileUpload`.
         5. Assert NO descendants exist with ids `attachRow` or `attachButton` (chip-strip absent).
       - **Test `chatDialogViewRendersCrmStyleSplitWithRightPane`:**
         Same assertions as above but after opening the chat from the header-button surface (`ChatDialogView`). Use the test framework's dialog-open helper.

    **B. NoticeRenderTest:**
       - Setup: seed a conversation + an `AiMessage(role=NOTICE, content="[admin] added attachment \"test.xlsx\"")` via `UnconstrainedDataManager.save`.
       - Navigate to `ChatView` and bind to the seeded conversation id (use whatever path `ChatPanelFragment.setConversationId(...)` exposes — call it directly from the test or via the navigation parameter).
       - Locate `messageListSlot` in the rendered DOM. Assert its children include at least one `<vaadin-message>` element with class `attachment-event` and `text` (or innerText) containing the literal `[admin] added attachment "test.xlsx"`.
       - Assert NO `MessageListItem` instances exist as children (post-Pitfall-7-Option-A migration).

    **C. SurfaceMountingTest:**
       - **Test `bothSurfacesMountReshapedFragment`:**
         1. Set `AiUiSettings.surface=FULL_ROUTE` via `UnconstrainedDataManager.save` (pre-test fixture).
         2. Navigate to ChatView; assert `attachmentsPanel` slot id present.
         3. Set `AiUiSettings.surface=HEADER_BUTTON`. Open the dialog. Assert `attachmentsPanel` slot id present in the dialog's mounted fragment.
       - **Test `phase12ContractFilesUnchanged` (process-level):**
         Use a `@Test` that runs `git diff --name-only origin/main -- <5-files>` via `ProcessBuilder` and asserts empty output. Five files: `ChatSurfaceMounter.java`, `AiUiSettingsService.java`, `AiUiSettings.java`, `ChatView.java`, `ChatDialogView.java`. If the repo's CI does not preserve git history in the test environment, fall back to a static-asserter that hashes each file's bytes against a recorded baseline; surface the file paths via test resource and compare. Choose the simpler path that is sustainable for CI.
       - If `git` is not invokable from a unit test in this environment, replace the process check with a `static @Test` that pulls the 5 file paths and computes their SHA-256 hashes; fail the test if the hash differs from a frozen reference set checked in alongside the test class. Pin reference hashes after the first green run.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.CrmStyleLayoutTest" --tests "com.vn.agent.view.chat.NoticeRenderTest" --tests "com.vn.agent.view.chat.SurfaceMountingTest"</automated>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/CrmStyleLayoutTest.java' -Pattern 'splitterPosition|attachmentsPanel|attachmentsGridLayout|attachmentsEmptyState|taskFileUpload').Count -ge 5"</automated>
  </verify>
  <done>
    Three UI tests compile and pass. Both surfaces render the new layout; NOTICE row renders as a non-bubble divider element; the 5 Phase 12 contract files are confirmed unmodified.
  </done>
</task>

<task type="auto">
  <name>Task 2: LiquibaseSchemaTest + LocaleParityTest</name>
  <read_first>
    - .planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-SPEC.md (REQ-6 SCHEMA-01 acceptance + REQ-9 I18N-01 acceptance)
    - .planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-RESEARCH.md (Pitfall 10 — `FK_AI_TASK_FILE__ON_CONVERSATION` survives; assertion on `referential_constraints`)
    - .planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-PATTERNS.md (LiquibaseSchemaTest entry; `messages_en.properties` + `messages_vi.properties` parity rule)
    - D:/DTH/ai-agent-core/jmix-app/src/main/resources/application.properties (find the agentstore datasource `spring.datasource.<...>.url` to determine the SQL dialect / catalog format)
    - D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties (post Plan-04 — full key list)
    - D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties (post Plan-04 — full key list)
  </read_first>
  <files>
    ai-agent/ai-agent/src/test/java/com/vn/agent/liquibase/LiquibaseSchemaTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/i18n/LocaleParityTest.java
  </files>
  <action>
    **A. LiquibaseSchemaTest.java:**
       - `@SpringBootTest` (boots the full app context, runs Liquibase 010..100 against the configured datasource).
       - Inject the agentstore `DataSource` (or a `JdbcTemplate` qualified for agentstore — confirm via `application.properties` for the agentstore datasource bean name; Phase 13 used `agentstoreDataSource`).
       - **Test `messageIdAndInjectedAtColumnsAbsent`:**
         Query: `SELECT column_name FROM information_schema.columns WHERE table_schema = current_schema() AND lower(table_name) = 'ai_task_file' AND lower(column_name) IN ('message_id','injected_at')` (PostgreSQL idiomatic; if the project is on H2, use H2's `INFORMATION_SCHEMA.COLUMNS` which has the same shape). Assert the returned list is empty.
       - **Test `messageFkAbsent_conversationFkPresent`:**
         Query `information_schema.referential_constraints` (or the dialect equivalent — for H2 it's `INFORMATION_SCHEMA.CONSTRAINTS WHERE CONSTRAINT_TYPE='REFERENTIAL'`). Filter to `table_name='AI_TASK_FILE'`. Assert the constraint name set contains `FK_AI_TASK_FILE__ON_CONVERSATION` and does NOT contain `FK_AI_TASK_FILE__ON_MESSAGE`. Use case-insensitive comparison.
       - **Test `messageIndexesAbsent`:**
         Query `information_schema.statistics` (MySQL/H2) or `pg_indexes` (PostgreSQL). Assert `IDX_AI_TASK_FILE__ON_MESSAGE` and `IDX_AI_TASK_FILE__INJECTED_AT` are absent. Case-insensitive.
       - Use a `try { ... } catch (DataAccessException) { ... }` to gracefully degrade across SQL dialects: try the PostgreSQL form first, fall back to the H2 form. Log which dialect succeeded so operators see clear test output.

    **B. LocaleParityTest.java:**
       - Plain JUnit 5 (no Spring context needed — read the property files via classpath). Use `Properties.load(InputStream)` for both bundles.
       - **Test `enAndViBundlesContainEveryNewChatViewAttachmentsKey`:**
         The expected key set (13 keys per Plan 13.1-04 + 1 from Plan 13.1-01):
         ```java
         Set<String> required = Set.of(
             "chatView.attachments.title",
             "chatView.attachments.uploadText",
             "chatView.attachments.dropLabel",
             "chatView.attachments.emptyState",
             "chatView.attachments.notice",
             "chatView.attachments.budgetExceeded",
             "chatView.attachments.action.download",
             "chatView.attachments.action.delete",
             "chatView.attachments.deleteConfirm.title",
             "chatView.attachments.deleteConfirm.message",
             "chatView.attachments.deleteConfirm.confirm",
             "chatView.attachments.deleteConfirm.cancel",
             "chatView.attachments.missingFileName",
             "com.vn.agent.entity/AiMessageRole.NOTICE"
         );
         ```
         For each key, assert `enBundle.containsKey(key) && viBundle.containsKey(key)` and assert neither value is blank. Use AssertJ `assertThat(en.keySet()).containsAll(required)` and same for vi.
       - **Test `enAndViBundlesShareSameKeySet (parity)`:**
         Sanity-check that the symmetric difference between the two bundles' key sets is empty for the `chatView.attachments.*` namespace. (Other namespaces may legitimately differ for legacy reasons; restrict the assertion to keys with prefix `chatView.attachments.`.)
       - File paths to load (classpath):
         * `com/vn/agent/messages_en.properties`
         * `com/vn/agent/messages_vi.properties`

    Implements REQ-6 + REQ-9 end-to-end.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.liquibase.LiquibaseSchemaTest" --tests "com.vn.agent.i18n.LocaleParityTest"</automated>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/test/java/com/vn/agent/liquibase/LiquibaseSchemaTest.java' -Pattern 'FK_AI_TASK_FILE__ON_MESSAGE|FK_AI_TASK_FILE__ON_CONVERSATION|IDX_AI_TASK_FILE__INJECTED_AT|MESSAGE_ID|INJECTED_AT').Count -ge 5"</automated>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/test/java/com/vn/agent/i18n/LocaleParityTest.java' -Pattern 'chatView\.attachments').Count -ge 1"</automated>
  </verify>
  <done>
    Both tests ship and run green. SchemaTest covers the 5 deletion targets + the surviving FK. LocaleParityTest covers all 14 required keys (13 attachments + 1 enum caption) in both bundles.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `@UiTest` Spring context | Tests run as system identity; ChatView / ChatDialogView are mounted in-memory. |
| `LiquibaseSchemaTest` JDBC query | Reads the test datasource's `information_schema` — no production data touched. |
| `LocaleParityTest` classpath read | Pure file-read; no side-effects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.1-24 | Repudiation | UI test passes by asserting the wrong DOM element | mitigate | Tests assert specific Vaadin component ids that match the SPEC verbatim (`attachmentsPanel`, `splitterPosition`, etc.). The id-set is the contract. |
| T-13.1-25 | Tampering | Schema test green on a stale DB snapshot (Liquibase 100 not applied) | mitigate | Test boots the full Spring context, which forces Liquibase to run on the test schema. If Liquibase 100 is missing or fails, the boot itself fails — test never reaches the assertions. |
| T-13.1-26 | Information Disclosure | LocaleParityTest leaks an unrelated key set in the failure message | accept | Test failure messages list only `chatView.attachments.*` keys; no PII / secrets in the property file. |
</threat_model>

<verification>
- `./gradlew :ai-agent:ai-agent:test --tests "*CrmStyleLayoutTest*" --tests "*NoticeRenderTest*" --tests "*SurfaceMountingTest*" --tests "*LiquibaseSchemaTest*" --tests "*LocaleParityTest*"` → all green.
- Combined with Plan 13.1-06 results, all 9 SPEC-required tests are green.
</verification>

<success_criteria>
1. CrmStyleLayoutTest covers both ChatView (FULL_ROUTE) and ChatDialogView (HEADER_BUTTON).
2. NoticeRenderTest asserts inline non-bubble divider text matches the formatted ledger string.
3. SurfaceMountingTest asserts both surfaces still mount correctly + the 5 Phase 12 contract files have zero diff.
4. LiquibaseSchemaTest covers `MESSAGE_ID` / `INJECTED_AT` columns + their FK + indexes are absent; `FK_AI_TASK_FILE__ON_CONVERSATION` is present.
5. LocaleParityTest covers all 14 required keys × 2 bundles.
6. `./gradlew test` (full module) passes.
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-07-SUMMARY.md` documenting:
- Test class file paths
- DOM ids asserted by CrmStyleLayoutTest
- LiquibaseSchemaTest dialect-detection branch
- LocaleParityTest required-keys list
- Final `./gradlew :ai-agent:ai-agent:test` result line counts
</output>
