---
phase: 13.1-chat-attachments-rightpane-and-persistent-context
plan: 06
type: execute
wave: 5
depends_on: [02, 03]
files_modified:
  - ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/AiTaskFileMediaResolverIntegrationTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/AiTaskFileCleanupJobTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/AiTaskFileNoVectorStoreInvocationTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/PerTurnMediaInjectionTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/BudgetCapTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/TtlConfigTest.java
  - ai-agent/ai-agent/src/test/java/com/vn/agent/orchestration/NoticeFilterTest.java
autonomous: true
requirements:
  - RES-01-test
  - AUDIT-01-test
  - LIFE-01-test
  - UX-01-filter-test
  - TEST-16-PORT
tags:
  - tests
  - integration
  - resolver
  - audit
  - lifecycle
  - notice-filter
must_haves:
  truths:
    - "PerTurnMediaInjectionTest (TEST-18): 3 sequential turns over the same xlsx → all 3 outbound user prompts contain Media whose bytes equal the original"
    - "BudgetCapTest: 11×1KB files with default caps → resolver returns 10 most-recent + emits 1 task_file_budget_exceeded audit row with the documented argumentsJson"
    - "BudgetCapTest sentinel branch: caps=-1 → returns all 11 + zero audit rows"
    - "TtlConfigTest: default ttlSeconds=86400; sentinel ttl-seconds=-1 skips cleanup-job purge; FK cascade still removes rows on conversation delete"
    - "NoticeFilterTest: NOTICE rows survive ProjectingChatMemoryRepository.saveAll wipe; outbound prompt contains zero NOTICE-derived messages"
    - "AiTaskFileNoVectorStoreInvocationTest widened scope still passes (TEST-16 invariant)"
    - "Existing AiTaskFileMediaResolverIntegrationTest + AiTaskFileCleanupJobTest updated for ttl-seconds=86400 and resolveActive contract"
  artifacts:
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/PerTurnMediaInjectionTest.java"
      provides: "TEST-18 multi-turn media injection regression"
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/BudgetCapTest.java"
      provides: "Cap + sentinel + audit-row regression"
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/TtlConfigTest.java"
      provides: "TTL default + sentinel + FK-cascade regression"
    - path: "ai-agent/ai-agent/src/test/java/com/vn/agent/orchestration/NoticeFilterTest.java"
      provides: "ProjectingChatMemoryRepository.saveAll NOTICE-survival regression + LLM-prompt absence assertion"
  key_links:
    - from: "BudgetCapTest"
      to: "AiTaskFileMediaResolver.resolveActive overflow path"
      via: "@SpringBootTest + Jackson parse of AI_AGENT_AUDIT_EVENT.argumentsJson"
      pattern: "task_file_budget_exceeded"
    - from: "TtlConfigTest"
      to: "AiTaskFileCleanupJob sentinel branch"
      via: "@TestPropertySource ai-agent.task-file.ttl-seconds=-1"
      pattern: "ttl-seconds=-1"
---

<objective>
Land the resolver / lifecycle / audit / notice-filter test wave for Phase 13.1: 4 new `@SpringBootTest` integration tests (PerTurnMediaInjection / BudgetCap / TtlConfig / NoticeFilter) PLUS the property-rename sweep on the 3 existing Phase 13 task-file tests left red after Plan 13.1-01 / 02 / 03. Plan 13.1-07 covers UI + schema + locale + surface-mounting tests separately.

Purpose: prove the resolver-side contracts (per-turn-all, budget cap, sentinel TTL, notice filter) hold end-to-end before the UI wave is verified. Splits the 9-test SPEC matrix into a logically cohesive backend-first cluster (this plan) + a UI/schema/locale cluster (Plan 13.1-07) to keep each plan within the ~50% context budget.

Output: 4 new test classes + 3 existing test classes patched for new property names + the `AiTaskFileNoVectorStoreInvocationTest` static scope widened to include the new package-info paragraph (Phase 13 invariant preserved).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-SPEC.md
@.planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-RESEARCH.md
@.planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-PATTERNS.md
@.planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-02-SUMMARY.md
@.planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-03-SUMMARY.md

<interfaces>
<!-- Phase 13 test scaffolding to follow as analog. -->

From `AiTaskFileMediaResolverIntegrationTest.java` (Phase 13 — closest analog for test shape):
- Uses `@SpringBootTest` with `properties = {"ai-agent.task-file.ttl=PT1H"}` (CHANGE → `ai-agent.task-file.ttl-seconds=3600`).
- Seeds AiConversation + AiTaskFile rows via `UnconstrainedDataManager.save` (memory `feedback_jmix_unconstrained_for_system_writes`).
- `@AfterEach` cleanup of seeded ids.

From `BuiltInMutationToolsAuditArgumentsTest.java` (Phase 11 analog for audit-row JSON parse via Jackson):
```java
List<AiAuditEvent> events = unconstrainedDataManager.load(AiAuditEvent.class)
    .query("select e from ai_AiAuditEvent e where e.eventName = :name order by e.createdDate desc")
    .parameter("name", "task_file_budget_exceeded")
    .store("agentstore")    // memory feedback_jmix_loadvalue_store does NOT apply; .load(Class).query is fine — but if loadValues is used .store("agentstore") is mandatory
    .list();
```

From SPEC §3 — argumentsJson MUST parse to `{conversationId, totalRows, totalBytes, keptRows, keptBytes, droppedRows, droppedBytes, perTurnMaxFiles, perTurnMaxTotalBytes}`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Property-rename sweep on existing tests + widened TEST-16 scope</name>
  <read_first>
    - .planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-RESEARCH.md (Pitfall 1 — 3 test files reference `ai-agent.task-file.ttl=PT1H`)
    - .planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-PATTERNS.md (`AiTaskFileProperties.java` Pitfalls list — 3 tests to update; "TEST-16 No-VectorStore Constraint (widened scope)" section — add `AiTaskFileCardFragmentRenderer.java` + `chat-panel-fragment.xml` to the static-grep file scope)
    - D:/DTH/ai-agent-core/ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/AiTaskFileMediaResolverIntegrationTest.java (full file)
    - D:/DTH/ai-agent-core/ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/AiTaskFileCleanupJobTest.java (full file)
    - D:/DTH/ai-agent-core/ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/AiTaskFileNoVectorStoreInvocationTest.java (full file — confirms `@SpringBootTest` properties + the runtime VectorStore-count assertion)
    - D:/DTH/ai-agent-core/ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/TaskFileNoVectorStoreSourceScannerTest.java (the static-grep test — widen file scope here)
  </read_first>
  <files>
    ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/AiTaskFileMediaResolverIntegrationTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/AiTaskFileCleanupJobTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/AiTaskFileNoVectorStoreInvocationTest.java
  </files>
  <action>
    1. Update `AiTaskFileMediaResolverIntegrationTest`:
       - REPLACE every property-key reference `ai-agent.task-file.ttl=PT1H` with `ai-agent.task-file.ttl-seconds=3600` (1 hour in seconds — keep the same effective TTL so seeded rows still expire predictably).
       - REPLACE every call to `taskFileMediaResolver.resolvePending(convId)` with `taskFileMediaResolver.resolveActive(convId)`.
       - REPLACE every reference to `Resolved.taskFileIds()` with the equivalent flow-aware path. If a test asserts on `taskFileIds`, replace with assertions on `Resolved.media()` size + content. If the test was specifically targeting the markInjected stamp, DELETE that test method (it tested behaviour that no longer exists post-Phase-13.1) and ADD a one-line `@Disabled("Phase 13.1: markInjected removed — superseded by PerTurnMediaInjectionTest")` placeholder comment in the SUMMARY rationale.
       - REPLACE every reference to `markInjected` (mock or assertion) with assertions that the bytes are present in `Resolved.media().get(0).getData()`.

    2. Update `AiTaskFileCleanupJobTest`:
       - REPLACE `ai-agent.task-file.ttl=PT1H` with `ai-agent.task-file.ttl-seconds=3600` everywhere it appears.
       - The TTL-related test cases continue to seed rows with `expiresAt` set explicitly via `Metadata.create` — those don't rely on the property value directly and need no logic change.

    3. Update `AiTaskFileNoVectorStoreInvocationTest`:
       - REPLACE `ai-agent.task-file.ttl=PT1H` with `ai-agent.task-file.ttl-seconds=3600`.
       - The runtime test (chat turn with attached pdf → assert pgvector count unchanged) needs no behaviour change.

    4. Widen TEST-16 static-grep scope in `TaskFileNoVectorStoreSourceScannerTest`:
       - Locate the file-set list that drives the static grep.
       - ADD the absolute paths:
         * `ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/AiTaskFileCardFragmentRenderer.java`
         * `ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/fragment/chat-panel-fragment.xml`
       - Keep all existing entries.
       - The allowlist behaviour (only `package-info.java` may name the forbidden tokens, and only inside JavaDoc) is unchanged.

    Implements RESEARCH Pitfall 1 + TEST-16-PORT widening.
  </action>
  <verify>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/AiTaskFileMediaResolverIntegrationTest.java','ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/AiTaskFileCleanupJobTest.java','ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/AiTaskFileNoVectorStoreInvocationTest.java' -Pattern 'ai-agent\.task-file\.ttl=PT').Count -eq 0"</automated>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/AiTaskFileMediaResolverIntegrationTest.java' -Pattern 'resolvePending|markInjected').Count -eq 0"</automated>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/TaskFileNoVectorStoreSourceScannerTest.java' -Pattern 'AiTaskFileCardFragmentRenderer|chat-panel-fragment\.xml').Count -ge 2"</automated>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.taskfile.AiTaskFileMediaResolverIntegrationTest" --tests "com.vn.agent.taskfile.AiTaskFileCleanupJobTest" --tests "com.vn.agent.taskfile.AiTaskFileNoVectorStoreInvocationTest" --tests "com.vn.agent.taskfile.TaskFileNoVectorStoreSourceScannerTest"</automated>
  </verify>
  <done>
    All 4 referenced tests run green with the new property name and `resolveActive` API. TaskFileNoVectorStoreSourceScannerTest covers the widened file set.
  </done>
</task>

<task type="auto">
  <name>Task 2: 4 new SpringBoot tests — PerTurnMediaInjection / BudgetCap / TtlConfig / NoticeFilter</name>
  <read_first>
    - .planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-SPEC.md (REQ-2 RES-01 — TEST-18 acceptance; REQ-3 AUDIT-01 acceptance; REQ-4 LIFE-01 acceptance; REQ-5 UX-01 filter acceptance)
    - .planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-RESEARCH.md (§Phase Requirements → Test Map; §Wave 0 Gaps lists each test path; §Validation Architecture)
    - .planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-PATTERNS.md (Test files section — pattern sources)
    - D:/DTH/ai-agent-core/ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/AiTaskFileMediaResolverIntegrationTest.java (post Task-1 — pattern source for SpringBoot scaffolding + seeding)
    - D:/DTH/ai-agent-core/ai-agent/ai-agent/src/test/java/com/vn/agent/tools/mutation/BuiltInMutationToolsAuditArgumentsTest.java (Phase 11 — pattern for parsing audit-row argumentsJson via Jackson)
    - D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/java/com/vn/agent/taskfile/AiTaskFileMediaResolver.java (post Plan 13.1-02 — final API surface)
    - D:/DTH/ai-agent-core/ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/ProjectingChatMemoryRepository.java (post Plan 13.1-03 — saveAll JPQL with NOTICE filter)
  </read_first>
  <files>
    ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/PerTurnMediaInjectionTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/BudgetCapTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/TtlConfigTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/orchestration/NoticeFilterTest.java
  </files>
  <action>
    Create 4 `@SpringBootTest` classes. Use `AiTaskFileMediaResolverIntegrationTest` as the structural analog (constructor, `@Autowired` injections, `UnconstrainedDataManager` for seeding, `@AfterEach` cleanup, `@Tag("integration")`). All seeding goes through `UnconstrainedDataManager.save(...)`.

    **A. `PerTurnMediaInjectionTest.java` (TEST-18, REQ-2):**
       - Class-level `@SpringBootTest` with `properties = {"ai-agent.task-file.ttl-seconds=86400", "ai-agent.task-file.per-turn-max-files=10", "ai-agent.task-file.per-turn-max-total-bytes=52428800"}`.
       - Inject `AiTaskFileMediaResolver`, `UnconstrainedDataManager`, `Metadata`, `FileStorageLocator`.
       - **Test `threeSequentialTurnsOverSameXlsxAllReceiveMedia`:**
         1. Seed an `AiConversation` for user `alice`.
         2. Read 100 KB of bytes from `src/test/resources/fixtures/sample.xlsx` (create the fixture if absent — copy a small valid xlsx already present in Phase 13 test resources; if none exists, generate via `Files.write` of `new byte[100_000]` with a `.xlsx` extension and contentType `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`).
         3. Save the bytes to FileStorage; create one `AiTaskFile` row pointing at the FileRef with `userUsername=alice`, `conversationId=<seeded>`, `expiresAt=now()+24h`.
         4. Loop `for (int turn = 0; turn < 3; turn++)`: call `resolver.resolveActive(conversationId)`, assert `result.media().size() == 1`, assert `Arrays.equals(result.media().get(0).getData().getInputStream().readAllBytes(), originalBytes)` (or whatever accessor `Media` exposes; if `getData()` returns `Resource`, read it via `Resource.getContentAsByteArray()`).
         5. Assert NO method named `markInjected` AND NO method named `loadPending` exists on `AiTaskFileRepository` AT ALL (any overload). Use iteration over `getDeclaredMethods()` so a renamed-overload cannot slip past:
            ```java
            java.lang.reflect.Method[] declared = AiTaskFileRepository.class.getDeclaredMethods();
            assertThat(java.util.Arrays.stream(declared).noneMatch(m -> m.getName().equals("markInjected")))
                .as("AiTaskFileRepository must have NO method named markInjected (any overload) — SPEC REQ-2")
                .isTrue();
            assertThat(java.util.Arrays.stream(declared).noneMatch(m -> m.getName().equals("loadPending")))
                .as("AiTaskFileRepository must have NO method named loadPending (any overload) — SPEC REQ-2")
                .isTrue();
            ```

    **B. `BudgetCapTest.java` (REQ-3):**
       - Class-level `@SpringBootTest` with default properties (caps 10 / 52428800).
       - Inject `AiTaskFileMediaResolver`, `UnconstrainedDataManager`, `Metadata`, `FileStorageLocator`, `ObjectMapper` (or `new ObjectMapper()` in-test).
       - **Test `eleven1KbFilesDropsOldestEmitsOneAuditRow`:**
         1. Seed conversation. Seed 11 AiTaskFile rows of 1 KB each with `createdDate` strictly increasing (file 0 oldest, file 10 newest). FileStorage entries created for each.
         2. Call `resolveActive(conversationId)`.
         3. Assert `result.media().size() == 10`.
         4. Assert `result.budgetExceeded() == true`.
         5. Query `select e from ai_AiAuditEvent e where e.eventName = 'task_file_budget_exceeded' order by e.createdDate desc` via `UnconstrainedDataManager` — assert exactly 1 row exists.
         6. Parse `event.getArgumentsJson()` via `ObjectMapper.readTree(...)`, assert keys: `conversationId == seededConvId`, `totalRows == 11`, `keptRows == 10`, `droppedRows == 1`, `keptBytes == 10240`, `droppedBytes == 1024`, `perTurnMaxFiles == 10`, `perTurnMaxTotalBytes == 52428800`.
       - **Test `sixTenMbFilesByteCapEnforcement`:**
         1. Seed 6 rows of 10 MB each (use sparse files / `byte[]` of right size).
         2. Call `resolveActive`. Assert `media().size() == 5`, `keptBytes == 52428800` in the audit row, `droppedRows == 1`.
       - **Test `sentinelDisablesCapsReturnsAllRowsZeroAuditRows`:** annotated with `@TestPropertySource(properties = {"ai-agent.task-file.per-turn-max-files=-1", "ai-agent.task-file.per-turn-max-total-bytes=-1"})` (or split into a nested `@SpringBootTest` class). Seed 11 rows. Assert `media().size() == 11`, `budgetExceeded() == false`, zero `task_file_budget_exceeded` audit rows.

    **C. `TtlConfigTest.java` (REQ-4):**
       - Three nested `@Nested` classes, each `@SpringBootTest` with different `properties = {...}`:
         a. `DefaultTtl` (no override): assert `taskFileProperties.getTtlSeconds() == 86400`.
         b. `SentinelMinusOneSkipsCleanup`: `properties = {"ai-agent.task-file.ttl-seconds=-1"}`. Seed an AiTaskFile row with `expiresAt=now()-1h` (ALREADY expired). Inject `AiTaskFileCleanupJob`, call `cleanupJob.deleteExpiredTaskFiles()` twice. Assert the row is still in the DB after both invocations. Inject the SLF4J logger via a `LogCaptor` (or trust the `sentinelLoggedOnce` semantics — assert `cleanupJob` field via reflection if needed). Acceptable if the assertion is row-existence only; logging is best-effort.
         c. `FkCascadeStillWorksUnderSentinel`: `properties = {"ai-agent.task-file.ttl-seconds=-1"}`. Seed conversation + AiTaskFile row. Delete the conversation via `unconstrainedDataManager.remove(conversation)`. Assert the AiTaskFile row is gone (FK `deleteCascade=true` from Liquibase 090 still works).

    **D. `NoticeFilterTest.java` (REQ-5):**
       - Class-level `@SpringBootTest`.
       - Inject `ProjectingChatMemoryRepository` (the unit-under-test), `UnconstrainedDataManager`, `Metadata`.
       - **Test `noticeRowsSurviveSaveAllProjection`:**
         1. Seed conversation. Seed 1 AiMessage(role=USER), 1 AiMessage(role=ASSISTANT), 1 AiMessage(role=NOTICE) all with strictly increasing seq.
         2. Build a Spring AI `List<Message>` containing only the USER+ASSISTANT pair (mimicking what the chat-memory advisor would project).
         3. Call `projectingChatMemoryRepository.saveAll(conversationId.toString(), messages)`.
         4. Query `select m from ai_AiMessage m where m.conversation.id = :cid` via UnconstrainedDataManager. Assert: USER + ASSISTANT may have been re-created (count == 2 of those roles), and the NOTICE row STILL exists (count == 1 of role NOTICE).
       - **Test `springAiPromptPathNeverContainsNoticeMessages`:**
         1. Seed conversation + USER + ASSISTANT + NOTICE rows.
         2. Inject `JdbcChatMemoryRepository` directly (Spring AI bean). Call `findByConversationId(conversationId.toString())`.
         3. Assert the returned `List<Message>` has exactly 0 messages whose `getMessageType()` corresponds to a NOTICE-style role. (NOTICE rows live only in `AI_AGENT_MESSAGE`, not `SPRING_AI_CHAT_MEMORY` — D-A2 — so the assertion may be vacuously true; ship the test as a regression guard against future schema drift.)

    Each test class follows the Phase 13 cleanup pattern: a `@AfterEach` method that clears every seeded id from agentstore + main store + FileStorage. Use `@Tag("integration")` so the tests are runnable individually via `./gradlew test --tests <class>`.
  </action>
  <verify>
    <automated>powershell -Command "@('PerTurnMediaInjectionTest','BudgetCapTest','TtlConfigTest','NoticeFilterTest') | ForEach-Object { if (-not (Test-Path \"ai-agent/ai-agent/src/test/java/com/vn/agent/taskfile/$_.java\") -and -not (Test-Path \"ai-agent/ai-agent/src/test/java/com/vn/agent/orchestration/$_.java\")) { throw \"Missing test file: $_\" } }"</automated>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.taskfile.PerTurnMediaInjectionTest" --tests "com.vn.agent.taskfile.BudgetCapTest" --tests "com.vn.agent.taskfile.TtlConfigTest" --tests "com.vn.agent.orchestration.NoticeFilterTest"</automated>
  </verify>
  <done>
    All 4 new test classes ship and pass with the documented assertions. Audit-row JSON shape parses correctly. Sentinel branches green on both BudgetCap and TtlConfig. NoticeFilter regression guards both the JPQL projection survival AND the Spring AI table separation.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test seeding via UnconstrainedDataManager | Tests run as system identity; `@AfterEach` cleanup ensures isolation between cases. |
| Audit-row read in BudgetCapTest | Reads `AI_AGENT_AUDIT_EVENT` via UnconstrainedDataManager; row written by the resolver under the seeded user identity. Test does not modify audit rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.1-22 | Tampering | Tests leave seeded rows behind, polluting downstream tests | mitigate | Each test ships an `@AfterEach` cleanup over its seeded id list; UnconstrainedDataManager performs the deletes idempotently. |
| T-13.1-23 | Information Disclosure | Test fixtures with real-looking data leak into snapshot artefacts | accept | Test fixtures are 100 KB / 1 KB synthetic byte arrays — no PII, no secrets. |
</threat_model>

<verification>
- `./gradlew :ai-agent:ai-agent:test --tests "*PerTurnMediaInjectionTest*" --tests "*BudgetCapTest*" --tests "*TtlConfigTest*" --tests "*NoticeFilterTest*" --tests "*AiTaskFile*Test*"` → all green.
- `./gradlew :ai-agent:ai-agent:test --tests "*TaskFileNoVectorStoreSourceScannerTest*"` → passes with widened scope.
</verification>

<success_criteria>
1. PerTurnMediaInjectionTest asserts 3 turns × identical Media bytes.
2. BudgetCapTest covers default cap, byte cap, and sentinel branch with audit-row JSON parse.
3. TtlConfigTest covers default, sentinel skip, FK cascade.
4. NoticeFilterTest covers saveAll projection survival + Spring AI prompt absence.
5. The 3 existing Phase 13 tests pass with `ai-agent.task-file.ttl-seconds=3600` and `resolveActive` API.
6. TaskFileNoVectorStoreSourceScannerTest scope widened to include the new card renderer + reshaped fragment XML; passes.
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-chat-attachments-rightpane-and-persistent-context/13.1-06-SUMMARY.md` documenting:
- Test class file paths + count of test methods per class
- Audit-row JSON shape verified by BudgetCapTest
- TTL sentinel branch confirmation
- TaskFileNoVectorStoreSourceScannerTest scope additions
- `./gradlew test` output line counts
</output>
