---
phase: 14-intent-driven-extraction-form-prefill
plan: 06
type: execute
wave: 4
depends_on: ["02", "04", "05"]
files_modified:
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/fragment/chat-panel-fragment.xml
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/StreamEventRenderer.java
  - ai-agent/ai-agent/src/main/resources/META-INF/resources/frontend/styles/ai-agent-chat.css
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties
autonomous: true
requirements:
  - EXTRACT-01
  - EXTRACT-07
tags:
  - jmix-views
  - ui
  - i18n
  - css
must_haves:
  truths:
    - "D-07 - intent picker is a Jmix radioButtonGroup with @Supply renderer, not hand-built toggles"
    - "D-08 - Auto is first/default, named intents are eligibility-filtered, row hides when none are eligible, and named send resets to Auto"
    - "D-09 - StreamEventRenderer never imports ViewNavigators; click delegates to OpenFormWithDraftHandler"
    - "D-10 - confirm control is inline in the assistant/message-list area as ai-agent-intent-confirm"
    - "D-11 - confirm row disables with draftExpired copy when the draft no longer exists"
    - "UI-SPEC - CSS appends only to ai-agent-chat.css and does not touch Phase 13.1 attachment selectors"
    - "Review HIGH - renderer reads StreamingEvent.ToolResult.payloadJson/toolName for prepare_form_draft and never parses human-readable summary for extraction"
    - "Review MEDIUM - malformed prepare_form_draft payload is a visible localized contract error, not a silent no-op"
  artifacts:
    - path: "ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/fragment/chat-panel-fragment.xml"
      provides: "<radioButtonGroup id=\"intentCardRow\"> between message list and input"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java"
      provides: "Intent row population, send reset, confirm row append"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/StreamEventRenderer.java"
      provides: "ToolResult payload detection without navigation"
---

<objective>
Add the Jmix chat UI for selecting a named intent and confirming a prepared draft. This plan modifies only the existing chat fragment substrate: descriptor radio group, controller population/send wiring, server-side confirm row append, renderer payload detection, CSS appendix, and bilingual message keys.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-intent-driven-extraction-form-prefill/14-SPEC.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-CONTEXT.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-UI-SPEC.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-RESEARCH.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-04-PLAN.md
@.planning/phases/14-intent-driven-extraction-form-prefill/14-05-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add intentCardRow XML and locale keys</name>
  <read_first>
    - `ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/fragment/chat-panel-fragment.xml`
    - `ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties`
    - `ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties`
    - UI-SPEC Copywriting Contract
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/fragment/chat-panel-fragment.xml,
    ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties,
    ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties
  </files>
  <action>
    1. Insert `<radioButtonGroup id="intentCardRow" classNames="ai-agent-intent-card-row" themeNames="horizontal" visible="false" width="100%" ariaLabel="msg:///chatView.intent.cardRow.ariaLabel"/>` between `messageListSlot` and `messageInputSlot`.
    2. Do not change the Phase 13.1 split layout, right-pane attachments, upload handler XML, or existing slot ids.
    3. Add all UI-SPEC Phase 14 message keys to both `messages_en.properties` and `messages_vi.properties`, including:
       - `chatView.intent.cardRow.ariaLabel`
       - `chatView.intent.auto.label`
       - `chatView.intent.auto.description`
       - `chatView.intent.confirmButton`
       - `chatView.intent.confirmButton.summary`
       - `chatView.intent.draftExpired`
       - `chatView.intent.permissionDenied`
       - `chatView.intent.draftPayloadInvalid`
       - plus reference customer keys if Plan 07 does not own them.
    4. Keep keys in the root message bundles, not per-view bundles.
  </action>
  <verify>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/fragment/chat-panel-fragment.xml' -Pattern '<radioButtonGroup id=\"intentCardRow\"').Count -eq 1"</automated>
    <automated>powershell -Command "$keys=@('chatView.intent.cardRow.ariaLabel','chatView.intent.auto.label','chatView.intent.auto.description','chatView.intent.confirmButton','chatView.intent.confirmButton.summary','chatView.intent.draftExpired','chatView.intent.permissionDenied','chatView.intent.draftPayloadInvalid'); foreach($f in @('ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties','ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties')){ foreach($k in $keys){ if(-not (Select-String -Path $f -Pattern ('^'+[regex]::Escape($k)+'=') -Quiet)){ throw \"$f missing $k\" } } }"</automated>
  </verify>
  <done>Descriptor and locale keys match the UI-SPEC contract.</done>
</task>

<task type="auto">
  <name>Task 2: Populate and render intent cards in ChatPanelFragment</name>
  <read_first>
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java`
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/extraction/IntentRegistry.java`
    - Jmix skill guidance for `@Supply` and `@Subscribe`
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java
  </files>
  <action>
    1. Add `@ViewComponent private RadioButtonGroup<IntentOption> intentCardRow;`.
    2. Inject `IntentRegistry` and `OpenFormWithDraftHandler`.
    3. Add `refreshIntentCardRow()` called from `onReady` and any conversation/user context refresh point if needed.
    4. Build `[Auto] + eligible intents`; if no named intents, set row invisible; otherwise set visible and default value to Auto.
    5. Add `@Supply(to = "intentCardRow", subject = "renderer")` returning a `ComponentRenderer` that creates Jmix/Vaadin card-shaped option content with selected class toggling.
    6. Add `@Subscribe("intentCardRow")` value-change handler to refresh selected styling if renderer needs it.
    7. Do not use raw `addValueChangeListener` for the XML-defined radio group.
  </action>
  <verify>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java' -Pattern '@Supply\\(to = \"intentCardRow\", subject = \"renderer\"\\)','@Subscribe\\(\"intentCardRow\"\\)','refreshIntentCardRow','intentCardRow.setVisible').Count -ge 4"</automated>
  </verify>
  <done>Intent radio group is populated and rendered through Jmix annotated handlers.</done>
</task>

<task type="auto">
  <name>Task 3: Send selected intent and reset named turns to Auto</name>
  <read_first>
    - `ChatPanelFragment.onSubmit(...)`
    - intent-aware `ChatService.stream(...)` overload from Plan 04
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java
  </files>
  <action>
    1. In `onSubmit`, read selected `IntentOption`.
    2. Pass `null` for Auto or the named `intentId` to `chatService.stream(...)`.
    3. After dispatching a named-intent turn and before response arrives, reset `intentCardRow` to Auto.
    4. Keep existing stop/progress/title/task-file behavior intact.
  </action>
  <verify>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java' -Pattern 'chatService.stream\\(.*intent','setValue\\(.*auto').Count -ge 2"</automated>
  </verify>
  <done>Named intent id reaches the chat service, and selection resets to Auto.</done>
</task>

<task type="auto">
  <name>Task 4: Detect open_form_with_draft tool result and append confirm row</name>
  <read_first>
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/StreamingEvent.java`
    - `ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/StreamEventRenderer.java`
    - `ChatPanelFragment.appendNoticeRow(String)` pattern
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/StreamEventRenderer.java,
    ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java
  </files>
  <action>
    1. Extend rendering output with a small value object if needed, e.g. `RenderedStreamEvent(markdown, DraftPayload)`, without breaking simple markdown callers.
    2. For `StreamingEvent.ToolResult`, check `toolName()` first. When `toolName == "prepare_form_draft"`, parse only `payloadJson()` from Plan 03 and detect `action == "open_form_with_draft"`; do not parse the human-readable `summary()` for extraction payloads.
    3. If `toolName == "prepare_form_draft"` but `payloadJson` is null, malformed, or has the wrong action, show localized contract-error copy (`chatView.intent.draftPayloadInvalid`) and render no confirm row. Keep parse failures silent only for non-extraction tool results.
    4. Add `appendIntentConfirmRow(UUID draftId, String entityName, String instanceName)`.
    5. Create a raw `div.ai-agent-intent-confirm` sibling under `messageListSlot`, set summary text through Element/Span APIs (escaped), and add a server-side button with external-link icon.
    6. Button click delegates to `OpenFormWithDraftHandler.open(...)`.
    7. If handler reports expired/missing draft, replace summary with `chatView.intent.draftExpired` and disable the button.
    8. If handler reports permission denied, show localized notification and keep button enabled.
    9. `clearMessageList()` must remove confirm rows along with notice rows.
    10. `StreamEventRenderer` must not import `ViewNavigators`.
  </action>
  <verify>
    <automated>powershell -Command "$f='ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/StreamEventRenderer.java'; foreach($p in @('payloadJson','toolName','prepare_form_draft','open_form_with_draft')){ if(-not (Select-String -Path $f -Pattern $p -Quiet)){ throw \"missing $p\" } }"</automated>
    <automated>powershell -Command "if(Select-String -Path 'ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/StreamEventRenderer.java' -Pattern 'ViewNavigators|\\.navigate\\(' -Quiet){ throw 'StreamEventRenderer must not navigate' }"</automated>
    <automated>powershell -Command "$f='ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ChatPanelFragment.java'; foreach($p in @('appendIntentConfirmRow','ai-agent-intent-confirm','OpenFormWithDraftHandler','draftPayloadInvalid')){ if(-not (Select-String -Path $f -Pattern $p -Quiet)){ throw \"missing $p\" } }"</automated>
  </verify>
  <done>Tool result payloads render a controller-owned confirm row without renderer navigation coupling.</done>
</task>

<task type="auto">
  <name>Task 5: Append Phase 14 CSS only</name>
  <read_first>
    - `ai-agent/ai-agent/src/main/resources/META-INF/resources/frontend/styles/ai-agent-chat.css`
    - UI-SPEC CSS Append Contract
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/resources/META-INF/resources/frontend/styles/ai-agent-chat.css
  </files>
  <action>
    1. Append a Phase 14 header comment and only the selectors locked in UI-SPEC:
       - `.ai-agent-intent-card-row`
       - `.ai-agent-intent-card`
       - `.ai-agent-intent-card--selected`
       - `.ai-agent-intent-card-row vaadin-radio-button`
       - `.ai-agent-intent-card-row vaadin-radio-button::part(radio)`
       - `.ai-agent-intent-card-row vaadin-radio-button::part(label)`
       - `.ai-agent-intent-confirm`
    2. Use Lumo CSS variables for spacing/color.
    3. Do not use raw `px`; use the UI-SPEC rem exceptions only.
    4. Do not edit or target Phase 13.1 selectors like `.ai-agent-attachment-notice`, `.attachments-dropzone`, or `.attachment-card-meta`.
  </action>
  <verify>
    <automated>powershell -Command "(Select-String -Path 'ai-agent/ai-agent/src/main/resources/META-INF/resources/frontend/styles/ai-agent-chat.css' -Pattern 'ai-agent-intent-card-row','ai-agent-intent-card--selected','ai-agent-intent-confirm').Count -ge 3"</automated>
    <automated>powershell -Command "if(-not (Select-String -Path 'ai-agent/ai-agent/src/main/resources/META-INF/resources/frontend/styles/ai-agent-chat.css' -Pattern 'min-height:\\s*4rem' -Quiet)){ throw 'missing min-height 4rem' }"</automated>
  </verify>
  <done>CSS is append-only and scoped to Phase 14 selectors.</done>
</task>

<task type="auto">
  <name>Task 6: Add UI tests and renderer tests</name>
  <read_first>
    - `ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/RenderStreamEventTest.java`
    - `ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/NoticeRenderTest.java`
    - `ai-agent/ai-agent/src/test/java/com/vn/agent/i18n/LocaleParityTest.java`
  </read_first>
  <files>
    ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/IntentCardRowTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/OpenFormWithDraftRenderingTest.java,
    ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/RenderStreamEventIntentPayloadTest.java
  </files>
  <action>
    1. Test Auto plus named intents render when registry has eligible intents.
    2. Test row hidden when registry returns zero named intents.
    3. Test selected named turn resets to Auto after submit.
    4. Test renderer detects valid payload and ignores invalid JSON.
    5. Test renderer consumes `ToolResult.payloadJson` for `prepare_form_draft` and does not parse human-readable `summary`.
    6. Test malformed/missing extraction payload shows localized `draftPayloadInvalid` toast and appends no confirm row.
    7. Test confirm row appends with escaped instance name and delegates to handler.
    8. Test expired result disables the row/button and uses localized copy.
    9. Run locale parity.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:test --tests "*IntentCardRowTest" --tests "*OpenFormWithDraftRenderingTest" --tests "*RenderStreamEventIntentPayloadTest" --tests "*LocaleParityTest"</automated>
  </verify>
  <done>UI behavior and locale parity are covered.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Component | Risk | Mitigation |
|-----------|-----------|------|------------|
| T-14-21 | Intent picker | Wrong tool surface because selection not sent | Submit path passes intent id and resets Auto |
| T-14-22 | Renderer | XSS through instanceName | Vaadin text APIs, no raw HTML |
| T-14-23 | Renderer | Navigation in pure renderer | Renderer emits marker only; handler navigates |
| T-14-24 | CSS | Regression of attachment right pane | Append-only scoped selectors |
</threat_model>

<verification>
- `./gradlew :ai-agent:ai-agent:compileJava`
- `./gradlew :ai-agent:ai-agent:test --tests "*Intent*" --tests "*RenderStreamEvent*"`
- `./gradlew :ai-agent:ai-agent:test --tests "*LocaleParityTest"`
- JetBrains file-problem checks on touched Java/XML/properties files.
</verification>

<success_criteria>
1. `intentCardRow` XML exists in the locked position.
2. `@Supply` renderer creates card-shaped options.
3. Named send passes intent id and resets to Auto.
4. `open_form_with_draft` payload renders an inline confirm row.
5. Confirm click delegates to `OpenFormWithDraftHandler`.
6. CSS and i18n meet UI-SPEC gates.
</success_criteria>

<output>
Create `.planning/phases/14-intent-driven-extraction-form-prefill/14-06-SUMMARY.md` with XML diff, renderer contract, send/reset behavior, confirm row behavior, CSS selectors, and test results.
</output>
