---
phase: 7.1-adopt-vaadin-messagelist-messageinput-for-chat-view
plan: 07
subsystem: phase-gate.close-out
tags: [phase-gate, uat, loc-ceiling, test-sweep, dep-diff, checkpoint-human-verify, ui-01, ui-02]
requires:
  - "Plan 07.1-01 — i18n bundle parity (chatView.stream.*)"
  - "Plan 07.1-02 — Pure StreamEventRenderer"
  - "Plan 07.1-03 — ChatPanelFragment on MessageList/MessageInput"
  - "Plan 07.1-04 — ChatView route-shell slim"
  - "Plan 07.1-05 — Narrowed deletion (ToolCallCardComponent + CitationDialog)"
  - "Plan 07.1-06 — Rewritten ChatViewStreamTest + ChatViewStopTest"
provides:
  - ".planning/phases/07-flow-ui/07-UAT.md amended with UAT-7.1-CHAT-01..07"
  - "Phase 7.1 LOC gate verified (371 ≤ 375 ceiling per A-04a)"
  - "Phase 7.1 test-suite gate verified (chat-view slice + LocaleParityTest green; full-suite failure count = 20 = Phase 7-07b baseline)"
  - "Dep-diff gate verified (zero 07.1-attributable production-dep additions vs base 16fdfe5)"
  - "checkpoint:human-verify emitted — awaiting Playwright UAT sign-off"
affects:
  - "Closes Phase 7.1 static verification; phase completion is gated on human UAT confirmation of the 7 new UAT steps"
tech_stack:
  added: []
  patterns:
    - "Amendment block in 07-UAT.md preserves Phase 7 history via strikethrough, appends the 7.1 chat steps"
    - "A-04a ceiling amendment applied at gate time — treats CONTEXT as authoritative over older plan text (Rule 1)"
key_files:
  created:
    - ".planning/phases/07.1-adopt-vaadin-messagelist-messageinput-for-chat-view/07.1-07-SUMMARY.md"
  modified:
    - ".planning/phases/07-flow-ui/07-UAT.md"
decisions:
  - "LOC ceiling check used 375 (per CONTEXT §A-04a amendment dated 2026-04-21, commit bda7ff9), NOT the 290 literal in the plan text. Plan Task 2 Step 1 said 290; CONTEXT §A-04a supersedes. Rule 1 plan-bug-fix applied at gate time."
  - "Full-suite baseline comparison: 20/201 failing tests is the exact Phase 7-07b baseline (EclipseLink metamodel + HttpClient OOM cascade in PromptInjectionHarnessTest / IngestionStatusWriterTest). Gate passes because failure count did NOT increase. Fixing these is Phase 8 infra work per STATE.md."
  - "Dep-diff used base commit 16fdfe5 (branch base / phase rename commit), NOT master. Pre-existing uncommitted spring-ai-rag api line in working tree is NOT 07.1-attributable and was explicitly out of scope per session notes — left untouched."
  - "i18n bundle filename is messages_en.properties + messages_vi.properties (plan text referenced messages.properties in one must-have; Rule 1 applied — actual bundles are the authoritative pair)."
  - "Task 3 is a checkpoint:human-verify — emitted the structured checkpoint block and STOPPED. Not auto-passed."
metrics:
  duration: ~8 min
  tasks_completed: 2
  tasks_checkpointed: 1
  files_modified: 1
  commits: 1
  completed: 2026-04-21
---

# Phase 7.1 Plan 07: Phase Close-out Summary

Phase 7.1 close-out: amended Phase 7 UAT with 7 new chat-view UAT steps scoped to the Vaadin MessageList/MessageInput architecture, ran all phase-gate checks (LOC 371 under amended 375 ceiling, chat-view + LocaleParity tests green, full-suite failure count held at the 20-test Phase 7-07b baseline, zero 07.1-attributable dep additions), and emitted a checkpoint:human-verify block awaiting Playwright UAT sign-off.

## Task 1 — 07-UAT.md amendment

Appended a `### Amendment (Phase 7.1 — MessageList/MessageInput adoption)` block to `.planning/phases/07-flow-ui/07-UAT.md` with:

- **UAT-7.1-CHAT-01** — Streaming renders into MessageList (user color 0, bot color 2, markdown accumulation)
- **UAT-7.1-CHAT-02** — Tool calls render as inline `**toolName**` + `_done — summary_` markdown (no expand/collapse)
- **UAT-7.1-CHAT-03** — Citations render as trailing **Sources** list; clicking deep-links `/ai-agent/knowledge?documentId=<uuid>` (A-03)
- **UAT-7.1-CHAT-04** — Stop click → input re-enables, stream halts, `/ai-agent/audit` row with `outcome = CANCELLED` (D-04)
- **UAT-7.1-CHAT-05** — New-chat confirm dialog gated by `hasMessages() || isStreaming()` (D-06)
- **UAT-7.1-CHAT-06** — `?conversationId=<uuid>` query param replays transcript via `setConversationId → setItems` (D-07)
- **UAT-7.1-CHAT-07** — Bilingual smoke: VI locale renders `Bạn` / `Trợ lý AI` / `Nguồn tham khảo` / localized Stop

The existing Phase 7 ChatView item (#2) and VI i18n item (#7) were struck through (not deleted) to preserve historical record.

**Acceptance (grep counts):**
- `grep -c "UAT-7\.1-CHAT-" 07-UAT.md` → 16 (7 IDs referenced across TOC + section headers + detail anchors; plan required ≥ 7) — PASS
- `grep "documentId="` → 1 match (UAT-7.1-CHAT-03) — PASS
- `grep "outcome = CANCELLED"` → 1 match (UAT-7.1-CHAT-04) — PASS

**Commit:** `cfc1830` — `docs(7.1-07): add UAT-7.1-CHAT-01..07 steps for MessageList adoption`

## Task 2 — Phase-gate checks

### LOC ceiling (A-04 / A-04a amendment)

```
ChatView.java              = 89 lines
ChatPanelFragment.java     = 282 lines
---------------------------------
Combined                   = 371 lines
Ceiling (A-04a, 2026-04-21) = 375
Aspirational (A-04)        = 250
Result                     = PASS (4 lines of headroom)
```

**A-04a amendment applied:** CONTEXT §A-04a (committed in `bda7ff9` today, 2026-04-21) raises the combined ceiling from 290 to 375 based on legitimate scope the original A-04 underweighted: six-variant `StreamingEvent` fanout, `activeRunId` late-latch bookkeeping, `setConversationId` transcript replay, and `onDetach` disposal wiring. The plan text's Task 2 Step 1 still literally said 290; CONTEXT is authoritative. Rule 1 plan-bug-fix applied — documented here per session directive.

### Chat-view scoped test slice

```
./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.view.chat.*"
→ BUILD SUCCESSFUL in 39s
```

All of: RenderStreamEventTest, ChatViewStreamTest, ChatViewStopTest, MarkdownRendererXssTest (kept per A-01) — green.

### LocaleParityTest

```
./gradlew :ai-agent:ai-agent:test --tests "com.vn.agent.i18n.LocaleParityTest"
→ BUILD SUCCESSFUL in 5s
```

### Full module test sweep

```
./gradlew :ai-agent:ai-agent:test
→ 201 tests completed, 20 failed
→ BUILD FAILED (expected — matches Phase 7-07b baseline exactly)
```

Baseline comparison: the failing tests are the known pre-existing Phase 7-07b set —
- `IngestionStatusWriterTest` (EclipseLink-metamodel / missing column, ~7 methods)
- `PromptInjectionHarnessTest` (`OutOfMemoryError` cascade from the `HttpClient-41-SelectorManager` thread, Spring context creation failure — pre-existing infra flake)
- `HybridAdvisorCoreTest`, `HybridAdvisorIntegrationTest`, assorted eval + RAG integration tests that share the same OOM-cascaded context

Count is 20 = the documented baseline. Gate passes: **no new 7.1-attributable failures**.

### Forbidden-import scan (A-01 scoped deletion)

```
grep -rn "import com.vn.agent.view.chat.fragment.ToolCallCardComponent" ai-agent/ai-agent/src/
→ 0 hits — PASS

grep -rn "import com.vn.agent.view.chat.fragment.CitationDialog" ai-agent/ai-agent/src/
→ 0 hits — PASS
```

And direct source-grep of the two chat-view files for any reference (not just import) to the deleted classes:

```
grep -nE "ToolCallCardComponent|CitationDialog" ChatView.java ChatPanelFragment.java
→ 0 hits — PASS
```

### A-01 kept-component sanity (must still exist)

```
grep -rn "import com.vn.agent.view.chat.MarkdownRenderer" .../view/conversation/
→ ConversationDetailView.java:12  (PASS — kept per A-01)

grep -rn "import com.vn.agent.view.chat.fragment.MessageBubbleComponent" .../view/conversation/
→ ConversationDetailView.java:13  (PASS — kept per A-01)
```

MarkdownRenderer + MessageBubbleComponent + flexmark + owasp-html-sanitizer remain live for `ConversationDetailView`; full cleanup deferred to D-12 follow-up phase.

### Dep-diff vs base 16fdfe5 (phase rename commit, NOT master)

```
git diff 16fdfe5 HEAD -- ai-agent/ai-agent/ai-agent.gradle | grep -E "^\+.*(implementation|api)"
→ 0 hits — PASS
```

Working-tree has a pre-existing uncommitted `+api 'org.springframework.ai:spring-ai-rag:1.1.4'` line that is NOT part of Phase 7.1 (explicit session directive — left untouched). Scoped diff against HEAD confirms 07.1 itself added zero production dependencies; flexmark + owasp-html-sanitizer remain but that's A-01-intended (ConversationDetailView still depends on them).

No code changes made in Task 2 — verification-only. No commit.

## Task 3 — checkpoint:human-verify

Emitted the standard GSD human-verify checkpoint block listing UAT-7.1-CHAT-01..07 for the user to run via Playwright / manual smoke against `./gradlew :jmix-app:bootRun` (http://localhost:8080, admin/admin). See final report returned to orchestrator.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 — Plan text vs authoritative CONTEXT] LOC ceiling raised 290 → 375**
- **Found during:** Task 2 Step 1 prep
- **Issue:** Plan Task 2 Step 1 literal text said `test "$LINES" -le 290` and "if > 290 STOP". CONTEXT §A-04a (committed `bda7ff9` today) raised ceiling to 375 based on legitimate scope rationale. Session note: "CONTEXT §A-04/§A-04a amendment is authoritative."
- **Fix:** Ran LOC check with ceiling 375. Result 371 passes under amended ceiling with 4-line headroom.
- **Files modified:** none (verification-only; rationale documented in this SUMMARY)
- **Commit:** n/a

**2. [Rule 1 — Plan text vs repo reality] i18n bundle filename**
- **Found during:** Reading must_haves truth #6
- **Issue:** Must-have bullet says "messages.properties and messages_vi.properties". Actual i18n bundle is `messages_en.properties` + `messages_vi.properties` per session directive.
- **Fix:** Interpret LocaleParityTest green run (passed) as the authoritative parity gate — test actually checks the files that exist. No plan-text edit made; noted here.
- **Files modified:** none
- **Commit:** n/a

### Out of scope (explicitly preserved)

- Pre-existing uncommitted edits in `ai-agent.gradle`, `KnowledgeBaseView.java`, `AiAgentGuardAutoConfiguration.java`, `jmix-app/build.gradle`, `jmix-app/settings.gradle` left untouched per session directive. These are NOT Phase 7.1 work.

## Must-haves confirmation (9 truths)

1. **ChatPanelFragment uses `MessageList.setMarkdown(true)` + `MessageInput`; zero refs to deleted components in ChatView/ChatPanelFragment/tests** — PASS (grep 0 hits on ToolCallCardComponent + CitationDialog in the chat-view subtree; Plan 07.1-05 closed this).
2. **`renderStreamEvent(StreamingEvent, labels, CitationState) → String` pure function with unit coverage over all 6 variants** — PASS (Plan 07.1-02 RenderStreamEventTest; chat-view test slice green).
3. **Stop path: UI → CancellationRegistry.cancel(runId) → registry disposes stored Disposable → ToolCallbackAuditDecorator records CANCELLED** — PASS (Plan 07.1-06 ChatViewStopTest behavioural + source-grep sentinel; D-04 contract preserved).
4. **conversationId query-param + New-chat unsaved-confirm dialog preserved** — PASS (D-07 + D-06 wired in Plan 07.1-03/04; Task 3 UAT-7.1-CHAT-05 + -06 will confirm live).
5. **Citations render as trailing Sources list → `/ai-agent/knowledge?documentId={uuid}`; CitationDialog deleted; KB route confirmed** — PASS (A-03 param; Plan 07.1-05 deletion; UAT-7.1-CHAT-03 awaits live confirm).
6. **i18n parity — every `chatView.stream.*` key in BOTH bundles; LocaleParityTest green** — PASS (Plan 07.1-01 + LocaleParityTest green this run).
7. **`./gradlew :ai-agent:ai-agent:test` green; rewritten ChatViewStreamTest + ChatViewStopTest + MarkdownRendererXssTest passing** — PASS for chat-view slice (the authoritative 7.1 scope). Full-suite has 20 pre-existing failures held at Phase 7-07b baseline.
8. **Combined ChatView.java + ChatPanelFragment.java ≤ 375 LOC** (amended A-04a ceiling) — PASS (371, 4-line headroom).
9. **No new production deps; flexmark + owasp-html-sanitizer STAY (A-01)** — PASS (dep-diff vs `16fdfe5` clean for committed tree; A-01 kept imports confirmed in ConversationDetailView).

## Follow-ups

- **D-12 / future phase:** Migrate `ConversationDetailView` to MessageList-based replay, then drop MarkdownRenderer + MessageBubbleComponent + MarkdownRendererXssTest + flexmark + owasp-html-sanitizer. The +4 LOC headroom under the 375 ceiling is deliberate; any future regression past 375 must trigger the deferred `ChatStreamController` extraction.
- **Phase 8 infra:** Resolve the 20-test Phase 7-07b baseline (EclipseLink metamodel + HttpClient OOM cascade) — tracked in STATE.md.

## Self-Check: PASSED

- Created file: `.planning/phases/07.1-adopt-vaadin-messagelist-messageinput-for-chat-view/07.1-07-SUMMARY.md` — FOUND
- Modified file: `.planning/phases/07-flow-ui/07-UAT.md` — FOUND, grep confirms 7 new UAT steps
- Commit `cfc1830` — FOUND in `git log`
- Base commit `16fdfe5` used for dep diff (not master) — confirmed clean
- LOC ceiling 375 (A-04a) applied instead of plan literal 290 — deviation documented
