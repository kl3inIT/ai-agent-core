# Phase 7.1: Adopt Vaadin MessageList / MessageInput for Chat View - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in `7.1-CONTEXT.md` — this log preserves the alternatives considered.

**Date:** 2026-04-21
**Phase:** 7.1-adopt-vaadin-messagelist-messageinput-for-chat-view
**Areas discussed:** Citations rendering, Stop semantics, Fragment shell, Content batching

---

## Gray Area Selection

| Option | Description | Selected |
|--------|-------------|----------|
| Citations rendering | Ref trailing **Sources:** list vs Phase 7 D-09 inline [n] + CitationDialog modal | ✓ |
| Stop semantics | Ref bare Disposable.dispose() vs Phase 7 D-03 CancellationRegistry + audit CANCELLED | ✓ |
| Fragment shell | Keep gutted ChatPanelFragment (D-29 substrate) vs inline into ChatView | ✓ |
| Content batching | Keep 50ms bufferTimeout(64) vs per-event appendText (ref-style) | ✓ |

All four gray areas selected for discussion.

---

## Citations Rendering

| Option | Description | Selected |
|--------|-------------|----------|
| Trailing markdown list (ref-style) | Emit `**Sources:**` + `[docName](/kb?docId=X)` list as trailing markdown. Delete CitationDialog. | ✓ |
| Keep CitationDialog as sibling layer | Inline `[n]` + post-render DOM listener on MessageList to catch clicks and open dialog | |
| Expand snippet inline in markdown | Footnote with snippet text + Open link embedded in markdown | |

**User's choice:** Trailing markdown list (ref-style).
**Notes:** Aligns with MessageList's markdown-first model; CitationDialog.java gets deleted; KB deep-link preserved via link href.

---

## Stop Semantics

| Option | Description | Selected |
|--------|-------------|----------|
| Keep CancellationRegistry integration | Button → CancellationRegistry.cancel(runId) → Flux dispose → audit CANCELLED | ✓ |
| Client-side dispose only (ref-style) | Just activeStreamDisposable.dispose(); no audit row integration | |
| No Stop button; rely on navigation | Delete Stop entirely; detach handler disposes | |

**User's choice:** Keep CancellationRegistry integration.
**Notes:** Audit contract (D-03) is load-bearing; won't regress for code-golf reasons.

---

## Fragment Shell

| Option | Description | Selected |
|--------|-------------|----------|
| Keep Fragment as gutted shell | ChatPanelFragment stays as @FragmentDescriptor; body rewritten; ChatView composes | ✓ |
| Inline into ChatView (ref-style) | Delete ChatPanelFragment + xml; ChatView owns everything | |
| Keep Fragment, move route parts to it | Fragment owns full chat surface; ChatView is a thin route wrapper | |

**User's choice:** Keep Fragment as gutted shell.
**Notes:** Preserves Phase 7 D-29 substrate for v2 floating launcher without a second refactor later. Pays ~30-50 lines against the ~250-line target — deliberate.

---

## Content Batching

| Option | Description | Selected |
|--------|-------------|----------|
| Drop batching, per-event appendText (ref-style) | .doOnNext(md -> ui.access(() -> botMsg.appendText(md))); no bufferTimeout | ✓ |
| Keep 50ms bufferTimeout(64) coalescing | Preserve Pitfall #7 fix; single appendText per 50ms window | |
| Drop now, add back if perf surfaces | Drop for initial impl; flag as deferred idea if UAT hits perf issues | |

**User's choice:** Drop batching, per-event appendText (ref-style).
**Notes:** Trust the platform (MessageListItem.appendText is streaming-designed; Vaadin Push batches server-side). Captured in Deferred Ideas as a "re-introduce if needed" rollback path.

---

## Claude's Discretion

Areas explicitly left for downstream planner:
- Exact `renderStreamEvent` signature (static method, instance method, helper record)
- Stop button visual placement (sibling to MessageInput vs above MessageList)
- `chatView.stream.*` exact key naming (parity across en+vi is the hard requirement)
- scrollToBottom implementation (reference's `setTimeout(..., 50)` JS trick or simpler)
- Unsaved-confirm detection heuristic for New chat
- `ChatProgressView.java` retention (planner triage — delete if orphaned)

## Deferred Ideas

- Content-event bufferTimeout re-introduction behind a config knob
- Citation snippet preview (hover-tooltip or markdown footnote instead of modal)
- MessageList adoption for ConversationDetailView (replay) — separate follow-up phase
- VirtualList for long transcripts (still deferred from Phase 7 D-06)
