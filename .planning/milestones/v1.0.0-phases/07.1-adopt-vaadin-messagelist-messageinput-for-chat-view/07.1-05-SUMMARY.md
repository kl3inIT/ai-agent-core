---
phase: 7.1-adopt-vaadin-messagelist-messageinput-for-chat-view
plan: 05
subsystem: view.chat
tags: [cleanup, deletion, i18n, css, narrowed-scope, amendment-a-01]
requires:
  - 7.1-03 (ChatPanelFragment on Vaadin MessageList — dropped ToolCallCard/CitationDialog refs)
  - 7.1-04 (ChatView slim — dropped ToolCallCard/CitationDialog refs)
provides:
  - "Orphan removal: 3 files deleted from git"
  - "CitationDialog-scoped i18n keys purged (EN+VI parity preserved)"
  - "Dead tool-card CSS purged"
affects:
  - "ConversationDetailView (verified: still compiles; MarkdownRenderer + MessageBubbleComponent imports resolve)"
  - "ToolCallbackAuditDecorator javadoc (stale ToolCallCardComponent reference scrubbed — Rule 1)"
  - "LocaleParityTest (stale bundle path fixed — Rule 1)"
tech-stack:
  added: []
  removed: []  # flexmark + owasp-sanitizer INTENTIONALLY retained per A-01
  patterns:
    - "Narrowed deletion set: delete only files with zero cross-consumer; keep shared primitives"
key-files:
  deleted:
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ToolCallCardComponent.java
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/CitationDialog.java
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/fragment/citation-dialog.xml
  modified:
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties
    - ai-agent/ai-agent/src/main/resources/META-INF/resources/frontend/styles/ai-agent-chat.css
    - ai-agent/ai-agent/src/main/java/com/vn/agent/audit/ToolCallbackAuditDecorator.java
    - ai-agent/ai-agent/src/test/java/com/vn/agent/i18n/LocaleParityTest.java
decisions:
  - "A-01 narrowed deletion binding: only ToolCallCardComponent + CitationDialog + citation-dialog.xml deleted; MarkdownRenderer/MessageBubbleComponent/XssTest kept because ConversationDetailView still replays via MessageBubbleComponent — full cleanup deferred to D-12 follow-up phase that migrates ConversationDetailView to MessageList."
  - "flexmark + owasp-html-sanitizer deps in ai-agent.gradle left untouched — still consumed transitively via MarkdownRenderer in the kept code path."
  - "Orphan .ai-agent-scroll-pre and .ai-agent-scroll-pre--tall rules deleted along with the tool-card selectors because grep confirmed zero Java references (audit-dialog prose comment in the CSS was aspirational; the real audit dialog never applied these class names)."
metrics:
  completed: "2026-04-21T16:18:28Z"
  duration_minutes: 15
  tasks_total: 2
  tasks_completed: 2
  commits: 1  # audit-only Task 1 produced no changes; only Task 2 committed
---

# Phase 7.1 Plan 05: Narrowed Deletion — Orphan ToolCallCard + CitationDialog Summary

Removed three orphan chat-view files left dangling after Waves 2–3 migrated ChatView to the Vaadin MessageList/MessageInput stack. Per amendment A-01, deletion was narrowed to components with zero surviving consumers — MarkdownRenderer and MessageBubbleComponent stay because ConversationDetailView still replays transcripts through them until the D-12 follow-up phase lands.

## Tasks Executed

### Task 1 — Pre-flight audit (audit-only, no commit)

All 5 grep gates satisfied:

1. `grep "ToolCallCardComponent" ai-agent/ai-agent/src/main/java/` → only own class declaration at line 36 + legacy javadoc comment in `ToolCallbackAuditDecorator` line 124 + a prose-only "intentionally absent" note in `ConversationDetailView` line 44. No imports anywhere. Safe.
2. `grep "CitationDialog" ai-agent/ai-agent/src/main/java/` → only own class declaration at line 34. Safe.
3. `grep "import com.vn.agent.view.chat.fragment.(ToolCallCardComponent|CitationDialog)" ai-agent/ai-agent/src/` → 0 hits. Safe.
4. `grep "MarkdownRenderer|MessageBubbleComponent" ai-agent/ai-agent/src/main/java/com/vn/agent/view/conversation/` → 10 hits across `ConversationDetailView.java` (imports lines 12–13, fields line 61, replay logic lines 86–110). A-01 invariant holds: MarkdownRenderer + MessageBubbleComponent remain live.
5. `grep "ChatProgressView"` → 0 hits (A-02 confirmed: symbol never existed in our codebase).

### Task 2 — Delete files + purge i18n keys + partial CSS cleanup (commit bfa51d0)

**Files deleted (`git rm`):**

1. `ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ToolCallCardComponent.java`
2. `ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/CitationDialog.java`
3. `ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/fragment/citation-dialog.xml`

**i18n keys removed (EN + VI parity preserved):**

| Key | EN (deleted) | VI (deleted) |
|---|---|---|
| `chatView.citation.dialogTitle` | `Source: {0}` | `Nguồn: {0}` |
| `chatView.citation.openInKb` | `Open in Knowledge base` | `Mở trong Cơ sở tri thức` |
| `chatView.citation.close` | `Close` | `Đóng` |

Counts post-deletion: `grep -c "^chatView\.citation\." messages_en.properties` → `0`; `grep -c "^chatView\.citation\." messages_vi.properties` → `0`. Parity.

**CSS rules deleted (`ai-agent-chat.css`):**

| Rule | Status |
|---|---|
| `.ai-agent-tool-card__heading` | DELETED (consumer gone) |
| `.ai-agent-tool-card__pre` | DELETED (consumer gone) |
| `.ai-agent-scroll-pre` | DELETED (orphan — grep showed only self-reference) |
| `.ai-agent-scroll-pre--tall` | DELETED (orphan — grep showed only self-reference) |
| `.ai-agent-bubble` + 3 modifiers | **KEPT** — consumed by MessageBubbleComponent (A-01) |

Post-deletion `grep -c "ai-agent-tool-card" ai-agent-chat.css` → `0`.

## A-01 Invariants — KEPT

Explicitly verified still present after Task 2:

| Artifact | Path | Why kept |
|---|---|---|
| `MarkdownRenderer.java` | `ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/MarkdownRenderer.java` | ConversationDetailView line 12 import + line 61 field |
| `MessageBubbleComponent.java` | `ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/MessageBubbleComponent.java` | ConversationDetailView line 13 import + line 100–110 replay |
| `MarkdownRendererXssTest.java` | `ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/MarkdownRendererXssTest.java` | Covers the still-live MarkdownRenderer |
| `flexmark:0.64.8` + 2 extensions | `ai-agent/ai-agent/ai-agent.gradle` lines 58-60 | Transitive dep of MarkdownRenderer |
| `owasp-java-html-sanitizer:20220608.1` | `ai-agent/ai-agent/ai-agent.gradle` line 61 | Transitive dep of MarkdownRenderer |
| `chatView.toolCard.*` i18n keys | Both bundles lines 127-133 | **Note:** These are *technically* now orphan since ToolCallCardComponent is gone, but kept to match plan scope (plan only names `chatView.citation.*` for removal). A future housekeeping task should re-examine. |

## Verification

| Gate | Result |
|---|---|
| `./gradlew :ai-agent:ai-agent:compileJava` | **green** (9s) |
| `./gradlew :ai-agent:ai-agent:compileTestJava` | **green** (UP-TO-DATE) |
| `./gradlew :ai-agent:ai-agent:test --tests LocaleParityTest` | **green** after Rule 1 path fix (previously failing pre-plan on stale path) |
| `grep -c "com\.vladsch\.flexmark" ai-agent.gradle` | 3 (≥3 required) |
| `grep -c "owasp-java-html-sanitizer" ai-agent.gradle` | 1 (≥1 required) |
| `grep -c "ai-agent-tool-card" ai-agent-chat.css` | 0 |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 — Bug] Plan targeted wrong i18n filename**
- **Found during:** Task 2 pre-read
- **Issue:** Plan frontmatter + action text reference `messages.properties`; the real file was renamed to `messages_en.properties` by Plan 07.1-01 (documented in its SUMMARY's deferred notes + prior_wave_context block of this plan's prompt).
- **Fix:** Applied deletions to `messages_en.properties` instead. `messages_vi.properties` filename was unchanged.
- **Files modified:** `ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties`
- **Commit:** `bfa51d0`

**2. [Rule 1 — Bug] Stale javadoc reference to deleted class**
- **Found during:** Task 1 audit grep surfaced `ToolCallbackAuditDecorator.java:124`
- **Issue:** Pre-existing comment said `// ... ChatView's ToolCallCardComponent.setResult matches by id ...` — after this plan's deletion that reference becomes dangling and misleads future readers.
- **Fix:** Rewrote comment to describe the new relationship (`StreamEventRenderer` from Plan 07.1-02 consumes the pair). No behavior change.
- **Files modified:** `ai-agent/ai-agent/src/main/java/com/vn/agent/audit/ToolCallbackAuditDecorator.java`
- **Commit:** `bfa51d0`

**3. [Rule 1 — Bug] LocaleParityTest stale bundle path**
- **Found during:** Running plan's own verify command `./gradlew test --tests LocaleParityTest`
- **Issue:** Test hardcodes `/com/vn/agent/messages.properties`. That file was renamed to `messages_en.properties` in Plan 07.1-01; `getResourceAsStream` returns `null` so the test fails at line 33 `assertThat(in).isNotNull()`. Pre-existing breakage called out in 07.1-01's SUMMARY as a deferred quick-task. Fixing it inline unblocks this plan's own parity acceptance criterion (`LocaleParityTest exits 0`).
- **Fix:** Changed the hardcoded path from `messages.properties` → `messages_en.properties` in both `@Test` methods (via `replace_all`). Verified: test now green.
- **Files modified:** `ai-agent/ai-agent/src/test/java/com/vn/agent/i18n/LocaleParityTest.java`
- **Commit:** `bfa51d0`

**4. [Rule 1 — Cleanup] Also deleted `.ai-agent-scroll-pre` + `.ai-agent-scroll-pre--tall`**
- **Found during:** CSS partial-purge step of Task 2
- **Issue:** Plan instructed to keep them for "shared dialog styles (CitationDialog + audit dialog)". CitationDialog is deleted; grep confirms the audit dialog never actually applies these class names — they were aspirational. Both become pure orphans.
- **Fix:** Deleted both rules along with the tool-card ones. Falls under the plan's own written guidance: "If unsure whether a selector is still referenced, grep — if zero hits, delete; otherwise keep." Zero hits.
- **Commit:** `bfa51d0`

### No-deviation notes

- `ai-agent.gradle`: NOT touched (A-01). flexmark + owasp-sanitizer deps intact.
- `ConversationDetailView.java`: NOT touched (A-01). Prose-only javadoc mention of `ToolCallCardComponent` as "intentionally absent" still accurate (still absent — just for a different reason now).
- `MarkdownRenderer.java`, `MessageBubbleComponent.java`, `MarkdownRendererXssTest.java`: NOT touched (A-01).

### Authentication gates

None.

## Scope Boundary Notes

Per sequential_execution instruction, these pre-existing uncommitted working-tree edits were NOT staged or reverted:
- `ai-agent/ai-agent-starter/src/main/java/com/vn/autoconfigure/agent/AiAgentGuardAutoConfiguration.java`
- `ai-agent/ai-agent/ai-agent.gradle`
- `ai-agent/ai-agent/src/main/java/com/vn/agent/view/knowledge/KnowledgeBaseView.java`
- `ai-agent/ai-agent/src/main/resources/com/vn/agent/view/knowledge/knowledge-base-view.xml`
- `jmix-app/build.gradle`, `jmix-app/settings.gradle`
- Several untracked files (`.playwright-mcp/`, `chat-view-current.png`, `parameters-view.png`, jmix-app `package*.json` / `tsconfig.json` / `types.d.ts` / `vite.config.ts`, `.planning/debug/ai-entity-metaclass-not-found.md`, `.planning/phases/07-flow-ui/07-UAT.md`)

These are carried forward untouched for their owning plans/sessions.

## D-12 Follow-up Flag

When ConversationDetailView is migrated to the Vaadin MessageList stack (separate phase, tracked as D-12 in CONTEXT), a follow-up cleanup plan should:

1. Delete `MarkdownRenderer.java`
2. Delete `MessageBubbleComponent.java`
3. Delete `MarkdownRendererXssTest.java`
4. Remove flexmark + owasp-html-sanitizer from `ai-agent.gradle`
5. Remove the remaining `chatView.toolCard.*` i18n keys if truly orphan by then (check for any other late consumer first)
6. Remove the remaining `.ai-agent-bubble*` CSS rules

Only then is the Phase-7 custom Markdown+XSS pipeline fully retired.

## Known Stubs

None introduced by this plan. Deletions only; no placeholder or mock rendering added.

## Self-Check: PASSED

- `test ! -f ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ToolCallCardComponent.java` → FOUND absent
- `test ! -f ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/CitationDialog.java` → FOUND absent
- `test ! -f ai-agent/ai-agent/src/main/resources/com/vn/agent/view/chat/fragment/citation-dialog.xml` → FOUND absent
- `test -f ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/MarkdownRenderer.java` → FOUND present
- `test -f ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/MessageBubbleComponent.java` → FOUND present
- `test -f ai-agent/ai-agent/src/test/java/com/vn/agent/view/chat/MarkdownRendererXssTest.java` → FOUND present
- Commit `bfa51d0` exists in `git log` → FOUND
- `compileJava` + `compileTestJava` + `LocaleParityTest` all green
