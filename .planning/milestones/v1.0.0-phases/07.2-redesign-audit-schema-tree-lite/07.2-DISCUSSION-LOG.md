# Phase 7.2: Redesign Audit Schema as Tree-Lite - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-24
**Phase:** 07.2-redesign-audit-schema-tree-lite
**Areas discussed:** Root row lifecycle, SPI breaking-change shape, Data migration path, Rename + naming, Fan-out coverage (follow-up), Retrieval scope (follow-up)

---

## Gray Area Selection

| Option | Description | Selected |
|--------|-------------|----------|
| Root row lifecycle | Mutable vs append-only root | ✓ |
| SPI breaking-change shape | Generalized vs per-kind vs deprecation | ✓ |
| Data migration path | Hard cutover vs backfill | ✓ |
| Rename + naming | Bundle vs split; entity name | ✓ |

**User's choice:** All four areas.

---

## Root Row Lifecycle

| Option | Description | Selected |
|--------|-------------|----------|
| Mutable root (Recommended) | INSERT on start, UPDATE same row on finish. One row per turn. | ✓ |
| Append-only ledger | CHAT_START + CHAT_FINISH rows; never UPDATE. | |
| Hybrid (mutable root, append-only children) | Root mutable; leaf events append-only. Same outcome, explicitly documented. | |

**User's choice:** Mutable root (Recommended).
**Notes:** Matches success criterion #2 literally ("one root row per turn"). Captured as D-01 + D-02 in CONTEXT.md. The hybrid framing is effectively absorbed into D-01 (root mutable, TOOL/RETRIEVAL children append-only).

---

## Fan-out Coverage (follow-up to Root Row)

| Option | Description | Selected |
|--------|-------------|----------|
| All events, kind-tagged (Recommended) | Fire onEventAudited on CHAT finish, TOOL, RETRIEVAL. | ✓ |
| Children only (TOOL / RETRIEVAL) | Skip chat-level rows as today. | |
| Claude's discretion | Planner picks. | |

**User's choice:** All events, kind-tagged.
**Notes:** Captured as D-05. Explicitly excludes `writeChatStart` — only committed rows with final state fan out.

---

## SPI Breaking-Change Shape

| Option | Description | Selected |
|--------|-------------|----------|
| Generalized single method (Recommended) | onEventAudited(UUID, String kind). | ✓ |
| Per-kind methods with defaults | onChatAudited / onToolCallAudited / onRetrievalAudited default no-op. | |
| Deprecation bridge | Keep old method as deprecated default routing to new. | |

**User's choice:** Generalized single method.
**Notes:** Captured as D-03 + D-04. Kind constants live in a public `AuditKind` final class (String constants, NOT an enum) to keep the kind column open-ended for host-defined kinds.

---

## Data Migration Path

| Option | Description | Selected |
|--------|-------------|----------|
| Hard cutover — drop + recreate (Recommended) | Drop old table, create fresh. No backfill. | ✓ |
| Non-destructive — ALTER + backfill | Add PARENT_ID nullable, synthesize roots from PRE+POST, link same-runId rows. | |
| Hybrid — drop in dev, backfill flag-gated | Default drop; property triggers backfill. | |

**User's choice:** Hard cutover.
**Notes:** Pre-v1 project; no production audit data exists. Captured as D-06. Document hard cutover in a short ADR/memory note so v1 consumers know there is no upgrade path from pre-v1 audit data.

---

## Rename Timing

| Option | Description | Selected |
|--------|-------------|----------|
| Bundle in one phase (Recommended) | Tree + rename in one changelog. | ✓ |
| Split: tree first, rename follow-up | Two migrations, two reviews. | |

**User's choice:** Bundle.
**Notes:** With hard cutover, bundling is free. Captured as D-07.

---

## Entity Name

| Option | Description | Selected |
|--------|-------------|----------|
| AiAuditEvent / AI_AGENT_AUDIT_EVENT (Recommended) | Event-shaped; mirrors onEventAudited. | ✓ |
| AiAgentAudit / AI_AGENT_AUDIT | Shorter "audit record" framing. | |
| Claude's discretion | Planner picks after reference read. | |

**User's choice:** AiAuditEvent / AI_AGENT_AUDIT_EVENT.
**Notes:** Captured as D-08.

---

## Retrieval Scope (follow-up)

| Option | Description | Selected |
|--------|-------------|----------|
| Method + round-trip test only (Recommended) | Ship writeRetrieval + test; advisor hookup in follow-up todo. | |
| Also wire retrieval into advisor chain | Ship end-to-end retrieval audit in 7.2. | ✓ |
| I'm ready for context | Skip the scope question. | |

**User's choice:** Also wire retrieval into advisor chain.
**Notes:** Scope expansion. Captured as D-09 + D-10. The previously-blocked retrieval-audit todo is folded into this phase. Planner must re-verify Spring AI 1.1.4 M4 `QuestionAnswerAdvisor` / `RetrievalAugmentationAdvisor` API shape during RESEARCH.md and instrument the actual Phase 5 RAG wiring.

---

## Claude's Discretion

- Exact Liquibase `<dropTable>` + retire-changelog pattern (verify on Postgres + HSQLDB).
- `AuditKind` constants file location (audit package vs spi package).
- `filtersJson` serialization format for retrieval rows.
- `RunContext` wiring style (constructor-injected vs ThreadLocal) — inherited from Phase 4.
- `AiAuditEvent` instance name exact string format.

## Deferred Ideas

- Additional event kinds: `GUARDRAIL`, `RATE_LIMIT`, `INJECTION_SCAN` — column width reserved, writing deferred.
- Deeper tree levels (retrieval → per-chunk children).
- Async executor pool for fan-out — ruled out by Phase 4 D-13.
- Historical audit data migration for downstream v1 consumers.
