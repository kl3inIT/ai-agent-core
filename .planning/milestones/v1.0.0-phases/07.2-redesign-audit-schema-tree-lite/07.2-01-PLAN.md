---
phase: 07.2-redesign-audit-schema-tree-lite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/090-ai-audit-event.xml
  - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiAuditEvent.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallAudit.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/spi/AuditKind.java
  - ai-agent/ai-agent/src/main/java/com/vn/agent/spi/AuditListener.java
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties
autonomous: true
requirements: [AUD-03, SPI-06]
tags: [jmix-entity, liquibase, spi, i18n, audit]
must_haves:
  truths:
    - "New AI_AGENT_AUDIT_EVENT table exists with PARENT_ID self-FK + nullable EVENT_NAME + retrieval-specific columns"
    - "Legacy AI_AGENT_TOOL_CALL_AUDIT table is dropped cleanly on both fresh HSQLDB boot and already-initialised DB"
    - "AiAuditEvent JPA entity boots with self-referential @Composition children collection"
    - "AuditKind.CHAT / .TOOL / .RETRIEVAL String constants are referenceable from com.vn.agent.spi"
    - "AuditListener SPI exposes only onEventAudited(UUID, String) — the old onToolCallAudited method is removed"
  artifacts:
    - path: "ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/090-ai-audit-event.xml"
      provides: "DDL: drop legacy table + create AI_AGENT_AUDIT_EVENT + self-FK + 4 indexes"
      contains: "AI_AGENT_AUDIT_EVENT"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiAuditEvent.java"
      provides: "Self-referential @Composition entity with PARENT_ID"
      contains: "@OneToMany(mappedBy = \"parent\""
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/spi/AuditKind.java"
      provides: "Public final class of kind String constants"
      contains: "CHAT"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/spi/AuditListener.java"
      provides: "Breaking SPI — onEventAudited(UUID, String)"
      contains: "onEventAudited"
  key_links:
    - from: "AiAuditEvent.children"
      to: "AiAuditEvent.parent"
      via: "@OneToMany(mappedBy=\"parent\") + @ManyToOne self-ref"
      pattern: "mappedBy = \"parent\""
    - from: "AI_AGENT_AUDIT_EVENT.PARENT_ID"
      to: "AI_AGENT_AUDIT_EVENT.ID"
      via: "FK_AI_AGENT_AUDIT_EVENT__ON_PARENT"
      pattern: "FK_AI_AGENT_AUDIT_EVENT__ON_PARENT"
---

<objective>
Ship the foundation layer for the tree-lite audit schema: (1) Liquibase `090-ai-audit-event.xml` that hard-drops the legacy `AI_AGENT_TOOL_CALL_AUDIT` table and creates the new `AI_AGENT_AUDIT_EVENT` table with `PARENT_ID` self-FK plus retrieval-specific columns; (2) `AiAuditEvent` JPA entity with self-referential `@Composition` children; deletion of the legacy `AiToolCallAudit.java`; (3) `AuditKind` public-constants SPI class; (4) `AuditListener` SPI breaking signature change; (5) i18n bundle updates (entity key rename + added attribute keys) in BOTH `messages_en.properties` and `messages_vi.properties`.

Purpose: Unblock every downstream task in this phase. Writers, advisors, UI, and tests all reference `AiAuditEvent` / `AuditKind` / the new `AuditListener` shape.
Output: DDL, entity, SPI contracts, i18n keys ready for Wave 2 consumers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07.2-redesign-audit-schema-tree-lite/07.2-CONTEXT.md
@.planning/phases/07.2-redesign-audit-schema-tree-lite/07.2-RESEARCH.md
@.planning/phases/07.2-redesign-audit-schema-tree-lite/07.2-PATTERNS.md
@CLAUDE.md

<interfaces>
From ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog.xml (master):
- `${uuid.type}` → `uuid` on Postgres, `varchar(36)` on HSQLDB
- `${offsetDateTime.type}` → `timestamp with time zone` on Postgres, `timestamp` on HSQLDB
- `<includeAll path="/com/vn/agent/liquibase/agentstore-changelog">` auto-picks up `090-ai-audit-event.xml` — DO NOT edit master.

From ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiConversation.java (parent-side @Composition analog):
```java
@Composition
@OnDelete(DeletePolicy.CASCADE)
@OneToMany(mappedBy = "conversation", cascade = CascadeType.ALL, orphanRemoval = true)
private List<AiMessage> messages;
```

From ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallAudit.java (column carry-over reference ONLY — file will be deleted):
Columns to carry forward (re-declared on AiAuditEvent): conversation (ManyToOne to AiConversation), userUsername, argumentsJson (@Lob), resultSummary (@Lob), outcome (String + enum getter), denialReason, latencyMs, startedAt, finishedAt, runId, promptHash, errorClass, kind.
Drop: phase. Rename: TOOL_NAME → EVENT_NAME (nullable). Widen: KIND length 8 → 16.
Add (retrieval-only, nullable): queryText (@Lob), topK (Integer), hitCount (Integer), topScore (Double), filtersJson (@Lob).
Add (tree): parent (ManyToOne self-ref, optional=true), children (@OneToMany mappedBy="parent", @Composition).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Liquibase 090-ai-audit-event.xml — drop legacy + create tree table</name>
  <read_first>
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/030-ai-tool-call-audit.xml (column shape analog, full file)
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog.xml (master — confirm `${uuid.type}` / `${offsetDateTime.type}` properties at lines 8-13 + `<includeAll>`)
    - .planning/phases/07.2-redesign-audit-schema-tree-lite/07.2-RESEARCH.md §Pitfall 4 lines 461-523 (full changeset template to copy)
    - .planning/phases/07.2-redesign-audit-schema-tree-lite/07.2-PATTERNS.md §liquibase (§Shared Pattern 6)
  </read_first>
  <action>
    Create `ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/090-ai-audit-event.xml` with `author="ai-agent"` and exactly 4 changesets, copied verbatim from RESEARCH §Pitfall 4 (lines 461-523):

    - **changeSet id="1"**: `<preConditions onFail="MARK_RAN"><tableExists tableName="AI_AGENT_TOOL_CALL_AUDIT"/></preConditions>` gating `<dropTable tableName="AI_AGENT_TOOL_CALL_AUDIT" cascadeConstraints="true"/>`. This implements A5/Pitfall 4 hard-cutover and is a no-op on fresh HSQLDB where the legacy table was never created.
    - **changeSet id="2"**: `<createTable tableName="AI_AGENT_AUDIT_EVENT">` with exact columns (types use `${uuid.type}` / `${offsetDateTime.type}` — NEVER the literal `datetime` that `030` used; RESEARCH §Shared Pattern 6):
      - `ID ${uuid.type}` PK nullable=false
      - `VERSION int` defaultValueNumeric="1" nullable=false
      - `PARENT_ID ${uuid.type}` nullable (root rows set null — D-01)
      - `CONVERSATION_ID ${uuid.type}` nullable
      - `USER_USERNAME varchar(255)`
      - `RUN_ID ${uuid.type}`
      - `KIND varchar(16)` nullable=false (widened from length 8 — D-03 open-ended kinds)
      - `EVENT_NAME varchar(128)` nullable (CHAT roots have null — removes `<chat>` sentinel)
      - `ARGUMENTS_JSON clob`
      - `RESULT_SUMMARY clob`
      - `QUERY_TEXT clob` (RETRIEVAL-only)
      - `TOP_K int` (RETRIEVAL-only)
      - `HIT_COUNT int` (RETRIEVAL-only)
      - `TOP_SCORE double` (RETRIEVAL-only)
      - `FILTERS_JSON clob` (RETRIEVAL-only; D-09 + security note — plain text, no PII forwarding beyond what `Filter.Expression.toString()` emits)
      - `OUTCOME varchar(16)` nullable (CHAT root null until finish — D-01)
      - `DENIAL_REASON varchar(512)`
      - `ERROR_CLASS varchar(255)`
      - `PROMPT_HASH varchar(64)`
      - `LATENCY_MS bigint`
      - `STARTED_AT ${offsetDateTime.type}` nullable=false
      - `FINISHED_AT ${offsetDateTime.type}` (nullable — CHAT root null until finish)
    - **changeSet id="3"**: two `<addForeignKeyConstraint>`:
      - `constraintName="FK_AI_AGENT_AUDIT_EVENT__ON_PARENT"` base=`AI_AGENT_AUDIT_EVENT.PARENT_ID` → references=`AI_AGENT_AUDIT_EVENT.ID`
      - `constraintName="FK_AI_AGENT_AUDIT_EVENT__ON_CONVERSATION"` base=`AI_AGENT_AUDIT_EVENT.CONVERSATION_ID` → references=`AI_AGENT_CONVERSATION.ID`
    - **changeSet id="4"**: four `<createIndex>` with names `IDX_AI_AGENT_AUDIT_EVENT__ON_PARENT` / `…__ON_CONVERSATION` / `…__ON_STARTED_AT` / `…__ON_RUN_ID` on the matching columns.

    DO NOT edit `030-ai-tool-call-audit.xml`, `080-ai-tool-call-audit-runid.xml`, or master `agentstore-changelog.xml` (`<includeAll>` picks up the new file automatically; editing any of the three breaks checksums on already-initialised DEV_CHANGELOG rows — Pitfall 4).

    Security mitigation (threat T-07.2-03 from threat_model): `FILTERS_JSON` is a `clob` and will receive `Filter.Expression.toString()` output only — no raw user prompt text is forwarded here. User's query text lands in `QUERY_TEXT` (already audit-intended per D-09). Same `@Lob` truncation discipline as `ARGUMENTS_JSON` (Phase 4 D-15) applies in the writer layer (Plan 02).
  </action>
  <verify>
    <automated>grep -c "AI_AGENT_AUDIT_EVENT" ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/agentstore-changelog/090-ai-audit-event.xml</automated>
  </verify>
  <done>
    File exists. `grep "FK_AI_AGENT_AUDIT_EVENT__ON_PARENT" 090-ai-audit-event.xml` returns a match. `grep "dropTable tableName=\"AI_AGENT_TOOL_CALL_AUDIT\"" 090-ai-audit-event.xml` returns a match. `grep -c '<createIndex' 090-ai-audit-event.xml` returns 4. `grep "QUERY_TEXT" 090-ai-audit-event.xml` returns a match.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create AiAuditEvent entity + delete AiToolCallAudit + AuditKind constants + AuditListener SPI rewrite</name>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallAudit.java (full file — column shape to carry forward)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiConversation.java lines 16-53 (@Composition parent-side analog)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiMessage.java lines 32-55 (child-side @ManyToOne + @InstanceName analog)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/spi/AuditListener.java (existing SPI — full file, it will be rewritten in place)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/orchestration/RunContext.java (structural analog for AuditKind final-class pattern)
    - .planning/phases/07.2-redesign-audit-schema-tree-lite/07.2-PATTERNS.md §entity/AiAuditEvent.java + §spi/AuditKind.java + §spi/AuditListener.java
    - .planning/phases/07.2-redesign-audit-schema-tree-lite/07.2-RESEARCH.md §Pattern 1 lines 210-266 + §Example 1 lines 550-568 + §Example 2 lines 570-589
  </read_first>
  <behavior>
    - AiAuditEvent can be persisted with PARENT_ID=null (root) and retrieved with children populated via fetch plan.
    - AiAuditEvent can be persisted with parent=some root and appears in `root.children` after re-load with children fetch plan.
    - AuditKind.CHAT / .TOOL / .RETRIEVAL are compile-time String constants usable in switch statements.
    - AuditListener.onEventAudited(id, kind) is the only method; old `onToolCallAudited(id)` is gone.
    - `grep -r onToolCallAudited ai-agent/ai-agent/src/main` returns 0 matches inside this task's scope (other references will be closed in Wave 2 — Plan 02 Task 3 covers the writer/dispatcher; Plan 05 covers the test).
  </behavior>
  <action>
    **2a. Create `ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiAuditEvent.java`** — full class per PATTERNS §entity/AiAuditEvent.java + RESEARCH §Pattern 1 + Claude's-Discretion resolutions from CONTEXT.md:

    Class-level annotations (mirror AiConversation.java:16-22):
    ```java
    @Store(name = "agentstore")
    @JmixEntity
    @Entity(name = "ai_AiAuditEvent")
    @Table(name = "AI_AGENT_AUDIT_EVENT", indexes = {
        @Index(name = "IDX_AI_AGENT_AUDIT_EVENT__ON_PARENT", columnList = "PARENT_ID"),
        @Index(name = "IDX_AI_AGENT_AUDIT_EVENT__ON_CONVERSATION", columnList = "CONVERSATION_ID"),
        @Index(name = "IDX_AI_AGENT_AUDIT_EVENT__ON_STARTED_AT", columnList = "STARTED_AT"),
        @Index(name = "IDX_AI_AGENT_AUDIT_EVENT__ON_RUN_ID", columnList = "RUN_ID")
    })
    public class AiAuditEvent { ... }
    ```

    Fields (NO Lombok — CLAUDE.md forbidden; explicit getters/setters for every field):
    - `@Id @Column(name="ID", nullable=false) @JmixGeneratedValue UUID id;`
    - `@Version @Column(name="VERSION", nullable=false) Integer version;`
    - `@ManyToOne(fetch=FetchType.LAZY) @JoinColumn(name="PARENT_ID") AiAuditEvent parent;` — NOT `@NotNull`, NOT `optional=false` (root rows have null parent — D-01).
    - `@Composition @OneToMany(mappedBy="parent", cascade=CascadeType.ALL, orphanRemoval=true) List<AiAuditEvent> children;` — the SELF-ref `@Composition` (RESEARCH A6 — verify by boot smoke in Plan 05).
    - `@ManyToOne(fetch=FetchType.LAZY) @OnDelete(DeletePolicy.CASCADE) @JoinColumn(name="CONVERSATION_ID") AiConversation conversation;` — carry-over from AiToolCallAudit, nullable (CHAT-only rows may have no conversation).
    - `@Column(name="USER_USERNAME", length=255) String userUsername;`
    - `@Column(name="RUN_ID") UUID runId;`
    - `@NotNull @Column(name="KIND", nullable=false, length=16) String kind;` — `length=16` widened from 8 per D-03; String NOT enum (D-03).
    - `@Column(name="EVENT_NAME", length=128) String eventName;` — nullable (CHAT root has null; replaces TOOL_NAME sentinel).
    - `@Lob @Column(name="ARGUMENTS_JSON") String argumentsJson;`
    - `@Lob @Column(name="RESULT_SUMMARY") String resultSummary;`
    - `@Lob @Column(name="QUERY_TEXT") String queryText;` — RETRIEVAL-only.
    - `@Column(name="TOP_K") Integer topK;` — RETRIEVAL-only.
    - `@Column(name="HIT_COUNT") Integer hitCount;` — RETRIEVAL-only.
    - `@Column(name="TOP_SCORE") Double topScore;` — RETRIEVAL-only.
    - `@Lob @Column(name="FILTERS_JSON") String filtersJson;` — RETRIEVAL-only.
    - `@Column(name="OUTCOME", length=16) String outcome;` — **nullable** (CHAT root null until finish — D-01). KEEP the String + enum getter/setter idiom from AiToolCallAudit.java:88-89:
      ```java
      public AiToolCallOutcome getOutcome() { return outcome == null ? null : AiToolCallOutcome.fromId(outcome); }
      public void setOutcome(AiToolCallOutcome v) { this.outcome = v == null ? null : v.getId(); }
      ```
      Add ALSO String overload getters/setters (`getOutcomeValue()`, `setOutcomeValue(String)`) OR expose a `setOutcome(String)` overload — pick whichever keeps the writer code (Plan 02) ergonomic; recommended: keep enum getter/setter named `getOutcome`/`setOutcome` and expose package-private `String getOutcomeRaw()` / `setOutcomeRaw(String)` used by `AuditWriter.writeChatFinish` to pass the String `"SUCCESS"` / `"ERROR"` literally.
    - `@Column(name="DENIAL_REASON", length=512) String denialReason;`
    - `@Column(name="ERROR_CLASS", length=255) String errorClass;`
    - `@Column(name="PROMPT_HASH", length=64) String promptHash;`
    - `@Column(name="LATENCY_MS") Long latencyMs;`
    - `@NotNull @Column(name="STARTED_AT", nullable=false) OffsetDateTime startedAt;`
    - `@Column(name="FINISHED_AT") OffsetDateTime finishedAt;`

    `@InstanceName` method (D-08 + Claude's-Discretion lock — use `-` for null eventName and `...` for null outcome, matching RESEARCH §Pattern 1 lines 259-264):
    ```java
    @InstanceName
    @DependsOnProperties({"kind", "eventName", "outcome"})
    public String getDisplayName() {
        String name = eventName == null ? "-" : eventName;
        String oc = outcome == null ? "..." : outcome;
        return kind + " / " + name + " / " + oc;
    }
    ```

    Explicit no-Lombok getters/setters for every field. Create entity via `Metadata.create(AiAuditEvent.class)` at call sites (Plan 02) — no constructor calls.

    **2b. Delete `ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallAudit.java`** — the file goes away entirely. Wave 2 plans close every remaining reference (AuditWriter, AuditAdvisor, ToolCallbackAuditDecorator, tests). This plan leaves the repo in a compilation-broken state intentionally — do NOT try to compile at this wave, but DO verify with `grep -rl "AiToolCallAudit" ai-agent/ai-agent/src/main/java` to produce a list of callers that Plan 02 must fix.

    **2c. Create `ai-agent/ai-agent/src/main/java/com/vn/agent/spi/AuditKind.java`** (Claude's-Discretion resolution: package `com.vn.agent.spi` per RESEARCH §Open Question 3 — hosts reference alongside AuditListener):
    ```java
    package com.vn.agent.spi;

    /**
     * SPI-06: kind tag carried by every AiAuditEvent row and passed to
     * {@link AuditListener#onEventAudited(java.util.UUID, String)}. Open-ended
     * String values — hosts may introduce additional kinds (e.g. "GUARDRAIL") after v1
     * without schema change. The underlying KIND column is varchar(16) to keep
     * the option open.
     */
    public final class AuditKind {
        public static final String CHAT = "CHAT";
        public static final String TOOL = "TOOL";
        public static final String RETRIEVAL = "RETRIEVAL";
        private AuditKind() { }
    }
    ```

    **2d. Rewrite `ai-agent/ai-agent/src/main/java/com/vn/agent/spi/AuditListener.java`** in place — replace the entire file body with the D-03 shape:
    ```java
    package com.vn.agent.spi;

    import java.util.UUID;

    public interface AuditListener {
        /**
         * Invoked after any audited event (CHAT / TOOL / RETRIEVAL / future kinds)
         * becomes durable (post-commit of its REQUIRES_NEW tx). Listeners MUST be
         * kind-tolerant — additional kinds may be added without SPI changes.
         *
         * @param auditId durable {@code AiAuditEvent.id}
         * @param kind    one of {@link AuditKind#CHAT}, {@link AuditKind#TOOL},
         *                {@link AuditKind#RETRIEVAL}, or a host-defined value
         */
        void onEventAudited(UUID auditId, String kind);
    }
    ```
    This is a BREAKING CHANGE (SPI-06 per D-03 — old `onToolCallAudited(UUID)` is REMOVED, not deprecated). Downstream callers will be fixed in Wave 2 (Plan 02 Task 3 — dispatcher + default bean) and Wave 5 (Plan 05 Task 3 — tests).

    Security mitigation (threat T-07.2-02): the breaking signature change is scoped to a solo codebase with a single in-tree default bean (`SpiDefaultsAutoConfiguration.defaultAuditListener`). Both grep (Plan 02) and compile (Wave 5) close every caller in the same phase commit train.
  </action>
  <verify>
    <automated>grep -c "@OneToMany(mappedBy = \"parent\"" ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiAuditEvent.java</automated>
  </verify>
  <done>
    `AiAuditEvent.java` exists and contains `@OneToMany(mappedBy = "parent"` exactly once. `AiAuditEvent.java` contains `@Composition` exactly once. `AiToolCallAudit.java` does NOT exist (`test -f … || echo gone`). `AuditKind.java` contains `public static final String CHAT = "CHAT"`. `AuditListener.java` contains `onEventAudited` and does NOT contain `onToolCallAudited`.
  </done>
</task>

<task type="auto">
  <name>Task 3: i18n updates — drop AiToolCallAudit keys + add AiAuditEvent keys in EN + VI</name>
  <read_first>
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties lines 40-80 + 235-280 (full entity + auditList blocks)
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties lines 40-80 + 235-280 (same blocks, VI)
    - .planning/phases/07.2-redesign-audit-schema-tree-lite/07.2-PATTERNS.md §messages_en.properties + messages_vi.properties
  </read_first>
  <action>
    Update BOTH `messages_en.properties` AND `messages_vi.properties` (CLAUDE.md rule — ALL locales MUST be kept in sync; single-locale edits are forbidden).

    **Drop (both locales):**
    - Every key prefixed `com.vn.agent.entity/AiToolCallAudit.` (current lines 42-59 per PATTERNS).
    - `auditList.column.tool` (line 249)
    - `auditList.column.phase` (line 250)
    - `auditList.detail.tool` (line 265)
    - `auditList.detail.phase` (line 266)

    **Add (both locales — entity-attribute block, replacing the dropped AiToolCallAudit block):**
    ```
    com.vn.agent.entity/AiAuditEvent=AI Audit Event
    com.vn.agent.entity/AiAuditEvent.id=Id
    com.vn.agent.entity/AiAuditEvent.version=Version
    com.vn.agent.entity/AiAuditEvent.parent=Parent event
    com.vn.agent.entity/AiAuditEvent.children=Children
    com.vn.agent.entity/AiAuditEvent.conversation=Conversation
    com.vn.agent.entity/AiAuditEvent.userUsername=User
    com.vn.agent.entity/AiAuditEvent.runId=Run id
    com.vn.agent.entity/AiAuditEvent.kind=Kind
    com.vn.agent.entity/AiAuditEvent.eventName=Event name
    com.vn.agent.entity/AiAuditEvent.argumentsJson=Arguments (JSON)
    com.vn.agent.entity/AiAuditEvent.resultSummary=Result summary
    com.vn.agent.entity/AiAuditEvent.queryText=Query text
    com.vn.agent.entity/AiAuditEvent.topK=Top K
    com.vn.agent.entity/AiAuditEvent.hitCount=Hit count
    com.vn.agent.entity/AiAuditEvent.topScore=Top score
    com.vn.agent.entity/AiAuditEvent.filtersJson=Filters (JSON)
    com.vn.agent.entity/AiAuditEvent.outcome=Outcome
    com.vn.agent.entity/AiAuditEvent.denialReason=Denial reason
    com.vn.agent.entity/AiAuditEvent.errorClass=Error class
    com.vn.agent.entity/AiAuditEvent.promptHash=Prompt hash
    com.vn.agent.entity/AiAuditEvent.latencyMs=Latency (ms)
    com.vn.agent.entity/AiAuditEvent.startedAt=Started at
    com.vn.agent.entity/AiAuditEvent.finishedAt=Finished at
    ```
    Translate EVERY label to Vietnamese for `messages_vi.properties` — mirror the existing VI translation style used for AiToolCallAudit at lines 42-59.

    **Add UI keys (both locales, in the `auditList.*` block that already exists):**
    ```
    auditList.column.kind=Kind
    auditList.column.eventName=Event name
    auditList.detail.kind=Kind
    auditList.detail.eventName=Event name
    auditList.detail.queryText=Query text
    auditList.detail.topK=Top K
    auditList.detail.hitCount=Hit count
    auditList.detail.topScore=Top score
    auditList.detail.filtersJson=Filters (JSON)
    ```
    Translate all labels for VI.

    **Keep unchanged (UI-scoped, not entity-coupled):** all other `auditList.*` keys, the view title `auditList.title`, the action-column keys.

    Per memory-note `feedback_jmix_messages_over_spring`, keep ALL keys in the root `messages_*.properties` bundle — do NOT move them to a per-view bundle (IntelliJ plugin stale-index issue).

    After editing, run a line-count diff: `wc -l messages_en.properties messages_vi.properties` — the two files MUST have the same line count (locale-parity invariant).
  </action>
  <verify>
    <automated>grep -c "com.vn.agent.entity/AiAuditEvent.eventName" ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_en.properties ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties</automated>
  </verify>
  <done>
    Both locale files contain `com.vn.agent.entity/AiAuditEvent.eventName`. Both files contain `com.vn.agent.entity/AiAuditEvent.queryText` + `com.vn.agent.entity/AiAuditEvent.topK` + `com.vn.agent.entity/AiAuditEvent.filtersJson`. Neither file contains `com.vn.agent.entity/AiToolCallAudit`. Neither file contains `auditList.column.phase`. Line counts match between EN and VI files.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB layer ↔ JPA | DDL cutover must be atomic — no in-flight audit rows between legacy drop and new create |
| add-on ↔ host host JPA scanner | Entity rename must not orphan references in `@JmixModule` metadata |
| SPI ↔ in-tree default bean | Breaking `AuditListener` signature change must not leave a compilation gap across the phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.2-01 | Tampering (data integrity) | Liquibase `dropTable` on already-initialised dev DB | mitigate | `<preConditions onFail="MARK_RAN">` gate + `cascadeConstraints="true"` tested on both Postgres + HSQLDB via `./gradlew :jmix-app:bootRun` smoke in Plan 05. Pre-v1 hard cutover documented in CONTEXT D-06. |
| T-07.2-02 | Denial (SPI compile break) | `AuditListener` breaking signature | mitigate | Solo-codebase scope confirmed (RESEARCH §Pitfall 5 lists all 6 callers); `SpiDefaultsAutoConfiguration` + `FoundationsBootSmokeTest` updated in the same phase commit (Plans 02, 05). |
| T-07.2-03 | Information Disclosure | `FILTERS_JSON` column captures retrieval filter expressions | mitigate | Column receives ONLY `Filter.Expression.toString()` output (no raw prompt text); column is `@Lob`, subject to same truncation discipline as `ARGUMENTS_JSON` (enforced in Plan 02 `AuditWriter.writeRetrieval` via the `cap()` helper pattern from `ToolCallbackAuditDecorator.java:40-54`). |
| T-07.2-04 | Information Disclosure | New `QUERY_TEXT` column captures user query verbatim | accept | D-09 explicitly audits user queries — this IS the feature. Access is gated by `AiAgentAdminRole` (V4 Access Control, unchanged from Phase 7 UI-10). |
</threat_model>

<verification>
- `./gradlew :ai-agent:ai-agent:compileJava` is EXPECTED TO FAIL at the end of Plan 01 (legacy `AiToolCallAudit` is deleted but Wave-2 callers have not been rewritten yet). This is intentional — Plan 02 closes the loop. Do NOT attempt to fix the compile errors in this plan.
- Liquibase XML validity: open `090-ai-audit-event.xml` in an IDE / Jmix's Liquibase tooling — element names must match the Liquibase 4.x XSD (`createTable`, `dropTable`, `addForeignKeyConstraint`, `createIndex`).
- i18n locale parity: `wc -l` output equal for EN + VI after edits.
</verification>

<success_criteria>
- `090-ai-audit-event.xml` present with 4 changesets (drop-gated + createTable + 2 FKs + 4 indexes).
- `AiAuditEvent.java` compiles against current classpath when considered in isolation (not Wave-2 callers).
- `AiToolCallAudit.java` does not exist.
- `AuditKind.java` present in `com.vn.agent.spi` with the three String constants.
- `AuditListener.java` contains only `onEventAudited(UUID, String)`; old method removed.
- EN + VI locale files updated with parity.
- Phase 7.2 requirements AUD-03 (columns/shape) + SPI-06 (breaking interface) are SATISFIED at the SPI/DDL/entity surface by the end of this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/07.2-redesign-audit-schema-tree-lite/07.2-01-SUMMARY.md` documenting: DDL changeset IDs, entity field list (with exact column-name mapping), `AuditKind` package decision (`com.vn.agent.spi`), `AuditListener` breaking change scope, list of callers still referencing the removed symbols (produced by `grep -rl "AiToolCallAudit\|onToolCallAudited" ai-agent/ai-agent/src`).
</output>
