---
phase: 07-flow-ui
plan: 04
type: execute
wave: 4
depends_on: [07-01, 07-02, 07-03]
files_modified:
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/conversation/ConversationListView.java
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/view/conversation/conversation-list-view.xml
  - ai-agent/ai-agent/src/main/java/com/vn/agent/view/conversation/ConversationDetailView.java
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/view/conversation/conversation-detail-view.xml
autonomous: true
requirements: [UI-03]
tags: [flowui, conversation, replay, role-aware]
must_haves:
  truths:
    - "Non-admin users see ConversationListView filtered to createdBy = currentUser"
    - "Admins (AiAgentAdminRole) see all conversations and get an extra User filter column"
    - "ConversationListView shows a Messages column (message count per conversation) per UI-SPEC — computed via JPQL subquery loader or an @JmixProperty on AiConversation so users can gauge conversation depth at a glance"
    - "Clicking a row opens ConversationDetailView with the conversation id as a route parameter"
    - "ConversationDetailView renders the full transcript read-only using the same MessageBubbleComponent + ToolCallCardComponent from 07-03"
    - "ConversationDetailView has a Continue in chat button that navigates to /ai-agent/chat?conversationId={uuid}"
    - "Both views are accessible to any authenticated Jmix user (AiAgentUserRole grants; admin variant via role check)"
  artifacts:
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/view/conversation/ConversationListView.java"
      provides: "@Route(\"ai-agent/conversations\") StandardListView<AiConversation> with role-aware filter + message-count column"
      contains: "StandardListView"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/view/conversation/ConversationDetailView.java"
      provides: "StandardDetailView replay using MessageBubbleComponent + ToolCallCardComponent"
      contains: "StandardDetailView"
    - path: "ai-agent/ai-agent/src/main/resources/com/vn/agent/view/conversation/conversation-list-view.xml"
      provides: "DataGrid with Title/Started/Messages columns, plus admin-visible User column"
      contains: "messageCount"
    - path: "ai-agent/ai-agent/src/main/resources/com/vn/agent/view/conversation/conversation-detail-view.xml"
      provides: "Read-only transcript layout with Continue in chat button"
      contains: "conversationDetail.action.continueInChat"
  key_links:
    - from: "ConversationListView.DataGrid.itemClickListener"
      to: "ConversationDetailView"
      via: "viewNavigators.detailView(...).editEntity(conversation).navigate()"
      pattern: "viewNavigators"
    - from: "ConversationDetailView.continueInChat"
      to: "ChatView"
      via: "UI.getCurrent().navigate(ChatView.class, QueryParameters.of(\"conversationId\", id.toString()))"
      pattern: "ChatView\\.class"
    - from: "ConversationListView.onBeforeShow"
      to: "AccessManager / CurrentAuthentication role check"
      via: "if currentUser has AiAgentAdminRole: drop createdBy filter + show User column"
      pattern: "AiAgentAdminRole\\.CODE|hasRole"
    - from: "ConversationListView.messageCount column"
      to: "AiMessage rows"
      via: "JPQL subquery `(select count(m) from AiAgent_AiMessage m where m.conversation = e)` in the collection loader, OR @JmixProperty on AiConversation with @DependsOnProperties — whichever compiles cleanly"
      pattern: "messageCount|count\\(m\\)"
---

<objective>
Wave 4 plan. Ships UI-03: ConversationListView + ConversationDetailView with role-aware scoping (D-24). Non-admin users see only their own conversations; admins see all plus a User filter column. Detail view reuses ChatView's bubble/tool-card components (D-23, D-25) in read-only mode and surfaces a Continue in chat button (D-26). Runs in Wave 4 because the detail view imports `MessageBubbleComponent` + `ToolCallCardComponent` from Plan 07-03 (Wave 3) — compilation order requires 07-03 to land first.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-flow-ui/07-CONTEXT.md
@.planning/phases/07-flow-ui/07-UI-SPEC.md
@.planning/phases/07-flow-ui/07-PATTERNS.md
@.planning/phases/07-flow-ui/07-03-PLAN.md
@ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiConversation.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiMessage.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiMessageRole.java
@ai-agent/ai-agent/src/main/java/com/vn/agent/security/AiAgentAdminRole.java

<interfaces>
From Phase 2 entities:
- `AiConversation`: fields `id` (UUID), `title` (String), `createdBy` (String — username), `createdDate` (OffsetDateTime), and an OrderBy for sorting. Instance name = `title`.
- `AiMessage`: fields `id`, `conversation` (ManyToOne AiConversation), `role` (AiMessageRole enum: USER / ASSISTANT / SYSTEM / TOOL), `content` (String), `toolCallsJson` (String, nullable), `createdDate`.

From Phase 4:
- Ownership is already enforced at DataManager layer via Jmix row-level policy (`AiAgentUserRowLevelRole`, see 02-08). For admin bypass, ConversationListView must explicitly apply admin filter logic server-side using Jmix-native role checks (not bypassing row-level security — admins already have broader row-level access per AiAgentAdminRole setup).

From Plan 07-03 (ALREADY LANDED in Wave 3):
- `MessageBubbleComponent` — reusable with `setMarkdown(String)` for replay (read-only).
- `ToolCallCardComponent` — reusable, constructor takes (toolCallId, toolName, argsJson, MessageSource) with setResult(summary, outcome).
- `CitationDialog` — reusable for showing referenced chunks.

Jmix APIs:
- `io.jmix.core.security.CurrentAuthentication.getUser().getUsername()` — current user id.
- `io.jmix.security.role.assignment.RoleAssignmentRepository` or similar; simplest admin check: `securityContextHelper` or `currentAuthentication.getAuthorities()` contains role code `AiAgentAdminRole.CODE` (= "ai-agent-admin").
- `ViewNavigators` for programmatic navigation between list and detail views.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ConversationListView (role-aware filter + admin-visible User column + messageCount column)</name>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiConversation.java (FULL — check whether a stored or @JmixProperty messageCount already exists; if so, use it directly)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiMessage.java (field shape for subquery)
    - .planning/phases/07-flow-ui/07-CONTEXT.md §D-24 (role-aware filter semantics)
    - .planning/phases/07-flow-ui/07-UI-SPEC.md §ConversationListView/DetailView (column keys — includes Messages count column)
    - D:/Study materials spring 2026/EXE101/ai/jmix-ai-backend/src/main/java/io/jmix/ai/backend/view/chatlog/ChatLogListView.java (pattern reference only)
    - Jmix skill `jmix-views` for StandardListView + dataGrid patterns
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/view/conversation/ConversationListView.java,
    ai-agent/ai-agent/src/main/resources/com/vn/agent/view/conversation/conversation-list-view.xml
  </files>
  <action>
    1. Create `conversation-list-view.xml`:

       ```xml
       <?xml version="1.0" encoding="UTF-8"?>
       <view xmlns="http://jmix.io/schema/flowui/view"
             title="msg://conversationList.title">
           <data>
               <collection id="conversationsDc" class="com.vn.agent.entity.AiConversation">
                   <fetchPlan extends="_base"/>
                   <loader id="conversationsDl" readOnly="true">
                       <query>
                           <![CDATA[
                             select e,
                                    (select count(m) from AiAgent_AiMessage m where m.conversation = e) as messageCount
                             from AiAgent_AiConversation e
                             order by e.createdDate desc
                           ]]>
                       </query>
                   </loader>
               </collection>
           </data>
           <facets>
               <dataLoadCoordinator auto="true"/>
           </facets>
           <layout padding="true" spacing="true">
               <hbox id="filterBar" spacing="true">
                   <textField id="titleFilter" placeholder="msg://conversationList.column.title"/>
                   <textField id="userFilter" visible="false" placeholder="msg://conversationList.column.user"/>
               </hbox>
               <dataGrid id="conversationsDataGrid" dataContainer="conversationsDc" width="100%" height="100%">
                   <columns>
                       <column property="title" header="msg://conversationList.column.title"/>
                       <column property="createdDate" header="msg://conversationList.column.createdDate"/>
                       <column key="messageCount" header="msg://conversationList.column.messageCount"/>
                       <column property="createdBy" header="msg://conversationList.column.user" visible="false"/>
                   </columns>
               </dataGrid>
               <div id="emptyStatePanel">
                   <h3 text="msg://conversationList.empty.heading"/>
                   <span text="msg://conversationList.empty.body"/>
               </div>
           </layout>
       </view>
       ```

       Rule 1 tolerance:
       - Exact JPQL entity names depend on `@JmixEntity(name="...")` — read AiConversation.java and AiMessage.java for the actual `name=` attribute values (likely `AiAgent_AiConversation` / `AiAgent_AiMessage`). Adapt.
       - **messageCount implementation — two acceptable paths (executor picks one):**
         (a) **JPQL subquery + projection loader** as shown above: loader returns a tuple and a DataGrid column renderer binds `messageCount` via a runtime `setRenderer(new TextRenderer<>(row -> loader.getTupleValue(row, "messageCount")))`. This is the shape Jmix natively supports when the loader is a `KeyValueCollectionLoader`. If AiConversation does NOT already expose messageCount, switch the loader to `<keyValueCollection>` with explicit property mappings.
         (b) **@JmixProperty on AiConversation** — add in Phase 2 entity:
           ```java
           @JmixProperty @DependsOnProperties("id")
           @Transient
           public Integer getMessageCount() { /* DataManager.loadValue count query */ }
           ```
           — but this runs per-row during grid rendering (N+1). Acceptable only for small result sets. Prefer (a).
       - The REQUIRED property: the Messages column MUST appear in the rendered grid with a real count per row — never omit, never placeholder. If the loader shape (a) is hard to wire in Jmix 2.8, fall back to a DataManager-powered custom ComponentRenderer that runs `dataManager.loadValue("select count(m) from AiAgent_AiMessage m where m.conversation.id = :cid", Long.class).parameter("cid", row.getId()).one()` — accept the N queries for v1; document the trade-off in SUMMARY.

    2. Create `ConversationListView.java`:
       - `@Route(value = "ai-agent/conversations", layout = DefaultMainViewParent.class) @ViewController("AiAgent_Conversation.list") @ViewDescriptor("conversation-list-view.xml") public class ConversationListView extends StandardListView<AiConversation> { ... }`
       - Fields:
         - `@ViewComponent private CollectionLoader<AiConversation> conversationsDl;`
         - `@ViewComponent private DataGrid<AiConversation> conversationsDataGrid;`
         - `@ViewComponent private TypedTextField<String> titleFilter;`
         - `@ViewComponent private TypedTextField<String> userFilter;`
         - `@Autowired private CurrentAuthentication currentAuthentication;`
         - `@Autowired private ViewNavigators viewNavigators;`
         - `@Autowired private DataManager dataManager;`  // for messageCount lookup if using path (b) fallback
         - `private boolean isAdmin;`
       - `@Subscribe public void onInit(InitEvent event)`:
         - `isAdmin = currentAuthentication.getAuthorities().stream().anyMatch(a -> a.getAuthority().equals(AiAgentAdminRole.CODE));` — Rule 1: Jmix 2.8 may expose role codes via `RoleCodes` or similar helper. Adapt to whichever works (prefer the simplest canonical Jmix helper; consult skill `jmix-security-roles`).
         - If `!isAdmin`: column `createdBy` stays `visible=false`; `userFilter.setVisible(false);`. Add JPQL parameter filter — preserve the messageCount subquery:
           ```java
           conversationsDl.setParameter("currentUser", currentAuthentication.getUser().getUsername());
           conversationsDl.setQuery(
               "select e, (select count(m) from AiAgent_AiMessage m where m.conversation = e) as messageCount " +
               "from AiAgent_AiConversation e where e.createdBy = :currentUser order by e.createdDate desc");
           ```
         - If `isAdmin`: `conversationsDataGrid.getColumnByKey("createdBy").setVisible(true); userFilter.setVisible(true);` — keep the base query unfiltered (row-level security already permits admin to see all via AiAgentAdminRole entity policies).
         - **messageCount column renderer** (if path (a) subquery tuple projection is used):
           ```java
           conversationsDataGrid.getColumnByKey("messageCount")
                   .setRenderer(new TextRenderer<>(row -> String.valueOf(
                           conversationsDl.getKeyValueContainer().getItem(row.getId()).getValue("messageCount"))));
           ```
           — adapt to the actual Jmix 2.8 tuple-access API. Fallback (path b): execute DataManager count in the renderer lambda.
         - Wire title filter: `titleFilter.addValueChangeListener(e -> applyFilter());` — apply both title and (when admin) user filter via JPQL parameter or DataManager Condition. Simplest: reload collection with Condition on each change.

       - `@Subscribe("conversationsDataGrid") public void onDoubleClick(ItemDoubleClickEvent<AiConversation> event)`:
         - `viewNavigators.detailView(this, AiConversation.class).editEntity(event.getItem()).navigate();`

       - Admin probe helper: extract to `private boolean currentUserIsAdmin() { ... }` so Plan 07-07 ConversationListRoleFilterTest can stub via reflection / Spring slice. Make the method package-private if Jmix prevents private override in a test subclass.

       - Javadoc: "UI-03 list view. D-24 role-aware scoping via isAdmin flag set at onInit. Messages column shows message count per conversation (JPQL subquery or DataManager projection). Row-level security still enforced by Jmix; this method adjusts only the displayed subset + extra User column for admins."
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:compileJava :ai-agent:ai-agent:processResources</automated>
  </verify>
  <done>
    - compileJava + processResources green.
    - `grep -c "AiAgent_Conversation.list" ai-agent/ai-agent/src/main/java/com/vn/agent/view/conversation/ConversationListView.java` prints `1` (matches menu.xml in 07-01).
    - `grep -c "AiAgentAdminRole\.CODE\|ai-agent-admin" ai-agent/ai-agent/src/main/java/com/vn/agent/view/conversation/ConversationListView.java` prints `1` or more.
    - `grep -c "currentAuthentication" ai-agent/ai-agent/src/main/java/com/vn/agent/view/conversation/ConversationListView.java` prints `2` or more (field + usage).
    - **messageCount column present:** `grep -cE "messageCount" ai-agent/ai-agent/src/main/resources/com/vn/agent/view/conversation/conversation-list-view.xml` prints `2` or more (column key + JPQL).
    - **messageCount header key seeded:** bundle contains `conversationList.column.messageCount` (verified in 07-01 bundle; if missing, executor ADDS it to both locale files and includes in the commit).
    - Every `text=` / `header=` / `placeholder=` / `title=` attribute in the XML uses `msg://`: `grep -oE '(text|header|placeholder|title)="[^m][^s][^g]' ai-agent/ai-agent/src/main/resources/com/vn/agent/view/conversation/conversation-list-view.xml` prints nothing.
  </done>
</task>

<task type="auto">
  <name>Task 2: ConversationDetailView (read-only replay + Continue in chat)</name>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiMessage.java
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/MessageBubbleComponent.java (created by 07-03)
    - ai-agent/ai-agent/src/main/java/com/vn/agent/view/chat/fragment/ToolCallCardComponent.java
    - .planning/phases/07-flow-ui/07-CONTEXT.md §D-23, D-25, D-26
    - .planning/phases/07-flow-ui/07-UI-SPEC.md §ConversationListView/DetailView
  </read_first>
  <files>
    ai-agent/ai-agent/src/main/java/com/vn/agent/view/conversation/ConversationDetailView.java,
    ai-agent/ai-agent/src/main/resources/com/vn/agent/view/conversation/conversation-detail-view.xml
  </files>
  <action>
    1. Create `conversation-detail-view.xml`:

       ```xml
       <?xml version="1.0" encoding="UTF-8"?>
       <view xmlns="http://jmix.io/schema/flowui/view">
           <data>
               <instance id="conversationDc" class="com.vn.agent.entity.AiConversation">
                   <fetchPlan extends="_base"/>
                   <loader id="conversationDl"/>
               </instance>
               <collection id="messagesDc" class="com.vn.agent.entity.AiMessage">
                   <fetchPlan extends="_base"/>
                   <loader id="messagesDl" readOnly="true">
                       <query>
                           <![CDATA[select m from AiAgent_AiMessage m where m.conversation = :conv order by m.createdDate asc]]>
                       </query>
                   </loader>
               </collection>
           </data>
           <facets>
               <dataLoadCoordinator auto="true"/>
           </facets>
           <layout padding="true" spacing="true">
               <hbox id="headerBar" width="100%" alignItems="CENTER" justifyContent="END">
                   <div id="readOnlyBanner">
                       <span text="msg://conversationDetail.banner.readOnly"/>
                   </div>
                   <button id="continueInChatButton" text="msg://conversationDetail.action.continueInChat"
                           themeNames="primary"/>
               </hbox>
               <scroller id="transcriptScroller" width="100%" height="100%">
                   <vbox id="transcriptList" padding="true" spacing="true" width="100%"/>
               </scroller>
           </layout>
       </view>
       ```

       Same Rule 1 caveat on entity JPQL name.

    2. Create `ConversationDetailView.java`:
       - `@Route(value = "ai-agent/conversations/:id", layout = DefaultMainViewParent.class) @ViewController("AiAgent_Conversation.detail") @ViewDescriptor("conversation-detail-view.xml") public class ConversationDetailView extends StandardDetailView<AiConversation> { ... }`
       - Fields:
         - `@ViewComponent private InstanceLoader<AiConversation> conversationDl;`
         - `@ViewComponent private CollectionLoader<AiMessage> messagesDl;`
         - `@ViewComponent private CollectionContainer<AiMessage> messagesDc;`
         - `@ViewComponent private VerticalLayout transcriptList;`
         - `@ViewComponent private JmixButton continueInChatButton;`
         - `@Autowired private MarkdownRenderer markdownRenderer;`
         - `@Autowired private MessageSource messages;`
         - `@Autowired private ObjectMapper objectMapper;` (for toolCallsJson parsing)

       - `@Subscribe public void onReady(ReadyEvent event)`:
         - `UUID convId = getEditedEntity().getId();`
         - `messagesDl.setParameter("conv", getEditedEntity()); messagesDl.load();`
         - `renderTranscript(messagesDc.getItems());`

       - `@Subscribe("continueInChatButton") public void onContinueInChat(ClickEvent<Button> event)`:
         - `UUID convId = getEditedEntity().getId();`
         - `UI.getCurrent().navigate("ai-agent/chat?conversationId=" + convId);`
         - Alternative typed API: `UI.getCurrent().navigate(ChatView.class, QueryParameters.of("conversationId", convId.toString()));` — use whichever compiles cleanly with the ChatView route declaration from 07-03.

       - `private void renderTranscript(Collection<AiMessage> messages)`:
         - `transcriptList.removeAll();`
         - For each message in chronological order:
           - Map `AiMessageRole` → `MessageBubbleComponent.Role` (USER, ASSISTANT, SYSTEM — map TOOL to a small badge or skip per D-08: tool-calls are separate cards).
           - If role is USER/ASSISTANT/SYSTEM: `MessageBubbleComponent bubble = new MessageBubbleComponent(mappedRole, markdownRenderer); bubble.setMarkdown(message.getContent()); transcriptList.add(bubble);`
           - If `message.getToolCallsJson()` is non-null and non-empty: parse the JSON array (shape documented by Phase 4 AuditWriter — executor reads ai-agent/ai-agent/src/main/java/com/vn/agent/audit/AuditWriter.java to confirm schema). For each entry: `new ToolCallCardComponent(tcId, toolName, argsJson, messages); card.setResult(resultSummary, outcome); transcriptList.add(card);`.
           - **Rule 1 tolerance:** if toolCallsJson schema is ambiguous, fall back to rendering a single `<Span>` with `msg("chatView.toolCard.collapsed", toolName, "")` placeholder and note in SUMMARY. The bubble display is the must-have; tool-card fidelity is nice-to-have.
  </action>
  <verify>
    <automated>./gradlew :ai-agent:ai-agent:compileJava :ai-agent:ai-agent:processResources</automated>
  </verify>
  <done>
    - compileJava + processResources green.
    - `grep -c "AiAgent_Conversation.detail" ai-agent/ai-agent/src/main/java/com/vn/agent/view/conversation/ConversationDetailView.java` prints `1`.
    - `grep -c "MessageBubbleComponent" ai-agent/ai-agent/src/main/java/com/vn/agent/view/conversation/ConversationDetailView.java` prints `2` or more.
    - `grep -c "conversationId\|ChatView" ai-agent/ai-agent/src/main/java/com/vn/agent/view/conversation/ConversationDetailView.java` prints `1` or more (Continue in chat wiring).
    - All `text=` attributes use `msg://`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DataManager query → UI | Row-level security is authoritative; admin-only User filter column must not bypass it |
| Route parameter `:id` → ConversationDetailView | User could type any UUID; ownership enforced by Jmix row-level policy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-12 | Elevation | ConversationListView isAdmin probe | mitigate | Jmix row-level security is the source of truth. isAdmin flag only decides UI affordances (extra column, extra filter). A non-admin who spoofs isAdmin=true still gets empty results because DataManager row-level filter remains applied. Plan 07-07 ConversationListRoleFilterTest verifies admin vs non-admin result counts. |
| T-07-13 | XSS | ConversationDetailView transcript render | mitigate | All AiMessage.content goes through MessageBubbleComponent.setMarkdown which uses MarkdownRenderer.toSafeHtml (OWASP sanitizer). Same protection as ChatView live path. |
</threat_model>

<verification>
- compileJava + processResources green
- All visible strings use msg:// keys (07-01 seeded the bundle; messageCount header key added here if missing)
- ConversationListView does NOT query AiConversation via EntityManager (Jmix convention)
- ConversationListView renders a messageCount column with real per-row counts
- ConversationDetailView reuses 07-03 components (no duplicated rendering)
</verification>

<success_criteria>
UI-03 delivered: user-scoped list + admin-aware list + per-conversation message count + transcript replay reusing live-chat components + Continue in chat. View ids match menu.xml entries from 07-01.
</success_criteria>

<output>
After completion, create `.planning/phases/07-flow-ui/07-04-SUMMARY.md` capturing:
- Exact Jmix 2.8 API used for role detection (CurrentAuthentication authorities vs a dedicated helper)
- Actual `@JmixEntity(name="...")` values discovered for AiConversation and AiMessage
- Whether messageCount was implemented via (a) JPQL subquery projection loader or (b) per-row DataManager count renderer — and the observed trade-off
- Whether the conversationList.column.messageCount key existed in 07-01 bundle or was added here
- Any toolCallsJson schema deviations noted from Phase 4
</output>
