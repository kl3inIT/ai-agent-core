---
phase: 04-orchestration-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/changelog/080-ai-tool-call-audit-runid.xml
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/changelog.xml
  - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallAudit.java
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties
  - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties
autonomous: true
requirements: [AUD-03]
must_haves:
  truths:
    - "AiToolCallAudit rows can be linked by a runId across pre/post and per-tool entries (D-12)."
    - "Existing Phase 2 changelog 030 is unchanged (additive migration only)."
  artifacts:
    - path: "ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/changelog/080-ai-tool-call-audit-runid.xml"
      provides: "Adds RUN_ID + KIND + PHASE + ERROR_CLASS + PROMPT_HASH columns"
      contains: "AI_AGENT_TOOL_CALL_AUDIT"
    - path: "ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallAudit.java"
      provides: "runId UUID + kind/phase/errorClass/promptHash fields with getters/setters"
      contains: "private UUID runId"
  key_links:
    - from: "changelog.xml"
      to: "080-ai-tool-call-audit-runid.xml"
      via: "<include> directive"
      pattern: "080-ai-tool-call-audit-runid"
---

<objective>
Add the `runId` (UUID) plus chat-vs-tool kind/phase discriminators required by D-12 to `AiToolCallAudit`.
Phase 2 shipped the entity without these columns; Phase 4's audit pipeline (D-10/D-12) cannot wire
without them. Pure additive migration — no destructive changes, no Phase 2 changelog edits.

Purpose: Unblock Plan 04-03 (`AuditWriter`, `AuditAdvisor`, `ToolCallbackAuditDecorator`) which writes
`runId`-correlated rows. Per PATTERNS.md callout this MUST be its own plan/task.
Output: New Liquibase changelog 080 + entity field additions + i18n keys + master include edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-orchestration-core/04-CONTEXT.md
@.planning/phases/04-orchestration-core/04-RESEARCH.md
@.planning/phases/04-orchestration-core/04-PATTERNS.md
@CLAUDE.md
@ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/changelog.xml
@ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/changelog/030-ai-tool-call-audit.xml
@ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallAudit.java
@ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties
@ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties

<interfaces>
From `030-ai-tool-call-audit.xml` (existing):
Columns: ID(uuid PK), VERSION(int), CONVERSATION_ID(uuid FK), USER_USERNAME(varchar 255),
TOOL_NAME(varchar 128 NN), ARGUMENTS_JSON(clob), RESULT_SUMMARY(clob), OUTCOME(varchar 16 NN),
DENIAL_REASON(varchar 512), LATENCY_MS(bigint), STARTED_AT(datetime NN), FINISHED_AT(datetime).
Indexes: IDX_AI_AGENT_TOOL_CALL_AUDIT__ON_CONVERSATION (CONVERSATION_ID), IDX_AI_AGENT_TOOL_CALL_AUDIT__ON_STARTED_AT (STARTED_AT).

Liquibase property tokens (declared in changelog.xml lines 18-23): `${uuid.type}` resolves to
`uuid` (postgres), `varchar(36)` (hsqldb), `uniqueidentifier` (mssql).

Entity already imports: `io.jmix.core.entity.annotation.JmixGeneratedValue`,
`io.jmix.core.metamodel.annotation.{InstanceName, JmixEntity, DependsOnProperties}`,
`jakarta.persistence.*`, `jakarta.validation.constraints.NotNull`,
`java.time.OffsetDateTime`, `java.util.UUID`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create Liquibase changelog 080 + register in master changelog</name>
  <files>ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/changelog/080-ai-tool-call-audit-runid.xml, ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/changelog.xml</files>
  <read_first>
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/changelog.xml
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/changelog/030-ai-tool-call-audit.xml
    - .planning/phases/04-orchestration-core/04-CONTEXT.md (D-12 column shape)
    - .planning/phases/04-orchestration-core/04-PATTERNS.md (AuditWriter section — runId mandate)
  </read_first>
  <action>
Create file `ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/changelog/080-ai-tool-call-audit-runid.xml`
with exact content (uses Phase 2 `${uuid.type}` token convention from changelog.xml lines 18-20):

```xml
<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!-- Phase 4 (D-12): runId threads pre/post chat rows AND every tool-call row of the same
         AskService.ask() invocation. KIND discriminates CHAT-level vs TOOL-level rows.
         PHASE discriminates PRE vs POST. PROMPT_HASH = SHA-256 hex of user message text.
         ERROR_CLASS = simple class name of any thrown Throwable. -->

    <changeSet id="1" author="ai-agent">
        <addColumn tableName="AI_AGENT_TOOL_CALL_AUDIT">
            <column name="RUN_ID" type="${uuid.type}"/>
            <column name="KIND" type="varchar(8)"/>
            <column name="PHASE" type="varchar(8)"/>
            <column name="PROMPT_HASH" type="varchar(64)"/>
            <column name="ERROR_CLASS" type="varchar(255)"/>
        </addColumn>
    </changeSet>

    <changeSet id="2" author="ai-agent">
        <createIndex indexName="IDX_AI_AGENT_TOOL_CALL_AUDIT__ON_RUN_ID"
                     tableName="AI_AGENT_TOOL_CALL_AUDIT">
            <column name="RUN_ID"/>
        </createIndex>
    </changeSet>

</databaseChangeLog>
```

Then edit `ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/changelog.xml`: append immediately
after the existing `<include file="/com/vn/agent/liquibase/changelog/070-ai-kb-vector-store.xml"/>` (line 31)
and BEFORE `</databaseChangeLog>` (line 33) the new include line:

```xml
    <include file="/com/vn/agent/liquibase/changelog/080-ai-tool-call-audit-runid.xml"/>
```

Do NOT modify any other line of `changelog.xml`. Do NOT modify `030-ai-tool-call-audit.xml`. Columns are
nullable so existing rows (none — table was empty pre-Phase 4) require no backfill. KIND will be enforced
NOT NULL at the entity layer via `@NotNull` (writer always sets it); enforcing NOT NULL at the DB level
would require a defaulted backfill we do not need.
  </action>
  <verify>
    <automated>grep -q "080-ai-tool-call-audit-runid" ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/changelog.xml &amp;&amp; grep -q "RUN_ID" ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/changelog/080-ai-tool-call-audit-runid.xml &amp;&amp; grep -q "PROMPT_HASH" ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/changelog/080-ai-tool-call-audit-runid.xml &amp;&amp; grep -q "IDX_AI_AGENT_TOOL_CALL_AUDIT__ON_RUN_ID" ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/changelog/080-ai-tool-call-audit-runid.xml</automated>
  </verify>
  <acceptance_criteria>
    - File `ai-agent/ai-agent/src/main/resources/com/vn/agent/liquibase/changelog/080-ai-tool-call-audit-runid.xml` exists.
    - File contains literal substrings: `RUN_ID`, `KIND`, `PHASE`, `PROMPT_HASH`, `ERROR_CLASS`, `${uuid.type}`, `IDX_AI_AGENT_TOOL_CALL_AUDIT__ON_RUN_ID`.
    - File contains exactly two `<changeSet` elements with `author="ai-agent"`.
    - `changelog.xml` contains literal `<include file="/com/vn/agent/liquibase/changelog/080-ai-tool-call-audit-runid.xml"/>` AFTER the 070 include line.
    - `changelog.xml` still contains all 7 original includes (010, 020, 030, 040, 050, 060, 070) — verify with `grep -c "include file=" changelog.xml` returns 8 (7 original + 1 new).
    - File 030-ai-tool-call-audit.xml is byte-identical to its prior state (no edits).
  </acceptance_criteria>
  <done>
    Changelog 080 created with five additive columns and a RUN_ID index; master changelog.xml includes it; original 030 changelog untouched.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add runId/kind/phase/errorClass/promptHash fields to AiToolCallAudit entity + i18n</name>
  <files>ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallAudit.java, ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties, ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties</files>
  <read_first>
    - ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallAudit.java
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties
    - ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties
    - .planning/phases/04-orchestration-core/04-CONTEXT.md (D-12)
    - CLAUDE.md (no Lombok on entities; getters/setters by hand)
  </read_first>
  <action>
Edit `ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallAudit.java`:

1. After existing `private OffsetDateTime finishedAt;` field block (around line 64), insert (preserving
   existing blank-line conventions):

```java
    @Column(name = "RUN_ID")
    private UUID runId;

    @Column(name = "KIND", length = 8)
    private String kind;

    @Column(name = "PHASE", length = 8)
    private String phase;

    @Column(name = "PROMPT_HASH", length = 64)
    private String promptHash;

    @Column(name = "ERROR_CLASS", length = 255)
    private String errorClass;
```

2. After the existing `setFinishedAt` setter (last setter in the class, around line 96), insert
   raw String getters/setters for all five new fields (mirror the existing flat getter/setter style;
   do NOT use the enum-coercion pattern that `getOutcome()/setOutcome(...)` uses — `kind` and `phase`
   are plain enum-name strings written by `AuditWriter` and read by tests, no enum class needed):

```java
    public UUID getRunId() { return runId; }
    public void setRunId(UUID runId) { this.runId = runId; }
    public String getKind() { return kind; }
    public void setKind(String kind) { this.kind = kind; }
    public String getPhase() { return phase; }
    public void setPhase(String phase) { this.phase = phase; }
    public String getPromptHash() { return promptHash; }
    public void setPromptHash(String promptHash) { this.promptHash = promptHash; }
    public String getErrorClass() { return errorClass; }
    public void setErrorClass(String errorClass) { this.errorClass = errorClass; }
```

3. Do NOT alter `getOutcome()/setOutcome(...)` or `getDisplayName()`. Do NOT add `@NotNull` to the
   new columns (DB columns are nullable per Task 1; runtime invariants enforced by `AuditWriter`).
4. Append to `ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties` (after line 52
   `com.vn.agent.entity/AiToolCallAudit.displayName=Name`) the five English labels:

```
com.vn.agent.entity/AiToolCallAudit.runId=Run ID
com.vn.agent.entity/AiToolCallAudit.kind=Kind
com.vn.agent.entity/AiToolCallAudit.phase=Phase
com.vn.agent.entity/AiToolCallAudit.promptHash=Prompt hash
com.vn.agent.entity/AiToolCallAudit.errorClass=Error class
```

5. Append to `ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties` (after line 52
   `com.vn.agent.entity/AiToolCallAudit.displayName=Tên`) the five Vietnamese labels:

```
com.vn.agent.entity/AiToolCallAudit.runId=Mã lượt chạy
com.vn.agent.entity/AiToolCallAudit.kind=Loại
com.vn.agent.entity/AiToolCallAudit.phase=Pha
com.vn.agent.entity/AiToolCallAudit.promptHash=Băm prompt
com.vn.agent.entity/AiToolCallAudit.errorClass=Lớp lỗi
```

After edits, run `./gradlew :ai-agent:ai-agent:compileJava` to confirm the entity still compiles. If
Jetbrains MCP is available, run `get_file_problems("ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallAudit.java", onlyErrors=false)` and resolve any problems beyond the pre-existing baseline.
  </action>
  <verify>
    <automated>grep -q "private UUID runId;" ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallAudit.java &amp;&amp; grep -q "public UUID getRunId" ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallAudit.java &amp;&amp; grep -q "private String promptHash;" ai-agent/ai-agent/src/main/java/com/vn/agent/entity/AiToolCallAudit.java &amp;&amp; grep -q "AiToolCallAudit.runId=Run ID" ai-agent/ai-agent/src/main/resources/com/vn/agent/messages.properties &amp;&amp; grep -q "AiToolCallAudit.runId=Mã lượt chạy" ai-agent/ai-agent/src/main/resources/com/vn/agent/messages_vi.properties &amp;&amp; ./gradlew :ai-agent:ai-agent:compileJava -q</automated>
  </verify>
  <acceptance_criteria>
    - `AiToolCallAudit.java` declares exactly five new private fields: `runId` (UUID), `kind` (String), `phase` (String), `promptHash` (String), `errorClass` (String).
    - `AiToolCallAudit.java` declares exactly ten new accessor methods (getter+setter for each of the five fields), preserving existing `getOutcome()/setOutcome(AiToolCallOutcome)` enum coercion methods unchanged.
    - `messages.properties` contains all five new keys with `=Run ID`, `=Kind`, `=Phase`, `=Prompt hash`, `=Error class` values.
    - `messages_vi.properties` contains all five new keys with the Vietnamese values shown above.
    - `./gradlew :ai-agent:ai-agent:compileJava -q` exits 0.
  </acceptance_criteria>
  <done>
    Entity gains five fields with hand-written accessors; both locale files document them; module compiles.
  </done>
</task>

</tasks>

<verification>
- Liquibase 080 changelog exists and is registered in master `changelog.xml` (no edits to 010-070).
- `AiToolCallAudit` entity gains the five fields needed by D-12 with String-typed `kind`/`phase` (no new enum class introduced — out of scope; `AuditWriter` writes literal `"CHAT"/"TOOL"` and `"PRE"/"POST"`).
- `./gradlew :ai-agent:ai-agent:compileJava` succeeds.
- After Plan 04-04 wires the host include, a fresh-DB `./gradlew :jmix-app:bootRun` will apply changelog 080 (manual verify deferred to Plan 04-05 boot smoke).
</verification>

<success_criteria>
- AUD-03 column shape (runId, promptHash, errorClass) materially present in DDL + entity (chat-pre/chat-post columns ready for 04-03 writer).
- No Phase 2 changelog edited.
- No regression in compileJava.
</success_criteria>

<output>
After completion, create `.planning/phases/04-orchestration-core/04-01-SUMMARY.md`.
</output>
