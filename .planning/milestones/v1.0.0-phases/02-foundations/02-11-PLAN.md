---
phase: 02-foundations
plan: 11
type: execute
wave: 6
depends_on: [01, 02, 03, 04, 05, 06, 07, 08, 09, 10]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
  - .planning/codebase/STACK.md
  - .planning/research/STACK.md
autonomous: true
requirements: []
tags: [docs, scope-management, d-10, d-02]
must_haves:
  truths:
    - "D-10 scope reductions (no AiExposureRule, no SPI-04, no UI-07, no TEST-06, no SPI-08 integration-test obligation) are reflected in all four source-of-truth docs"
    - "D-02 correction (Jmix add-on Liquibase requires explicit host <include>, not auto-discovery) is captured in docs so it does not regress"
    - "Spring AI version reference is 1.1.4 everywhere (D-10), including STACK.md, PROJECT.md Constraints, and PROJECT.md Requirements bullets"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Source-of-truth REQ list aligned with D-10"
      contains: "ENT-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 2 + Phase 3 rows aligned with D-10"
      contains: "Phase 2"
    - path: ".planning/PROJECT.md"
      provides: "Vision doc aligned with D-10 (no exposure-layer bullet, Spring AI 1.1.4)"
      contains: "1.1.4"
    - path: ".planning/codebase/STACK.md"
      provides: "Codebase stack doc aligned to Spring AI 1.1.4"
      contains: "1.1.4"
    - path: ".planning/research/STACK.md"
      provides: "Research stack doc aligned to Spring AI 1.1.4"
      contains: "1.1.4"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/02-foundations/02-CONTEXT.md"
      via: "D-10 scope-reduction trace"
      pattern: "D-10"
    - from: ".planning/PROJECT.md"
      to: ".planning/phases/02-foundations/02-CONTEXT.md"
      via: "D-10 scope-reduction trace"
      pattern: "D-10"
---

<objective>
Apply D-10's scope reductions and D-02's Liquibase-include correction across the four
authoritative planning documents so that downstream phases plan against the correct
surface area.

Purpose: Without this edit, REQUIREMENTS.md still claims 6 entities (including
`AiExposureRule`), 8 SPIs (including `EntityExposurePolicy` + "each SPI has integration
test with custom host impl"), a Phase 7 `ExposureRuleListView`, and a TEST-06 ArchUnit
obligation — none of which Phase 2 delivers nor Phase 3-8 should plan for. STACK.md
still references Spring AI 1.0.2 though the BOM is already at 1.1.4.

Output: Edits only — no new code files. Four planning docs touched plus two STACK.md
copies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-foundations/02-CONTEXT.md
@.planning/phases/02-foundations/02-RESEARCH.md

<interfaces>
D-10 from `.planning/phases/02-foundations/02-CONTEXT.md` (per prior reads):

- Drop `AiExposureRule` entity (and its related UI/SPI). ENT-01 becomes **5** entities.
- Drop SPI-04 (`EntityExposurePolicy`). SPI count becomes **7 declared, 6 with defaults** (AuditListener / ToolGuard / ToolContributor / ContextContributor / PromptContextContributor / CustomIngester + the kept "RoleContributor"-style). Use the exact SPI list from 02-RESEARCH.md §"SPI Signatures" (6 SPIs).
- Drop UI-07 (`ExposureRuleListView`).
- Drop TEST-06 (ArchUnit "no `.impl.` / `.internal.`" rule). Per MEMORY.md "Avoid ArchUnit until drift".
- Drop SPI-08 obligation ("each SPI has a default implementation + at least one integration test exercising a custom host impl") — replace with a lighter contract: each SPI has a default + plan 10's boot smoke test asserts defaults auto-wire.
- Reword TOOL-08 to NOT mention ArchUnit: read-only posture enforced by code review + unit-test convention instead.
- Update Spring AI version references from `1.0.2` → `1.1.4` in STACK.md and PROJECT.md.

D-02 correction (from 02-RESEARCH.md §"AIConfiguration extension" footnote and plan 04):
- Jmix does NOT auto-discover add-on Liquibase changelogs. The host's master
  `jmix-app/src/main/resources/com/vn/jmixapp/liquibase/changelog.xml` must
  explicitly `<include file="/com/vn/agent/liquibase/changelog.xml"/>`.
- Any doc language suggesting auto-discovery must be corrected.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply D-10 scope reductions to REQUIREMENTS.md and ROADMAP.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>

  <read_first>
    1. `.planning/REQUIREMENTS.md` (full file, 149 lines) — current source of truth
    2. `.planning/ROADMAP.md` (full file, 296 lines) — phase rows + traceability table
    3. `.planning/phases/02-foundations/02-CONTEXT.md` §D-10 — authoritative scope-reduction decision
    4. `.planning/phases/02-foundations/02-RESEARCH.md` §"SPI Signatures" — exact 6-SPI list to substitute
  </read_first>

  <action>
    **Edit REQUIREMENTS.md:**

    1. **ENT-01** — change "Six Jmix JPA entities: `AiConversation`, `AiMessage`, `AiToolCallAudit`, `AiParameters`, `AiKnowledgeDocument`, `AiExposureRule`" to:
       `ENT-01: Five Jmix JPA entities: AiConversation, AiMessage, AiToolCallAudit, AiParameters, AiKnowledgeDocument — UUID + @JmixGeneratedValue + @Version + @InstanceName, no Lombok. (AiExposureRule dropped per D-10 in Phase 02 CONTEXT; see MEMORY note "AI is just another Jmix client".)`

    2. **SPI section** — replace the entire `SPI-01..SPI-08` block with these 6 bullets (matching 02-RESEARCH.md §"SPI Signatures"):
       ```
       - [ ] **SPI-01**: `ToolContributor` — hosts register additional `@Tool`-annotated beans
       - [ ] **SPI-02**: `ContextContributor` — inject per-request context (user, tenant, env) into prompt
       - [ ] **SPI-03**: `PromptContextContributor` — augment system prompt with host-specific instructions
       - [ ] **SPI-05**: `ToolGuard` — veto tool calls
       - [ ] **SPI-06**: `AuditListener` — observe audit writes for side-channels
       - [ ] **SPI-07**: `CustomIngester` — plug in additional KB sources
       ```
       Note: SPI numbering preserved (no renumber) to keep cross-doc refs stable — just remove SPI-04 and SPI-08. Add a trailing line:
       `> Note: SPI-04 (EntityExposurePolicy) dropped per D-10. SPI-08 (per-SPI integration test with custom host impl) dropped per D-10; each SPI has a default and is smoke-tested via the Phase 02 boot test.`

    3. **TOOL-08** — change "Read-only posture enforced by ArchUnit: no `DataManager.save()` / `.remove()` in tool bodies" to:
       `TOOL-08: Read-only posture enforced by code review + unit tests asserting each tool class's public methods call only DataManager read-path operations (DataManager.load / DataManager.getCount / DataManager.loadValues). (ArchUnit deferred per MEMORY note "Avoid ArchUnit until drift".)`

    4. **UI-07** — delete the line entirely. After deletion, **renumber UI-08..UI-10 down by one** (UI-08→UI-07, UI-09→UI-08, UI-10→UI-09) OR keep the gap with an explanatory line. Prefer **keep the gap** and add a trailing note right after UI-06:
       `> Note: UI-07 (ExposureRuleListView) dropped per D-10 (AiExposureRule entity removed).`

    5. **TEST-06** — change the text to:
       `TEST-06: (removed per D-10 — ArchUnit rules deferred per MEMORY note "Avoid ArchUnit until drift"). Code review + the existing forbidden-import convention in CLAUDE.md remain authoritative until rule drift justifies ArchUnit.`

    6. **Traceability footer** — at the end of the file, before `*Last updated: ...*`, append:
       ```
       ## Scope Changes Log

       | Date | Decision | Change |
       |------|----------|--------|
       | 2026-04-18 | D-10 (Phase 02 CONTEXT) | ENT-01 6→5 entities (AiExposureRule dropped); SPI-04 + SPI-08 removed; UI-07 removed; TEST-06 converted to convention; TOOL-08 de-ArchUnit'd |
       ```

    7. Bump the `*Last updated:*` line at the very bottom to `2026-04-18 after Phase 02 planning — D-10 applied`.

    **Edit ROADMAP.md:**

    8. Phase 2 row in the summary table (line ~13) — change **Requirements** column to:
       `ENT-01..04, SEC-01..04, SPI-01, SPI-02, SPI-03, SPI-05, SPI-06, SPI-07 (interfaces only)`
       (Remove SPI-04; remove TEST-06.)

    9. Phase 2 detail section (line ~64) — replace the "**Requirements:**" line identically:
       `**Requirements:** ENT-01, ENT-02, ENT-03, ENT-04, SEC-01, SEC-02, SEC-03, SEC-04, SPI-01, SPI-02, SPI-03, SPI-05, SPI-06, SPI-07 (interfaces only, no impl)`

    10. Phase 2 Deliverables bullet list — replace the "Six JPA entities..." bullet with:
        `- Five JPA entities with UUID + @Version + @InstanceName, Liquibase changelogs (AI_AGENT_* prefix)`
        And replace "Seven SPI interfaces in functional module..." with:
        `- Six SPI interfaces in functional module (method signatures + Javadoc; no impl yet). SPI-04 EntityExposurePolicy dropped per D-10.`
        Remove the "ArchUnit rules in test source..." bullet entirely and replace with:
        `- Default no-op beans for all six SPIs (SpiDefaultsAutoConfiguration with @ConditionalOnMissingBean)`

    11. Phase 2 Success criteria — replace item 1 "`./gradlew test` passes (ArchUnit rules clean)" with:
        `1. ./gradlew test passes — foundations boot smoke test (5 assertions) green`
        Update item 4 "All seven SPIs compile..." → "All six SPIs compile; each has one no-op default bean registered".

    12. **Finalize the Phase 2 Plans list** at the bottom of the Phase 2 section (replacing any placeholder). Insert/replace after the Success criteria block:
        ```
        **Plans:** 11 plans

        Plans:
        - [ ] 02-01-PLAN.md — Entity enums (AiMessageRole, AiKnowledgeDocumentStatus, AiToolCallOutcome) + enum i18n
        - [ ] 02-02-PLAN.md — Six SPI interfaces + ToolVetoedException in com.vn.agent.spi
        - [ ] 02-03-PLAN.md — Five JPA entities (AiConversation, AiMessage, AiToolCallAudit, AiParameters, AiKnowledgeDocument) + entity/attribute i18n
        - [ ] 02-04-PLAN.md — Add-on master changelog + five step changelogs (010-050) + host <include> edit
        - [ ] 02-05-PLAN.md — SPRING_AI_CHAT_MEMORY changeset (Postgres + HSQLDB variants) + application.properties initialize-schema:never
        - [ ] 02-06-PLAN.md — pgvector extension + AI_AGENT_KB_VECTOR_STORE table + HNSW index (Postgres-only with preCondition gating)
        - [ ] 02-07-PLAN.md — SpiDefaultsAutoConfiguration with six @ConditionalOnMissingBean no-op beans
        - [ ] 02-08-PLAN.md — AiAgentUserRole, AiAgentAdminRole, AiAgentUserRowLevelRole + role i18n
        - [ ] 02-09-PLAN.md — AIConfiguration @JmixModule dependsOn widened to include DataConfiguration + SecurityConfiguration
        - [ ] 02-10-PLAN.md — FoundationsBootSmokeTest (5 @Test methods: Liquibase, entities, row-level, SPI defaults, roles)
        - [ ] 02-11-PLAN.md — D-10 doc updates (REQUIREMENTS, ROADMAP, PROJECT, STACK) + D-02 Liquibase-include correction
        ```

    13. Phase 3 row/detail — remove `SPI-04 (impl)` from the Requirements column/line (it's gone). Keep TOOL-08 as a Phase 3 requirement but add footnote text after the TOOL-08 reference:
        `TOOL-08 enforced via code review + unit tests, not ArchUnit (D-10).`

    14. Phase 7 row/detail — remove `UI-07` from the UI-01..10 range. State the phase's UI requirement list explicitly: `UI-01, UI-02, UI-03, UI-04, UI-05, UI-06, UI-08, UI-09, UI-10` (skip UI-07). Remove the `ExposureRuleListView` deliverable bullet. Adjust success criteria count if it referenced exposure.

    15. Phase 8 row — drop `TEST-06 (completion)` from the Requirements column.

    16. **Requirements Traceability table** at the bottom — update:
        - `SPI-01..08 | 2 (contracts) + 3–6 (impls)` → `SPI-01..03, SPI-05..07 | 2 (contracts) + 3–6 (impls)`
        - Leave TEST-02..07 grouping alone but add a footnote under the table: `TEST-06 removed per D-10.`

    17. Bump `*Last updated:*` at the very bottom to `2026-04-18 after Phase 02 planning — D-10 applied`.

    18. Update the header summary: change `**Phases:** 8` (keep) but `Total v1 requirements mapped: 73 of 73 ✓` → compute the new total (73 − 1 entity req row never existed; but minus SPI-04, SPI-08, UI-07, TEST-06 = 4; new total = `69 of 69`). Replace with `**Total v1 requirements mapped:** 69 of 69 ✓ (was 73 pre-D-10; AiExposureRule/SPI-04/SPI-08/UI-07/TEST-06 dropped)`.

    Do NOT touch PROJECT.md or STACK.md in this task — Task 2 handles them.
  </action>

  <verify>
    <automated>cd D:/DTH/ai-agent-core && grep -c "AiExposureRule" .planning/REQUIREMENTS.md && grep -c "AiExposureRule" .planning/ROADMAP.md && grep -c "SPI-04" .planning/REQUIREMENTS.md && grep -c "SPI-08" .planning/REQUIREMENTS.md && grep -cE "^- \[ \] \*\*UI-07" .planning/REQUIREMENTS.md && grep -c "ArchUnit" .planning/REQUIREMENTS.md && grep -c "11 plans" .planning/ROADMAP.md && grep -c "02-11-PLAN.md" .planning/ROADMAP.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "AiExposureRule" .planning/REQUIREMENTS.md` returns `0`
    - `grep -c "AiExposureRule" .planning/ROADMAP.md` returns `0`
    - `grep -cE "^- \[ \] \*\*SPI-04" .planning/REQUIREMENTS.md` returns `0` (SPI-04 bullet removed)
    - `grep -cE "^- \[ \] \*\*SPI-08" .planning/REQUIREMENTS.md` returns `0` (SPI-08 bullet removed)
    - `grep -cE "^- \[ \] \*\*UI-07" .planning/REQUIREMENTS.md` returns `0`
    - `grep -cE "^- \[ \] \*\*TEST-06" .planning/REQUIREMENTS.md` returns `1` (line kept but content changed to "removed")
    - `grep -c "ArchUnit" .planning/REQUIREMENTS.md` returns `<= 2` (only in "deferred per MEMORY" notes, not as enforcement obligation)
    - `grep -c "11 plans" .planning/ROADMAP.md` returns `>= 1`
    - `grep -c "02-11-PLAN.md" .planning/ROADMAP.md` returns `>= 1`
    - `grep -c "02-01-PLAN.md" .planning/ROADMAP.md` returns `>= 1` (all 11 plan filenames appear)
    - `grep -cE "ENT-01.*five|Five Jmix JPA entities" .planning/REQUIREMENTS.md` returns `>= 1`
    - `grep "Scope Changes Log" .planning/REQUIREMENTS.md` returns 1 line
    - `grep -c "D-10" .planning/ROADMAP.md` returns `>= 1`
  </acceptance_criteria>

  <done>
    REQUIREMENTS.md and ROADMAP.md reflect D-10: 5 entities, 6 SPIs, no UI-07, no TEST-06 enforcement, TOOL-08 de-ArchUnit'd. ROADMAP has finalized Phase 2 plan list (11 plans, 02-01..02-11).
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply D-10 to PROJECT.md and Spring AI 1.1.4 to both STACK.md files</name>
  <files>.planning/PROJECT.md, .planning/codebase/STACK.md, .planning/research/STACK.md</files>

  <read_first>
    1. `.planning/PROJECT.md` (full file, 155 lines) — already in context above
    2. `.planning/codebase/STACK.md` — Spring AI version reference
    3. `.planning/research/STACK.md` — Spring AI version reference
    4. `.planning/phases/02-foundations/02-CONTEXT.md` §D-10 — authoritative
    5. `.planning/phases/02-foundations/02-RESEARCH.md` §"Spring AI version delta (1.0.2 → 1.1.4)" (lines ~934-949) — verified delta
  </read_first>

  <action>
    **Edit PROJECT.md:**

    1. Line 26 (Requirements → Active → Add-on packaging & integration): change `Spring Boot auto-configuration wires Spring AI 1.0.2 primitives...` to `Spring Boot auto-configuration wires Spring AI 1.1.4 primitives (ChatClient, advisors, VectorStore, ChatMemory)`.

    2. Remove the bullet `- [ ] Additional AI exposure layer (allowlist/denylist) on top of Jmix security controls what the model can see in planning` from the "Metadata-first runtime" section. Replace it with (per D-10 + MEMORY "AI is just another Jmix client"):
       `- [ ] Authorization flows exclusively through Jmix AccessManager + DataManager — no parallel AI-only exposure layer shipped in v1 (per D-10; revisit if a concrete consumer need surfaces)`

    3. "Spring AI 1.0.2 integration" section heading (line 35) → rename to `Spring AI 1.1.4 integration`. The sub-bullet body references "1.0.2" — update to `1.1.4`.

    4. Line 37 (the `StructuredOutputValidationAdvisor` bullet) — add a trailing parenthetical: `(existence in 1.1.4 to be verified in Phase 6 per research flag)`.

    5. SPI section (lines 58-64) — remove the `- [ ] Entity exposure policy — programmatic control of which MetaClass/MetaProperty the agent sees` bullet (dropped with SPI-04). Final SPI count in this section = 5 (Tool / Context / Prompt / Guard / Audit) + CustomIngester (note it's listed elsewhere as RAG-07). Do NOT renumber anything.

    6. Constraints section (line 108) — change `Spring AI version: 1.0.2 — required per product spec; milestone release means extra docs verification` to `Spring AI version: 1.1.4 — pinned via BOM (upgraded from 1.0.2 between Phase 1 wave start and Phase 2 start; STACK.md updated accordingly)`.

    7. Deferred Decisions section (line 133) — add a second entry:
       ```
       - **Ship `AiExposureRule` entity + `EntityExposurePolicy` SPI + `ExposureRuleListView`** — was part of v1 scope. **Deferred** per [D-10 in Phase 2 CONTEXT](phases/02-foundations/02-CONTEXT.md). Trigger: a concrete consumer case where Jmix AccessManager + DataManager row-/attribute-level policies are insufficient to constrain what the agent sees. Until then, authorization is the host's existing Jmix security stack (per MEMORY note "AI is just another Jmix client").
       ```

    8. Add a third entry to Deferred Decisions:
       ```
       - **ArchUnit enforcement of layering / `.impl.` imports / `no DataManager.save in @Tool`** (TEST-06, parts of TOOL-08) — was part of v1 scope. **Deferred** per D-10 per MEMORY note "Avoid ArchUnit until drift". Code review + targeted unit-test conventions remain authoritative until rule drift justifies ArchUnit.
       ```

    9. Update the "Add-on packaging" section's module-split bullet (line 24) — leave as is (already covered by D-01).

    10. Bump `*Last updated: ...*` at the bottom to `2026-04-18 after Phase 02 planning — D-10 applied`.

    **Edit .planning/codebase/STACK.md:**

    11. Find every literal `1.0.2` and replace with `1.1.4`. If the doc has a table row for Spring AI, update the value column. Add a trailing footnote (once):
        `> Upgraded 1.0.2 → 1.1.4 between Phase 1 wave start and Phase 2 start. BOM pinned in ai-agent/build.gradle. Table deltas verified in .planning/phases/02-foundations/02-RESEARCH.md §"Spring AI version delta".`

    **Edit .planning/research/STACK.md:**

    12. Same treatment — every `1.0.2` → `1.1.4`, plus the same trailing footnote.

    **D-02 correction (in whichever of the three docs mentions Liquibase auto-discovery):**

    13. Grep the four docs for "auto-discover" / "auto-include" / "changelog.*auto" language. Where any doc claims Jmix auto-discovers add-on changelogs, add a correction sentence:
        `Correction (D-02 in 02-CONTEXT, validated in 02-RESEARCH §"AIConfiguration extension"): Jmix does NOT auto-discover add-on Liquibase changelogs. The host application's master changelog.xml must explicitly <include file="/com/vn/agent/liquibase/changelog.xml"/>. Plan 02-04 documents the exact host-side edit.`
        If no doc mentions auto-discovery, SKIP — do not fabricate.
  </action>

  <verify>
    <automated>cd D:/DTH/ai-agent-core && grep -c "1.0.2" .planning/PROJECT.md && grep -c "1.1.4" .planning/PROJECT.md && grep -c "AiExposureRule\|Entity exposure policy" .planning/PROJECT.md && grep -c "1.0.2" .planning/codebase/STACK.md && grep -c "1.0.2" .planning/research/STACK.md && grep -c "1.1.4" .planning/codebase/STACK.md && grep -c "1.1.4" .planning/research/STACK.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "1.0.2" .planning/PROJECT.md` returns `0`
    - `grep -c "1.1.4" .planning/PROJECT.md` returns `>= 3` (appears in Active, integration heading, Constraints)
    - `grep -c "AiExposureRule" .planning/PROJECT.md` returns `>= 1` (only in the new Deferred Decisions bullet, not in active requirements)
    - `grep -cE "^- \[ \] Additional AI exposure layer" .planning/PROJECT.md` returns `0` (original bullet removed)
    - `grep -cE "^- \[ \] Entity exposure policy" .planning/PROJECT.md` returns `0` (SPI bullet removed)
    - `grep -c "1.0.2" .planning/codebase/STACK.md` returns `0`
    - `grep -c "1.0.2" .planning/research/STACK.md` returns `0`
    - `grep -c "1.1.4" .planning/codebase/STACK.md` returns `>= 1`
    - `grep -c "1.1.4" .planning/research/STACK.md` returns `>= 1`
    - `grep -c "Deferred per D-10\|per D-10\|D-10 in Phase 2 CONTEXT" .planning/PROJECT.md` returns `>= 2`
  </acceptance_criteria>

  <done>
    PROJECT.md reflects D-10 scope reduction (exposure layer deferred, SPI count aligned) and Spring AI 1.1.4 across Active/Constraints. Both STACK.md files reference Spring AI 1.1.4 with a delta footnote. D-02 Liquibase correction landed wherever relevant.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning docs → future planners | These docs are the source of truth downstream phases plan against. Stale info here propagates into wrong plans. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11-01 | Tampering | Traceability table in ROADMAP.md | mitigate | New "Scope Changes Log" in REQUIREMENTS.md + explicit D-10 footnote in ROADMAP Phase 2/3/7/8 rows gives auditable trail. |
| T-02-11-02 | Repudiation | A future planner claims "TOOL-08 said ArchUnit" | mitigate | TOOL-08 rewritten in place with the replacement convention + MEMORY citation; no ambiguity. |
| T-02-11-03 | Information Disclosure | Leaving `AiExposureRule` references in random places implies a security layer that does not exist | mitigate | grep assertions in acceptance criteria ensure the string is absent everywhere except the Deferred Decisions log. |
| T-02-11-04 | Denial of Service | Typo in plan filenames in ROADMAP Plans list causes orchestrator to skip a plan | mitigate | Task 1 acceptance criteria verify every `02-NN-PLAN.md` filename appears by grep — any typo fails validation. |
| T-02-11-05 | Elevation of Privilege | ArchUnit-less TOOL-08 creates a gap where a future contributor adds `DataManager.save()` in a tool body | accept | Documented as explicit MEMORY-driven trade-off ("Avoid ArchUnit until drift"). Code review + convention stand in; re-add ArchUnit when drift appears. |
</threat_model>

<verification>
- Both tasks' greps pass
- Manual read-through confirms: (a) 5 entities stated consistently, (b) 6 SPIs stated consistently, (c) no orphan references to ExposureRule/ExposurePolicy outside Deferred Decisions, (d) Spring AI 1.1.4 throughout, (e) ROADMAP Phase 2 Plans list includes 02-01 through 02-11 exactly
</verification>

<success_criteria>
- REQUIREMENTS.md, ROADMAP.md, PROJECT.md, .planning/codebase/STACK.md, .planning/research/STACK.md all aligned with D-10 and the Spring AI 1.1.4 pin
- Phase 2 Plans list in ROADMAP is finalized with 11 entries
- Scope Changes Log in REQUIREMENTS.md documents the D-10 reductions
- Deferred Decisions in PROJECT.md captures the exposure-layer and ArchUnit deferrals with trigger conditions
</success_criteria>

<output>
After completion, create `.planning/phases/02-foundations/02-11-SUMMARY.md` recording:
- Diffs applied to each of the five doc files
- Confirmation that all acceptance-criteria greps passed
- Note whether any STACK.md mentioned auto-discovery (D-02 correction applied or skipped)
</output>
